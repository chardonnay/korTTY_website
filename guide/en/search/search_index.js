var __index = {"config": {"indexing": "full","lang": ["en"],"min_search_length": 3,"prebuild_index": false,"separator": "[\\s\\-]+"},"docs": [{"location": "index.html","text": "korTTY Guide The complete reference for korTTY — a modern, cross-platform JavaFX SSH client with tabbed terminals, SFTP tooling, a background job scheduler, snippet management, terminal-effect plugins, integrated local GGUF inference and OpenAI-compatible AI assistance. Every feature, setting and tool, explained in detail. Press F1 inside korTTY to open this guide Getting started Install korTTY, set your master password, and learn the main window. → Start here Connections & terminal SSH/Mosh connections, tabs, split-screen, tunnels, jump servers and SFTP. → Connections Settings reference Every setting across all configuration tabs, with types, defaults and where each value is stored. → Settings Menus & shortcuts The full menu tree and every keyboard shortcut. → Menu reference AI assistant & agent tools Profiles, skills, the AI Manager, and the terminal AI agent's tools. → AI assistant Local models Install, import, route, and run private GGUF models with integrated llama.cpp. → Local models RAG knowledge stores Review files or folders, build a safe local HNSW index, and add cited context to AI profiles. → Knowledge stores What's new Release notes for the current and previous versions. → Release notes About this guide This guide is bundled inside korTTY ( Help → Manual , shortcut F1 ) and published online. It is available in English and German , and is kept in sync with the code automatically. The korTTY version this guide was built for is shown in the footer.","title": "Home"},{"location": "index.html#kortty-guide","text": "The complete reference for korTTY — a modern, cross-platform JavaFX SSH client with tabbed terminals, SFTP tooling, a background job scheduler, snippet management, terminal-effect plugins, integrated local GGUF inference and OpenAI-compatible AI assistance. Every feature, setting and tool, explained in detail. Press F1 inside korTTY to open this guide Getting started Install korTTY, set your master password, and learn the main window. → Start here Connections & terminal SSH/Mosh connections, tabs, split-screen, tunnels, jump servers and SFTP. → Connections Settings reference Every setting across all configuration tabs, with types, defaults and where each value is stored. → Settings Menus & shortcuts The full menu tree and every keyboard shortcut. → Menu reference AI assistant & agent tools Profiles, skills, the AI Manager, and the terminal AI agent's tools. → AI assistant Local models Install, import, route, and run private GGUF models with integrated llama.cpp. → Local models RAG knowledge stores Review files or folders, build a safe local HNSW index, and add cited context to AI profiles. → Knowledge stores What's new Release notes for the current and previous versions. → Release notes About this guide This guide is bundled inside korTTY ( Help → Manual , shortcut F1 ) and published online. It is available in English and German , and is kept in sync with the code automatically. The korTTY version this guide was built for is shown in the footer.","title": "korTTY Guide"},{"location": "about/anonymous-data.html","text": "Anonymous data for application optimization korTTY can collect anonymous usage statistics to help decide which features are worth improving and to find crashes and frequent errors. This is entirely optional and can be turned on or off at any time. On a first launch the checkbox in the master-password setup comes pre-selected , so confirming that dialog as-is starts collection — clear it there, or switch it off later, and nothing is collected. Overview Your decision, once. Nothing is collected before you confirm. You are asked once, together with the master-password setup on first launch, where the checkbox is pre-selected; existing installations are asked once after unlocking, with a dialog that counts every dismissal as \"no\". Anonymous. No account, no login, and no persistent device identifier is transmitted. Revocable. You can change your decision at any time under Settings → Privacy . Turning it off stops collection immediately and discards any data that has not yet been sent. What is collected Data Example Event names app start, feature used (e.g. a tool opened, a backup created) Aggregate counts and flags number of open terminal tabs, whether AI is enabled App version 2.5.1 Operating system and version macOS 15, Windows 11, Linux App language de, en An anonymous session ID a random number regenerated on every launch and after one hour of inactivity The session ID is not a persistent identifier: it is newly generated each time and cannot be traced back to you across launches. What is never collected korTTY never transmits any of the following: Hostnames, IP addresses, usernames, or connection names and addresses File names, paths, or directory contents Snippet content, terminal output, or AI prompt and chat text Passwords, SSH keys, GPG keys, or API keys Error messages (only the type of error and the korTTY class where it occurred) Where the data goes Usage statistics are processed by Aptabase , an open-source, privacy-first analytics service. korTTY uses Aptabase's EU region ( eu.aptabase.com ), so the data is processed on servers located in the European Union in compliance with the GDPR . See the Aptabase privacy policy for details. If no connection is available, events are cached locally in ~/.kortty and sent later — including after a restart — so a temporary lack of connectivity does not lose or block anything. This offline cache holds only the same anonymous events; it is discarded if you opt out, and events older than three days are dropped. Why korTTY collects it The goal is to make korTTY better with real, anonymous evidence instead of guesswork: Prioritize features that people actually use, and retire ones that nobody does. Find crashes and frequent errors so they can be fixed in the next release. Measure whether releases improve stability over time. Your choices First launch: the setup dialog for the master password includes the pre-selected checkbox and this information; clearing it before you click Setup declines. Any time: open Settings → Privacy to enable or disable collection. The same page links back to this chapter. Turning it off stops all collection immediately and discards data that has not yet been sent. Your consent record Your decision and the date it was made are stored locally in ~/.kortty/global-settings.xml (see Configuration files ) as a record of consent. If a future korTTY version changes what is collected, you will be asked again so your choice always reflects the current scope.","title": "Anonymous data for application optimization"},{"location": "about/anonymous-data.html#anonymous-data-for-application-optimization","text": "korTTY can collect anonymous usage statistics to help decide which features are worth improving and to find crashes and frequent errors. This is entirely optional and can be turned on or off at any time. On a first launch the checkbox in the master-password setup comes pre-selected , so confirming that dialog as-is starts collection — clear it there, or switch it off later, and nothing is collected.","title": "Anonymous data for application optimization"},{"location": "about/anonymous-data.html#overview","text": "Your decision, once. Nothing is collected before you confirm. You are asked once, together with the master-password setup on first launch, where the checkbox is pre-selected; existing installations are asked once after unlocking, with a dialog that counts every dismissal as \"no\". Anonymous. No account, no login, and no persistent device identifier is transmitted. Revocable. You can change your decision at any time under Settings → Privacy . Turning it off stops collection immediately and discards any data that has not yet been sent.","title": "Overview"},{"location": "about/anonymous-data.html#what-is-collected","text": "Data Example Event names app start, feature used (e.g. a tool opened, a backup created) Aggregate counts and flags number of open terminal tabs, whether AI is enabled App version 2.5.1 Operating system and version macOS 15, Windows 11, Linux App language de, en An anonymous session ID a random number regenerated on every launch and after one hour of inactivity The session ID is not a persistent identifier: it is newly generated each time and cannot be traced back to you across launches.","title": "What is collected"},{"location": "about/anonymous-data.html#what-is-never-collected","text": "korTTY never transmits any of the following: Hostnames, IP addresses, usernames, or connection names and addresses File names, paths, or directory contents Snippet content, terminal output, or AI prompt and chat text Passwords, SSH keys, GPG keys, or API keys Error messages (only the type of error and the korTTY class where it occurred)","title": "What is never collected"},{"location": "about/anonymous-data.html#where-the-data-goes","text": "Usage statistics are processed by Aptabase , an open-source, privacy-first analytics service. korTTY uses Aptabase's EU region ( eu.aptabase.com ), so the data is processed on servers located in the European Union in compliance with the GDPR . See the Aptabase privacy policy for details. If no connection is available, events are cached locally in ~/.kortty and sent later — including after a restart — so a temporary lack of connectivity does not lose or block anything. This offline cache holds only the same anonymous events; it is discarded if you opt out, and events older than three days are dropped.","title": "Where the data goes"},{"location": "about/anonymous-data.html#why-kortty-collects-it","text": "The goal is to make korTTY better with real, anonymous evidence instead of guesswork: Prioritize features that people actually use, and retire ones that nobody does. Find crashes and frequent errors so they can be fixed in the next release. Measure whether releases improve stability over time.","title": "Why korTTY collects it"},{"location": "about/anonymous-data.html#your-choices","text": "First launch: the setup dialog for the master password includes the pre-selected checkbox and this information; clearing it before you click Setup declines. Any time: open Settings → Privacy to enable or disable collection. The same page links back to this chapter. Turning it off stops all collection immediately and discards data that has not yet been sent.","title": "Your choices"},{"location": "about/anonymous-data.html#your-consent-record","text": "Your decision and the date it was made are stored locally in ~/.kortty/global-settings.xml (see Configuration files ) as a record of consent. If a future korTTY version changes what is collected, you will be asked again so your choice always reflects the current scope.","title": "Your consent record"},{"location": "about/feedback.html","text": "Feedback & support korTTY is developed openly on GitHub: https://github.com/chardonnay/korTTY . Bug reports and feature ideas are both handled as GitHub issues. You need a free GitHub account to open an issue. Report a bug Search for existing reports first at https://github.com/chardonnay/korTTY/issues (include closed issues) — your problem may already be known or even fixed in a newer release. Collect the key facts: The korTTY version — shown in Help > About and in the footer of this manual. Your operating system and version (e.g. macOS 15, Windows 11, Ubuntu 24.04). Exact steps to reproduce the problem, what you expected, and what happened instead. A relevant excerpt from the application log ~/.kortty/kortty.log (see Configuration files ) and, for crashes, any crash report your OS produced. A screenshot if the problem is visual. Open the issue at https://github.com/chardonnay/korTTY/issues/new : use a short, specific title (e.g. \"SFTP tab freezes when renaming a file on Windows 11\" ), paste the collected facts into the description, and submit it. Stay reachable — follow-up questions on the issue are common; enable GitHub notifications for it. Data Security Log files and screenshots can contain sensitive information such as hostnames, usernames, file paths or session details. Review and redact everything you attach to a public GitHub issue. Suggest a feature or idea Check https://github.com/chardonnay/korTTY/issues for similar suggestions first — add a comment or a 👍 reaction there instead of opening a duplicate. Open a new issue at https://github.com/chardonnay/korTTY/issues/new with a title starting with Feature: (e.g. \"Feature: bookmark folders in the connection manager\" ). Describe the use case — what you are trying to accomplish and why the current behavior falls short — rather than only the solution you have in mind. Mockups or examples from other tools are welcome. Release notes for shipped changes are collected in Release notes .","title": "Feedback & support"},{"location": "about/feedback.html#feedback-support","text": "korTTY is developed openly on GitHub: https://github.com/chardonnay/korTTY . Bug reports and feature ideas are both handled as GitHub issues. You need a free GitHub account to open an issue.","title": "Feedback &amp; support"},{"location": "about/feedback.html#report-a-bug","text": "Search for existing reports first at https://github.com/chardonnay/korTTY/issues (include closed issues) — your problem may already be known or even fixed in a newer release. Collect the key facts: The korTTY version — shown in Help > About and in the footer of this manual. Your operating system and version (e.g. macOS 15, Windows 11, Ubuntu 24.04). Exact steps to reproduce the problem, what you expected, and what happened instead. A relevant excerpt from the application log ~/.kortty/kortty.log (see Configuration files ) and, for crashes, any crash report your OS produced. A screenshot if the problem is visual. Open the issue at https://github.com/chardonnay/korTTY/issues/new : use a short, specific title (e.g. \"SFTP tab freezes when renaming a file on Windows 11\" ), paste the collected facts into the description, and submit it. Stay reachable — follow-up questions on the issue are common; enable GitHub notifications for it. Data Security Log files and screenshots can contain sensitive information such as hostnames, usernames, file paths or session details. Review and redact everything you attach to a public GitHub issue.","title": "Report a bug"},{"location": "about/feedback.html#suggest-a-feature-or-idea","text": "Check https://github.com/chardonnay/korTTY/issues for similar suggestions first — add a comment or a 👍 reaction there instead of opening a duplicate. Open a new issue at https://github.com/chardonnay/korTTY/issues/new with a title starting with Feature: (e.g. \"Feature: bookmark folders in the connection manager\" ). Describe the use case — what you are trying to accomplish and why the current behavior falls short — rather than only the solution you have in mind. Mockups or examples from other tools are welcome. Release notes for shipped changes are collected in Release notes .","title": "Suggest a feature or idea"},{"location": "about/release-notes.html","text": "Release notes What changed in the current release. The version this guide was built for is shown in the footer. v2.18.0 Terminal Coding agents — korTTY recognises Claude Code, Codex and Gemini CLI in local shell tabs and classifies each pane as working, blocked on a question, done or idle, and now shows it everywhere: a glyph in the tab title, chips, accent bars, rollups and pane rows in the dashboard (which expands to a waiting agent and offers Send Enter / Esc / Interrupt in the context menu), a dockable Coding Agents panel ( View → Coding Agents , Ctrl + Alt + G ) listing every agent of every window with one-click answers (y, n, Enter, Esc, ↑, ↓, Ctrl+C), Explain, Rename and a prompt box, a status-bar strip, Ctrl + Alt + N to jump to the next waiting agent, a badge with the number of waiting agents on the app icon (Dock, Windows taskbar, KDE/Ubuntu launcher; window title elsewhere) and a desktop notification when an agent needs a decision or finishes while you are not looking at its pane. Detection reads the terminal screen locally and can be switched off in Settings → Terminal → Coding agents , where the badge and the notifications have their own toggles; the rules are JSON files you can override under ~/.kortty/coding-agents/ . See Coding agents . Control API and kortty-cli — korTTY can now open a local socket so a script or a coding agent on the same computer can list your windows, read any pane, type into it, wait for output, split a local shell, and drive a detected coding agent. It is off by default and is switched on under Settings → Terminal → Control API , where a status line reports what the listener is actually doing. Nothing is reachable over the network and no other user of the computer can connect: the endpoint lives in an owner-only ~/.kortty/control/ directory and needs a token that is regenerated on every start. Every action is logged with byte counts only, never terminal text, and the first time a program types into a pane you get one desktop notification, so a takeover is never silent. An administrator can deny the feature outright with the control-api policy key. The kortty-cli client ships beside the application in every native package and handles discovery, authentication and exit codes for you. See Control API and Control CLI . Anonymous usage statistics now cover coding agents and the Control API — if you allowed anonymous statistics, korTTY also reports which agent kinds are detected, how often agent notifications fire, which Control API methods are used and whether the coding-agent and Control API settings are on. Only kinds, states, method names and counts are sent — never terminal text, prompts or pane names. See Anonymous data . Local AI Go back to an older llama.cpp or MLX runtime — when a new runtime release causes problems, Other version… in the Runtimes table of AI → AI Manager → Local Models lists every version the signed stable channel still offers for this computer and installs the one you choose through the same verification as an update. A version other than the newest stays pinned , so update checks keep it until you pick the newest version again or choose Unpin ; a pinned version the channel later withdraws is dropped automatically. See Runtimes . Both runtimes stay visible in the Runtimes table — the table had a fixed height that fit two rows only at the default font, so with a larger UI font scale or an AtlantaFX design only llama.cpp was visible and MLX needed scrolling. The table now sizes itself to its rows and follows font-scale and design changes while the AI Manager is open. Being offline is no longer reported as a failed runtime update — the startup check for the llama.cpp and MLX runtimes ran into an error with a stack trace whenever the computer had no network yet. An unreachable channel now ends in an Offline status and a single log line, installed runtimes stay usable, and an installation you start yourself still says why it did not happen. MLX runtime updates are published like llama.cpp ones — the MLX index is now read from the same immutable, versioned releases as the llama.cpp index instead of a release whose files were replaced on every update; builds fall back to the previous location until the first combined release exists. AI chat Attach the selected file to an AI request — when the text you right-click in the terminal is a single file name, AI → Summarize / Solve Problem / Ask and Ask Agent… offer to send that file's content along with the request, so the model can work with the file rather than only its name. The file is resolved against the pane's current directory (over SFTP in SSH tabs, from disk in local-shell tabs) and is attached only after korTTY has checked that it exists, is readable, is UTF-8 text and fits the profile's selection limit; a checkbox in the confirmation dialog lets you send the text alone. The attachment is shown above the chat, stays in context for follow-up prompts and is kept with a saved chat. A Flowchart button beside it renders a Mermaid flowchart of the attached script in the chat. See Attaching a selected file to the chat . Performance Dialogs open noticeably faster — the WebKit engine is loaded in the background after start instead of on the first dialog that needs it, the AI Manager and the settings dialog build their tabs on first visit, a stored connection's secrets are no longer re-derived on every open, and the snippet editor keeps a ready editor in reserve. Measured on a development build, the first AI Manager open dropped from about 1.4 s to under 0.1 s and the settings dialog from about 350 ms to 45 ms. The spare editor can be switched off with Keep a pre-warmed editor ready . See Snippet Editor settings . Guide and snippets The built-in guide has a new look and a working offline search — the guide now uses the Dracula theme, because the previous theme reaches end of life in November 2026. Search works without a network connection and suggests results as you type, and German pages now declare German as their language, so screen readers and hyphenation use the right one. Clearer coding-agents diagram and a wider guide search field — the diagram on the coding-agents page shows one short note per box and no longer routes a line through other boxes, and the search field in the guide header is wide enough for its German placeholder. Flow diagrams keep loops a model drew in the wrong shape — when a model drew a loop's condition as a plain action with two exits, the repair cut the exit after the loop and the whole diagram was replaced by the local fallback. Such a loop head is now turned into the loop decision. Earlier releases Only the current release is listed here, so the guide stays short in every language it is translated into. Every version is on the GitHub releases page ; the curated notes for earlier versions are kept in the repository, in app-docs/release-notes-archive.md and app-docs/RELEASE_NOTES.adoc .","title": "Release notes"},{"location": "about/release-notes.html#release-notes","text": "What changed in the current release. The version this guide was built for is shown in the footer.","title": "Release notes"},{"location": "about/release-notes.html#v2180","text": "","title": "v2.18.0"},{"location": "about/release-notes.html#terminal","text": "Coding agents — korTTY recognises Claude Code, Codex and Gemini CLI in local shell tabs and classifies each pane as working, blocked on a question, done or idle, and now shows it everywhere: a glyph in the tab title, chips, accent bars, rollups and pane rows in the dashboard (which expands to a waiting agent and offers Send Enter / Esc / Interrupt in the context menu), a dockable Coding Agents panel ( View → Coding Agents , Ctrl + Alt + G ) listing every agent of every window with one-click answers (y, n, Enter, Esc, ↑, ↓, Ctrl+C), Explain, Rename and a prompt box, a status-bar strip, Ctrl + Alt + N to jump to the next waiting agent, a badge with the number of waiting agents on the app icon (Dock, Windows taskbar, KDE/Ubuntu launcher; window title elsewhere) and a desktop notification when an agent needs a decision or finishes while you are not looking at its pane. Detection reads the terminal screen locally and can be switched off in Settings → Terminal → Coding agents , where the badge and the notifications have their own toggles; the rules are JSON files you can override under ~/.kortty/coding-agents/ . See Coding agents . Control API and kortty-cli — korTTY can now open a local socket so a script or a coding agent on the same computer can list your windows, read any pane, type into it, wait for output, split a local shell, and drive a detected coding agent. It is off by default and is switched on under Settings → Terminal → Control API , where a status line reports what the listener is actually doing. Nothing is reachable over the network and no other user of the computer can connect: the endpoint lives in an owner-only ~/.kortty/control/ directory and needs a token that is regenerated on every start. Every action is logged with byte counts only, never terminal text, and the first time a program types into a pane you get one desktop notification, so a takeover is never silent. An administrator can deny the feature outright with the control-api policy key. The kortty-cli client ships beside the application in every native package and handles discovery, authentication and exit codes for you. See Control API and Control CLI . Anonymous usage statistics now cover coding agents and the Control API — if you allowed anonymous statistics, korTTY also reports which agent kinds are detected, how often agent notifications fire, which Control API methods are used and whether the coding-agent and Control API settings are on. Only kinds, states, method names and counts are sent — never terminal text, prompts or pane names. See Anonymous data .","title": "Terminal"},{"location": "about/release-notes.html#local-ai","text": "Go back to an older llama.cpp or MLX runtime — when a new runtime release causes problems, Other version… in the Runtimes table of AI → AI Manager → Local Models lists every version the signed stable channel still offers for this computer and installs the one you choose through the same verification as an update. A version other than the newest stays pinned , so update checks keep it until you pick the newest version again or choose Unpin ; a pinned version the channel later withdraws is dropped automatically. See Runtimes . Both runtimes stay visible in the Runtimes table — the table had a fixed height that fit two rows only at the default font, so with a larger UI font scale or an AtlantaFX design only llama.cpp was visible and MLX needed scrolling. The table now sizes itself to its rows and follows font-scale and design changes while the AI Manager is open. Being offline is no longer reported as a failed runtime update — the startup check for the llama.cpp and MLX runtimes ran into an error with a stack trace whenever the computer had no network yet. An unreachable channel now ends in an Offline status and a single log line, installed runtimes stay usable, and an installation you start yourself still says why it did not happen. MLX runtime updates are published like llama.cpp ones — the MLX index is now read from the same immutable, versioned releases as the llama.cpp index instead of a release whose files were replaced on every update; builds fall back to the previous location until the first combined release exists.","title": "Local AI"},{"location": "about/release-notes.html#ai-chat","text": "Attach the selected file to an AI request — when the text you right-click in the terminal is a single file name, AI → Summarize / Solve Problem / Ask and Ask Agent… offer to send that file's content along with the request, so the model can work with the file rather than only its name. The file is resolved against the pane's current directory (over SFTP in SSH tabs, from disk in local-shell tabs) and is attached only after korTTY has checked that it exists, is readable, is UTF-8 text and fits the profile's selection limit; a checkbox in the confirmation dialog lets you send the text alone. The attachment is shown above the chat, stays in context for follow-up prompts and is kept with a saved chat. A Flowchart button beside it renders a Mermaid flowchart of the attached script in the chat. See Attaching a selected file to the chat .","title": "AI chat"},{"location": "about/release-notes.html#performance","text": "Dialogs open noticeably faster — the WebKit engine is loaded in the background after start instead of on the first dialog that needs it, the AI Manager and the settings dialog build their tabs on first visit, a stored connection's secrets are no longer re-derived on every open, and the snippet editor keeps a ready editor in reserve. Measured on a development build, the first AI Manager open dropped from about 1.4 s to under 0.1 s and the settings dialog from about 350 ms to 45 ms. The spare editor can be switched off with Keep a pre-warmed editor ready . See Snippet Editor settings .","title": "Performance"},{"location": "about/release-notes.html#guide-and-snippets","text": "The built-in guide has a new look and a working offline search — the guide now uses the Dracula theme, because the previous theme reaches end of life in November 2026. Search works without a network connection and suggests results as you type, and German pages now declare German as their language, so screen readers and hyphenation use the right one. Clearer coding-agents diagram and a wider guide search field — the diagram on the coding-agents page shows one short note per box and no longer routes a line through other boxes, and the search field in the guide header is wide enough for its German placeholder. Flow diagrams keep loops a model drew in the wrong shape — when a model drew a loop's condition as a plain action with two exits, the repair cut the exit after the loop and the whole diagram was replaced by the local fallback. Such a loop head is now turned into the loop decision. Earlier releases Only the current release is listed here, so the guide stays short in every language it is translated into. Every version is on the GitHub releases page ; the curated notes for earlier versions are kept in the repository, in app-docs/release-notes-archive.md and app-docs/RELEASE_NOTES.adoc .","title": "Guide and snippets"},{"location": "features/ai-assistant.html","text": "AI assistant KorTTY can analyze selected terminal text with an OpenAI-compatible AI endpoint, an integrated local llama.cpp model, or a configured local CLI and open the answer in a temporary AI result tab. You can also start agent-style workflows to automate SSH tasks or get plans reviewed before implementation. When Configuration > Prevent System Sleep is enabled on macOS or Windows, korTTY keeps the computer awake while an AI API, local-model, web-tool, or local AI CLI request is waiting for a result. The assertion is released after the last concurrent AI request finishes; if no terminal is connected and no future or running Scheduler job exists, the computer may then sleep normally. Display sleep remains available. Data Security Selected terminal text is transmitted to the configured AI service for analysis. That text can contain sensitive information such as credentials, hostnames, file paths, stack traces, or other operational details. For sensitive data, use an integrated local GGUF model or another endpoint you trust. Embedded inference uses an authenticated loopback-only server; remote providers receive the reviewed request over the network. If you provide an API Key , korTTY stores it encrypted with your master password. Setup Open Edit > Global Settings . Go to AI . Create one or more AI profiles in Settings > AI or AI > AI Manager > Profiles . Choose HTTP API , Local CLI , or Integrated llama.cpp as the connection mode. For HTTP profiles, enter an API URL, model, and optional encrypted API key. For an embedded profile, select an installed GGUF model; korTTY supplies the private endpoint and temporary key. Use AI Manager > Local Models first when no GGUF is installed. Optionally configure Prompt optimization , Max characters , Tokenizer , Token limit , warning thresholds, token reset cycle, supported Reasoning effort, and Internet access per profile. korTTY exposes reasoning choices based on the configured endpoint and model; LM Studio's exact native capability metadata takes precedence over compatibility probes so a silently converted effort is not shown as supported. Profiles without a supported explicit reasoning mode keep provider-default behavior. Optionally set an AI request timeout in minutes. korTTY does not time out AI requests on its own, so a long task such as the snippet editor's Full code analysis runs until the model answers. The value on the Profiles page applies to every profile; Timeout for this profile overrides it for a single profile, and 0 there means that profile never times out. See AI settings . Click Test AI Connection . Optionally choose a Default profile , then assign separate Text/translation and Coding roles under AI Manager > Local AI . An empty role uses the default profile. Optionally configure the default language for AI-generated text inside code comments and program output, enable the extra instructions field for snippet AI actions, and set how many alternative solutions the snippet editor should request. Optionally configure the terminal-agent input-history size (default 20, range 5–100), the agent command name, case-insensitive command matching, execution target, prompt-hook usage, per-run setup dialog, debug/runtime visibility, and activity-panel preferences for AI Agent and AI Planning . Optionally disable the confirmation dialog for Summarize and Solve Problem if you want a faster workflow. Ask always opens the prompt dialog. AI Profile Setup Wizard A guided wizard creates AI profiles with support for both local and cloud-based language models. To open the wizard: Settings > AI , click Add Profile or Edit an existing profile. Tools > AI Manager > Profiles , click Add . The wizard guides you through: Connection Type - Choose local LM Studio or a cloud provider. LM Studio Setup - If local: pick a loaded LM Studio model from the list or use Auto mode. Cloud Provider Setup - If cloud: select the provider (Anthropic Claude API, OpenAI, or other OpenAI-compatible endpoint). API Details - Enter API Key and select or enter the Model name — the model list is pre-filled with common models for the chosen provider, and Load models merges the endpoint's live model list on top. Optionally configure Reasoning effort (if the provider/model supports extended thinking). Profile Name - Enter a display name for the profile (e.g., \"Claude Opus\", \"Local LM Studio\"). Native Anthropic (Claude) API support is included alongside existing OpenAI-compatible endpoints. For fully integrated GGUF inference, use the separate AI Manager > Local Models > Setup assistant . It checks hardware, recommends Text/Coding/embedding models, reviews license and exact size, verifies the immutable download, creates an embedded profile, assigns the selected role, and runs a real local chat or embedding function test. See Local models with llama.cpp . Model selection The model selector in Settings > AI and Tools > AI Manager > Profiles is editable: For known cloud providers the dropdown is pre-filled with common model names for the configured endpoint, so a concrete model can be chosen without an API key. The refresh button next to the selector merges the endpoint's live /v1/models list on top when the API key is valid. Choosing a listed model stores it as the manual selection. Typing a model name stores it as the manual selection so any OpenAI-compatible endpoint works. Auto is offered only for local LM Studio endpoints, where korTTY can actually detect the loaded model. Cloud profiles need a concrete model; if none is selected, requests stop with an explicit \"select a specific AI model\" error. An Integrated llama.cpp profile lists installed model IDs from ~/.kortty/llm/models.xml ; it does not use the HTTP URL or manual cloud-model field. Role routing, RAG, and prompt order korTTY classifies its own action types deterministically without asking a model. Translation, summary, problem solving, questions, prose descriptions, and AI ASCII-art pictures use the Text role; coding, completion, snippet review, security fixes, diagrams, and workflow generation use the Coding role. An explicit profile choice, security-check profile, or connection-specific profile takes precedence, followed by the role profile and then the default. When matching knowledge stores are assigned to the request's Text or Coding role, an ordinary AI request is assembled in this order: korTTY's action/output contract, selected AI Skills, bounded untrusted RAG context with [R1] source markers, the resolved model-family preset, and finally the provider transport. Only retrieved excerpts are added, not the complete knowledge store; choosing a cloud profile sends those excerpts to that provider, so the knowledge-store role/profile assignment is the explicit disclosure decision. Strict JSON and code-payload rules remain authoritative. The dedicated source-only diagram request used by snippets and workflows is a narrow exception: after its fixed output contract korTTY always adds one compact, immutable action skill for the requested diagram family (logical-structure flowchart, sequence, state, class, or ER) and omits every configurable library skill and retrieval context. For a profile with a fixed model selection, korTTY automatically sets the request-scoped Reasoning value to none only when that value is available, without changing the saved profile. The action skill is not one of the 39 shipped configurable built-in skills: users cannot disable or edit it, and it adds no matching/classification request. The Full code analysis stages that apply selected improvements and its security-fix follow-up use the same fixed-model rule because they return bounded, machine-parsed full-script replacements; the preceding analysis still uses the profile's configured Reasoning value. An Auto profile is not overridden from previously discovered capabilities because its loaded model can change; an explicitly configured none value still applies. Autonomous Agent, Planning, Swarm, and scheduled prompts require explicit RAG opt-in. See RAG knowledge stores for the retrieval and privacy details. Local LM Studio model selection For local LM Studio profiles, korTTY can discover currently loaded LLM model keys and exact reasoning options through LM Studio's GET /api/v1/models endpoint. A binary model that reports only off and on therefore no longer shows the Minimal, Low, Medium, High, or Extra high levels as supported merely because LM Studio would silently convert an unsupported value; an explicit none request switches this feature off, while omitting the reasoning parameter uses the model's published default. Auto mode resolves the effective model immediately before connection tests, AI chat and follow-up requests, terminal AI actions, and Terminal AI Agent runs. If exactly one LLM is loaded, korTTY uses that model. If multiple LLMs are loaded, korTTY uses the saved preferred model only when that model is currently loaded. If no LLM is loaded, or multiple LLMs are loaded without a valid saved preference, korTTY stops the request with an explicit error instead of guessing. AI internet access Internet access is configured per AI profile. Existing and new profiles default to Disabled . Mode Behavior Disabled No web tools or MCP integrations are sent with AI requests. KorTTY Tavily Tool korTTY adds a web_search and a web_extract tool to eligible OpenAI-compatible /v1/chat/completions requests. Tool calls are executed by korTTY: searches through POST https://api.tavily.com/search (5 results with short snippets), page reads through POST https://api.tavily.com/extract , which returns the text of one web page or online document, capped at 12,000 characters. LM Studio Tavily MCP korTTY sends an LM Studio native /api/v1/chat request with a Tavily MCP integration. Bright Data Web MCP korTTY sends an LM Studio native /api/v1/chat request with a Bright Data MCP integration. Brave Search MCP korTTY sends an LM Studio native /api/v1/chat request with a configured Brave Search MCP plugin ID. SearXNG MCP korTTY sends an LM Studio native /api/v1/chat request with a configured SearXNG MCP plugin ID. LM Studio Toolpack korTTY sends an LM Studio native /api/v1/chat request with the configured LM Studio Toolpack MCP plugin ID. Required provider configuration is entered under Settings > AI > Internet tool configuration . API keys and tokens are stored encrypted with the master password. MCP server labels and plugin IDs are stored as normal settings. Important behavior: Snippet AI, text correction, translation, snippet descriptions, alternative-solution requests, and AI ASCII-art generation do not use internet access. Direct korTTY web tools have a 5-second connect timeout, a 20-second request timeout (40 seconds for page reads), and a maximum of two web-tool rounds with up to three tool calls each per AI request. A mode can only be selected in a profile once its required global setting (for example the Tavily API key) is stored under Settings > AI > Internet tool configuration ; otherwise the AI Manager explains where the value belongs and keeps the previous mode. The profile's own API Key field is only for the AI provider itself. Internet use is shown where it happens. In the AI chat, a collapsed Internet research line above the reply summarizes the searches, page reads and failures; expanding it lists every query, read URL, source and error. It is stored with saved chats. In the Terminal AI Agent, each step that used the internet gets a Web research activity row with the same details. For LM Studio MCP modes korTTY shows the tool calls LM Studio reports, with the URLs found in their output. LM Studio MCP requests with internet access use a longer total request timeout because the MCP server runs behind LM Studio. Canceling a running request interrupts the Java HTTP request where the active provider supports interruption. Tool errors are returned to the model as structured data. If the web tool times out, fails authentication, returns no results, or reaches the tool-round limit, the model is instructed to say that explicitly and not invent web facts. For terminal-agent JSON planning, korTTY offers its direct web tools only when the user task contains a web signal word (for example current, latest, search, research, docs, changelog, CVE, website, download, version) or a URL. Local file/script review tasks should be handled by shell commands such as sed , cat , find , or test commands, not by web search. Enable AI agent: offer internet research on every step under Settings > AI > Internet tool configuration to offer the tools on every agent step and let the model decide; this uses more Tavily credits and tokens. LM Studio MCP modes always attach their integration to agent steps. AI Skills AI Skills are reusable local instruction blocks that korTTY can add to AI requests. Use them for persistent preferences such as coding standards, review rules, operational policies, or language-specific style guidance. Open AI > AI Manager > AI Skills . The library moved here from the global settings dialog; Save writes it immediately, and pending edits are also saved when the AI Manager is closed. Skill fields Skill name - Human-readable name shown in the skill list and activity logs. Description - Short explanation used by automatic skill matching. Tags - Comma-separated keywords used by automatic skill matching. Target - AI Chat/Functions , AI Agent , or Both . Active - Enables or disables only this skill. Skill Markdown - The instruction body sent to the model when the skill is selected. Controls Enable AI Skills disables or enables all skills globally. Automatically send only matching skills sends only active skills that match the current request — at most the two highest-scoring matches; skills assigned to the active connection or explicitly selected in the snippet editor's AI skills picker are added on top of that limit. When disabled, all active skills with a matching target are sent. Add creates a new active skill with target Both . Delete removes the selected skill after confirmation. Import accepts .md and .markdown files. Export writes Markdown files for the selected skill, or all skills if none is selected. The skill list can be sorted alphabetically or by enabled/disabled status. Import/export format --- kortty-ai-skill: 1 name: My Skill description: Short purpose statement enabled: true target: BOTH tags: [linux, bash] --- Skill instructions as Markdown. Plain Markdown without korTTY frontmatter is imported with the file name as the skill name, target Both , and disabled by default so you can review it before use. Claude/Codex-style SKILL.md frontmatter with name , description , and tags is also accepted and imported disabled by default unless it is korTTY's own export format. When an AI Agent run uses one or more skills, the terminal-agent activity panel logs the selected skill names. Connection tests never send skills, so Reply with exactly OK tests remain stable. Terminal selections Using AI for selected text Select text in the terminal. Right-click the selected text. Open AI and choose: Summarize - Creates a concise summary of the selected output. Solve Problem - Analyzes the selected error output and suggests likely fixes. Ask - Sends the selection together with your own follow-up question or instruction. Confirm the request in the preview dialog. You can edit the selected text before sending it. For Ask , add your own prompt. The dialog also shows the estimated request tokens and projected remaining quota. The response opens in a temporary AI tab. You can continue the same context with follow-up prompts from the bottom composer field. Use Save in the AI tab to store the conversation under a custom title. Reopen saved conversations later via Tools > AI Manager or ++Ctrl+Shift+Y++ (++Cmd+Shift+Y++ on macOS). Attaching a selected file to the chat When the selection looks like a single file name — for example a name from ls output, quoted if it contains spaces — korTTY offers to send that file's content along with the request, so the model can take the file into account instead of only its name. The file is resolved against the pane's current directory, over SFTP in SSH tabs and from the local filesystem in local-shell tabs, exactly like Open in Snippet Editor . The confirmation dialog shows an Attach file checkbox with the file name. It is pre-selected, and you can clear it to send only the selected text. While the file is being checked, OK waits; clear the checkbox to send without waiting. Before anything is attached, korTTY verifies that the file exists in the current directory, is a regular file, is readable with your permissions, decodes as UTF-8 text, and fits into the profile's Max characters limit (see AI settings ) together with the selected text. Binary files, oversized files, and files that fail any check are not attached; the dialog states the reason and the checkbox is disabled. When the confirmation dialog is disabled for Summarize and Solve Problem , and always for Ask Agent… (which has no preview dialog), the file is attached automatically if it passes the checks; otherwise the request is sent without it and the status bar says why. No attachment is offered after an identity switch inside the session ( su , an inner ssh ), because the file would be resolved against the wrong login — the same rule that greys out Open in Snippet Editor . The attached file is listed above the chat messages, stays in context for every follow-up prompt, and is stored with a saved chat so a reopened conversation keeps it. The Flowchart button beside the attachment asks the active profile for a Mermaid flowchart of the attached script (the same logical-structure diagram the Snippet Editor generates) and shows it as a rendered diagram in the chat. If the model's answer is unusable, korTTY draws the local structural flowchart instead. AI result tab features The conversation transcript is read-only and not included in saved project/session state. Assistant answers display Markdown as a formatted preview: headings, bold and italic text, strikethrough, lists, quotations, and highlighted inline code. Text wraps to the available width and can be selected and copied; user messages keep their literal text. The conversation automatically scrolls to the newest user message or assistant answer, including content that grows after rendering. Scroll up with the mouse wheel, keyboard, or scrollbar to pause this behavior while reading earlier messages. Scrolling back to the bottom automatically resumes it. <think> ... </think> blocks are removed from the visible output. The toolbar lets you copy the conversation, save or rename the chat, share/export it to PDF/Markdown/plain text, retry the last request, close the tab, cancel running requests, and change the font size. The response language defaults to the current GUI language. You can change the response language and the active AI profile per chat before sending a follow-up prompt. Follow-up prompts in Summarize and Solve Problem continue as normal chat questions; they are not forced back into the original summarizing/problem-analysis prompt. Detected code blocks get their own copy button and can also be saved directly into the Snippet Manager. Blocks that contain images, diagrams, or math render as images instead — see Rendered images, diagrams, and math . Rendered markdown tables can be copied as a whole table, a single column, or a single cell. The chosen AI tab font size is stored globally and reused for future AI result tabs. Token usage is recorded per AI profile after successful requests so warnings and reset cycles remain accurate. If a saved chat references an AI profile that no longer exists, korTTY asks you to choose a replacement profile before you continue with follow-up prompts. Rendered images, diagrams, and math AI answers that contain images, diagrams, or math formulas are rendered as images inside the chat instead of showing raw markup. This also applies to saved chats reopened from the AI Manager. Content in the AI answer Rendered as ```svg / ```xml / ```html code block (or untagged block) containing an <svg> document Inline vector image Markdown image link with a data:image/png;base64,… URI in the answer text, or a code block containing only such a data URI Inline raster image (PNG, JPEG, GIF, BMP; up to 8 MB decoded) with a copy image button ```mermaid code block Mermaid diagram (bundled library, no network) ```latex / ```tex / ```math code block, or $$ … $$ math in the answer text Typeset formula (bundled MathJax, no network) Every rendered block keeps a header with the usual copy button and a Show code / Show image toggle, so the underlying source stays one click away. While a Mermaid/math block is still rendering, the source remains visible; if rendering fails (for example a Mermaid syntax error), the block stays on the source view and the header shows the reason. plantuml / puml fences and untagged @startuml content are ordinary source-code blocks and are not rendered specially. Example prompts that produce rendered answers: Draw a simple house as an SVG image. Create a Mermaid flowchart of a typical login flow. Create a Mermaid sequence diagram for an SSH handshake. Explain the Pythagorean theorem and show the formula. A Mermaid answer fenced with the mermaid language tag and this body renders as a flowchart: flowchart TD Login-->Validate; Validate-->|ok| Session; Validate-->|fail| Error; And display math in the answer text renders as a typeset formula: $$a^2 + b^2 = c^2$$ Rendering details and requirements SVG and rendered Mermaid output is displayed with JavaScript disabled and scripts/event handlers stripped from the document. Mermaid 11.17.2 runs with its strict security level from a SHA-256-pinned local bundle; LaTeX is typeset by a separately loaded local MathJax bundle. Neither needs internet access. Chat Mermaid retains the full bundled diagram support, including flowchart, sequence, class, state, ER, mindmap, and architecture diagrams. Frontmatter, directives, network/file/data/JavaScript URLs, external images/icons, links, and click callbacks are rejected; source, edge-count, raster-size, and timeout limits protect the renderer. Full LaTeX documents ( \\documentclass ) intentionally stay code blocks; only formulas are typeset. Mermaid follows the active chat's light/dark palette; other rendered images and formulas retain a readable neutral canvas. AI Manager Open AI > AI Manager or press ++Ctrl+Shift+Y++ (++Cmd+Shift+Y++ on macOS). The AI Manager is a modeless window: you can keep it open while you continue working in the main window. Invoking AI Manager again restores and focuses the same manager for that main window instead of opening a duplicate. Its active primary section remains marked by a bold accent underline after focus moves into the section's controls. The manager combines profile, local-inference, retrieval, and saved-chat management: Profiles - Create, edit, test, save, and remove AI profiles. The profile list shows the current quota/usage status for each profile. Local Models - Search Hugging Face, import/download/configure GGUF files, run the post-install function test, and start or stop several llama.cpp sidecars. Local AI - Assign Text/translation, Coding and session journal profiles, choose the embedding model and preferred runtime backend, store an encrypted Hugging Face token, and select the runtime-update policy. Knowledge Stores - Create local HNSW stores, add reviewed files or recursive folders, synchronize sources, and run a test search. Saved Chats - Open, rename, refresh, or delete previously saved AI conversations. Saved AI Swarm conversations appear in their own Swarm Chats section, including those produced by scheduled swarm jobs. Use Settings > AI for global behavior switches and AI Manager for day-to-day profile, model, knowledge-store, and chat management. Ask the manual (AI docs search) The built-in manual ( Help > Manual , F1 ) includes an AI-powered search. Toggle AI search in the manual window's toolbar to open a side panel, type a question in natural language — for example \"How do I run the AI agent in the terminal window?\" — and press Enter . How it works: korTTY selects the most relevant sections from the bundled offline manual (no embeddings, no external search service) and sends only those excerpts together with your question to your default AI profile . The answer is generated exclusively from the manual content and is written in the app language. If the manual does not cover the question, the assistant says so instead of guessing. Answers cite their sources: click an inline citation or an entry in the Sources list to jump the manual view directly to the referenced page and section. If nothing in the manual matches the question, korTTY answers locally without contacting the AI endpoint at all. Requirements: A configured AI profile (see Setup ); the default profile is used. An unlocked master-password vault when the profile stores an encrypted API key. Data Security The question text and the selected manual excerpts are transmitted to the configured AI endpoint. The manual content itself is public documentation, but your question is free text — avoid pasting secrets into it, or use a trusted local endpoint such as LM Studio . Internet access modes are always disabled for manual questions. AI Agent and AI Planning korTTY supports agent-style workflows for an active terminal session. SSH and local shells The agent's command-execution engine is decoupled from SSH behind an AgentCommandRunner abstraction with two backends — SSH (exec channel) and local (a fresh local process). The AI Agent and AI Planning therefore run both in SSH sessions and in local shells on Windows, macOS and Linux: commands use a native local backend (PowerShell via -EncodedCommand , cmd.exe , or POSIX /bin/sh ), the environment probe and system prompt are platform-aware so the model generates native commands, and the same approval flow applies. A local run captures the interactive shell's current directory once and uses it for the probe and every command. Local-shell limitation: no sudo /administrator elevation on Windows. The JobScheduler's headless AI-agent action stays SSH-only. Starting the agent AI Agent - Start from Tools > AI Agent... , from the terminal right-click menu, or with the terminal shortcut command. The agent can open in a dedicated chat tab or target the active terminal window, depending on Settings > AI . AI Planning - Start from Tools > AI Planning... , from the terminal right-click menu, or with agent-plan / agent -plan . Planning mode asks clarifying questions, proposes one or more options, creates a final plan report, and lets you start the accepted plan. Activity panel and placement Split-local activity panel - Terminal-targeted runs appear at the bottom of the terminal split where the run was started. Each split has its own panel, so different splits can run their own agent tasks in parallel. Panel placement - Use View > AI Agent Panel to choose At Bottom (default), Dock Left , or Dock Right . Docked mode shows the activity in a resizable side panel attached to the main window; drag its divider to resize. The placement and width are remembered across restarts. In side mode there is one outer tab per terminal of the active terminal tab, and runs are stacked vertically. Switching to another terminal tab swaps the dock to that tab's terminals. Agent status indicators - A per-terminal AI-agent status icon appears in the Dashboard tree and prefixed to the terminal's tab title: ✋ awaiting input ⚡ working ⏸ paused ✓ finished Concurrent runs - Multiple concurrent runs per split are shown as closable tabs in the activity panel (one tab per run), with a per-widget concurrency cap of 5 runs. Finished runs stay as tabs until closed. Typing while running - Typing is no longer locked while a run is active. You can continue typing in the shell prompt and launch another agent ... command (it opens a new concurrent tab). Only run-control keys are intercepted: ++Esc++ or ++Ctrl+C++ cancel the selected tab's run; ++Ctrl+R++ toggles that run's thinking details. Pause and Resume - Each run tab shows pause and cancel buttons. Pause parks the agent at a safe point between steps; paused time is excluded from the run work-time. Current directory - Terminal shortcut runs use korTTY's tracked current directory for SSH and local shells. A local run captures one stable directory for its probe and every command, so commands and generated files remain relative to where the interactive shell was working when the run began. Approvals and sudo - The agent can request explicit approval before command execution and can ask for a sudo password in the activity panel. Password input is masked, can be submitted with ++Enter++, and allows up to three wrong-password retries. If a password is cached, it is used only for the current agent/session context. When user input (sudo password / command approval) is required, the panel auto-expands. Collapsed status bar - When the panel is collapsed, it shows a compact status bar with the run prompt, state, pause/cancel buttons, and an expand button. A spinner appears while the agent is actively working, and a bold ✋ marker signals when user input is required. Keep collapsed - Use Keep collapsed to make the panel start minimized to the status bar. Terminal output - Final terminal-agent answers are written back to the terminal area so the shell transcript contains the answer. Panel controls - Use the reload button to run the selected agent command again. Copy or snippet actions are available on activity rows. Export the current run or full history as Markdown, plain text, YAML, XML, JSON, PDF, or Asciidoctor. Use Expand all to keep activity details open. Use A- and A+ to change the activity font size. Drag the resize handle to change the panel height. Terminal agent shortcut commands When terminal agent shortcuts are enabled and the terminal is at a shell prompt, korTTY intercepts these commands locally instead of sending them to the server: agent <goal> agent-ask <question> agent-plan <task> agent -plan <task> The base command name is configurable in Settings > AI . If you rename agent , korTTY derives the matching -ask and -plan commands automatically. The same settings page can make the command name case-insensitive and can disable the per-run setup dialog; when the dialog is disabled, korTTY uses the configured default profile. TAB completion and prompt history At the shell prompt, TAB completion is enhanced for agent commands: Type the agent command name (e.g., agent ) then press ++TAB++ to offer the command variants ( agent , agent-ask , agent-plan ). Type the command plus a space (e.g., agent ) then press ++TAB++ to offer the recent agent-prompt history. Each row shows the prompt and the date/time it was last run, de-duplicated by prompt text (newest first). Each history row shows the prompt on the left and the date/time it was last run on the right. Prompts longer than 60 characters are shortened with an ellipsis; the full prompt is still inserted when the entry is selected. The history popup is resizable — drag the grip in the bottom-right corner — and remembers its size across restarts. It shows a vertical scrollbar when the history is longer than the popup. In the history popup, remove a single prompt by clicking the row's ✕ button (or pressing ++Del++ on keyboards with a forward-delete key), or remove the entire history with Clear all (a two-step confirm guards against accidental wipes). Deletions are saved immediately. Outside this context, ++TAB++ is normal shell completion. History size is configurable in Settings > AI (default 20, range 5–100). How the AI Agent works The Terminal AI Agent is a controlled terminal automation workflow. It does not run arbitrary model output directly in the interactive terminal. Instead, each turn follows this pattern: korTTY probes the active SSH or local-shell session with a non-interactive command and records compact context such as current user, host, operating system, active terminal working directory, sudo availability, disk path, and recent command state. korTTY sends the user task, probe snapshot, previous command results, active AI Skills, and optionally web-tool availability to the selected AI profile. The model must return a strict JSON decision: run commands, ask for confirmation, finish, or block. korTTY validates the JSON schema and command constraints. Invalid responses are repaired once; unsafe or unsupported command decisions are rejected. korTTY runs approved commands through the active backend: SSH exec channels for SSH sessions or fresh local processes for local shells. Each command starts in the directory captured for the run. A cd inside one command does not persist to the next command. Command output is added to the activity panel and to the next model turn until the task is complete, blocked, canceled, or the turn limit is reached. Suitable tasks The agent is suitable for: inspecting files, directories, package state, logs, service state, and system configuration; creating or modifying scripts and configuration files when the task asks for it; running tests, syntax checks, linters, or read-only diagnostic commands; summarizing command output and explaining findings; planning multi-step operational changes before implementation. The agent is not intended for: interactive full-screen programs such as vim , nano , top , less , ssh , or commands that wait for prompts; long-running daemons or commands without clear completion; secret exfiltration or blind destructive commands; web research for local files unless you explicitly ask for external/current information. Example commands agent show the 10 largest XML files in this directory agent update groesste_xml.pl so the -r flag searches subdirectories recursively agent check why nginx failed to start and suggest the safest fix agent-ask what user and directory am I currently using? agent-plan migrate this host from package X to package Y For local file review tasks, name the file in the task. The agent should then inspect it with shell commands such as sed -n , cat , file , or language-specific syntax checks. If an internet-enabled profile is active, korTTY still keeps web tools away from these local file planning prompts unless your task clearly asks for current or external information. Activity row icons Activity rows use semantic emoji icons to indicate the action type: Icon Meaning 💾 Request or input: write/create file 📖 Action: read file/directory ▶️ Action: run/execute command 📁 Action: directory operation 📦 Context: package manager ⚙️ Context: service or system 🌐 Action: network operation 🔍 Action: inspect/analyze 💭 State: thinking/reasoning 💬 Output: message or result ✋ Required: awaiting user input (sudo/approval) ❌ State: error or failed activity 🚫 State: cancelled activity Red styling is reserved for error and failed states. Safety and failure handling korTTY adds guardrails around agent execution: Commands are limited per turn and must be non-interactive. Commands that change the system or require privilege can be routed through confirmation depending on settings and model decision. Sudo uses sudo -n and activity-panel password prompts. korTTY does not allow sudo -S , su , or commands that wait indefinitely for a terminal password prompt. SSH directories are tracked from shell hooks, terminal prompt context, and probe results; if a tracked remote directory no longer exists, korTTY retries the probe from the SSH default directory and reports the issue. Local runs refresh the shell process directory or use an absolute native prompt path, freeze the result for the run, and stop safely when a changed directory cannot be determined or mapped. While a terminal-targeted run is active, normal typing is still allowed; only run-control keys (++Esc++/++Ctrl+C++ to cancel the selected run, ++Ctrl+R++ to toggle its thinking details) are intercepted. Web-search failures, HTTP errors, authentication errors, empty results, and timeouts are surfaced as explicit tool errors. If the AI response does not match the required JSON schema, korTTY asks for a repair. If repair also fails, the run is blocked with an explanation. AI Swarm (multi-server) The AI Swarm broadcasts one AI-agent task to many servers at once: each selected server gets its own agent run — including servers without an open terminal — and the per-server answers are combined into a single comparison table. The swarm tab adds an animated per-agent status strip, expandable live transcripts, pause/resume/restart/stop controls per agent and swarm-wide, AI-free script execution from the Snippet Manager, and a Schedule… button that turns the run into an unattended JobScheduler job. Open it with AI > AI Swarm... or Ctrl + Alt + S (Cmd on macOS); see the AI Swarm page for the full feature set. Generate Workflow Script After a finished agent run completes successfully, a Workflow button turns the run into a single self-contained, reproducible script in a chosen language (Bash, Python, Perl, Ruby, PowerShell, Ansible playbook, Windows-CMD batch, or AppleScript ) with robust error handling, detailed comments, and a header (script name, creator, date/time). Script generation: Auto-loads matching AI Skills (e.g., a language-quality skill for the target language). Can produce several language variants and multiple suggestions as inline tabs. Supports header templates from the fixed non-deletable Script-Header snippet category. Optionally includes a Mermaid flowchart for the script logic. A working spinner is shown while the diagram is generated, so it is clear the AI connection is busy. Saves into the Snippet Manager with a short auto-generated name plus correct extension (de-duplicated by full name including extension). Tags the snippet as workflow for easy filtering. Auto-sets the System (OS) column from the agent's probed OS (any Linux distro becomes Linux). Internet access is forced OFF during generation. Target languages: Bash, Python, Perl, Ruby, PowerShell, Ansible, plus Windows-CMD ( .cmd batch — @echo off lead line, REM header comments, errorlevel checks) and AppleScript ( .applescript — osascript shebang, -- comments, try / on error handling). Each generated-script editor has A− / A+ buttons and supports Ctrl + mouse wheel (Cmd on macOS) to change the font size; the chosen size is remembered across sessions. The workflow dialog is resizable and remembers its size and position for future use. Clearer AI backend errors When the AI server runs out of memory or hits a resource limit (e.g. LM Studio/MLX \"Resource limit exceeded\", \"metal::malloc\"), the dialog shows a short, actionable hint instead of the raw multi-line backend stack trace; all other AI errors are collapsed to a single line.","title": "AI assistant"},{"location": "features/ai-assistant.html#ai-assistant","text": "KorTTY can analyze selected terminal text with an OpenAI-compatible AI endpoint, an integrated local llama.cpp model, or a configured local CLI and open the answer in a temporary AI result tab. You can also start agent-style workflows to automate SSH tasks or get plans reviewed before implementation. When Configuration > Prevent System Sleep is enabled on macOS or Windows, korTTY keeps the computer awake while an AI API, local-model, web-tool, or local AI CLI request is waiting for a result. The assertion is released after the last concurrent AI request finishes; if no terminal is connected and no future or running Scheduler job exists, the computer may then sleep normally. Display sleep remains available. Data Security Selected terminal text is transmitted to the configured AI service for analysis. That text can contain sensitive information such as credentials, hostnames, file paths, stack traces, or other operational details. For sensitive data, use an integrated local GGUF model or another endpoint you trust. Embedded inference uses an authenticated loopback-only server; remote providers receive the reviewed request over the network. If you provide an API Key , korTTY stores it encrypted with your master password.","title": "AI assistant"},{"location": "features/ai-assistant.html#setup","text": "Open Edit > Global Settings . Go to AI . Create one or more AI profiles in Settings > AI or AI > AI Manager > Profiles . Choose HTTP API , Local CLI , or Integrated llama.cpp as the connection mode. For HTTP profiles, enter an API URL, model, and optional encrypted API key. For an embedded profile, select an installed GGUF model; korTTY supplies the private endpoint and temporary key. Use AI Manager > Local Models first when no GGUF is installed. Optionally configure Prompt optimization , Max characters , Tokenizer , Token limit , warning thresholds, token reset cycle, supported Reasoning effort, and Internet access per profile. korTTY exposes reasoning choices based on the configured endpoint and model; LM Studio's exact native capability metadata takes precedence over compatibility probes so a silently converted effort is not shown as supported. Profiles without a supported explicit reasoning mode keep provider-default behavior. Optionally set an AI request timeout in minutes. korTTY does not time out AI requests on its own, so a long task such as the snippet editor's Full code analysis runs until the model answers. The value on the Profiles page applies to every profile; Timeout for this profile overrides it for a single profile, and 0 there means that profile never times out. See AI settings . Click Test AI Connection . Optionally choose a Default profile , then assign separate Text/translation and Coding roles under AI Manager > Local AI . An empty role uses the default profile. Optionally configure the default language for AI-generated text inside code comments and program output, enable the extra instructions field for snippet AI actions, and set how many alternative solutions the snippet editor should request. Optionally configure the terminal-agent input-history size (default 20, range 5–100), the agent command name, case-insensitive command matching, execution target, prompt-hook usage, per-run setup dialog, debug/runtime visibility, and activity-panel preferences for AI Agent and AI Planning . Optionally disable the confirmation dialog for Summarize and Solve Problem if you want a faster workflow. Ask always opens the prompt dialog.","title": "Setup"},{"location": "features/ai-assistant.html#ai-profile-setup-wizard","text": "A guided wizard creates AI profiles with support for both local and cloud-based language models. To open the wizard: Settings > AI , click Add Profile or Edit an existing profile. Tools > AI Manager > Profiles , click Add . The wizard guides you through: Connection Type - Choose local LM Studio or a cloud provider. LM Studio Setup - If local: pick a loaded LM Studio model from the list or use Auto mode. Cloud Provider Setup - If cloud: select the provider (Anthropic Claude API, OpenAI, or other OpenAI-compatible endpoint). API Details - Enter API Key and select or enter the Model name — the model list is pre-filled with common models for the chosen provider, and Load models merges the endpoint's live model list on top. Optionally configure Reasoning effort (if the provider/model supports extended thinking). Profile Name - Enter a display name for the profile (e.g., \"Claude Opus\", \"Local LM Studio\"). Native Anthropic (Claude) API support is included alongside existing OpenAI-compatible endpoints. For fully integrated GGUF inference, use the separate AI Manager > Local Models > Setup assistant . It checks hardware, recommends Text/Coding/embedding models, reviews license and exact size, verifies the immutable download, creates an embedded profile, assigns the selected role, and runs a real local chat or embedding function test. See Local models with llama.cpp .","title": "AI Profile Setup Wizard"},{"location": "features/ai-assistant.html#model-selection","text": "The model selector in Settings > AI and Tools > AI Manager > Profiles is editable: For known cloud providers the dropdown is pre-filled with common model names for the configured endpoint, so a concrete model can be chosen without an API key. The refresh button next to the selector merges the endpoint's live /v1/models list on top when the API key is valid. Choosing a listed model stores it as the manual selection. Typing a model name stores it as the manual selection so any OpenAI-compatible endpoint works. Auto is offered only for local LM Studio endpoints, where korTTY can actually detect the loaded model. Cloud profiles need a concrete model; if none is selected, requests stop with an explicit \"select a specific AI model\" error. An Integrated llama.cpp profile lists installed model IDs from ~/.kortty/llm/models.xml ; it does not use the HTTP URL or manual cloud-model field.","title": "Model selection"},{"location": "features/ai-assistant.html#role-routing-rag-and-prompt-order","text": "korTTY classifies its own action types deterministically without asking a model. Translation, summary, problem solving, questions, prose descriptions, and AI ASCII-art pictures use the Text role; coding, completion, snippet review, security fixes, diagrams, and workflow generation use the Coding role. An explicit profile choice, security-check profile, or connection-specific profile takes precedence, followed by the role profile and then the default. When matching knowledge stores are assigned to the request's Text or Coding role, an ordinary AI request is assembled in this order: korTTY's action/output contract, selected AI Skills, bounded untrusted RAG context with [R1] source markers, the resolved model-family preset, and finally the provider transport. Only retrieved excerpts are added, not the complete knowledge store; choosing a cloud profile sends those excerpts to that provider, so the knowledge-store role/profile assignment is the explicit disclosure decision. Strict JSON and code-payload rules remain authoritative. The dedicated source-only diagram request used by snippets and workflows is a narrow exception: after its fixed output contract korTTY always adds one compact, immutable action skill for the requested diagram family (logical-structure flowchart, sequence, state, class, or ER) and omits every configurable library skill and retrieval context. For a profile with a fixed model selection, korTTY automatically sets the request-scoped Reasoning value to none only when that value is available, without changing the saved profile. The action skill is not one of the 39 shipped configurable built-in skills: users cannot disable or edit it, and it adds no matching/classification request. The Full code analysis stages that apply selected improvements and its security-fix follow-up use the same fixed-model rule because they return bounded, machine-parsed full-script replacements; the preceding analysis still uses the profile's configured Reasoning value. An Auto profile is not overridden from previously discovered capabilities because its loaded model can change; an explicitly configured none value still applies. Autonomous Agent, Planning, Swarm, and scheduled prompts require explicit RAG opt-in. See RAG knowledge stores for the retrieval and privacy details.","title": "Role routing, RAG, and prompt order"},{"location": "features/ai-assistant.html#local-lm-studio-model-selection","text": "For local LM Studio profiles, korTTY can discover currently loaded LLM model keys and exact reasoning options through LM Studio's GET /api/v1/models endpoint. A binary model that reports only off and on therefore no longer shows the Minimal, Low, Medium, High, or Extra high levels as supported merely because LM Studio would silently convert an unsupported value; an explicit none request switches this feature off, while omitting the reasoning parameter uses the model's published default. Auto mode resolves the effective model immediately before connection tests, AI chat and follow-up requests, terminal AI actions, and Terminal AI Agent runs. If exactly one LLM is loaded, korTTY uses that model. If multiple LLMs are loaded, korTTY uses the saved preferred model only when that model is currently loaded. If no LLM is loaded, or multiple LLMs are loaded without a valid saved preference, korTTY stops the request with an explicit error instead of guessing.","title": "Local LM Studio model selection"},{"location": "features/ai-assistant.html#ai-internet-access","text": "Internet access is configured per AI profile. Existing and new profiles default to Disabled . Mode Behavior Disabled No web tools or MCP integrations are sent with AI requests. KorTTY Tavily Tool korTTY adds a web_search and a web_extract tool to eligible OpenAI-compatible /v1/chat/completions requests. Tool calls are executed by korTTY: searches through POST https://api.tavily.com/search (5 results with short snippets), page reads through POST https://api.tavily.com/extract , which returns the text of one web page or online document, capped at 12,000 characters. LM Studio Tavily MCP korTTY sends an LM Studio native /api/v1/chat request with a Tavily MCP integration. Bright Data Web MCP korTTY sends an LM Studio native /api/v1/chat request with a Bright Data MCP integration. Brave Search MCP korTTY sends an LM Studio native /api/v1/chat request with a configured Brave Search MCP plugin ID. SearXNG MCP korTTY sends an LM Studio native /api/v1/chat request with a configured SearXNG MCP plugin ID. LM Studio Toolpack korTTY sends an LM Studio native /api/v1/chat request with the configured LM Studio Toolpack MCP plugin ID. Required provider configuration is entered under Settings > AI > Internet tool configuration . API keys and tokens are stored encrypted with the master password. MCP server labels and plugin IDs are stored as normal settings. Important behavior: Snippet AI, text correction, translation, snippet descriptions, alternative-solution requests, and AI ASCII-art generation do not use internet access. Direct korTTY web tools have a 5-second connect timeout, a 20-second request timeout (40 seconds for page reads), and a maximum of two web-tool rounds with up to three tool calls each per AI request. A mode can only be selected in a profile once its required global setting (for example the Tavily API key) is stored under Settings > AI > Internet tool configuration ; otherwise the AI Manager explains where the value belongs and keeps the previous mode. The profile's own API Key field is only for the AI provider itself. Internet use is shown where it happens. In the AI chat, a collapsed Internet research line above the reply summarizes the searches, page reads and failures; expanding it lists every query, read URL, source and error. It is stored with saved chats. In the Terminal AI Agent, each step that used the internet gets a Web research activity row with the same details. For LM Studio MCP modes korTTY shows the tool calls LM Studio reports, with the URLs found in their output. LM Studio MCP requests with internet access use a longer total request timeout because the MCP server runs behind LM Studio. Canceling a running request interrupts the Java HTTP request where the active provider supports interruption. Tool errors are returned to the model as structured data. If the web tool times out, fails authentication, returns no results, or reaches the tool-round limit, the model is instructed to say that explicitly and not invent web facts. For terminal-agent JSON planning, korTTY offers its direct web tools only when the user task contains a web signal word (for example current, latest, search, research, docs, changelog, CVE, website, download, version) or a URL. Local file/script review tasks should be handled by shell commands such as sed , cat , find , or test commands, not by web search. Enable AI agent: offer internet research on every step under Settings > AI > Internet tool configuration to offer the tools on every agent step and let the model decide; this uses more Tavily credits and tokens. LM Studio MCP modes always attach their integration to agent steps.","title": "AI internet access"},{"location": "features/ai-assistant.html#ai-skills","text": "AI Skills are reusable local instruction blocks that korTTY can add to AI requests. Use them for persistent preferences such as coding standards, review rules, operational policies, or language-specific style guidance. Open AI > AI Manager > AI Skills . The library moved here from the global settings dialog; Save writes it immediately, and pending edits are also saved when the AI Manager is closed.","title": "AI Skills"},{"location": "features/ai-assistant.html#skill-fields","text": "Skill name - Human-readable name shown in the skill list and activity logs. Description - Short explanation used by automatic skill matching. Tags - Comma-separated keywords used by automatic skill matching. Target - AI Chat/Functions , AI Agent , or Both . Active - Enables or disables only this skill. Skill Markdown - The instruction body sent to the model when the skill is selected.","title": "Skill fields"},{"location": "features/ai-assistant.html#controls","text": "Enable AI Skills disables or enables all skills globally. Automatically send only matching skills sends only active skills that match the current request — at most the two highest-scoring matches; skills assigned to the active connection or explicitly selected in the snippet editor's AI skills picker are added on top of that limit. When disabled, all active skills with a matching target are sent. Add creates a new active skill with target Both . Delete removes the selected skill after confirmation. Import accepts .md and .markdown files. Export writes Markdown files for the selected skill, or all skills if none is selected. The skill list can be sorted alphabetically or by enabled/disabled status.","title": "Controls"},{"location": "features/ai-assistant.html#importexport-format","text": "--- kortty-ai-skill: 1 name: My Skill description: Short purpose statement enabled: true target: BOTH tags: [linux, bash] --- Skill instructions as Markdown. Plain Markdown without korTTY frontmatter is imported with the file name as the skill name, target Both , and disabled by default so you can review it before use. Claude/Codex-style SKILL.md frontmatter with name , description , and tags is also accepted and imported disabled by default unless it is korTTY's own export format. When an AI Agent run uses one or more skills, the terminal-agent activity panel logs the selected skill names. Connection tests never send skills, so Reply with exactly OK tests remain stable.","title": "Import/export format"},{"location": "features/ai-assistant.html#terminal-selections","text": "","title": "Terminal selections"},{"location": "features/ai-assistant.html#using-ai-for-selected-text","text": "Select text in the terminal. Right-click the selected text. Open AI and choose: Summarize - Creates a concise summary of the selected output. Solve Problem - Analyzes the selected error output and suggests likely fixes. Ask - Sends the selection together with your own follow-up question or instruction. Confirm the request in the preview dialog. You can edit the selected text before sending it. For Ask , add your own prompt. The dialog also shows the estimated request tokens and projected remaining quota. The response opens in a temporary AI tab. You can continue the same context with follow-up prompts from the bottom composer field. Use Save in the AI tab to store the conversation under a custom title. Reopen saved conversations later via Tools > AI Manager or ++Ctrl+Shift+Y++ (++Cmd+Shift+Y++ on macOS).","title": "Using AI for selected text"},{"location": "features/ai-assistant.html#attaching-a-selected-file-to-the-chat","text": "When the selection looks like a single file name — for example a name from ls output, quoted if it contains spaces — korTTY offers to send that file's content along with the request, so the model can take the file into account instead of only its name. The file is resolved against the pane's current directory, over SFTP in SSH tabs and from the local filesystem in local-shell tabs, exactly like Open in Snippet Editor . The confirmation dialog shows an Attach file checkbox with the file name. It is pre-selected, and you can clear it to send only the selected text. While the file is being checked, OK waits; clear the checkbox to send without waiting. Before anything is attached, korTTY verifies that the file exists in the current directory, is a regular file, is readable with your permissions, decodes as UTF-8 text, and fits into the profile's Max characters limit (see AI settings ) together with the selected text. Binary files, oversized files, and files that fail any check are not attached; the dialog states the reason and the checkbox is disabled. When the confirmation dialog is disabled for Summarize and Solve Problem , and always for Ask Agent… (which has no preview dialog), the file is attached automatically if it passes the checks; otherwise the request is sent without it and the status bar says why. No attachment is offered after an identity switch inside the session ( su , an inner ssh ), because the file would be resolved against the wrong login — the same rule that greys out Open in Snippet Editor . The attached file is listed above the chat messages, stays in context for every follow-up prompt, and is stored with a saved chat so a reopened conversation keeps it. The Flowchart button beside the attachment asks the active profile for a Mermaid flowchart of the attached script (the same logical-structure diagram the Snippet Editor generates) and shows it as a rendered diagram in the chat. If the model's answer is unusable, korTTY draws the local structural flowchart instead.","title": "Attaching a selected file to the chat"},{"location": "features/ai-assistant.html#ai-result-tab-features","text": "The conversation transcript is read-only and not included in saved project/session state. Assistant answers display Markdown as a formatted preview: headings, bold and italic text, strikethrough, lists, quotations, and highlighted inline code. Text wraps to the available width and can be selected and copied; user messages keep their literal text. The conversation automatically scrolls to the newest user message or assistant answer, including content that grows after rendering. Scroll up with the mouse wheel, keyboard, or scrollbar to pause this behavior while reading earlier messages. Scrolling back to the bottom automatically resumes it. <think> ... </think> blocks are removed from the visible output. The toolbar lets you copy the conversation, save or rename the chat, share/export it to PDF/Markdown/plain text, retry the last request, close the tab, cancel running requests, and change the font size. The response language defaults to the current GUI language. You can change the response language and the active AI profile per chat before sending a follow-up prompt. Follow-up prompts in Summarize and Solve Problem continue as normal chat questions; they are not forced back into the original summarizing/problem-analysis prompt. Detected code blocks get their own copy button and can also be saved directly into the Snippet Manager. Blocks that contain images, diagrams, or math render as images instead — see Rendered images, diagrams, and math . Rendered markdown tables can be copied as a whole table, a single column, or a single cell. The chosen AI tab font size is stored globally and reused for future AI result tabs. Token usage is recorded per AI profile after successful requests so warnings and reset cycles remain accurate. If a saved chat references an AI profile that no longer exists, korTTY asks you to choose a replacement profile before you continue with follow-up prompts.","title": "AI result tab features"},{"location": "features/ai-assistant.html#rendered-images-diagrams-and-math","text": "AI answers that contain images, diagrams, or math formulas are rendered as images inside the chat instead of showing raw markup. This also applies to saved chats reopened from the AI Manager. Content in the AI answer Rendered as ```svg / ```xml / ```html code block (or untagged block) containing an <svg> document Inline vector image Markdown image link with a data:image/png;base64,… URI in the answer text, or a code block containing only such a data URI Inline raster image (PNG, JPEG, GIF, BMP; up to 8 MB decoded) with a copy image button ```mermaid code block Mermaid diagram (bundled library, no network) ```latex / ```tex / ```math code block, or $$ … $$ math in the answer text Typeset formula (bundled MathJax, no network) Every rendered block keeps a header with the usual copy button and a Show code / Show image toggle, so the underlying source stays one click away. While a Mermaid/math block is still rendering, the source remains visible; if rendering fails (for example a Mermaid syntax error), the block stays on the source view and the header shows the reason. plantuml / puml fences and untagged @startuml content are ordinary source-code blocks and are not rendered specially. Example prompts that produce rendered answers: Draw a simple house as an SVG image. Create a Mermaid flowchart of a typical login flow. Create a Mermaid sequence diagram for an SSH handshake. Explain the Pythagorean theorem and show the formula. A Mermaid answer fenced with the mermaid language tag and this body renders as a flowchart: flowchart TD Login-->Validate; Validate-->|ok| Session; Validate-->|fail| Error; And display math in the answer text renders as a typeset formula: $$a^2 + b^2 = c^2$$ Rendering details and requirements SVG and rendered Mermaid output is displayed with JavaScript disabled and scripts/event handlers stripped from the document. Mermaid 11.17.2 runs with its strict security level from a SHA-256-pinned local bundle; LaTeX is typeset by a separately loaded local MathJax bundle. Neither needs internet access. Chat Mermaid retains the full bundled diagram support, including flowchart, sequence, class, state, ER, mindmap, and architecture diagrams. Frontmatter, directives, network/file/data/JavaScript URLs, external images/icons, links, and click callbacks are rejected; source, edge-count, raster-size, and timeout limits protect the renderer. Full LaTeX documents ( \\documentclass ) intentionally stay code blocks; only formulas are typeset. Mermaid follows the active chat's light/dark palette; other rendered images and formulas retain a readable neutral canvas.","title": "Rendered images, diagrams, and math"},{"location": "features/ai-assistant.html#ai-manager","text": "Open AI > AI Manager or press ++Ctrl+Shift+Y++ (++Cmd+Shift+Y++ on macOS). The AI Manager is a modeless window: you can keep it open while you continue working in the main window. Invoking AI Manager again restores and focuses the same manager for that main window instead of opening a duplicate. Its active primary section remains marked by a bold accent underline after focus moves into the section's controls. The manager combines profile, local-inference, retrieval, and saved-chat management: Profiles - Create, edit, test, save, and remove AI profiles. The profile list shows the current quota/usage status for each profile. Local Models - Search Hugging Face, import/download/configure GGUF files, run the post-install function test, and start or stop several llama.cpp sidecars. Local AI - Assign Text/translation, Coding and session journal profiles, choose the embedding model and preferred runtime backend, store an encrypted Hugging Face token, and select the runtime-update policy. Knowledge Stores - Create local HNSW stores, add reviewed files or recursive folders, synchronize sources, and run a test search. Saved Chats - Open, rename, refresh, or delete previously saved AI conversations. Saved AI Swarm conversations appear in their own Swarm Chats section, including those produced by scheduled swarm jobs. Use Settings > AI for global behavior switches and AI Manager for day-to-day profile, model, knowledge-store, and chat management.","title": "AI Manager"},{"location": "features/ai-assistant.html#ask-the-manual-ai-docs-search","text": "The built-in manual ( Help > Manual , F1 ) includes an AI-powered search. Toggle AI search in the manual window's toolbar to open a side panel, type a question in natural language — for example \"How do I run the AI agent in the terminal window?\" — and press Enter . How it works: korTTY selects the most relevant sections from the bundled offline manual (no embeddings, no external search service) and sends only those excerpts together with your question to your default AI profile . The answer is generated exclusively from the manual content and is written in the app language. If the manual does not cover the question, the assistant says so instead of guessing. Answers cite their sources: click an inline citation or an entry in the Sources list to jump the manual view directly to the referenced page and section. If nothing in the manual matches the question, korTTY answers locally without contacting the AI endpoint at all. Requirements: A configured AI profile (see Setup ); the default profile is used. An unlocked master-password vault when the profile stores an encrypted API key. Data Security The question text and the selected manual excerpts are transmitted to the configured AI endpoint. The manual content itself is public documentation, but your question is free text — avoid pasting secrets into it, or use a trusted local endpoint such as LM Studio . Internet access modes are always disabled for manual questions.","title": "Ask the manual (AI docs search)"},{"location": "features/ai-assistant.html#ai-agent-and-ai-planning","text": "korTTY supports agent-style workflows for an active terminal session. SSH and local shells The agent's command-execution engine is decoupled from SSH behind an AgentCommandRunner abstraction with two backends — SSH (exec channel) and local (a fresh local process). The AI Agent and AI Planning therefore run both in SSH sessions and in local shells on Windows, macOS and Linux: commands use a native local backend (PowerShell via -EncodedCommand , cmd.exe , or POSIX /bin/sh ), the environment probe and system prompt are platform-aware so the model generates native commands, and the same approval flow applies. A local run captures the interactive shell's current directory once and uses it for the probe and every command. Local-shell limitation: no sudo /administrator elevation on Windows. The JobScheduler's headless AI-agent action stays SSH-only.","title": "AI Agent and AI Planning"},{"location": "features/ai-assistant.html#starting-the-agent","text": "AI Agent - Start from Tools > AI Agent... , from the terminal right-click menu, or with the terminal shortcut command. The agent can open in a dedicated chat tab or target the active terminal window, depending on Settings > AI . AI Planning - Start from Tools > AI Planning... , from the terminal right-click menu, or with agent-plan / agent -plan . Planning mode asks clarifying questions, proposes one or more options, creates a final plan report, and lets you start the accepted plan.","title": "Starting the agent"},{"location": "features/ai-assistant.html#activity-panel-and-placement","text": "Split-local activity panel - Terminal-targeted runs appear at the bottom of the terminal split where the run was started. Each split has its own panel, so different splits can run their own agent tasks in parallel. Panel placement - Use View > AI Agent Panel to choose At Bottom (default), Dock Left , or Dock Right . Docked mode shows the activity in a resizable side panel attached to the main window; drag its divider to resize. The placement and width are remembered across restarts. In side mode there is one outer tab per terminal of the active terminal tab, and runs are stacked vertically. Switching to another terminal tab swaps the dock to that tab's terminals. Agent status indicators - A per-terminal AI-agent status icon appears in the Dashboard tree and prefixed to the terminal's tab title: ✋ awaiting input ⚡ working ⏸ paused ✓ finished Concurrent runs - Multiple concurrent runs per split are shown as closable tabs in the activity panel (one tab per run), with a per-widget concurrency cap of 5 runs. Finished runs stay as tabs until closed. Typing while running - Typing is no longer locked while a run is active. You can continue typing in the shell prompt and launch another agent ... command (it opens a new concurrent tab). Only run-control keys are intercepted: ++Esc++ or ++Ctrl+C++ cancel the selected tab's run; ++Ctrl+R++ toggles that run's thinking details. Pause and Resume - Each run tab shows pause and cancel buttons. Pause parks the agent at a safe point between steps; paused time is excluded from the run work-time. Current directory - Terminal shortcut runs use korTTY's tracked current directory for SSH and local shells. A local run captures one stable directory for its probe and every command, so commands and generated files remain relative to where the interactive shell was working when the run began. Approvals and sudo - The agent can request explicit approval before command execution and can ask for a sudo password in the activity panel. Password input is masked, can be submitted with ++Enter++, and allows up to three wrong-password retries. If a password is cached, it is used only for the current agent/session context. When user input (sudo password / command approval) is required, the panel auto-expands. Collapsed status bar - When the panel is collapsed, it shows a compact status bar with the run prompt, state, pause/cancel buttons, and an expand button. A spinner appears while the agent is actively working, and a bold ✋ marker signals when user input is required. Keep collapsed - Use Keep collapsed to make the panel start minimized to the status bar. Terminal output - Final terminal-agent answers are written back to the terminal area so the shell transcript contains the answer. Panel controls - Use the reload button to run the selected agent command again. Copy or snippet actions are available on activity rows. Export the current run or full history as Markdown, plain text, YAML, XML, JSON, PDF, or Asciidoctor. Use Expand all to keep activity details open. Use A- and A+ to change the activity font size. Drag the resize handle to change the panel height.","title": "Activity panel and placement"},{"location": "features/ai-assistant.html#terminal-agent-shortcut-commands","text": "When terminal agent shortcuts are enabled and the terminal is at a shell prompt, korTTY intercepts these commands locally instead of sending them to the server: agent <goal> agent-ask <question> agent-plan <task> agent -plan <task> The base command name is configurable in Settings > AI . If you rename agent , korTTY derives the matching -ask and -plan commands automatically. The same settings page can make the command name case-insensitive and can disable the per-run setup dialog; when the dialog is disabled, korTTY uses the configured default profile.","title": "Terminal agent shortcut commands"},{"location": "features/ai-assistant.html#tab-completion-and-prompt-history","text": "At the shell prompt, TAB completion is enhanced for agent commands: Type the agent command name (e.g., agent ) then press ++TAB++ to offer the command variants ( agent , agent-ask , agent-plan ). Type the command plus a space (e.g., agent ) then press ++TAB++ to offer the recent agent-prompt history. Each row shows the prompt and the date/time it was last run, de-duplicated by prompt text (newest first). Each history row shows the prompt on the left and the date/time it was last run on the right. Prompts longer than 60 characters are shortened with an ellipsis; the full prompt is still inserted when the entry is selected. The history popup is resizable — drag the grip in the bottom-right corner — and remembers its size across restarts. It shows a vertical scrollbar when the history is longer than the popup. In the history popup, remove a single prompt by clicking the row's ✕ button (or pressing ++Del++ on keyboards with a forward-delete key), or remove the entire history with Clear all (a two-step confirm guards against accidental wipes). Deletions are saved immediately. Outside this context, ++TAB++ is normal shell completion. History size is configurable in Settings > AI (default 20, range 5–100).","title": "TAB completion and prompt history"},{"location": "features/ai-assistant.html#how-the-ai-agent-works","text": "The Terminal AI Agent is a controlled terminal automation workflow. It does not run arbitrary model output directly in the interactive terminal. Instead, each turn follows this pattern: korTTY probes the active SSH or local-shell session with a non-interactive command and records compact context such as current user, host, operating system, active terminal working directory, sudo availability, disk path, and recent command state. korTTY sends the user task, probe snapshot, previous command results, active AI Skills, and optionally web-tool availability to the selected AI profile. The model must return a strict JSON decision: run commands, ask for confirmation, finish, or block. korTTY validates the JSON schema and command constraints. Invalid responses are repaired once; unsafe or unsupported command decisions are rejected. korTTY runs approved commands through the active backend: SSH exec channels for SSH sessions or fresh local processes for local shells. Each command starts in the directory captured for the run. A cd inside one command does not persist to the next command. Command output is added to the activity panel and to the next model turn until the task is complete, blocked, canceled, or the turn limit is reached.","title": "How the AI Agent works"},{"location": "features/ai-assistant.html#suitable-tasks","text": "The agent is suitable for: inspecting files, directories, package state, logs, service state, and system configuration; creating or modifying scripts and configuration files when the task asks for it; running tests, syntax checks, linters, or read-only diagnostic commands; summarizing command output and explaining findings; planning multi-step operational changes before implementation. The agent is not intended for: interactive full-screen programs such as vim , nano , top , less , ssh , or commands that wait for prompts; long-running daemons or commands without clear completion; secret exfiltration or blind destructive commands; web research for local files unless you explicitly ask for external/current information.","title": "Suitable tasks"},{"location": "features/ai-assistant.html#example-commands","text": "agent show the 10 largest XML files in this directory agent update groesste_xml.pl so the -r flag searches subdirectories recursively agent check why nginx failed to start and suggest the safest fix agent-ask what user and directory am I currently using? agent-plan migrate this host from package X to package Y For local file review tasks, name the file in the task. The agent should then inspect it with shell commands such as sed -n , cat , file , or language-specific syntax checks. If an internet-enabled profile is active, korTTY still keeps web tools away from these local file planning prompts unless your task clearly asks for current or external information.","title": "Example commands"},{"location": "features/ai-assistant.html#activity-row-icons","text": "Activity rows use semantic emoji icons to indicate the action type: Icon Meaning 💾 Request or input: write/create file 📖 Action: read file/directory ▶️ Action: run/execute command 📁 Action: directory operation 📦 Context: package manager ⚙️ Context: service or system 🌐 Action: network operation 🔍 Action: inspect/analyze 💭 State: thinking/reasoning 💬 Output: message or result ✋ Required: awaiting user input (sudo/approval) ❌ State: error or failed activity 🚫 State: cancelled activity Red styling is reserved for error and failed states.","title": "Activity row icons"},{"location": "features/ai-assistant.html#safety-and-failure-handling","text": "korTTY adds guardrails around agent execution: Commands are limited per turn and must be non-interactive. Commands that change the system or require privilege can be routed through confirmation depending on settings and model decision. Sudo uses sudo -n and activity-panel password prompts. korTTY does not allow sudo -S , su , or commands that wait indefinitely for a terminal password prompt. SSH directories are tracked from shell hooks, terminal prompt context, and probe results; if a tracked remote directory no longer exists, korTTY retries the probe from the SSH default directory and reports the issue. Local runs refresh the shell process directory or use an absolute native prompt path, freeze the result for the run, and stop safely when a changed directory cannot be determined or mapped. While a terminal-targeted run is active, normal typing is still allowed; only run-control keys (++Esc++/++Ctrl+C++ to cancel the selected run, ++Ctrl+R++ to toggle its thinking details) are intercepted. Web-search failures, HTTP errors, authentication errors, empty results, and timeouts are surfaced as explicit tool errors. If the AI response does not match the required JSON schema, korTTY asks for a repair. If repair also fails, the run is blocked with an explanation.","title": "Safety and failure handling"},{"location": "features/ai-assistant.html#ai-swarm-multi-server","text": "The AI Swarm broadcasts one AI-agent task to many servers at once: each selected server gets its own agent run — including servers without an open terminal — and the per-server answers are combined into a single comparison table. The swarm tab adds an animated per-agent status strip, expandable live transcripts, pause/resume/restart/stop controls per agent and swarm-wide, AI-free script execution from the Snippet Manager, and a Schedule… button that turns the run into an unattended JobScheduler job. Open it with AI > AI Swarm... or Ctrl + Alt + S (Cmd on macOS); see the AI Swarm page for the full feature set.","title": "AI Swarm (multi-server)"},{"location": "features/ai-assistant.html#generate-workflow-script","text": "After a finished agent run completes successfully, a Workflow button turns the run into a single self-contained, reproducible script in a chosen language (Bash, Python, Perl, Ruby, PowerShell, Ansible playbook, Windows-CMD batch, or AppleScript ) with robust error handling, detailed comments, and a header (script name, creator, date/time). Script generation: Auto-loads matching AI Skills (e.g., a language-quality skill for the target language). Can produce several language variants and multiple suggestions as inline tabs. Supports header templates from the fixed non-deletable Script-Header snippet category. Optionally includes a Mermaid flowchart for the script logic. A working spinner is shown while the diagram is generated, so it is clear the AI connection is busy. Saves into the Snippet Manager with a short auto-generated name plus correct extension (de-duplicated by full name including extension). Tags the snippet as workflow for easy filtering. Auto-sets the System (OS) column from the agent's probed OS (any Linux distro becomes Linux). Internet access is forced OFF during generation. Target languages: Bash, Python, Perl, Ruby, PowerShell, Ansible, plus Windows-CMD ( .cmd batch — @echo off lead line, REM header comments, errorlevel checks) and AppleScript ( .applescript — osascript shebang, -- comments, try / on error handling). Each generated-script editor has A− / A+ buttons and supports Ctrl + mouse wheel (Cmd on macOS) to change the font size; the chosen size is remembered across sessions. The workflow dialog is resizable and remembers its size and position for future use. Clearer AI backend errors When the AI server runs out of memory or hits a resource limit (e.g. LM Studio/MLX \"Resource limit exceeded\", \"metal::malloc\"), the dialog shows a short, actionable hint instead of the raw multi-line backend stack trace; all other AI errors are collapsed to a single line.","title": "Generate Workflow Script"},{"location": "features/ai-swarm.html","text": "AI Swarm The AI Swarm broadcasts one AI-agent task to many servers at once. Each selected server gets its own independent agent run over its SSH session, and the per-server answers are combined into a single comparison table — one row per server — in a shared conversation. Open it with Tools > AI Swarm... or Ctrl + Alt + S (Cmd on macOS). The swarm opens as a regular tab, so terminals stay usable while a swarm runs. The swarm tab The tab is split into three areas: Area Purpose Status strip Animated orb per agent above the conversation — live state overview Agents One row per server with status badge, elapsed time, token count, and an expandable live transcript Conversation The shared chat: your prompt, per-run progress, and the combined answer The composer at the bottom ( Ask all selected servers… ) is a clearly framed, three-line input. Send starts a run on every selected target; follow-up prompts continue the same conversation. Selecting targets Select servers… opens a picker over your saved connections. The target summary next to it shows how many servers are selected, how many already have an open terminal ( Open: n ), and how many will run without one ( Without terminal: n ). Servers without an open terminal are fully supported : the swarm opens a background SSH session for them on demand — no terminal tab is opened or required. This needs the master-password vault to be unlocked (the stored credentials are used), and the server key is accepted on first contact, the same trust model the terminal uses for new connections. Connect missing (n) remains available as an explicit opt-in if you want terminals opened instead. Include local shell adds your local machine to the swarm (local shells always need their open tab and are excluded from headless runs). Status strip One orb per agent, colored and animated by state: State Orb Meaning Queued gray Waiting for a free slot Running blue, pulsing with an orbiting dot Agent is working; elapsed time ticks Awaiting input amber, blinking ring An approval dialog is waiting for you Paused violet with pause bars Paused via the run controls; the timer stops Unusually long blue with an amber ping ring Running far longer than its peers (see below) Done green Answer collected Failed red The run errored; details are in the agent row Cancelled / Skipped dark gray Stopped, or skipped (e.g. unsupported shell) Adaptive slow detection — an agent is flagged unusually long when its elapsed time exceeds max(60 s, 2 × median of the finished agents) ; until at least two agents have finished, a fixed 180 s threshold applies. Paused and waiting agents are never flagged, and paused time is subtracted from the elapsed time, so the comparison stays fair. While a run is active, clicking an orb scrolls to and highlights that agent's row in the Agents list; hovering shows the server name and elapsed time. Legend chips below the orbs summarize the counts (running, waiting, paused, done, failed). After the run, the strip freezes at the final state. The strip scales from a single server to large fleets — orbs shrink and pack into rows as the agent count grows: Agent rows and live transcripts Each server has a row in the Agents list showing its status badge, elapsed time, and token count. Left-click a row to expand it inline and watch the agent's live transcript (commands, output, and progress) while it runs — no extra window needed. Very long transcripts are trimmed from the front so the latest output is always visible. Right-click a row for per-agent control: Pause , Resume , Restart , and Stop apply to that agent only. Restarting one agent does not disturb the others; its answer is replaced in the combined result. Run control The toolbar offers the same four controls for the whole swarm : Pause , Resume , Restart , and Stop . Pausing is cooperative — each agent pauses at its next safe checkpoint (the badge shows Pausing… until it takes effect), and elapsed timers stop while paused. Read-only mode and approvals The Read-only checkbox keeps every agent restricted to non-mutating commands. With read-only off, the Approval policy decides how system-changing commands are confirmed: Policy Behavior One approval for all The first agent that needs a change raises one dialog; Approve on all covers every server in the run Per server Each server's changes are approved individually The approval dialog also offers Cancel swarm to stop the whole run. Combined answer and row details When all agents finish, the swarm combines the per-server answers into one Markdown comparison table with exactly one row per server. The last column is always titled \"Fehler\" and lists deviations, missing data, and errors (or - when there is nothing to report), regardless of the response language. Table cells are often too small for full command output — click any table row to open it in a separate Row details window with a readable layout, A− / A+ font-size buttons, and a copy-to-clipboard button. Conversation copy, export, and saving The conversation header has a Copy button (whole conversation to the clipboard) and an Export menu with Plain text , Markdown , and PDF . Save stores the conversation as a named swarm chat; saved swarm chats appear in a dedicated Swarm Chats section of the AI Manager and can be reopened later. Run scripts without AI Run script… executes a Snippet Manager script on all swarm targets in parallel — without any AI involvement . The dialog offers a searchable script picker (by name, category, language, or ID), a parameter field (one parameter per line), and a live summary; the Run button is the single confirmation. The script is transferred Base64-encoded (no quoting or special-character issues) and decoded on the server, with parameters passed as $1 , $2 , …. Progress appears in the same agent rows — expand a row to watch the live output — and the result is a per-server table with exit code and output. Non-POSIX shells (e.g. Windows targets) are skipped with a Skipped: shell is not POSIX note while the rest of the swarm proceeds; unreachable servers are reported as Not connected . Stop cancels a running script run. Generate multi-server workflow The Workflow button turns the current swarm task into a single reusable multi-server script via the Generate multi-server workflow dialog: choose the script language, the host-list source (selected connections, manual list, or external host file/inventory), and multi-server hardening options (parallel fan-out, per-host timeout, retry with backoff, aggregated end-of-run report, jump host, sudo/become, dry-run, and more). The dialog includes: Syntax highlighting — the generated script is shown in a full editor with highlighting for the selected language. Visible progress — a working animation with a live elapsed counter ( Generating… 0:42 ) while the AI works, and the total duration ( Done — took 1:37 ) when it finishes. Additional instructions — a three-line field for extra guidance the AI must follow, with a History menu of your last 10 distinct entries. Save to Snippets — saves the script to the Snippet Manager with a fitting, pre-filled script name and the correct file extension. Hardening options — the same per-script Hardening options as the single-host workflow generator (strict mode, error traps, idempotency, dry-run, --help , and more) are applied to the generated script automatically with their all-on defaults; this dialog shows no panel for them. They are separate from the multi-server options above. Input hardening — a collapsible Input hardening panel asks the AI to build an input-validation guard block into the generated script (parameter allowlists and length limits, file format checks, an adjustable MAX_FILE_SIZE limit, security warnings in the script's log, and a FORCE=1 / --force override). The size check uses metadata before file content is read, and 0 means unlimited. Strictly opt-in — the master check box starts unticked. Tab activity indicator The AI Swarm tab itself shows a colored status dot, so you can watch progress from any other tab: Dot Meaning Blue, pulsing Swarm is running Amber, fast pulse An agent is waiting for your input Violet, steady Swarm is paused Green, steady Run finished — stays until the next run starts Scheduling swarm runs (JobScheduler) Swarm runs can execute unattended as JobScheduler jobs using the AI Swarm action type. The Schedule… button in the swarm toolbar is the fastest path: it opens the JobScheduler with a new job pre-filled from the current tab — the selected servers, the current prompt, the AI profile, and the read-only setting. The job is created disabled so you can review the schedule before enabling it. Scheduled swarm jobs run completely headless over background SSH sessions — no terminal tabs are opened. Swarm-specific job fields: Field Description AI profile The AI profile used for all agents in the run AI prompt The task broadcast to every target server Auto-approve Approve system-changing commands without a dialog (unattended runs have nobody to ask) Swarm parallelism How many servers run concurrently (1–16, default 4) Swarm read-only Restrict all agents to non-mutating commands (default: on) Results land in two places : the job journal records the outcome per run, and the full conversation — including the combined comparison table — is stored as a saved swarm chat , so you can open it later from the AI Manager's Swarm Chats section and click through the result table like an interactive run. The scheduler's master-password and host-key gates apply as for other job types. Recommended workflow: tune interactively, then schedule Prompt quality decides result quality. Run the swarm interactively first, refine the prompt until the comparison table looks right, then click Schedule… — the tuned prompt and target list carry over into the job. Typical combinations of swarm + scheduler: Nightly fleet health report — a read-only prompt like \"Report disk usage, failed systemd units, and pending security updates\" across all production servers every night; review the combined table each morning from the AI Manager. Configuration drift detection — ask for the effective settings of a service on every host; deviations stand out in the per-server rows and the Fehler column. Patch-level inventory — collect kernel and package versions across the fleet on a weekly schedule and export the resulting table. Unattended changes A scheduled swarm with read-only off and auto-approve on changes systems without anyone watching. Keep scheduled swarms read-only unless the prompt is deliberately designed (and tested interactively) to make changes.","title": "AI Swarm"},{"location": "features/ai-swarm.html#ai-swarm","text": "The AI Swarm broadcasts one AI-agent task to many servers at once. Each selected server gets its own independent agent run over its SSH session, and the per-server answers are combined into a single comparison table — one row per server — in a shared conversation. Open it with Tools > AI Swarm... or Ctrl + Alt + S (Cmd on macOS). The swarm opens as a regular tab, so terminals stay usable while a swarm runs.","title": "AI Swarm"},{"location": "features/ai-swarm.html#the-swarm-tab","text": "The tab is split into three areas: Area Purpose Status strip Animated orb per agent above the conversation — live state overview Agents One row per server with status badge, elapsed time, token count, and an expandable live transcript Conversation The shared chat: your prompt, per-run progress, and the combined answer The composer at the bottom ( Ask all selected servers… ) is a clearly framed, three-line input. Send starts a run on every selected target; follow-up prompts continue the same conversation.","title": "The swarm tab"},{"location": "features/ai-swarm.html#selecting-targets","text": "Select servers… opens a picker over your saved connections. The target summary next to it shows how many servers are selected, how many already have an open terminal ( Open: n ), and how many will run without one ( Without terminal: n ). Servers without an open terminal are fully supported : the swarm opens a background SSH session for them on demand — no terminal tab is opened or required. This needs the master-password vault to be unlocked (the stored credentials are used), and the server key is accepted on first contact, the same trust model the terminal uses for new connections. Connect missing (n) remains available as an explicit opt-in if you want terminals opened instead. Include local shell adds your local machine to the swarm (local shells always need their open tab and are excluded from headless runs).","title": "Selecting targets"},{"location": "features/ai-swarm.html#status-strip","text": "One orb per agent, colored and animated by state: State Orb Meaning Queued gray Waiting for a free slot Running blue, pulsing with an orbiting dot Agent is working; elapsed time ticks Awaiting input amber, blinking ring An approval dialog is waiting for you Paused violet with pause bars Paused via the run controls; the timer stops Unusually long blue with an amber ping ring Running far longer than its peers (see below) Done green Answer collected Failed red The run errored; details are in the agent row Cancelled / Skipped dark gray Stopped, or skipped (e.g. unsupported shell) Adaptive slow detection — an agent is flagged unusually long when its elapsed time exceeds max(60 s, 2 × median of the finished agents) ; until at least two agents have finished, a fixed 180 s threshold applies. Paused and waiting agents are never flagged, and paused time is subtracted from the elapsed time, so the comparison stays fair. While a run is active, clicking an orb scrolls to and highlights that agent's row in the Agents list; hovering shows the server name and elapsed time. Legend chips below the orbs summarize the counts (running, waiting, paused, done, failed). After the run, the strip freezes at the final state. The strip scales from a single server to large fleets — orbs shrink and pack into rows as the agent count grows:","title": "Status strip"},{"location": "features/ai-swarm.html#agent-rows-and-live-transcripts","text": "Each server has a row in the Agents list showing its status badge, elapsed time, and token count. Left-click a row to expand it inline and watch the agent's live transcript (commands, output, and progress) while it runs — no extra window needed. Very long transcripts are trimmed from the front so the latest output is always visible. Right-click a row for per-agent control: Pause , Resume , Restart , and Stop apply to that agent only. Restarting one agent does not disturb the others; its answer is replaced in the combined result.","title": "Agent rows and live transcripts"},{"location": "features/ai-swarm.html#run-control","text": "The toolbar offers the same four controls for the whole swarm : Pause , Resume , Restart , and Stop . Pausing is cooperative — each agent pauses at its next safe checkpoint (the badge shows Pausing… until it takes effect), and elapsed timers stop while paused.","title": "Run control"},{"location": "features/ai-swarm.html#read-only-mode-and-approvals","text": "The Read-only checkbox keeps every agent restricted to non-mutating commands. With read-only off, the Approval policy decides how system-changing commands are confirmed: Policy Behavior One approval for all The first agent that needs a change raises one dialog; Approve on all covers every server in the run Per server Each server's changes are approved individually The approval dialog also offers Cancel swarm to stop the whole run.","title": "Read-only mode and approvals"},{"location": "features/ai-swarm.html#combined-answer-and-row-details","text": "When all agents finish, the swarm combines the per-server answers into one Markdown comparison table with exactly one row per server. The last column is always titled \"Fehler\" and lists deviations, missing data, and errors (or - when there is nothing to report), regardless of the response language. Table cells are often too small for full command output — click any table row to open it in a separate Row details window with a readable layout, A− / A+ font-size buttons, and a copy-to-clipboard button.","title": "Combined answer and row details"},{"location": "features/ai-swarm.html#conversation-copy-export-and-saving","text": "The conversation header has a Copy button (whole conversation to the clipboard) and an Export menu with Plain text , Markdown , and PDF . Save stores the conversation as a named swarm chat; saved swarm chats appear in a dedicated Swarm Chats section of the AI Manager and can be reopened later.","title": "Conversation copy, export, and saving"},{"location": "features/ai-swarm.html#run-scripts-without-ai","text": "Run script… executes a Snippet Manager script on all swarm targets in parallel — without any AI involvement . The dialog offers a searchable script picker (by name, category, language, or ID), a parameter field (one parameter per line), and a live summary; the Run button is the single confirmation. The script is transferred Base64-encoded (no quoting or special-character issues) and decoded on the server, with parameters passed as $1 , $2 , …. Progress appears in the same agent rows — expand a row to watch the live output — and the result is a per-server table with exit code and output. Non-POSIX shells (e.g. Windows targets) are skipped with a Skipped: shell is not POSIX note while the rest of the swarm proceeds; unreachable servers are reported as Not connected . Stop cancels a running script run.","title": "Run scripts without AI"},{"location": "features/ai-swarm.html#generate-multi-server-workflow","text": "The Workflow button turns the current swarm task into a single reusable multi-server script via the Generate multi-server workflow dialog: choose the script language, the host-list source (selected connections, manual list, or external host file/inventory), and multi-server hardening options (parallel fan-out, per-host timeout, retry with backoff, aggregated end-of-run report, jump host, sudo/become, dry-run, and more). The dialog includes: Syntax highlighting — the generated script is shown in a full editor with highlighting for the selected language. Visible progress — a working animation with a live elapsed counter ( Generating… 0:42 ) while the AI works, and the total duration ( Done — took 1:37 ) when it finishes. Additional instructions — a three-line field for extra guidance the AI must follow, with a History menu of your last 10 distinct entries. Save to Snippets — saves the script to the Snippet Manager with a fitting, pre-filled script name and the correct file extension. Hardening options — the same per-script Hardening options as the single-host workflow generator (strict mode, error traps, idempotency, dry-run, --help , and more) are applied to the generated script automatically with their all-on defaults; this dialog shows no panel for them. They are separate from the multi-server options above. Input hardening — a collapsible Input hardening panel asks the AI to build an input-validation guard block into the generated script (parameter allowlists and length limits, file format checks, an adjustable MAX_FILE_SIZE limit, security warnings in the script's log, and a FORCE=1 / --force override). The size check uses metadata before file content is read, and 0 means unlimited. Strictly opt-in — the master check box starts unticked.","title": "Generate multi-server workflow"},{"location": "features/ai-swarm.html#tab-activity-indicator","text": "The AI Swarm tab itself shows a colored status dot, so you can watch progress from any other tab: Dot Meaning Blue, pulsing Swarm is running Amber, fast pulse An agent is waiting for your input Violet, steady Swarm is paused Green, steady Run finished — stays until the next run starts","title": "Tab activity indicator"},{"location": "features/ai-swarm.html#scheduling-swarm-runs-jobscheduler","text": "Swarm runs can execute unattended as JobScheduler jobs using the AI Swarm action type. The Schedule… button in the swarm toolbar is the fastest path: it opens the JobScheduler with a new job pre-filled from the current tab — the selected servers, the current prompt, the AI profile, and the read-only setting. The job is created disabled so you can review the schedule before enabling it. Scheduled swarm jobs run completely headless over background SSH sessions — no terminal tabs are opened. Swarm-specific job fields: Field Description AI profile The AI profile used for all agents in the run AI prompt The task broadcast to every target server Auto-approve Approve system-changing commands without a dialog (unattended runs have nobody to ask) Swarm parallelism How many servers run concurrently (1–16, default 4) Swarm read-only Restrict all agents to non-mutating commands (default: on) Results land in two places : the job journal records the outcome per run, and the full conversation — including the combined comparison table — is stored as a saved swarm chat , so you can open it later from the AI Manager's Swarm Chats section and click through the result table like an interactive run. The scheduler's master-password and host-key gates apply as for other job types. Recommended workflow: tune interactively, then schedule Prompt quality decides result quality. Run the swarm interactively first, refine the prompt until the comparison table looks right, then click Schedule… — the tuned prompt and target list carry over into the job. Typical combinations of swarm + scheduler: Nightly fleet health report — a read-only prompt like \"Report disk usage, failed systemd units, and pending security updates\" across all production servers every night; review the combined table each morning from the AI Manager. Configuration drift detection — ask for the effective settings of a service on every host; deviations stand out in the per-server rows and the Fehler column. Patch-level inventory — collect kernel and package versions across the fleet on a weekly schedule and export the resulting table. Unattended changes A scheduled swarm with read-only off and auto-approve on changes systems without anyone watching. Keep scheduled swarms read-only unless the prompt is deliberately designed (and tested interactively) to make changes.","title": "Scheduling swarm runs (JobScheduler)"},{"location": "features/ai-tools.html","text": "Terminal AI agent & tools korTTY's Terminal AI Agent is a controlled automation workflow that enables safe, intelligent command execution on remote servers — and, since the execution engine was decoupled behind an AgentCommandRunner abstraction (SSH exec-channel and local-process backends), in local shells on Windows, macOS and Linux too. Unlike naive automation, the agent probes the session state, reasons about each step, and waits for human approval before executing system-changing commands. SSH vs. local shells In local shells, commands use a native local backend (PowerShell via -EncodedCommand , cmd.exe , or POSIX /bin/sh ) and the environment probe and system prompt are platform-aware. The agent captures the interactive shell's current directory when a run starts and uses that same snapshot for its probe and every command in the run. In the Flatpak package, these processes run on the host through flatpak-spawn --host rather than inside the Freedesktop runtime sandbox. Local-shell limitations: no sudo /administrator elevation on Windows. The JobScheduler's headless AI-agent action stays SSH-only. RAG is explicit for autonomous work Knowledge stores assigned to normal Text/Coding requests do not automatically expand the Terminal AI Agent, Planning, Swarm, or scheduled job context. Those autonomous flows require an explicit RAG opt-in; see RAG knowledge stores . This prevents a background workflow from reading indexed local sources merely because the same model profile is used for interactive chat. Command variants The Terminal AI Agent is triggered via shortcut commands at the shell prompt. When enabled in Settings > AI , KorTTY intercepts these commands locally instead of sending them to the server: agent <goal> agent-ask <question> agent-plan <task> agent -plan <task> The base command name is configurable in Settings > AI . If you rename agent , KorTTY automatically derives the matching -ask and -plan commands. The same settings page can: Make the command name case-insensitive Disable the per-run setup dialog (uses the configured default profile when disabled) Note KorTTY recognizes these shortcuts in the connector input path before normal shell execution. Keyboard input and clipboard paste are assembled from the same byte stream, so pasted file names and Unicode text are included in the request, the complete prompt is saved in agent history, and one Enter produces exactly one agent run. Command purposes agent <goal> — Execute safe terminal commands to accomplish a goal. The agent inspects the session, plans non-interactive commands, requests approval when needed, and (by default) writes the final answer back to the terminal. The run dialog offers a Show final answer in the terminal too option: leave it on to mirror the answer into the terminal, or turn it off so the answer stays only in the AI agent activity panel. Either way the commands still run and their output and any files they create remain on the server; only the display of the final answer text changes. The last choice is remembered as the default. agent-ask <question> — Get a non-executing answer about the current session context without running any commands. When started from the terminal right-click menu ( AI → Ask AI Agent ) with text selected, the selection is sent along as context, so the question is answered about the selected output or script. agent-plan <task> / agent -plan <task> — Enter planning mode first. The agent asks clarifying questions, proposes approaches, generates a final plan, and runs implementation only after you click Implement . Examples agent show the 10 largest XML files in this directory agent update groesste_xml.pl so the -r flag searches subdirectories recursively agent check why nginx failed to start and suggest the safest fix agent-ask what user and directory am I currently using? agent-plan migrate this host from package X to package Y TAB completion and prompt history At the shell prompt, TAB completion is enhanced for agent commands: Type the agent command name (e.g., agent ) then press Tab to show command variants ( agent , agent-ask , agent-plan ). Type the command + a space (e.g., agent ) then press Tab to show recent agent-prompt history. Each row displays the prompt and the date/time it was last run, de-duplicated by prompt text (newest first). Prompts longer than 60 characters are shortened with an ellipsis for readability; the full prompt is still inserted when selected. The history popup is resizable — drag the grip in the bottom-right corner — and remembers its size across restarts. It shows a vertical scrollbar when the history exceeds the popup height. In the history popup, click a row's ✕ button (or press Del on keyboards with a forward-delete key) to remove a single prompt, or use Clear all (two-step confirm) to wipe the entire history. Deletions are saved immediately. Outside this context, Tab remains normal shell completion. History size is configurable in Settings > AI (default 20, range 5–100). How the AI Agent works The Terminal AI Agent follows a strict, safe execution loop: Probe the session — KorTTY probes the active terminal session with a non-interactive command and records compact context: current user, host, operating system, active terminal working directory, sudo availability, disk path, and recent command state. Send context to the model — KorTTY sends the user task, probe snapshot, previous command results, active AI Skills, and optionally web-tool availability to the selected AI profile. Model returns a JSON decision — The model must return a strict JSON response: run commands, ask for confirmation, finish, or block. Validate the decision — KorTTY validates the JSON schema and command constraints. Invalid responses are repaired once; unsafe or unsupported decisions are rejected. Execute approved commands — KorTTY runs approved commands through the active backend: SSH exec channels for SSH sessions, or a fresh local process for local shells. Each command starts in the tracked active terminal directory captured for this run. A cd inside a one-shot command does not persist to the next command, while an interactive cd completed before the run is included in the captured directory. Iterate or conclude — Command output is added to the activity panel and to the next model turn until the task completes, is blocked, cancelled, or the turn limit is reached (8 turns maximum). Suitable tasks Inspecting files, directories, package state, logs, service state, and system configuration Creating or modifying scripts and configuration files when the task asks for it Running tests, syntax checks, linters, or read-only diagnostic commands Summarizing command output and explaining findings Planning multi-step operational changes before implementation Not suitable for Interactive full-screen programs such as vim , nano , top , less , ssh , or commands that wait for prompts Long-running daemons or commands without clear completion Secret exfiltration or blind destructive commands Web research for local files unless the user explicitly asks for external/current information Tip For local file review tasks, name the file in your prompt. The agent should then inspect it with shell commands such as sed -n , cat , file , or language-specific syntax checks. When an internet-enabled profile is active, KorTTY still keeps web tools away from local file planning unless your task clearly asks for current or external information. Activity panel Terminal-targeted agent runs use an inline activity panel at the bottom of the current terminal split. Panel features Run tabs — Multiple concurrent runs appear as closable tabs. Click a tab to select it; only the selected tab's run is controlled by run-control keys and buttons. Up to 5 concurrent runs per split. Controls — Each run has reload, pause/resume, cancel buttons, and per-row copy/snippet actions. The reload button re-runs the command with the currently active AI profile, so switching profiles between runs takes effect on the rerun. Details — The panel shows the user prompt in a two-line scrollable field, agent messages, read/run actions, task timing, reported token usage, semantic activity markers, and collapsible details. AI profile row — Every run log starts with an AI profile: <name> (<model>) entry, so the protocol records which profile and model produced the run. Model reasoning — Expanding a 💭 thinking row shows the model's full reasoning when the provider exposes it (Anthropic extended thinking when the profile's Reasoning effort is enabled, OpenAI-compatible reasoning_content , LM Studio reasoning output, or <think> blocks from local CLI models). Models without exposed reasoning keep the short decision summary. Status bar — When collapsed, the panel shows a compact status bar with the run prompt, state, pause/cancel buttons, and expand button. A spinner shows while the agent is working; a bold ✋ marker signals when user input is required. Keep collapsed — Use Keep collapsed to make the panel start minimized and stay collapsed when new activity or input prompts arrive. You can still expand manually. Resizing — Drag the resize handle to change panel height. Enable Remember size to persist height and font size across application restarts. Parallel splits — Different splits can run their own agent tasks in parallel; each split has its own activity panel. Font controls — Use A− and A+ to change the activity font size. Export — Save the current run or all panel-history runs as Markdown, plain text, YAML, XML, JSON, PDF, or Asciidoctor. Exports include the AI profile, model/LLM, reasoning status, run timestamps, total runtime, per-activity runtimes, token usage, and detail text. Expand all — Keep activity details open instead of compacting them. This option is saved globally. Panel placement Use View → AI Agent Panel to choose where the activity panel appears: At Bottom (default) — The activity panel appears below the terminal split where the run was started. Dock Left / Dock Right — The panel appears as a resizable side panel docked to the main window (like the file browser). In side mode there is one outer tab per terminal of the active tab, with runs stacked vertically. Dragging the divider resizes the dock; placement and width are remembered across restarts. Switching to another terminal tab swaps the dock to that tab's terminals. Status indicators A per-terminal AI-agent status icon appears in the Dashboard and prefixed to the terminal tab title, aggregated across that terminal's runs and refreshed about once a second: ✋ — Awaiting user input (approval or sudo password) ⚡ — Working (agent is actively planning or running commands) ⏸ — Paused (run is parked at a safe checkpoint) ✓ — Finished (run completed successfully) Activity icons Activity rows use semantic emoji icons (static, non-blinking): Icon Meaning 💾 Request or input: write/create file 📖 Action: read file/directory ▶️ Action: run/execute command 📁 Action: directory operation 📦 Context: package manager ⚙️ Context: service or system 🌐 Action: network operation 🔍 Action: inspect/analyze 💭 State: thinking/reasoning 💬 Output: message or result ✋ Required: awaiting user input (sudo/approval) ❌ State: error or failed activity 🚫 State: cancelled activity Red styling is reserved for error and failed states. Safety and failure handling KorTTY enforces multiple guardrails around agent execution: Command limits Per-turn limit — A maximum of 3 commands per turn prevents runaway automation. Non-interactive only — The agent rejects interactive commands (like vim , less , su ) that would hang in a non-interactive command backend. Mutating-command detection — Commands that change the system ( chmod , rm , mv , mkdir , etc.) are flagged for confirmation unless the agent has auto-approval bypass. Privilege handling Sudo detection — The agent detects whether the current user has sudo access and whether a password is required. Passwordless sudo — When sudo -n (no-password sudo) is available, the agent uses it without prompting. Password prompts — When a password is required, the agent requests it via an activity-panel password input field (masked, not echoed to the terminal). Password retries — Up to 3 wrong-password retries are allowed before the run cancels. No sudo -S — KorTTY does not allow sudo -S (stdin password) or other interactive password methods in automated runs. Session caching — Sudo passwords can be cached for the current session so you don't re-enter them for every command in the same run. Approval gates Mutating commands — By default, the agent asks for approval before running commands that modify the system (unless auto-approval is enabled in settings). Confirmation dialog — The approval dialog shows the planned commands and their purposes. You can approve once, approve all remaining commands in the run, or cancel. Sudo preflight — After accepting a planning report, if the plan requires sudo and your SSH session requires a sudo password, KorTTY performs the preflight check immediately after acceptance so execution is not interrupted later. Directory tracking Active directory — Terminal shortcut runs use KorTTY's tracked current directory for both SSH and local shells. For a local run, korTTY refreshes the shell process directory on macOS/Linux, accepts an existing absolute path from a native PowerShell/cmd prompt, then uses the last trusted directory or the connection's start directory only when no directory-changing command made that fallback stale. Stable run snapshot — The directory is captured outside the JavaFX thread when the run begins. The environment probe and all commands in that run use the same snapshot even if the interactive shell changes directory later. Unknown or foreign directory — After cd , pushd , popd , or Set-Location , an unknown directory is an error rather than a silent fallback. WSL, Git Bash, Cygwin, and custom commands remain best-effort on Windows; if their path namespace cannot be mapped to the local filesystem, korTTY stops the action instead of targeting a same-named file elsewhere. SSH directory loss — If a tracked remote directory no longer exists, korTTY retries the probe from the SSH default directory and reports the issue. Typing during execution Not locked — While a terminal-targeted run is active, normal typing is still allowed. You can keep typing in the shell prompt and launch new agent commands (they open new tabs). Run-control keys only — Only run-control keys are intercepted: Esc or Ctrl + C — Cancel the selected run's tab Ctrl + R — Toggle the selected run's thinking details Web tools Explicit failures — Web-search failures, HTTP errors, authentication errors, empty results, and timeouts are surfaced as explicit tool errors instead of the model making up facts. JSON schema repair One repair attempt — If the AI response does not match the required JSON schema, KorTTY asks for a repair. If repair also fails, the run is blocked with an explanation. Generate Workflow Script After a finished agent run completes successfully, a Workflow button converts the run into a single self-contained, reproducible script in a chosen language (Bash, Python, Perl, Ruby, PowerShell, Ansible playbook, Windows-CMD batch, or AppleScript ) with robust error handling, detailed comments, and a deterministic metadata header (script name, creator, date/time). For fleet-wide tasks, the AI Swarm tab has its own Generate multi-server workflow dialog that additionally handles host lists and multi-server hardening options, shows the generated script with syntax highlighting and a live elapsed counter, keeps a history of additional instructions, and saves to Snippets with a pre-filled name. Script generation capabilities Auto-load matching AI Skills — Skills like language-quality guidelines for the target language are automatically included. Hardening options — A collapsible panel of production-quality techniques (strict mode, error traps, meaningful exit codes, logging, idempotency, dry-run, --help , and more) that are baked into the generated script. All are on by default; untick any you don't want. See Hardening options for what each one means. Input hardening — A second collapsible panel that asks the AI to build an input-validation guard block into the generated script: parameter allowlists and length limits, file format checks, a maximum input-file size controlled by the adjustable MAX_FILE_SIZE script variable, security warnings in the script's log, and a FORCE=1 / --force override. The guard checks file metadata before it reads content, and 0 means unlimited. Strictly opt-in — the master check box starts unticked. See Input hardening . Eight target languages — Bash, Python, Perl, Ruby, PowerShell, Ansible, plus Windows-CMD ( .cmd batch — @echo off , REM headers, errorlevel checks) and AppleScript ( .applescript — osascript shebang, -- comments, try / on error ). Multiple language variants — Generate several language variants and suggestions as inline tabs within the workflow dialog. Adjustable font size — Each generated-script editor has A− / A+ buttons and supports Ctrl + mouse wheel (Cmd on macOS); the chosen size is remembered across sessions. Header templates — Use reusable headers from the fixed non-deletable Script-Header snippet category. Mermaid diagram — Optionally include a Mermaid flowchart depicting the script logic. Its dedicated source-only request omits AI Skills and knowledge-store excerpts and sets the request-scoped Reasoning value to none only when the provider advertised that value; the saved profile remains unchanged. A working spinner is shown while the diagram is generated. Snippet Manager — Save the generated script into the Snippet Manager with a short auto-generated name and correct file extension. Scripts are de-duplicated by full name including extension. Workflow tagging — The snippet is tagged as workflow for easy filtering. OS detection — The System (OS) column is auto-set from the agent's probed OS (any Linux distro → Linux). No internet access — Internet access is forced OFF during generation regardless of the profile's internet mode. Resizable dialog — The workflow dialog remembers its size and position for future use. Terminal-targeted behavior Current working directory The agent tracks the current directory for the active SSH or local-shell session. Local runs freeze the best trusted directory once at run start and use it for the probe and every command, so generated files are created where the interactive shell was working when the run began. Input requirements When user input is required (sudo password or command approval): Auto-expand — The activity panel auto-expands and selects the run that needs input. Masked input — Sudo password input is masked in the activity panel. Submit — Press Enter or click the submit button to send the password. Up to 3 retries — Wrong passwords can be retried up to 3 times before the run cancels. Bold badge — The run tab shows a bold ✋ \"input required\" badge while waiting. Terminal output Final terminal-agent answers are written back to the terminal area so the shell transcript contains the answer, not only the activity panel state. Transcripts Dedicated agent and planning tabs can copy and save their transcript for later review or sharing. Running commands To start an agent task: At a shell prompt in an active SSH or local-shell terminal, type your agent command: agent show the 10 largest files in this directory The agent probes the session — It immediately collects the current user, OS, working directory, and sudo status. For planning mode , the agent asks clarifying questions and proposes approaches before you approve: Answer the questions or propose your own approach Review the final plan Click Implement to start execution The activity panel appears at the bottom of the split showing: The user prompt Each decision made (which commands to run) Real-time command output as it runs Any approval or password requests Approvals — When the agent needs approval to run system-changing commands, click Approve once (just this set) or Approve always (all remaining sets in the run), or click Cancel . Sudo password — When prompted, enter your sudo password. It can optionally be cached for the session. The run completes when the task succeeds, is blocked by an error, or is cancelled by the user. After completion , a Workflow button appears if you'd like to convert the successful run into a reusable script. Keyboard shortcuts Shortcut Action Tab at agent command Show agent , agent-ask , agent-plan variants Tab after agent Show recent agent-prompt history Esc or Ctrl + C during run Cancel the selected run's tab Ctrl + R during run Toggle thinking details for the selected run Activity panel ⏸ button Pause the selected run at a safe checkpoint Activity panel ▶️ button Resume a paused run Planning mode Planning mode ( agent-plan or agent -plan ) offers an advisory workflow before execution: Clarifying questions — The agent asks follow-up questions to understand your requirements. Propose options — Based on your answers, the agent suggests one or more approaches with feasibility, risks, and prerequisites. Final plan — After you select an approach, the agent generates a detailed final plan with steps, success criteria, and risks. Implement — Only after you review and click Implement does execution begin. Sudo preflight — If the plan requires sudo and a password is needed, KorTTY checks the password immediately so execution is not interrupted later. Settings and configuration Configure the Terminal AI Agent in Settings > AI : Setting Effect AI Agent enabled Enable or disable the agent feature globally Agent command name The base command (default: agent ); other variants are derived automatically Case-insensitive Make command matching case-insensitive Execution target Choose whether agent runs open in dedicated tabs or inline at the terminal split Per-run setup dialog Show a setup dialog before each run (disabled uses the default profile) Input history size Number of recent prompts to remember (5–100, default 20) Default profile The AI profile used when the setup dialog is disabled Activity panel placement Choose At Bottom or Dock Left/Right Keep collapsed preference Start the panel minimized and keep it collapsed during execution AI Skills AI Skills are reusable local instruction blocks that the agent can use. For agent runs, enable or disable skills globally, or let KorTTY automatically match only relevant skills to the task. Skills can cover: Operational policies and standards Security best practices Language-specific coding guidelines System administration conventions See the AI Skills section in the main AI documentation for setup and management details. Internet access The agent can optionally use web tools when the task clearly requires current or external information: Disabled (default) — No web tools or MCP integrations are sent with agent requests. KorTTY Tavily Tool — Direct web search via Tavily API. LM Studio MCP modes — Integrate Tavily, Bright Data, Brave Search, SearXNG, or LM Studio Toolpack through LM Studio's native MCP support. Configure internet access per AI profile in Settings > AI > Internet access . Warning Web tools are withheld from local file/script review tasks unless your prompt clearly asks for current or external information. Inspecting a local file should use shell commands like sed , cat , find , or language-specific tools, not web search.","title": "Terminal AI agent & tools"},{"location": "features/ai-tools.html#terminal-ai-agent-tools","text": "korTTY's Terminal AI Agent is a controlled automation workflow that enables safe, intelligent command execution on remote servers — and, since the execution engine was decoupled behind an AgentCommandRunner abstraction (SSH exec-channel and local-process backends), in local shells on Windows, macOS and Linux too. Unlike naive automation, the agent probes the session state, reasons about each step, and waits for human approval before executing system-changing commands. SSH vs. local shells In local shells, commands use a native local backend (PowerShell via -EncodedCommand , cmd.exe , or POSIX /bin/sh ) and the environment probe and system prompt are platform-aware. The agent captures the interactive shell's current directory when a run starts and uses that same snapshot for its probe and every command in the run. In the Flatpak package, these processes run on the host through flatpak-spawn --host rather than inside the Freedesktop runtime sandbox. Local-shell limitations: no sudo /administrator elevation on Windows. The JobScheduler's headless AI-agent action stays SSH-only. RAG is explicit for autonomous work Knowledge stores assigned to normal Text/Coding requests do not automatically expand the Terminal AI Agent, Planning, Swarm, or scheduled job context. Those autonomous flows require an explicit RAG opt-in; see RAG knowledge stores . This prevents a background workflow from reading indexed local sources merely because the same model profile is used for interactive chat.","title": "Terminal AI agent &amp; tools"},{"location": "features/ai-tools.html#command-variants","text": "The Terminal AI Agent is triggered via shortcut commands at the shell prompt. When enabled in Settings > AI , KorTTY intercepts these commands locally instead of sending them to the server: agent <goal> agent-ask <question> agent-plan <task> agent -plan <task> The base command name is configurable in Settings > AI . If you rename agent , KorTTY automatically derives the matching -ask and -plan commands. The same settings page can: Make the command name case-insensitive Disable the per-run setup dialog (uses the configured default profile when disabled) Note KorTTY recognizes these shortcuts in the connector input path before normal shell execution. Keyboard input and clipboard paste are assembled from the same byte stream, so pasted file names and Unicode text are included in the request, the complete prompt is saved in agent history, and one Enter produces exactly one agent run.","title": "Command variants"},{"location": "features/ai-tools.html#command-purposes","text": "agent <goal> — Execute safe terminal commands to accomplish a goal. The agent inspects the session, plans non-interactive commands, requests approval when needed, and (by default) writes the final answer back to the terminal. The run dialog offers a Show final answer in the terminal too option: leave it on to mirror the answer into the terminal, or turn it off so the answer stays only in the AI agent activity panel. Either way the commands still run and their output and any files they create remain on the server; only the display of the final answer text changes. The last choice is remembered as the default. agent-ask <question> — Get a non-executing answer about the current session context without running any commands. When started from the terminal right-click menu ( AI → Ask AI Agent ) with text selected, the selection is sent along as context, so the question is answered about the selected output or script. agent-plan <task> / agent -plan <task> — Enter planning mode first. The agent asks clarifying questions, proposes approaches, generates a final plan, and runs implementation only after you click Implement .","title": "Command purposes"},{"location": "features/ai-tools.html#examples","text": "agent show the 10 largest XML files in this directory agent update groesste_xml.pl so the -r flag searches subdirectories recursively agent check why nginx failed to start and suggest the safest fix agent-ask what user and directory am I currently using? agent-plan migrate this host from package X to package Y","title": "Examples"},{"location": "features/ai-tools.html#tab-completion-and-prompt-history","text": "At the shell prompt, TAB completion is enhanced for agent commands: Type the agent command name (e.g., agent ) then press Tab to show command variants ( agent , agent-ask , agent-plan ). Type the command + a space (e.g., agent ) then press Tab to show recent agent-prompt history. Each row displays the prompt and the date/time it was last run, de-duplicated by prompt text (newest first). Prompts longer than 60 characters are shortened with an ellipsis for readability; the full prompt is still inserted when selected. The history popup is resizable — drag the grip in the bottom-right corner — and remembers its size across restarts. It shows a vertical scrollbar when the history exceeds the popup height. In the history popup, click a row's ✕ button (or press Del on keyboards with a forward-delete key) to remove a single prompt, or use Clear all (two-step confirm) to wipe the entire history. Deletions are saved immediately. Outside this context, Tab remains normal shell completion. History size is configurable in Settings > AI (default 20, range 5–100).","title": "TAB completion and prompt history"},{"location": "features/ai-tools.html#how-the-ai-agent-works","text": "The Terminal AI Agent follows a strict, safe execution loop: Probe the session — KorTTY probes the active terminal session with a non-interactive command and records compact context: current user, host, operating system, active terminal working directory, sudo availability, disk path, and recent command state. Send context to the model — KorTTY sends the user task, probe snapshot, previous command results, active AI Skills, and optionally web-tool availability to the selected AI profile. Model returns a JSON decision — The model must return a strict JSON response: run commands, ask for confirmation, finish, or block. Validate the decision — KorTTY validates the JSON schema and command constraints. Invalid responses are repaired once; unsafe or unsupported decisions are rejected. Execute approved commands — KorTTY runs approved commands through the active backend: SSH exec channels for SSH sessions, or a fresh local process for local shells. Each command starts in the tracked active terminal directory captured for this run. A cd inside a one-shot command does not persist to the next command, while an interactive cd completed before the run is included in the captured directory. Iterate or conclude — Command output is added to the activity panel and to the next model turn until the task completes, is blocked, cancelled, or the turn limit is reached (8 turns maximum).","title": "How the AI Agent works"},{"location": "features/ai-tools.html#suitable-tasks","text": "Inspecting files, directories, package state, logs, service state, and system configuration Creating or modifying scripts and configuration files when the task asks for it Running tests, syntax checks, linters, or read-only diagnostic commands Summarizing command output and explaining findings Planning multi-step operational changes before implementation","title": "Suitable tasks"},{"location": "features/ai-tools.html#not-suitable-for","text": "Interactive full-screen programs such as vim , nano , top , less , ssh , or commands that wait for prompts Long-running daemons or commands without clear completion Secret exfiltration or blind destructive commands Web research for local files unless the user explicitly asks for external/current information Tip For local file review tasks, name the file in your prompt. The agent should then inspect it with shell commands such as sed -n , cat , file , or language-specific syntax checks. When an internet-enabled profile is active, KorTTY still keeps web tools away from local file planning unless your task clearly asks for current or external information.","title": "Not suitable for"},{"location": "features/ai-tools.html#activity-panel","text": "Terminal-targeted agent runs use an inline activity panel at the bottom of the current terminal split.","title": "Activity panel"},{"location": "features/ai-tools.html#panel-features","text": "Run tabs — Multiple concurrent runs appear as closable tabs. Click a tab to select it; only the selected tab's run is controlled by run-control keys and buttons. Up to 5 concurrent runs per split. Controls — Each run has reload, pause/resume, cancel buttons, and per-row copy/snippet actions. The reload button re-runs the command with the currently active AI profile, so switching profiles between runs takes effect on the rerun. Details — The panel shows the user prompt in a two-line scrollable field, agent messages, read/run actions, task timing, reported token usage, semantic activity markers, and collapsible details. AI profile row — Every run log starts with an AI profile: <name> (<model>) entry, so the protocol records which profile and model produced the run. Model reasoning — Expanding a 💭 thinking row shows the model's full reasoning when the provider exposes it (Anthropic extended thinking when the profile's Reasoning effort is enabled, OpenAI-compatible reasoning_content , LM Studio reasoning output, or <think> blocks from local CLI models). Models without exposed reasoning keep the short decision summary. Status bar — When collapsed, the panel shows a compact status bar with the run prompt, state, pause/cancel buttons, and expand button. A spinner shows while the agent is working; a bold ✋ marker signals when user input is required. Keep collapsed — Use Keep collapsed to make the panel start minimized and stay collapsed when new activity or input prompts arrive. You can still expand manually. Resizing — Drag the resize handle to change panel height. Enable Remember size to persist height and font size across application restarts. Parallel splits — Different splits can run their own agent tasks in parallel; each split has its own activity panel. Font controls — Use A− and A+ to change the activity font size. Export — Save the current run or all panel-history runs as Markdown, plain text, YAML, XML, JSON, PDF, or Asciidoctor. Exports include the AI profile, model/LLM, reasoning status, run timestamps, total runtime, per-activity runtimes, token usage, and detail text. Expand all — Keep activity details open instead of compacting them. This option is saved globally.","title": "Panel features"},{"location": "features/ai-tools.html#panel-placement","text": "Use View → AI Agent Panel to choose where the activity panel appears: At Bottom (default) — The activity panel appears below the terminal split where the run was started. Dock Left / Dock Right — The panel appears as a resizable side panel docked to the main window (like the file browser). In side mode there is one outer tab per terminal of the active tab, with runs stacked vertically. Dragging the divider resizes the dock; placement and width are remembered across restarts. Switching to another terminal tab swaps the dock to that tab's terminals.","title": "Panel placement"},{"location": "features/ai-tools.html#status-indicators","text": "A per-terminal AI-agent status icon appears in the Dashboard and prefixed to the terminal tab title, aggregated across that terminal's runs and refreshed about once a second: ✋ — Awaiting user input (approval or sudo password) ⚡ — Working (agent is actively planning or running commands) ⏸ — Paused (run is parked at a safe checkpoint) ✓ — Finished (run completed successfully)","title": "Status indicators"},{"location": "features/ai-tools.html#activity-icons","text": "Activity rows use semantic emoji icons (static, non-blinking): Icon Meaning 💾 Request or input: write/create file 📖 Action: read file/directory ▶️ Action: run/execute command 📁 Action: directory operation 📦 Context: package manager ⚙️ Context: service or system 🌐 Action: network operation 🔍 Action: inspect/analyze 💭 State: thinking/reasoning 💬 Output: message or result ✋ Required: awaiting user input (sudo/approval) ❌ State: error or failed activity 🚫 State: cancelled activity Red styling is reserved for error and failed states.","title": "Activity icons"},{"location": "features/ai-tools.html#safety-and-failure-handling","text": "KorTTY enforces multiple guardrails around agent execution:","title": "Safety and failure handling"},{"location": "features/ai-tools.html#command-limits","text": "Per-turn limit — A maximum of 3 commands per turn prevents runaway automation. Non-interactive only — The agent rejects interactive commands (like vim , less , su ) that would hang in a non-interactive command backend. Mutating-command detection — Commands that change the system ( chmod , rm , mv , mkdir , etc.) are flagged for confirmation unless the agent has auto-approval bypass.","title": "Command limits"},{"location": "features/ai-tools.html#privilege-handling","text": "Sudo detection — The agent detects whether the current user has sudo access and whether a password is required. Passwordless sudo — When sudo -n (no-password sudo) is available, the agent uses it without prompting. Password prompts — When a password is required, the agent requests it via an activity-panel password input field (masked, not echoed to the terminal). Password retries — Up to 3 wrong-password retries are allowed before the run cancels. No sudo -S — KorTTY does not allow sudo -S (stdin password) or other interactive password methods in automated runs. Session caching — Sudo passwords can be cached for the current session so you don't re-enter them for every command in the same run.","title": "Privilege handling"},{"location": "features/ai-tools.html#approval-gates","text": "Mutating commands — By default, the agent asks for approval before running commands that modify the system (unless auto-approval is enabled in settings). Confirmation dialog — The approval dialog shows the planned commands and their purposes. You can approve once, approve all remaining commands in the run, or cancel. Sudo preflight — After accepting a planning report, if the plan requires sudo and your SSH session requires a sudo password, KorTTY performs the preflight check immediately after acceptance so execution is not interrupted later.","title": "Approval gates"},{"location": "features/ai-tools.html#directory-tracking","text": "Active directory — Terminal shortcut runs use KorTTY's tracked current directory for both SSH and local shells. For a local run, korTTY refreshes the shell process directory on macOS/Linux, accepts an existing absolute path from a native PowerShell/cmd prompt, then uses the last trusted directory or the connection's start directory only when no directory-changing command made that fallback stale. Stable run snapshot — The directory is captured outside the JavaFX thread when the run begins. The environment probe and all commands in that run use the same snapshot even if the interactive shell changes directory later. Unknown or foreign directory — After cd , pushd , popd , or Set-Location , an unknown directory is an error rather than a silent fallback. WSL, Git Bash, Cygwin, and custom commands remain best-effort on Windows; if their path namespace cannot be mapped to the local filesystem, korTTY stops the action instead of targeting a same-named file elsewhere. SSH directory loss — If a tracked remote directory no longer exists, korTTY retries the probe from the SSH default directory and reports the issue.","title": "Directory tracking"},{"location": "features/ai-tools.html#typing-during-execution","text": "Not locked — While a terminal-targeted run is active, normal typing is still allowed. You can keep typing in the shell prompt and launch new agent commands (they open new tabs). Run-control keys only — Only run-control keys are intercepted: Esc or Ctrl + C — Cancel the selected run's tab Ctrl + R — Toggle the selected run's thinking details","title": "Typing during execution"},{"location": "features/ai-tools.html#web-tools","text": "Explicit failures — Web-search failures, HTTP errors, authentication errors, empty results, and timeouts are surfaced as explicit tool errors instead of the model making up facts.","title": "Web tools"},{"location": "features/ai-tools.html#json-schema-repair","text": "One repair attempt — If the AI response does not match the required JSON schema, KorTTY asks for a repair. If repair also fails, the run is blocked with an explanation.","title": "JSON schema repair"},{"location": "features/ai-tools.html#generate-workflow-script","text": "After a finished agent run completes successfully, a Workflow button converts the run into a single self-contained, reproducible script in a chosen language (Bash, Python, Perl, Ruby, PowerShell, Ansible playbook, Windows-CMD batch, or AppleScript ) with robust error handling, detailed comments, and a deterministic metadata header (script name, creator, date/time). For fleet-wide tasks, the AI Swarm tab has its own Generate multi-server workflow dialog that additionally handles host lists and multi-server hardening options, shows the generated script with syntax highlighting and a live elapsed counter, keeps a history of additional instructions, and saves to Snippets with a pre-filled name.","title": "Generate Workflow Script"},{"location": "features/ai-tools.html#script-generation-capabilities","text": "Auto-load matching AI Skills — Skills like language-quality guidelines for the target language are automatically included. Hardening options — A collapsible panel of production-quality techniques (strict mode, error traps, meaningful exit codes, logging, idempotency, dry-run, --help , and more) that are baked into the generated script. All are on by default; untick any you don't want. See Hardening options for what each one means. Input hardening — A second collapsible panel that asks the AI to build an input-validation guard block into the generated script: parameter allowlists and length limits, file format checks, a maximum input-file size controlled by the adjustable MAX_FILE_SIZE script variable, security warnings in the script's log, and a FORCE=1 / --force override. The guard checks file metadata before it reads content, and 0 means unlimited. Strictly opt-in — the master check box starts unticked. See Input hardening . Eight target languages — Bash, Python, Perl, Ruby, PowerShell, Ansible, plus Windows-CMD ( .cmd batch — @echo off , REM headers, errorlevel checks) and AppleScript ( .applescript — osascript shebang, -- comments, try / on error ). Multiple language variants — Generate several language variants and suggestions as inline tabs within the workflow dialog. Adjustable font size — Each generated-script editor has A− / A+ buttons and supports Ctrl + mouse wheel (Cmd on macOS); the chosen size is remembered across sessions. Header templates — Use reusable headers from the fixed non-deletable Script-Header snippet category. Mermaid diagram — Optionally include a Mermaid flowchart depicting the script logic. Its dedicated source-only request omits AI Skills and knowledge-store excerpts and sets the request-scoped Reasoning value to none only when the provider advertised that value; the saved profile remains unchanged. A working spinner is shown while the diagram is generated. Snippet Manager — Save the generated script into the Snippet Manager with a short auto-generated name and correct file extension. Scripts are de-duplicated by full name including extension. Workflow tagging — The snippet is tagged as workflow for easy filtering. OS detection — The System (OS) column is auto-set from the agent's probed OS (any Linux distro → Linux). No internet access — Internet access is forced OFF during generation regardless of the profile's internet mode. Resizable dialog — The workflow dialog remembers its size and position for future use.","title": "Script generation capabilities"},{"location": "features/ai-tools.html#terminal-targeted-behavior","text": "","title": "Terminal-targeted behavior"},{"location": "features/ai-tools.html#current-working-directory","text": "The agent tracks the current directory for the active SSH or local-shell session. Local runs freeze the best trusted directory once at run start and use it for the probe and every command, so generated files are created where the interactive shell was working when the run began.","title": "Current working directory"},{"location": "features/ai-tools.html#input-requirements","text": "When user input is required (sudo password or command approval): Auto-expand — The activity panel auto-expands and selects the run that needs input. Masked input — Sudo password input is masked in the activity panel. Submit — Press Enter or click the submit button to send the password. Up to 3 retries — Wrong passwords can be retried up to 3 times before the run cancels. Bold badge — The run tab shows a bold ✋ \"input required\" badge while waiting.","title": "Input requirements"},{"location": "features/ai-tools.html#terminal-output","text": "Final terminal-agent answers are written back to the terminal area so the shell transcript contains the answer, not only the activity panel state.","title": "Terminal output"},{"location": "features/ai-tools.html#transcripts","text": "Dedicated agent and planning tabs can copy and save their transcript for later review or sharing.","title": "Transcripts"},{"location": "features/ai-tools.html#running-commands","text": "To start an agent task: At a shell prompt in an active SSH or local-shell terminal, type your agent command: agent show the 10 largest files in this directory The agent probes the session — It immediately collects the current user, OS, working directory, and sudo status. For planning mode , the agent asks clarifying questions and proposes approaches before you approve: Answer the questions or propose your own approach Review the final plan Click Implement to start execution The activity panel appears at the bottom of the split showing: The user prompt Each decision made (which commands to run) Real-time command output as it runs Any approval or password requests Approvals — When the agent needs approval to run system-changing commands, click Approve once (just this set) or Approve always (all remaining sets in the run), or click Cancel . Sudo password — When prompted, enter your sudo password. It can optionally be cached for the session. The run completes when the task succeeds, is blocked by an error, or is cancelled by the user. After completion , a Workflow button appears if you'd like to convert the successful run into a reusable script.","title": "Running commands"},{"location": "features/ai-tools.html#keyboard-shortcuts","text": "Shortcut Action Tab at agent command Show agent , agent-ask , agent-plan variants Tab after agent Show recent agent-prompt history Esc or Ctrl + C during run Cancel the selected run's tab Ctrl + R during run Toggle thinking details for the selected run Activity panel ⏸ button Pause the selected run at a safe checkpoint Activity panel ▶️ button Resume a paused run","title": "Keyboard shortcuts"},{"location": "features/ai-tools.html#planning-mode","text": "Planning mode ( agent-plan or agent -plan ) offers an advisory workflow before execution: Clarifying questions — The agent asks follow-up questions to understand your requirements. Propose options — Based on your answers, the agent suggests one or more approaches with feasibility, risks, and prerequisites. Final plan — After you select an approach, the agent generates a detailed final plan with steps, success criteria, and risks. Implement — Only after you review and click Implement does execution begin. Sudo preflight — If the plan requires sudo and a password is needed, KorTTY checks the password immediately so execution is not interrupted later.","title": "Planning mode"},{"location": "features/ai-tools.html#settings-and-configuration","text": "Configure the Terminal AI Agent in Settings > AI : Setting Effect AI Agent enabled Enable or disable the agent feature globally Agent command name The base command (default: agent ); other variants are derived automatically Case-insensitive Make command matching case-insensitive Execution target Choose whether agent runs open in dedicated tabs or inline at the terminal split Per-run setup dialog Show a setup dialog before each run (disabled uses the default profile) Input history size Number of recent prompts to remember (5–100, default 20) Default profile The AI profile used when the setup dialog is disabled Activity panel placement Choose At Bottom or Dock Left/Right Keep collapsed preference Start the panel minimized and keep it collapsed during execution","title": "Settings and configuration"},{"location": "features/ai-tools.html#ai-skills","text": "AI Skills are reusable local instruction blocks that the agent can use. For agent runs, enable or disable skills globally, or let KorTTY automatically match only relevant skills to the task. Skills can cover: Operational policies and standards Security best practices Language-specific coding guidelines System administration conventions See the AI Skills section in the main AI documentation for setup and management details.","title": "AI Skills"},{"location": "features/ai-tools.html#internet-access","text": "The agent can optionally use web tools when the task clearly requires current or external information: Disabled (default) — No web tools or MCP integrations are sent with agent requests. KorTTY Tavily Tool — Direct web search via Tavily API. LM Studio MCP modes — Integrate Tavily, Bright Data, Brave Search, SearXNG, or LM Studio Toolpack through LM Studio's native MCP support. Configure internet access per AI profile in Settings > AI > Internet access . Warning Web tools are withheld from local file/script review tasks unless your prompt clearly asks for current or external information. Inspecting a local file should use shell commands like sed , cat , find , or language-specific tools, not web search.","title": "Internet access"},{"location": "features/ascii-art.html","text": "ASCII art Create ASCII art in three ways: render text as a FIGlet banner, let the AI draw a picture from a subject such as \"house in the forest\", or convert an existing image file. All three sit in one dialog behind their own tab, share a zoomable preview, and copy to the clipboard — plain, as a code comment, or as ready-made print statements — for use in terminal scripts, login banners, or documentation. Accessing the tool Open the dialog from Tools > ASCII Art... in the menu bar, or press Ctrl + Shift + A ( Cmd + Shift + A on macOS). Text Banner tab Style Choose a FIGlet font style from the dropdown. Standard and Slant come from the jfiglet library; 3-D , banner , big , block , cosmic , Digital , Lean , roman , script and small are bundled FIGfonts. A style whose font file cannot be loaded is left out of the list. Switch between styles with the dropdown, with the arrow keys ( Left , Right , Up , Down ) while the dropdown has focus, or with the ◀ and ▶ buttons beside it. Text Type or paste the text to convert into the Text field. Multi-line input is supported — each line is converted on its own, and blank lines stay blank. The Preview re-renders as you type and whenever you change the style. AI Picture tab Instead of lettering, this tab asks a model to draw the subject as a small vector drawing (a restricted SVG), which korTTY then converts into ASCII characters itself — a model is far better at describing shapes than at typing aligned characters. Control What it does Subject The thing to draw, for example house in the forest . Pressing Enter starts the generation. Size The character grid the picture is fitted into: Small (40 × 20), Medium (60 × 30), Large (80 × 40) or Extra large (100 × 50). Remembered between sessions. Generate Requests a picture for the subject. AI profile Which profile handles this run. The choice is transient and does not change your default profile. New variation Redraws the same subject with a different treatment — viewpoint, level of detail, scene context, geometric style, proportions, contrast or tones — asking for something different again on each retry. Cancel Stops the running generation; the previous picture stays. The tab needs at least one configured AI profile and the AI features switch enabled; otherwise its controls stay disabled and the status line says so. The status line names the current step: asking the AI for a drawing, converting it to characters, asking once more, or asking the AI to type the picture directly. If an answer holds no usable drawing, korTTY asks once more and tells the model what was wrong; if that fails too, it falls back to asking the model to type the ASCII picture directly and says so in the status line. Errors, a cut-off answer and \"no usable picture\" replies are reported there as well, together with the reason. Note The model returns a drawing rather than characters: korTTY draws it off-screen, fits it into the chosen size and picks one printable ASCII character per cell, so the result is always plain ASCII that fits the grid, with blank edge rows and columns trimmed. A drawing is checked before it is shown — it must contain shapes, lie inside its canvas, be neither blank nor one solid block, and be large enough. Only the direct-typing fallback shows the model's own characters, cleaned as before and cropped to the chosen size: a fenced code block is unwrapped, reasoning blocks and control characters are removed, tabs become spaces, and blank leading and trailing lines are trimmed. For this action korTTY switches the model's reasoning off where the profile allows it, caps the answer length and skips knowledge stores, because a picture gains nothing from either. Image File tab This tab converts an existing picture — a photo, a logo, an icon — into ASCII art on your machine; no AI profile is involved. Open a file with Open image… or drop it onto the tab. PNG, JPEG, GIF and BMP are read. Control What it does Size The character grid the picture is fitted into, keeping its aspect ratio: a landscape photo uses fewer rows, a portrait one fewer columns. Remembered between sessions. Brightness Shifts all tones lighter or darker. Contrast Spreads or flattens the tones around mid grey. Invert Swaps light and dark. ASCII ink is dark on light, so a light subject on a dark background reads correctly only when inverted. Reset Returns brightness, contrast and inversion to neutral. Every change converts again immediately, and the status line shows the source size in pixels and the result size in characters. Note Before your adjustments apply, korTTY stretches the picture's own tonal range to full contrast (ignoring the darkest and lightest percent of pixels), because photos cluster in the mid tones and would otherwise become one grey block. Transparent pixels count as white paper. Very large files are reduced to 1600 pixels on the long edge when they are opened; more detail cannot reach the character grid anyway. Freestanding subjects, logos and icons convert well; busy photographs need a large size and usually some contrast. Preview zoom All tabs share one zoom level, so a banner and a picture are always shown at the same size. Action Controls Enlarge + button, Ctrl + + , or Ctrl and scroll up over the preview Shrink − button, Ctrl + - , or Ctrl and scroll down over the preview Reset ⟲ button or Ctrl + 0 The percentage between the buttons shows the current level, from 50 % to 333 %, where 100 % is the default 12 px monospace size. Copying to clipboard Copy to Clipboard copies the preview of the tab that is currently open, so you get the banner from the Text Banner tab and the picture from the AI Picture or Image File tab. The Copy as row above the buttons decides the shape of what lands on the clipboard, so a picture can be pasted straight into program code: Control What it does Comment Puts a comment marker in front of every picture line, or wraps the picture in a block comment, so it can sit inside source code without being executed. Output code Turns every picture line into a print statement of the chosen language, with the quoting and escaping that language needs, so a script prints the picture as-is. Gap The number of spaces placed before every picture line — between the comment marker and the picture, or inside the printed string — to align the picture. 0 to 20, default 1. Comment and output code exclude each other: choosing one resets the other to None . All three choices are remembered between sessions, and the copy button's tooltip shows the first line as it will be copied. Comment styles Style Languages # Bash, Perl, Python, Ruby, YAML, TOML, PowerShell // Java, C, C++, JavaScript, TypeScript, Go, Rust, Kotlin, Swift, PHP -- SQL, Lua, Haskell, Ada ; Lisp, Clojure, INI, assembler % LaTeX, MATLAB, Erlang ' VB, VBA REM Batch /* … */ C-style block comment; every picture line is prefixed with * <!-- … --> XML, HTML, SVG, Markdown [ \"…\", … ] JSON string array — the picture as a list of string values Output code Language Statement per picture line Bash / sh heredoc cat <<'KORTTY_ASCII_ART' … KORTTY_ASCII_ART — no escaping at all Bash echo '…' POSIX sh printf '%s\\n' '…' Perl print '…', \"\\n\"; Python print(\"…\") Ruby puts '…' PHP echo '…', PHP_EOL; JavaScript console.log(\"…\"); Java System.out.println(\"…\"); C puts(\"…\"); C# Console.WriteLine(\"…\"); Go fmt.Println(\"…\") Rust println!(\"…\"); Kotlin println(\"…\") Swift print(\"…\") Lua print(\"…\") PowerShell Write-Output '…' Batch echo(… Note A few characters have to change so the copied text stays valid: inside a C-style block comment */ becomes +/ , inside an XML comment every second hyphen of a -- run becomes = because XML forbids -- in comments, JSON strings escape backslashes and quotes, Rust doubles { and } , Kotlin escapes $ , C rewrites ?? so it is not read as a trigraph, and Batch escapes ^ & | < > ( ) \" and doubles % . Line comments and the Bash heredoc copy the picture unchanged. Three cases stay outside what escaping can fix: with a gap of 0, Bash's echo swallows a picture line that is exactly -n or -e (use the heredoc, printf , or the default gap of 1); a Batch file with delayed expansion enabled still interprets ! , and a REM line still expands %~ ; and a // comment whose last picture line ends in a backslash would swallow the next source line in C and C++, so korTTY appends an empty // line in that case. Example The banner style with the input text \"Nostromo\": # # ## # #### #### ##### ##### #### # # #### # # # # # # # # # # # ## ## # # # # # # # #### # # # # # # ## # # # # # # # # # # ##### # # # # # # # ## # # # # # # # # # # # # # # # #### #### # # # #### # # #### Dialog state The dialog remembers its window position, its size, the preview zoom level, the picture sizes of the AI Picture and Image File tabs, and the copy settings between sessions.","title": "ASCII art"},{"location": "features/ascii-art.html#ascii-art","text": "Create ASCII art in three ways: render text as a FIGlet banner, let the AI draw a picture from a subject such as \"house in the forest\", or convert an existing image file. All three sit in one dialog behind their own tab, share a zoomable preview, and copy to the clipboard — plain, as a code comment, or as ready-made print statements — for use in terminal scripts, login banners, or documentation.","title": "ASCII art"},{"location": "features/ascii-art.html#accessing-the-tool","text": "Open the dialog from Tools > ASCII Art... in the menu bar, or press Ctrl + Shift + A ( Cmd + Shift + A on macOS).","title": "Accessing the tool"},{"location": "features/ascii-art.html#text-banner-tab","text": "","title": "Text Banner tab"},{"location": "features/ascii-art.html#style","text": "Choose a FIGlet font style from the dropdown. Standard and Slant come from the jfiglet library; 3-D , banner , big , block , cosmic , Digital , Lean , roman , script and small are bundled FIGfonts. A style whose font file cannot be loaded is left out of the list. Switch between styles with the dropdown, with the arrow keys ( Left , Right , Up , Down ) while the dropdown has focus, or with the ◀ and ▶ buttons beside it.","title": "Style"},{"location": "features/ascii-art.html#text","text": "Type or paste the text to convert into the Text field. Multi-line input is supported — each line is converted on its own, and blank lines stay blank. The Preview re-renders as you type and whenever you change the style.","title": "Text"},{"location": "features/ascii-art.html#ai-picture-tab","text": "Instead of lettering, this tab asks a model to draw the subject as a small vector drawing (a restricted SVG), which korTTY then converts into ASCII characters itself — a model is far better at describing shapes than at typing aligned characters. Control What it does Subject The thing to draw, for example house in the forest . Pressing Enter starts the generation. Size The character grid the picture is fitted into: Small (40 × 20), Medium (60 × 30), Large (80 × 40) or Extra large (100 × 50). Remembered between sessions. Generate Requests a picture for the subject. AI profile Which profile handles this run. The choice is transient and does not change your default profile. New variation Redraws the same subject with a different treatment — viewpoint, level of detail, scene context, geometric style, proportions, contrast or tones — asking for something different again on each retry. Cancel Stops the running generation; the previous picture stays. The tab needs at least one configured AI profile and the AI features switch enabled; otherwise its controls stay disabled and the status line says so. The status line names the current step: asking the AI for a drawing, converting it to characters, asking once more, or asking the AI to type the picture directly. If an answer holds no usable drawing, korTTY asks once more and tells the model what was wrong; if that fails too, it falls back to asking the model to type the ASCII picture directly and says so in the status line. Errors, a cut-off answer and \"no usable picture\" replies are reported there as well, together with the reason. Note The model returns a drawing rather than characters: korTTY draws it off-screen, fits it into the chosen size and picks one printable ASCII character per cell, so the result is always plain ASCII that fits the grid, with blank edge rows and columns trimmed. A drawing is checked before it is shown — it must contain shapes, lie inside its canvas, be neither blank nor one solid block, and be large enough. Only the direct-typing fallback shows the model's own characters, cleaned as before and cropped to the chosen size: a fenced code block is unwrapped, reasoning blocks and control characters are removed, tabs become spaces, and blank leading and trailing lines are trimmed. For this action korTTY switches the model's reasoning off where the profile allows it, caps the answer length and skips knowledge stores, because a picture gains nothing from either.","title": "AI Picture tab"},{"location": "features/ascii-art.html#image-file-tab","text": "This tab converts an existing picture — a photo, a logo, an icon — into ASCII art on your machine; no AI profile is involved. Open a file with Open image… or drop it onto the tab. PNG, JPEG, GIF and BMP are read. Control What it does Size The character grid the picture is fitted into, keeping its aspect ratio: a landscape photo uses fewer rows, a portrait one fewer columns. Remembered between sessions. Brightness Shifts all tones lighter or darker. Contrast Spreads or flattens the tones around mid grey. Invert Swaps light and dark. ASCII ink is dark on light, so a light subject on a dark background reads correctly only when inverted. Reset Returns brightness, contrast and inversion to neutral. Every change converts again immediately, and the status line shows the source size in pixels and the result size in characters. Note Before your adjustments apply, korTTY stretches the picture's own tonal range to full contrast (ignoring the darkest and lightest percent of pixels), because photos cluster in the mid tones and would otherwise become one grey block. Transparent pixels count as white paper. Very large files are reduced to 1600 pixels on the long edge when they are opened; more detail cannot reach the character grid anyway. Freestanding subjects, logos and icons convert well; busy photographs need a large size and usually some contrast.","title": "Image File tab"},{"location": "features/ascii-art.html#preview-zoom","text": "All tabs share one zoom level, so a banner and a picture are always shown at the same size. Action Controls Enlarge + button, Ctrl + + , or Ctrl and scroll up over the preview Shrink − button, Ctrl + - , or Ctrl and scroll down over the preview Reset ⟲ button or Ctrl + 0 The percentage between the buttons shows the current level, from 50 % to 333 %, where 100 % is the default 12 px monospace size.","title": "Preview zoom"},{"location": "features/ascii-art.html#copying-to-clipboard","text": "Copy to Clipboard copies the preview of the tab that is currently open, so you get the banner from the Text Banner tab and the picture from the AI Picture or Image File tab. The Copy as row above the buttons decides the shape of what lands on the clipboard, so a picture can be pasted straight into program code: Control What it does Comment Puts a comment marker in front of every picture line, or wraps the picture in a block comment, so it can sit inside source code without being executed. Output code Turns every picture line into a print statement of the chosen language, with the quoting and escaping that language needs, so a script prints the picture as-is. Gap The number of spaces placed before every picture line — between the comment marker and the picture, or inside the printed string — to align the picture. 0 to 20, default 1. Comment and output code exclude each other: choosing one resets the other to None . All three choices are remembered between sessions, and the copy button's tooltip shows the first line as it will be copied.","title": "Copying to clipboard"},{"location": "features/ascii-art.html#comment-styles","text": "Style Languages # Bash, Perl, Python, Ruby, YAML, TOML, PowerShell // Java, C, C++, JavaScript, TypeScript, Go, Rust, Kotlin, Swift, PHP -- SQL, Lua, Haskell, Ada ; Lisp, Clojure, INI, assembler % LaTeX, MATLAB, Erlang ' VB, VBA REM Batch /* … */ C-style block comment; every picture line is prefixed with * <!-- … --> XML, HTML, SVG, Markdown [ \"…\", … ] JSON string array — the picture as a list of string values","title": "Comment styles"},{"location": "features/ascii-art.html#output-code","text": "Language Statement per picture line Bash / sh heredoc cat <<'KORTTY_ASCII_ART' … KORTTY_ASCII_ART — no escaping at all Bash echo '…' POSIX sh printf '%s\\n' '…' Perl print '…', \"\\n\"; Python print(\"…\") Ruby puts '…' PHP echo '…', PHP_EOL; JavaScript console.log(\"…\"); Java System.out.println(\"…\"); C puts(\"…\"); C# Console.WriteLine(\"…\"); Go fmt.Println(\"…\") Rust println!(\"…\"); Kotlin println(\"…\") Swift print(\"…\") Lua print(\"…\") PowerShell Write-Output '…' Batch echo(… Note A few characters have to change so the copied text stays valid: inside a C-style block comment */ becomes +/ , inside an XML comment every second hyphen of a -- run becomes = because XML forbids -- in comments, JSON strings escape backslashes and quotes, Rust doubles { and } , Kotlin escapes $ , C rewrites ?? so it is not read as a trigraph, and Batch escapes ^ & | < > ( ) \" and doubles % . Line comments and the Bash heredoc copy the picture unchanged. Three cases stay outside what escaping can fix: with a gap of 0, Bash's echo swallows a picture line that is exactly -n or -e (use the heredoc, printf , or the default gap of 1); a Batch file with delayed expansion enabled still interprets ! , and a REM line still expands %~ ; and a // comment whose last picture line ends in a backslash would swallow the next source line in C and C++, so korTTY appends an empty // line in that case.","title": "Output code"},{"location": "features/ascii-art.html#example","text": "The banner style with the input text \"Nostromo\": # # ## # #### #### ##### ##### #### # # #### # # # # # # # # # # # ## ## # # # # # # # #### # # # # # # ## # # # # # # # # # # ##### # # # # # # # ## # # # # # # # # # # # # # # # #### #### # # # #### # # ####","title": "Example"},{"location": "features/ascii-art.html#dialog-state","text": "The dialog remembers its window position, its size, the preview zoom level, the picture sizes of the AI Picture and Image File tabs, and the copy settings between sessions.","title": "Dialog state"},{"location": "features/backup.html","text": "Backup & restore KorTTY creates encrypted backups of all your settings, connections, credentials, and SSH keys. Use backup and restore to protect your configuration or move it between machines. Features Encrypted backups — All backups are encrypted using either password-protected ZIP or GPG encryption Configuration backup — Includes connections, credentials, SSH/GPG keys, trusted interactive SSH host keys, global settings, JobScheduler configuration, snippets, AI chat history, local-model registrations, and knowledge-store source metadata Regenerable local AI data excluded — GGUF weights, native llama.cpp runtimes, signed-catalog cache, temporary sidecar files, and HNSW snapshots are intentionally not copied into the archive Projects directory — All saved project workspaces are included in the backup Automatic rotation — Old backups are automatically moved to an old-backups subdirectory with timestamps Configurable retention — Set a maximum number of backups to keep (0 = unlimited; oldest backups are deleted automatically) Import/restore — Restore from previously created backups with optional overwrite control Flexible decryption — Both password-encrypted ZIP and GPG-encrypted formats are supported for import Creating a backup Open Edit → Create Backup... or press Ctrl + Shift + B (Cmd+Shift+B on macOS) Select a destination directory for the backup file The backup is created using the encryption method configured in Settings → Backup KorTTY names the current backup kortty-backup.zip in the target directory. If a backup already exists there, it is automatically rotated into an old-backups subdirectory with a timestamp appended (e.g., kortty-backup_2025-06-24_14-30-45.zip ). The backup includes: Item Details Connections All saved SSH connections and groups Credentials Stored usernames and passwords (encrypted) SSH keys Key references with encrypted passphrases, plus the copied key files in ~/.kortty/ssh-keys/ Trusted interactive hosts ssh-host-keys.properties , shared by Terminal, SFTP, and the Mosh SSH bootstrap; the transient .lock companion is not included GPG keys GPG public keys for backup encryption Settings Global application settings, terminal configurations, themes, and AI profiles JobScheduler jobs All scheduled jobs, host-key pins, and encrypted sudo passwords Snippets Code snippets and script templates with metadata Snippet variables Custom variables for snippet substitution AI chats Saved AI conversation histories and profiles Local AI configuration Local GGUF registrations and typed launch settings, Text/Coding roles, preferred runtime backend/update policy, and encrypted Hugging Face token Knowledge-store configuration Store metadata and source paths, filters, sync modes, and embedding configuration; not the HNSW vectors Projects All .kortty project workspace files Backup encryption Password-protected ZIP (default) Open Settings → Backup Select Encryption Type: Password Choose or create a credential to use as the encryption password Optionally set Maximum Backups (0 = unlimited) Save Password-protected backups are encrypted with AES-256 (via the zip4j library); the credential's password encrypts all files in the archive. Backups created by older korTTY versions used legacy ZIP encryption and can still be imported — the decryption method is read from the archive itself. Note that AES-encrypted ZIPs need an AES-capable tool (7-Zip, WinZip, unzip 6+) if you ever extract one outside korTTY. GPG encryption Open Settings → Backup Select Encryption Type: GPG Choose a GPG key from Manage GPG Keys... Optionally set Maximum Backups (0 = unlimited) Save GPG backups encrypt the backup file using the public key of your selected GPG key. KorTTY creates a temporary ZIP first, then encrypts it with gpg --encrypt and stores the .gpg file. The temporary ZIP is securely deleted after encryption. Tip If you don't have GPG keys set up yet, use Management → Manage GPG Keys... to import keys from your system keyring or add them manually. Importing a backup Open Edit → Import Backup... Select a backup file ( .zip or .gpg ) If the backup is password-protected, enter the password when prompted Choose whether to Overwrite Existing Files : Checked — Backup files will replace any existing files in your configuration Unchecked — Existing files are skipped; only missing files are imported Click Import Restart the application for all changes to take effect Warning Importing a backup with Overwrite enabled will replace your current settings, connections, and credentials. If you are unsure, uncheck this option to merge the backup without overwriting. Backup file contents Both .zip and .gpg backups contain the same files: connections.xml — All SSH connections and groups credentials.xml — Stored credentials (still encrypted with your master password) ssh-keys.xml — SSH key references and encrypted passphrases ssh-keys/ — Copied SSH key files (only keys you placed there via Copy to User Directory ; keys referenced in their original locations are not collected). Restored key files get owner-only permissions, and an import merges — it never deletes or, without Overwrite , replaces keys already present ssh-host-keys.properties — Trusted public host keys for interactive Terminal, SFTP, and Mosh bootstrap connections ( ssh-host-keys.properties.lock is intentionally excluded) gpg-keys.xml — GPG public keys global-settings.xml — Application settings, themes, AI profiles, terminal defaults job-scheduler.xml — JobScheduler jobs, host-key pins, encrypted sudo passwords snippets.xml — Code snippets and templates snippet-variables.xml — Custom snippet variables ai-chats.xml — Saved AI conversations master.key — Hash of your master password (for verification on import) llm/models.xml — Local GGUF registrations and runtime settings (model weights are not included) rag/stores.json — Knowledge-store and source configuration (vector snapshots are not included) projects/ — All saved project workspace files ( .kortty ) Note All passwords and credentials inside the backup remain encrypted with your master password. When you import a backup, you must unlock the master password for KorTTY to decrypt the credentials. Rebuild local AI assets after a restore The backup excludes llm/models/ , llm/runtime/ , llm/catalog/ , llm/run/ , and local index.hnsw snapshots. After moving to another computer, restore or download the GGUF files and a compatible runtime, reconnect any external model/source paths, then run Update now in each knowledge store to regenerate its index. The signed catalog cache refreshes automatically or falls back to the bootstrap. Original source documents and external Qdrant data are not part of a korTTY configuration backup. Backup retention and cleanup When you create a new backup in a directory that already contains one, KorTTY: Creates the new backup as kortty-backup.zip Moves the existing backup to old-backups/kortty-backup_<timestamp>.zip If the number of old backups exceeds Maximum Backups , deletes the oldest ones To keep unlimited old backups, set Maximum Backups to 0 in Settings → Backup . To keep only the current backup, set Maximum Backups to 1 (old backups are still rotated but then immediately deleted). Using backups across machines Export your current configuration: On machine A, open Edit → Create Backup... and save to a USB drive or cloud storage Move the backup file: Copy kortty-backup.zip (or .gpg ) to machine B Import on the new machine: On machine B, open Edit → Import Backup... Select the backup file from step 2 Enter the backup password if prompted Leave Overwrite unchecked unless you want to replace existing connections Restart KorTTY All backed-up connections, settings, snippets, saved chats, interactive host-key trust decisions, model registrations, and knowledge-source definitions will be available on machine B. Restored host keys are still matched by normalized host name and port, so a changed key remains blocked after migration. Local model weights, runtime packages, source documents, and HNSW vectors must be restored or regenerated separately. Troubleshooting \"Backup file not found\" Verify the file path is correct and the file exists. Check the directory permissions. \"Password required for password-encrypted backup\" Password-protected backups need the correct password. Verify you are entering the credential password (from Settings → Backup ), not your master password. \"GPG key not found\" The GPG key used for encryption is missing. Use Management → Manage GPG Keys... to import or add the key, then try again. \"No password selected for backup encryption\" or \"No GPG key selected\" Configure a password credential or GPG key in Settings → Backup before creating a backup. Import succeeded but changes did not take effect Restart KorTTY for imported settings to become active. If you imported credentials, you may also need to unlock the master password after restart. Backup file is larger than expected Large backups can occur if you have many saved AI chats or a large projects directory. GGUF weights, llama.cpp runtime packages, and HNSW snapshots are excluded and cannot be the cause.","title": "Backup & restore"},{"location": "features/backup.html#backup-restore","text": "KorTTY creates encrypted backups of all your settings, connections, credentials, and SSH keys. Use backup and restore to protect your configuration or move it between machines.","title": "Backup &amp; restore"},{"location": "features/backup.html#features","text": "Encrypted backups — All backups are encrypted using either password-protected ZIP or GPG encryption Configuration backup — Includes connections, credentials, SSH/GPG keys, trusted interactive SSH host keys, global settings, JobScheduler configuration, snippets, AI chat history, local-model registrations, and knowledge-store source metadata Regenerable local AI data excluded — GGUF weights, native llama.cpp runtimes, signed-catalog cache, temporary sidecar files, and HNSW snapshots are intentionally not copied into the archive Projects directory — All saved project workspaces are included in the backup Automatic rotation — Old backups are automatically moved to an old-backups subdirectory with timestamps Configurable retention — Set a maximum number of backups to keep (0 = unlimited; oldest backups are deleted automatically) Import/restore — Restore from previously created backups with optional overwrite control Flexible decryption — Both password-encrypted ZIP and GPG-encrypted formats are supported for import","title": "Features"},{"location": "features/backup.html#creating-a-backup","text": "Open Edit → Create Backup... or press Ctrl + Shift + B (Cmd+Shift+B on macOS) Select a destination directory for the backup file The backup is created using the encryption method configured in Settings → Backup KorTTY names the current backup kortty-backup.zip in the target directory. If a backup already exists there, it is automatically rotated into an old-backups subdirectory with a timestamp appended (e.g., kortty-backup_2025-06-24_14-30-45.zip ). The backup includes: Item Details Connections All saved SSH connections and groups Credentials Stored usernames and passwords (encrypted) SSH keys Key references with encrypted passphrases, plus the copied key files in ~/.kortty/ssh-keys/ Trusted interactive hosts ssh-host-keys.properties , shared by Terminal, SFTP, and the Mosh SSH bootstrap; the transient .lock companion is not included GPG keys GPG public keys for backup encryption Settings Global application settings, terminal configurations, themes, and AI profiles JobScheduler jobs All scheduled jobs, host-key pins, and encrypted sudo passwords Snippets Code snippets and script templates with metadata Snippet variables Custom variables for snippet substitution AI chats Saved AI conversation histories and profiles Local AI configuration Local GGUF registrations and typed launch settings, Text/Coding roles, preferred runtime backend/update policy, and encrypted Hugging Face token Knowledge-store configuration Store metadata and source paths, filters, sync modes, and embedding configuration; not the HNSW vectors Projects All .kortty project workspace files","title": "Creating a backup"},{"location": "features/backup.html#backup-encryption","text": "","title": "Backup encryption"},{"location": "features/backup.html#password-protected-zip-default","text": "Open Settings → Backup Select Encryption Type: Password Choose or create a credential to use as the encryption password Optionally set Maximum Backups (0 = unlimited) Save Password-protected backups are encrypted with AES-256 (via the zip4j library); the credential's password encrypts all files in the archive. Backups created by older korTTY versions used legacy ZIP encryption and can still be imported — the decryption method is read from the archive itself. Note that AES-encrypted ZIPs need an AES-capable tool (7-Zip, WinZip, unzip 6+) if you ever extract one outside korTTY.","title": "Password-protected ZIP (default)"},{"location": "features/backup.html#gpg-encryption","text": "Open Settings → Backup Select Encryption Type: GPG Choose a GPG key from Manage GPG Keys... Optionally set Maximum Backups (0 = unlimited) Save GPG backups encrypt the backup file using the public key of your selected GPG key. KorTTY creates a temporary ZIP first, then encrypts it with gpg --encrypt and stores the .gpg file. The temporary ZIP is securely deleted after encryption. Tip If you don't have GPG keys set up yet, use Management → Manage GPG Keys... to import keys from your system keyring or add them manually.","title": "GPG encryption"},{"location": "features/backup.html#importing-a-backup","text": "Open Edit → Import Backup... Select a backup file ( .zip or .gpg ) If the backup is password-protected, enter the password when prompted Choose whether to Overwrite Existing Files : Checked — Backup files will replace any existing files in your configuration Unchecked — Existing files are skipped; only missing files are imported Click Import Restart the application for all changes to take effect Warning Importing a backup with Overwrite enabled will replace your current settings, connections, and credentials. If you are unsure, uncheck this option to merge the backup without overwriting.","title": "Importing a backup"},{"location": "features/backup.html#backup-file-contents","text": "Both .zip and .gpg backups contain the same files: connections.xml — All SSH connections and groups credentials.xml — Stored credentials (still encrypted with your master password) ssh-keys.xml — SSH key references and encrypted passphrases ssh-keys/ — Copied SSH key files (only keys you placed there via Copy to User Directory ; keys referenced in their original locations are not collected). Restored key files get owner-only permissions, and an import merges — it never deletes or, without Overwrite , replaces keys already present ssh-host-keys.properties — Trusted public host keys for interactive Terminal, SFTP, and Mosh bootstrap connections ( ssh-host-keys.properties.lock is intentionally excluded) gpg-keys.xml — GPG public keys global-settings.xml — Application settings, themes, AI profiles, terminal defaults job-scheduler.xml — JobScheduler jobs, host-key pins, encrypted sudo passwords snippets.xml — Code snippets and templates snippet-variables.xml — Custom snippet variables ai-chats.xml — Saved AI conversations master.key — Hash of your master password (for verification on import) llm/models.xml — Local GGUF registrations and runtime settings (model weights are not included) rag/stores.json — Knowledge-store and source configuration (vector snapshots are not included) projects/ — All saved project workspace files ( .kortty ) Note All passwords and credentials inside the backup remain encrypted with your master password. When you import a backup, you must unlock the master password for KorTTY to decrypt the credentials. Rebuild local AI assets after a restore The backup excludes llm/models/ , llm/runtime/ , llm/catalog/ , llm/run/ , and local index.hnsw snapshots. After moving to another computer, restore or download the GGUF files and a compatible runtime, reconnect any external model/source paths, then run Update now in each knowledge store to regenerate its index. The signed catalog cache refreshes automatically or falls back to the bootstrap. Original source documents and external Qdrant data are not part of a korTTY configuration backup.","title": "Backup file contents"},{"location": "features/backup.html#backup-retention-and-cleanup","text": "When you create a new backup in a directory that already contains one, KorTTY: Creates the new backup as kortty-backup.zip Moves the existing backup to old-backups/kortty-backup_<timestamp>.zip If the number of old backups exceeds Maximum Backups , deletes the oldest ones To keep unlimited old backups, set Maximum Backups to 0 in Settings → Backup . To keep only the current backup, set Maximum Backups to 1 (old backups are still rotated but then immediately deleted).","title": "Backup retention and cleanup"},{"location": "features/backup.html#using-backups-across-machines","text": "Export your current configuration: On machine A, open Edit → Create Backup... and save to a USB drive or cloud storage Move the backup file: Copy kortty-backup.zip (or .gpg ) to machine B Import on the new machine: On machine B, open Edit → Import Backup... Select the backup file from step 2 Enter the backup password if prompted Leave Overwrite unchecked unless you want to replace existing connections Restart KorTTY All backed-up connections, settings, snippets, saved chats, interactive host-key trust decisions, model registrations, and knowledge-source definitions will be available on machine B. Restored host keys are still matched by normalized host name and port, so a changed key remains blocked after migration. Local model weights, runtime packages, source documents, and HNSW vectors must be restored or regenerated separately.","title": "Using backups across machines"},{"location": "features/backup.html#troubleshooting","text": "\"Backup file not found\" Verify the file path is correct and the file exists. Check the directory permissions. \"Password required for password-encrypted backup\" Password-protected backups need the correct password. Verify you are entering the credential password (from Settings → Backup ), not your master password. \"GPG key not found\" The GPG key used for encryption is missing. Use Management → Manage GPG Keys... to import or add the key, then try again. \"No password selected for backup encryption\" or \"No GPG key selected\" Configure a password credential or GPG key in Settings → Backup before creating a backup. Import succeeded but changes did not take effect Restart KorTTY for imported settings to become active. If you imported credentials, you may also need to unlock the master password after restart. Backup file is larger than expected Large backups can occur if you have many saved AI chats or a large projects directory. GGUF weights, llama.cpp runtime packages, and HNSW snapshots are excluded and cannot be the cause.","title": "Troubleshooting"},{"location": "features/coding-agents.html","text": "Coding agents KorTTY recognises when a terminal-based coding agent — Claude Code , Codex or Gemini CLI — is running inside one of your local shell tabs and keeps track of what it is currently doing: working, waiting for your answer, finished, or sitting idle at its prompt. The analysis happens entirely inside korTTY on your own machine. What it finds is shown wherever you look: as a glyph in the tab title, as chips and accents in the dashboard, in a dockable Coding Agents panel with quick answers and a prompt box, as a strip in the status bar, as a count on the app icon and, when you are not looking at the pane, as a desktop notification. This page explains what is detected, how each of these surfaces works, how to switch things off, and how to adjust or extend the screen rules when an agent changes its user interface. What it detects Detection covers the three agents below, started from a Local Shell tab — directly, through a package-manager wrapper such as npx , or as a node , bun or deno script. SSH and Mosh tabs are not analysed, because the agent runs on the remote machine and korTTY cannot see its process. Agent Recognised executables Claude Code claude , claude-code Codex codex Gemini CLI gemini Every split pane is tracked on its own, so a tab with two panes can show one agent working while the other waits for a permission decision. A pane that runs no agent, or whose agent has exited, simply has no detection result. States A detected agent is always in exactly one of five states. The list is ordered by urgency: when several panes are summarised, the most urgent state wins. State Meaning BLOCKED The agent is waiting for you — a permission dialog ( Do you want to proceed? ), a question picker, or a (y/n) prompt is on screen. DONE The agent has finished. Either it shows an explicit completion screen that a rule recognises, or — far more often — it went from WORKING back to its empty prompt while you were not looking at its pane. KorTTY then keeps it marked DONE (✓) until you look at the pane; the moment its tab is selected and its pane focused in the front window, it becomes IDLE. WORKING The agent is thinking, running a tool or streaming output — typically recognisable by its spinner line and the esc to interrupt hint. IDLE The agent is running and its input box is empty, waiting for your next instruction. UNKNOWN The agent process is present but the screen matches none of the rules and the rule file asks for a strict result instead of a fallback. Dashboard marking Open the dashboard with View → Show Dashboard ( Ctrl + Shift + D ) and every connection row that hosts a coding agent shows a chip after the protocol badge: the state glyph, the agent's short name (or the alias you gave it) and, for an agent that is waiting, working or done, how long it has been in that state ( ✋ claude 2:14 ). An idle agent gets a dim, outlined chip so you still see that one is present. A row that also runs korTTY's own AI agent merges both marks into one chip. The row itself carries a coloured accent bar on its left edge — amber while the agent waits for you (the row is tinted as well), blue while it works, green when it is done — and the status dot of a waiting agent pulses while the window is in front and animations are enabled. Group and environment rows above a marked connection show a rollup chip such as ✋ 1 · ⚡ 2 and take the accent of their most urgent child, and the footer appends the same rollup to the connection count ( 3 of 5 connected · ✋ 1 · ⚡ 2 ), so a waiting agent is visible even when its group is collapsed. A split tab grows pane rows below the connection ( Pane 2 · api , named after the pane's working directory, with your home directory written ~ as the shell writes it), one per pane, so each pane can be reached on its own. Every pane is listed, whether or not an agent was detected in it: a split where one pane runs an agent and the other does not is the ordinary case, and showing only the agent's pane would hide half the tab. A pane without an agent carries no chip and no accent. An unsplit tab gets no pane rows, because its connection row already is the pane. When an agent becomes blocked, the dashboard expands the tree down to its row once per transition — a waiting agent is never hidden inside a collapsed group — and respects Collapse all afterwards. The context menu of a marked row adds Focus Pane , which selects the tab and focuses exactly that pane, Show in Coding Agents Panel , which docks the panel and selects the agent, and, while the agent waits for you, Send Enter , Send Esc and Interrupt (Ctrl+C) to answer the dialog without leaving the dashboard. The tab title shows the same glyph as the chip — ✋ waiting, ⚡ working, ✓ done, merged with korTTY's own AI-agent glyph where both apply. The Coding Agents panel The panel lists every detected agent of every korTTY window in one place. Open it with View → Coding Agents → Dock Left / Dock Right , toggle it on its last-used side with Show/Hide ( Ctrl + Alt + G ), from the ⋯ menu inside the panel, from the status strip's context menu, or with Show in Coding Agents Panel in the dashboard. Placement and width are remembered across restarts. Agents that wait for you are listed first (longest waiting on top), then the working, done and idle ones in window, tab and pane order; the row of the pane you are currently in carries a left accent. Every row shows the state dot and name, a state chip such as ✋ Waiting for you · 2:14 , the location line ( Window 2 › api › Pane 2 · ~/proj/api ) and the last screen line that matched a rule — the evidence. Below them sit the quick keys : Focus brings the pane to the front, y , n , Enter and Esc answer a dialog (they are highlighted while the agent waits for you), ↑ and ↓ move through a question picker, Ctrl+C interrupts, Explain opens a drawer with the full detection explanation (agent, state, matching rule, evidence, process and time in state), and Rename… gives the agent an alias that replaces its name in every chip, row and notification — an empty alias restores the agent name, and the alias survives a reconnect but is dropped when the pane closes. Every key goes to the pane's own terminal connection, exactly as if you had typed it there; the buttons are disabled while the pane is not connected. The prompt box at the bottom sends longer text to the agent selected in the target list: Enter sends, Shift + Enter inserts a line break. A single line is sent followed by Enter. A multi-line prompt is wrapped in bracketed paste — so the agent receives it as one pasted block instead of several submitted lines — but only when that agent has switched bracketed paste on, which Claude Code, Codex and Gemini CLI all do at their prompt; otherwise the lines are sent as typed. Two prompts are refused with a message in the panel's status line instead of being sent: while the agent is waiting for a decision the Send button stays disabled, because the text would land inside the permission dialog — answer it with y, n, Enter or Esc first; and a prompt whose first line starts with korTTY's own AI shortcut (the command name configured under Settings → AI, agent by default) is rejected, because korTTY's shortcut filter would divert that line to its own AI agent instead of the coding agent — rephrase the line. Errors of a quick key or prompt (pane closed, not connected, write failed) appear in the same status line for a few seconds, never as a dialog, and a successful send confirms with Sent to Claude Code . The Next blocked button in the panel header jumps to the next agent that waits for you, across all windows and wrapping around; it is the same action as Ctrl + Alt + N and a click on the status strip. Status strip The right end of the status bar shows a compact strip while at least one coding agent is known: up to three chips for waiting, working and done agents ( ✋ 1 · ⚡ 2 · ✓ 1 , zero counts are omitted), with the waiting dot pulsing while the window is in front. A click jumps to the next agent waiting for you — or, when none is waiting, to the first agent in display order — and a right-click offers Show/Hide for the panel and Next Blocked Agent . The tooltip summarises the counts. The strip disappears together with the status bar when you hide it under View . App icon badge and notifications The number of agents waiting for a decision — across all windows — is shown as a badge on the app icon so you notice it while working in another application. How the badge is drawn depends on the platform: on macOS the packaged app sets the Dock badge; on Windows korTTY draws the count into its own taskbar and title-bar icon; on Linux it emits the launcher-entry signal that KDE Plasma, Ubuntu Dock and Dash to Dock render on the launcher icon — this needs an installed desktop file (the deb, rpm or pacman package) and a reachable session bus, which korTTY talks to directly. Stock GNOME Shell without a dock extension shows no launcher counters at all. Wherever no icon badge is available — an unpacked archive, ./gradlew run , an unsupported desktop — korTTY falls back to the window title , which becomes (2) KorTTY while two agents wait and returns to KorTTY at zero. When an agent becomes blocked while no korTTY window is focused, the app additionally asks for attention once (the Dock icon bounces, the launcher entry is marked urgent). A desktop notification is shown when an agent needs a decision or finishes while you are not looking at its pane — that is, while its tab is not the selected tab of the front window, or its pane is not the focused one. The title names the agent ( Claude Code needs a decision , Claude Code finished ), the body shows the location and working directory followed by the matching screen line. A blocked state has to persist for a moment before it is reported, an agent is reported at most once every ten seconds, the same transition is never reported twice, and nothing is shown for the pane you are looking at. Notifications use the operating system's own service: the Notification Center on macOS , the tray balloon on Windows , and notify-send on Linux (through the host in the Flatpak package). Nothing leaves this computer — the text is the screen line korTTY already read. Both surfaces are on by default and have their own toggles under Settings → Terminal → Coding agents (see Enabling detection ). Switching the badge off clears it immediately; switching notifications off stops new ones without touching the badge. Keyboard shortcuts Shortcut Action Ctrl + Alt + G Show or hide the Coding Agents panel on its last-used side (right by default) Ctrl + Alt + N Jump to the next coding agent waiting for a decision, across windows Enter / Shift + Enter In the panel's prompt box: send the prompt / insert a line break On macOS use Cmd where Ctrl is shown. How detection works KorTTY only believes an agent is present when two independent observations agree — this double evidence keeps a stray claude in a shell script or a quoted prompt fragment from producing false alarms: Process tree. The pane's local shell has a known process ID. KorTTY walks that process's descendants and looks for the newest live one whose executable name — or, for node , bun and deno , the script it runs — belongs to one of the supported agents. Screen rules. The text currently visible in the pane (never the scrollback), together with the window title the agent sets and whether it switched to the alternate screen, is matched against the rule file of that agent. The first rule match confirms the process; from then on the rules decide the state. The screen is re-evaluated about 200 ms after the first change in a burst of output — further changes inside that window are coalesced — so a long stream of output triggers one evaluation per 200 ms window instead of one per line, and a short burst exactly one. A slow heartbeat additionally notices when the agent exits without printing anything, when the tab disconnects, or when you switch detection off. Rules are evaluated by priority; the first rule whose conditions all hold determines the state. If the agent process is confirmed but no rule matches, the rule file's fallback state (IDLE for the bundled files) is reported. Privacy Screen text is analysed only inside korTTY, in memory, on this computer. Nothing is sent to any service, written to disk, or handed to the AI assistant — detection is plain pattern matching against local rule files. If you prefer not to have local shell screens inspected at all, switch the feature off under Settings → Terminal → Coding agents . Enabling detection Detection is on by default and is controlled by three toggles in Settings → Terminal , section Coding agents . Changing them takes effect immediately for all open tabs — no restart or reconnect is needed. Setting Type Values Default Stored as Detect coding agents (Claude Code, Codex, Gemini CLI) in local shell tabs toggle — On codingAgentDetectionEnabled Desktop notification when a coding agent needs a decision or finishes while you are not looking at its pane toggle — On codingAgentNotificationsEnabled Show the number of agents waiting for a decision on the app icon toggle — On codingAgentAppBadgeEnabled When the detection toggle is off, korTTY neither walks the process tree nor reads the screen of any pane, and the dashboard marks, panel, strip, badge and notifications all fall silent. The panel's placement and width are not settings-dialog options: they follow the View → Coding Agents menu and are stored as codingAgentPanelPlacement ( HIDDEN , LEFT or RIGHT ) and codingAgentPanelWidth . Custom rules Each agent's screen rules live in a small JSON file. KorTTY ships one bundled file per agent and lets you replace it with your own copy under your configuration directory: ~/.kortty/coding-agents/claude-code.json ~/.kortty/coding-agents/codex.json ~/.kortty/coding-agents/gemini-cli.json An override replaces the bundled file for that agent as a whole — there is no merging, so start from the bundled file and edit it. The file must be a regular file (symbolic links are not followed), and its kind must match the file name. An invalid file — malformed JSON, an unknown key, a bad regular expression, a duplicate rule id, or a rule without any condition — is ignored with a warning in the log and the bundled rules stay in effect, so a typo can never silently disable detection. Rule files are read at startup; restart korTTY after editing one. Rule file schema The schema is strict: every key not listed here is an error, so misspelt keys are reported instead of being ignored. { \"kind\" : \"CLAUDE_CODE | CODEX | GEMINI_CLI\" , \"version\" : 1 , \"comment\" : \"optional free text, e.g. the agent version the rules were written against\" , \"fallbackState\" : \"IDLE | WORKING | BLOCKED | DONE | UNKNOWN (optional, default IDLE)\" , \"rules\" : [ { \"id\" : \"unique-rule-id\" , \"state\" : \"WORKING | BLOCKED | DONE | IDLE\" , \"priority\" : 100 , \"region\" : { \"bottomNonEmptyLines\" : 8 }, \"anyLineRegex\" : [ \"Java regex, matched per line\" ], \"regex\" : \"Java regex, matched against the whole region\" , \"contains\" : [ \"literal text that must all be present\" ], \"notContains\" : [ \"literal text that vetoes the rule\" ], \"title\" : \"Java regex matched against the window title\" , \"alternateScreen\" : false } ] } Field Required Meaning kind yes The agent this file describes; must match the file name ( claude-code , codex , gemini-cli ). version yes Schema version; currently always 1 . comment no Free text for your own notes, for example which agent version the rules were verified against. fallbackState no State reported while the agent process is present but no rule matches. Defaults to IDLE ; UNKNOWN makes detection stricter. rules yes The list of rules, evaluated by descending priority ; ties keep file order. May be empty. rules[].id yes Unique within the file; letters, digits, . , _ and - . Shown in diagnostics so you can see which rule fired. rules[].state yes The state this rule reports: WORKING , BLOCKED , DONE or IDLE . rules[].priority yes Higher values are checked first. rules[].region no { \"bottomNonEmptyLines\": N } restricts matching to the bottom N non-empty screen lines; 0 or absent means the whole screen. rules[].anyLineRegex no Regular expressions tried against each line of the region; the first matching line becomes the rule's evidence. rules[].regex no One regular expression tried against the region joined with newlines (multi-line, dot matches newline). rules[].contains no Literal substrings that must all occur in the region. rules[].notContains no Literal substrings whose presence anywhere in the region vetoes the rule. rules[].title no Regular expression that must match the window title set by the agent; never matches while no title has been received. rules[].alternateScreen no When present, the pane's alternate-screen flag must equal this value. A rule must declare at least one of anyLineRegex , regex , contains , title or alternateScreen . All conditions of a rule must hold for it to match. Regular expressions use Java syntax and are compiled with Unicode case folding; put (?i) at the start of a pattern to make it case-insensitive. Remember that JSON requires backslashes to be doubled, so \\s is written as \"\\\\s\" . Patterns in anyLineRegex are searched within a line (they need not match the whole line), and ^ and $ anchor to the line. Most agents keep their status line and prompt at the bottom of the screen, so a small bottomNonEmptyLines region keeps rules fast and prevents older output from matching. Example: bundled Claude Code rules { \"kind\" : \"CLAUDE_CODE\" , \"version\" : 1 , \"comment\" : \"Permission dialogs (Bash, Edit, Write, MCP) and the AskUserQuestion picker are BLOCKED; a tool call showing 'Running…' or the spinner status line with 'esc to interrupt' is WORKING; the empty input prompt with '? for shortcuts' is IDLE. The onboarding menus (theme picker, login method) intentionally match no rule. Claude Code does not use the alternate screen, so no rule depends on that flag. Re-verify against live sessions after an agent update.\" , \"fallbackState\" : \"IDLE\" , \"rules\" : [ { \"id\" : \"permission-prompt\" , \"state\" : \"BLOCKED\" , \"priority\" : 1000 , \"region\" : { \"bottomNonEmptyLines\" : 20 }, \"anyLineRegex\" : [ \"Do you want to (proceed|make this edit|create|run|allow)\" , \"Do you want to allow this connection\\\\?\" , \"Would you like to proceed\\\\?\" ], \"regex\" : \"^\\\\s*[│┃]?\\\\s*❯?\\\\s*1\\\\.\\\\s*Yes\\\\b\" , \"notContains\" : [ \"esc to interrupt\" ] }, { \"id\" : \"question-picker\" , \"state\" : \"BLOCKED\" , \"priority\" : 950 , \"region\" : { \"bottomNonEmptyLines\" : 20 }, \"anyLineRegex\" : [ \"(?i)\\\\bEsc to cancel\\\\b\" ], \"regex\" : \"(?i)Enter to (select|confirm)|Arrow keys to navigate|↑/?↓ to navigate|Review your answers\" , \"notContains\" : [ \"esc to interrupt\" , \"Enter to set as default\" ] }, { \"id\" : \"working-tool-running\" , \"state\" : \"WORKING\" , \"priority\" : 910 , \"region\" : { \"bottomNonEmptyLines\" : 12 }, \"anyLineRegex\" : [ \"^\\\\s*⎿\\\\s+Running…\" ] }, { \"id\" : \"working-spinner\" , \"state\" : \"WORKING\" , \"priority\" : 900 , \"region\" : { \"bottomNonEmptyLines\" : 8 }, \"anyLineRegex\" : [ \"(?i)esc to interrupt\" , \"^\\\\s*[✻✳✶✽✢·\\\\*]\\\\s+\\\\S.*…\" ] }, { \"id\" : \"idle-prompt\" , \"state\" : \"IDLE\" , \"priority\" : 100 , \"region\" : { \"bottomNonEmptyLines\" : 6 }, \"anyLineRegex\" : [ \"^\\\\s*[│┃]?\\\\s*[❯>]\\\\s*$\" , \"\\\\?\\\\s+for shortcuts\" ], \"notContains\" : [ \"esc to interrupt\" ] } ] } The two BLOCKED rules combine a per-line pattern with a whole-region regex , so a permission dialog is only reported when both the question and its 1. Yes option are on screen, and a question picker only together with its navigation footer — the onboarding menus of a fresh installation show a similar list but neither footer, and must not count as blocked. The notContains veto on the BLOCKED and IDLE rules matters too: while Claude Code is working, its status line stays on screen together with older dialog text, and the veto keeps the higher-priority prompt rules from firing on stale lines. Maintaining rules Agents change their interfaces often, and a rule that matched last month may silently stop matching after an update. KorTTY's bundled rules are therefore pinned by screen fixtures : for every agent, the repository keeps captured terminal screens with the state and rule they must produce, and the test suite fails as soon as a bundled rule no longer matches its fixture or a rule has no fixture at all. When you notice a wrong or missing state, the most useful report is the visible screen text of the pane at that moment, the agent and its version, and the state you expected — it becomes a new fixture and a rule fix in the next release. Until then, a local override file lets you correct the rule for yourself immediately. The repository ships a recorder for exactly this purpose: scripts/capture-coding-agent-fixtures.py runs an agent inside a pseudo-terminal and passes your keyboard through, so the session behaves like a normal terminal. Press F12 at every interesting moment — the permission dialog, the spinner, the empty prompt — and the visible screen is written to a fixture file; Ctrl + ] stops the recording. Each file starts with a expect state=REVIEW rule=REVIEW line: set the state and rule id you expect there, and drop the file into src/test/resources/coding-agents/<kind>/ ( claude-code , codex or gemini-cli ). From then on the test suite guards the rule against that screen, and a rule change that would break it is caught before release. Driving agents from a script Everything the Coding Agents panel does — listing agents, reading their state, prompting them, answering their questions, renaming them, starting a new one in a split pane — is also reachable from a local script or another coding agent through korTTY's control API and its kortty-cli client. It is off by default; the agent.* commands need a detected agent, so they need detection switched on as well. Limitations and troubleshooting Flatpak. In the Flatpak package the local shell runs on the host through flatpak-spawn , so the process ID korTTY holds belongs to the sandbox-side helper and the agent's process tree is not visible. No agent is detected in Flatpak local shells. Remote clients as the shell command. A local shell tab whose configured shell command is itself ssh or mosh runs the agent on another machine; the process tree ends at the client and no agent is detected. The same applies to any SSH or Mosh tab. Unusual wrappers. An agent started through a launcher that korTTY does not recognise — a custom shell script that exec s into a differently named binary, a container, or a terminal multiplexer running outside the tab — is not identified, because the executable name in the process tree does not map to a known agent. Wrong or flickering state. The agent's screen has changed, or a dialog is rendered in a way the bundled rules do not cover. Copy the bundled rule file to ~/.kortty/coding-agents/<agent>.json , adjust the pattern, restart korTTY, and report the screen text so the bundled rules can be fixed. No state at all although the agent is running. Check that the toggle in Settings → Terminal → Coding agents is on, that the tab is a Local Shell tab, and look for a coding-agents warning in the log — an invalid override file falls back to the bundled rules, but a bundled rule file that never matches your agent version leaves the agent unconfirmed. No badge on GNOME. Stock GNOME Shell renders no launcher counters, and korTTY cannot detect whether a dock extension that would is installed, so the count is emitted but may stay invisible; the window-title fallback is used only where korTTY knows for certain that no icon badge exists (no installed desktop file, no reachable session bus, ./gradlew run ). No notification although the agent waited. Notifications are suppressed for the pane you are looking at, for a blocked state that lasted less than a moment, for a second report of the same transition, and within ten seconds of the previous one for that pane; on Linux notify-send must be on the PATH, and the setting under Settings → Terminal → Coding agents must be on. The prompt was refused. A waiting agent must be answered first (y, n, Enter or Esc), and a first line that begins with korTTY's own AI shortcut command must be rephrased — see The Coding Agents panel .","title": "Coding agents"},{"location": "features/coding-agents.html#coding-agents","text": "KorTTY recognises when a terminal-based coding agent — Claude Code , Codex or Gemini CLI — is running inside one of your local shell tabs and keeps track of what it is currently doing: working, waiting for your answer, finished, or sitting idle at its prompt. The analysis happens entirely inside korTTY on your own machine. What it finds is shown wherever you look: as a glyph in the tab title, as chips and accents in the dashboard, in a dockable Coding Agents panel with quick answers and a prompt box, as a strip in the status bar, as a count on the app icon and, when you are not looking at the pane, as a desktop notification. This page explains what is detected, how each of these surfaces works, how to switch things off, and how to adjust or extend the screen rules when an agent changes its user interface.","title": "Coding agents"},{"location": "features/coding-agents.html#what-it-detects","text": "Detection covers the three agents below, started from a Local Shell tab — directly, through a package-manager wrapper such as npx , or as a node , bun or deno script. SSH and Mosh tabs are not analysed, because the agent runs on the remote machine and korTTY cannot see its process. Agent Recognised executables Claude Code claude , claude-code Codex codex Gemini CLI gemini Every split pane is tracked on its own, so a tab with two panes can show one agent working while the other waits for a permission decision. A pane that runs no agent, or whose agent has exited, simply has no detection result.","title": "What it detects"},{"location": "features/coding-agents.html#states","text": "A detected agent is always in exactly one of five states. The list is ordered by urgency: when several panes are summarised, the most urgent state wins. State Meaning BLOCKED The agent is waiting for you — a permission dialog ( Do you want to proceed? ), a question picker, or a (y/n) prompt is on screen. DONE The agent has finished. Either it shows an explicit completion screen that a rule recognises, or — far more often — it went from WORKING back to its empty prompt while you were not looking at its pane. KorTTY then keeps it marked DONE (✓) until you look at the pane; the moment its tab is selected and its pane focused in the front window, it becomes IDLE. WORKING The agent is thinking, running a tool or streaming output — typically recognisable by its spinner line and the esc to interrupt hint. IDLE The agent is running and its input box is empty, waiting for your next instruction. UNKNOWN The agent process is present but the screen matches none of the rules and the rule file asks for a strict result instead of a fallback.","title": "States"},{"location": "features/coding-agents.html#dashboard-marking","text": "Open the dashboard with View → Show Dashboard ( Ctrl + Shift + D ) and every connection row that hosts a coding agent shows a chip after the protocol badge: the state glyph, the agent's short name (or the alias you gave it) and, for an agent that is waiting, working or done, how long it has been in that state ( ✋ claude 2:14 ). An idle agent gets a dim, outlined chip so you still see that one is present. A row that also runs korTTY's own AI agent merges both marks into one chip. The row itself carries a coloured accent bar on its left edge — amber while the agent waits for you (the row is tinted as well), blue while it works, green when it is done — and the status dot of a waiting agent pulses while the window is in front and animations are enabled. Group and environment rows above a marked connection show a rollup chip such as ✋ 1 · ⚡ 2 and take the accent of their most urgent child, and the footer appends the same rollup to the connection count ( 3 of 5 connected · ✋ 1 · ⚡ 2 ), so a waiting agent is visible even when its group is collapsed. A split tab grows pane rows below the connection ( Pane 2 · api , named after the pane's working directory, with your home directory written ~ as the shell writes it), one per pane, so each pane can be reached on its own. Every pane is listed, whether or not an agent was detected in it: a split where one pane runs an agent and the other does not is the ordinary case, and showing only the agent's pane would hide half the tab. A pane without an agent carries no chip and no accent. An unsplit tab gets no pane rows, because its connection row already is the pane. When an agent becomes blocked, the dashboard expands the tree down to its row once per transition — a waiting agent is never hidden inside a collapsed group — and respects Collapse all afterwards. The context menu of a marked row adds Focus Pane , which selects the tab and focuses exactly that pane, Show in Coding Agents Panel , which docks the panel and selects the agent, and, while the agent waits for you, Send Enter , Send Esc and Interrupt (Ctrl+C) to answer the dialog without leaving the dashboard. The tab title shows the same glyph as the chip — ✋ waiting, ⚡ working, ✓ done, merged with korTTY's own AI-agent glyph where both apply.","title": "Dashboard marking"},{"location": "features/coding-agents.html#the-coding-agents-panel","text": "The panel lists every detected agent of every korTTY window in one place. Open it with View → Coding Agents → Dock Left / Dock Right , toggle it on its last-used side with Show/Hide ( Ctrl + Alt + G ), from the ⋯ menu inside the panel, from the status strip's context menu, or with Show in Coding Agents Panel in the dashboard. Placement and width are remembered across restarts. Agents that wait for you are listed first (longest waiting on top), then the working, done and idle ones in window, tab and pane order; the row of the pane you are currently in carries a left accent. Every row shows the state dot and name, a state chip such as ✋ Waiting for you · 2:14 , the location line ( Window 2 › api › Pane 2 · ~/proj/api ) and the last screen line that matched a rule — the evidence. Below them sit the quick keys : Focus brings the pane to the front, y , n , Enter and Esc answer a dialog (they are highlighted while the agent waits for you), ↑ and ↓ move through a question picker, Ctrl+C interrupts, Explain opens a drawer with the full detection explanation (agent, state, matching rule, evidence, process and time in state), and Rename… gives the agent an alias that replaces its name in every chip, row and notification — an empty alias restores the agent name, and the alias survives a reconnect but is dropped when the pane closes. Every key goes to the pane's own terminal connection, exactly as if you had typed it there; the buttons are disabled while the pane is not connected. The prompt box at the bottom sends longer text to the agent selected in the target list: Enter sends, Shift + Enter inserts a line break. A single line is sent followed by Enter. A multi-line prompt is wrapped in bracketed paste — so the agent receives it as one pasted block instead of several submitted lines — but only when that agent has switched bracketed paste on, which Claude Code, Codex and Gemini CLI all do at their prompt; otherwise the lines are sent as typed. Two prompts are refused with a message in the panel's status line instead of being sent: while the agent is waiting for a decision the Send button stays disabled, because the text would land inside the permission dialog — answer it with y, n, Enter or Esc first; and a prompt whose first line starts with korTTY's own AI shortcut (the command name configured under Settings → AI, agent by default) is rejected, because korTTY's shortcut filter would divert that line to its own AI agent instead of the coding agent — rephrase the line. Errors of a quick key or prompt (pane closed, not connected, write failed) appear in the same status line for a few seconds, never as a dialog, and a successful send confirms with Sent to Claude Code . The Next blocked button in the panel header jumps to the next agent that waits for you, across all windows and wrapping around; it is the same action as Ctrl + Alt + N and a click on the status strip.","title": "The Coding Agents panel"},{"location": "features/coding-agents.html#status-strip","text": "The right end of the status bar shows a compact strip while at least one coding agent is known: up to three chips for waiting, working and done agents ( ✋ 1 · ⚡ 2 · ✓ 1 , zero counts are omitted), with the waiting dot pulsing while the window is in front. A click jumps to the next agent waiting for you — or, when none is waiting, to the first agent in display order — and a right-click offers Show/Hide for the panel and Next Blocked Agent . The tooltip summarises the counts. The strip disappears together with the status bar when you hide it under View .","title": "Status strip"},{"location": "features/coding-agents.html#app-icon-badge-and-notifications","text": "The number of agents waiting for a decision — across all windows — is shown as a badge on the app icon so you notice it while working in another application. How the badge is drawn depends on the platform: on macOS the packaged app sets the Dock badge; on Windows korTTY draws the count into its own taskbar and title-bar icon; on Linux it emits the launcher-entry signal that KDE Plasma, Ubuntu Dock and Dash to Dock render on the launcher icon — this needs an installed desktop file (the deb, rpm or pacman package) and a reachable session bus, which korTTY talks to directly. Stock GNOME Shell without a dock extension shows no launcher counters at all. Wherever no icon badge is available — an unpacked archive, ./gradlew run , an unsupported desktop — korTTY falls back to the window title , which becomes (2) KorTTY while two agents wait and returns to KorTTY at zero. When an agent becomes blocked while no korTTY window is focused, the app additionally asks for attention once (the Dock icon bounces, the launcher entry is marked urgent). A desktop notification is shown when an agent needs a decision or finishes while you are not looking at its pane — that is, while its tab is not the selected tab of the front window, or its pane is not the focused one. The title names the agent ( Claude Code needs a decision , Claude Code finished ), the body shows the location and working directory followed by the matching screen line. A blocked state has to persist for a moment before it is reported, an agent is reported at most once every ten seconds, the same transition is never reported twice, and nothing is shown for the pane you are looking at. Notifications use the operating system's own service: the Notification Center on macOS , the tray balloon on Windows , and notify-send on Linux (through the host in the Flatpak package). Nothing leaves this computer — the text is the screen line korTTY already read. Both surfaces are on by default and have their own toggles under Settings → Terminal → Coding agents (see Enabling detection ). Switching the badge off clears it immediately; switching notifications off stops new ones without touching the badge.","title": "App icon badge and notifications"},{"location": "features/coding-agents.html#keyboard-shortcuts","text": "Shortcut Action Ctrl + Alt + G Show or hide the Coding Agents panel on its last-used side (right by default) Ctrl + Alt + N Jump to the next coding agent waiting for a decision, across windows Enter / Shift + Enter In the panel's prompt box: send the prompt / insert a line break On macOS use Cmd where Ctrl is shown.","title": "Keyboard shortcuts"},{"location": "features/coding-agents.html#how-detection-works","text": "KorTTY only believes an agent is present when two independent observations agree — this double evidence keeps a stray claude in a shell script or a quoted prompt fragment from producing false alarms: Process tree. The pane's local shell has a known process ID. KorTTY walks that process's descendants and looks for the newest live one whose executable name — or, for node , bun and deno , the script it runs — belongs to one of the supported agents. Screen rules. The text currently visible in the pane (never the scrollback), together with the window title the agent sets and whether it switched to the alternate screen, is matched against the rule file of that agent. The first rule match confirms the process; from then on the rules decide the state. The screen is re-evaluated about 200 ms after the first change in a burst of output — further changes inside that window are coalesced — so a long stream of output triggers one evaluation per 200 ms window instead of one per line, and a short burst exactly one. A slow heartbeat additionally notices when the agent exits without printing anything, when the tab disconnects, or when you switch detection off. Rules are evaluated by priority; the first rule whose conditions all hold determines the state. If the agent process is confirmed but no rule matches, the rule file's fallback state (IDLE for the bundled files) is reported. Privacy Screen text is analysed only inside korTTY, in memory, on this computer. Nothing is sent to any service, written to disk, or handed to the AI assistant — detection is plain pattern matching against local rule files. If you prefer not to have local shell screens inspected at all, switch the feature off under Settings → Terminal → Coding agents .","title": "How detection works"},{"location": "features/coding-agents.html#enabling-detection","text": "Detection is on by default and is controlled by three toggles in Settings → Terminal , section Coding agents . Changing them takes effect immediately for all open tabs — no restart or reconnect is needed. Setting Type Values Default Stored as Detect coding agents (Claude Code, Codex, Gemini CLI) in local shell tabs toggle — On codingAgentDetectionEnabled Desktop notification when a coding agent needs a decision or finishes while you are not looking at its pane toggle — On codingAgentNotificationsEnabled Show the number of agents waiting for a decision on the app icon toggle — On codingAgentAppBadgeEnabled When the detection toggle is off, korTTY neither walks the process tree nor reads the screen of any pane, and the dashboard marks, panel, strip, badge and notifications all fall silent. The panel's placement and width are not settings-dialog options: they follow the View → Coding Agents menu and are stored as codingAgentPanelPlacement ( HIDDEN , LEFT or RIGHT ) and codingAgentPanelWidth .","title": "Enabling detection"},{"location": "features/coding-agents.html#custom-rules","text": "Each agent's screen rules live in a small JSON file. KorTTY ships one bundled file per agent and lets you replace it with your own copy under your configuration directory: ~/.kortty/coding-agents/claude-code.json ~/.kortty/coding-agents/codex.json ~/.kortty/coding-agents/gemini-cli.json An override replaces the bundled file for that agent as a whole — there is no merging, so start from the bundled file and edit it. The file must be a regular file (symbolic links are not followed), and its kind must match the file name. An invalid file — malformed JSON, an unknown key, a bad regular expression, a duplicate rule id, or a rule without any condition — is ignored with a warning in the log and the bundled rules stay in effect, so a typo can never silently disable detection. Rule files are read at startup; restart korTTY after editing one.","title": "Custom rules"},{"location": "features/coding-agents.html#rule-file-schema","text": "The schema is strict: every key not listed here is an error, so misspelt keys are reported instead of being ignored. { \"kind\" : \"CLAUDE_CODE | CODEX | GEMINI_CLI\" , \"version\" : 1 , \"comment\" : \"optional free text, e.g. the agent version the rules were written against\" , \"fallbackState\" : \"IDLE | WORKING | BLOCKED | DONE | UNKNOWN (optional, default IDLE)\" , \"rules\" : [ { \"id\" : \"unique-rule-id\" , \"state\" : \"WORKING | BLOCKED | DONE | IDLE\" , \"priority\" : 100 , \"region\" : { \"bottomNonEmptyLines\" : 8 }, \"anyLineRegex\" : [ \"Java regex, matched per line\" ], \"regex\" : \"Java regex, matched against the whole region\" , \"contains\" : [ \"literal text that must all be present\" ], \"notContains\" : [ \"literal text that vetoes the rule\" ], \"title\" : \"Java regex matched against the window title\" , \"alternateScreen\" : false } ] } Field Required Meaning kind yes The agent this file describes; must match the file name ( claude-code , codex , gemini-cli ). version yes Schema version; currently always 1 . comment no Free text for your own notes, for example which agent version the rules were verified against. fallbackState no State reported while the agent process is present but no rule matches. Defaults to IDLE ; UNKNOWN makes detection stricter. rules yes The list of rules, evaluated by descending priority ; ties keep file order. May be empty. rules[].id yes Unique within the file; letters, digits, . , _ and - . Shown in diagnostics so you can see which rule fired. rules[].state yes The state this rule reports: WORKING , BLOCKED , DONE or IDLE . rules[].priority yes Higher values are checked first. rules[].region no { \"bottomNonEmptyLines\": N } restricts matching to the bottom N non-empty screen lines; 0 or absent means the whole screen. rules[].anyLineRegex no Regular expressions tried against each line of the region; the first matching line becomes the rule's evidence. rules[].regex no One regular expression tried against the region joined with newlines (multi-line, dot matches newline). rules[].contains no Literal substrings that must all occur in the region. rules[].notContains no Literal substrings whose presence anywhere in the region vetoes the rule. rules[].title no Regular expression that must match the window title set by the agent; never matches while no title has been received. rules[].alternateScreen no When present, the pane's alternate-screen flag must equal this value. A rule must declare at least one of anyLineRegex , regex , contains , title or alternateScreen . All conditions of a rule must hold for it to match. Regular expressions use Java syntax and are compiled with Unicode case folding; put (?i) at the start of a pattern to make it case-insensitive. Remember that JSON requires backslashes to be doubled, so \\s is written as \"\\\\s\" . Patterns in anyLineRegex are searched within a line (they need not match the whole line), and ^ and $ anchor to the line. Most agents keep their status line and prompt at the bottom of the screen, so a small bottomNonEmptyLines region keeps rules fast and prevents older output from matching.","title": "Rule file schema"},{"location": "features/coding-agents.html#example-bundled-claude-code-rules","text": "{ \"kind\" : \"CLAUDE_CODE\" , \"version\" : 1 , \"comment\" : \"Permission dialogs (Bash, Edit, Write, MCP) and the AskUserQuestion picker are BLOCKED; a tool call showing 'Running…' or the spinner status line with 'esc to interrupt' is WORKING; the empty input prompt with '? for shortcuts' is IDLE. The onboarding menus (theme picker, login method) intentionally match no rule. Claude Code does not use the alternate screen, so no rule depends on that flag. Re-verify against live sessions after an agent update.\" , \"fallbackState\" : \"IDLE\" , \"rules\" : [ { \"id\" : \"permission-prompt\" , \"state\" : \"BLOCKED\" , \"priority\" : 1000 , \"region\" : { \"bottomNonEmptyLines\" : 20 }, \"anyLineRegex\" : [ \"Do you want to (proceed|make this edit|create|run|allow)\" , \"Do you want to allow this connection\\\\?\" , \"Would you like to proceed\\\\?\" ], \"regex\" : \"^\\\\s*[│┃]?\\\\s*❯?\\\\s*1\\\\.\\\\s*Yes\\\\b\" , \"notContains\" : [ \"esc to interrupt\" ] }, { \"id\" : \"question-picker\" , \"state\" : \"BLOCKED\" , \"priority\" : 950 , \"region\" : { \"bottomNonEmptyLines\" : 20 }, \"anyLineRegex\" : [ \"(?i)\\\\bEsc to cancel\\\\b\" ], \"regex\" : \"(?i)Enter to (select|confirm)|Arrow keys to navigate|↑/?↓ to navigate|Review your answers\" , \"notContains\" : [ \"esc to interrupt\" , \"Enter to set as default\" ] }, { \"id\" : \"working-tool-running\" , \"state\" : \"WORKING\" , \"priority\" : 910 , \"region\" : { \"bottomNonEmptyLines\" : 12 }, \"anyLineRegex\" : [ \"^\\\\s*⎿\\\\s+Running…\" ] }, { \"id\" : \"working-spinner\" , \"state\" : \"WORKING\" , \"priority\" : 900 , \"region\" : { \"bottomNonEmptyLines\" : 8 }, \"anyLineRegex\" : [ \"(?i)esc to interrupt\" , \"^\\\\s*[✻✳✶✽✢·\\\\*]\\\\s+\\\\S.*…\" ] }, { \"id\" : \"idle-prompt\" , \"state\" : \"IDLE\" , \"priority\" : 100 , \"region\" : { \"bottomNonEmptyLines\" : 6 }, \"anyLineRegex\" : [ \"^\\\\s*[│┃]?\\\\s*[❯>]\\\\s*$\" , \"\\\\?\\\\s+for shortcuts\" ], \"notContains\" : [ \"esc to interrupt\" ] } ] } The two BLOCKED rules combine a per-line pattern with a whole-region regex , so a permission dialog is only reported when both the question and its 1. Yes option are on screen, and a question picker only together with its navigation footer — the onboarding menus of a fresh installation show a similar list but neither footer, and must not count as blocked. The notContains veto on the BLOCKED and IDLE rules matters too: while Claude Code is working, its status line stays on screen together with older dialog text, and the veto keeps the higher-priority prompt rules from firing on stale lines.","title": "Example: bundled Claude Code rules"},{"location": "features/coding-agents.html#maintaining-rules","text": "Agents change their interfaces often, and a rule that matched last month may silently stop matching after an update. KorTTY's bundled rules are therefore pinned by screen fixtures : for every agent, the repository keeps captured terminal screens with the state and rule they must produce, and the test suite fails as soon as a bundled rule no longer matches its fixture or a rule has no fixture at all. When you notice a wrong or missing state, the most useful report is the visible screen text of the pane at that moment, the agent and its version, and the state you expected — it becomes a new fixture and a rule fix in the next release. Until then, a local override file lets you correct the rule for yourself immediately. The repository ships a recorder for exactly this purpose: scripts/capture-coding-agent-fixtures.py runs an agent inside a pseudo-terminal and passes your keyboard through, so the session behaves like a normal terminal. Press F12 at every interesting moment — the permission dialog, the spinner, the empty prompt — and the visible screen is written to a fixture file; Ctrl + ] stops the recording. Each file starts with a expect state=REVIEW rule=REVIEW line: set the state and rule id you expect there, and drop the file into src/test/resources/coding-agents/<kind>/ ( claude-code , codex or gemini-cli ). From then on the test suite guards the rule against that screen, and a rule change that would break it is caught before release.","title": "Maintaining rules"},{"location": "features/coding-agents.html#driving-agents-from-a-script","text": "Everything the Coding Agents panel does — listing agents, reading their state, prompting them, answering their questions, renaming them, starting a new one in a split pane — is also reachable from a local script or another coding agent through korTTY's control API and its kortty-cli client. It is off by default; the agent.* commands need a detected agent, so they need detection switched on as well.","title": "Driving agents from a script"},{"location": "features/coding-agents.html#limitations-and-troubleshooting","text": "Flatpak. In the Flatpak package the local shell runs on the host through flatpak-spawn , so the process ID korTTY holds belongs to the sandbox-side helper and the agent's process tree is not visible. No agent is detected in Flatpak local shells. Remote clients as the shell command. A local shell tab whose configured shell command is itself ssh or mosh runs the agent on another machine; the process tree ends at the client and no agent is detected. The same applies to any SSH or Mosh tab. Unusual wrappers. An agent started through a launcher that korTTY does not recognise — a custom shell script that exec s into a differently named binary, a container, or a terminal multiplexer running outside the tab — is not identified, because the executable name in the process tree does not map to a known agent. Wrong or flickering state. The agent's screen has changed, or a dialog is rendered in a way the bundled rules do not cover. Copy the bundled rule file to ~/.kortty/coding-agents/<agent>.json , adjust the pattern, restart korTTY, and report the screen text so the bundled rules can be fixed. No state at all although the agent is running. Check that the toggle in Settings → Terminal → Coding agents is on, that the tab is a Local Shell tab, and look for a coding-agents warning in the log — an invalid override file falls back to the bundled rules, but a bundled rule file that never matches your agent version leaves the agent unconfirmed. No badge on GNOME. Stock GNOME Shell renders no launcher counters, and korTTY cannot detect whether a dock extension that would is installed, so the count is emitted but may stay invisible; the window-title fallback is used only where korTTY knows for certain that no icon badge exists (no installed desktop file, no reachable session bus, ./gradlew run ). No notification although the agent waited. Notifications are suppressed for the pane you are looking at, for a blocked state that lasted less than a moment, for a second report of the same transition, and within ten seconds of the previous one for that pane; on Linux notify-send must be on the PATH, and the setting under Settings → Terminal → Coding agents must be on. The prompt was refused. A waiting agent must be answered first (y, n, Enter or Esc), and a first line that begins with korTTY's own AI shortcut command must be rephrased — see The Coding Agents panel .","title": "Limitations and troubleshooting"},{"location": "features/connections.html","text": "Connections korTTY manages SSH, Mosh and local-shell connections through three entry points: Quick Connect , the Connection Manager , and saved Projects . Quick Connect Open with Ctrl + K (or Connections → Quick Connect… ). Enter host, port, username and authentication, and connect without saving. Frequently used connections appear as quick buttons; a live search filters them. Saved connections can be picked from a dropdown with its own search field that matches name, host or tag ( * works as a wildcard); a saved connection's tag is shown next to its name (🏷) in the dropdown and in the quick buttons' tooltips. Connection Manager Connections → Manage Connections… opens a searchable tree of saved connections (optionally grouped); the search field matches name, host, IP address or tag , with * as a wildcard. From here you create, edit, duplicate, delete, tag, import and export connections. Creating / editing a connection The connection editor has these tabs: Tab Contents Connection Host, port, username, protocol (SSH / Mosh / Local Shell), authentication (password / key / keyboard-interactive), Host key verification (use default / verify / don't verify), group/folder assignment and an optional free-text tag . For Local Shell connections host, port, username and authentication are not required and are disabled. Terminal Settings Per-connection colors, font, ANSI/TrueColor handling, terminal effect SSH Tunnels Local / remote / dynamic port forwarding Jump Server Bastion-host chaining Terminal Logging Writes this connection's terminal output to a file — folder, format, daily rotation, compression and retention. See Terminal logging . Journal Per-connection session journal : enable journaling for this connection and configure its capture log and AI summarization Window Geometry Saved size/position for this connection AI Per-connection AI defaults: the AI profile and AI Skills used by terminal AI features on this connection Tags Every saved connection can carry one optional free-text tag — a label such as prod , staging or a customer name — independent of the group/folder hierarchy. Set it on the connection editor's Connection tab (next to the group), or in bulk in the Connection Manager. Tags are stored with the connection in connections.xml and survive duplicating, exporting and importing. Visible — tagged connections show a 🏷 badge after their name in the Connection Manager tree and in Quick Connect's saved-connections dropdown; the tag also appears in the tooltip of the frequently-used quick buttons. Searchable — the Connection Manager search (local and Teamwork tabs) and Quick Connect's saved-connections search match tags as well as names and hosts. Bulk assign / remove — select one or more servers and choose Assign Tag from the context menu to tag them in one step. The prompt is pre-filled when all selected connections already share the same tag, and clearing that pre-filled value removes the tag. Remove Tag clears the tag and is enabled only while the selection contains at least one tagged connection. The same two entries on a folder's context menu apply to every connection in that folder, subfolders included. Export by tag — once at least one tag exists, the Connection Manager's export dialog offers a Connections to export choice: keep the pre-selected connections, or export all connections with these tags — pick one or more tags from the list, the header's connection count follows the selection live, and the export button stays disabled while nothing matches. Protocols SSH Mosh Local Shell Standard SSH via Apache MINA SSHD. Supports password, public-key and keyboard-interactive authentication, keep-alive, and OSC 8 clickable hyperlinks. Roaming, latency-friendly Mosh transport (mosh4j). The Mosh backend is bundled in native builds; existing connections need no migration. Opens the local machine's shell in a terminal tab (no network) via a pty4j-backed pseudo-terminal. Host, port, username and authentication are not required. See Local Shell below. SSH host-key verification Interactive Terminal and SFTP connections use the same trust-on-first-use (TOFU) host-key store. Mosh uses it for the SSH bootstrap as well. Trust is keyed by the normalized host name and port, so different saved connections to the same endpoint share one decision. On the first connection, korTTY shows the key algorithm and OpenSSH SHA-256 fingerprint. Verify that fingerprint with the server administrator before selecting Yes ; No is the safe default. A matching key is accepted silently on later connections. If the server presents a different key, korTTY hard-blocks the connection, shows the expected and offered fingerprints, and does not retry because repeating the attempt cannot resolve a possible man-in-the-middle attack. The first-use prompt can be turned off for hosts where it is not wanted — set Host key verification on the connection editor's Connection tab or in Quick Connect ( Use default / Verify / Don't verify ), per group via the Connection Manager's group context menu, or globally under Settings → Terminal . The relaxation is accept-new only: an unknown key is pinned without a prompt, but a key that differs from one already pinned for that host is still hard-blocked. See Relaxing host-key verification . The interactive pins are stored atomically in ~/.kortty/ssh-host-keys.properties , with cross-process locking so two korTTY windows cannot overwrite each other's decisions. These endpoint-based pins are separate from the connection-ID-based pins used by unattended JobScheduler SSH, SFTP, and Rsync jobs. When a new split connection is opened, the SSH handshake runs on a worker while a progress dialog keeps the JavaFX interface responsive. This allows both host-key confirmation and keyboard-interactive authentication to complete without blocking the UI. Local Shell A Local Shell connection spawns a local pseudo-terminal (PTY) on your own machine instead of connecting to a remote host. It is selectable in both Quick Connect and the Connection Manager ; for these connections host, port, username and authentication are not required (and are disabled in the dialogs), and no password prompt is shown. Choosing a shell Platform Options Windows PowerShell (default) or cmd.exe . Git Bash , Cygwin and WSL are also offered as presets — but only when actually installed (Git Bash/Cygwin are detected via their usual install locations / PATH ; WSL appears only when wsl.exe is present and at least one distribution is installed). macOS / Linux Defaults to your $SHELL (falling back to /bin/zsh or /bin/bash ). A free-form Custom command field accepts any executable with arguments (e.g. pwsh.exe , wsl.exe -d Ubuntu , a Git Bash path), and an optional start directory can be set. The command parser is quote-aware, so shell paths containing spaces — like \"C:\\Program Files\\Git\\bin\\bash.exe\" — launch correctly. When korTTY runs from its Flatpak package, the local shell is started on the host through flatpak-spawn --host , including the selected start directory and terminal locale. The package has host-filesystem access so terminal file actions can use host paths. Because the sandbox-side process ID belongs to flatpak-spawn rather than the host shell, current-directory tracking uses trusted absolute prompt paths instead of reading /proc/<pid>/cwd ; if no safe host path can be established, path-dependent actions stop with an explicit error. Terminal features in local shells Terminal logging and recording, plus the AI input/data hooks, work for local shells via a shared ObservableTtyConnector interface. Typed and pasted agent requests use the same byte-level input path, and terminal file actions plus local agent runs follow the interactive shell's current directory. macOS/Linux use the local process directory; native PowerShell and cmd use absolute prompt paths. WSL, Git Bash, Cygwin, and custom commands are best-effort when their shell path namespace differs from the host filesystem, and an unmappable directory produces an explicit error instead of a wrong-file fallback. Features that depend on an SSH channel stay SSH-only. AI Agent in local shells The AI Agent and AI Planning also run in local shells on Windows, macOS and Linux — see AI assistant . Tunnels & jump servers SSH tunnels — forward ports through the connection: local ( -L ), remote ( -R ) or dynamic / SOCKS ( -D ). Jump server (bastion) — route the connection through an intermediate host; SSH terminal and SFTP sessions both hop through it. See Jump Server . Import from other clients Connections → Import… reads connection files from MTPuTTY , MobaXterm and PuTTY Connection Manager , with group filtering and credential handling. More to come This page is part of the scaffolded guide. The full feature library — SFTP, snippets, JobScheduler, AI assistant & tools, terminal recording, security, and the complete settings tables — is being filled in next.","title": "Connections"},{"location": "features/connections.html#connections","text": "korTTY manages SSH, Mosh and local-shell connections through three entry points: Quick Connect , the Connection Manager , and saved Projects .","title": "Connections"},{"location": "features/connections.html#quick-connect","text": "Open with Ctrl + K (or Connections → Quick Connect… ). Enter host, port, username and authentication, and connect without saving. Frequently used connections appear as quick buttons; a live search filters them. Saved connections can be picked from a dropdown with its own search field that matches name, host or tag ( * works as a wildcard); a saved connection's tag is shown next to its name (🏷) in the dropdown and in the quick buttons' tooltips.","title": "Quick Connect"},{"location": "features/connections.html#connection-manager","text": "Connections → Manage Connections… opens a searchable tree of saved connections (optionally grouped); the search field matches name, host, IP address or tag , with * as a wildcard. From here you create, edit, duplicate, delete, tag, import and export connections.","title": "Connection Manager"},{"location": "features/connections.html#creating-editing-a-connection","text": "The connection editor has these tabs: Tab Contents Connection Host, port, username, protocol (SSH / Mosh / Local Shell), authentication (password / key / keyboard-interactive), Host key verification (use default / verify / don't verify), group/folder assignment and an optional free-text tag . For Local Shell connections host, port, username and authentication are not required and are disabled. Terminal Settings Per-connection colors, font, ANSI/TrueColor handling, terminal effect SSH Tunnels Local / remote / dynamic port forwarding Jump Server Bastion-host chaining Terminal Logging Writes this connection's terminal output to a file — folder, format, daily rotation, compression and retention. See Terminal logging . Journal Per-connection session journal : enable journaling for this connection and configure its capture log and AI summarization Window Geometry Saved size/position for this connection AI Per-connection AI defaults: the AI profile and AI Skills used by terminal AI features on this connection","title": "Creating / editing a connection"},{"location": "features/connections.html#tags","text": "Every saved connection can carry one optional free-text tag — a label such as prod , staging or a customer name — independent of the group/folder hierarchy. Set it on the connection editor's Connection tab (next to the group), or in bulk in the Connection Manager. Tags are stored with the connection in connections.xml and survive duplicating, exporting and importing. Visible — tagged connections show a 🏷 badge after their name in the Connection Manager tree and in Quick Connect's saved-connections dropdown; the tag also appears in the tooltip of the frequently-used quick buttons. Searchable — the Connection Manager search (local and Teamwork tabs) and Quick Connect's saved-connections search match tags as well as names and hosts. Bulk assign / remove — select one or more servers and choose Assign Tag from the context menu to tag them in one step. The prompt is pre-filled when all selected connections already share the same tag, and clearing that pre-filled value removes the tag. Remove Tag clears the tag and is enabled only while the selection contains at least one tagged connection. The same two entries on a folder's context menu apply to every connection in that folder, subfolders included. Export by tag — once at least one tag exists, the Connection Manager's export dialog offers a Connections to export choice: keep the pre-selected connections, or export all connections with these tags — pick one or more tags from the list, the header's connection count follows the selection live, and the export button stays disabled while nothing matches.","title": "Tags"},{"location": "features/connections.html#protocols","text": "SSH Mosh Local Shell Standard SSH via Apache MINA SSHD. Supports password, public-key and keyboard-interactive authentication, keep-alive, and OSC 8 clickable hyperlinks. Roaming, latency-friendly Mosh transport (mosh4j). The Mosh backend is bundled in native builds; existing connections need no migration. Opens the local machine's shell in a terminal tab (no network) via a pty4j-backed pseudo-terminal. Host, port, username and authentication are not required. See Local Shell below.","title": "Protocols"},{"location": "features/connections.html#ssh-host-key-verification","text": "Interactive Terminal and SFTP connections use the same trust-on-first-use (TOFU) host-key store. Mosh uses it for the SSH bootstrap as well. Trust is keyed by the normalized host name and port, so different saved connections to the same endpoint share one decision. On the first connection, korTTY shows the key algorithm and OpenSSH SHA-256 fingerprint. Verify that fingerprint with the server administrator before selecting Yes ; No is the safe default. A matching key is accepted silently on later connections. If the server presents a different key, korTTY hard-blocks the connection, shows the expected and offered fingerprints, and does not retry because repeating the attempt cannot resolve a possible man-in-the-middle attack. The first-use prompt can be turned off for hosts where it is not wanted — set Host key verification on the connection editor's Connection tab or in Quick Connect ( Use default / Verify / Don't verify ), per group via the Connection Manager's group context menu, or globally under Settings → Terminal . The relaxation is accept-new only: an unknown key is pinned without a prompt, but a key that differs from one already pinned for that host is still hard-blocked. See Relaxing host-key verification . The interactive pins are stored atomically in ~/.kortty/ssh-host-keys.properties , with cross-process locking so two korTTY windows cannot overwrite each other's decisions. These endpoint-based pins are separate from the connection-ID-based pins used by unattended JobScheduler SSH, SFTP, and Rsync jobs. When a new split connection is opened, the SSH handshake runs on a worker while a progress dialog keeps the JavaFX interface responsive. This allows both host-key confirmation and keyboard-interactive authentication to complete without blocking the UI.","title": "SSH host-key verification"},{"location": "features/connections.html#local-shell","text": "A Local Shell connection spawns a local pseudo-terminal (PTY) on your own machine instead of connecting to a remote host. It is selectable in both Quick Connect and the Connection Manager ; for these connections host, port, username and authentication are not required (and are disabled in the dialogs), and no password prompt is shown.","title": "Local Shell"},{"location": "features/connections.html#choosing-a-shell","text": "Platform Options Windows PowerShell (default) or cmd.exe . Git Bash , Cygwin and WSL are also offered as presets — but only when actually installed (Git Bash/Cygwin are detected via their usual install locations / PATH ; WSL appears only when wsl.exe is present and at least one distribution is installed). macOS / Linux Defaults to your $SHELL (falling back to /bin/zsh or /bin/bash ). A free-form Custom command field accepts any executable with arguments (e.g. pwsh.exe , wsl.exe -d Ubuntu , a Git Bash path), and an optional start directory can be set. The command parser is quote-aware, so shell paths containing spaces — like \"C:\\Program Files\\Git\\bin\\bash.exe\" — launch correctly. When korTTY runs from its Flatpak package, the local shell is started on the host through flatpak-spawn --host , including the selected start directory and terminal locale. The package has host-filesystem access so terminal file actions can use host paths. Because the sandbox-side process ID belongs to flatpak-spawn rather than the host shell, current-directory tracking uses trusted absolute prompt paths instead of reading /proc/<pid>/cwd ; if no safe host path can be established, path-dependent actions stop with an explicit error.","title": "Choosing a shell"},{"location": "features/connections.html#terminal-features-in-local-shells","text": "Terminal logging and recording, plus the AI input/data hooks, work for local shells via a shared ObservableTtyConnector interface. Typed and pasted agent requests use the same byte-level input path, and terminal file actions plus local agent runs follow the interactive shell's current directory. macOS/Linux use the local process directory; native PowerShell and cmd use absolute prompt paths. WSL, Git Bash, Cygwin, and custom commands are best-effort when their shell path namespace differs from the host filesystem, and an unmappable directory produces an explicit error instead of a wrong-file fallback. Features that depend on an SSH channel stay SSH-only. AI Agent in local shells The AI Agent and AI Planning also run in local shells on Windows, macOS and Linux — see AI assistant .","title": "Terminal features in local shells"},{"location": "features/connections.html#tunnels-jump-servers","text": "SSH tunnels — forward ports through the connection: local ( -L ), remote ( -R ) or dynamic / SOCKS ( -D ). Jump server (bastion) — route the connection through an intermediate host; SSH terminal and SFTP sessions both hop through it. See Jump Server .","title": "Tunnels &amp; jump servers"},{"location": "features/connections.html#import-from-other-clients","text": "Connections → Import… reads connection files from MTPuTTY , MobaXterm and PuTTY Connection Manager , with group filtering and credential handling. More to come This page is part of the scaffolded guide. The full feature library — SFTP, snippets, JobScheduler, AI assistant & tools, terminal recording, security, and the complete settings tables — is being filled in next.","title": "Import from other clients"},{"location": "features/file-browser.html","text": "File browser The File Browser is a dockable sidebar that browses your local filesystem next to the terminal. It shows a directory tree with a navigation toolbar, a path bar, a filter, type-aware icons and a status footer. (For browsing a remote server over SFTP, use the SFTP file manager instead.) Opening and docking Toggle the panel from the menu bar: Menu item Shortcut View → File Browser → Show on Left Shift + Cmd + B / Shift + Ctrl + B View → File Browser → Show on Right Shift + Cmd + R / Shift + Ctrl + R Selecting the item again hides the panel. A draggable divider resizes it (160–420 px). The panel's position, width, \"show hidden\" state and last directory are remembered across restarts . Navigation The toolbar and path bar drive where the tree is rooted: Control Action Back / Forward Return to a previously visited directory Up Root the tree at the parent directory (may go above your home directory) Home Root the tree at your home directory Refresh Reload the current directory, keeping expanded folders open New folder / New file Create an item in the selected (or current) directory Sort Choose the sort key (Name, Size, Date modified) and direction (Ascending / Descending) Show hidden files Toggle dotfiles on or off The path bar accepts a typed path and roots the tree there. A leading ~ expands to your home directory; absolute paths outside home are allowed. The filter field below it narrows the listing to entries whose name contains the typed text (case-insensitive). Directories load in the background, so a large or network-mounted folder no longer freezes the window; a brief loading indicator appears while a directory is read. Keyboard shortcuts When the tree has focus: Shortcut Action Enter Open a file, or expand/collapse a folder F2 Rename the selected item inline Backspace Go up to the parent directory Cmd + R / Ctrl + R Refresh Del or Cmd + Backspace Delete the selection (choose Trash or permanent) Cmd + C / Ctrl + C , Cmd + V / Ctrl + V Copy and paste files Cmd + F / Ctrl + F Jump to the filter field File operations Right-click an entry for the full menu: Open — open the file with the operating system's default application. Open in Snippet Editor — load a text file (up to 10 MB) into the Snippet Editor . Rename — rename inline; a name clash is resolved by appending (2) , (3) , … Copy / Cut / Paste — move or copy within the browser. Copy Path — copy the item's absolute path to the clipboard. Delete — a confirmation prompt offers Move to Trash or Delete Permanently . Moving to the Trash is reversible; permanent deletion cannot be undone. On a system without a Trash, only permanent deletion is offered. The deletion runs in the background so a large folder does not freeze the window. New Folder / New File — create an item. Set owner / group / permissions — change ownership and POSIX permissions (where the filesystem supports it). Archive — pack the selection into a ZIP , TAR or TAR.GZ archive. Details — show type, size, path, modification time and permissions. Each row shows a type-aware icon (folder, code, image, archive, document or executable), with a badge for symbolic links. The footer reports how many folders and files the current directory holds and how many entries are selected. Drag and drop Drag files out of the browser onto another application to copy them, and drag files into the browser to copy or move them into a folder. Dropping onto a folder targets that folder; dropping elsewhere targets the current directory. Name clashes are resolved with a (2) suffix rather than overwriting.","title": "File browser"},{"location": "features/file-browser.html#file-browser","text": "The File Browser is a dockable sidebar that browses your local filesystem next to the terminal. It shows a directory tree with a navigation toolbar, a path bar, a filter, type-aware icons and a status footer. (For browsing a remote server over SFTP, use the SFTP file manager instead.)","title": "File browser"},{"location": "features/file-browser.html#opening-and-docking","text": "Toggle the panel from the menu bar: Menu item Shortcut View → File Browser → Show on Left Shift + Cmd + B / Shift + Ctrl + B View → File Browser → Show on Right Shift + Cmd + R / Shift + Ctrl + R Selecting the item again hides the panel. A draggable divider resizes it (160–420 px). The panel's position, width, \"show hidden\" state and last directory are remembered across restarts .","title": "Opening and docking"},{"location": "features/file-browser.html#navigation","text": "The toolbar and path bar drive where the tree is rooted: Control Action Back / Forward Return to a previously visited directory Up Root the tree at the parent directory (may go above your home directory) Home Root the tree at your home directory Refresh Reload the current directory, keeping expanded folders open New folder / New file Create an item in the selected (or current) directory Sort Choose the sort key (Name, Size, Date modified) and direction (Ascending / Descending) Show hidden files Toggle dotfiles on or off The path bar accepts a typed path and roots the tree there. A leading ~ expands to your home directory; absolute paths outside home are allowed. The filter field below it narrows the listing to entries whose name contains the typed text (case-insensitive). Directories load in the background, so a large or network-mounted folder no longer freezes the window; a brief loading indicator appears while a directory is read.","title": "Navigation"},{"location": "features/file-browser.html#keyboard-shortcuts","text": "When the tree has focus: Shortcut Action Enter Open a file, or expand/collapse a folder F2 Rename the selected item inline Backspace Go up to the parent directory Cmd + R / Ctrl + R Refresh Del or Cmd + Backspace Delete the selection (choose Trash or permanent) Cmd + C / Ctrl + C , Cmd + V / Ctrl + V Copy and paste files Cmd + F / Ctrl + F Jump to the filter field","title": "Keyboard shortcuts"},{"location": "features/file-browser.html#file-operations","text": "Right-click an entry for the full menu: Open — open the file with the operating system's default application. Open in Snippet Editor — load a text file (up to 10 MB) into the Snippet Editor . Rename — rename inline; a name clash is resolved by appending (2) , (3) , … Copy / Cut / Paste — move or copy within the browser. Copy Path — copy the item's absolute path to the clipboard. Delete — a confirmation prompt offers Move to Trash or Delete Permanently . Moving to the Trash is reversible; permanent deletion cannot be undone. On a system without a Trash, only permanent deletion is offered. The deletion runs in the background so a large folder does not freeze the window. New Folder / New File — create an item. Set owner / group / permissions — change ownership and POSIX permissions (where the filesystem supports it). Archive — pack the selection into a ZIP , TAR or TAR.GZ archive. Details — show type, size, path, modification time and permissions. Each row shows a type-aware icon (folder, code, image, archive, document or executable), with a badge for symbolic links. The footer reports how many folders and files the current directory holds and how many entries are selected.","title": "File operations"},{"location": "features/file-browser.html#drag-and-drop","text": "Drag files out of the browser onto another application to copy them, and drag files into the browser to copy or move them into a folder. Dropping onto a folder targets that folder; dropping elsewhere targets the current directory. Name clashes are resolved with a (2) suffix rather than overwriting.","title": "Drag and drop"},{"location": "features/i18n.html","text": "Languages & dynamic translation KorTTY supports eight built-in languages and can generate translations for any additional language via translation APIs, allowing you to use the interface in your preferred language. Built-in Languages KorTTY includes full translations for the following languages: English (default) German Italian Spanish Portuguese French Croatian Dutch The application automatically detects your system language on first run and loads the matching translation if available. If your system language is not in the built-in list, English is used as a fallback. Changing Language To switch to a different built-in language: Open Settings → Language Select the desired language from the dropdown Restart the application for the change to take effect To automatically use your system language: Select Auto-detect (System Language) in Settings → Language The application will detect your system locale on startup Dynamic Translation (Additional Languages) You can generate language files for any language supported by a translation API. This allows KorTTY to display its interface in languages beyond the eight built-in options. Translation Providers KorTTY supports the following translation services: Provider API Key Required API used Notes Google Translate Yes Cloud Translation v2 (Basic) Default, widely available; a plain API key is enough DeepL Yes DeepL API v2 High-quality translations; Free and Pro endpoints are picked automatically LibreTranslate Optional LibreTranslate /translate Optional for public instances; self-hosted option available Microsoft Translator Yes Azure AI Translator v3.0 Enterprise-grade translations Yandex Yes Yandex Cloud Translate v2 Needs a service account API key from Yandex Cloud, not the retired v1.5 key Yandex keys stored by an earlier korTTY version Yandex stopped issuing keys for the old Translate API v1.5 and switched off the free keys it had already issued, so no obtainable v1.5 key authenticates any more. korTTY now talks to the Yandex Cloud Translate v2 endpoint instead, and a v1.5 key stored in an earlier korTTY version will not work: create a service account in the Yandex Cloud console, give it the ai.translate.user role, issue an API key for it, and paste that key into Settings → Translation . If you also filled in the API URL field with the old translate.yandex.net/api/v1.5/... address, clear it — korTTY ignores that address and logs a warning until you do. Generating a Translation To generate translations for a new language: Open Settings → Translation Choose a Translation API from the dropdown Enter your API key if required (the key is stored encrypted with your master password) Optionally set a custom API URL (for example, to point to a self-hosted LibreTranslate instance) Click Test API Connection to verify the setup works Select the target language from the language dropdown Click Generate Language File The generated language file is saved to ~/.kortty/i18n/messages_XX.properties , where XX is the two-letter language code. After generation, restart the application to make the new language available in Settings → Language . After App Upgrades When KorTTY is upgraded to a new version, previously generated language files are automatically detected as outdated if they were created with an older app version. In Settings → Translation , a notice appears indicating which languages need updating. To refresh all outdated language files after an upgrade: Open Settings → Translation Click Regenerate outdated All generated languages are re-translated to include new or changed keys from the new version Regenerating is not urgent: until you do it, any string added after your file was generated falls back to the packaged translation for that language, or to English when the language ships no packaged file. You see a translated or English label, never a raw key name. Storage and Persistence Generated language files are stored in ~/.kortty/i18n/ Each file is a standard Java properties file (messages_XX.properties) Files persist across application restarts and updates Language files are included in encrypted backups created via Edit → Create Backup How It Works KorTTY's internationalization system is built around a resource bundle architecture: Base language : English strings are defined in i18n/messages.properties bundled with the application Built-in languages : Pre-translated properties files are packaged with the application Dynamic languages : Generated properties files are loaded from ~/.kortty/i18n/ on startup Placeholder preservation : Translation APIs preserve parameterized placeholders like {0} , {1} , and ${variable} during translation Lookup fallback : A file in ~/.kortty/i18n/ is consulted first, then the packaged file for the same language, then the English base bundle — so a key the generated file lacks still resolves The LanguageManager class detects available locales from both built-in translations and dynamically generated files, allowing you to seamlessly mix both types. Platform Locale Detection KorTTY uses your operating system's locale settings as the default when auto-detect is enabled. The language detection works across platforms: macOS : Reads system language from System Preferences Windows : Uses Windows language settings Linux : Respects the LANG environment variable and system locale settings If your system's primary language is not in the built-in or generated list, English is automatically selected. Tips for Translation Quality Optimizing Translation Results Shorter chunks : Translation APIs work best with moderately sized strings; KorTTY batches translations in groups of 25 strings Placeholder safety : Placeholders ( {0} , ${var} ) are automatically protected during translation and never altered Test after generation : After generating a new language, review the interface for any awkward phrasing and consider adjusting API providers if quality is insufficient LibreTranslate Self-Hosting If you run a self-hosted LibreTranslate instance, you can point KorTTY to it by entering the custom API URL in the translation settings. This avoids API key dependencies and keeps your data local. Troubleshooting API Connection Issues If Test API Connection fails, verify: - Your API key is correct and valid - The API provider's service is not experiencing outages - Your network can reach the API endpoint - For custom URLs, ensure the endpoint is reachable and running Missing Languages After Upgrade If a generated language file disappears after upgrading KorTTY: - Check that the file still exists at ~/.kortty/i18n/messages_XX.properties - Verify that the app version recorded in the file matches the current app version - Regenerate the language using Settings → Translation → Regenerate outdated if needed","title": "Languages & dynamic translation"},{"location": "features/i18n.html#languages-dynamic-translation","text": "KorTTY supports eight built-in languages and can generate translations for any additional language via translation APIs, allowing you to use the interface in your preferred language.","title": "Languages &amp; dynamic translation"},{"location": "features/i18n.html#built-in-languages","text": "KorTTY includes full translations for the following languages: English (default) German Italian Spanish Portuguese French Croatian Dutch The application automatically detects your system language on first run and loads the matching translation if available. If your system language is not in the built-in list, English is used as a fallback.","title": "Built-in Languages"},{"location": "features/i18n.html#changing-language","text": "To switch to a different built-in language: Open Settings → Language Select the desired language from the dropdown Restart the application for the change to take effect To automatically use your system language: Select Auto-detect (System Language) in Settings → Language The application will detect your system locale on startup","title": "Changing Language"},{"location": "features/i18n.html#dynamic-translation-additional-languages","text": "You can generate language files for any language supported by a translation API. This allows KorTTY to display its interface in languages beyond the eight built-in options.","title": "Dynamic Translation (Additional Languages)"},{"location": "features/i18n.html#translation-providers","text": "KorTTY supports the following translation services: Provider API Key Required API used Notes Google Translate Yes Cloud Translation v2 (Basic) Default, widely available; a plain API key is enough DeepL Yes DeepL API v2 High-quality translations; Free and Pro endpoints are picked automatically LibreTranslate Optional LibreTranslate /translate Optional for public instances; self-hosted option available Microsoft Translator Yes Azure AI Translator v3.0 Enterprise-grade translations Yandex Yes Yandex Cloud Translate v2 Needs a service account API key from Yandex Cloud, not the retired v1.5 key Yandex keys stored by an earlier korTTY version Yandex stopped issuing keys for the old Translate API v1.5 and switched off the free keys it had already issued, so no obtainable v1.5 key authenticates any more. korTTY now talks to the Yandex Cloud Translate v2 endpoint instead, and a v1.5 key stored in an earlier korTTY version will not work: create a service account in the Yandex Cloud console, give it the ai.translate.user role, issue an API key for it, and paste that key into Settings → Translation . If you also filled in the API URL field with the old translate.yandex.net/api/v1.5/... address, clear it — korTTY ignores that address and logs a warning until you do.","title": "Translation Providers"},{"location": "features/i18n.html#generating-a-translation","text": "To generate translations for a new language: Open Settings → Translation Choose a Translation API from the dropdown Enter your API key if required (the key is stored encrypted with your master password) Optionally set a custom API URL (for example, to point to a self-hosted LibreTranslate instance) Click Test API Connection to verify the setup works Select the target language from the language dropdown Click Generate Language File The generated language file is saved to ~/.kortty/i18n/messages_XX.properties , where XX is the two-letter language code. After generation, restart the application to make the new language available in Settings → Language .","title": "Generating a Translation"},{"location": "features/i18n.html#after-app-upgrades","text": "When KorTTY is upgraded to a new version, previously generated language files are automatically detected as outdated if they were created with an older app version. In Settings → Translation , a notice appears indicating which languages need updating. To refresh all outdated language files after an upgrade: Open Settings → Translation Click Regenerate outdated All generated languages are re-translated to include new or changed keys from the new version Regenerating is not urgent: until you do it, any string added after your file was generated falls back to the packaged translation for that language, or to English when the language ships no packaged file. You see a translated or English label, never a raw key name.","title": "After App Upgrades"},{"location": "features/i18n.html#storage-and-persistence","text": "Generated language files are stored in ~/.kortty/i18n/ Each file is a standard Java properties file (messages_XX.properties) Files persist across application restarts and updates Language files are included in encrypted backups created via Edit → Create Backup","title": "Storage and Persistence"},{"location": "features/i18n.html#how-it-works","text": "KorTTY's internationalization system is built around a resource bundle architecture: Base language : English strings are defined in i18n/messages.properties bundled with the application Built-in languages : Pre-translated properties files are packaged with the application Dynamic languages : Generated properties files are loaded from ~/.kortty/i18n/ on startup Placeholder preservation : Translation APIs preserve parameterized placeholders like {0} , {1} , and ${variable} during translation Lookup fallback : A file in ~/.kortty/i18n/ is consulted first, then the packaged file for the same language, then the English base bundle — so a key the generated file lacks still resolves The LanguageManager class detects available locales from both built-in translations and dynamically generated files, allowing you to seamlessly mix both types.","title": "How It Works"},{"location": "features/i18n.html#platform-locale-detection","text": "KorTTY uses your operating system's locale settings as the default when auto-detect is enabled. The language detection works across platforms: macOS : Reads system language from System Preferences Windows : Uses Windows language settings Linux : Respects the LANG environment variable and system locale settings If your system's primary language is not in the built-in or generated list, English is automatically selected.","title": "Platform Locale Detection"},{"location": "features/i18n.html#tips-for-translation-quality","text": "Optimizing Translation Results Shorter chunks : Translation APIs work best with moderately sized strings; KorTTY batches translations in groups of 25 strings Placeholder safety : Placeholders ( {0} , ${var} ) are automatically protected during translation and never altered Test after generation : After generating a new language, review the interface for any awkward phrasing and consider adjusting API providers if quality is insufficient LibreTranslate Self-Hosting If you run a self-hosted LibreTranslate instance, you can point KorTTY to it by entering the custom API URL in the translation settings. This avoids API key dependencies and keeps your data local.","title": "Tips for Translation Quality"},{"location": "features/i18n.html#troubleshooting","text": "API Connection Issues If Test API Connection fails, verify: - Your API key is correct and valid - The API provider's service is not experiencing outages - Your network can reach the API endpoint - For custom URLs, ensure the endpoint is reachable and running Missing Languages After Upgrade If a generated language file disappears after upgrading KorTTY: - Check that the file still exists at ~/.kortty/i18n/messages_XX.properties - Verify that the app version recorded in the file matches the current app version - Regenerate the language using Settings → Translation → Regenerate outdated if needed","title": "Troubleshooting"},{"location": "features/jobscheduler.html","text": "JobScheduler The JobScheduler runs unattended background jobs while KorTTY is open. It does not require an operating system service or an active SSH terminal tab. Jobs execute automatically on a configured schedule using saved SSH connections from the Connection Manager. On macOS and Windows, an enabled job with a future run keeps the computer from entering system sleep while korTTY is running when Configuration > Prevent System Sleep is enabled, so the scheduled time remains reachable. The Scheduler uses a single wake-up for the next due run and owns no polling timer when no enabled future job exists. With no terminal connection, future or running Scheduler job, or active AI request, system sleep remains available even while the setting is checked. While a job is running, macOS does not put korTTY into App Nap. Display sleep is not blocked. Linux power inhibition is not yet supported. Open it with Tools > JobScheduler... . The dialog remembers its window position and size. The German interface now translates all labels, action names, status values, validation messages, target selectors and supporting prompts; technical protocol and format names such as SFTP, Rsync, ZIP, TAR, stdout and stderr remain unchanged. Overview JobScheduler supports six types of actions: COMMAND — Run a non-interactive remote shell command SNIPPET_SCRIPT — Execute a SnippetManager script on the target with optional parameters AI_AGENT — Run a headless AI agent with explicit auto-approval AI_SWARM — Broadcast one AI-agent task to all selected targets in parallel and store the combined answer as a saved swarm chat SFTP — Upload, download, sync, delete, rename, create directories, set permissions, change ownership, remote-copy, or create archives RSYNC_SYNC — Synchronize directories via external rsync over SSH Job Configuration The JobScheduler dialog has three tabs: Job , Action , and Journal . Job Tab: Targets and Schedules Use the Job tab to define where and when a job runs. Field Description Enabled Enables or disables the job. Name Display name shown in the job list, journal, and menu-bar status. Connection Opens the Connection Manager selection dialog. Select individual SSH TCP servers, whole groups, or both. Mosh connections are not supported. Working directory Optional remote directory. Use Browse... to connect to the target and pick a directory when the path is not known. Journal LIMITED_REDACTED stores bounded, redacted excerpts. FULL stores full output/transcripts (KorTTY-managed secrets are still redacted). Active from / Active until Optional date range for the job. The job will not run outside this range. Window start / Window end Time window for the job (e.g., 09:00 to 17:00). Values are selected from validated time lists. Interval minutes Optional repeated interval inside the active window. If set, the job runs every N minutes between window start and end. Fixed times Optional explicit start times (e.g., 09:30, 12:00, 15:30). Values are selected from validated time lists. Weekdays Optional weekday filter. Use the all-toggle to select or clear all weekdays at once. Schedule calculations use the local system time zone. If no fixed time and no interval are configured, the next run is the window start on an allowed date. Note Scheduler jobs support saved SSH TCP connections only. Mosh targets are blocked as unsupported and the reason is written to the journal. Host Keys, Sudo, and Secrets Host-key verification is secure by default. Before unattended SSH/SFTP/Rsync execution, select the target and click Confirm host key so KorTTY stores the pinned fingerprint and OpenSSH public-key material. The checkbox Disable host-key verification for this job disables host-key verification only for the selected job. This is unsafe and should only be used when the risk is understood. Sudo passwords can be stored for one server or for a server group: Server-specific sudo passwords are used first. Group sudo passwords are used as fallback. Stored sudo passwords are encrypted with the master password. If the master password is locked and a job needs SSH, sudo, API, or archive secrets, the job is blocked and journaled. For Rsync jobs, Use sudo means passwordless remote sudo only through sudo -n rsync . Stored sudo passwords are not used by the current Rsync integration. Action Tab: Job Types and Configuration Use the Action tab to choose what the job does. The tab shows only the fields that the selected action can use, so unrelated fields are hidden. The action selector includes: Action Purpose COMMAND Run a non-interactive remote command. SNIPPET_SCRIPT Run a SnippetManager script on the selected target. The Snippet search field filters the script dropdown by snippet name, category, language, or ID; Snippet parameters passes additional arguments as one argv value per line. AI_AGENT Run the headless scheduler AI agent. Unattended command execution requires Auto-approve AI commands on the job. AI_SWARM Run the AI Swarm headlessly on every selected target in parallel and combine the answers into one comparison table. SFTP_UPLOAD Upload a local path to a remote path. SFTP_DOWNLOAD Download a remote path to a local path. SFTP_SYNC Synchronize local and remote paths in the selected upload/download direction. SFTP_DELETE Delete a remote path. SFTP_RENAME Rename a remote path. SFTP_MKDIR Create a remote directory. SFTP_CHMOD Change permissions. Numeric modes such as 755 and symbolic modes such as u+rw,o-w are accepted. SFTP_CHOWN Change owner and/or group. Owner and group buttons can query the target and show available values. SFTP_COPY_REMOTE Copy a remote path to another remote path on the same target. SFTP_ARCHIVE Create a remote archive. RSYNC_SYNC Synchronize one or more directories via external rsync over SSH. Path fields provide local Finder/Explorer selection where the path is local and remote directory browsing where the path is remote. Remote browsing requires a selected target and host-key verification unless the job explicitly disables host-key verification. Snippet Script Jobs Snippet script jobs use the selected SnippetManager entry without requiring an open terminal tab. KorTTY resolves built-in snippet variables and stored SnippetManager variables before execution. Missing snippets, missing stored variable values, and unsupported snippet languages block the job and write the reason to the journal. Additional snippet parameters are entered one per line so values with spaces are passed as single script arguments. AI Swarm Jobs AI Swarm jobs run one AI-agent prompt on all selected targets in parallel over background SSH sessions — no terminal tabs are opened. Beyond the shared AI profile , AI prompt , and Auto-approve AI commands fields, two swarm-specific fields apply: Field Description Swarm parallelism How many targets run concurrently (1–16, default 4). Swarm read-only Restricts every agent to non-mutating commands. On by default. Results are stored twice: the journal records the run outcome, and the full conversation — including the combined per-server comparison table — is saved as a swarm chat that can be reopened from the AI Manager's Swarm Chats section. The fastest way to create an AI Swarm job is the Schedule… button in the AI Swarm tab : it prefills a new job with the tab's current targets, prompt, AI profile, and read-only setting. See that page for recommended swarm/scheduler usage scenarios. Warning A scheduled swarm with Swarm read-only off and Auto-approve AI commands on changes systems unattended. Test the prompt interactively in the AI Swarm tab before enabling such a job. SFTP Archive Jobs SFTP archive jobs support ZIP, password-protected ZIP, TAR, and TAR.BZ2. Archive sources and exclude patterns accept one path or pattern per line. The archive can optionally be downloaded after creation. When Use sudo staging for SFTP paths is enabled, KorTTY stages files in a temporary location and uses sudo-assisted remote commands such as mv , cp , tar , chmod , and chown where elevated rights are required. Cleanup failures are written to the journal. Rsync Jobs RSYNC_SYNC supports upload and download between the local filesystem and saved SSH TCP connections. Upload : local source directories are synchronized under the remote target root. Download : remote source directories are synchronized under the local target root. Multiple sources are supported. Delete missing files adds --delete ; it is off by default. For downloads from multiple group targets, KorTTY writes each target into its own subdirectory below the local target root to avoid overwriting files from another server. KorTTY builds Rsync execution as a ProcessBuilder argument list instead of shell-concatenating the command. The command uses -a --itemize-changes ; --delete is added only when the job checkbox is enabled. Rsync Prerequisites rsync is taken from PATH , unless an explicit binary path is configured in Settings > SFTP > JobScheduler Rsync . ssh must be available in PATH . Host-key pinning is required unless the job explicitly disables host-key verification. Password and private-key passphrase authentication use a temporary owner-only SSH_ASKPASS helper. Secrets, helper paths, and temporary secret-file paths are redacted before journaling. Journal Tab The Journal tab lists job runs with local KorTTY timestamps, status, job name, and summary. The columns Started , Status , Job , and Summary are sortable; the default order shows the newest started entries first. The search row can match all persisted journal fields or only selected columns such as status, job, summary, stdout, stderr, and detail. Enter multiple whitespace-separated terms when every term must occur somewhere in the selected search scope; use * inside a term as a wildcard, for example backup*fail . Selecting a row shows stdout, stderr, and detail text. Use Delete selected to remove selected journal entries. By default, KorTTY automatically deletes scheduler journal entries older than 14 days; set the retention value to 0 to keep entries indefinitely. The protocol/detail area below the table is separated by a vertical splitter. Resize it to give the output more or less height; KorTTY stores that divider position in the global settings. In the detail text area, mark text and right-click to copy the selected text to the clipboard. Journal statuses include successful, failed, blocked, cancelled, and running/system entries. Reasons such as locked master password, missing host-key pin, missing rsync / ssh , unsupported Mosh target, or shutdown drain are written as journal details. Menu-Bar Status and Cancellation When Show Jobs status in menu bar is enabled, KorTTY shows the scheduler status after Help only if an enabled scheduler entry exists or a job is currently running. The status shows the running job, cancellation state, or the next job with a live countdown. Click the status menu to see: Open JobScheduler... running jobs and cancel entries up to five next queued jobs with start time and live countdown Right-click the status label for a compact menu with cancel actions for running jobs and a shortcut to open JobScheduler. Cancellation requests are journaled and running SSH/Rsync work is interrupted cleanly where possible. Quitting KorTTY While Jobs Run If KorTTY is about to exit while JobScheduler jobs are running, it shows a warning with the active job names. Choosing Cancel keeps KorTTY running. Choosing Wait and quit starts shutdown drain mode: new scheduled or manual job starts are blocked; the shutdown wait is written to the journal; KorTTY waits for running jobs to complete or cancel; KorTTY exits automatically after the drain finishes. Security and Secrets Host keys are pinned by default to prevent man-in-the-middle attacks on unattended execution. Sudo passwords are stored encrypted with the master password. SSH key passphrases and archive passwords are stored encrypted. KorTTY redacts managed secrets (passwords, passphrases, archive credentials) from journal output before persistence. If the master password is locked when a job needs SSH, sudo, API, or archive secrets, the job is blocked. Troubleshooting Warning JobScheduler job is blocked: Open Tools > JobScheduler... > Journal and inspect the selected entry's detail text. Common causes are: - Locked master password - Missing host-key pin - Unsupported Mosh target - Missing rsync or ssh in PATH - Old host-key pin without OpenSSH public-key material for Rsync JobScheduler Rsync cannot start: Verify local rsync --version and ssh -V , or configure the Rsync binary path in Settings > SFTP > JobScheduler Rsync . JobScheduler remote browser cannot open: Select exactly one target and confirm the host key first, unless the job explicitly disables host-key verification.","title": "JobScheduler"},{"location": "features/jobscheduler.html#jobscheduler","text": "The JobScheduler runs unattended background jobs while KorTTY is open. It does not require an operating system service or an active SSH terminal tab. Jobs execute automatically on a configured schedule using saved SSH connections from the Connection Manager. On macOS and Windows, an enabled job with a future run keeps the computer from entering system sleep while korTTY is running when Configuration > Prevent System Sleep is enabled, so the scheduled time remains reachable. The Scheduler uses a single wake-up for the next due run and owns no polling timer when no enabled future job exists. With no terminal connection, future or running Scheduler job, or active AI request, system sleep remains available even while the setting is checked. While a job is running, macOS does not put korTTY into App Nap. Display sleep is not blocked. Linux power inhibition is not yet supported. Open it with Tools > JobScheduler... . The dialog remembers its window position and size. The German interface now translates all labels, action names, status values, validation messages, target selectors and supporting prompts; technical protocol and format names such as SFTP, Rsync, ZIP, TAR, stdout and stderr remain unchanged.","title": "JobScheduler"},{"location": "features/jobscheduler.html#overview","text": "JobScheduler supports six types of actions: COMMAND — Run a non-interactive remote shell command SNIPPET_SCRIPT — Execute a SnippetManager script on the target with optional parameters AI_AGENT — Run a headless AI agent with explicit auto-approval AI_SWARM — Broadcast one AI-agent task to all selected targets in parallel and store the combined answer as a saved swarm chat SFTP — Upload, download, sync, delete, rename, create directories, set permissions, change ownership, remote-copy, or create archives RSYNC_SYNC — Synchronize directories via external rsync over SSH","title": "Overview"},{"location": "features/jobscheduler.html#job-configuration","text": "The JobScheduler dialog has three tabs: Job , Action , and Journal .","title": "Job Configuration"},{"location": "features/jobscheduler.html#job-tab-targets-and-schedules","text": "Use the Job tab to define where and when a job runs. Field Description Enabled Enables or disables the job. Name Display name shown in the job list, journal, and menu-bar status. Connection Opens the Connection Manager selection dialog. Select individual SSH TCP servers, whole groups, or both. Mosh connections are not supported. Working directory Optional remote directory. Use Browse... to connect to the target and pick a directory when the path is not known. Journal LIMITED_REDACTED stores bounded, redacted excerpts. FULL stores full output/transcripts (KorTTY-managed secrets are still redacted). Active from / Active until Optional date range for the job. The job will not run outside this range. Window start / Window end Time window for the job (e.g., 09:00 to 17:00). Values are selected from validated time lists. Interval minutes Optional repeated interval inside the active window. If set, the job runs every N minutes between window start and end. Fixed times Optional explicit start times (e.g., 09:30, 12:00, 15:30). Values are selected from validated time lists. Weekdays Optional weekday filter. Use the all-toggle to select or clear all weekdays at once. Schedule calculations use the local system time zone. If no fixed time and no interval are configured, the next run is the window start on an allowed date. Note Scheduler jobs support saved SSH TCP connections only. Mosh targets are blocked as unsupported and the reason is written to the journal.","title": "Job Tab: Targets and Schedules"},{"location": "features/jobscheduler.html#host-keys-sudo-and-secrets","text": "Host-key verification is secure by default. Before unattended SSH/SFTP/Rsync execution, select the target and click Confirm host key so KorTTY stores the pinned fingerprint and OpenSSH public-key material. The checkbox Disable host-key verification for this job disables host-key verification only for the selected job. This is unsafe and should only be used when the risk is understood. Sudo passwords can be stored for one server or for a server group: Server-specific sudo passwords are used first. Group sudo passwords are used as fallback. Stored sudo passwords are encrypted with the master password. If the master password is locked and a job needs SSH, sudo, API, or archive secrets, the job is blocked and journaled. For Rsync jobs, Use sudo means passwordless remote sudo only through sudo -n rsync . Stored sudo passwords are not used by the current Rsync integration.","title": "Host Keys, Sudo, and Secrets"},{"location": "features/jobscheduler.html#action-tab-job-types-and-configuration","text": "Use the Action tab to choose what the job does. The tab shows only the fields that the selected action can use, so unrelated fields are hidden. The action selector includes: Action Purpose COMMAND Run a non-interactive remote command. SNIPPET_SCRIPT Run a SnippetManager script on the selected target. The Snippet search field filters the script dropdown by snippet name, category, language, or ID; Snippet parameters passes additional arguments as one argv value per line. AI_AGENT Run the headless scheduler AI agent. Unattended command execution requires Auto-approve AI commands on the job. AI_SWARM Run the AI Swarm headlessly on every selected target in parallel and combine the answers into one comparison table. SFTP_UPLOAD Upload a local path to a remote path. SFTP_DOWNLOAD Download a remote path to a local path. SFTP_SYNC Synchronize local and remote paths in the selected upload/download direction. SFTP_DELETE Delete a remote path. SFTP_RENAME Rename a remote path. SFTP_MKDIR Create a remote directory. SFTP_CHMOD Change permissions. Numeric modes such as 755 and symbolic modes such as u+rw,o-w are accepted. SFTP_CHOWN Change owner and/or group. Owner and group buttons can query the target and show available values. SFTP_COPY_REMOTE Copy a remote path to another remote path on the same target. SFTP_ARCHIVE Create a remote archive. RSYNC_SYNC Synchronize one or more directories via external rsync over SSH. Path fields provide local Finder/Explorer selection where the path is local and remote directory browsing where the path is remote. Remote browsing requires a selected target and host-key verification unless the job explicitly disables host-key verification.","title": "Action Tab: Job Types and Configuration"},{"location": "features/jobscheduler.html#rsync-jobs","text": "RSYNC_SYNC supports upload and download between the local filesystem and saved SSH TCP connections. Upload : local source directories are synchronized under the remote target root. Download : remote source directories are synchronized under the local target root. Multiple sources are supported. Delete missing files adds --delete ; it is off by default. For downloads from multiple group targets, KorTTY writes each target into its own subdirectory below the local target root to avoid overwriting files from another server. KorTTY builds Rsync execution as a ProcessBuilder argument list instead of shell-concatenating the command. The command uses -a --itemize-changes ; --delete is added only when the job checkbox is enabled.","title": "Rsync Jobs"},{"location": "features/jobscheduler.html#rsync-prerequisites","text": "rsync is taken from PATH , unless an explicit binary path is configured in Settings > SFTP > JobScheduler Rsync . ssh must be available in PATH . Host-key pinning is required unless the job explicitly disables host-key verification. Password and private-key passphrase authentication use a temporary owner-only SSH_ASKPASS helper. Secrets, helper paths, and temporary secret-file paths are redacted before journaling.","title": "Rsync Prerequisites"},{"location": "features/jobscheduler.html#journal-tab","text": "The Journal tab lists job runs with local KorTTY timestamps, status, job name, and summary. The columns Started , Status , Job , and Summary are sortable; the default order shows the newest started entries first. The search row can match all persisted journal fields or only selected columns such as status, job, summary, stdout, stderr, and detail. Enter multiple whitespace-separated terms when every term must occur somewhere in the selected search scope; use * inside a term as a wildcard, for example backup*fail . Selecting a row shows stdout, stderr, and detail text. Use Delete selected to remove selected journal entries. By default, KorTTY automatically deletes scheduler journal entries older than 14 days; set the retention value to 0 to keep entries indefinitely. The protocol/detail area below the table is separated by a vertical splitter. Resize it to give the output more or less height; KorTTY stores that divider position in the global settings. In the detail text area, mark text and right-click to copy the selected text to the clipboard. Journal statuses include successful, failed, blocked, cancelled, and running/system entries. Reasons such as locked master password, missing host-key pin, missing rsync / ssh , unsupported Mosh target, or shutdown drain are written as journal details.","title": "Journal Tab"},{"location": "features/jobscheduler.html#menu-bar-status-and-cancellation","text": "When Show Jobs status in menu bar is enabled, KorTTY shows the scheduler status after Help only if an enabled scheduler entry exists or a job is currently running. The status shows the running job, cancellation state, or the next job with a live countdown. Click the status menu to see: Open JobScheduler... running jobs and cancel entries up to five next queued jobs with start time and live countdown Right-click the status label for a compact menu with cancel actions for running jobs and a shortcut to open JobScheduler. Cancellation requests are journaled and running SSH/Rsync work is interrupted cleanly where possible.","title": "Menu-Bar Status and Cancellation"},{"location": "features/jobscheduler.html#quitting-kortty-while-jobs-run","text": "If KorTTY is about to exit while JobScheduler jobs are running, it shows a warning with the active job names. Choosing Cancel keeps KorTTY running. Choosing Wait and quit starts shutdown drain mode: new scheduled or manual job starts are blocked; the shutdown wait is written to the journal; KorTTY waits for running jobs to complete or cancel; KorTTY exits automatically after the drain finishes.","title": "Quitting KorTTY While Jobs Run"},{"location": "features/jobscheduler.html#security-and-secrets","text": "Host keys are pinned by default to prevent man-in-the-middle attacks on unattended execution. Sudo passwords are stored encrypted with the master password. SSH key passphrases and archive passwords are stored encrypted. KorTTY redacts managed secrets (passwords, passphrases, archive credentials) from journal output before persistence. If the master password is locked when a job needs SSH, sudo, API, or archive secrets, the job is blocked.","title": "Security and Secrets"},{"location": "features/jobscheduler.html#troubleshooting","text": "Warning JobScheduler job is blocked: Open Tools > JobScheduler... > Journal and inspect the selected entry's detail text. Common causes are: - Locked master password - Missing host-key pin - Unsupported Mosh target - Missing rsync or ssh in PATH - Old host-key pin without OpenSSH public-key material for Rsync JobScheduler Rsync cannot start: Verify local rsync --version and ssh -V , or configure the Rsync binary path in Settings > SFTP > JobScheduler Rsync . JobScheduler remote browser cannot open: Select exactly one target and confirm the host key first, unless the job explicitly disables host-key verification.","title": "Troubleshooting"},{"location": "features/jump-server.html","text": "Jump Server (Bastion Host) A jump server (bastion host) acts as an intermediate gateway to reach servers on a private network. korTTY tunnels the connection through a jump server so you can reach an internal system from your local machine without direct network access to it. Connection flow korTTY authenticates to the jump server with the jump server's own credentials, opens a tunnel through it, and then opens the real SSH session to the target host through that tunnel. The target's credentials are used only for the target, and the jump server's credentials only for the jump server — neither host is offered the other's password or key. Both SSH terminal and SFTP connections to the target route through the jump server; there is nothing extra to configure for SFTP. Warning Mosh connections cannot go through a jump server: a Mosh session runs over UDP, which the jump server's SSH tunnel (TCP) does not forward. korTTY refuses the combination up front — the Jump Server tab warns as soon as it is configured, and connecting fails immediately with a clear message instead of stalling after the SSH bootstrap. Use the SSH protocol for targets behind a bastion, or disable the jump server. Configuration To configure a jump server for a connection: Open the Connection Manager and edit (or create) a connection. Go to the Jump Server tab. Enable Jump Server . Enter the jump server's details: Host — hostname or IP address of the jump server Port — SSH port (default: 22) Username — login username for the jump server Choose an Authentication method: Password — the password is stored encrypted with your master password. Leave the field empty when editing to keep the previously stored password. SSH key file (no passphrase) — the path to an unencrypted private key file. Passphrase-protected keys are not supported for the jump hop. Click Save . Host key verification The jump server's host key is verified on first use exactly like any other host: korTTY shows the key's SHA-256 fingerprint and asks you to confirm it, then pins it. On later connections a changed jump-server key is refused, the same trust-on-first-use protection the target host gets. The bastion is always checked strictly: relaxing host-key verification applies to target hosts only, never to the jump hop. This holds even when host-key verification has been relaxed for the target: the per-connection, per-group and global opt-outs never cover the bastion, so its key is always verified strictly. See Relaxing host-key verification . The target host is verified under its own name even though the transport goes through the tunnel, so a compromised bastion cannot substitute a different target host key unnoticed. When to use it Target servers are behind a firewall and only reachable through a bastion host. Security policy requires routing through a gateway. Internal infrastructure uses private addresses and needs an intermediary for access. Note Jump server passwords are stored encrypted with your master password, alongside the connection's own credentials. Because the stored password is never shown back, editing the connection with an empty jump-server password field keeps the stored one; type a new value only to change it. If the master password vault is locked when you save a new jump password, korTTY reports that the jump password could not be saved and leaves the other settings untouched.","title": "Jump Server (Bastion Host)"},{"location": "features/jump-server.html#jump-server-bastion-host","text": "A jump server (bastion host) acts as an intermediate gateway to reach servers on a private network. korTTY tunnels the connection through a jump server so you can reach an internal system from your local machine without direct network access to it.","title": "Jump Server (Bastion Host)"},{"location": "features/jump-server.html#connection-flow","text": "korTTY authenticates to the jump server with the jump server's own credentials, opens a tunnel through it, and then opens the real SSH session to the target host through that tunnel. The target's credentials are used only for the target, and the jump server's credentials only for the jump server — neither host is offered the other's password or key. Both SSH terminal and SFTP connections to the target route through the jump server; there is nothing extra to configure for SFTP. Warning Mosh connections cannot go through a jump server: a Mosh session runs over UDP, which the jump server's SSH tunnel (TCP) does not forward. korTTY refuses the combination up front — the Jump Server tab warns as soon as it is configured, and connecting fails immediately with a clear message instead of stalling after the SSH bootstrap. Use the SSH protocol for targets behind a bastion, or disable the jump server.","title": "Connection flow"},{"location": "features/jump-server.html#configuration","text": "To configure a jump server for a connection: Open the Connection Manager and edit (or create) a connection. Go to the Jump Server tab. Enable Jump Server . Enter the jump server's details: Host — hostname or IP address of the jump server Port — SSH port (default: 22) Username — login username for the jump server Choose an Authentication method: Password — the password is stored encrypted with your master password. Leave the field empty when editing to keep the previously stored password. SSH key file (no passphrase) — the path to an unencrypted private key file. Passphrase-protected keys are not supported for the jump hop. Click Save .","title": "Configuration"},{"location": "features/jump-server.html#host-key-verification","text": "The jump server's host key is verified on first use exactly like any other host: korTTY shows the key's SHA-256 fingerprint and asks you to confirm it, then pins it. On later connections a changed jump-server key is refused, the same trust-on-first-use protection the target host gets. The bastion is always checked strictly: relaxing host-key verification applies to target hosts only, never to the jump hop. This holds even when host-key verification has been relaxed for the target: the per-connection, per-group and global opt-outs never cover the bastion, so its key is always verified strictly. See Relaxing host-key verification . The target host is verified under its own name even though the transport goes through the tunnel, so a compromised bastion cannot substitute a different target host key unnoticed.","title": "Host key verification"},{"location": "features/jump-server.html#when-to-use-it","text": "Target servers are behind a firewall and only reachable through a bastion host. Security policy requires routing through a gateway. Internal infrastructure uses private addresses and needs an intermediary for access. Note Jump server passwords are stored encrypted with your master password, alongside the connection's own credentials. Because the stored password is never shown back, editing the connection with an empty jump-server password field keeps the stored one; type a new value only to change it. If the master password vault is locked when you save a new jump password, korTTY reports that the jump password could not be saved and leaves the other settings untouched.","title": "When to use it"},{"location": "features/local-models.html","text": "Local models (llama.cpp and MLX) korTTY can run local language models directly through integrated, model-specific sidecar servers: GGUF models through the pinned llama.cpp llama-server on every platform, and — on Apple Silicon Macs — MLX models through the official mlx-lm server. You do not need LM Studio, Ollama, or a cloud inference account for local chat, text, coding, translation, and embedding workloads. Open AI > AI Manager and use these tabs: Local Models installs, imports, configures, starts, stops, and removes local model registrations (GGUF everywhere, additionally MLX on Apple Silicon). Local AI assigns profiles to the Text/translation and Coding roles and to the session journal's AI summaries, selects the RAG embedding model, stores an optional Hugging Face token, and records the llama.cpp runtime-update policy. Profiles creates an Integrated llama.cpp or Integrated MLX (Apple Silicon) profile for an installed model and controls its prompt optimization preset. Every action button in the tab carries a matching glyph (install, wizard, import, configure, set as default, start, stop, remove, refresh, search, load more, download, pause, cancel), and the pause control switches between pause and resume glyphs during a download. Runtimes The Runtimes table at the top of Local Models lists both embedded runtimes with their installed version, backend, and state ( Ready , Not installed , or Revoked ): the llama.cpp runtime (every platform) and, on Apple Silicon, the MLX runtime. Three actions operate on the selected row: Install runtime / Check-install runtime update downloads, verifies, and activates the newest compatible package from the signed stable channel — the llama.cpp index for the llama row, the mlx-stable index for the MLX row. Import local package… installs a runtime package file ( .zip ) without downloading it, for example on an air-gapped machine. The file is accepted only when its SHA-256 matches a non-revoked entry of the Ed25519-signed stable index for the current platform; anything not published by the signed channel is refused. The verified package then runs through exactly the same installation machinery as a download (extraction hardening, health/sanity check, idle-only activation). Remove deletes the selected runtime after confirmation. Sidecars must be idle; installed models stay registered but cannot start until a runtime is installed again, and the revocation denylist survives removal so a withdrawn package remains blocked even across reinstalls. Storage and network access Model inference stays on this computer. korTTY contacts Hugging Face only when you search for or download a model that you approve. Public repositories work without a token; an optional token for private or gated repositories is encrypted with the master password. Official builds can also fetch the separately signed model/prompt catalog over HTTPS in the background; no prompt, source document, or model weight is sent with that request. Quick setup assistant Select AI > AI Manager > Local Models > Setup assistant . The six-step assistant covers privacy, detected system memory and backend guidance, optional role-specific recommendations, license and exact download-size review, verified installation, and a final readiness summary. On Choose models for optional roles , enable any combination of Text and translation , Coding , and RAG embeddings . Every enabled slot has its own recommendation selector that lists every catalog model the detected memory supports for that role, ordered by preference with the preferred default preselected — a real choice, not a single fixed suggestion. Disabled slots are left unchanged. Text and Coding may deliberately share one compatible recommendation, in which case korTTY downloads and registers that GGUF only once. Opening the assistant from a missing-embedding warning preselects only the RAG-embedding slot. The built-in bootstrap catalog uses conservative RAM tiers. The table shows the preselected default per role; the selector of an enabled slot additionally offers every other catalog model whose memory requirement the detected tier meets: Detected memory Text recommendation Coding recommendation RAG embeddings Less than 16 GiB Qwen3 1.7B, Q4_K_M Qwen3 1.7B, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 16–23 GiB Qwen3 4B, Q4_K_M Qwen2.5-Coder 7B Instruct, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 24 GiB or more Qwen3 8B, Q4_K_M Qwen2.5-Coder 7B Instruct, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 For the RAG-embeddings slot, Qwen3-Embedding 0.6B Q8_0 remains the preselected default on every tier; the selector also offers Qwen3-Embedding 4B Q4_K_M (from 16 GiB), Qwen3-Embedding 8B Q4_K_M (from 24 GiB), and — without a minimum memory requirement — the multilingual BGE-M3 Q8_0 and the very small, fast Nomic Embed Text v1.5 Q8_0 . Recommendations are starting points, not hardware guarantees. The model table labels the estimated fit as Comfortable , Possible , Too large , or Unknown ; context size and simultaneous models also affect memory use. Before Install becomes available, korTTY loads each selected repository at the catalog's fixed revision in the background, verifies that the requested quantization exists, and displays the pinned repository, license, and exact combined GGUF download size. You must review and accept those values. A mutable repository revision or missing quantization stops the assistant before any download starts. Runtime installation, GGUF downloads, verification, registration, and function tests run asynchronously so the JavaFX dialog remains responsive. The progress view names the current model and phase; Cancel installation stops at a safe boundary and retains resumable .part files, while Retry restarts a failed or cancelled run from the verified selections. The Hub-reported context length remains visible for comparison, but a new local model starts with the conservative 4,096-token runtime context. Increase it later under Configure only after considering the additional RAM/VRAM use. After every selected GGUF is downloaded and registered, the assistant sends a real local chat-completion request or, for an embedding-only model, a real embedding request through the installed runtime. Only after all tests pass does it atomically save the selected Text/Coding profile IDs and RAG embedding-model ID in global-settings.xml ; it creates a reusable Local: … embedded profile when a selected chat model has none and makes the first such profile the default only when no default exists. A failed test does not produce a successful finish page or persist partially updated role assignments. Signed model and prompt catalog korTTY always contains a small bootstrap catalog with the memory-tier recommendations and prompt-family detection needed for offline setup. All nine bundled model recommendations carry a concrete 40-character Hugging Face commit, so even the bootstrap never resolves a mutable repository head when the assistant later fetches metadata or GGUF files. Official builds can update those data independently from the application through model-prompt-catalog-v1.json and its detached Ed25519 signature model-prompt-catalog-v1.sig . On the first use of recommendations or automatic prompt detection, korTTY immediately loads the last signature-verified cache and performs at most one background refresh from the fixed HTTPS stable channel. The catalog can update recommended model IDs, fixed revisions, quantizations, roles, RAM thresholds and ordering, plus the mapping from model-name tokens to built-in prompt presets. It cannot inject arbitrary executable code or replace korTTY's built-in action, safety, JSON, or code-output contracts. The exact catalog bytes are verified before strict schema-v1 parsing, and unknown schema fields are rejected. A failed download, invalid signature, or invalid catalog leaves the last valid cache untouched. If the application build has no valid catalog public trust root, korTTY performs no catalog network request, does not trust an existing cache, and uses only the built-in bootstrap. Each catalog also carries a positive monotonic sequence. During refresh, korTTY rejects a signed catalog whose sequence is older than the last accepted cache or bootstrap, and rejects an equal-sequence catalog with a different version. A newly accepted catalog with the highest sequence must be written to the atomic cache before it becomes active; the protected promotion workflow separately requires every official sequence to be strictly greater than the sequence in the latest published release, preventing a correctly signed older catalog from replaying obsolete recommendations. Find and download models The Hugging Face browser searches model repositories and shows repository, format, architecture, available quantizations, license, selected-quantization size, context length, publication age, and a hardware estimate. The Age column reflects when the repository was first published on the Hub — shown as today , days, months, or years, and — when the Hub does not report a creation date. On Apple Silicon a Format selector next to the search field switches between GGUF + MLX (default), GGUF , and MLX ; each enabled format keeps its own cursor pagination, and the Format column identifies every row. Other platforms search GGUF only. Click a column header to sort the results by that column — a second click reverses the direction, and the header shows the active sort arrow. Size, context and age sort numerically (repositories without a reported creation date sort last), and the hardware estimate sorts from best to worst fit; the chosen order persists while background metadata loads complete. Only repositories this computer can actually run appear: korTTY completes the lightweight search listing with exact per-file metadata in the background and hides rows whose size is unknown or whose smallest quantization exceeds the detected memory. The status line reports how many usable repositories are shown and how many were hidden. Selecting a row loads exact metadata for that repository's immutable revision and shows Loading… while Size and Hardware estimate refresh; the chosen quantization remains selected. Projector, quantization-matrix, and speculative-decoding helper GGUFs are excluded from the downloadable language-model choices. Load more continues the same search without discarding earlier results. Select a repository and quantization, review its license and size, then choose Download and install . korTTY installs only an immutable 40-character repository revision with exact file metadata. After you confirm the license and download size, a fixed Model download panel at the bottom identifies the repository and quantization, the current file and multipart shard, transferred and total bytes, elapsed time, transfer rate, and estimated remaining time. Its full-width progress bar and Pause / Resume and Cancel controls remain available while you continue to review the manager. Downloads use .part files, free-space checks, HTTP Range and If-Range , content-digest verification (SHA-256 for LFS files, the git blob hash for small in-tree files), and multipart shard ordering. Cancellation, including closing the AI Manager during a transfer, keeps the partial data needed for a later resume. An unpinned repository or a missing file digest is rejected instead of silently installing mutable content. An MLX repository is downloaded as one complete directory (safetensors weights, tokenizer, and configuration files); its quantization is a repository-level property such as 4BIT or 8BIT , so the quantization selector shows exactly one entry. If no MLX runtime is installed yet, korTTY asks whether to download and register the model anyway — it can be started as soon as the runtime is installed. MLX models on Apple Silicon On Apple Silicon Macs (macOS 14 or newer), korTTY additionally runs MLX models — Apple's array framework for machine learning on Apple GPUs — through the official mlx-lm server. The mlx-community organization on Hugging Face publishes thousands of ready-converted models. Registration and lifecycle mirror GGUF models: installed MLX models appear in the same table with backend MLX , support Start selected , Stop selected , Remove , and Configure (display name and unload-after idle time), and report the same runtime states. Isolation mirrors llama.cpp: every model runs as its own loopback-only sidecar with a random port and a generated per-process API key. korTTY wraps mlx_lm.server — which has no authentication of its own — in its own launcher that rejects every unauthenticated request except the local health probe, forces offline Hugging Face access, strips inherited Python and token environment overrides, and exits after the configured idle time. The MLX runtime package is separate from both the application installer and the llama.cpp runtime: a pinned relocatable CPython plus a hash-locked mlx-lm wheel set, built in CI and published through the same Ed25519-signed index mechanism as the llama.cpp packages and resolved through the same latest release. Install, update, locally import, or remove it in the Runtimes table; without an installed MLX runtime, MLX models stay registered but cannot start. Profiles : choose Integrated MLX (Apple Silicon) as the profile connection and select the installed model from the Local MLX model list. LM-Studio-MCP internet modes are not available for embedded profiles; the korTTY web-search tool works normally. Reasoning models (for example Qwen3 conversions) are handled like their GGUF counterparts: korTTY separates the inline chain-of-thought from the answer and retries a reply that contains only reasoning once automatically (except for the dedicated snippet/workflow diagram request, which fails immediately instead). Import existing GGUF files Select Import GGUF , choose one or more .gguf files, and then choose one mode: Mode Behavior Managed copy Copies the GGUF into korTTY's model directory using a temporary file and atomic activation where the filesystem supports it. External reference Registers the original path. Moving or deleting that file makes the model unavailable; removing the registration never deletes the external file. A compatible, verified llama-server runtime must also be installed. Runtime packages live separately under ~/.kortty/llm/runtime/ and are not part of the base application installer. When you download or import the first model, korTTY offers to install the matching signed stable runtime before continuing; you can also choose Install runtime in Local Models . The model manager does not accept an arbitrary executable as a replacement for the verified package. Configure and run models The installed-models table also carries an Age column: the model's upstream publication date, captured once at install time. Models installed before this column existed and manually imported GGUF files have no captured date and show — . Select an installed model and choose Configure . The available settings are deliberately typed and bounded; arbitrary server arguments are not accepted. Saving first asks the runtime manager to stop the model. If that model is processing a request, korTTY refuses the change and leaves both the running sidecar and persisted configuration untouched; retry after the request finishes. Setting Values Default Backend Auto, CPU, Metal, Vulkan Auto Context size 512–2,097,152 tokens 4,096 CPU threads 0–1,024; 0 means automatic Automatic GPU layers -1–10,000; -1 means automatic Automatic Unload after 1–1,440 minutes, or Never 10 minutes The per-model backend describes how that model should run. In AI Manager > Local AI , Preferred runtime backend controls which signed native package is installed and updated: macOS offers Auto, CPU, and Metal; Windows/Linux offer Auto, CPU, and Vulkan. Automatic (keep active backend) preserves the already active backend during updates. For a first installation, Auto prefers Metal and falls back to CPU on macOS; on Windows and Linux it prefers CPU and falls back to Vulkan only when no compatible CPU package is published. An explicitly selected backend requires an exact compatible package and never changes to another backend silently. Use multi-selection and Start selected to load several different models concurrently. Profiles that point to the same installed model and runtime configuration share one authenticated sidecar. The table reports STOPPED , STARTING , LOADING , READY , BUSY , SLEEPING , or FAILED . You can mark one installed model as the default with Set as default in the action bar or the table's right-click menu; Clear default in the same menu removes the mark. The default model is shown with a leading ★ in the Name column and is pre-selected when the manager opens, so the Start/Configure/Stop actions target it by default. This is a convenience marker for the manager only — it does not change which AI profile the agent, chat, or other AI features use; those continue to follow the AI profile configuration. If a selected Metal or Vulkan model requires a different package from the active runtime, korTTY offers to download, verify, and activate the matching signed package without interrupting current requests. Models that require incompatible GPU runtimes must be started separately with the matching preferred backend. After the configured idle time, llama.cpp releases the model tensors from RAM/VRAM and korTTY marks the model as sleeping while retaining the lightweight process. The next request acquires a runtime lease and wakes the model automatically. Stop selected terminates only idle sidecars; an active generation is never interrupted by model removal or a normal stop request. To make this activity visible, korTTY records the request and model lifecycle in kortty.log (under the log directory configured in Configuration > Global Settings > Logging , default ~/.kortty/logs ). Every AI request logs one AI request sent line and a matching AI request done / AI request failed line with the action, provider, model, the reasoning level the request actually uses ( reasoningEffort= ), approximate input size, duration and token count — metadata only, never the prompt or response text. Local models additionally log when a model is Loaded , Unloaded … (sleeping) after the idle timeout, or Unloaded … sidecar stopped , for both the llama.cpp and MLX runtimes. All of these are INFO lines, so they appear with the default log level. Errors reported by the model itself are logged too, so a failing request can be diagnosed from the log alone: AI request failed carries the server's own error text (its error.message , or the raw response body when it is not JSON) and appends | caused by: … for each nested message, which is what distinguishes a rejected request parameter from an unloaded model or an unreachable endpoint. A connection test that the model refuses logs the reason as Embedded MLX/llama.cpp connection test failed ; previously the caller only saw a failed test with no explanation. When the reasoning level stored in the profile is not one the model offers, korTTY logs a warning naming the configured level, the level used instead and the levels actually available, and then sends the request at the substitute level. Without that line the request would silently run at a different level than the AI Manager shows. A local sidecar that fails to start or exits unexpectedly logs the tail of its own server log, so the runtime's error output ends up in kortty.log as well. Text, coding, translation, and embedding roles In AI Manager > Local AI , choose separate profiles for Text and translation and Coding . Leaving either choice at Use default AI profile preserves the normal fallback. The same pane also holds a Session journal profile selector used only for the session journal 's AI summaries — the Text/Coding roles do not apply there; leave it on the default profile unless the journal should run on a different model. Terminal summarization, problem solving, questions, descriptions, and translation use the Text role; snippet generation, completion, analysis, security fixes, and workflow generation use the Coding role. Profile resolution follows the most specific available choice: an explicitly selected profile, then a security-specific profile or connection profile where applicable, then the Text/Coding role, then the default profile. The same role mechanism works with embedded, remote HTTP, or local CLI profiles; a role does not force a local model. Dynamic UI translation can use Local AI text profile under Configuration > Global Settings > Translation . This path sends the translation request to the assigned embedded Text profile and does not require a translation-provider API key. The RAG embedding model ID identifies the installed local model used to vectorize knowledge-store documents and searches. Use a dedicated embedding GGUF rather than a chat model unless that model explicitly supports the embeddings route and the configured vector dimensions. Prompt optimization Each AI profile has a Prompt optimization preset. Auto (model detection) uses the verified catalog's model-name mapping, whose bootstrap recognizes Qwen, DeepSeek, Mistral/Mixtral, Gemma, Phi, GPT-OSS, and Llama names; Generic adds no family-specific guidance. You can also force any family preset when a model name is unusual. Presets add short compatibility instructions after korTTY's action contract and AI Skills while preserving the existing strict JSON, code-payload, and safety requirements. GGUF chat templates themselves remain llama.cpp's responsibility. The preset asks supported models to return only the requested final format and to keep reasoning traces out of JSON/code responses. Runtime isolation and updates Every loaded configuration starts llama-server on 127.0.0.1 with a random port and a generated per-process API key. korTTY passes a fixed model path, removes inherited LLAMA_ARG_* and Hugging Face token overrides, and starts the pinned server with offline mode plus its web UI, agent, UI MCP proxy, and slot endpoint disabled. Chat and embeddings use authenticated OpenAI-compatible local routes. Chat sidecars keep the server's default reasoning parsing, which correctly handles both <think> -style reasoning models (whose chain-of-thought is returned separately) and channel/harmony models such as gpt-oss (which require that parsing to produce a final answer at all). The pinned server can still occasionally answer a whole reply with an HTTP 500 (“does not match the expected … format”) when a reply parses ambiguously; korTTY retries a local server error, an empty reply, or a reasoning-only reply once automatically before reporting it (for the dedicated snippet/workflow diagram request, only a server error is retried), and defensively strips any reasoning markers that leak into the content. The runtime build is independent of the application installer. gradle/llama-cpp-pins.properties pins the upstream llama.cpp tag, full commit, and source-archive SHA-256 (plus the history of shipped pins), while build.gradle.kts keeps the API-contract version and korTTY package revision. The korTTY repository detects candidates and validates its source-side native matrix but holds no publication token or runtime signing key. The public korTTY llama.cpp runtime channel owns explicit human-dispatched stable publication: it rebuilds the reviewed source commit, runs authentication, chat/completion, embeddings, JSON-schema, sleep/wake, and parallel-sidecar smoke tests on every package plus a separate Qdrant contract, and publishes immutable descriptors with its own scoped github.token only after entering the protected signing environment. CUDA is not a v1 backend. The update format uses the detached Ed25519 signature runtime-index-v1.sig over the exact bytes of runtime-index-v1.json . Each entry binds the runtime ID, upstream tag and commit, API-contract version, minimum korTTY version, platform, architecture, backend, compressed size, SHA-256, HTTPS URL, executable path, and revocation status. The public verification key is auditable at config/trust/llama-runtime-ed25519-public.pem and embedded in normal local and release builds; an optional CI or Gradle override must match that pinned identity exactly. A missing or invalid trust root, mismatched override, index signature, package size, or package hash stops the operation before untrusted code is activated. A package is installed beside the active version and checked locally with a bounded llama-server --version launch before activation. Activation and the atomic rebinding of registered models occur only while all local inference is idle. The candidate then remains pending first launch until a real GGUF-backed server reaches its authenticated API: success promotes it into the healthy history, while that first real startup failing restores the newest non-revoked previous runtime, rebinds affected models, removes the failed package, and restarts runtime management. The two newest healthy installations are retained, and a package staged during active work is retried after the requests finish. Release CI performs the deeper authenticated chat, embedding, JSON-schema, sleep/wake, and parallel-sidecar contract tests before publication. A verified index withdrawal is enforced immediately for Notify me and Install stable updates automatically checks. korTTY first persists the revoked runtime/installation IDs in a durable denylist and writes a package-local quarantine marker, then clears the active pointer, stops its sidecars, removes revoked versions from rollback history, and replaces affected model bindings with a non-executable marker. Both the runtime installer and every new process launch consult those guards, so an interrupted cleanup or stale models.xml entry cannot restart the withdrawn executable. Local AI remains blocked until a compatible signed replacement is installed; the main window and Local Models status identify the revoked runtime and whether a verified replacement is available. Off performs no network check, so it cannot learn about a newly published withdrawal until the user explicitly checks or enables signed-index checks. The MLX runtime (Apple Silicon) has no per-model quarantine marker because MLX models bind to a model directory rather than to the runtime; its enforcement clears the active pointer, stops every MLX sidecar, and records the withdrawn id in a durable denylist so it can never be reinstalled or reactivated. Registered MLX models stay registered but cannot start until a non-revoked MLX runtime is installed. The single Runtime updates (llama.cpp & MLX) choice controls the automatic check started with korTTY and whenever the preference is saved, and applies to both embedded runtimes: Policy Startup behavior Off Performs no runtime-update network request for either runtime. Non-revoked installed runtimes remain usable, while every withdrawal already persisted locally stays enforced. Notify me (default) Verifies each installed runtime's signed stable index and shows a notification when a compatible update for an installed runtime is available (llama.cpp and, on Apple Silicon, MLX), without automatically installing it. Without an installed runtime no popup appears; the available package is still listed in Local Models. A verified withdrawal of either runtime is enforced immediately. Install stable updates automatically Downloads, verifies, installs, and activates a compatible stable package for each runtime, including a safe replacement for a withdrawn active version; a busy runtime defers its update instead of interrupting an active request. The Runtimes table in Local Models shows the installed version and state per runtime. The action button reads Install runtime when the selected runtime has no verified package and Check/install runtime update otherwise, and checks/installs the stable channel even when the stored automatic policy is Off or Notify me . Runtime candidates are discovered by a weekly workflow, but promotion remains a deliberate, reviewed release action rather than adopting every upstream tag automatically. Other version… switches the selected runtime to any version the signed stable channel still offers for this computer — typically back to an older one when a newer runtime causes problems. The list marks the newest, the active, and the pinned version; revoked versions are never listed. Choosing a version other than the newest pins it: update checks keep that version under every policy, while withdrawals are still enforced, and the Runtimes table shows Pinned with the version. Choosing the newest version, Unpin , or Check/install runtime update follows the stable channel again. If the signed channel revokes a pinned version, korTTY drops the pin so a safe runtime can be installed. A llama.cpp version switch goes through the same health check, idle-only activation, and pending first launch as an update; MLX keeps only the active version on disk, so switching back downloads the verified package again. Files and backup behavior Path Purpose Included in a korTTY backup? ~/.kortty/global-settings.xml Embedded profiles, Text/Coding assignments, embedding model ID, runtime backend/update policy, encrypted Hugging Face token Yes ~/.kortty/llm/models.xml Local GGUF model registrations and typed runtime settings Yes ~/.kortty/llm/mlx-models.json Local MLX model registrations (Apple Silicon) Yes ~/.kortty/llm/models/ Managed GGUF weights No; download or copy them again ~/.kortty/llm/mlx/models/ Managed MLX model directories No; download them again ~/.kortty/llm/mlx/runtime/ Regenerable MLX runtime packages (pinned CPython + mlx-lm) No; reinstall a compatible package ~/.kortty/llm/runtime/ Regenerable llama.cpp packages and active-package metadata No; reinstall a compatible package ~/.kortty/llm/catalog/last-valid-catalog-v1.json Regenerable signature-verified model/prompt catalog cache No; korTTY reverts to the bootstrap and refreshes it again ~/.kortty/llm/run/ Temporary sidecar keys and logs No Troubleshooting No llama.cpp runtime is installed Open AI > AI Manager > Local Models and choose Install runtime , or accept the installation prompt when downloading or importing a model. korTTY downloads the matching signed stable package; the application installer intentionally does not contain a native runtime. A signed runtime check or installation fails Confirm that this korTTY build contains the official runtime-channel public key, that HTTPS access to the stable index and package is available, and that the platform/backend combination is published. korTTY fails closed instead of bypassing a missing trust root, invalid signature, checksum mismatch, incompatible API contract, or runtime quarantine. The runtime update coordinator records the complete failure cause and stack trace in kortty.log under the log directory configured in Configuration > Global Settings > Logging (default ~/.kortty/logs ), even when the manager shows a shorter status message. The update check cannot reach the runtime channel A machine that is offline, behind a captive portal, or whose link is not up yet reports no failure: the startup check ends in an Offline status, already installed runtimes stay usable, every withdrawal already persisted locally stays enforced, and the check simply runs again at the next application start or when the update policy is saved again. An unreachable channel is recorded as a single informational line in kortty.log rather than as a failure with a stack trace, because an unreachable server says nothing about korTTY's own state. An installation you start yourself still reports the unreachable channel in the Local Models status, so a deliberate action never fails silently. The runtime is reported as revoked Open AI > AI Manager > Local Models and install the offered verified replacement. Do not remove the package marker or edit models.xml : the durable denylist still blocks the installation, and revoked versions are deliberately ineligible for rollback or reinstallation. If no compatible replacement is listed, local AI stays unavailable until the stable channel publishes one for this platform/backend. A new runtime version causes problems Select the runtime in AI > AI Manager > Local Models , choose Other version… , and install the previous version. It stays pinned, so update checks do not upgrade it again; choose the newest version or Unpin once a fixed release is available. A new runtime rolls back on its first model start The lightweight --version check passed, but the first real GGUF-backed authenticated API start failed. korTTY restores the newest healthy non-revoked package when available and reports Rolled back ; inspect the Local Models error, model/backend compatibility, and memory settings before trying the update again. A model remains in FAILED Verify that the GGUF and executable still exist, that the executable is runnable, and that the chosen backend is available on this computer. Reduce context size or simultaneous models when system or GPU memory is insufficient. An MLX model cannot start MLX requires an Apple Silicon Mac with macOS 14 or newer and an installed korTTY MLX runtime package under ~/.kortty/llm/mlx/runtime/ . Registered MLX models stay listed without the runtime but report the missing-runtime message when started. The runtime package is published through the signed runtime channel; developers can build an unsigned local package with scripts/build-mlx-runtime-local.sh . The setup function test fails after installation The GGUF remains registered so you can inspect it. Confirm that the matching signed runtime backend is active, reduce the model context or GPU layers if memory is tight, and retry by starting the model. Embedding tests additionally require readable GGUF embedding-dimension metadata. A gated Hugging Face repository returns an authorization error Accept the repository terms on Hugging Face, unlock the korTTY master-password vault, and save an authorized token under AI Manager > Local AI . The token is sent only to the trusted Hugging Face host, not to redirected storage hosts. A download cannot resume If the repository's ETag or immutable file metadata changed, korTTY restarts that file instead of appending incompatible bytes. A checksum mismatch deletes the invalid partial file. Further reading llama.cpp — the inference engine behind korTTY's GGUF sidecars, including the llama-server documentation . GGUF format specification — the single-file model format used by llama.cpp, and llama.cpp's quantization overview explaining labels such as Q4_K_M . Apple MLX and mlx-lm — Apple's machine-learning framework for Apple Silicon and its official language-model server used by korTTY's MLX sidecars. Hugging Face Hub documentation — repositories, revisions, gated models, and access tokens; MLX conversions are collected under mlx-community .","title": "Local models (llama.cpp and MLX)"},{"location": "features/local-models.html#local-models-llamacpp-and-mlx","text": "korTTY can run local language models directly through integrated, model-specific sidecar servers: GGUF models through the pinned llama.cpp llama-server on every platform, and — on Apple Silicon Macs — MLX models through the official mlx-lm server. You do not need LM Studio, Ollama, or a cloud inference account for local chat, text, coding, translation, and embedding workloads. Open AI > AI Manager and use these tabs: Local Models installs, imports, configures, starts, stops, and removes local model registrations (GGUF everywhere, additionally MLX on Apple Silicon). Local AI assigns profiles to the Text/translation and Coding roles and to the session journal's AI summaries, selects the RAG embedding model, stores an optional Hugging Face token, and records the llama.cpp runtime-update policy. Profiles creates an Integrated llama.cpp or Integrated MLX (Apple Silicon) profile for an installed model and controls its prompt optimization preset. Every action button in the tab carries a matching glyph (install, wizard, import, configure, set as default, start, stop, remove, refresh, search, load more, download, pause, cancel), and the pause control switches between pause and resume glyphs during a download.","title": "Local models (llama.cpp and MLX)"},{"location": "features/local-models.html#runtimes","text": "The Runtimes table at the top of Local Models lists both embedded runtimes with their installed version, backend, and state ( Ready , Not installed , or Revoked ): the llama.cpp runtime (every platform) and, on Apple Silicon, the MLX runtime. Three actions operate on the selected row: Install runtime / Check-install runtime update downloads, verifies, and activates the newest compatible package from the signed stable channel — the llama.cpp index for the llama row, the mlx-stable index for the MLX row. Import local package… installs a runtime package file ( .zip ) without downloading it, for example on an air-gapped machine. The file is accepted only when its SHA-256 matches a non-revoked entry of the Ed25519-signed stable index for the current platform; anything not published by the signed channel is refused. The verified package then runs through exactly the same installation machinery as a download (extraction hardening, health/sanity check, idle-only activation). Remove deletes the selected runtime after confirmation. Sidecars must be idle; installed models stay registered but cannot start until a runtime is installed again, and the revocation denylist survives removal so a withdrawn package remains blocked even across reinstalls. Storage and network access Model inference stays on this computer. korTTY contacts Hugging Face only when you search for or download a model that you approve. Public repositories work without a token; an optional token for private or gated repositories is encrypted with the master password. Official builds can also fetch the separately signed model/prompt catalog over HTTPS in the background; no prompt, source document, or model weight is sent with that request.","title": "Runtimes"},{"location": "features/local-models.html#quick-setup-assistant","text": "Select AI > AI Manager > Local Models > Setup assistant . The six-step assistant covers privacy, detected system memory and backend guidance, optional role-specific recommendations, license and exact download-size review, verified installation, and a final readiness summary. On Choose models for optional roles , enable any combination of Text and translation , Coding , and RAG embeddings . Every enabled slot has its own recommendation selector that lists every catalog model the detected memory supports for that role, ordered by preference with the preferred default preselected — a real choice, not a single fixed suggestion. Disabled slots are left unchanged. Text and Coding may deliberately share one compatible recommendation, in which case korTTY downloads and registers that GGUF only once. Opening the assistant from a missing-embedding warning preselects only the RAG-embedding slot. The built-in bootstrap catalog uses conservative RAM tiers. The table shows the preselected default per role; the selector of an enabled slot additionally offers every other catalog model whose memory requirement the detected tier meets: Detected memory Text recommendation Coding recommendation RAG embeddings Less than 16 GiB Qwen3 1.7B, Q4_K_M Qwen3 1.7B, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 16–23 GiB Qwen3 4B, Q4_K_M Qwen2.5-Coder 7B Instruct, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 24 GiB or more Qwen3 8B, Q4_K_M Qwen2.5-Coder 7B Instruct, Q4_K_M Qwen3-Embedding 0.6B, Q8_0 For the RAG-embeddings slot, Qwen3-Embedding 0.6B Q8_0 remains the preselected default on every tier; the selector also offers Qwen3-Embedding 4B Q4_K_M (from 16 GiB), Qwen3-Embedding 8B Q4_K_M (from 24 GiB), and — without a minimum memory requirement — the multilingual BGE-M3 Q8_0 and the very small, fast Nomic Embed Text v1.5 Q8_0 . Recommendations are starting points, not hardware guarantees. The model table labels the estimated fit as Comfortable , Possible , Too large , or Unknown ; context size and simultaneous models also affect memory use. Before Install becomes available, korTTY loads each selected repository at the catalog's fixed revision in the background, verifies that the requested quantization exists, and displays the pinned repository, license, and exact combined GGUF download size. You must review and accept those values. A mutable repository revision or missing quantization stops the assistant before any download starts. Runtime installation, GGUF downloads, verification, registration, and function tests run asynchronously so the JavaFX dialog remains responsive. The progress view names the current model and phase; Cancel installation stops at a safe boundary and retains resumable .part files, while Retry restarts a failed or cancelled run from the verified selections. The Hub-reported context length remains visible for comparison, but a new local model starts with the conservative 4,096-token runtime context. Increase it later under Configure only after considering the additional RAM/VRAM use. After every selected GGUF is downloaded and registered, the assistant sends a real local chat-completion request or, for an embedding-only model, a real embedding request through the installed runtime. Only after all tests pass does it atomically save the selected Text/Coding profile IDs and RAG embedding-model ID in global-settings.xml ; it creates a reusable Local: … embedded profile when a selected chat model has none and makes the first such profile the default only when no default exists. A failed test does not produce a successful finish page or persist partially updated role assignments.","title": "Quick setup assistant"},{"location": "features/local-models.html#signed-model-and-prompt-catalog","text": "korTTY always contains a small bootstrap catalog with the memory-tier recommendations and prompt-family detection needed for offline setup. All nine bundled model recommendations carry a concrete 40-character Hugging Face commit, so even the bootstrap never resolves a mutable repository head when the assistant later fetches metadata or GGUF files. Official builds can update those data independently from the application through model-prompt-catalog-v1.json and its detached Ed25519 signature model-prompt-catalog-v1.sig . On the first use of recommendations or automatic prompt detection, korTTY immediately loads the last signature-verified cache and performs at most one background refresh from the fixed HTTPS stable channel. The catalog can update recommended model IDs, fixed revisions, quantizations, roles, RAM thresholds and ordering, plus the mapping from model-name tokens to built-in prompt presets. It cannot inject arbitrary executable code or replace korTTY's built-in action, safety, JSON, or code-output contracts. The exact catalog bytes are verified before strict schema-v1 parsing, and unknown schema fields are rejected. A failed download, invalid signature, or invalid catalog leaves the last valid cache untouched. If the application build has no valid catalog public trust root, korTTY performs no catalog network request, does not trust an existing cache, and uses only the built-in bootstrap. Each catalog also carries a positive monotonic sequence. During refresh, korTTY rejects a signed catalog whose sequence is older than the last accepted cache or bootstrap, and rejects an equal-sequence catalog with a different version. A newly accepted catalog with the highest sequence must be written to the atomic cache before it becomes active; the protected promotion workflow separately requires every official sequence to be strictly greater than the sequence in the latest published release, preventing a correctly signed older catalog from replaying obsolete recommendations.","title": "Signed model and prompt catalog"},{"location": "features/local-models.html#find-and-download-models","text": "The Hugging Face browser searches model repositories and shows repository, format, architecture, available quantizations, license, selected-quantization size, context length, publication age, and a hardware estimate. The Age column reflects when the repository was first published on the Hub — shown as today , days, months, or years, and — when the Hub does not report a creation date. On Apple Silicon a Format selector next to the search field switches between GGUF + MLX (default), GGUF , and MLX ; each enabled format keeps its own cursor pagination, and the Format column identifies every row. Other platforms search GGUF only. Click a column header to sort the results by that column — a second click reverses the direction, and the header shows the active sort arrow. Size, context and age sort numerically (repositories without a reported creation date sort last), and the hardware estimate sorts from best to worst fit; the chosen order persists while background metadata loads complete. Only repositories this computer can actually run appear: korTTY completes the lightweight search listing with exact per-file metadata in the background and hides rows whose size is unknown or whose smallest quantization exceeds the detected memory. The status line reports how many usable repositories are shown and how many were hidden. Selecting a row loads exact metadata for that repository's immutable revision and shows Loading… while Size and Hardware estimate refresh; the chosen quantization remains selected. Projector, quantization-matrix, and speculative-decoding helper GGUFs are excluded from the downloadable language-model choices. Load more continues the same search without discarding earlier results. Select a repository and quantization, review its license and size, then choose Download and install . korTTY installs only an immutable 40-character repository revision with exact file metadata. After you confirm the license and download size, a fixed Model download panel at the bottom identifies the repository and quantization, the current file and multipart shard, transferred and total bytes, elapsed time, transfer rate, and estimated remaining time. Its full-width progress bar and Pause / Resume and Cancel controls remain available while you continue to review the manager. Downloads use .part files, free-space checks, HTTP Range and If-Range , content-digest verification (SHA-256 for LFS files, the git blob hash for small in-tree files), and multipart shard ordering. Cancellation, including closing the AI Manager during a transfer, keeps the partial data needed for a later resume. An unpinned repository or a missing file digest is rejected instead of silently installing mutable content. An MLX repository is downloaded as one complete directory (safetensors weights, tokenizer, and configuration files); its quantization is a repository-level property such as 4BIT or 8BIT , so the quantization selector shows exactly one entry. If no MLX runtime is installed yet, korTTY asks whether to download and register the model anyway — it can be started as soon as the runtime is installed.","title": "Find and download models"},{"location": "features/local-models.html#mlx-models-on-apple-silicon","text": "On Apple Silicon Macs (macOS 14 or newer), korTTY additionally runs MLX models — Apple's array framework for machine learning on Apple GPUs — through the official mlx-lm server. The mlx-community organization on Hugging Face publishes thousands of ready-converted models. Registration and lifecycle mirror GGUF models: installed MLX models appear in the same table with backend MLX , support Start selected , Stop selected , Remove , and Configure (display name and unload-after idle time), and report the same runtime states. Isolation mirrors llama.cpp: every model runs as its own loopback-only sidecar with a random port and a generated per-process API key. korTTY wraps mlx_lm.server — which has no authentication of its own — in its own launcher that rejects every unauthenticated request except the local health probe, forces offline Hugging Face access, strips inherited Python and token environment overrides, and exits after the configured idle time. The MLX runtime package is separate from both the application installer and the llama.cpp runtime: a pinned relocatable CPython plus a hash-locked mlx-lm wheel set, built in CI and published through the same Ed25519-signed index mechanism as the llama.cpp packages and resolved through the same latest release. Install, update, locally import, or remove it in the Runtimes table; without an installed MLX runtime, MLX models stay registered but cannot start. Profiles : choose Integrated MLX (Apple Silicon) as the profile connection and select the installed model from the Local MLX model list. LM-Studio-MCP internet modes are not available for embedded profiles; the korTTY web-search tool works normally. Reasoning models (for example Qwen3 conversions) are handled like their GGUF counterparts: korTTY separates the inline chain-of-thought from the answer and retries a reply that contains only reasoning once automatically (except for the dedicated snippet/workflow diagram request, which fails immediately instead).","title": "MLX models on Apple Silicon"},{"location": "features/local-models.html#import-existing-gguf-files","text": "Select Import GGUF , choose one or more .gguf files, and then choose one mode: Mode Behavior Managed copy Copies the GGUF into korTTY's model directory using a temporary file and atomic activation where the filesystem supports it. External reference Registers the original path. Moving or deleting that file makes the model unavailable; removing the registration never deletes the external file. A compatible, verified llama-server runtime must also be installed. Runtime packages live separately under ~/.kortty/llm/runtime/ and are not part of the base application installer. When you download or import the first model, korTTY offers to install the matching signed stable runtime before continuing; you can also choose Install runtime in Local Models . The model manager does not accept an arbitrary executable as a replacement for the verified package.","title": "Import existing GGUF files"},{"location": "features/local-models.html#configure-and-run-models","text": "The installed-models table also carries an Age column: the model's upstream publication date, captured once at install time. Models installed before this column existed and manually imported GGUF files have no captured date and show — . Select an installed model and choose Configure . The available settings are deliberately typed and bounded; arbitrary server arguments are not accepted. Saving first asks the runtime manager to stop the model. If that model is processing a request, korTTY refuses the change and leaves both the running sidecar and persisted configuration untouched; retry after the request finishes. Setting Values Default Backend Auto, CPU, Metal, Vulkan Auto Context size 512–2,097,152 tokens 4,096 CPU threads 0–1,024; 0 means automatic Automatic GPU layers -1–10,000; -1 means automatic Automatic Unload after 1–1,440 minutes, or Never 10 minutes The per-model backend describes how that model should run. In AI Manager > Local AI , Preferred runtime backend controls which signed native package is installed and updated: macOS offers Auto, CPU, and Metal; Windows/Linux offer Auto, CPU, and Vulkan. Automatic (keep active backend) preserves the already active backend during updates. For a first installation, Auto prefers Metal and falls back to CPU on macOS; on Windows and Linux it prefers CPU and falls back to Vulkan only when no compatible CPU package is published. An explicitly selected backend requires an exact compatible package and never changes to another backend silently. Use multi-selection and Start selected to load several different models concurrently. Profiles that point to the same installed model and runtime configuration share one authenticated sidecar. The table reports STOPPED , STARTING , LOADING , READY , BUSY , SLEEPING , or FAILED . You can mark one installed model as the default with Set as default in the action bar or the table's right-click menu; Clear default in the same menu removes the mark. The default model is shown with a leading ★ in the Name column and is pre-selected when the manager opens, so the Start/Configure/Stop actions target it by default. This is a convenience marker for the manager only — it does not change which AI profile the agent, chat, or other AI features use; those continue to follow the AI profile configuration. If a selected Metal or Vulkan model requires a different package from the active runtime, korTTY offers to download, verify, and activate the matching signed package without interrupting current requests. Models that require incompatible GPU runtimes must be started separately with the matching preferred backend. After the configured idle time, llama.cpp releases the model tensors from RAM/VRAM and korTTY marks the model as sleeping while retaining the lightweight process. The next request acquires a runtime lease and wakes the model automatically. Stop selected terminates only idle sidecars; an active generation is never interrupted by model removal or a normal stop request. To make this activity visible, korTTY records the request and model lifecycle in kortty.log (under the log directory configured in Configuration > Global Settings > Logging , default ~/.kortty/logs ). Every AI request logs one AI request sent line and a matching AI request done / AI request failed line with the action, provider, model, the reasoning level the request actually uses ( reasoningEffort= ), approximate input size, duration and token count — metadata only, never the prompt or response text. Local models additionally log when a model is Loaded , Unloaded … (sleeping) after the idle timeout, or Unloaded … sidecar stopped , for both the llama.cpp and MLX runtimes. All of these are INFO lines, so they appear with the default log level. Errors reported by the model itself are logged too, so a failing request can be diagnosed from the log alone: AI request failed carries the server's own error text (its error.message , or the raw response body when it is not JSON) and appends | caused by: … for each nested message, which is what distinguishes a rejected request parameter from an unloaded model or an unreachable endpoint. A connection test that the model refuses logs the reason as Embedded MLX/llama.cpp connection test failed ; previously the caller only saw a failed test with no explanation. When the reasoning level stored in the profile is not one the model offers, korTTY logs a warning naming the configured level, the level used instead and the levels actually available, and then sends the request at the substitute level. Without that line the request would silently run at a different level than the AI Manager shows. A local sidecar that fails to start or exits unexpectedly logs the tail of its own server log, so the runtime's error output ends up in kortty.log as well.","title": "Configure and run models"},{"location": "features/local-models.html#text-coding-translation-and-embedding-roles","text": "In AI Manager > Local AI , choose separate profiles for Text and translation and Coding . Leaving either choice at Use default AI profile preserves the normal fallback. The same pane also holds a Session journal profile selector used only for the session journal 's AI summaries — the Text/Coding roles do not apply there; leave it on the default profile unless the journal should run on a different model. Terminal summarization, problem solving, questions, descriptions, and translation use the Text role; snippet generation, completion, analysis, security fixes, and workflow generation use the Coding role. Profile resolution follows the most specific available choice: an explicitly selected profile, then a security-specific profile or connection profile where applicable, then the Text/Coding role, then the default profile. The same role mechanism works with embedded, remote HTTP, or local CLI profiles; a role does not force a local model. Dynamic UI translation can use Local AI text profile under Configuration > Global Settings > Translation . This path sends the translation request to the assigned embedded Text profile and does not require a translation-provider API key. The RAG embedding model ID identifies the installed local model used to vectorize knowledge-store documents and searches. Use a dedicated embedding GGUF rather than a chat model unless that model explicitly supports the embeddings route and the configured vector dimensions.","title": "Text, coding, translation, and embedding roles"},{"location": "features/local-models.html#prompt-optimization","text": "Each AI profile has a Prompt optimization preset. Auto (model detection) uses the verified catalog's model-name mapping, whose bootstrap recognizes Qwen, DeepSeek, Mistral/Mixtral, Gemma, Phi, GPT-OSS, and Llama names; Generic adds no family-specific guidance. You can also force any family preset when a model name is unusual. Presets add short compatibility instructions after korTTY's action contract and AI Skills while preserving the existing strict JSON, code-payload, and safety requirements. GGUF chat templates themselves remain llama.cpp's responsibility. The preset asks supported models to return only the requested final format and to keep reasoning traces out of JSON/code responses.","title": "Prompt optimization"},{"location": "features/local-models.html#runtime-isolation-and-updates","text": "Every loaded configuration starts llama-server on 127.0.0.1 with a random port and a generated per-process API key. korTTY passes a fixed model path, removes inherited LLAMA_ARG_* and Hugging Face token overrides, and starts the pinned server with offline mode plus its web UI, agent, UI MCP proxy, and slot endpoint disabled. Chat and embeddings use authenticated OpenAI-compatible local routes. Chat sidecars keep the server's default reasoning parsing, which correctly handles both <think> -style reasoning models (whose chain-of-thought is returned separately) and channel/harmony models such as gpt-oss (which require that parsing to produce a final answer at all). The pinned server can still occasionally answer a whole reply with an HTTP 500 (“does not match the expected … format”) when a reply parses ambiguously; korTTY retries a local server error, an empty reply, or a reasoning-only reply once automatically before reporting it (for the dedicated snippet/workflow diagram request, only a server error is retried), and defensively strips any reasoning markers that leak into the content. The runtime build is independent of the application installer. gradle/llama-cpp-pins.properties pins the upstream llama.cpp tag, full commit, and source-archive SHA-256 (plus the history of shipped pins), while build.gradle.kts keeps the API-contract version and korTTY package revision. The korTTY repository detects candidates and validates its source-side native matrix but holds no publication token or runtime signing key. The public korTTY llama.cpp runtime channel owns explicit human-dispatched stable publication: it rebuilds the reviewed source commit, runs authentication, chat/completion, embeddings, JSON-schema, sleep/wake, and parallel-sidecar smoke tests on every package plus a separate Qdrant contract, and publishes immutable descriptors with its own scoped github.token only after entering the protected signing environment. CUDA is not a v1 backend. The update format uses the detached Ed25519 signature runtime-index-v1.sig over the exact bytes of runtime-index-v1.json . Each entry binds the runtime ID, upstream tag and commit, API-contract version, minimum korTTY version, platform, architecture, backend, compressed size, SHA-256, HTTPS URL, executable path, and revocation status. The public verification key is auditable at config/trust/llama-runtime-ed25519-public.pem and embedded in normal local and release builds; an optional CI or Gradle override must match that pinned identity exactly. A missing or invalid trust root, mismatched override, index signature, package size, or package hash stops the operation before untrusted code is activated. A package is installed beside the active version and checked locally with a bounded llama-server --version launch before activation. Activation and the atomic rebinding of registered models occur only while all local inference is idle. The candidate then remains pending first launch until a real GGUF-backed server reaches its authenticated API: success promotes it into the healthy history, while that first real startup failing restores the newest non-revoked previous runtime, rebinds affected models, removes the failed package, and restarts runtime management. The two newest healthy installations are retained, and a package staged during active work is retried after the requests finish. Release CI performs the deeper authenticated chat, embedding, JSON-schema, sleep/wake, and parallel-sidecar contract tests before publication. A verified index withdrawal is enforced immediately for Notify me and Install stable updates automatically checks. korTTY first persists the revoked runtime/installation IDs in a durable denylist and writes a package-local quarantine marker, then clears the active pointer, stops its sidecars, removes revoked versions from rollback history, and replaces affected model bindings with a non-executable marker. Both the runtime installer and every new process launch consult those guards, so an interrupted cleanup or stale models.xml entry cannot restart the withdrawn executable. Local AI remains blocked until a compatible signed replacement is installed; the main window and Local Models status identify the revoked runtime and whether a verified replacement is available. Off performs no network check, so it cannot learn about a newly published withdrawal until the user explicitly checks or enables signed-index checks. The MLX runtime (Apple Silicon) has no per-model quarantine marker because MLX models bind to a model directory rather than to the runtime; its enforcement clears the active pointer, stops every MLX sidecar, and records the withdrawn id in a durable denylist so it can never be reinstalled or reactivated. Registered MLX models stay registered but cannot start until a non-revoked MLX runtime is installed. The single Runtime updates (llama.cpp & MLX) choice controls the automatic check started with korTTY and whenever the preference is saved, and applies to both embedded runtimes: Policy Startup behavior Off Performs no runtime-update network request for either runtime. Non-revoked installed runtimes remain usable, while every withdrawal already persisted locally stays enforced. Notify me (default) Verifies each installed runtime's signed stable index and shows a notification when a compatible update for an installed runtime is available (llama.cpp and, on Apple Silicon, MLX), without automatically installing it. Without an installed runtime no popup appears; the available package is still listed in Local Models. A verified withdrawal of either runtime is enforced immediately. Install stable updates automatically Downloads, verifies, installs, and activates a compatible stable package for each runtime, including a safe replacement for a withdrawn active version; a busy runtime defers its update instead of interrupting an active request. The Runtimes table in Local Models shows the installed version and state per runtime. The action button reads Install runtime when the selected runtime has no verified package and Check/install runtime update otherwise, and checks/installs the stable channel even when the stored automatic policy is Off or Notify me . Runtime candidates are discovered by a weekly workflow, but promotion remains a deliberate, reviewed release action rather than adopting every upstream tag automatically. Other version… switches the selected runtime to any version the signed stable channel still offers for this computer — typically back to an older one when a newer runtime causes problems. The list marks the newest, the active, and the pinned version; revoked versions are never listed. Choosing a version other than the newest pins it: update checks keep that version under every policy, while withdrawals are still enforced, and the Runtimes table shows Pinned with the version. Choosing the newest version, Unpin , or Check/install runtime update follows the stable channel again. If the signed channel revokes a pinned version, korTTY drops the pin so a safe runtime can be installed. A llama.cpp version switch goes through the same health check, idle-only activation, and pending first launch as an update; MLX keeps only the active version on disk, so switching back downloads the verified package again.","title": "Runtime isolation and updates"},{"location": "features/local-models.html#files-and-backup-behavior","text": "Path Purpose Included in a korTTY backup? ~/.kortty/global-settings.xml Embedded profiles, Text/Coding assignments, embedding model ID, runtime backend/update policy, encrypted Hugging Face token Yes ~/.kortty/llm/models.xml Local GGUF model registrations and typed runtime settings Yes ~/.kortty/llm/mlx-models.json Local MLX model registrations (Apple Silicon) Yes ~/.kortty/llm/models/ Managed GGUF weights No; download or copy them again ~/.kortty/llm/mlx/models/ Managed MLX model directories No; download them again ~/.kortty/llm/mlx/runtime/ Regenerable MLX runtime packages (pinned CPython + mlx-lm) No; reinstall a compatible package ~/.kortty/llm/runtime/ Regenerable llama.cpp packages and active-package metadata No; reinstall a compatible package ~/.kortty/llm/catalog/last-valid-catalog-v1.json Regenerable signature-verified model/prompt catalog cache No; korTTY reverts to the bootstrap and refreshes it again ~/.kortty/llm/run/ Temporary sidecar keys and logs No","title": "Files and backup behavior"},{"location": "features/local-models.html#troubleshooting","text": "No llama.cpp runtime is installed Open AI > AI Manager > Local Models and choose Install runtime , or accept the installation prompt when downloading or importing a model. korTTY downloads the matching signed stable package; the application installer intentionally does not contain a native runtime. A signed runtime check or installation fails Confirm that this korTTY build contains the official runtime-channel public key, that HTTPS access to the stable index and package is available, and that the platform/backend combination is published. korTTY fails closed instead of bypassing a missing trust root, invalid signature, checksum mismatch, incompatible API contract, or runtime quarantine. The runtime update coordinator records the complete failure cause and stack trace in kortty.log under the log directory configured in Configuration > Global Settings > Logging (default ~/.kortty/logs ), even when the manager shows a shorter status message. The update check cannot reach the runtime channel A machine that is offline, behind a captive portal, or whose link is not up yet reports no failure: the startup check ends in an Offline status, already installed runtimes stay usable, every withdrawal already persisted locally stays enforced, and the check simply runs again at the next application start or when the update policy is saved again. An unreachable channel is recorded as a single informational line in kortty.log rather than as a failure with a stack trace, because an unreachable server says nothing about korTTY's own state. An installation you start yourself still reports the unreachable channel in the Local Models status, so a deliberate action never fails silently. The runtime is reported as revoked Open AI > AI Manager > Local Models and install the offered verified replacement. Do not remove the package marker or edit models.xml : the durable denylist still blocks the installation, and revoked versions are deliberately ineligible for rollback or reinstallation. If no compatible replacement is listed, local AI stays unavailable until the stable channel publishes one for this platform/backend. A new runtime version causes problems Select the runtime in AI > AI Manager > Local Models , choose Other version… , and install the previous version. It stays pinned, so update checks do not upgrade it again; choose the newest version or Unpin once a fixed release is available. A new runtime rolls back on its first model start The lightweight --version check passed, but the first real GGUF-backed authenticated API start failed. korTTY restores the newest healthy non-revoked package when available and reports Rolled back ; inspect the Local Models error, model/backend compatibility, and memory settings before trying the update again. A model remains in FAILED Verify that the GGUF and executable still exist, that the executable is runnable, and that the chosen backend is available on this computer. Reduce context size or simultaneous models when system or GPU memory is insufficient. An MLX model cannot start MLX requires an Apple Silicon Mac with macOS 14 or newer and an installed korTTY MLX runtime package under ~/.kortty/llm/mlx/runtime/ . Registered MLX models stay listed without the runtime but report the missing-runtime message when started. The runtime package is published through the signed runtime channel; developers can build an unsigned local package with scripts/build-mlx-runtime-local.sh . The setup function test fails after installation The GGUF remains registered so you can inspect it. Confirm that the matching signed runtime backend is active, reduce the model context or GPU layers if memory is tight, and retry by starting the model. Embedding tests additionally require readable GGUF embedding-dimension metadata. A gated Hugging Face repository returns an authorization error Accept the repository terms on Hugging Face, unlock the korTTY master-password vault, and save an authorized token under AI Manager > Local AI . The token is sent only to the trusted Hugging Face host, not to redirected storage hosts. A download cannot resume If the repository's ETag or immutable file metadata changed, korTTY restarts that file instead of appending incompatible bytes. A checksum mismatch deletes the invalid partial file.","title": "Troubleshooting"},{"location": "features/local-models.html#further-reading","text": "llama.cpp — the inference engine behind korTTY's GGUF sidecars, including the llama-server documentation . GGUF format specification — the single-file model format used by llama.cpp, and llama.cpp's quantization overview explaining labels such as Q4_K_M . Apple MLX and mlx-lm — Apple's machine-learning framework for Apple Silicon and its official language-model server used by korTTY's MLX sidecars. Hugging Face Hub documentation — repositories, revisions, gated models, and access tokens; MLX conversions are collected under mlx-community .","title": "Further reading"},{"location": "features/projects.html","text": "Projects (workspaces) Projects save and restore your complete workspace state—all open windows, tabs, SSH connections, and terminal sessions. This lets you quickly switch between different work contexts without manually reconnecting or repositioning windows. Saving a Project Open File > Save Project or press Ctrl + S ( Cmd + S on macOS). Enter a name and optional description for the project. Configure Auto-Reconnect : When enabled, opening the project will automatically reconnect all saved SSH sessions. When disabled, windows and tabs are restored but you must manually reconnect. Click Save . Projects are stored as .kortty files in ~/.kortty/projects/ . Opening a Project Open File > Open Project or press Ctrl + O ( Cmd + O on macOS). Select a .kortty project file from the file browser. The Project Preview dialog appears, showing: Number of windows to be restored Tabs and connections in each window Project metadata (name, description, last modified) Click Open to load the project. What Gets Saved A project captures the complete state of your workspace: Component Details Windows All open KorTTY windows and their positions/sizes Tabs All terminal tabs in each window, including split-pane configurations Connections The saved connection names for each tab Dashboard Dashboard visibility and divider position Active Tab Which tab was active in each window Terminal Sessions Session state including cursor position and scrollback (if supported by the session) Note AI result tabs and tool tabs (managers opened as tabs) are not saved with projects. They remain only in the current session and are lost when you close the tab or open a project. Auto-Reconnect When Auto-Reconnect is enabled, KorTTY automatically: Restores all windows with their saved geometry (position and size) Reconnects each SSH tab using the original connection settings Restores the active tab and dashboard state If Auto-Reconnect is disabled, windows and tabs are restored but you must manually reconnect each tab by clicking it or using Reconnect from the context menu. Project File Storage Projects are stored in ~/.kortty/projects/ as compressed .kortty files. Each project includes: Metadata (name, description, creation/modification timestamps) Complete window and tab state Connection references (by name) Dashboard visibility and layout Use Cases Projects are useful for: Context switching — Save a \"production systems\" project, a \"development\" project, and a \"testing\" project; open the one you need Team handoffs — Share projects with colleagues to set up identical workspace layouts and connections Multi-window layouts — Save a complex setup across multiple monitor windows and restore it instantly Session recovery — Quickly restore your last known configuration if the app crashes or you accidentally close tabs","title": "Projects (workspaces)"},{"location": "features/projects.html#projects-workspaces","text": "Projects save and restore your complete workspace state—all open windows, tabs, SSH connections, and terminal sessions. This lets you quickly switch between different work contexts without manually reconnecting or repositioning windows.","title": "Projects (workspaces)"},{"location": "features/projects.html#saving-a-project","text": "Open File > Save Project or press Ctrl + S ( Cmd + S on macOS). Enter a name and optional description for the project. Configure Auto-Reconnect : When enabled, opening the project will automatically reconnect all saved SSH sessions. When disabled, windows and tabs are restored but you must manually reconnect. Click Save . Projects are stored as .kortty files in ~/.kortty/projects/ .","title": "Saving a Project"},{"location": "features/projects.html#opening-a-project","text": "Open File > Open Project or press Ctrl + O ( Cmd + O on macOS). Select a .kortty project file from the file browser. The Project Preview dialog appears, showing: Number of windows to be restored Tabs and connections in each window Project metadata (name, description, last modified) Click Open to load the project.","title": "Opening a Project"},{"location": "features/projects.html#what-gets-saved","text": "A project captures the complete state of your workspace: Component Details Windows All open KorTTY windows and their positions/sizes Tabs All terminal tabs in each window, including split-pane configurations Connections The saved connection names for each tab Dashboard Dashboard visibility and divider position Active Tab Which tab was active in each window Terminal Sessions Session state including cursor position and scrollback (if supported by the session) Note AI result tabs and tool tabs (managers opened as tabs) are not saved with projects. They remain only in the current session and are lost when you close the tab or open a project.","title": "What Gets Saved"},{"location": "features/projects.html#auto-reconnect","text": "When Auto-Reconnect is enabled, KorTTY automatically: Restores all windows with their saved geometry (position and size) Reconnects each SSH tab using the original connection settings Restores the active tab and dashboard state If Auto-Reconnect is disabled, windows and tabs are restored but you must manually reconnect each tab by clicking it or using Reconnect from the context menu.","title": "Auto-Reconnect"},{"location": "features/projects.html#project-file-storage","text": "Projects are stored in ~/.kortty/projects/ as compressed .kortty files. Each project includes: Metadata (name, description, creation/modification timestamps) Complete window and tab state Connection references (by name) Dashboard visibility and layout","title": "Project File Storage"},{"location": "features/projects.html#use-cases","text": "Projects are useful for: Context switching — Save a \"production systems\" project, a \"development\" project, and a \"testing\" project; open the one you need Team handoffs — Share projects with colleagues to set up identical workspace layouts and connections Multi-window layouts — Save a complex setup across multiple monitor windows and restore it instantly Session recovery — Quickly restore your last known configuration if the app crashes or you accidentally close tabs","title": "Use Cases"},{"location": "features/rag.html","text": "RAG knowledge stores A knowledge store lets an AI profile answer with relevant excerpts from local files without sending the complete source collection to the model. The default store uses a local cosine-similarity HNSW snapshot; an existing Qdrant service is available as an optional second store type. Create a local knowledge store Open AI > AI Manager > Knowledge Stores and choose Create . The beginner flow needs a name, an installed embedding model, role assignments, and at least one reviewed source. Store type, vector dimensions, and optional Qdrant connection fields stay collapsed under Advanced ; korTTY manages the local HNSW graph parameters internally, so normal use requires no vector-index tuning. If no embedding model is installed, the same flow opens the local-model setup assistant with the embedding role preselected. Install the preselected Qwen3-Embedding 0.6B Q8_0 model, or pick one of the catalog alternatives the assistant offers for this computer's memory — Qwen3-Embedding 4B or 8B Q4_K_M on machines with at least 16 or 24 GiB, the multilingual BGE-M3 Q8_0 , or the very small and fast Nomic Embed Text v1.5 Q8_0 — then return to Create . An index is tied to that exact embedding model and dimension count; changing either requires rebuilding it. To add content: Choose Add files for one or more individual files, or Add folder for a recursive directory source. While korTTY scans the selection on a background worker, the Knowledge Stores tab remains responsive and reports Checking selected files and folders… . You can cancel this scan before the review dialog opens. Review the preview table. Each row shows path, format, size, status, and an explanation when skipped; the summary distinguishes unchanged files from files that will be indexed and groups recognized formats. Confirm only after the preview matches what you intended to index. Wait for text extraction, chunking, embeddings, and snapshot activation to finish. You can cancel before activation without damaging the previous snapshot. The source table shows the path, source type, synchronization mode, status, file/chunk counts, problem count, and last successful index. These values persist across restarts. Available actions include Add files , Add folder , Update now , Disable/Enable , Remove , and Test search . A folder is always reviewed first Adding a directory never means “send every file.” korTTY scans without following symbolic links, applies the central format allowlist and safe directory exclusions, validates the file contents on a background worker, and presents the accepted and skipped set before indexing begins. The source is not saved and indexing does not start unless you confirm that preview. Supported formats File-name matching is case-insensitive. A recognized suffix is necessary but not sufficient: non-PDF files must contain valid UTF-8 text without binary NUL/control bytes, and PDFs must be readable and contain extractable text. Category Supported files Documents .txt , .md , .markdown , .adoc , .asciidoc , .rst , text-based .pdf Structured data .json , .jsonl , .xml , .yaml , .yml , .toml , .ini , .cfg , .conf , .properties , .csv , .tsv Shell and scripts .sh , .bash , .zsh , .fish , .bat , .cmd , .awk , .ps1 , .psm1 , .psd1 , .py , .pyw , .pyi , .rb , .pl , .php , .lua JVM and .NET .java , .kt , .kts , .groovy , .gradle , .gvy , .gy , .gsh , .scala , .sc , .cs JavaScript and web .js , .jsx , .mjs , .cjs , .ts , .tsx , .mts , .cts , .html , .htm , .css , .scss , .sass , .less , .vue , .svelte Systems and data code .c , .h , .cc , .cpp , .cxx , .hpp , .hxx , .hh , .inl , .go , .rs , .swift , .sql Known extensionless files README , LICENSE , Dockerfile , Makefile The following are not indexed in v1: Office documents, images or OCR, archives, databases, audio, video, and arbitrary binary formats. Password-protected PDFs and PDFs with no extractable text are reported as skipped. The default limit is 50 MiB per file. A source can set it from 1 MiB to 1 GiB under Advanced , but custom include globs can only narrow the central allowlist; they cannot turn an unsupported or binary format into an accepted source. One preview is also bounded to 5,000 visited files, 500 MiB of accepted source data, and 100 MiB of extracted characters. Safe folder scanning Folder sources are recursive by default. korTTY does not follow symbolic links and excludes hidden paths plus common metadata, dependency, build, cache, and IDE directories: .git .hg .svn .gradle .idea .vscode .venv venv node_modules vendor build target dist out coverage __pycache__ Optional include/exclude globs and .gitignore handling are available under Advanced . Include rules are evaluated after the fixed format allowlist. A single file already covered by a folder source is not added a second time, and identical or overlapping folder sources are rejected with the path of the existing source. Automatic and manual synchronization Each file or folder has its own synchronization mode: Mode Behavior Automatic Default. korTTY watches the file or directory while running, groups bursts of changes for three seconds, and then runs an incremental synchronization. At application start it also reconciles enabled Automatic sources independently of credential or JobScheduler startup, catching changes made while korTTY was closed. Manual The source changes only when you choose Update now . File-system watching is platform-dependent. If a source cannot be watched reliably, it remains usable and manually refreshable; use Update now whenever the manager reports limited monitoring. Synchronization hashes the complete accepted file. Update now performs the same cancellable background scan and shows the same review table before any work begins. A second scan or indexing operation is refused while one is already active, preventing overlapping preview/index jobs for the pane. Unchanged files keep their existing chunks and vectors; new or changed files are extracted, chunked, and embedded again. Deleted files remove their chunks on the next synchronization, and a rename is processed as one deletion plus one addition. Disable keeps the source configuration and indexed chunks but excludes them from retrieval. Remove asks for confirmation and deletes that source's chunks. Removing a source never deletes the original files. Deleting an entire local store also removes its confirmed index directory under ~/.kortty/rag/stores/ , but never touches the configured source documents. Deleting a Qdrant store definition first removes the vectors for its configured sources, but does not delete the external Qdrant service itself. Indexing and failure safety Text is divided into deterministic chunks of approximately 800 tokens with a 120-token overlap. PDF chunks retain their page number for citations. Embeddings are requested from the selected authenticated local llama.cpp model in batches of 32. For a local store, korTTY builds a deterministic hierarchical HNSW graph in memory with cosine similarity. Exponentially distributed upper layers provide greedy entry-point navigation, while construction and bounded search use internal M , efConstruction , and efSearch defaults. korTTY writes the candidate to a temporary index.hnsw snapshot, flushes it, and then activates it with an atomic rename where supported. Cancellation, extraction errors, embedding errors, or a power loss before activation leave the previous active snapshot untouched. A legacy single-layer v1 snapshot is rebuilt and atomically promoted to the hierarchical v2 format when first opened. During indexing, the status line reports the current source state, documents, chunks, problems, and percentage. Completion summarizes newly indexed, unchanged, removed, and skipped documents; the source table retains status, file/chunk/problem counts, and completion time, while the source configuration persists the content hashes used for the next comparison. Unchanged document hashes reuse their vectors while changed, new, and deleted documents update the replacement snapshot. A configured file or folder root that has actually been deleted is treated as an incremental deletion: korTTY atomically replaces that source with no chunks, reports the number of removed documents, persists empty hashes/counts, and marks the source MISSING , preventing stale excerpts from remaining retrievable. Other fatal scan failures, such as a type mismatch, permission/extraction error, or symbolic-link root, report ERROR and preserve the previous active snapshot instead of erasing known-good vectors. Readable files can still be indexed when unrelated entries were skipped with warnings. The optional Qdrant adapter creates or validates a cosine collection, stores the same chunk metadata, and updates one source at a time through the REST API. Remote endpoints must use HTTPS; plain HTTP is accepted only for a loopback Qdrant instance. The configured endpoint and vault-protected API key belong to that Qdrant store; local HNSW requires no database service. Use a store with an AI profile When creating or configuring a knowledge store, select Text , Coding , or both roles. korTTY associates the knowledge store with the profiles currently assigned to those roles. Enable Autonomous workflows separately only when agent-style background prompts may retrieve from this knowledge store. Cloud profiles receive retrieved excerpts korTTY never sends the complete source collection, HNSW graph, or Qdrant collection to a chat model. It sends only the bounded excerpts retrieved for the current request. Those excerpts remain on this computer with an integrated llama.cpp or provably loopback HTTP profile, but they leave the computer when the selected Text/Coding profile uses a cloud endpoint. Selecting a knowledge-store role records that knowledge store on the profile currently assigned to the role and is therefore explicit permission to disclose matching excerpts to that profile. The service factory may automatically add other eligible role-assigned knowledge stores only to integrated or loopback profiles; cloud, LAN, hostname-lookalike, and CLI profiles use only their persisted explicit knowledge-store assignments. For ordinary korTTY AI actions, retrieval uses the user's prompt, or the selected terminal/snippet text when there is no separate prompt. The query is embedded with the store's model, searches only enabled sources, and applies these fixed bounds: At most six excerpts in total. At most two excerpts from one source. At most 4,000 tokens and never more than 25% of the target model's context window. The result is inserted after korTTY's action contract and AI Skills and before the model-family prompt preset. It is wrapped in <retrieved_context> and explicitly marked as untrusted data, not instructions. Source locations receive stable markers such as [R1] ; whenever an answer relies on an excerpt, the model is required to cite that exact marker. Text inside a source cannot override system rules, request tools, reveal secrets, or close the retrieved-context wrapper. Terminal AI Agent, Planning, Swarm, and scheduled agent prompts do not receive RAG context merely because a normal profile has stores attached. These autonomous flows require their own explicit RAG opt-in so background automation never expands its local-data scope silently. The dedicated snippet/workflow Diagram request is a second, always-on exception: it is built from the source alone and never receives knowledge-store excerpts, so diagrams show no [R1] -style source markers even when the profile has stores attached. A vision prompt — such as the session journal's AI screenshot analysis — is a third: the knowledge store holds text, and a query built from a generic image prompt would only add irrelevant context to an already large image payload, so retrieval is skipped for those requests regardless of the profile's assigned stores. Test retrieval Select a store and choose Test search . Enter a question or search phrase; the result dialog shows the bounded retrieved-context block with ranked [R1] source markers, source paths, and PDF pages where available. A test search runs retrieval only and does not send the excerpts to a chat model, which makes it useful for checking chunking and source coverage before assigning the store to a role. If results are weak: Confirm that the expected source is enabled and successfully synchronized. Use terms that appear in the documents, then compare with a natural-language question. Check the scan report for unsupported, binary, non-UTF-8, oversized, protected, or image-only files. Rebuild after changing the embedding model or dimensions; an index created by another embedding configuration is rejected. Files and backup behavior Path Purpose Included in a korTTY backup? ~/.kortty/rag/stores.json Store definitions, embedding configuration, role/autonomous assignments, source paths/filters/sync modes, document hashes, status/counts, and last-success timestamps Yes Store directory such as ~/.kortty/rag/stores/<id>/index.hnsw Regenerable vectors, chunks, and HNSW graph No Original source files and folders User-owned knowledge documents No; back them up with their normal storage workflow After restoring on another computer, restore or reconnect the original source paths, reinstall the embedding model, and run Update now to rebuild the local snapshot. Qdrant data remains the responsibility of the Qdrant deployment.","title": "RAG knowledge stores"},{"location": "features/rag.html#rag-knowledge-stores","text": "A knowledge store lets an AI profile answer with relevant excerpts from local files without sending the complete source collection to the model. The default store uses a local cosine-similarity HNSW snapshot; an existing Qdrant service is available as an optional second store type.","title": "RAG knowledge stores"},{"location": "features/rag.html#create-a-local-knowledge-store","text": "Open AI > AI Manager > Knowledge Stores and choose Create . The beginner flow needs a name, an installed embedding model, role assignments, and at least one reviewed source. Store type, vector dimensions, and optional Qdrant connection fields stay collapsed under Advanced ; korTTY manages the local HNSW graph parameters internally, so normal use requires no vector-index tuning. If no embedding model is installed, the same flow opens the local-model setup assistant with the embedding role preselected. Install the preselected Qwen3-Embedding 0.6B Q8_0 model, or pick one of the catalog alternatives the assistant offers for this computer's memory — Qwen3-Embedding 4B or 8B Q4_K_M on machines with at least 16 or 24 GiB, the multilingual BGE-M3 Q8_0 , or the very small and fast Nomic Embed Text v1.5 Q8_0 — then return to Create . An index is tied to that exact embedding model and dimension count; changing either requires rebuilding it. To add content: Choose Add files for one or more individual files, or Add folder for a recursive directory source. While korTTY scans the selection on a background worker, the Knowledge Stores tab remains responsive and reports Checking selected files and folders… . You can cancel this scan before the review dialog opens. Review the preview table. Each row shows path, format, size, status, and an explanation when skipped; the summary distinguishes unchanged files from files that will be indexed and groups recognized formats. Confirm only after the preview matches what you intended to index. Wait for text extraction, chunking, embeddings, and snapshot activation to finish. You can cancel before activation without damaging the previous snapshot. The source table shows the path, source type, synchronization mode, status, file/chunk counts, problem count, and last successful index. These values persist across restarts. Available actions include Add files , Add folder , Update now , Disable/Enable , Remove , and Test search . A folder is always reviewed first Adding a directory never means “send every file.” korTTY scans without following symbolic links, applies the central format allowlist and safe directory exclusions, validates the file contents on a background worker, and presents the accepted and skipped set before indexing begins. The source is not saved and indexing does not start unless you confirm that preview.","title": "Create a local knowledge store"},{"location": "features/rag.html#supported-formats","text": "File-name matching is case-insensitive. A recognized suffix is necessary but not sufficient: non-PDF files must contain valid UTF-8 text without binary NUL/control bytes, and PDFs must be readable and contain extractable text. Category Supported files Documents .txt , .md , .markdown , .adoc , .asciidoc , .rst , text-based .pdf Structured data .json , .jsonl , .xml , .yaml , .yml , .toml , .ini , .cfg , .conf , .properties , .csv , .tsv Shell and scripts .sh , .bash , .zsh , .fish , .bat , .cmd , .awk , .ps1 , .psm1 , .psd1 , .py , .pyw , .pyi , .rb , .pl , .php , .lua JVM and .NET .java , .kt , .kts , .groovy , .gradle , .gvy , .gy , .gsh , .scala , .sc , .cs JavaScript and web .js , .jsx , .mjs , .cjs , .ts , .tsx , .mts , .cts , .html , .htm , .css , .scss , .sass , .less , .vue , .svelte Systems and data code .c , .h , .cc , .cpp , .cxx , .hpp , .hxx , .hh , .inl , .go , .rs , .swift , .sql Known extensionless files README , LICENSE , Dockerfile , Makefile The following are not indexed in v1: Office documents, images or OCR, archives, databases, audio, video, and arbitrary binary formats. Password-protected PDFs and PDFs with no extractable text are reported as skipped. The default limit is 50 MiB per file. A source can set it from 1 MiB to 1 GiB under Advanced , but custom include globs can only narrow the central allowlist; they cannot turn an unsupported or binary format into an accepted source. One preview is also bounded to 5,000 visited files, 500 MiB of accepted source data, and 100 MiB of extracted characters.","title": "Supported formats"},{"location": "features/rag.html#safe-folder-scanning","text": "Folder sources are recursive by default. korTTY does not follow symbolic links and excludes hidden paths plus common metadata, dependency, build, cache, and IDE directories: .git .hg .svn .gradle .idea .vscode .venv venv node_modules vendor build target dist out coverage __pycache__ Optional include/exclude globs and .gitignore handling are available under Advanced . Include rules are evaluated after the fixed format allowlist. A single file already covered by a folder source is not added a second time, and identical or overlapping folder sources are rejected with the path of the existing source.","title": "Safe folder scanning"},{"location": "features/rag.html#automatic-and-manual-synchronization","text": "Each file or folder has its own synchronization mode: Mode Behavior Automatic Default. korTTY watches the file or directory while running, groups bursts of changes for three seconds, and then runs an incremental synchronization. At application start it also reconciles enabled Automatic sources independently of credential or JobScheduler startup, catching changes made while korTTY was closed. Manual The source changes only when you choose Update now . File-system watching is platform-dependent. If a source cannot be watched reliably, it remains usable and manually refreshable; use Update now whenever the manager reports limited monitoring. Synchronization hashes the complete accepted file. Update now performs the same cancellable background scan and shows the same review table before any work begins. A second scan or indexing operation is refused while one is already active, preventing overlapping preview/index jobs for the pane. Unchanged files keep their existing chunks and vectors; new or changed files are extracted, chunked, and embedded again. Deleted files remove their chunks on the next synchronization, and a rename is processed as one deletion plus one addition. Disable keeps the source configuration and indexed chunks but excludes them from retrieval. Remove asks for confirmation and deletes that source's chunks. Removing a source never deletes the original files. Deleting an entire local store also removes its confirmed index directory under ~/.kortty/rag/stores/ , but never touches the configured source documents. Deleting a Qdrant store definition first removes the vectors for its configured sources, but does not delete the external Qdrant service itself.","title": "Automatic and manual synchronization"},{"location": "features/rag.html#indexing-and-failure-safety","text": "Text is divided into deterministic chunks of approximately 800 tokens with a 120-token overlap. PDF chunks retain their page number for citations. Embeddings are requested from the selected authenticated local llama.cpp model in batches of 32. For a local store, korTTY builds a deterministic hierarchical HNSW graph in memory with cosine similarity. Exponentially distributed upper layers provide greedy entry-point navigation, while construction and bounded search use internal M , efConstruction , and efSearch defaults. korTTY writes the candidate to a temporary index.hnsw snapshot, flushes it, and then activates it with an atomic rename where supported. Cancellation, extraction errors, embedding errors, or a power loss before activation leave the previous active snapshot untouched. A legacy single-layer v1 snapshot is rebuilt and atomically promoted to the hierarchical v2 format when first opened. During indexing, the status line reports the current source state, documents, chunks, problems, and percentage. Completion summarizes newly indexed, unchanged, removed, and skipped documents; the source table retains status, file/chunk/problem counts, and completion time, while the source configuration persists the content hashes used for the next comparison. Unchanged document hashes reuse their vectors while changed, new, and deleted documents update the replacement snapshot. A configured file or folder root that has actually been deleted is treated as an incremental deletion: korTTY atomically replaces that source with no chunks, reports the number of removed documents, persists empty hashes/counts, and marks the source MISSING , preventing stale excerpts from remaining retrievable. Other fatal scan failures, such as a type mismatch, permission/extraction error, or symbolic-link root, report ERROR and preserve the previous active snapshot instead of erasing known-good vectors. Readable files can still be indexed when unrelated entries were skipped with warnings. The optional Qdrant adapter creates or validates a cosine collection, stores the same chunk metadata, and updates one source at a time through the REST API. Remote endpoints must use HTTPS; plain HTTP is accepted only for a loopback Qdrant instance. The configured endpoint and vault-protected API key belong to that Qdrant store; local HNSW requires no database service.","title": "Indexing and failure safety"},{"location": "features/rag.html#use-a-store-with-an-ai-profile","text": "When creating or configuring a knowledge store, select Text , Coding , or both roles. korTTY associates the knowledge store with the profiles currently assigned to those roles. Enable Autonomous workflows separately only when agent-style background prompts may retrieve from this knowledge store. Cloud profiles receive retrieved excerpts korTTY never sends the complete source collection, HNSW graph, or Qdrant collection to a chat model. It sends only the bounded excerpts retrieved for the current request. Those excerpts remain on this computer with an integrated llama.cpp or provably loopback HTTP profile, but they leave the computer when the selected Text/Coding profile uses a cloud endpoint. Selecting a knowledge-store role records that knowledge store on the profile currently assigned to the role and is therefore explicit permission to disclose matching excerpts to that profile. The service factory may automatically add other eligible role-assigned knowledge stores only to integrated or loopback profiles; cloud, LAN, hostname-lookalike, and CLI profiles use only their persisted explicit knowledge-store assignments. For ordinary korTTY AI actions, retrieval uses the user's prompt, or the selected terminal/snippet text when there is no separate prompt. The query is embedded with the store's model, searches only enabled sources, and applies these fixed bounds: At most six excerpts in total. At most two excerpts from one source. At most 4,000 tokens and never more than 25% of the target model's context window. The result is inserted after korTTY's action contract and AI Skills and before the model-family prompt preset. It is wrapped in <retrieved_context> and explicitly marked as untrusted data, not instructions. Source locations receive stable markers such as [R1] ; whenever an answer relies on an excerpt, the model is required to cite that exact marker. Text inside a source cannot override system rules, request tools, reveal secrets, or close the retrieved-context wrapper. Terminal AI Agent, Planning, Swarm, and scheduled agent prompts do not receive RAG context merely because a normal profile has stores attached. These autonomous flows require their own explicit RAG opt-in so background automation never expands its local-data scope silently. The dedicated snippet/workflow Diagram request is a second, always-on exception: it is built from the source alone and never receives knowledge-store excerpts, so diagrams show no [R1] -style source markers even when the profile has stores attached. A vision prompt — such as the session journal's AI screenshot analysis — is a third: the knowledge store holds text, and a query built from a generic image prompt would only add irrelevant context to an already large image payload, so retrieval is skipped for those requests regardless of the profile's assigned stores.","title": "Use a store with an AI profile"},{"location": "features/rag.html#test-retrieval","text": "Select a store and choose Test search . Enter a question or search phrase; the result dialog shows the bounded retrieved-context block with ranked [R1] source markers, source paths, and PDF pages where available. A test search runs retrieval only and does not send the excerpts to a chat model, which makes it useful for checking chunking and source coverage before assigning the store to a role. If results are weak: Confirm that the expected source is enabled and successfully synchronized. Use terms that appear in the documents, then compare with a natural-language question. Check the scan report for unsupported, binary, non-UTF-8, oversized, protected, or image-only files. Rebuild after changing the embedding model or dimensions; an index created by another embedding configuration is rejected.","title": "Test retrieval"},{"location": "features/rag.html#files-and-backup-behavior","text": "Path Purpose Included in a korTTY backup? ~/.kortty/rag/stores.json Store definitions, embedding configuration, role/autonomous assignments, source paths/filters/sync modes, document hashes, status/counts, and last-success timestamps Yes Store directory such as ~/.kortty/rag/stores/<id>/index.hnsw Regenerable vectors, chunks, and HNSW graph No Original source files and folders User-owned knowledge documents No; back them up with their normal storage workflow After restoring on another computer, restore or reconnect the original source paths, reinstall the embedding model, and run Update now to rebuild the local snapshot. Qdrant data remains the responsibility of the Qdrant deployment.","title": "Files and backup behavior"},{"location": "features/recording.html","text": "Terminal recording KorTTY records terminal sessions as lightweight replay files with optional color capture and video export. Session recordings capture screen-state changes and timing events without continuous pixel capture, making them efficient and portable. Recording format and storage Terminal recording saves session data into compressed JSONL replay files, one per terminal tab session: Replay file format : .korttyrec.jsonl.gz (JDK/GZIP streaming-compressed) Legacy format : .korttyrec.jsonl files remain readable (uncompressed) Default storage : ~/.kortty/recordings Content : Screen-state changes, timing events, and optional color runs Note Recordings do not continuously capture pixels and do not stream over the network. File size depends on activity level and whether color capture is enabled. Configuration Enable recording To make recording available after every app restart: Open Settings > Video Enable Enable terminal recording after app restart Click Save To enable recording only until KorTTY quits: Open Tools > Video Manager Select Enable terminal recording for this app session Click Save Configure storage and behavior In Tools > Video Manager , set these options: Option Description Storage path Directory where replay files are saved (default: ~/.kortty/recordings ) Default format KorTTY replay format for new recordings Default split scope Record active split only or entire tab Auto-pause on idle Pause recording when terminal is inactive Idle threshold Seconds of inactivity before pause (default: 20) Capture colors Include per-cell terminal style runs in new recordings ffmpeg path Path to local ffmpeg binary for video export Tip Enable color capture before recording if you want exported videos to reproduce terminal colors accurately. Existing recordings without color data can still export, but colors won't be preserved. ffmpeg configuration Video export requires a local ffmpeg installation: In Tools > Video Manager , enter the full path to ffmpeg (or leave blank to auto-detect) Click Check to verify availability If ffmpeg is not found, video export remains disabled but replay viewing and other features work normally On macOS and Linux, if ffmpeg is in your PATH, KorTTY auto-detects it. On Windows, you may need to provide the full path. Recording a session Start recording Open or focus an SSH terminal tab If recording is enabled, click Start recording in the terminal bar, or: Choose Tools > Start/Stop Terminal Recording , or Press Ctrl + Shift + E ( Cmd + Shift + E on macOS) If the tab contains split terminals, choose: Active split — record only the focused terminal pane Entire tab — record all visible splits The terminal bar control is hidden until recording is enabled or the menu item/shortcut is used. Stop recording Click Stop recording in the terminal bar, or Press Ctrl + Shift + E ( Cmd + Shift + E on macOS) Multiple segments Start and stop recording as often as needed within the same tab. All segments append to the same replay file until the tab closes. Each recording start/stop creates a new segment in the file. Exporting videos Exported videos require ffmpeg to be available and configured. Export options Open Tools > Video Manager Select a .korttyrec.jsonl.gz replay file Verify that ffmpeg status says video export is enabled Click Export In the export dialog: Choose Export entire recording or specify a custom time range For custom ranges, enter start/end times as MM (minutes) or MM:SS (minutes and seconds) Values past the replay duration are rejected Choose whether to Include terminal colors (available only if the replay contains color data) Select output format: WebM/VP9 or MKV/FFV1 Choose output location Export progress During export, a progress dialog shows: Current phase (frame rendering, video encoding) Progress bar Estimated remaining time Export uses the recorded terminal geometry to avoid cropping wide terminal screens. If the geometry is not available, KorTTY computes a legacy fallback size. Viewing and managing recordings View a replay Open Tools > Video Manager Select a .korttyrec.jsonl.gz replay file Click View to open the replay in the built-in viewer Use the timeline controls: Timeline slider — Drag to jump to any position Time jump — Enter 5 for minute 5, 5:30 for 5 minutes 30 seconds Speed — Set playback speed between 1x and 20x Rename a replay Select a replay in Tools > Video Manager Click Rename Enter a new file name The file is renamed in the storage folder Delete a replay Select a replay in Tools > Video Manager Click Delete Confirm the deletion Recording events and JSONL format Replay files contain timestamped events in JSONL format (one event per line, gzip-compressed). The decompressed JSONL may contain: Event Description recording_start Recording session began screen Screen-state change (may include styleRuns with foreground/background values) auto_pause Recording paused due to inactivity auto_resume Recording resumed after inactivity recording_stop Recording stopped manually session_closed Terminal tab closed Each screen event includes terminal content, dimensions, and optional color style runs. Tips and best practices Large terminal windows : Terminal geometry is recorded, so wide screens export correctly without cropping Color accuracy : Enable color capture before recording if you plan to export video with terminal colors Idle pause : Auto-pause helps reduce file size during long idle periods; enable it unless you need to preserve exact timing Split-scope selection : Choose your recording scope (active split vs. entire tab) each time you start recording to match your workflow Legacy files : Old .korttyrec.jsonl (uncompressed) replay files are still readable in the Video Manager","title": "Terminal recording"},{"location": "features/recording.html#terminal-recording","text": "KorTTY records terminal sessions as lightweight replay files with optional color capture and video export. Session recordings capture screen-state changes and timing events without continuous pixel capture, making them efficient and portable.","title": "Terminal recording"},{"location": "features/recording.html#recording-format-and-storage","text": "Terminal recording saves session data into compressed JSONL replay files, one per terminal tab session: Replay file format : .korttyrec.jsonl.gz (JDK/GZIP streaming-compressed) Legacy format : .korttyrec.jsonl files remain readable (uncompressed) Default storage : ~/.kortty/recordings Content : Screen-state changes, timing events, and optional color runs Note Recordings do not continuously capture pixels and do not stream over the network. File size depends on activity level and whether color capture is enabled.","title": "Recording format and storage"},{"location": "features/recording.html#configuration","text": "","title": "Configuration"},{"location": "features/recording.html#enable-recording","text": "To make recording available after every app restart: Open Settings > Video Enable Enable terminal recording after app restart Click Save To enable recording only until KorTTY quits: Open Tools > Video Manager Select Enable terminal recording for this app session Click Save","title": "Enable recording"},{"location": "features/recording.html#configure-storage-and-behavior","text": "In Tools > Video Manager , set these options: Option Description Storage path Directory where replay files are saved (default: ~/.kortty/recordings ) Default format KorTTY replay format for new recordings Default split scope Record active split only or entire tab Auto-pause on idle Pause recording when terminal is inactive Idle threshold Seconds of inactivity before pause (default: 20) Capture colors Include per-cell terminal style runs in new recordings ffmpeg path Path to local ffmpeg binary for video export Tip Enable color capture before recording if you want exported videos to reproduce terminal colors accurately. Existing recordings without color data can still export, but colors won't be preserved.","title": "Configure storage and behavior"},{"location": "features/recording.html#ffmpeg-configuration","text": "Video export requires a local ffmpeg installation: In Tools > Video Manager , enter the full path to ffmpeg (or leave blank to auto-detect) Click Check to verify availability If ffmpeg is not found, video export remains disabled but replay viewing and other features work normally On macOS and Linux, if ffmpeg is in your PATH, KorTTY auto-detects it. On Windows, you may need to provide the full path.","title": "ffmpeg configuration"},{"location": "features/recording.html#recording-a-session","text": "","title": "Recording a session"},{"location": "features/recording.html#start-recording","text": "Open or focus an SSH terminal tab If recording is enabled, click Start recording in the terminal bar, or: Choose Tools > Start/Stop Terminal Recording , or Press Ctrl + Shift + E ( Cmd + Shift + E on macOS) If the tab contains split terminals, choose: Active split — record only the focused terminal pane Entire tab — record all visible splits The terminal bar control is hidden until recording is enabled or the menu item/shortcut is used.","title": "Start recording"},{"location": "features/recording.html#stop-recording","text": "Click Stop recording in the terminal bar, or Press Ctrl + Shift + E ( Cmd + Shift + E on macOS)","title": "Stop recording"},{"location": "features/recording.html#multiple-segments","text": "Start and stop recording as often as needed within the same tab. All segments append to the same replay file until the tab closes. Each recording start/stop creates a new segment in the file.","title": "Multiple segments"},{"location": "features/recording.html#exporting-videos","text": "Exported videos require ffmpeg to be available and configured.","title": "Exporting videos"},{"location": "features/recording.html#export-options","text": "Open Tools > Video Manager Select a .korttyrec.jsonl.gz replay file Verify that ffmpeg status says video export is enabled Click Export In the export dialog: Choose Export entire recording or specify a custom time range For custom ranges, enter start/end times as MM (minutes) or MM:SS (minutes and seconds) Values past the replay duration are rejected Choose whether to Include terminal colors (available only if the replay contains color data) Select output format: WebM/VP9 or MKV/FFV1 Choose output location","title": "Export options"},{"location": "features/recording.html#export-progress","text": "During export, a progress dialog shows: Current phase (frame rendering, video encoding) Progress bar Estimated remaining time Export uses the recorded terminal geometry to avoid cropping wide terminal screens. If the geometry is not available, KorTTY computes a legacy fallback size.","title": "Export progress"},{"location": "features/recording.html#viewing-and-managing-recordings","text": "","title": "Viewing and managing recordings"},{"location": "features/recording.html#view-a-replay","text": "Open Tools > Video Manager Select a .korttyrec.jsonl.gz replay file Click View to open the replay in the built-in viewer Use the timeline controls: Timeline slider — Drag to jump to any position Time jump — Enter 5 for minute 5, 5:30 for 5 minutes 30 seconds Speed — Set playback speed between 1x and 20x","title": "View a replay"},{"location": "features/recording.html#rename-a-replay","text": "Select a replay in Tools > Video Manager Click Rename Enter a new file name The file is renamed in the storage folder","title": "Rename a replay"},{"location": "features/recording.html#delete-a-replay","text": "Select a replay in Tools > Video Manager Click Delete Confirm the deletion","title": "Delete a replay"},{"location": "features/recording.html#recording-events-and-jsonl-format","text": "Replay files contain timestamped events in JSONL format (one event per line, gzip-compressed). The decompressed JSONL may contain: Event Description recording_start Recording session began screen Screen-state change (may include styleRuns with foreground/background values) auto_pause Recording paused due to inactivity auto_resume Recording resumed after inactivity recording_stop Recording stopped manually session_closed Terminal tab closed Each screen event includes terminal content, dimensions, and optional color style runs.","title": "Recording events and JSONL format"},{"location": "features/recording.html#tips-and-best-practices","text": "Large terminal windows : Terminal geometry is recorded, so wide screens export correctly without cropping Color accuracy : Enable color capture before recording if you plan to export video with terminal colors Idle pause : Auto-pause helps reduce file size during long idle periods; enable it unless you need to preserve exact timing Split-scope selection : Choose your recording scope (active split vs. entire tab) each time you start recording to match your workflow Legacy files : Old .korttyrec.jsonl (uncompressed) replay files are still readable in the Video Manager","title": "Tips and best practices"},{"location": "features/security.html","text": "Security: credentials, keys & encryption KorTTY protects your sensitive data with industry-standard encryption and centralized key management. All connection passwords, SSH key passphrases, and credential storage use AES-256-GCM encryption derived from a master password that is created on first launch. Master Password On first launch, you are prompted to create a master password (minimum 6 characters) that encrypts all stored secrets. Setup Enter a password (the field border turns green when length is sufficient, red when too short). A strength indicator shows password quality; a warning appears for weak or common passwords, but you can confirm if needed. Confirm the password. Click Setup . At-Rest Encryption The master password itself is hashed with PBKDF2 (310,000 iterations) and never stored in plain text. The salt and hash are stored in ~/.kortty/master.key . On subsequent launches, KorTTY prompts you to enter the master password to unlock encrypted data. Turning off Require master password on startup in Settings > Security hides this prompt, but stored passwords will not be accessible until you enter the master password manually. Optional auto-login weakens at-rest protection The second Security option, Disable master password prompt on startup (auto-login) , also removes the prompt but keeps the vault fully usable: korTTY writes your master password to ~/.kortty/master.autounlock — obfuscated only, not encrypted , with owner-only file permissions — and unlocks automatically on every start. The obfuscation key is embedded in the application, so file permissions are the only real boundary; anyone who can read ~/.kortty or a backup can decrypt all saved secrets. On a brand-new profile the option bootstraps a default master password with no dialog at all. korTTY asks for confirmation before enabling it, it is meant for throwaway/test environments, and a policy configuration that requires a master password disables it. Details: Security settings . Note If you lose the master password, encrypted data cannot be recovered. Delete master.key and credentials.xml , restart, set a new master password, and re-enter your passwords. Encryption Model All sensitive data at rest is encrypted using AES-256-GCM : Algorithm : AES-256-GCM (Galois/Counter Mode) Key Derivation : PBKDF2WithHmacSHA256 with 310,000 iterations IV Length : 12 bytes (random per encryption) Authentication Tag : 128 bits (built-in integrity verification) Salt Length : 32 bytes (random per master password setup) Each encrypted value combines a random IV with the ciphertext, encoded as Base64 for storage. Credential Management Store centralized username/password credentials that can be reused across multiple connections. Opening the Manager Menu: Management > Manage Credentials Adding Credentials Click Add . Fill in: Name — Descriptive identifier Username — Login username Password — Stored encrypted with AES-256-GCM Environment — Production, Development, Test, or Staging Server Pattern (optional) — Glob pattern (e.g., *.example.com , 10.0.0.* ) for automatic credential matching to connections Description (optional) — Free-text notes Click OK . Using Credentials in Connections When creating or editing a connection: Go to the Connection tab. Select a stored credential from the Credentials dropdown. Username and password are filled in automatically. The following diagram shows how credentials and SSH keys flow from encrypted storage to active connections: Features Environment-specific — Organize credentials by deployment environment Server Pattern Matching — Automatically assign credentials to matching servers Encrypted Storage — Passwords are encrypted with AES-256-GCM Automatic Usage — Select credentials directly in connection settings SSH Key Management Centralized management of private SSH keys with encrypted passphrases. Opening the Manager Menu: Management > Manage SSH Keys Adding Keys Click Add . Select the path to your private SSH key file. (Optional) Enter the passphrase — it will be encrypted and stored. Click OK . Key Features Centralized Management — Manage all SSH keys in one place Encrypted Passphrases — Key passphrases are stored encrypted with AES-256-GCM Key Copying — Use Copy to User Directory to copy keys to ~/.kortty/ssh-keys/ ; copied keys are included in encrypted backups and restored with owner-only permissions Wildcard Search — Quick search for keys using * patterns Automatic Usage — Select keys directly in connection settings Using Keys in Connections When creating or editing a connection: Go to the Connection tab. Select Private Key as the authentication method. Select the desired key from the SSH Keys dropdown. The key path and passphrase are filled in automatically. Interactive SSH host-key trust Terminal and SFTP connections, including the SSH bootstrap used by Mosh, share a trust-on-first-use (TOFU) verifier keyed by normalized host name and port. On first use, korTTY displays the server key algorithm and OpenSSH SHA-256 fingerprint; verify it out of band before accepting. The confirmation defaults to No . A previously trusted matching key is accepted silently, while a changed key is hard-blocked with the expected and offered fingerprints and is never retried automatically. Interactive pins are written atomically to ~/.kortty/ssh-host-keys.properties ; a companion lock coordinates simultaneous korTTY processes. This store is distinct from the JobScheduler's connection-ID-based host-key pins in job-scheduler.xml , which protect unattended SSH, SFTP, and Rsync execution. Relaxing host-key verification For lab or throwaway hosts you can turn the first-use prompt off and have korTTY accept an unknown key silently. This is an accept-new relaxation, not blind trust: a key that differs from one already pinned for that host is still hard-blocked, so a man-in-the-middle on a host you have connected to before is still caught. It is off by default and can be set at three scopes, in precedence order: Per connection — the Host key verification control in the Connection Manager's connection editor and in Quick Connect, with three states: Use default (inherit), Verify (force strict even if the group or global setting relaxed it), and Don't verify . Per group — right-click a group in the Connection Manager and toggle Disable host key verification ; it applies to every connection in the group that inherits. Globally — Settings → Terminal → Disable host key verification for all connections , the base default for every connection that inherits at both levels above. A jump server's own host key is never relaxed by any of these — the bastion is always verified strictly. GPG Key Management Manage GPG keys for backup encryption and connection/snippet export encryption. Opening the Manager Menu: Management > Manage GPG Keys Adding Keys Manual Entry — Click Add to enter key ID and email manually. System Import — Click Import from GPG to import keys from your system's GPG keyring. Editing and Removing Keys Select a key from the list. Click Edit to modify details, or Delete to remove. Using Keys for Backup Open Settings > Backup . Select GPG Encryption as the encryption type. Choose the GPG key to use for encryption. GPG-encrypted backups and exports are stored as .gpg files and require your system's gpg command and a usable public key for decryption. Stored security data The following sensitive and security-related data is stored in ~/.kortty/ ; secret values are encrypted, while public verification material is not: File Contents Encryption credentials.xml Stored username/password credentials AES-256-GCM ssh-keys.xml SSH key paths and encrypted passphrases AES-256-GCM connections.xml Connection passwords (inline) and key passphrases (if not using SSH key management) AES-256-GCM ssh-host-keys.properties Trusted public host keys for interactive Terminal, SFTP, and Mosh bootstrap connections Public verification data; not encrypted job-scheduler.xml Scheduler sudo passwords and archive passwords; journal entries redact KorTTY-managed secrets AES-256-GCM master.key Master password hash (PBKDF2, 310,000 iterations) and salt PBKDF2 hash only master.autounlock Remembered master password for the optional auto-login Obfuscated only — not encrypted; owner-only file permissions global-settings.xml AI profile API keys, translation API keys, optional Hugging Face token AES-256-GCM Security Best Practices Warning Selected terminal text sent to AI services can contain sensitive information such as credentials, hostnames, file paths, stack traces, or operational details. For sensitive data, prefer an integrated local GGUF model, or verify that you trust the remote endpoint before sending anything. Master Password Use a strong, unique master password (at least 12 characters, mix of upper/lowercase, numbers, symbols). Never share your master password. Store it securely (password manager recommended). SSH Keys Keep private key files protected with a passphrase. Copy keys to ~/.kortty/ssh-keys/ for inclusion in encrypted backups; keys left in their original locations are only referenced and must be migrated separately. Limit key file permissions (e.g., chmod 600 ). Verify a first-use host-key fingerprint through a trusted channel before accepting it. Treat a changed-key warning as a possible server rebuild, DNS error, or man-in-the-middle attack and investigate instead of reconnecting repeatedly. JobScheduler Host-key Pinning : Host keys are pinned by default for unattended SSH/SFTP/Rsync jobs to prevent man-in-the-middle attacks. These connection-ID pins are intentionally separate from the normalized endpoint pins used by interactive Terminal/SFTP sessions. Sudo Passwords : Scheduler sudo passwords are encrypted and stored in ~/.kortty/job-scheduler.xml . Journal Redaction : Job journal entries redact KorTTY-managed secrets before persistence (redacted mode is the default; full mode stores unredacted output). Backup Encryption Always encrypt backups using either password-protected ZIP or GPG encryption. Store backup files in a secure location. Test restore procedures periodically to ensure backups are usable. AI Integration API keys for AI endpoints are encrypted with your master password. The optional Hugging Face token is encrypted with the master password and is used only for approved model search/download requests to the trusted Hugging Face host. Each integrated llama-server binds only to 127.0.0.1 on a random port and requires a generated local API key. Offline mode is mandatory; web UI, agent, UI MCP proxy, slot endpoint, and inherited server-option overrides are disabled. GGUF downloads require an immutable repository revision and exact SHA-256 metadata. Runtime indexes require an Ed25519 signature, and every runtime ZIP is checked against its signed size and SHA-256 before safe extraction. Official application builds embed only the public runtime-channel trust root; a missing or invalid key fails closed before any update request, while the signing private key remains isolated in the human-dispatched promotion workflow. Signed runtime withdrawals are durable and fail closed. A verified index adds withdrawn runtime and installation IDs to llm/runtime/revoked-v1 , marks each installed package, clears a matching active pointer, stops its sidecars, removes it from healthy rollback history, and quarantines affected model bindings. The installer and process launcher both reject those packages, including after an interrupted update. Notification-only checks enforce a withdrawal without silently installing the offered replacement; Off makes no index request and therefore learns no new withdrawal until an explicit or enabled check. A newly activated runtime is not promoted to healthy history merely because its bounded --version check passed. It remains pending until the first real GGUF-backed authenticated API start succeeds; that start failing removes the candidate and restores/rebinds the newest healthy non-revoked package when one exists. Model recommendations and automatic prompt-family detection can refresh from a separate Ed25519-signed HTTPS catalog. The last valid cache is re-verified before use, and a monotonic sequence rejects signed older replays or equal-sequence version collisions before an atomic high-water update. Without the independent catalog public key, korTTY trusts neither network nor cache data and falls back to the built-in bootstrap. Production catalog and runtime signing are restricted to their main-branch, reviewer-protected GitHub environments; application builds receive only the public trust roots. AI profile configuration is stored locally; only your reviewed terminal selection or prompt is sent to the chosen service. Embedded inference stays on this computer. Knowledge-store scanning follows a fixed text allowlist, validates content, refuses symbolic links, and presents a preview. Only bounded retrieved excerpts, not the complete knowledge store, enter the model prompt. Those excerpts stay local for integrated/loopback profiles but leave the computer when an explicitly assigned cloud profile handles the request; knowledge-store roles and persisted profile assignments are the user's disclosure authorization. Remote Qdrant knowledge stores require HTTPS; plain HTTP is accepted only for loopback, and the optional API key remains vault-protected. Internet access is disabled by default for AI profiles; enable only when needed. Snippet AI actions never use internet access, even if the profile has it enabled. The fixed snippet/workflow Diagram request never receives knowledge-store excerpts, even when the profile has stores attached — the diagram prompt is built from the source alone. Security Overview Feature Implementation Master Password Hashing PBKDF2 with 310,000 iterations Credential Encryption AES-256-GCM SSH Key Passphrases Encrypted with AES-256-GCM and master password Interactive SSH/SFTP/Mosh host keys Shared normalized host:port TOFU, first-use fingerprint confirmation (optionally relaxed to accept-new), silent exact match, hard block on change AI API Keys Encrypted with AES-256-GCM and master password Embedded llama.cpp Loopback-only random port, generated API key, offline/hardened server flags, request leases GGUF/runtime supply chain Immutable revisions, SHA-256 verification, signed runtime index, durable revocation quarantine, rollback after failed health check or first real API start Model/prompt catalog Independent Ed25519 trust root, strict schema, monotonic anti-replay sequence, reverified atomic cache, protected human promotion, bootstrap fallback RAG source ingestion Central allowlist, UTF-8/PDF content checks, no symlink traversal, reviewed preview RAG prompt context Fixed retrieval limits, stable source markers, explicitly untrusted wrapper, explicit profile-based local/cloud disclosure Optional auto-login Master password stored obfuscated in master.autounlock with owner-only permissions; off by default, confirmation required, blocked by a policy that requires a master password Backup Encryption AES-256 password-protected ZIP or GPG-encrypted (legacy ZIP-encrypted backups remain importable) JobScheduler Secrets Sudo and archive passwords encrypted; journal redaction enabled by default JobScheduler Host Keys Host-key pinning required by default for unattended SSH/SFTP/Rsync jobs Credentials Never stored in plain text Changing the Master Password To change your master password (which re-encrypts all stored secrets with a new derivation): Open Settings > Security . Click Change Master Password . Enter your current (old) master password. Enter the new master password twice. Every master-password-protected secret is automatically re-encrypted with the new key: connection and jump-server passwords, SSH-key passphrases, stored credentials, AI-profile API keys and the global AI/translation/Hugging Face keys, RAG store secrets, and JobScheduler sudo/archive passwords. The change is staged — the new password only takes over once every store has migrated, so an error part-way through leaves the old password in effect. Individual secrets that cannot be migrated are left untouched, counted in the result message, and noted in the log; re-enter those manually. Configuration Files Reference All KorTTY data is stored under ~/.kortty/ . Key security-related files: ~/.kortty/ ├── master.key # Master password hash and salt (PBKDF2) ├── master.autounlock # Optional auto-login password (obfuscated, owner-only permissions) ├── credentials.xml # Encrypted credentials (AES-256-GCM) ├── ssh-keys.xml # SSH key paths and encrypted passphrases ├── gpg-keys.xml # GPG keys for backup/export encryption ├── connections.xml # Connection passwords and key passphrases ├── ssh-host-keys.properties # Interactive Terminal/SFTP/Mosh host-key pins ├── global-settings.xml # AI API keys and other encrypted settings ├── llm/models.xml # Model paths and typed launch settings (no model contents) ├── llm/runtime/ # Regenerable native packages; excluded from backup ├── llm/catalog/ # Regenerable signed-catalog cache; excluded from backup ├── llm/run/ # Temporary per-process API keys/logs; excluded from backup ├── rag/stores.json # Knowledge-store/source configuration ├── rag/stores/ # Regenerable HNSW snapshots; excluded from backup ├── job-scheduler.xml # JobScheduler sudo/archive passwords (encrypted) ├── kortty.log # Application log └── history/ # Compressed terminal session history","title": "Security — credentials, keys & encryption"},{"location": "features/security.html#security-credentials-keys-encryption","text": "KorTTY protects your sensitive data with industry-standard encryption and centralized key management. All connection passwords, SSH key passphrases, and credential storage use AES-256-GCM encryption derived from a master password that is created on first launch.","title": "Security: credentials, keys &amp; encryption"},{"location": "features/security.html#master-password","text": "On first launch, you are prompted to create a master password (minimum 6 characters) that encrypts all stored secrets.","title": "Master Password"},{"location": "features/security.html#setup","text": "Enter a password (the field border turns green when length is sufficient, red when too short). A strength indicator shows password quality; a warning appears for weak or common passwords, but you can confirm if needed. Confirm the password. Click Setup .","title": "Setup"},{"location": "features/security.html#at-rest-encryption","text": "The master password itself is hashed with PBKDF2 (310,000 iterations) and never stored in plain text. The salt and hash are stored in ~/.kortty/master.key . On subsequent launches, KorTTY prompts you to enter the master password to unlock encrypted data. Turning off Require master password on startup in Settings > Security hides this prompt, but stored passwords will not be accessible until you enter the master password manually. Optional auto-login weakens at-rest protection The second Security option, Disable master password prompt on startup (auto-login) , also removes the prompt but keeps the vault fully usable: korTTY writes your master password to ~/.kortty/master.autounlock — obfuscated only, not encrypted , with owner-only file permissions — and unlocks automatically on every start. The obfuscation key is embedded in the application, so file permissions are the only real boundary; anyone who can read ~/.kortty or a backup can decrypt all saved secrets. On a brand-new profile the option bootstraps a default master password with no dialog at all. korTTY asks for confirmation before enabling it, it is meant for throwaway/test environments, and a policy configuration that requires a master password disables it. Details: Security settings . Note If you lose the master password, encrypted data cannot be recovered. Delete master.key and credentials.xml , restart, set a new master password, and re-enter your passwords.","title": "At-Rest Encryption"},{"location": "features/security.html#encryption-model","text": "All sensitive data at rest is encrypted using AES-256-GCM : Algorithm : AES-256-GCM (Galois/Counter Mode) Key Derivation : PBKDF2WithHmacSHA256 with 310,000 iterations IV Length : 12 bytes (random per encryption) Authentication Tag : 128 bits (built-in integrity verification) Salt Length : 32 bytes (random per master password setup) Each encrypted value combines a random IV with the ciphertext, encoded as Base64 for storage.","title": "Encryption Model"},{"location": "features/security.html#credential-management","text": "Store centralized username/password credentials that can be reused across multiple connections.","title": "Credential Management"},{"location": "features/security.html#opening-the-manager","text": "Menu: Management > Manage Credentials","title": "Opening the Manager"},{"location": "features/security.html#adding-credentials","text": "Click Add . Fill in: Name — Descriptive identifier Username — Login username Password — Stored encrypted with AES-256-GCM Environment — Production, Development, Test, or Staging Server Pattern (optional) — Glob pattern (e.g., *.example.com , 10.0.0.* ) for automatic credential matching to connections Description (optional) — Free-text notes Click OK .","title": "Adding Credentials"},{"location": "features/security.html#using-credentials-in-connections","text": "When creating or editing a connection: Go to the Connection tab. Select a stored credential from the Credentials dropdown. Username and password are filled in automatically. The following diagram shows how credentials and SSH keys flow from encrypted storage to active connections:","title": "Using Credentials in Connections"},{"location": "features/security.html#features","text": "Environment-specific — Organize credentials by deployment environment Server Pattern Matching — Automatically assign credentials to matching servers Encrypted Storage — Passwords are encrypted with AES-256-GCM Automatic Usage — Select credentials directly in connection settings","title": "Features"},{"location": "features/security.html#ssh-key-management","text": "Centralized management of private SSH keys with encrypted passphrases.","title": "SSH Key Management"},{"location": "features/security.html#opening-the-manager_1","text": "Menu: Management > Manage SSH Keys","title": "Opening the Manager"},{"location": "features/security.html#adding-keys","text": "Click Add . Select the path to your private SSH key file. (Optional) Enter the passphrase — it will be encrypted and stored. Click OK .","title": "Adding Keys"},{"location": "features/security.html#key-features","text": "Centralized Management — Manage all SSH keys in one place Encrypted Passphrases — Key passphrases are stored encrypted with AES-256-GCM Key Copying — Use Copy to User Directory to copy keys to ~/.kortty/ssh-keys/ ; copied keys are included in encrypted backups and restored with owner-only permissions Wildcard Search — Quick search for keys using * patterns Automatic Usage — Select keys directly in connection settings","title": "Key Features"},{"location": "features/security.html#using-keys-in-connections","text": "When creating or editing a connection: Go to the Connection tab. Select Private Key as the authentication method. Select the desired key from the SSH Keys dropdown. The key path and passphrase are filled in automatically.","title": "Using Keys in Connections"},{"location": "features/security.html#interactive-ssh-host-key-trust","text": "Terminal and SFTP connections, including the SSH bootstrap used by Mosh, share a trust-on-first-use (TOFU) verifier keyed by normalized host name and port. On first use, korTTY displays the server key algorithm and OpenSSH SHA-256 fingerprint; verify it out of band before accepting. The confirmation defaults to No . A previously trusted matching key is accepted silently, while a changed key is hard-blocked with the expected and offered fingerprints and is never retried automatically. Interactive pins are written atomically to ~/.kortty/ssh-host-keys.properties ; a companion lock coordinates simultaneous korTTY processes. This store is distinct from the JobScheduler's connection-ID-based host-key pins in job-scheduler.xml , which protect unattended SSH, SFTP, and Rsync execution.","title": "Interactive SSH host-key trust"},{"location": "features/security.html#relaxing-host-key-verification","text": "For lab or throwaway hosts you can turn the first-use prompt off and have korTTY accept an unknown key silently. This is an accept-new relaxation, not blind trust: a key that differs from one already pinned for that host is still hard-blocked, so a man-in-the-middle on a host you have connected to before is still caught. It is off by default and can be set at three scopes, in precedence order: Per connection — the Host key verification control in the Connection Manager's connection editor and in Quick Connect, with three states: Use default (inherit), Verify (force strict even if the group or global setting relaxed it), and Don't verify . Per group — right-click a group in the Connection Manager and toggle Disable host key verification ; it applies to every connection in the group that inherits. Globally — Settings → Terminal → Disable host key verification for all connections , the base default for every connection that inherits at both levels above. A jump server's own host key is never relaxed by any of these — the bastion is always verified strictly.","title": "Relaxing host-key verification"},{"location": "features/security.html#gpg-key-management","text": "Manage GPG keys for backup encryption and connection/snippet export encryption.","title": "GPG Key Management"},{"location": "features/security.html#opening-the-manager_2","text": "Menu: Management > Manage GPG Keys","title": "Opening the Manager"},{"location": "features/security.html#adding-keys_1","text": "Manual Entry — Click Add to enter key ID and email manually. System Import — Click Import from GPG to import keys from your system's GPG keyring.","title": "Adding Keys"},{"location": "features/security.html#editing-and-removing-keys","text": "Select a key from the list. Click Edit to modify details, or Delete to remove.","title": "Editing and Removing Keys"},{"location": "features/security.html#using-keys-for-backup","text": "Open Settings > Backup . Select GPG Encryption as the encryption type. Choose the GPG key to use for encryption. GPG-encrypted backups and exports are stored as .gpg files and require your system's gpg command and a usable public key for decryption.","title": "Using Keys for Backup"},{"location": "features/security.html#stored-security-data","text": "The following sensitive and security-related data is stored in ~/.kortty/ ; secret values are encrypted, while public verification material is not: File Contents Encryption credentials.xml Stored username/password credentials AES-256-GCM ssh-keys.xml SSH key paths and encrypted passphrases AES-256-GCM connections.xml Connection passwords (inline) and key passphrases (if not using SSH key management) AES-256-GCM ssh-host-keys.properties Trusted public host keys for interactive Terminal, SFTP, and Mosh bootstrap connections Public verification data; not encrypted job-scheduler.xml Scheduler sudo passwords and archive passwords; journal entries redact KorTTY-managed secrets AES-256-GCM master.key Master password hash (PBKDF2, 310,000 iterations) and salt PBKDF2 hash only master.autounlock Remembered master password for the optional auto-login Obfuscated only — not encrypted; owner-only file permissions global-settings.xml AI profile API keys, translation API keys, optional Hugging Face token AES-256-GCM","title": "Stored security data"},{"location": "features/security.html#security-best-practices","text": "Warning Selected terminal text sent to AI services can contain sensitive information such as credentials, hostnames, file paths, stack traces, or operational details. For sensitive data, prefer an integrated local GGUF model, or verify that you trust the remote endpoint before sending anything.","title": "Security Best Practices"},{"location": "features/security.html#master-password_1","text": "Use a strong, unique master password (at least 12 characters, mix of upper/lowercase, numbers, symbols). Never share your master password. Store it securely (password manager recommended).","title": "Master Password"},{"location": "features/security.html#ssh-keys","text": "Keep private key files protected with a passphrase. Copy keys to ~/.kortty/ssh-keys/ for inclusion in encrypted backups; keys left in their original locations are only referenced and must be migrated separately. Limit key file permissions (e.g., chmod 600 ). Verify a first-use host-key fingerprint through a trusted channel before accepting it. Treat a changed-key warning as a possible server rebuild, DNS error, or man-in-the-middle attack and investigate instead of reconnecting repeatedly.","title": "SSH Keys"},{"location": "features/security.html#jobscheduler","text": "Host-key Pinning : Host keys are pinned by default for unattended SSH/SFTP/Rsync jobs to prevent man-in-the-middle attacks. These connection-ID pins are intentionally separate from the normalized endpoint pins used by interactive Terminal/SFTP sessions. Sudo Passwords : Scheduler sudo passwords are encrypted and stored in ~/.kortty/job-scheduler.xml . Journal Redaction : Job journal entries redact KorTTY-managed secrets before persistence (redacted mode is the default; full mode stores unredacted output).","title": "JobScheduler"},{"location": "features/security.html#backup-encryption","text": "Always encrypt backups using either password-protected ZIP or GPG encryption. Store backup files in a secure location. Test restore procedures periodically to ensure backups are usable.","title": "Backup Encryption"},{"location": "features/security.html#ai-integration","text": "API keys for AI endpoints are encrypted with your master password. The optional Hugging Face token is encrypted with the master password and is used only for approved model search/download requests to the trusted Hugging Face host. Each integrated llama-server binds only to 127.0.0.1 on a random port and requires a generated local API key. Offline mode is mandatory; web UI, agent, UI MCP proxy, slot endpoint, and inherited server-option overrides are disabled. GGUF downloads require an immutable repository revision and exact SHA-256 metadata. Runtime indexes require an Ed25519 signature, and every runtime ZIP is checked against its signed size and SHA-256 before safe extraction. Official application builds embed only the public runtime-channel trust root; a missing or invalid key fails closed before any update request, while the signing private key remains isolated in the human-dispatched promotion workflow. Signed runtime withdrawals are durable and fail closed. A verified index adds withdrawn runtime and installation IDs to llm/runtime/revoked-v1 , marks each installed package, clears a matching active pointer, stops its sidecars, removes it from healthy rollback history, and quarantines affected model bindings. The installer and process launcher both reject those packages, including after an interrupted update. Notification-only checks enforce a withdrawal without silently installing the offered replacement; Off makes no index request and therefore learns no new withdrawal until an explicit or enabled check. A newly activated runtime is not promoted to healthy history merely because its bounded --version check passed. It remains pending until the first real GGUF-backed authenticated API start succeeds; that start failing removes the candidate and restores/rebinds the newest healthy non-revoked package when one exists. Model recommendations and automatic prompt-family detection can refresh from a separate Ed25519-signed HTTPS catalog. The last valid cache is re-verified before use, and a monotonic sequence rejects signed older replays or equal-sequence version collisions before an atomic high-water update. Without the independent catalog public key, korTTY trusts neither network nor cache data and falls back to the built-in bootstrap. Production catalog and runtime signing are restricted to their main-branch, reviewer-protected GitHub environments; application builds receive only the public trust roots. AI profile configuration is stored locally; only your reviewed terminal selection or prompt is sent to the chosen service. Embedded inference stays on this computer. Knowledge-store scanning follows a fixed text allowlist, validates content, refuses symbolic links, and presents a preview. Only bounded retrieved excerpts, not the complete knowledge store, enter the model prompt. Those excerpts stay local for integrated/loopback profiles but leave the computer when an explicitly assigned cloud profile handles the request; knowledge-store roles and persisted profile assignments are the user's disclosure authorization. Remote Qdrant knowledge stores require HTTPS; plain HTTP is accepted only for loopback, and the optional API key remains vault-protected. Internet access is disabled by default for AI profiles; enable only when needed. Snippet AI actions never use internet access, even if the profile has it enabled. The fixed snippet/workflow Diagram request never receives knowledge-store excerpts, even when the profile has stores attached — the diagram prompt is built from the source alone.","title": "AI Integration"},{"location": "features/security.html#security-overview","text": "Feature Implementation Master Password Hashing PBKDF2 with 310,000 iterations Credential Encryption AES-256-GCM SSH Key Passphrases Encrypted with AES-256-GCM and master password Interactive SSH/SFTP/Mosh host keys Shared normalized host:port TOFU, first-use fingerprint confirmation (optionally relaxed to accept-new), silent exact match, hard block on change AI API Keys Encrypted with AES-256-GCM and master password Embedded llama.cpp Loopback-only random port, generated API key, offline/hardened server flags, request leases GGUF/runtime supply chain Immutable revisions, SHA-256 verification, signed runtime index, durable revocation quarantine, rollback after failed health check or first real API start Model/prompt catalog Independent Ed25519 trust root, strict schema, monotonic anti-replay sequence, reverified atomic cache, protected human promotion, bootstrap fallback RAG source ingestion Central allowlist, UTF-8/PDF content checks, no symlink traversal, reviewed preview RAG prompt context Fixed retrieval limits, stable source markers, explicitly untrusted wrapper, explicit profile-based local/cloud disclosure Optional auto-login Master password stored obfuscated in master.autounlock with owner-only permissions; off by default, confirmation required, blocked by a policy that requires a master password Backup Encryption AES-256 password-protected ZIP or GPG-encrypted (legacy ZIP-encrypted backups remain importable) JobScheduler Secrets Sudo and archive passwords encrypted; journal redaction enabled by default JobScheduler Host Keys Host-key pinning required by default for unattended SSH/SFTP/Rsync jobs Credentials Never stored in plain text","title": "Security Overview"},{"location": "features/security.html#changing-the-master-password","text": "To change your master password (which re-encrypts all stored secrets with a new derivation): Open Settings > Security . Click Change Master Password . Enter your current (old) master password. Enter the new master password twice. Every master-password-protected secret is automatically re-encrypted with the new key: connection and jump-server passwords, SSH-key passphrases, stored credentials, AI-profile API keys and the global AI/translation/Hugging Face keys, RAG store secrets, and JobScheduler sudo/archive passwords. The change is staged — the new password only takes over once every store has migrated, so an error part-way through leaves the old password in effect. Individual secrets that cannot be migrated are left untouched, counted in the result message, and noted in the log; re-enter those manually.","title": "Changing the Master Password"},{"location": "features/security.html#configuration-files-reference","text": "All KorTTY data is stored under ~/.kortty/ . Key security-related files: ~/.kortty/ ├── master.key # Master password hash and salt (PBKDF2) ├── master.autounlock # Optional auto-login password (obfuscated, owner-only permissions) ├── credentials.xml # Encrypted credentials (AES-256-GCM) ├── ssh-keys.xml # SSH key paths and encrypted passphrases ├── gpg-keys.xml # GPG keys for backup/export encryption ├── connections.xml # Connection passwords and key passphrases ├── ssh-host-keys.properties # Interactive Terminal/SFTP/Mosh host-key pins ├── global-settings.xml # AI API keys and other encrypted settings ├── llm/models.xml # Model paths and typed launch settings (no model contents) ├── llm/runtime/ # Regenerable native packages; excluded from backup ├── llm/catalog/ # Regenerable signed-catalog cache; excluded from backup ├── llm/run/ # Temporary per-process API keys/logs; excluded from backup ├── rag/stores.json # Knowledge-store/source configuration ├── rag/stores/ # Regenerable HNSW snapshots; excluded from backup ├── job-scheduler.xml # JobScheduler sudo/archive passwords (encrypted) ├── kortty.log # Application log └── history/ # Compressed terminal session history","title": "Configuration Files Reference"},{"location": "features/session-journal.html","text": "Session journal The session journal documents a terminal session as a readable timeline: every server output line and every command you type is written to a capture log, an AI periodically condenses the activity into short journal entries, and the result is rendered as a self-contained HTML page with connection details, color-coded excerpts, screenshots and your own notes. Journals are managed like saved chats — in their own manager window with search, editing and export. Storage and formats Each journal is one self-contained directory under ~/.kortty/journals (configurable in Settings > Logging > Session Journal ): File Purpose journal.xml The curated journal document: metadata, AI summaries, markers, notes, screenshot references session-log.json / .xml / .yaml The append-only capture log — timestamped server output and typed input lines with sequence ids session-log-2.json.zst , … Rotated log parts; closed parts are zstd-compressed automatically, the journal never deletes history. Journals recorded by older versions keep their .gz parts and remain fully readable journal.html The generated timeline page, regenerated automatically after every change screenshots/*.png Screenshots you attached during the session The capture-log format is selectable in the journal manager's Options dialog: JSON (JSON Lines, the default), XML or YAML . All formats carry the same fields, and every entry is exactly one line, so a crash never corrupts more than the last line. JSON is the default because log tooling reads it without needing a parser of its own — not because it saves space. Size barely separates the three: for ordinary output XML is about 9 bytes per entry smaller, for output full of < , > and & JSON is roughly 10 % smaller (XML has to escape those, JSON does not), and once a finished part is compressed all three land within 2 % of each other. YAML is the largest, since it writes JSON mappings with a - prefix. The active log part stays uncompressed for live reads; rotation (default 25 MB per part) and session end compress finished parts to .zst (zstd — journals from older versions keep their .gz parts and open exactly as before). Two more things keep long, noisy sessions small and complete. Consecutive identical output lines (progress loops, tail -f repeats) are coalesced syslog-style: the first occurrence is written immediately, follow-ups are counted and stored as one entry with a repeat count. The viewer shows such a run compactly as line ×12 , while copying or exporting the log reproduces the original lines in full. And when server output arrives faster than the log can persist it, the capture applies backpressure instead of dropping lines — the terminal may briefly slow down under an extreme flood, but the journal stays complete. Rotation is configurable per connection on the Journal tab: Maximum size per log part (MB) (default 25) and Maximum rotated log parts (default 20). After the configured number of parts, output capture stops with a note in the journal; input, screenshots and notes continue. An enterprise policy can cap the part count via max-log-parts . Enabling the journal Automatically for a connection Open Connections > Manage Connections and edit a connection On the Journal tab, enable Enable session journal for this connection Optionally adjust Capture typed input lines , Generate AI summaries , the per-connection Summary interval , and the rotation pair Maximum size per log part (MB) and Maximum rotated log parts Every future connection of this server then starts its journal automatically. The journal survives reconnects — one journal per tab lifetime, with a reconnect marker in the log. Retroactively for a running session Use Tools > Start/Stop Session Journal ( Ctrl + Alt + T ), the tab context menu ( Session Journal > Start journal ) or the journal bar's Start journal button. The existing scrollback is imported into the journal as seed entries first, so the timeline covers what already happened, then live capture attaches. The journal bar While a journal is available, a bar below the terminal shows its state ( Journal active since HH:MM ) and offers Stop journal , Screenshot and Note : Screenshot ( Ctrl + Alt + C , also in the terminal's right-click menu) snapshots the terminal — in a split layout, the right-click menu captures exactly the pane under the cursor — and files it into the journal timeline. Note opens the note editor for a free-text remark that appears as its own timeline entry at the current position. Writing notes Notes are written in the same editor everywhere they are edited — the journal bar's Note button, the live panel's, the entry form in the viewer , and the screenshot editor : The field holds at least six lines and the dialog can be resized, so a note can be a paragraph instead of a single line. Links are clickable. Any http:// or https:// address in a note becomes a link on the journal page — click it and the address opens in your system browser, never inside the journal view. Only those two schemes ever become links, and only in text you wrote yourself: AI summaries and terminal excerpts stay literal. Translate hands the note to the AI and replaces it with the translation in the language picked next to the button. The list offers the interface languages and any language korTTY has translations for, and it accepts a typed language it does not list. Your choice is remembered for the next note. The translation runs on the profile assigned to Text and translation profile in the AI manager — the same role the snippet editor's translation uses — falling back to your default profile when that role is unset. Internet access stays disabled for it, exactly as for the summaries; Cmd + Z / Ctrl + Z brings the original back. Without a usable AI profile the button is disabled and says so. When the connection ends A journal is bound to its tab, not to a single connection. When the connection ends while the journal is running — a reboot , a dropped network, or the server closing the session — the tab stays open and a red decision bar appears instead of the tab closing silently: Reconnect re-establishes the connection and the journal simply continues, with a reconnect marker in the capture log. Work that resumes after a server reboot lands in the same journal. End journal stops the journal and writes its closing summary (and, if enabled, the AI title) exactly like the journal bar's stop button. The tab then shows the plain reconnect bar, so reconnecting is still possible — with journaling enabled for the connection, that new session starts a fresh journal. Closing the tab instead also ends the journal with its closing summary. Without a running journal the behavior is unchanged: a cleanly ended connection closes the tab, an error keeps it open with the reconnect bar (double-click to reconnect). The live journal panel View > Live Journal (or Ctrl + Alt + L ) docks the running journal's full journal page — the same page the viewer shows — to the left or right of the terminal, kept up to date in real time. Selecting the checked side in the menu hides the panel again; the divider next to it adjusts the width, and side and width are remembered across restarts. Two things update live while the session runs: The live log — the Live Log button in the panel header opens the page's log panel in follow mode, streaming the capture log as it is written: command output, the commands you typed, notes and screenshot markers, each with a timestamp. It starts hidden; lines accumulate either way, so opening it later shows everything. Scrolling up pauses following, scrolling back to the bottom resumes it, the ✕ in its corner hides it again (the button stays in sync), and dragging its top edge adjusts the height — which is remembered. The view keeps the newest 5000 lines; everything stays in the capture log and in the entry cards' log excerpts. The timeline — new cards (AI summaries, notes, screenshots) and edits appear moments after they happen, without losing your scroll position. A terminal AI-agent run adds its own AI agent card the moment it finishes: your prompt as the title, the agent's final answer as the body, and a meta line with the model, the run duration and the reported token count. Long answers collapse to a preview — click the text (or Show full answer ) to expand. Agent work is part of the journal record even though the summarizer treats the agent's inline terminal text as noise. Because it is the real journal page, everything the viewer's page offers works right here: click an entry card to see its log excerpt, search the journal, jump between marked entries, and right-click a screenshot to open the annotation editor (pen, box, unreadable, text and a note) or copy it — the edited picture appears in the panel as soon as you save. Right-clicking an entry offers the same marker picker as the viewer. The cards adapt to the panel width, so the text stays readable however narrow or wide you drag it. Jumping to a time The ◷ button in the page header opens a time field: type a time and the timeline scrolls to the entry closest to it and highlights it briefly. The input is lenient — 19:00 , 19.00 , 1900 and 19 all mean the same, and a date can be prefixed ( 13.08. 19:00 , 13.08.2026 19:00 or 2026-08-13 19:00 ). Without a date the time is matched against each entry's own day, so a session running past midnight jumps to the nearest occurrence rather than always the first day. The panel's header adds the instant controls: Note and Screenshot act on the shown journal exactly like the journal bar — a note you add appears in both the timeline and the live log — Live Log shows or hides the log view, and Open Viewer opens the full viewer window for editing, search & replace and exports. The ⋯ menu switches the page between light and dark, refreshes it, and opens the page appearance settings. The panel follows your tabs with a memory: it shows the journal of the current tab, and when you switch tabs it only switches along if the newly selected tab also has a running journal — otherwise it keeps showing the journal it already displays. When the shown journal is stopped or its tab is closed, the page stays visible with a Journal stopped / Tab closed badge until you select another tab with a live journal. Everything shown has already passed password protection — suppressed input and redacted secrets never reach the panel. AI summaries While the journal runs, the AI summarizer periodically reads the newest capture-log lines and appends a compact journal entry (title, summary and a suggested marker: info, important or error). Defaults and limits: Option Where Default Summary interval Settings > Logging > Session Journal (global) or per connection 5 minutes Max. terminal lines per AI evaluation Journal manager Options 100 Token budget for context fill Journal manager Options , visible when max lines is 0 130000 Split backlog into multiple prompts (chunking) Journal manager Options off AI profile for summaries Journal manager Options or Settings > Logging > Session Journal Default profile Analyze screenshots with AI (description and tags) Journal manager Options on Semantic journal search (embeddings) Journal manager Options off Summaries use your default AI profile unless you pick a dedicated journal profile. That choice is available in three equivalent places: the journal manager's Options dialog, Settings > Logging > Session Journal , and AI > AI Manager > Local AI next to the Text and Coding roles. The Text/Coding role profiles themselves are deliberately not used for the journal. Setting max lines to 0 switches to context filling: the summarizer packs as many of the newest lines as fit into the configured token budget. With chunking enabled the whole backlog is processed instead of only the newest window — consecutive windows of the configured size, one AI prompt each. Warning Chunking can take very long on large sessions and is not recommended for everyday use — it is intended for power users with capable hardware and a powerful LLM. When the session ends, the summarizer writes a closing session summary entry (what was accomplished, which errors occurred) and extracts up to twelve verbatim keywords — hostnames, script and file names, error classes — into the journal metadata, where the manager's filter, keyword chips and AI search pick them up. Optionally — Let the AI title the journal when the session ends in the Options dialog — a final AI call names the journal, unless you renamed it manually. Note The journal works without AI: if no AI profile is available, AI features are disabled, or summaries are switched off, the timeline records raw activity entries instead. AI summary prompts never use internet-access tools; the terminal excerpt goes only to the configured AI profile. AI screenshot analysis When the journal's AI profile accepts images, screenshots are analyzed as well: the model writes a short description (one to three sentences) and a handful of lowercase tags , shown to the right of the thumbnail on the journal page. Tags are clickable chips — clicking one starts a journal search for that tag — and both texts are found by the manager's Search contents scan, rewritten by search and replace and the automatic redaction rules, and included in the Markdown and PDF exports. Like the summaries, the analysis answers in the language the journal was created under. Analyze screenshots with AI (description and tags) in the journal manager's Options dialog controls the automatic analysis on capture (on by default). It runs only when AI summaries are enabled for the connection and the profile can take images; otherwise the screenshot is simply filed unanalyzed. Analyze screenshot with AI in the right-click menu of a screenshot analyzes one picture on demand — the way to analyze screenshots in journals recorded earlier, to retry a failed run, or to re-run the analysis after making something unreadable in the annotation editor . The analysis always reads the annotated picture, never the untouched .orig.png capture. Whether a profile can take images is a per-profile property: Image input (vision) in the AI settings defaults to Auto — for a local LM Studio endpoint korTTY reads the answer from the model metadata (during the same refresh that discovers the reasoning options), for cloud endpoints it recognizes the common vision-capable model names — and can be overridden with Enabled / Disabled for models the detection misjudges. Warning The automatic analysis sends the screenshot at capture time — before you had a chance to make anything unreadable. The picture goes only to the configured AI profile and never through internet-access tools, but for sessions whose screen content must not leave the machine, switch the option off or have your administrator forbid the analysis via enterprise policy — the policy mandate also disables the manual run. Asking the AI about a journal AI Q&A in the viewer's toolbar opens a chat panel next to the journal page. Ask anything about this session — \"Were screenshots taken that show errors from result_complex.pl?\" — and the AI answers from what the journal already collected while the session ran: the AI summaries, screenshot descriptions and tags, and your notes. The raw capture log is never sent to the model. When a question needs hard evidence from the log — exact error lines, whether a script really failed, how often something occurred — the model names a few literal search strings, korTTY's own streaming search scans the capture log for them, and only the match counts plus a handful of sample lines go back to the model for the final answer. The panel shows both next to the answer: Sources — the journal entries the answer cites; clicking one scrolls the timeline to that entry. Log evidence — per search string the exact number of matching log lines, with clickable samples that open the log panel scrolled to the very line. Follow-up questions continue the conversation (the panel keeps the recent exchanges as context); New conversation starts over. Save as note appends a question-and-answer pair to the journal timeline as an entry, so a finding becomes part of the record. Note If no AI profile is reachable or the request fails, the panel degrades instead of erroring out: it extracts the identifiers from your question, runs the internal text search, and shows the matching entries and log lines with a notice that no model was involved. The Q&A never uses internet-access tools, and administrators can forbid it entirely ( ai-ask under enterprise policy ). Password protection Typed input is captured only as complete submitted lines, and several layers keep passwords out of the journal: When the server output ends in a password prompt ( password: , [sudo] password for … , passphrase , PIN , and localized variants), the next submitted input line is suppressed and logged as a redacted placeholder — the typed text is never buffered or written. The connection's own stored password is additionally replaced by *** wherever it would appear in captured text. Capture typed input lines can be disabled per connection entirely; commands then only appear as the server's echo in the output stream. Warning The prompt detection is a heuristic — a remote terminal cannot reliably know when the server disabled echo. Exotic or full-screen password prompts may not be recognized, and secrets pasted into visible commands (other than the connection's own credentials) are captured like any other text. Treat journals of sensitive sessions accordingly. If something slipped through anyway, the viewer's search and replace removes it from the entries and the capture log after the fact. Administrators can also have korTTY redact patterns automatically — see Enterprise policy below. The journal page journal.html is fully self-contained (no external resources) and works in the built-in viewer, in any browser, and inside the exported bundle: A sticky header shows who was connected to which server, start time, duration, and counts for entries, commands, errors and screenshots, plus the journal description. Live journals show a live badge. The line below the title only carries what the title does not already say, so a journal named after its endpoint states the connection once instead of three times. The timeline groups entries by day; each entry carries its time, a marker dot and badge in the colour you gave that marker, the AI title and summary, and color-coded input (green) and output (blue) excerpts. Clicking an entry slides a log panel in from the bottom with the exact capture-log range behind that entry. The panel has its own scrollbar, a search field with a match counter and ▲/▼ navigation ( Enter / Shift + Enter also cycle matches, Esc closes), and colors input and output lines differently. Screenshot entries show thumbnails; clicking one opens a full-size lightbox. If the AI screenshot analysis ran, its description and tag chips sit to the right of the picture, and a chip click searches the journal for that tag. The page renders dark by default, follows the system light/dark preference and has its own theme toggle. Screenshots, excerpt panels, the timeline column and the log panel size themselves to the window, so the page stays readable in a narrow viewer tab as well as full screen. Long excerpts scroll inside their own box instead of stretching the timeline. Searching the journal The magnifier in the header opens a search bar directly under the connection details ( Ctrl + F works too). Typing a term or a whole sentence highlights every occurrence across the timeline — entry titles, AI summaries, AI screenshot descriptions and tags, input and output excerpts, notes and timestamps — and shows a match counter; ▲ and ▼ or Enter / Shift + Enter jump between the hits, Esc or ✕ closes the bar and clears the highlighting. Note This searches the journal entries. The raw capture log has its own search inside the log panel, the journal manager can search across all journals with Search contents or the AI search , and the viewer's AI Q&A answers questions about this journal. Jumping between marked entries When at least one entry carries a marker, the header gains a ◆ button that opens a marker bar. Pick All markers or a single one, then step through the matches with ▲ and ▼ — the list wraps around, and the current entry is scrolled into view and briefly outlined. Alt + Down and Alt + Up do the same without the mouse, and Alt + M toggles the bar; Esc closes it. A journal without markers ships neither the button nor the bar, so the header stays as it was. Picking a time range with the mouse Inside korTTY the header also carries a ⇥ button that switches the timeline into range mode. Click the first entry, then the last one — everything between them is highlighted and the bar shows the span and how many entries it covers. The order does not matter: clicking the later entry first works just as well. Add another window puts the current selection aside and starts a new one, so several windows can be collected in one pass. Use for export opens the export dialog with those windows already filled in. Cancel or Esc leaves range mode. While range mode is on, clicking an entry selects instead of opening the log panel. The button is absent in an external browser, because exporting needs the app. In the edit mode's entry table the same thing is available without the timeline: select several rows, right-click and choose Use selection as time window . Copying content Every entry carries a copy button in its top-right corner: text entries copy the whole entry, screenshot entries copy the image. The log panel has the same button for the log section it currently shows. Right-clicking the page opens a copy menu with more targeted actions, depending on what you clicked: Action Copies Copy selection The currently selected text (shown whenever a selection exists) Copy summary The entry's time, title and AI summary Copy entry The same plus the input/output excerpts and your note Copy screenshot The screenshot itself, as an image, onto the clipboard Copy screenshot path The screenshot's path inside the journal folder Copy log section Every line of the log range currently shown in the panel, with timestamps Inside korTTY the copy actions use the app's clipboard, so images land on the system clipboard ready to paste. In an external browser text still copies normally; copying an image may fall back to its path, because browsers block reading local image data from a file:// page. Appearance The A− , A and A+ buttons in the page header scale the whole page between 70 % and 250 %. korTTY remembers the size and applies it to every journal page it renders afterwards, so a page that regenerates (a new AI summary, an edited marker) comes back at the size you chose. Opened standalone in a browser the page remembers the size per browser instead. The viewer's Appearance button opens a small panel with the rest: Setting Effect Colour scheme Automatic keeps the page's own dark/light pair and follows the operating system. Follow the terminal theme derives the page colours from your terminal's background and foreground. The remaining entries (Paper, Midnight, Ocean, Forest, Retrowave, High contrast) are fixed palettes. Text font The font for headings, summaries and notes. (default) restores the page's own stack. Monospace font The font for the input/output excerpts and the log panel. Text size The same 70–250 % as the A−/A/A+ buttons. Changes preview immediately in the viewer and are saved for every journal page. With a fixed scheme the page's ◐ toggle stays visible but is disabled, since the scheme already decides the colours. Managing journals Tools > Session Journals… ( Ctrl + Alt + J ) opens the journal manager: all journals in a table sorted by start time (newest first) with duration, connection, server, title and entry count. Running journals are marked and cannot be renamed or deleted while live. The filter field matches title, connection, host, user, description and the AI keywords; enabling Search contents additionally scans the journal entries — including AI screenshot descriptions and tags — and capture logs of every journal in the background (the logs are read streaming, part by part, so even huge journals do not load into memory). Below the filter field, clickable keyword chips show the most common AI keywords across the listed journals — with exactly one journal selected, that journal's own keywords. Clicking a chip filters by it. The keywords come from the closing session summary, which extracts up to twelve verbatim search terms (hostnames, script and file names, error classes) into the journal metadata. Open (or double-click) opens the journal viewer; Rename changes the title; Delete asks for confirmation and then permanently removes the journal folder including the log and all screenshots. Several journals can be selected at once ( Ctrl / Shift click) to delete or export them in one step. Running journals cannot be renamed or deleted. The Description area below the table stores a free-text description per journal; it appears on the journal page and in every export and is included in the content search. Options holds the global capture and AI settings described above, plus Catch up summaries : it counts the closed journals that were never summarized (recorded while summaries were off or no model was reachable) and, on demand, runs the regular summarizer over them one by one behind a progress dialog — cancellable between journals, and an interrupted run resumes where it stopped. AI search across all journals AI search next to the filter field opens a search panel under the table. Ask a question across every stored journal — \"In which journals did result_complex.pl exit with an error?\" — and korTTY answers in two stages: a fast local ranking picks the most relevant journals from their metadata and collected entries, then a single AI request over those candidates writes the summary and selects the journals that actually answer the question. As with the per-journal Q&A , the model only ever sees the collected entries, never the raw logs; exact log positions come from the internal streaming search. The result appears on both sides of the panel: The answer chat on the left supports follow-up questions about the same results. The hit tree on the right lists each matching journal with its total hit count (hovering shows the AI's one-sentence reason), and under it the individual hits — matching entries and exact log lines with timestamp and snippet. Clicking a hit opens the journal and jumps straight to that entry or log line. Hits you have already opened turn muted with a check mark, and that marking survives new searches and restarts — orientation for working through a long hit list. The header shows the total: n hits in m journals . Repeated identical output lines are stored coalesced, and the count includes their repeat factors, so it reflects real occurrences. The table itself stays complete: journals with hits get a sorted Hits column and a row highlight instead of filtering everything else away. Selection only restricts the search to the journals selected in the table. Note Without a reachable AI profile the search still works: the question's identifiers are matched against the journals and logs directly, only the AI summary and journal selection are skipped (a notice says so). Optionally, Semantic journal search in the Options dialog adds embedding-based ranking on top of the lexical one — it requires a local embedding model configured in the knowledge stores and falls back to the lexical ranking silently whenever embeddings are unavailable. The viewer and editing The viewer shows the journal page in an embedded browser and refreshes automatically while the journal is still being written. Open in browser hands the page to your system browser. Edit splits the view: an entry table next to a form with the entry's Title , Summary , a marker picker and a notes field. Editing lets you correct or categorize entries — flag failures, highlight important findings, or rewrite a summary. Saving regenerates the page at the edited entry's position; a marker you set manually is never overwritten by the AI or by a rule. The quickest way to mark a single entry is the timeline itself: right-click it and choose Set marker… . Screenshot notes and annotations A screenshot on its own rarely says why it was taken. Right-clicking one — the thumbnail in the timeline or the full-size lightbox — offers two actions of its own: Action What it does Edit screenshot… Opens the editor described below Analyze screenshot with AI Runs the AI screenshot analysis for this picture; offered when an image-capable AI profile is configured and policy allows the analysis Export screenshot… Saves the picture, with its marks, to a file you choose Edit and export also sit in the context menu of the edit mode's entry table, and double-clicking a screenshot row opens the editor directly. These actions only appear inside korTTY: a standalone page in a browser can neither rewrite the journal nor reach a file dialog. Inside korTTY the journal's title is editable from the page too: double-click it, or right-click it and choose Rename journal… — the same rename the manager offers, subject to the same organisation policy. The editor itself: Tool What it does Pen A thick freehand stroke for circling or underlining something Box A rectangle you drag to whatever size you need Unreadable A rectangle whose contents are coarsened into blocks until they cannot be read — for hiding a value while leaving the surrounding context in place. Width sets how coarse the blocks are. Text A label with a dark halo, so it stays readable on a light terminal background Colour applies to the next mark (red to start with), Width sets the pen thickness and scales the text labels with it. Undo removes the last mark, Remove all clears them. Below the picture sits a five-line Note field for the remark that belongs to the screenshot; it is the same note the entry carries elsewhere in the journal. Marks are stored as data and are re-editable at any time — reopening the editor shows them again rather than a flattened picture. The annotated version becomes the image the timeline, the PDF, the Markdown export and the HTML bundle all show; the unmarked capture stays in the journal folder as shot-000004.orig.png . Warning Annotation draws on top of the picture — Unreadable included. It is not redaction. The unmarked capture remains on this machine inside the journal folder as shot-000004.orig.png . It is never copied into an export, so a box you drew over something sensitive does hold in an exported document, but anyone with access to the journal folder itself can still open the original. To remove something from a journal for good, use Search and replace or the redaction rules — and delete the .orig.png by hand if a screenshot is the problem. Markers Beyond the four built-in markers ( None , Info , Important , Error ) you can define your own — a name like Software installation and a colour of your choice. Manage markers… next to the marker picker opens the editor: Colour , Name and Counts as . The last one decides which built-in value the marker degrades to, which is what keeps a journal readable in an older korTTY and what makes an Outage marker count towards the error total. Add , Duplicate and Delete . Deleting also removes the rules that pointed at that marker, so no rule is left silently doing nothing. Markers live in your settings and are available in every journal. A marker you actually use is additionally stored inside that journal, so an exported or shared journal renders in the right colours on its own — and deleting a marker later never changes how an existing journal looks. Automatic markers The lower half of the same dialog holds rules that set markers on their own. Switch on Set markers automatically in new entries , then add a rule per search term: Column Meaning Active Whether the rule is applied at all Marker Which marker to set Search term A word or a whole sentence; with Regex off it is matched literally Regex Treat the search term as a regular expression Ignore case On by default Rules are checked from top to bottom and the first match wins — use ▲/▼ to order them. They look at the entry's title, summary, note and the input/output excerpts, never at the raw capture log, and they run after the redaction rules, so a redacted secret can never trigger one. A marker you set by hand is never overwritten; a marker the AI suggested may be. Apply now runs the rules over the journal that is currently open and reports how many entries changed — that also works while the session is still running. Tick Also overwrite markers set by hand only if you really want your own choices replaced. Search and replace Searching finds a term. Search & replace rewrites every occurrence. Use it to erase something that must not stay in the journal — a password pasted into a visible command, a token in a server response — or simply to correct a recurring word. It is reachable from two places: the Search & replace… button in edit mode, and the Replace… button in the search bar on the journal page , which opens the same dialog with the term you were searching for already filled in. That button only appears inside korTTY — the page is generated from the journal files, so a copy opened in a browser can search but has no way to rewrite anything. Option Effect Search for / Replace with The text to find and what to put in its place ( *** by default) Regular expression Treats the search text as a regex; $1 in the replacement inserts a captured group Ignore case Matches every casing Rewrite the capture log as well On by default. Off changes only the journal entries and leaves the raw log untouched Count matches A dry run over the real journal: reports how many entry fields and log lines would change, without writing anything Replacing covers every entry title, AI summary, note and excerpt, and — unless you turned it off — every capture-log part including the compressed ones. The file header and every untouched line are preserved exactly, so the log keeps its structure. Warning Replacing rewrites the journal files in place and cannot be undone. Use Count matches first, especially with a regular expression. Documents you already exported are separate files and are not changed — export them again afterwards. A journal that is still being written cannot be rewritten; stop the session first. The search text is never written to korTTY's own log, because for a redaction it is the secret. Deleting an entry Delete entry removes the selected timeline entry after a confirmation, and a screenshot entry's image file with it. It does not touch the capture log — to remove a text from there as well, use Search & replace. Exporting The Export menu in the manager and the viewer offers three formats: Format Content PDF The simple journal: header with connection details and statistics, day-grouped entries with marker badges, input/output excerpts, notes — and, if selected, downscaled embedded screenshots Markdown The same simple journal as a .md file; screenshots are copied into a sibling <name>-files/ folder HTML bundle (complete) A zip archive of the whole journal — journal.html , journal.xml , the decompressed capture logs and all screenshots — laid out so the page works immediately after unzipping PDF and Markdown ask whether screenshots should be included. Exporting only part of a journal The export dialog can narrow down what actually goes into the document. Every filter is optional, and the footer keeps a live count of how many entries would be exported. Time windows. Add as many as you like; an entry only has to fall into one of them, so 08:00–12:00 plus 14:00–16:00 exports both blocks of a day. Leaving the dates empty applies the window to every day the journal spans, and a window whose start is later than its end runs across midnight. The times may be approximate — that is the point: The input is forgiving: 8 , 08 , 8:00 , 8.30 and 0800 all work. Each window is widened by the Tolerance (± 5 minutes by default, set it to 0 for exact boundaries). An entry summarizes everything since the previous one, so an entry written at 12:03 that covers 11:58 onwards still belongs to a window ending at 12:00. Without that, the entry on the boundary — usually the interesting one — would fall out of every window. Topic. A word or a sentence, matched against titles, summaries, notes and excerpts; Regular expression switches to regex matching. Let the AI choose the entries hands the topic and the entries to the AI instead, which finds installing Apache even where none of those words appear literally. It needs an AI profile and is greyed out otherwise; if the model is unreachable or answers with nonsense, the export falls back to the text match and says so rather than failing. Markers. All entries, only marked ones, or only the markers you tick. The list shows the markers that journal actually uses. Filtered HTML bundles Without a filter the HTML bundle stays the verbatim copy it always was. With one it is rebuilt : journal.xml holds only the exported entries, and the marker definitions travel with them. The capture log is rewritten to the sequence ranges those entries reference, clipped to the requested time windows. This is not optional — a bundle is the artefact you hand to someone else, and twelve entries next to eight hours of terminal output would be exactly the leak the filter is meant to prevent. Only the screenshots still referenced are copied, and journal.html is re-rendered so its deep links resolve. The header counts are recalculated to match what the bundle shows. Every filtered export — PDF, Markdown and bundle alike — carries an Excerpt banner naming the range and the entry count, so nobody mistakes it for the complete session. Exporting several journals With more than one journal selected, the export writes a single zip archive that keeps each journal separate: one PDF or Markdown document per journal, or one folder per journal for the HTML bundle. Names are taken from the journal titles, with a numeric suffix if two titles collide. Filters apply to every selected journal. A journal in which the filter matches nothing is skipped and reported afterwards, so one empty result cannot kill a ten-journal export; only if every journal comes out empty does the export refuse — before writing a file. Every archive — including the HTML bundle of a single journal — can be protected with a password . The option sits in the export dialog and encrypts the archive with AES-256 ; without it the archive is written unencrypted. Journals contain full terminal transcripts, so an unprotected archive is a deliberate choice. Warning The password is not stored anywhere. korTTY cannot recover an encrypted archive if you lose it. Footer and watermark By default every exported document carries a footer stating that it was created with korTTY, with a link to the project repository — at the bottom of each PDF page, at the end of the Markdown file, and in the footer of the journal page inside the HTML bundle. PDFs can additionally carry a diagonal watermark, which is off by default . Both are configured under Configuration → Global Settings → Export , where you can change the footer text, turn the footer off, enable the watermark and choose its text and colour. The same settings apply to AI chat exports. Enterprise policy Administrators can deny the feature ( session-journal under [rule.features] ), or mandate its behavior via [rule.session-journal] : force a journal for every connection, fix the log format, AI line window or storage directory, forbid renaming or deleting journals, prescribe a naming template, enforce the closing AI title, force the AI screenshot analysis on or off — a forced off also disables the manual per-screenshot run — and forbid the on-demand AI over journal content ( ai-ask = false removes the viewer's Q&A panel and the manager's AI search while leaving the summaries untouched). See Policy configuration for the keys. Automatic redaction A [[rule.session-journal.replace]] list makes korTTY apply search-and-replace automatically, with regular expressions if the administrator wants them — for cloud access keys, internal hostnames, ticket numbers, anything that must never end up in a transcript: [[rule.session-journal.replace]] pattern = \"AKIA[0-9A-Z]{16}\" replacement = \"***AWS-ACCESS-KEY***\" regex = true label = \"AWS access keys\" These rules run on the capture thread, before a line is written, so a matching text never reaches the log file in the first place; they are applied to AI summaries and notes as well. Every rule of every matching policy tier applies — a rule adding a pattern never switches another one off. The dialog above tells you how many mandated rules are in force. Journals written before a rule existed are not rewritten retroactively; use Search & replace for those. See Policy configuration for every key.","title": "Session journal"},{"location": "features/session-journal.html#session-journal","text": "The session journal documents a terminal session as a readable timeline: every server output line and every command you type is written to a capture log, an AI periodically condenses the activity into short journal entries, and the result is rendered as a self-contained HTML page with connection details, color-coded excerpts, screenshots and your own notes. Journals are managed like saved chats — in their own manager window with search, editing and export.","title": "Session journal"},{"location": "features/session-journal.html#storage-and-formats","text": "Each journal is one self-contained directory under ~/.kortty/journals (configurable in Settings > Logging > Session Journal ): File Purpose journal.xml The curated journal document: metadata, AI summaries, markers, notes, screenshot references session-log.json / .xml / .yaml The append-only capture log — timestamped server output and typed input lines with sequence ids session-log-2.json.zst , … Rotated log parts; closed parts are zstd-compressed automatically, the journal never deletes history. Journals recorded by older versions keep their .gz parts and remain fully readable journal.html The generated timeline page, regenerated automatically after every change screenshots/*.png Screenshots you attached during the session The capture-log format is selectable in the journal manager's Options dialog: JSON (JSON Lines, the default), XML or YAML . All formats carry the same fields, and every entry is exactly one line, so a crash never corrupts more than the last line. JSON is the default because log tooling reads it without needing a parser of its own — not because it saves space. Size barely separates the three: for ordinary output XML is about 9 bytes per entry smaller, for output full of < , > and & JSON is roughly 10 % smaller (XML has to escape those, JSON does not), and once a finished part is compressed all three land within 2 % of each other. YAML is the largest, since it writes JSON mappings with a - prefix. The active log part stays uncompressed for live reads; rotation (default 25 MB per part) and session end compress finished parts to .zst (zstd — journals from older versions keep their .gz parts and open exactly as before). Two more things keep long, noisy sessions small and complete. Consecutive identical output lines (progress loops, tail -f repeats) are coalesced syslog-style: the first occurrence is written immediately, follow-ups are counted and stored as one entry with a repeat count. The viewer shows such a run compactly as line ×12 , while copying or exporting the log reproduces the original lines in full. And when server output arrives faster than the log can persist it, the capture applies backpressure instead of dropping lines — the terminal may briefly slow down under an extreme flood, but the journal stays complete. Rotation is configurable per connection on the Journal tab: Maximum size per log part (MB) (default 25) and Maximum rotated log parts (default 20). After the configured number of parts, output capture stops with a note in the journal; input, screenshots and notes continue. An enterprise policy can cap the part count via max-log-parts .","title": "Storage and formats"},{"location": "features/session-journal.html#enabling-the-journal","text": "","title": "Enabling the journal"},{"location": "features/session-journal.html#automatically-for-a-connection","text": "Open Connections > Manage Connections and edit a connection On the Journal tab, enable Enable session journal for this connection Optionally adjust Capture typed input lines , Generate AI summaries , the per-connection Summary interval , and the rotation pair Maximum size per log part (MB) and Maximum rotated log parts Every future connection of this server then starts its journal automatically. The journal survives reconnects — one journal per tab lifetime, with a reconnect marker in the log.","title": "Automatically for a connection"},{"location": "features/session-journal.html#retroactively-for-a-running-session","text": "Use Tools > Start/Stop Session Journal ( Ctrl + Alt + T ), the tab context menu ( Session Journal > Start journal ) or the journal bar's Start journal button. The existing scrollback is imported into the journal as seed entries first, so the timeline covers what already happened, then live capture attaches.","title": "Retroactively for a running session"},{"location": "features/session-journal.html#the-journal-bar","text": "While a journal is available, a bar below the terminal shows its state ( Journal active since HH:MM ) and offers Stop journal , Screenshot and Note : Screenshot ( Ctrl + Alt + C , also in the terminal's right-click menu) snapshots the terminal — in a split layout, the right-click menu captures exactly the pane under the cursor — and files it into the journal timeline. Note opens the note editor for a free-text remark that appears as its own timeline entry at the current position.","title": "The journal bar"},{"location": "features/session-journal.html#writing-notes","text": "Notes are written in the same editor everywhere they are edited — the journal bar's Note button, the live panel's, the entry form in the viewer , and the screenshot editor : The field holds at least six lines and the dialog can be resized, so a note can be a paragraph instead of a single line. Links are clickable. Any http:// or https:// address in a note becomes a link on the journal page — click it and the address opens in your system browser, never inside the journal view. Only those two schemes ever become links, and only in text you wrote yourself: AI summaries and terminal excerpts stay literal. Translate hands the note to the AI and replaces it with the translation in the language picked next to the button. The list offers the interface languages and any language korTTY has translations for, and it accepts a typed language it does not list. Your choice is remembered for the next note. The translation runs on the profile assigned to Text and translation profile in the AI manager — the same role the snippet editor's translation uses — falling back to your default profile when that role is unset. Internet access stays disabled for it, exactly as for the summaries; Cmd + Z / Ctrl + Z brings the original back. Without a usable AI profile the button is disabled and says so.","title": "Writing notes"},{"location": "features/session-journal.html#when-the-connection-ends","text": "A journal is bound to its tab, not to a single connection. When the connection ends while the journal is running — a reboot , a dropped network, or the server closing the session — the tab stays open and a red decision bar appears instead of the tab closing silently: Reconnect re-establishes the connection and the journal simply continues, with a reconnect marker in the capture log. Work that resumes after a server reboot lands in the same journal. End journal stops the journal and writes its closing summary (and, if enabled, the AI title) exactly like the journal bar's stop button. The tab then shows the plain reconnect bar, so reconnecting is still possible — with journaling enabled for the connection, that new session starts a fresh journal. Closing the tab instead also ends the journal with its closing summary. Without a running journal the behavior is unchanged: a cleanly ended connection closes the tab, an error keeps it open with the reconnect bar (double-click to reconnect).","title": "When the connection ends"},{"location": "features/session-journal.html#the-live-journal-panel","text": "View > Live Journal (or Ctrl + Alt + L ) docks the running journal's full journal page — the same page the viewer shows — to the left or right of the terminal, kept up to date in real time. Selecting the checked side in the menu hides the panel again; the divider next to it adjusts the width, and side and width are remembered across restarts. Two things update live while the session runs: The live log — the Live Log button in the panel header opens the page's log panel in follow mode, streaming the capture log as it is written: command output, the commands you typed, notes and screenshot markers, each with a timestamp. It starts hidden; lines accumulate either way, so opening it later shows everything. Scrolling up pauses following, scrolling back to the bottom resumes it, the ✕ in its corner hides it again (the button stays in sync), and dragging its top edge adjusts the height — which is remembered. The view keeps the newest 5000 lines; everything stays in the capture log and in the entry cards' log excerpts. The timeline — new cards (AI summaries, notes, screenshots) and edits appear moments after they happen, without losing your scroll position. A terminal AI-agent run adds its own AI agent card the moment it finishes: your prompt as the title, the agent's final answer as the body, and a meta line with the model, the run duration and the reported token count. Long answers collapse to a preview — click the text (or Show full answer ) to expand. Agent work is part of the journal record even though the summarizer treats the agent's inline terminal text as noise. Because it is the real journal page, everything the viewer's page offers works right here: click an entry card to see its log excerpt, search the journal, jump between marked entries, and right-click a screenshot to open the annotation editor (pen, box, unreadable, text and a note) or copy it — the edited picture appears in the panel as soon as you save. Right-clicking an entry offers the same marker picker as the viewer. The cards adapt to the panel width, so the text stays readable however narrow or wide you drag it.","title": "The live journal panel"},{"location": "features/session-journal.html#jumping-to-a-time","text": "The ◷ button in the page header opens a time field: type a time and the timeline scrolls to the entry closest to it and highlights it briefly. The input is lenient — 19:00 , 19.00 , 1900 and 19 all mean the same, and a date can be prefixed ( 13.08. 19:00 , 13.08.2026 19:00 or 2026-08-13 19:00 ). Without a date the time is matched against each entry's own day, so a session running past midnight jumps to the nearest occurrence rather than always the first day. The panel's header adds the instant controls: Note and Screenshot act on the shown journal exactly like the journal bar — a note you add appears in both the timeline and the live log — Live Log shows or hides the log view, and Open Viewer opens the full viewer window for editing, search & replace and exports. The ⋯ menu switches the page between light and dark, refreshes it, and opens the page appearance settings. The panel follows your tabs with a memory: it shows the journal of the current tab, and when you switch tabs it only switches along if the newly selected tab also has a running journal — otherwise it keeps showing the journal it already displays. When the shown journal is stopped or its tab is closed, the page stays visible with a Journal stopped / Tab closed badge until you select another tab with a live journal. Everything shown has already passed password protection — suppressed input and redacted secrets never reach the panel.","title": "Jumping to a time"},{"location": "features/session-journal.html#ai-summaries","text": "While the journal runs, the AI summarizer periodically reads the newest capture-log lines and appends a compact journal entry (title, summary and a suggested marker: info, important or error). Defaults and limits: Option Where Default Summary interval Settings > Logging > Session Journal (global) or per connection 5 minutes Max. terminal lines per AI evaluation Journal manager Options 100 Token budget for context fill Journal manager Options , visible when max lines is 0 130000 Split backlog into multiple prompts (chunking) Journal manager Options off AI profile for summaries Journal manager Options or Settings > Logging > Session Journal Default profile Analyze screenshots with AI (description and tags) Journal manager Options on Semantic journal search (embeddings) Journal manager Options off Summaries use your default AI profile unless you pick a dedicated journal profile. That choice is available in three equivalent places: the journal manager's Options dialog, Settings > Logging > Session Journal , and AI > AI Manager > Local AI next to the Text and Coding roles. The Text/Coding role profiles themselves are deliberately not used for the journal. Setting max lines to 0 switches to context filling: the summarizer packs as many of the newest lines as fit into the configured token budget. With chunking enabled the whole backlog is processed instead of only the newest window — consecutive windows of the configured size, one AI prompt each. Warning Chunking can take very long on large sessions and is not recommended for everyday use — it is intended for power users with capable hardware and a powerful LLM. When the session ends, the summarizer writes a closing session summary entry (what was accomplished, which errors occurred) and extracts up to twelve verbatim keywords — hostnames, script and file names, error classes — into the journal metadata, where the manager's filter, keyword chips and AI search pick them up. Optionally — Let the AI title the journal when the session ends in the Options dialog — a final AI call names the journal, unless you renamed it manually. Note The journal works without AI: if no AI profile is available, AI features are disabled, or summaries are switched off, the timeline records raw activity entries instead. AI summary prompts never use internet-access tools; the terminal excerpt goes only to the configured AI profile.","title": "AI summaries"},{"location": "features/session-journal.html#ai-screenshot-analysis","text": "When the journal's AI profile accepts images, screenshots are analyzed as well: the model writes a short description (one to three sentences) and a handful of lowercase tags , shown to the right of the thumbnail on the journal page. Tags are clickable chips — clicking one starts a journal search for that tag — and both texts are found by the manager's Search contents scan, rewritten by search and replace and the automatic redaction rules, and included in the Markdown and PDF exports. Like the summaries, the analysis answers in the language the journal was created under. Analyze screenshots with AI (description and tags) in the journal manager's Options dialog controls the automatic analysis on capture (on by default). It runs only when AI summaries are enabled for the connection and the profile can take images; otherwise the screenshot is simply filed unanalyzed. Analyze screenshot with AI in the right-click menu of a screenshot analyzes one picture on demand — the way to analyze screenshots in journals recorded earlier, to retry a failed run, or to re-run the analysis after making something unreadable in the annotation editor . The analysis always reads the annotated picture, never the untouched .orig.png capture. Whether a profile can take images is a per-profile property: Image input (vision) in the AI settings defaults to Auto — for a local LM Studio endpoint korTTY reads the answer from the model metadata (during the same refresh that discovers the reasoning options), for cloud endpoints it recognizes the common vision-capable model names — and can be overridden with Enabled / Disabled for models the detection misjudges. Warning The automatic analysis sends the screenshot at capture time — before you had a chance to make anything unreadable. The picture goes only to the configured AI profile and never through internet-access tools, but for sessions whose screen content must not leave the machine, switch the option off or have your administrator forbid the analysis via enterprise policy — the policy mandate also disables the manual run.","title": "AI screenshot analysis"},{"location": "features/session-journal.html#asking-the-ai-about-a-journal","text": "AI Q&A in the viewer's toolbar opens a chat panel next to the journal page. Ask anything about this session — \"Were screenshots taken that show errors from result_complex.pl?\" — and the AI answers from what the journal already collected while the session ran: the AI summaries, screenshot descriptions and tags, and your notes. The raw capture log is never sent to the model. When a question needs hard evidence from the log — exact error lines, whether a script really failed, how often something occurred — the model names a few literal search strings, korTTY's own streaming search scans the capture log for them, and only the match counts plus a handful of sample lines go back to the model for the final answer. The panel shows both next to the answer: Sources — the journal entries the answer cites; clicking one scrolls the timeline to that entry. Log evidence — per search string the exact number of matching log lines, with clickable samples that open the log panel scrolled to the very line. Follow-up questions continue the conversation (the panel keeps the recent exchanges as context); New conversation starts over. Save as note appends a question-and-answer pair to the journal timeline as an entry, so a finding becomes part of the record. Note If no AI profile is reachable or the request fails, the panel degrades instead of erroring out: it extracts the identifiers from your question, runs the internal text search, and shows the matching entries and log lines with a notice that no model was involved. The Q&A never uses internet-access tools, and administrators can forbid it entirely ( ai-ask under enterprise policy ).","title": "Asking the AI about a journal"},{"location": "features/session-journal.html#password-protection","text": "Typed input is captured only as complete submitted lines, and several layers keep passwords out of the journal: When the server output ends in a password prompt ( password: , [sudo] password for … , passphrase , PIN , and localized variants), the next submitted input line is suppressed and logged as a redacted placeholder — the typed text is never buffered or written. The connection's own stored password is additionally replaced by *** wherever it would appear in captured text. Capture typed input lines can be disabled per connection entirely; commands then only appear as the server's echo in the output stream. Warning The prompt detection is a heuristic — a remote terminal cannot reliably know when the server disabled echo. Exotic or full-screen password prompts may not be recognized, and secrets pasted into visible commands (other than the connection's own credentials) are captured like any other text. Treat journals of sensitive sessions accordingly. If something slipped through anyway, the viewer's search and replace removes it from the entries and the capture log after the fact. Administrators can also have korTTY redact patterns automatically — see Enterprise policy below.","title": "Password protection"},{"location": "features/session-journal.html#the-journal-page","text": "journal.html is fully self-contained (no external resources) and works in the built-in viewer, in any browser, and inside the exported bundle: A sticky header shows who was connected to which server, start time, duration, and counts for entries, commands, errors and screenshots, plus the journal description. Live journals show a live badge. The line below the title only carries what the title does not already say, so a journal named after its endpoint states the connection once instead of three times. The timeline groups entries by day; each entry carries its time, a marker dot and badge in the colour you gave that marker, the AI title and summary, and color-coded input (green) and output (blue) excerpts. Clicking an entry slides a log panel in from the bottom with the exact capture-log range behind that entry. The panel has its own scrollbar, a search field with a match counter and ▲/▼ navigation ( Enter / Shift + Enter also cycle matches, Esc closes), and colors input and output lines differently. Screenshot entries show thumbnails; clicking one opens a full-size lightbox. If the AI screenshot analysis ran, its description and tag chips sit to the right of the picture, and a chip click searches the journal for that tag. The page renders dark by default, follows the system light/dark preference and has its own theme toggle. Screenshots, excerpt panels, the timeline column and the log panel size themselves to the window, so the page stays readable in a narrow viewer tab as well as full screen. Long excerpts scroll inside their own box instead of stretching the timeline.","title": "The journal page"},{"location": "features/session-journal.html#searching-the-journal","text": "The magnifier in the header opens a search bar directly under the connection details ( Ctrl + F works too). Typing a term or a whole sentence highlights every occurrence across the timeline — entry titles, AI summaries, AI screenshot descriptions and tags, input and output excerpts, notes and timestamps — and shows a match counter; ▲ and ▼ or Enter / Shift + Enter jump between the hits, Esc or ✕ closes the bar and clears the highlighting. Note This searches the journal entries. The raw capture log has its own search inside the log panel, the journal manager can search across all journals with Search contents or the AI search , and the viewer's AI Q&A answers questions about this journal.","title": "Searching the journal"},{"location": "features/session-journal.html#jumping-between-marked-entries","text": "When at least one entry carries a marker, the header gains a ◆ button that opens a marker bar. Pick All markers or a single one, then step through the matches with ▲ and ▼ — the list wraps around, and the current entry is scrolled into view and briefly outlined. Alt + Down and Alt + Up do the same without the mouse, and Alt + M toggles the bar; Esc closes it. A journal without markers ships neither the button nor the bar, so the header stays as it was.","title": "Jumping between marked entries"},{"location": "features/session-journal.html#picking-a-time-range-with-the-mouse","text": "Inside korTTY the header also carries a ⇥ button that switches the timeline into range mode. Click the first entry, then the last one — everything between them is highlighted and the bar shows the span and how many entries it covers. The order does not matter: clicking the later entry first works just as well. Add another window puts the current selection aside and starts a new one, so several windows can be collected in one pass. Use for export opens the export dialog with those windows already filled in. Cancel or Esc leaves range mode. While range mode is on, clicking an entry selects instead of opening the log panel. The button is absent in an external browser, because exporting needs the app. In the edit mode's entry table the same thing is available without the timeline: select several rows, right-click and choose Use selection as time window .","title": "Picking a time range with the mouse"},{"location": "features/session-journal.html#copying-content","text": "Every entry carries a copy button in its top-right corner: text entries copy the whole entry, screenshot entries copy the image. The log panel has the same button for the log section it currently shows. Right-clicking the page opens a copy menu with more targeted actions, depending on what you clicked: Action Copies Copy selection The currently selected text (shown whenever a selection exists) Copy summary The entry's time, title and AI summary Copy entry The same plus the input/output excerpts and your note Copy screenshot The screenshot itself, as an image, onto the clipboard Copy screenshot path The screenshot's path inside the journal folder Copy log section Every line of the log range currently shown in the panel, with timestamps Inside korTTY the copy actions use the app's clipboard, so images land on the system clipboard ready to paste. In an external browser text still copies normally; copying an image may fall back to its path, because browsers block reading local image data from a file:// page.","title": "Copying content"},{"location": "features/session-journal.html#appearance","text": "The A− , A and A+ buttons in the page header scale the whole page between 70 % and 250 %. korTTY remembers the size and applies it to every journal page it renders afterwards, so a page that regenerates (a new AI summary, an edited marker) comes back at the size you chose. Opened standalone in a browser the page remembers the size per browser instead. The viewer's Appearance button opens a small panel with the rest: Setting Effect Colour scheme Automatic keeps the page's own dark/light pair and follows the operating system. Follow the terminal theme derives the page colours from your terminal's background and foreground. The remaining entries (Paper, Midnight, Ocean, Forest, Retrowave, High contrast) are fixed palettes. Text font The font for headings, summaries and notes. (default) restores the page's own stack. Monospace font The font for the input/output excerpts and the log panel. Text size The same 70–250 % as the A−/A/A+ buttons. Changes preview immediately in the viewer and are saved for every journal page. With a fixed scheme the page's ◐ toggle stays visible but is disabled, since the scheme already decides the colours.","title": "Appearance"},{"location": "features/session-journal.html#managing-journals","text": "Tools > Session Journals… ( Ctrl + Alt + J ) opens the journal manager: all journals in a table sorted by start time (newest first) with duration, connection, server, title and entry count. Running journals are marked and cannot be renamed or deleted while live. The filter field matches title, connection, host, user, description and the AI keywords; enabling Search contents additionally scans the journal entries — including AI screenshot descriptions and tags — and capture logs of every journal in the background (the logs are read streaming, part by part, so even huge journals do not load into memory). Below the filter field, clickable keyword chips show the most common AI keywords across the listed journals — with exactly one journal selected, that journal's own keywords. Clicking a chip filters by it. The keywords come from the closing session summary, which extracts up to twelve verbatim search terms (hostnames, script and file names, error classes) into the journal metadata. Open (or double-click) opens the journal viewer; Rename changes the title; Delete asks for confirmation and then permanently removes the journal folder including the log and all screenshots. Several journals can be selected at once ( Ctrl / Shift click) to delete or export them in one step. Running journals cannot be renamed or deleted. The Description area below the table stores a free-text description per journal; it appears on the journal page and in every export and is included in the content search. Options holds the global capture and AI settings described above, plus Catch up summaries : it counts the closed journals that were never summarized (recorded while summaries were off or no model was reachable) and, on demand, runs the regular summarizer over them one by one behind a progress dialog — cancellable between journals, and an interrupted run resumes where it stopped.","title": "Managing journals"},{"location": "features/session-journal.html#ai-search-across-all-journals","text": "AI search next to the filter field opens a search panel under the table. Ask a question across every stored journal — \"In which journals did result_complex.pl exit with an error?\" — and korTTY answers in two stages: a fast local ranking picks the most relevant journals from their metadata and collected entries, then a single AI request over those candidates writes the summary and selects the journals that actually answer the question. As with the per-journal Q&A , the model only ever sees the collected entries, never the raw logs; exact log positions come from the internal streaming search. The result appears on both sides of the panel: The answer chat on the left supports follow-up questions about the same results. The hit tree on the right lists each matching journal with its total hit count (hovering shows the AI's one-sentence reason), and under it the individual hits — matching entries and exact log lines with timestamp and snippet. Clicking a hit opens the journal and jumps straight to that entry or log line. Hits you have already opened turn muted with a check mark, and that marking survives new searches and restarts — orientation for working through a long hit list. The header shows the total: n hits in m journals . Repeated identical output lines are stored coalesced, and the count includes their repeat factors, so it reflects real occurrences. The table itself stays complete: journals with hits get a sorted Hits column and a row highlight instead of filtering everything else away. Selection only restricts the search to the journals selected in the table. Note Without a reachable AI profile the search still works: the question's identifiers are matched against the journals and logs directly, only the AI summary and journal selection are skipped (a notice says so). Optionally, Semantic journal search in the Options dialog adds embedding-based ranking on top of the lexical one — it requires a local embedding model configured in the knowledge stores and falls back to the lexical ranking silently whenever embeddings are unavailable.","title": "AI search across all journals"},{"location": "features/session-journal.html#the-viewer-and-editing","text": "The viewer shows the journal page in an embedded browser and refreshes automatically while the journal is still being written. Open in browser hands the page to your system browser. Edit splits the view: an entry table next to a form with the entry's Title , Summary , a marker picker and a notes field. Editing lets you correct or categorize entries — flag failures, highlight important findings, or rewrite a summary. Saving regenerates the page at the edited entry's position; a marker you set manually is never overwritten by the AI or by a rule. The quickest way to mark a single entry is the timeline itself: right-click it and choose Set marker… .","title": "The viewer and editing"},{"location": "features/session-journal.html#screenshot-notes-and-annotations","text": "A screenshot on its own rarely says why it was taken. Right-clicking one — the thumbnail in the timeline or the full-size lightbox — offers two actions of its own: Action What it does Edit screenshot… Opens the editor described below Analyze screenshot with AI Runs the AI screenshot analysis for this picture; offered when an image-capable AI profile is configured and policy allows the analysis Export screenshot… Saves the picture, with its marks, to a file you choose Edit and export also sit in the context menu of the edit mode's entry table, and double-clicking a screenshot row opens the editor directly. These actions only appear inside korTTY: a standalone page in a browser can neither rewrite the journal nor reach a file dialog. Inside korTTY the journal's title is editable from the page too: double-click it, or right-click it and choose Rename journal… — the same rename the manager offers, subject to the same organisation policy. The editor itself: Tool What it does Pen A thick freehand stroke for circling or underlining something Box A rectangle you drag to whatever size you need Unreadable A rectangle whose contents are coarsened into blocks until they cannot be read — for hiding a value while leaving the surrounding context in place. Width sets how coarse the blocks are. Text A label with a dark halo, so it stays readable on a light terminal background Colour applies to the next mark (red to start with), Width sets the pen thickness and scales the text labels with it. Undo removes the last mark, Remove all clears them. Below the picture sits a five-line Note field for the remark that belongs to the screenshot; it is the same note the entry carries elsewhere in the journal. Marks are stored as data and are re-editable at any time — reopening the editor shows them again rather than a flattened picture. The annotated version becomes the image the timeline, the PDF, the Markdown export and the HTML bundle all show; the unmarked capture stays in the journal folder as shot-000004.orig.png . Warning Annotation draws on top of the picture — Unreadable included. It is not redaction. The unmarked capture remains on this machine inside the journal folder as shot-000004.orig.png . It is never copied into an export, so a box you drew over something sensitive does hold in an exported document, but anyone with access to the journal folder itself can still open the original. To remove something from a journal for good, use Search and replace or the redaction rules — and delete the .orig.png by hand if a screenshot is the problem.","title": "Screenshot notes and annotations"},{"location": "features/session-journal.html#markers","text": "Beyond the four built-in markers ( None , Info , Important , Error ) you can define your own — a name like Software installation and a colour of your choice. Manage markers… next to the marker picker opens the editor: Colour , Name and Counts as . The last one decides which built-in value the marker degrades to, which is what keeps a journal readable in an older korTTY and what makes an Outage marker count towards the error total. Add , Duplicate and Delete . Deleting also removes the rules that pointed at that marker, so no rule is left silently doing nothing. Markers live in your settings and are available in every journal. A marker you actually use is additionally stored inside that journal, so an exported or shared journal renders in the right colours on its own — and deleting a marker later never changes how an existing journal looks.","title": "Markers"},{"location": "features/session-journal.html#search-and-replace","text": "Searching finds a term. Search & replace rewrites every occurrence. Use it to erase something that must not stay in the journal — a password pasted into a visible command, a token in a server response — or simply to correct a recurring word. It is reachable from two places: the Search & replace… button in edit mode, and the Replace… button in the search bar on the journal page , which opens the same dialog with the term you were searching for already filled in. That button only appears inside korTTY — the page is generated from the journal files, so a copy opened in a browser can search but has no way to rewrite anything. Option Effect Search for / Replace with The text to find and what to put in its place ( *** by default) Regular expression Treats the search text as a regex; $1 in the replacement inserts a captured group Ignore case Matches every casing Rewrite the capture log as well On by default. Off changes only the journal entries and leaves the raw log untouched Count matches A dry run over the real journal: reports how many entry fields and log lines would change, without writing anything Replacing covers every entry title, AI summary, note and excerpt, and — unless you turned it off — every capture-log part including the compressed ones. The file header and every untouched line are preserved exactly, so the log keeps its structure. Warning Replacing rewrites the journal files in place and cannot be undone. Use Count matches first, especially with a regular expression. Documents you already exported are separate files and are not changed — export them again afterwards. A journal that is still being written cannot be rewritten; stop the session first. The search text is never written to korTTY's own log, because for a redaction it is the secret.","title": "Search and replace"},{"location": "features/session-journal.html#deleting-an-entry","text": "Delete entry removes the selected timeline entry after a confirmation, and a screenshot entry's image file with it. It does not touch the capture log — to remove a text from there as well, use Search & replace.","title": "Deleting an entry"},{"location": "features/session-journal.html#exporting","text": "The Export menu in the manager and the viewer offers three formats: Format Content PDF The simple journal: header with connection details and statistics, day-grouped entries with marker badges, input/output excerpts, notes — and, if selected, downscaled embedded screenshots Markdown The same simple journal as a .md file; screenshots are copied into a sibling <name>-files/ folder HTML bundle (complete) A zip archive of the whole journal — journal.html , journal.xml , the decompressed capture logs and all screenshots — laid out so the page works immediately after unzipping PDF and Markdown ask whether screenshots should be included.","title": "Exporting"},{"location": "features/session-journal.html#exporting-only-part-of-a-journal","text": "The export dialog can narrow down what actually goes into the document. Every filter is optional, and the footer keeps a live count of how many entries would be exported. Time windows. Add as many as you like; an entry only has to fall into one of them, so 08:00–12:00 plus 14:00–16:00 exports both blocks of a day. Leaving the dates empty applies the window to every day the journal spans, and a window whose start is later than its end runs across midnight. The times may be approximate — that is the point: The input is forgiving: 8 , 08 , 8:00 , 8.30 and 0800 all work. Each window is widened by the Tolerance (± 5 minutes by default, set it to 0 for exact boundaries). An entry summarizes everything since the previous one, so an entry written at 12:03 that covers 11:58 onwards still belongs to a window ending at 12:00. Without that, the entry on the boundary — usually the interesting one — would fall out of every window. Topic. A word or a sentence, matched against titles, summaries, notes and excerpts; Regular expression switches to regex matching. Let the AI choose the entries hands the topic and the entries to the AI instead, which finds installing Apache even where none of those words appear literally. It needs an AI profile and is greyed out otherwise; if the model is unreachable or answers with nonsense, the export falls back to the text match and says so rather than failing. Markers. All entries, only marked ones, or only the markers you tick. The list shows the markers that journal actually uses.","title": "Exporting only part of a journal"},{"location": "features/session-journal.html#filtered-html-bundles","text": "Without a filter the HTML bundle stays the verbatim copy it always was. With one it is rebuilt : journal.xml holds only the exported entries, and the marker definitions travel with them. The capture log is rewritten to the sequence ranges those entries reference, clipped to the requested time windows. This is not optional — a bundle is the artefact you hand to someone else, and twelve entries next to eight hours of terminal output would be exactly the leak the filter is meant to prevent. Only the screenshots still referenced are copied, and journal.html is re-rendered so its deep links resolve. The header counts are recalculated to match what the bundle shows. Every filtered export — PDF, Markdown and bundle alike — carries an Excerpt banner naming the range and the entry count, so nobody mistakes it for the complete session.","title": "Filtered HTML bundles"},{"location": "features/session-journal.html#exporting-several-journals","text": "With more than one journal selected, the export writes a single zip archive that keeps each journal separate: one PDF or Markdown document per journal, or one folder per journal for the HTML bundle. Names are taken from the journal titles, with a numeric suffix if two titles collide. Filters apply to every selected journal. A journal in which the filter matches nothing is skipped and reported afterwards, so one empty result cannot kill a ten-journal export; only if every journal comes out empty does the export refuse — before writing a file. Every archive — including the HTML bundle of a single journal — can be protected with a password . The option sits in the export dialog and encrypts the archive with AES-256 ; without it the archive is written unencrypted. Journals contain full terminal transcripts, so an unprotected archive is a deliberate choice. Warning The password is not stored anywhere. korTTY cannot recover an encrypted archive if you lose it.","title": "Exporting several journals"},{"location": "features/session-journal.html#footer-and-watermark","text": "By default every exported document carries a footer stating that it was created with korTTY, with a link to the project repository — at the bottom of each PDF page, at the end of the Markdown file, and in the footer of the journal page inside the HTML bundle. PDFs can additionally carry a diagonal watermark, which is off by default . Both are configured under Configuration → Global Settings → Export , where you can change the footer text, turn the footer off, enable the watermark and choose its text and colour. The same settings apply to AI chat exports.","title": "Footer and watermark"},{"location": "features/session-journal.html#enterprise-policy","text": "Administrators can deny the feature ( session-journal under [rule.features] ), or mandate its behavior via [rule.session-journal] : force a journal for every connection, fix the log format, AI line window or storage directory, forbid renaming or deleting journals, prescribe a naming template, enforce the closing AI title, force the AI screenshot analysis on or off — a forced off also disables the manual per-screenshot run — and forbid the on-demand AI over journal content ( ai-ask = false removes the viewer's Q&A panel and the manager's AI search while leaving the summaries untouched). See Policy configuration for the keys.","title": "Enterprise policy"},{"location": "features/session-journal.html#automatic-redaction","text": "A [[rule.session-journal.replace]] list makes korTTY apply search-and-replace automatically, with regular expressions if the administrator wants them — for cloud access keys, internal hostnames, ticket numbers, anything that must never end up in a transcript: [[rule.session-journal.replace]] pattern = \"AKIA[0-9A-Z]{16}\" replacement = \"***AWS-ACCESS-KEY***\" regex = true label = \"AWS access keys\" These rules run on the capture thread, before a line is written, so a matching text never reaches the log file in the first place; they are applied to AI summaries and notes as well. Every rule of every matching policy tier applies — a rule adding a pattern never switches another one off. The dialog above tells you how many mandated rules are in force. Journals written before a rule existed are not rewritten retroactively; use Search & replace for those. See Policy configuration for every key.","title": "Automatic redaction"},{"location": "features/sftp.html","text": "SFTP file manager The integrated SFTP Manager provides a graphical file manager for transferring files between your local machine and remote servers via SFTP. It features a dual-panel layout, full file operations support, and seamless integration with the Snippet Editor for remote file editing. Opening SFTP Manager You can open the SFTP Manager in two ways: Menu: Tools > Open SFTP Manager Dashboard: Right-click a connection > \"Open SFTP Manager\" If the connection uses a temporary SSH key that has expired, you will be prompted to enter a new key before the connection can proceed. Interface The SFTP Manager uses a two-panel layout for easy side-by-side file management: Left Panel (Local) Right Panel (Remote) Browse local files Browse remote files Upload to remote Download to local Sortable columns Both panels display the same columns, all of which are sortable by clicking the column header: Column Description Name File or directory name Type Directory (📁) or file (📄) indicator Size File size in human-readable format (directories show —) Date Last modified date and time User Owner name (local: from filesystem; remote: from SFTP or UID) Group Group name (local: from filesystem; remote: from SFTP or GID) Permissions Unix-style permissions (e.g., rwxr-xr-x ) Default sort order By default, files are sorted by the Type column in this order: Parent directory ( .. ) — always at the top Directories starting with \".\" (e.g., .git , .config ) — alphabetically Other directories — alphabetically Files starting with \".\" (e.g., .bashrc ) — alphabetically All other files — alphabetically Click any column header to sort by that column. Clicking the Type column again toggles between ascending and descending order. File operations The SFTP Manager supports a full range of file operations: Operation How Upload Select local file(s), click Upload (or drag and drop) Download Select remote file(s), click Download Delete Select file(s), click Delete Rename Select a file, click Rename Copy Copy files within the same panel Edit in Snippet Editor Select exactly one local or remote file, then use the Edit toolbar menu or the right-click context menu Create Directory Click \"New Folder\" in either panel Create ZIP Select multiple files/directories, click \"Create ZIP\" Set Owner/Permissions Select file(s), use context menu or button. Separate fields for User, Group, and octal permissions (e.g., 755) Editing files with Snippet Editor The Edit in Snippet Editor action is enabled only for a single selected file. It is disabled for: Folders The parent-directory entry ( .. ) Multiple selections Local files are read directly from the local filesystem; remote files are downloaded through the active SFTP session into the editor. When the file opens in the Snippet Editor, the full toolbar remains available, including: Formatting and syntax checking Editor profiles and styling Line numbers and word wrap Configured AI actions The file-mode buttons provide these save choices: Overwrite file — writes the current editor content back to the original local or remote file Save as... — writes a new local file through a file chooser, or for remote files prompts for a new file name in the same remote directory Save as snippet — stores the current content as a new Snippet Manager snippet without marking the source file as saved Search Both panels support glob pattern search using the * wildcard. For example: *.log finds all log files in the current directory *.{py,sh} finds Python and shell files (if your shell supports brace expansion) backup* finds all files starting with \"backup\" Enter the pattern in the search field to quickly filter displayed files without leaving the current directory.","title": "SFTP file manager"},{"location": "features/sftp.html#sftp-file-manager","text": "The integrated SFTP Manager provides a graphical file manager for transferring files between your local machine and remote servers via SFTP. It features a dual-panel layout, full file operations support, and seamless integration with the Snippet Editor for remote file editing.","title": "SFTP file manager"},{"location": "features/sftp.html#opening-sftp-manager","text": "You can open the SFTP Manager in two ways: Menu: Tools > Open SFTP Manager Dashboard: Right-click a connection > \"Open SFTP Manager\" If the connection uses a temporary SSH key that has expired, you will be prompted to enter a new key before the connection can proceed.","title": "Opening SFTP Manager"},{"location": "features/sftp.html#interface","text": "The SFTP Manager uses a two-panel layout for easy side-by-side file management: Left Panel (Local) Right Panel (Remote) Browse local files Browse remote files Upload to remote Download to local","title": "Interface"},{"location": "features/sftp.html#sortable-columns","text": "Both panels display the same columns, all of which are sortable by clicking the column header: Column Description Name File or directory name Type Directory (📁) or file (📄) indicator Size File size in human-readable format (directories show —) Date Last modified date and time User Owner name (local: from filesystem; remote: from SFTP or UID) Group Group name (local: from filesystem; remote: from SFTP or GID) Permissions Unix-style permissions (e.g., rwxr-xr-x )","title": "Sortable columns"},{"location": "features/sftp.html#default-sort-order","text": "By default, files are sorted by the Type column in this order: Parent directory ( .. ) — always at the top Directories starting with \".\" (e.g., .git , .config ) — alphabetically Other directories — alphabetically Files starting with \".\" (e.g., .bashrc ) — alphabetically All other files — alphabetically Click any column header to sort by that column. Clicking the Type column again toggles between ascending and descending order.","title": "Default sort order"},{"location": "features/sftp.html#file-operations","text": "The SFTP Manager supports a full range of file operations: Operation How Upload Select local file(s), click Upload (or drag and drop) Download Select remote file(s), click Download Delete Select file(s), click Delete Rename Select a file, click Rename Copy Copy files within the same panel Edit in Snippet Editor Select exactly one local or remote file, then use the Edit toolbar menu or the right-click context menu Create Directory Click \"New Folder\" in either panel Create ZIP Select multiple files/directories, click \"Create ZIP\" Set Owner/Permissions Select file(s), use context menu or button. Separate fields for User, Group, and octal permissions (e.g., 755)","title": "File operations"},{"location": "features/sftp.html#editing-files-with-snippet-editor","text": "The Edit in Snippet Editor action is enabled only for a single selected file. It is disabled for: Folders The parent-directory entry ( .. ) Multiple selections Local files are read directly from the local filesystem; remote files are downloaded through the active SFTP session into the editor. When the file opens in the Snippet Editor, the full toolbar remains available, including: Formatting and syntax checking Editor profiles and styling Line numbers and word wrap Configured AI actions The file-mode buttons provide these save choices: Overwrite file — writes the current editor content back to the original local or remote file Save as... — writes a new local file through a file chooser, or for remote files prompts for a new file name in the same remote directory Save as snippet — stores the current content as a new Snippet Manager snippet without marking the source file as saved","title": "Editing files with Snippet Editor"},{"location": "features/sftp.html#search","text": "Both panels support glob pattern search using the * wildcard. For example: *.log finds all log files in the current directory *.{py,sh} finds Python and shell files (if your shell supports brace expansion) backup* finds all files starting with \"backup\" Enter the pattern in the search field to quickly filter displayed files without leaving the current directory.","title": "Search"},{"location": "features/snippets.html","text": "Snippet manager The Snippet Manager lets you store, organize, and quickly insert reusable code snippets, scripts, and configuration templates. Manage snippets across multiple languages with syntax highlighting, advanced search, AI-assisted editing, placeholder variables, and flexible export options. Overview The Snippet Manager includes the following features: System (OS) column — A sortable operating-system column for each snippet (Any, Linux, macOS, Windows). Auto-set when a snippet is created via Generate Workflow Script . Sortable columns — All columns (Name, Language, Category, System, Tags) are sortable. Script-Header category — A fixed, non-deletable category containing reusable header templates for workflow-script generation. Opening the Snippet Manager Menu: Tools → Snippet Manager Shortcut: Ctrl + Shift + S ( Cmd + Shift + S on macOS) Creating and editing snippets Click Add (or Edit to modify an existing snippet). Fill in the fields: Name — A descriptive name. Code language — Select the programming language (Bash, Python, Java, JavaScript, TypeScript, SQL, XML, JSON, YAML, and more). Enables syntax highlighting. The add ( + ) button next to the list adds a language that is not offered yet: type its name once and it is stored and offered in every future snippet editor. A self-added language is used for the AI prompts and the file extension; syntax highlighting falls back to plain text unless korTTY happens to ship a grammar for it. Text language — What language AI-written code should use for comments and for messages shown to users, logged, or printed as help. The default, Automatic — keep the script's language , leaves the snippet's own prose alone: korTTY writes new text in the language the script already uses and translates nothing. Picking a language instead is a deliberate instruction to convert the snippet's existing text into it. It is independent of the korTTY interface language. Tick Remember as default to keep the choice for future snippets; otherwise it applies to this editor only. Category — Select an existing category or type a new one. The fixed non-deletable Script-Header category contains reusable header templates for generated workflow scripts. System — Optionally select a target operating system (Any, Linux, macOS, Windows). Auto-set when created via Generate Workflow Script based on the agent's probed OS; you can manually override it for any snippet. Tags — Comma-separated keywords for searching (e.g., docker, deploy, backup ). Description — Optional free-text description of the snippet. Content — The snippet code. The editor provides live syntax highlighting based on the selected language. Click OK . If the snippet content changed, KorTTY saves the edited snippet while closing the dialog. When editing an existing entry, Save as new snippet stores the current content as a new snippet with a new ID and leaves the original unchanged. Editor toolbar and features The snippet editor toolbar provides: Format Code — Format the content using local formatters or AI-assisted formatting. Check Syntax — Validate the syntax (local or AI-assisted). AI Text — Correct spelling, translate, or generate technical descriptions. AI Code — Complete code, run a full code analysis, improve a selection (readability, robustness, performance, comments, or a custom instruction), migrate the snippet into a single language, check security, or generate diagrams. One-liner — Export as a terminal one-liner. Editor zoom — Adjust text size with Ctrl + + and Ctrl + - . Editor profiles — Switch between built-in IntelliJ-inspired profiles and custom color schemes. Background brightness — Adjust editor background. Word Wrap — Toggle line wrapping. Line numbers — Toggle line-number display. When the editor opens from the SFTP Manager for a local or remote file, the same toolbar remains available and the dialog uses file-mode save buttons. Column ruler and line-width formatting Above the content field, the column ruler keeps the current caret column fixed at the left as Column N and shows a live marker at the matching editor position. Moving the mouse over the live marker shows Position N . Click the ruler to set a maximum line-length marker (columns 20–240). Right-click that marker to format the content to the selected width or remove the marker. Line-width formatting works locally only for formatters that support configurable width: Prettier-backed web formats (JavaScript, TypeScript, HTML, CSS) Python (Black) Perl (Perl::Tidy) For languages without local line-width support, KorTTY asks whether to use AI-assisted formatting. Both local and AI-assisted formatting show a before/after preview before applying changes. Format Code Format Code uses KorTTY's shared local formatter service: Built-in formatters: JSON, XML, YAML/YML, TOML, INI/properties, Groovy Bundled formatters: Java (google-java-format), Bash/shell (shfmt), Web/JS/TS/HTML/CSS (Prettier), SQL (sql-formatter), Perl (Perl::Tidy) Fallback: Optional PATH fallbacks for developer setups when a bundled formatter is missing Prettier runs as its offline Standalone browser build with only the Babel, Estree, TypeScript, HTML and PostCSS plugins; SQL uses the bundled sql-formatter browser build. Both are initialized lazily in an isolated JavaFX WebView and need no installed or packaged Node.js runtime. Requests are serialized and retain the same 15-second timeout, provider display and configurable Prettier line width as the process backend; a failed or timed-out engine is discarded before the next request. If local formatting is unavailable and the configured AI profile provides snippet AI capability, KorTTY asks whether to use AI assistance. AI formatting is applied only after the before/after preview is accepted. Editor profiles Switch between: Current custom colors — User-defined palette 10 built-in IntelliJ-inspired profiles — Predefined color schemes User-created profiles — Custom profiles you create Profiles store foreground/background colors, syntax colors, cursor color, and cursor style. AI-assisted editor functions If AI is configured, the snippet editor offers additional actions: AI suggestions AI suggestion — Generates a file name, description, Code language and Text language from the current code content. A detected code language that is not in the list yet is added to it, so the detection is never silently dropped. Correct spelling — On the description field; sends only description text to the AI. Text language When AI is configured, Text language — directly below Code language in the editor form — chooses what language AI-written code uses for its comments and messages, and which language spelling correction works in. Its default is Automatic — keep the script's language . korTTY works out which language the snippet's comments and messages are already written in and requires the AI to keep it: existing text is left as it is, and anything new is written to match. A script commented in English stays English even when you run korTTY in German. Identifiers, file paths, commands, options, configuration keys and other code tokens are never touched in either mode. Selecting a language instead is an explicit instruction to convert the snippet's prose into it — the previous behaviour, now opt-in rather than automatic. A selection-based action applies the contract to its returned selection, a full replacement to the complete script. When korTTY cannot tell which language a script uses — there is too little text to judge, or the comments mix languages — it asks before changing anything, rather than guessing. Tick Remember for this snippet in that dialog and the question is not asked again for that snippet. Dismissing the dialog cancels the action and leaves the code untouched. All of this is independent of both the korTTY interface language and the Code language selector, which continues to define the programming language and syntax highlighting. Analysis reports, improvement descriptions and analysis-diagram labels always follow the korTTY interface language. The list offers korTTY's own interface languages plus any AI-generated language you have added. AI suggestion can also fill it for you: it reads the snippet's comments and its printed output ( echo , print , printf , Write-Host and similar) and preselects the language it finds — including a language korTTY has no interface translation for, which is then added to the list. A script without any human-readable text leaves the current choice untouched. Leave Remember as default unticked for a temporary choice that applies only to the current editor window. Tick it to save the selected language as the default for newly opened snippet editors; this updates the existing Default language for AI text in code setting under Settings → AI . Other editor windows that are already open keep their own selection. Spelling correction uses the selected language for grammar and spelling rules without translating the text. Translate selection… keeps its separate target-language dialog and initially selects the current text language. Local formatters and syntax checks are unaffected. AI Text menu Correct spelling in selection — Fix typos in selected text. Translate selection… — Translate selected text to another language. Technical description — Generate documentation for selected code or the whole snippet. Optional additional instructions If enabled in Settings → AI , the editor shows a shared instructions field sent with spelling correction, translation, and technical description requests. Last AI change toggle The ↺ button switches between the original code and the last AI-generated editor change. AI Code completions The snippet editor offers two kinds of code completion, both inside the Monaco editor: a completion list you open on demand, and optional ghost text that appears on its own after a typing pause. The keys are listed under Keyboard shortcuts . Completion list — Press Shift + Tab with the cursor at the end of a line that has text on it, or Ctrl + Space anywhere in the editor (on macOS that is the physical Ctrl key; because macOS often reserves Ctrl + Space for switching input sources, Cmd + I and Alt + Esc open the list as well). The list opens at once with candidates harvested from the snippet itself — the arrays, hashes, variables and functions that fit the spot, followed by idioms of the language: after for x in in a Bash script every array comes first, after use POSIX in Perl the usual qw(...) import lists. Up / Down move the selection, typing filters the list, Enter or Tab inserts the selected entry, and Esc closes the list. An idiom with placeholders is inserted as a template in which Tab jumps from one placeholder to the next. AI Complete in the AI Code menu and in the editor's context menu opens the same list. Tab still indents, and Shift + Tab anywhere but at the end of a non-empty line still outdents. The combination is yours to choose: Configuration → Global Settings → Snippet Editor has an AI Completion Shortcut field that records whatever you press — one key, or one key with up to three modifiers — and applies it to snippet editors opened afterwards, while Ctrl + Space keeps working; see Snippet Editor settings . AI candidates — When an AI profile is configured, every list also asks the model for up to five completions. They are marked with a ✦ star, sort after the local entries, and join the open list as they arrive — the local entries are usable in the meantime, and the status line reports the request and how many candidates it added. The list's selection jumps back to its first entry when they arrive. While another AI action of the editor is running, the list shows only the local candidates. Auto AI Complete — A toggle in the AI Code menu that turns on ghost text: when you pause for a moment with the cursor at the end of a non-empty line, the model's continuation appears greyed out after the cursor. Tab accepts it, Esc dismisses it, and Alt + ] / Alt + [ step through the alternatives (the physical ] and [ keys of a US layout, whatever they print on yours). Off by default and remembered only for the current editor session; a data notice asks for your consent once per application start, not once per editor. While a ghost-text request runs, the hint bar shows it with a Cancel button; the list's own AI request reports in the status line only. Language services — For JavaScript, TypeScript, JSON, CSS and HTML, Monaco's own language suggestions appear in the same list beside korTTY's candidates. The first list in such a snippet can take a moment while the language service starts. A completion request never blocks the editor's other AI actions. Starting one of them — Full code analysis , an improvement, a security check, a diagram — cancels a running completion request, stops the ghost-text timer and hides visible ghost text, so the two cannot overlap. A completion request that has not answered after 30 seconds is abandoned, whatever timeout the profile allows. An accepted AI entry or ghost text counts as an AI change, so the ↺ button switches back to the text before it; local entries are ordinary edits. AI Code actions The AI Code menu groups the actions that read or rewrite the code itself: AI Complete / Auto AI Complete — AI Complete opens the completion list at the cursor, the same list Shift + Tab opens; Auto AI Complete toggles ghost text after a typing pause (see AI Code completions above). Full code analysis — Opens a rich analysis window: a plain-language summary of what the script does, its external dependencies, categorized improvement suggestions you can tick and apply, and an auto-generated flow diagram. See Full code analysis below. Improve readability / robustness / performance — Rewrites the selected code region toward one goal without unrelated changes. Before Improve robustness starts, it displays two optional panels for additional rules: Hardening options , Input hardening . If at least one rule is active, KorTTY rewrites the complete snippet so it can apply global prologue or epilogue changes. Optimize code comments — Comments the selected code region: the AI inserts explanations of what the code does and why directly above or beside the lines they belong to, using the language's own comment syntax, and replaces outdated or misleading comments. Executable code stays untouched. The comments are written in the editor's Text language . Available from the AI Code menu and from the editor's right-click menu on a selection. Custom improvement… — Rewrites the selected code region following a free-text instruction you type. It displays the same two optional rule panels: Hardening options , Input hardening . As with Improve robustness , KorTTY rewrites the complete snippet when any hardening rule is active. Migrate into one language… — Rewrites a mixed-language snippet so that all of it is written in one target language. KorTTY detects the mix locally and only offers what actually applies; an orchestration format (Azure DevOps pipeline, GitHub Actions, Jenkinsfile, Ansible, …) embeds shell by design and is therefore never migrated as a whole — only its script steps can be unified, and a platform conversion is offered but never preselected. See Language unification . Security Check — Generates a security report. Select findings to fix; KorTTY applies them with a before/after preview that highlights what changed and why. See Security Check below. Diagram — Opens the snippet's saved diagrams, or generates the first logical-structure flowchart when none exists yet. A snippet can store several diagrams across five families — logical structure, sequence, state, class, and ER. See Mermaid diagrams below. The editor context menu also offers AI Assistant… , which opens an instruction dialog for the current cursor position: KorTTY sends the full snippet, cursor offset, line, column, and your instruction to the configured AI profile and shows the result as a before/after preview. The context menu additionally has a Generate diagram submenu listing the five diagram families. With a code selection its label switches to Generate diagram from selection and the chosen family diagrams only the selected lines — the selection is snapped to whole lines and the resulting diagram remembers its line range. Without a selection the whole snippet is diagrammed. Every generated diagram is added to the snippet's saved diagrams; see Mermaid diagrams . Readability, performance, comment optimization, and a custom improvement rewrite only the selected region when both hardening panels have no selected rules, so select a code region first . Improve robustness and Custom improvement instead rewrite the complete snippet whenever a classic hardening option or supported Input hardening guard is active, because those rules may need the prologue and epilogue. Every result is shown as a before/after preview (the Review AI change window) before anything is applied; an incomplete full-script response with an omission marker is refused. Warning Snippet AI actions send the current snippet content, selection or cursor metadata, and prompt instructions to the configured default AI profile (or, for Security Check, the dedicated security-check profile). Eligible actions can additionally send enabled, configurable AI Skills; the source-only diagram request sends no configurable library skill and instead always carries korTTY's compact built-in action skill for the chosen diagram family. Snippet AI actions do not enable internet tools, even when the selected profile has internet access. Code completion is more restrained: the local candidates in the completion list are computed by korTTY itself and never leave the machine, and an AI completion request sends only the code around the cursor — up to 6,000 characters before it and 1,500 after it — together with the names of the snippet's arrays, variables and functions, rather than the whole snippet. Completion requests skip knowledge stores; locally matched AI Skills still apply. Auto AI Complete repeats that request after every typing pause while it is on, so leave it off for sensitive snippets unless you trust the configured endpoint. Full code analysis Full code analysis opens a dedicated window that examines the whole snippet at once and offers concrete improvements you can apply. The window is non-modal — you can keep editing the snippet while it stays open — and its title bar shows the script's file name so you can tell several analyses apart. The snippet editor's own title bar likewise shows the name of the file you are editing. When you apply a selection, this analysis window becomes the anchor of a three-part working surface: the narrow AI-processing window docks to one side of it and the review preview to the other, and all three stay open until you close the analysis window. The report and the flow diagram are generated by two separate AI requests : the analysis request returns the summary, dependencies and improvements, and as soon as the window opens the diagram pane starts its own dedicated diagram request — the same focused request Regenerate uses — while a spinner is shown. Each request carries one line-numbered copy of the script; neither repeats a second raw-script copy in the same prompt. The analysis request always sends the complete script. The diagram request sends it complete up to 400 lines and, above that, a condensed structural outline instead: its definitions and top-level flow with the elided runs marked and the original line numbers kept, so code references still point at real snippet lines. Sending every line of a four-thousand-line script made models transcribe it rather than summarize it. Over OpenAI-compatible HTTP, korTTY constrains the initial analysis report to a strict summary/dependencies/improvements JSON schema, and the diagram answer to its own title/mermaid/codeReferences schema — the diagram grammar requires quoted node labels, and an endpoint that honors the schema cannot lose their escaping. Both retry once without the schema only when the endpoint explicitly rejects structured output; malformed model output is not retried, and a diagram is never re-requested at all. The diagram request is deliberately compact and source-grounded: it uses the fixed Mermaid schema, the script and label language, and an immutable built-in Mermaid action skill, but it does not add configurable library skills or knowledge-store excerpts. That required skill maps runtime control flow rather than declaration order, groups repeated same-purpose work, retains real decisions, error paths and loop exits, and requires every node to lie on a connected path from start to stop with an exact source range. If the none Reasoning value is available and the active profile has a fixed model selection, korTTY automatically sets that value for this request only; otherwise it keeps the profile's configured value, and the stored profile is never changed. An Auto profile is not overridden from previously discovered capabilities because its loaded model can change; an explicitly configured none value still applies. OpenAI-compatible HTTP, LM Studio native, and embedded llama.cpp/MLX transports cap the diagram response at 32,768 output tokens; Anthropic retains its separate provider cap. That cap covers the whole completion, so it deliberately leaves room for a thinking model's hidden reasoning: the diagram JSON itself is far smaller, but a model that reasons can otherwise spend the entire budget before emitting a single character. For embedded llama.cpp/MLX, korTTY does not repeat a response that is empty or contains only the model's reasoning. The automatic diagram request can be turned off with the Generate automatically checkbox in the diagram pane's header. Details are under Right — flow diagram below. Keeping the diagram out of the analysis request produces markedly more faithful flowcharts, especially with local models, and the report is readable while the diagram is still loading. Clicking Re-run repeats the analysis with the selected profile and configurable AI Skills and starts the separate dedicated diagram request with the same mandatory Mermaid skill. Starting another generation or closing the diagram view cancels its superseded client request. Before accepting a fresh AI result, korTTY rejects disconnected nodes, backward terminal paths, incomplete decision branches, and a runaway node count — the diagram request states the snippet's node limit to the model (12 for snippets up to 200 lines, growing linearly to 24 at 1,000 lines or more), and the validator rejects a diagram only when it exceeds three times that limit (36, or 72), so an over-drawn summary keeps its diagram while a transcription of the script does not. A node declared more than once — a model that draws every node as a box in a chain and again as a decision on its own line — keeps its first declaration, and counts as one node when korTTY compares what the model drew with what the repairs kept. That comparison covers nodes and edges alike: a diagram whose repairs drop more than half of either is refused for the local fallback, because a model that draws branches the strict dialect cannot show keeps most of its nodes while what would be rendered is a straight chain of them. A node without a valid source mapping no longer discards the diagram; it simply has no hover reference, and the log names the unmapped nodes. A rejected diagram answer is kept whole under ai-answers/ in the log directory , like an unusable apply answer: the rejection names one broken rule, and whether the grammar could learn the shorthand the model wrote is only decidable on the whole answer. The general renderer stays backward-compatible with safe diagrams saved by older korTTY versions. If the answer's JSON envelope cannot be read — typically because the model wrote korTTY's required quoted labels into the JSON string without escaping them — the diagram is recovered from the answer's own text (inline <think> reasoning is skipped first), validated exactly like any other, and used without asking the model again. A model that escapes the mermaid value's line breaks twice — valid JSON whose value decodes to a single line of literal \\n sequences — has those line breaks restored before the grammar reads it, wherever a diagram is validated, rendered or saved: no diagram korTTY accepts is a single line, so the sequences can only be the line breaks, and a source that already carries real line breaks is left exactly as written. The restricted flowchart grammar reads the Mermaid that models actually write and turns it into its own strict dialect before rendering or saving: nodes declared inline in an edge ( start_1([\"Start\"]) --> n1[\"Print header\"] ), chained edges ( a --> b --> c ), labels as -->|yes| or -- yes --> , the :::class shorthand, unquoted labels and Mermaid's other node shapes (read as actions), plain %% comment lines, and the slips models make — a label whose pipe became a bracket, a shape closed one character early. What the shorthand cannot express is repaired where the intent is unambiguous: a node the model forgot to class gets the neutral default, a second class or a second declaration for the same id is ignored, a decision with one labelled branch gets the complementary label of the response language, a diamond with a single exit is an action, an id that only appears in an edge becomes a node, a stray terminal becomes an action and every dead end continues to stop_1 , a start_1 that is missing or never connected is wired to the flow's real entry (the node nothing leads to that reaches most of the diagram), a statement written with a stray class prefix is still read, and a node nothing leads to is pruned. The repairs may trim a diagram but never hollow it out: a result that kept fewer than half of the nodes the model drew, or none, is rejected with that reason. A line it still cannot accept is quoted in the rejection reason, and a diagram that fans out from an action into parallel branches is still rejected, because the dialect has no way to draw it faithfully. classDef , style and linkStyle lines in a fresh answer are dropped rather than rejected — korTTY styles the four semantic classes itself, and a model told to assign them often defines them with colors as well; the log counts what was removed, and a saved diagram is still validated unchanged. If the provider reports that the diagram response was cut off at its cap, the request fails or no safe, usable Mermaid source is returned, korTTY keeps the analysis and shows its deterministic local fallback diagram without silently sending another request. The fallback is labelled: a notice above the diagram says that the AI diagram was rejected and why (for example that it declared more nodes than the limit allows), and the same reason is written to the log, so a fallback is never mistaken for an AI result; the fallback also recognizes indented conditional blocks in common scripting languages. The summary, dependencies, improvement descriptions and diagram labels use the current korTTY interface language. The separate Text language becomes relevant only after you click Apply selected , because that action returns a full replacement: by default the rewritten script keeps the language its comments and messages are already written in, and only a language explicitly chosen there converts them. The analysis window carries its own Text language selector, pre-set from the editor's choice, so the decision sits where you tick the improvements. Apply selected and the equivalent security-fix apply action automatically request none Reasoning only when that value is available and the profile has a fixed model selection; an Auto profile keeps its configured or provider-default behavior unless none was explicitly configured. The saved profile remains unchanged. This prevents a reasoning model from consuming the bounded replacement budget before it emits the machine-parsed script. If a provider nevertheless reaches the output limit with no visible answer, korTTY records the usage, reports the localized output-limit failure and leaves the editor unchanged instead of misreporting an ordinary empty response or retrying the request. A toolbar runs along the top of the window, the report and flow diagram fill the two panes below it, and a script-header selector plus a collapsible hardening panel sit in the footer. The window is freely resizable, and korTTY remembers its position and size across sessions — including when Re-run replaces the window with a fresh analysis. During Apply selected , the AI-processing window and the review preview dock to opposite edges of it, match its height, and follow it when you move or resize it. If there is no room beside the analysis window where it currently sits, korTTY slides it far enough to open both sides up, and narrows it only when the three genuinely do not fit side by side on that screen; the original width comes back when the docked windows go. Each docked window stays freely resizable and korTTY remembers the width you give it for the next run. Drag a docked window away and it detaches, keeping the position you gave it; Arrange windows in the processing window lays all three out across the screen again. Toolbar: Select all improvements — The first control at the far left ticks or unticks all Security, Optimization and Design improvements at once. Extra spacing clearly separates this bulk action from the following Profile: indicator. This control never changes any dependency selection. Profile in use — The name of the AI profile the analysis ran with is shown beside this checkbox (for the default profile its actual name is shown, e.g. Profile: LM Studio — not just \"Default profile\"), so you can always tell which model produced the report. AI skills — When AI Skills are configured, a row shows which skills were included and lets you change them; see AI skills for this analysis below. Re-run — A transient AI-profile picker plus a Re-run button repeat the analysis with the chosen profile and your current AI-skill selection. The picker resets to the default when the window is reopened. A− / A+ — Adjust the reading font size (remembered across sessions). Copy — Copy the summary, improvements and dependencies to the clipboard as plain text. Export — Save the whole report (including the diagram) as a file; see Export the report below. Left — analysis and improvements: Summary — A short, plain-language description of what the script does. It is a description, not a pickable item, so it is shown as a plain block without a selection accent. Improvements — Suggestions grouped into Security , Optimization and Design sections. Each section title carries a colour-coded icon and a count — a padlock for Security , a gauge for Optimization , stacked layers for Design , and a module hexagon for Dependencies — and each suggestion has a severity badge, an explanation, and a concrete recommendation. Tick the ones you want; use Select all improvements to toggle every improvement at once. Empty sections are hidden. Dependencies — External programs, scripts or services the snippet relies on, each with its Purpose and a Reduce/replace suggestion. Tick each dependency independently to have its suggestion applied too; Select all improvements leaves these checkboxes unchanged. Right — flow diagram: An auto-generated Mermaid flowchart from a dedicated diagram-only request renders while a spinner is shown, then fills the pane. It carries the full diagram toolbar: zoom − / slider / + / Fit , Save SVG / Save PNG , Copy image / Copy Mermaid , a Dark mode control and a Background colour picker (both remembered), and Regenerate . Regenerate deliberately sends one new dedicated source-only diagram request using the analysis window's active profile; configurable AI Skills and knowledge-store excerpts remain reserved for the analysis request, while the required built-in Mermaid action skill is always included. See Diagram appearance below. Hover code references — Moving the mouse over a diagram node shows the matching lines from the snippet, so you can trace each step back to the code — the same behaviour as the standalone Diagram window. Generate automatically — A checkbox in the pane's header controls whether the diagram request starts on its own when the window opens. Untick it to skip the automatic AI request entirely — the pane then shows a hint instead, and Regenerate remains the manual way to request the diagram. Ticking the box while the window is open fetches the diagram immediately. The choice is remembered across sessions (default: on) and does not affect the standalone Diagram window, which renders saved diagrams without an AI request. AI skills for this analysis: When AI Skills are configured, a row at the top of the window shows exactly which skills were included in the analysis, as chips, with an (auto-selected) or (manual) badge: Auto-selected — korTTY pre-selects the skills relevant to the snippet by matching each skill's tags, name and description against the snippet's language and content, and includes at most the two highest-scoring ordinary matches in the analysis. Explicitly pinned or connection-assigned skills remain outside that automatic limit. This is why the badge reads (auto-selected) on the first run. Manual selection — Click Select… to open a searchable picker : type in the search field to filter your saved skills by name, description or tags, then tick or untick the skills you want. As soon as you change the set, the badge switches to (manual) and korTTY keeps your choice instead of auto-selecting. Changing the skills does not re-analyse immediately — the new set is applied to the report request on the next Re-run . That explicit snippet selection, together with any skills assigned to the active connection, is used as an allowlist: korTTY does not run global relevance detection again or append other skills. Skills you include here are sent to the analysis regardless of each skill's configured target ; the separate diagram request intentionally omits those configurable skills and always uses its own immutable Mermaid action skill instead. The row appears only when at least one configurable AI Skill is enabled. Hardening options: At the bottom, a collapsible Hardening options panel lets you attach production-quality techniques (strict mode, error traps, meaningful exit codes, logging, idempotency, --dry-run , --help , and more) to the fixes that get applied. The panel title includes the number of currently ticked options — for example Hardening options (11) — and korTTY remembers whether you left the panel open or closed and restores that state the next time the window opens. See Hardening options for what each option means and how it is applied. Input hardening: Below it, a second collapsible Input hardening panel asks the AI to build an input-validation guard block into the script when the fixes are applied: parameter allowlists and length limits, file format checks, a maximum input-file size controlled by an adjustable MAX_FILE_SIZE variable, security warnings in the script's own log, and a FORCE=1 / --force override. The size check uses metadata before file content is read, and 0 means unlimited. It is strictly opt-in — the master check box starts unticked — and its title counts only the sub-options that are effectively active. The panel is disabled for YAML/YML/Ansible snippets because a script-level guard does not apply to these declarative formats. See Input hardening for the full guard contract. Script header: A Script header selector lets you prepend one of your saved Script-Header snippets (from the fixed Script-Header category ) to the code when you apply the analysis. Pick a header — or leave it on No header (the default) — and its content, with variables substituted, is inserted at the top of the snippet, after an existing shebang / lead line, as part of the same change. Apply selected: When you click Apply selected in the report, korTTY keeps the analysis window open and processes the ticked improvements, dependency suggestions and hardening options as an atomic sequence. A separate narrow AI processing window appears docked beside it. A docked window whose content does not cover it — which shows as a blank band beside the preview — is corrected whenever its size changes, not only in the second after it opens: content that did not follow its window is laid out again, and a scene that lags the window (the window at the width the dock set, the scene still at the width the platform last reported) is brought up to date by asking the platform for the size again; the log names the sizes either way. The docked preview also no longer applies its stored free geometry on opening — the dock places it in that same moment, and two sizes written to one window at once is how a scene ends up narrower than its window. Two independent progress bars at the top track Improvements and Code hardening , followed by elapsed wall-clock time and cumulative token usage reported by the provider; when a provider supplies no usage data, the value is explicitly shown as not reported rather than estimated. The checklist lists improvements first, then classic and Input-hardening requirements. Each analyzed improvement or dependency row places the report's matching colour-coded category icon directly after its ID. Descriptions in this compact checklist are limited to three lines with an ellipsis. The complete descriptions remain visible in the analysis report beside it. The checklist no longer repeats category or severity text on the right; severity remains available in the analysis report, while hardening requirements need no redundant category label because they are already grouped under Code hardening . Pending entries use a neutral marker, all entries in the active provider batch are highlighted as running, a repair attempt is marked separately, each completed entry receives a green checkmark on its right, and the failed entry is marked if the sequence stops. If every entry completed and only the final verification rejected the combined result, the header names that instead of pointing at a marked entry. When the run ends — completed, failed or cancelled — the processing window does not close itself. It adds a run summary : the final duration, the token usage split into prompt, completion and total, the AI profile the run used, how many work items completed, and the number of repair attempts if there were any. Copy summary puts the same figures on the clipboard, and Show preview again reopens the review window if you closed it before deciding. Closing the analysis window cancels its running apply task and closes the docked windows with it; after a failure the report stays open so you can inspect the stopped step and retry the selection. korTTY batches selected analysis items and dependencies into apply stages of up to three items each — six in edit mode, where the three-item limit that keeps a whole-script answer finishable does not apply — then handles classic hardening and Input hardening separately in batches of at most three mandatory requirements. Every stage sends the whole script, so the number of stages is what an apply costs in prompt tokens and time. In edit mode the line-numbered script comes before everything that is specific to the stage or the attempt — the items, the requirements, the repair paragraphs and the line count — so every request of a run shares one prefix: system prompt, contract, language lines, and the script up to the first line the previous stage changed. An endpoint with a prefix cache serves that prefix without processing it again (MiniMax reports it as cached tokens; LM Studio and llama.cpp reuse their KV cache), which is time on a local model and money on a metered one; a repeated attempt with unchanged input shares the whole script. Measured before this order, consecutive stages shared 128 tokens, because the stage's items sat in front of the 62,000 tokens of script. Every stage receives the complete result of the previous stage and must preserve its existing behaviour. For a snippet of up to 400 lines each stage returns the complete rewritten script. Above that it works in edit mode : the stage receives the line-numbered snippet and returns only the changed regions — edits with a 1-based startLine / endLine range and the replacement lines — which korTTY applies locally before running exactly the same verification as for a returned script. Replacement lines are taken exactly as returned — their indentation, blank lines and a repeated closing keyword are the code, not noise — and an edit with an entry that is not a string is left to the repair round. A 4,000-line script returned complete is around 60,000 output tokens, at the model's completion cap and twelve minutes per stage, and one lost quotation mark in that much JSON lost all of it; the changed regions are a few hundred lines at most. An edit whose range starts outside the snippet, reaches more than one line past its end, overlaps an earlier edit, or is hollow — a range of three or more lines whose replacement is nothing but its own unchanged first line, the signature of a model that emitted one entry and stopped; a range shortened by its last lines is a deletion and is applied — is left out with a note in the log while the trustworthy edits are applied. So that the repair round really asks for what a dropped edit was meant to do, a changes entry whose anchor line comes from a dropped edit and from no applied one is ignored, which leaves its analysis id unechoed. An edit-mode stage whose answer ran into its output-token limit gets one second attempt as well, asking for the changed regions only: such an answer holds the changed regions alone, so the limit is a runaway answer rather than the stage's real size, and how long an answer the model writes varies per attempt. A whole-file answer still fails there, since for a large script the limit is the real constraint. A stage none of whose edits can be applied, or whose edits collapse the script — seen live as two edits \"covering\" 1,199 lines with an omission marker in place of the code; in edit mode the omission-marker check looks at each edit (a range of three or more lines replaced by nothing but such a comment) rather than at the whole script, since a comment that merely mentions unchanged code among real lines is just a comment — gets one second attempt whose request says what was wrong with the first answer (the answer is archived like an unusable one), the same single second chance a collapsed whole-file answer gets, and is refused like an incomplete script if that fails too. An applied edit that shrinks a region of a hundred lines or more to less than a tenth is named in the log. An edit-mode answer whose JSON does not parse is read tolerantly before it is requested again. Live answers from MiniMax-M3 showed what actually breaks: a quotation mark inside a code line left unescaped, an escape JSON does not know such as \\$ , a missing ] before an edit's closing brace or a missing } between two edits, a trailing comma, a newline inside an entry, and the summary written as a nested object. The recovery reads every edit's line range and replacement lines from the text itself. When every replacement line sits on its own line the line end is the boundary and nothing can mislead it; in a compact one-line answer the boundary is the answer's own delimiter — \",\" or \", \" , whichever style the model used for its keys — and a bracket only closes the array when structure, not code, follows it. Whatever the read cannot be certain about fails it: a compact entry left with an odd number of raw quotes (the split fell inside a quoted pair — unless the entry plus one quote is a line of the snippet, the signature of a closing quote the model swallowed, as in res=\"\" written as res=\" ; a code line ending in a quote whose escaped last quote the model fused with the closing quote, as in body=\"{\" written as body=\\\"{\\\" followed by the next entry, is read the same way when the snippet knows the line, and so is a known line in which the model escaped one quote and left the others raw). The transport judges an edit answer with the same snippet oracle before deciding on a schemaless retry, so an answer the stage can read is never requested a second time, a bracket followed by anything but stacked closers and the next key or edit, a scan that runs into the answer's own keys (the array was never closed), a raw quote pair in the other delimiter style, a wrapped line holding several entries, a bare } line inside an entry that a raw line break split, and two entries of which one plus the delimiter is part of a line of the snippet (a code line such as awk -F\",\" that was split in two, even when the model also changed it). It is all or nothing: every edit in the answer must be readable, each with exactly one endLine , or the answer goes the retry route rather than applying half of what the model meant. The log says when edits were recovered this way. The analysis answer gets its own, simpler repair: its fields are prose, where a quote that is not followed by a JSON terminator is content, so a raw quote pair inside a title, detail or suggestion — which once cost a complete 24,000-character analysis of a 4,000-line script — is escaped and the answer read; an analysis korTTY still cannot use is archived like an unusable edit answer. The contract deliberately does not ask the model to put every entry on its own line: tested live, MiniMax-M3 read that as one entry per edit and answered with ranges replaced by their first line, or with hundreds of one-line edits up to the output cap. On endpoints that accept response_format and ignore it, such as MiniMax, the JSON-only reminder that only the schemaless retry used to carry is sent with the first attempt as well. When the retry does happen, the log names how the first answer failed — an unparsable JSON-shaped answer, one the model abbreviated itself with ... or etc , one that was cut off, a fenced block, prose — together with the place the JSON broke (Gson's line, column and path such as $.edits[3].replacementLines[7] , and the text around it), and the complete answer is kept as a file under ai-answers/ in the log directory (the twenty most recent, and never longer than the log retention days, since they hold script regions in plain text), named in the same log line, so a failed stage can be diagnosed from a report without debug logging. A streamed answer that the endpoint never closed — no finish_reason and no [DONE] — is logged as such, with the raw stream kept in the same place, because an answer that stops mid-string a few thousand tokens in reads as \"cut off\" and only the raw stream tells whether the model stopped or the connection did. The request log shows the prompt tokens an endpoint served from its prefix cache when it reports them. The log names how many edits covered how many lines, and the output cap for an edit-mode stage is 32,768 tokens so a model that transcribes the file anyway is stopped early. Every stage also opens with one log line — its number, phase, analysis ids, requirement ids, line count and mode — and a stage whose result is kept but gets the repair attempt for work it did not verify says so, naming the requirements not verified, the earlier requirements it dropped, or the analysis ids it did not echo, so a log alone maps every request to its stage and to the reason for a fourth request in a three-stage run. An improvement the model did not echo in changes[].finding opens that repair round; a dependency suggestion it did not echo does not — a reduce/replace suggestion (\"consider consolidating\", \"prefer dig\") may rightly leave the code alone, and a model that changed nothing seldom echoes the id, so asking again cost two full requests in a 47-item run for nothing the items needed. Such a stage is accepted with the unechoed suggestions named in the log. When another cause does open the repair round, the suggestions it did not echo are named in that request as suggestions — implement where it applies, otherwise leave the code and do not echo — not as items to apply. A suggestion whose edit was dropped (hollow, overlapping, out of range) still opens the round like an improvement, since the model did try to change it; and an id that names an improvement counts as one even when a suggestion shares it, because the ids are the model's own. Intermediate scripts are never inserted into the editor. If a stage's response stream is dropped mid-answer by the connection, korTTY retries that stage once automatically — distinct from the fragment-repair attempt below — before giving up. If a stage still fails, is cut off or returns an incomplete replacement — or you stop the run with the editor's cancel button — before the first stage has completed, korTTY discards the sequence and leaves the editor unchanged. After at least one completed stage, the same aborts open an Apply interrupted prompt instead: Continue remaining stages resumes the run at the aborted stage and repeats it while keeping the completed stages' work — a fresh processing window opens with those stages already checked off, and the final cumulative verification still covers the whole selection; Preview partial result opens the completed stages' combined rewrite in the usual review preview, titled Apply improvements — review partial changes — each completed stage's requirements were already verified when that stage finished, and only the final cumulative re-check is skipped, because requirements of stages that never ran are missing by definition, while the degenerate-replacement guard still applies before the preview opens; Discard (also the prompt's Esc/close action) throws everything away as before. If a resumed run aborts again, the prompt reappears with the newer state. When every stage completed and only the final cumulative verification failed, the prompt omits the resume choice, since re-running zero remaining stages would fail identically. Closing the analysis window or the snippet editor mid-run remains a deliberate discard and never shows the prompt. A fully completed sequence opens the single Apply improvements — review changes window with the final cumulatively verified script, docked opposite the processing window; the analysis window, the completed checklist and the run summary all stay open after the preview is dismissed, so what the run did and cost can still be read. Closing the analysis window is what clears the group. This uses more model calls and can consume more total input tokens than one oversized request, but each individual task is substantially smaller for local models. Every selected classic and input-hardening rule keeps one stable, separately numbered mandatory identifier across the stages. Each stage confirms its completed identifiers in one compact list instead of repeating a full change explanation for every rule. The final validation checks the cumulative identifier set, while explicit flags and guard literals such as --dry-run , --yes , --help , --verbose , MAX_FILE_SIZE , FORCE , --force , and SECURITY: must still occur in the final code when their rules are active. Because every stage rewrites the whole script, each one is additionally checked against the literals of the rules earlier stages already delivered: a stage that removes earlier hardening work while implementing its own is rejected on the spot and gets its one repair attempt, which names exactly what to restore. That keeps such a regression from surfacing only in the final validation, where the remaining stages could no longer be resumed. Every stage returns the complete script as a JSON array with one source line per entry, avoiding one large escape-sensitive JSON string. Over OpenAI-compatible HTTP, a strict response schema also requires a conservative minimum number of returned source lines. korTTY repeats a stage without that schema only when the endpoint explicitly rejects the structured-output capability. If structured output is unavailable and a local model emits source escapes such as \\s without valid JSON escaping, the compatibility parser preserves those code characters and still verifies the mandatory checklist. Every stage requires one complete rewritten script, including every code section that needs no intentional change copied from its input. Every stage carries the same Text language contract: by default the script's own prose language, which the stage must preserve rather than translate, or a language you chose explicitly, into which it must convert the text. OpenAI-compatible HTTP, LM Studio native, and embedded llama.cpp/MLX transports choose a per-stage completion safety ceiling from 32,768 to 65,536 tokens based on the current source size. Anthropic retains its separate provider cap. This ceiling prevents unbounded output, but it is not a capacity guarantee for arbitrarily large scripts: a very large full-script replacement can be refused when the provider reports truncation. Any response that introduces an omission marker such as rest unchanged , collapses a substantial script into a short fragment, or otherwise fails to contain the complete replacement is rejected before the next stage or preview. A short non-truncated fragment receives exactly one repair attempt for the same stage, and the progress window identifies that retry. If the repair answer is also bad, korTTY aborts the sequence. The code in the editor remains unchanged throughout. A valid final result shows the original and rewritten script side by side, with changed lines highlighted and per-change reasons, exactly like the Security-Check review below. The editor remains unchanged until you confirm Apply change in this preview. Any chosen Script header is prepended to the result before it is shown. A header on its own — with no improvements, dependencies or hardening ticked — is inserted directly, without an AI round-trip, and still shown as a before/after preview first. At most one repair attempt is also allowed when a complete response fails to confirm every mandatory identifier and required literal. korTTY uses that complete returned script as the repair input, names the identifiers that still need verification or implementation, and asks the model to preserve every other change. If the repair answer fails again, the localized status names the still-missing requirements — each identifier with the rule it stands for, so the option to reconsider is obvious — and the editor remains unchanged. Export the report: The Export button saves the full report — summary, categorized improvements, dependencies and the flow diagram — as a self-contained file in an attractive, print-friendly design. The export header records the script name, the AI profile used, the date, and the AI skills that were included: PDF — A paginated document with the diagram embedded as an image. HTML — A single self-contained web page (the diagram is embedded inline) that opens in any browser. Markdown — A .md file, with the diagram saved next to it as a PNG. Security Check The Security Check report window lists each finding with a colour-coded severity badge (findings are sorted most-severe first). From this window you can: Adjust the reading font size with A− / A+ (remembered across sessions). Copy all findings to the clipboard. Select all findings at once, then apply the selected fixes. Choose a dedicated Security profile — the AI profile used for security checks. The choice is remembered permanently and is also available in Configuration → Global Settings → AI ; leave it on Use default profile to reuse the default. Changing it takes effect immediately. Re-run check to repeat the review with the newly selected profile. When you apply fixes, the Review security fixes window shows the original and corrected code side by side. Changed lines are highlighted automatically and carry a marker in the margin. Hover anywhere in a changed block to see which finding(s) it addresses — for example S1 , or S1 + S2 when one block covers two findings — together with the reason for the change. Hover matching tolerates re-indented or case-shifted lines, and a reason whose anchor line cannot be found at all is attached to the remaining changed blocks in order, so explanations no longer go missing from the diff. The same explanations are also listed as cards below the diff: each card carries the finding's badge and colour-coded category icon (the same icons as the analysis sections) plus the line range it affects on the corrected side (for example Lines 23–40 ), so the reasoning stays visible even when a marker cannot be placed. A Highlight picker below the diff narrows the review to a single finding: pick SEC-1 , a hardening requirement, or any other listed id and only that finding's places keep their marker and get a coloured line background, while every other changed block is muted to a neutral tint. The window scrolls to the first place and the explanation cards below shrink to that finding. The ◀ / ▶ buttons beside the picker walk the list one finding at a time — from All changes they enter it at either end, and the ends wrap into each other — so a report can be worked through without opening the dropdown for every step. All changes restores the full colouring. The picker appears once at least two findings carry a reason. Muted blocks stay visible and keep their change markers — this window is the approval step before the editor is touched, so a real change is never made invisible. The summary at the top of the window scrolls inside its own pane and sits above a draggable divider: a staged apply's summary runs to one paragraph per stage, and the divider decides how much of the window it may use. The window remembers the size and position you gave it, and the divider once you move it yourself — until then it keeps following the summary's own height, so a one-line summary never claims the room a multi-paragraph one needs. When it opens docked beside a Full code analysis run it remembers only its width, because the dock decides the rest. This review window does not block the rest of korTTY: your terminal sessions stay usable while you read the diff, and so does the processing window beside it. Because the snippet can therefore be edited while the review is open, korTTY checks before applying that the editor still holds the text the result was produced from — if it changed, nothing is applied and you are told to run the analysis again rather than have a stale full replacement overwrite what you typed. The preview font size can be zoomed and is remembered across sessions. The same review window (and its explanation cards) is used when applying Full code analysis improvements. AI profile, re-run and zoom The AI-code report windows (Full code analysis, Security Check, the technical-description and alternative-solution dialogs, and the change-review diff) share a small toolbar: AI profile — Pick a different AI profile for the next run of that window. The choice is transient: it resets to the default profile when the window is reopened. (Security Check keeps its own permanently remembered Security profile instead.) Re-run — Repeat the request with the currently selected profile. A− / A+ — Adjust the reading or preview font size; the chosen size is remembered across sessions, separately per window type. Copy — Copy the report or content to the clipboard. AI skills When AI Skills are configured, the snippet editor shows an AI skills picker. Skills relevant to the snippet's language are pre-selected automatically, and any skill you tick here is applied to skill-relevant AI-code actions such as completion, analysis, improvement and security checks regardless of the skill's configured target. The fixed-contract Diagram action intentionally omits those configurable library skills to keep the source-grounded request small and predictable; korTTY always supplies its separate compact action skill for the requested diagram family, which is internal and therefore does not appear in the picker or count toward the 39 configurable built-in AI Skills. The picker appears only when at least one configurable AI Skill is enabled. The Full code analysis window surfaces this same selection as a row of chips — labelled (auto-selected) or (manual) — and lets you refine it just for that analysis through a searchable picker. Changes made there apply after you next click Re-run . See Full code analysis . Text correction and translation For selection-based text correction and translation, KorTTY only rewrites editable comment text, string literals, and user-facing text segments. A selection may begin or end inside such a segment: KorTTY uses the surrounding snippet to recognize the selected words and replaces only the overlapping text. It does not rewrite logical code structure. Technical descriptions If text is selected, the AI describes only that region. If nothing is selected, the AI describes the whole snippet. The description dialog lets you: Copy the generated description Format it with the comment syntax of the current snippet language Insert it into the snippet above the selected code or at the top Alternative solutions Right-click a selected code region and choose Alternative solution to: Request multiple alternative implementations (up to the configured limit) Add a 3-line field for additional instructions Reload and regenerate new alternatives Zoom an individual preview to the full dialog area Apply exactly the originally selected code when ready Mermaid diagrams Mermaid diagrams are stored with the snippet, and a snippet can keep several of them side by side. If the snippet content changes after diagram generation, KorTTY marks a diagram as possibly outdated and offers regeneration. Diagram families: Five families can be generated, each restricted to a compact, safe dialect: Logical structure ( flowchart TD — the control-flow view and the default), Sequence diagram ( sequenceDiagram — who talks to whom at runtime: the script, remote hosts, external commands, APIs), State diagram ( stateDiagram-v2 — observable states and transitions such as connection lifecycles and retry logic), Class diagram ( classDiagram — types, members and relations declared in the code), and ER diagram ( erDiagram — data entities and relations the code implies, such as SQL tables). Saved diagrams dialog: The list on the left shows every stored diagram with its family, title and — for a selection-scoped diagram — its line range. New diagram is a menu offering the five families, Regenerate re-runs the selected diagram with its original family and scope, and Delete removes the selected diagram after a confirmation. Diagrams are persisted when the snippet is saved. Diagrams from a selection: The editor context menu's Generate diagram from selection entries diagram only the selected lines. The selection is snapped to whole lines, the AI sees just that region, and the stored diagram remembers the line range: its code references point at the absolute snippet lines, and Regenerate re-reads the same lines from the current content. Generation: Logical-structure diagrams use stable node IDs with the semantic classes setup , work , success , and failure , complete decisions with localized yes / no edge labels, and a source mapping from every node to exact snippet lines; the request asks for a complete mapping, and a node the model leaves unmapped keeps its place in the diagram without a hover reference. The other families keep source references optional: entries that name a declared participant, state, class, or entity are stored, incomplete mappings are simply dropped. Every family's AI request carries its own small internal action skill with family-specific quality rules — grouped runtime behavior for flowcharts and sequence diagrams, observable states instead of statements, only declared types and members, no invented schemas. Hover code references and click-to-code navigation are a logical-structure feature. Rendering: Local only — the SHA-256-pinned Mermaid 11.17.2 browser bundle is included with KorTTY and runs in an isolated, lazily created JavaFX WebView. No rendering server, Graphviz installation, Java subprocess, or first-use download is required. Dialog features: Sanitized SVG display with JavaScript disabled, scaling without distortion, zoom slider and buttons with fit, SVG/PNG export, image and Mermaid-source clipboard copy, hover code references, and the shared Diagram appearance controls. Safety and recovery: A diagram whose JSON envelope is unreadable is recovered from the answer text and then validated like any other, so a mis-escaped reply costs no second request. KorTTY rejects frontmatter, directives, links, callbacks, external images/icons, oversized sources and overly complex graphs before any rendering, and validates every generated diagram against its family's restricted grammar with per-family compactness caps (for example at most 12 sequence participants, 12 states, 12 classes, or 12 entities; a flowchart may declare 12 action/decision nodes for a snippet of up to 200 lines and up to 24 for a script of 1,000 lines or more). A rejected whole-snippet logical-structure result falls back to the deterministic local flowchart without another AI request, and the editor's status line names the rejection reason together with the fallback; selection-scoped and non-flowchart generations report the failure and its reason instead, and nothing broken is ever saved. A response that was cut off at the output-token cap is reported as such rather than as an ordinary failure, because the usual cause is a thinking model spending the whole budget on reasoning — a smaller selection or a model that reasons less then helps. Safe restricted diagrams saved by older korTTY versions remain renderable even when they predate these stricter generation-quality rules. Requests are serialized with a 30-second timeout; cancellation or timeout discards the renderer, and the hidden WebView is released after idle time. Upgrade cleanup: Saved legacy diagram entries are discarded without removing their owning snippets or chats. KorTTY also removes its retired diagram-renderer download cache and abandoned temporary render directories without following symbolic links. Diagram appearance Both diagram windows — the standalone Diagram dialog and the Full code analysis flow diagram — share the same zoom controls and two appearance controls; the appearance controls remember their setting across sessions: Zoom — − and + step the scale, the slider between them sets it directly, and Fit returns to the size that shows the whole diagram in the pane. The slider's track runs in powers of two, so the fitted size sits exactly in its middle and one step to either side halves or doubles it; the percentage beside the slider is the resulting scale, which for a large diagram fitted into a small pane starts below 100 %. Dragging the slider redraws the diagram a few times a second rather than on every pixel, so a large flowchart follows the drag without stuttering, and the diagram is always redrawn at the value you let go on. Zoom is deliberately not remembered: every diagram opens fitted to its pane. Dark mode — A Dark mode button with three choices: Auto — follows the operating system's light/dark appearance. When you switch the OS to dark mode the diagram follows on the next render (and when the window regains focus). Light — always light. Dark — always dark. A manual choice is permanent until you change it. Dark mode recolours the whole diagram — a dark canvas, darkened node cards with light text, and light connectors and labels — not just the page margin. - Background — A colour picker for the page/canvas colour in light mode. It applies to the diagram itself and to any exported SVG/PNG. The picker is disabled while dark mode is active, because dark mode drives the appearance. Placeholder variables Snippets can contain placeholder variables that are replaced when you insert the snippet. Built-in variables These variables are automatically replaced: Variable Replacement ${date} Current date in YYYY-MM-DD format ${time} Current time in HH:MM:SS format ${datetime} Current date and time in YYYY-MM-DD HH:MM:SS format ${hostname} Local machine hostname ${username} Current system username ${clipboard} Current clipboard content ${cursor} Cursor position (removed from text; position returned) Custom variables Any ${variableName} not in the built-in list is treated as a custom variable. When you insert the snippet: KorTTY checks the Variable Manager for stored values Variables without stored values prompt for input Sending snippets to the terminal The Snippet Manager can send a selected snippet directly to the active terminal. Send to Terminal Keeps existing behavior Supported script languages are embedded as a terminal one-liner where possible Other snippets use the existing fallback path Send to Terminal with Parameters Opens a dialog for missing ${...} placeholder variables and script arguments Script arguments are entered one per line; empty lines are ignored If you confirm without script arguments, the result is the same as Send to Terminal , but missing placeholder variables can still be filled in Script arguments Supported for Bash/shell, Python, Perl, and Ruby snippets: Arguments are passed individually and shell-quoted Not appended as raw shell text If arguments are entered for unsupported languages, KorTTY shows an information message and sends nothing Terminal display For embedded/base64 one-liners, the terminal shows the KorTTY snippet: ... label instead of echoing the full generated command. Import and export Snippets can be imported and exported in multiple formats. Data format exports Use Export to save selected snippets, or all snippets when nothing is selected. Use Import to merge snippets from a file. Format Extension Use case JSON .json Data interchange, programmatic access XML .xml Structured data, tool integration YAML .yaml Human-readable, configuration-friendly Script-focused exports For script-specific exports, choose: Plain text script files Opens a target-folder chooser Writes one file per snippet Filename comes from the snippet's Name column, including extension Unsafe path characters are sanitized Duplicate names receive a suffix such as script (2).sh ZIP script archive Writes one ZIP containing one script file per snippet Keep the extension from the Name column or force one extension for all files Supported forced extensions: .sh , .py , .pl , .rb , .ps1 , .sql , .txt , or custom ZIP encryption options Unencrypted — Standard ZIP archive AES password-protected — Password-encrypted with AES-256 GPG-encrypted — Creates a .zip.gpg file; requires local gpg command and a usable public key Tip Select two snippets, export them as plain text and confirm the created files use the names from the Name column. Then export the same selection as a ZIP with a forced .txt extension and verify all ZIP entries use .txt . For password export, confirm the ZIP requires the password before extraction. For GPG export, decrypt the .zip.gpg with your local GPG setup and inspect the ZIP entries.","title": "Snippet manager"},{"location": "features/snippets.html#snippet-manager","text": "The Snippet Manager lets you store, organize, and quickly insert reusable code snippets, scripts, and configuration templates. Manage snippets across multiple languages with syntax highlighting, advanced search, AI-assisted editing, placeholder variables, and flexible export options.","title": "Snippet manager"},{"location": "features/snippets.html#overview","text": "The Snippet Manager includes the following features: System (OS) column — A sortable operating-system column for each snippet (Any, Linux, macOS, Windows). Auto-set when a snippet is created via Generate Workflow Script . Sortable columns — All columns (Name, Language, Category, System, Tags) are sortable. Script-Header category — A fixed, non-deletable category containing reusable header templates for workflow-script generation.","title": "Overview"},{"location": "features/snippets.html#opening-the-snippet-manager","text": "Menu: Tools → Snippet Manager Shortcut: Ctrl + Shift + S ( Cmd + Shift + S on macOS)","title": "Opening the Snippet Manager"},{"location": "features/snippets.html#creating-and-editing-snippets","text": "Click Add (or Edit to modify an existing snippet). Fill in the fields: Name — A descriptive name. Code language — Select the programming language (Bash, Python, Java, JavaScript, TypeScript, SQL, XML, JSON, YAML, and more). Enables syntax highlighting. The add ( + ) button next to the list adds a language that is not offered yet: type its name once and it is stored and offered in every future snippet editor. A self-added language is used for the AI prompts and the file extension; syntax highlighting falls back to plain text unless korTTY happens to ship a grammar for it. Text language — What language AI-written code should use for comments and for messages shown to users, logged, or printed as help. The default, Automatic — keep the script's language , leaves the snippet's own prose alone: korTTY writes new text in the language the script already uses and translates nothing. Picking a language instead is a deliberate instruction to convert the snippet's existing text into it. It is independent of the korTTY interface language. Tick Remember as default to keep the choice for future snippets; otherwise it applies to this editor only. Category — Select an existing category or type a new one. The fixed non-deletable Script-Header category contains reusable header templates for generated workflow scripts. System — Optionally select a target operating system (Any, Linux, macOS, Windows). Auto-set when created via Generate Workflow Script based on the agent's probed OS; you can manually override it for any snippet. Tags — Comma-separated keywords for searching (e.g., docker, deploy, backup ). Description — Optional free-text description of the snippet. Content — The snippet code. The editor provides live syntax highlighting based on the selected language. Click OK . If the snippet content changed, KorTTY saves the edited snippet while closing the dialog. When editing an existing entry, Save as new snippet stores the current content as a new snippet with a new ID and leaves the original unchanged.","title": "Creating and editing snippets"},{"location": "features/snippets.html#editor-toolbar-and-features","text": "The snippet editor toolbar provides: Format Code — Format the content using local formatters or AI-assisted formatting. Check Syntax — Validate the syntax (local or AI-assisted). AI Text — Correct spelling, translate, or generate technical descriptions. AI Code — Complete code, run a full code analysis, improve a selection (readability, robustness, performance, comments, or a custom instruction), migrate the snippet into a single language, check security, or generate diagrams. One-liner — Export as a terminal one-liner. Editor zoom — Adjust text size with Ctrl + + and Ctrl + - . Editor profiles — Switch between built-in IntelliJ-inspired profiles and custom color schemes. Background brightness — Adjust editor background. Word Wrap — Toggle line wrapping. Line numbers — Toggle line-number display. When the editor opens from the SFTP Manager for a local or remote file, the same toolbar remains available and the dialog uses file-mode save buttons.","title": "Editor toolbar and features"},{"location": "features/snippets.html#column-ruler-and-line-width-formatting","text": "Above the content field, the column ruler keeps the current caret column fixed at the left as Column N and shows a live marker at the matching editor position. Moving the mouse over the live marker shows Position N . Click the ruler to set a maximum line-length marker (columns 20–240). Right-click that marker to format the content to the selected width or remove the marker. Line-width formatting works locally only for formatters that support configurable width: Prettier-backed web formats (JavaScript, TypeScript, HTML, CSS) Python (Black) Perl (Perl::Tidy) For languages without local line-width support, KorTTY asks whether to use AI-assisted formatting. Both local and AI-assisted formatting show a before/after preview before applying changes.","title": "Column ruler and line-width formatting"},{"location": "features/snippets.html#format-code","text": "Format Code uses KorTTY's shared local formatter service: Built-in formatters: JSON, XML, YAML/YML, TOML, INI/properties, Groovy Bundled formatters: Java (google-java-format), Bash/shell (shfmt), Web/JS/TS/HTML/CSS (Prettier), SQL (sql-formatter), Perl (Perl::Tidy) Fallback: Optional PATH fallbacks for developer setups when a bundled formatter is missing Prettier runs as its offline Standalone browser build with only the Babel, Estree, TypeScript, HTML and PostCSS plugins; SQL uses the bundled sql-formatter browser build. Both are initialized lazily in an isolated JavaFX WebView and need no installed or packaged Node.js runtime. Requests are serialized and retain the same 15-second timeout, provider display and configurable Prettier line width as the process backend; a failed or timed-out engine is discarded before the next request. If local formatting is unavailable and the configured AI profile provides snippet AI capability, KorTTY asks whether to use AI assistance. AI formatting is applied only after the before/after preview is accepted.","title": "Format Code"},{"location": "features/snippets.html#editor-profiles","text": "Switch between: Current custom colors — User-defined palette 10 built-in IntelliJ-inspired profiles — Predefined color schemes User-created profiles — Custom profiles you create Profiles store foreground/background colors, syntax colors, cursor color, and cursor style.","title": "Editor profiles"},{"location": "features/snippets.html#ai-assisted-editor-functions","text": "If AI is configured, the snippet editor offers additional actions:","title": "AI-assisted editor functions"},{"location": "features/snippets.html#ai-suggestions","text": "AI suggestion — Generates a file name, description, Code language and Text language from the current code content. A detected code language that is not in the list yet is added to it, so the detection is never silently dropped. Correct spelling — On the description field; sends only description text to the AI.","title": "AI suggestions"},{"location": "features/snippets.html#text-language","text": "When AI is configured, Text language — directly below Code language in the editor form — chooses what language AI-written code uses for its comments and messages, and which language spelling correction works in. Its default is Automatic — keep the script's language . korTTY works out which language the snippet's comments and messages are already written in and requires the AI to keep it: existing text is left as it is, and anything new is written to match. A script commented in English stays English even when you run korTTY in German. Identifiers, file paths, commands, options, configuration keys and other code tokens are never touched in either mode. Selecting a language instead is an explicit instruction to convert the snippet's prose into it — the previous behaviour, now opt-in rather than automatic. A selection-based action applies the contract to its returned selection, a full replacement to the complete script. When korTTY cannot tell which language a script uses — there is too little text to judge, or the comments mix languages — it asks before changing anything, rather than guessing. Tick Remember for this snippet in that dialog and the question is not asked again for that snippet. Dismissing the dialog cancels the action and leaves the code untouched. All of this is independent of both the korTTY interface language and the Code language selector, which continues to define the programming language and syntax highlighting. Analysis reports, improvement descriptions and analysis-diagram labels always follow the korTTY interface language. The list offers korTTY's own interface languages plus any AI-generated language you have added. AI suggestion can also fill it for you: it reads the snippet's comments and its printed output ( echo , print , printf , Write-Host and similar) and preselects the language it finds — including a language korTTY has no interface translation for, which is then added to the list. A script without any human-readable text leaves the current choice untouched. Leave Remember as default unticked for a temporary choice that applies only to the current editor window. Tick it to save the selected language as the default for newly opened snippet editors; this updates the existing Default language for AI text in code setting under Settings → AI . Other editor windows that are already open keep their own selection. Spelling correction uses the selected language for grammar and spelling rules without translating the text. Translate selection… keeps its separate target-language dialog and initially selects the current text language. Local formatters and syntax checks are unaffected.","title": "Text language"},{"location": "features/snippets.html#ai-text-menu","text": "Correct spelling in selection — Fix typos in selected text. Translate selection… — Translate selected text to another language. Technical description — Generate documentation for selected code or the whole snippet.","title": "AI Text menu"},{"location": "features/snippets.html#optional-additional-instructions","text": "If enabled in Settings → AI , the editor shows a shared instructions field sent with spelling correction, translation, and technical description requests.","title": "Optional additional instructions"},{"location": "features/snippets.html#last-ai-change-toggle","text": "The ↺ button switches between the original code and the last AI-generated editor change.","title": "Last AI change toggle"},{"location": "features/snippets.html#ai-code-completions","text": "The snippet editor offers two kinds of code completion, both inside the Monaco editor: a completion list you open on demand, and optional ghost text that appears on its own after a typing pause. The keys are listed under Keyboard shortcuts . Completion list — Press Shift + Tab with the cursor at the end of a line that has text on it, or Ctrl + Space anywhere in the editor (on macOS that is the physical Ctrl key; because macOS often reserves Ctrl + Space for switching input sources, Cmd + I and Alt + Esc open the list as well). The list opens at once with candidates harvested from the snippet itself — the arrays, hashes, variables and functions that fit the spot, followed by idioms of the language: after for x in in a Bash script every array comes first, after use POSIX in Perl the usual qw(...) import lists. Up / Down move the selection, typing filters the list, Enter or Tab inserts the selected entry, and Esc closes the list. An idiom with placeholders is inserted as a template in which Tab jumps from one placeholder to the next. AI Complete in the AI Code menu and in the editor's context menu opens the same list. Tab still indents, and Shift + Tab anywhere but at the end of a non-empty line still outdents. The combination is yours to choose: Configuration → Global Settings → Snippet Editor has an AI Completion Shortcut field that records whatever you press — one key, or one key with up to three modifiers — and applies it to snippet editors opened afterwards, while Ctrl + Space keeps working; see Snippet Editor settings . AI candidates — When an AI profile is configured, every list also asks the model for up to five completions. They are marked with a ✦ star, sort after the local entries, and join the open list as they arrive — the local entries are usable in the meantime, and the status line reports the request and how many candidates it added. The list's selection jumps back to its first entry when they arrive. While another AI action of the editor is running, the list shows only the local candidates. Auto AI Complete — A toggle in the AI Code menu that turns on ghost text: when you pause for a moment with the cursor at the end of a non-empty line, the model's continuation appears greyed out after the cursor. Tab accepts it, Esc dismisses it, and Alt + ] / Alt + [ step through the alternatives (the physical ] and [ keys of a US layout, whatever they print on yours). Off by default and remembered only for the current editor session; a data notice asks for your consent once per application start, not once per editor. While a ghost-text request runs, the hint bar shows it with a Cancel button; the list's own AI request reports in the status line only. Language services — For JavaScript, TypeScript, JSON, CSS and HTML, Monaco's own language suggestions appear in the same list beside korTTY's candidates. The first list in such a snippet can take a moment while the language service starts. A completion request never blocks the editor's other AI actions. Starting one of them — Full code analysis , an improvement, a security check, a diagram — cancels a running completion request, stops the ghost-text timer and hides visible ghost text, so the two cannot overlap. A completion request that has not answered after 30 seconds is abandoned, whatever timeout the profile allows. An accepted AI entry or ghost text counts as an AI change, so the ↺ button switches back to the text before it; local entries are ordinary edits.","title": "AI Code completions"},{"location": "features/snippets.html#ai-code-actions","text": "The AI Code menu groups the actions that read or rewrite the code itself: AI Complete / Auto AI Complete — AI Complete opens the completion list at the cursor, the same list Shift + Tab opens; Auto AI Complete toggles ghost text after a typing pause (see AI Code completions above). Full code analysis — Opens a rich analysis window: a plain-language summary of what the script does, its external dependencies, categorized improvement suggestions you can tick and apply, and an auto-generated flow diagram. See Full code analysis below. Improve readability / robustness / performance — Rewrites the selected code region toward one goal without unrelated changes. Before Improve robustness starts, it displays two optional panels for additional rules: Hardening options , Input hardening . If at least one rule is active, KorTTY rewrites the complete snippet so it can apply global prologue or epilogue changes. Optimize code comments — Comments the selected code region: the AI inserts explanations of what the code does and why directly above or beside the lines they belong to, using the language's own comment syntax, and replaces outdated or misleading comments. Executable code stays untouched. The comments are written in the editor's Text language . Available from the AI Code menu and from the editor's right-click menu on a selection. Custom improvement… — Rewrites the selected code region following a free-text instruction you type. It displays the same two optional rule panels: Hardening options , Input hardening . As with Improve robustness , KorTTY rewrites the complete snippet when any hardening rule is active. Migrate into one language… — Rewrites a mixed-language snippet so that all of it is written in one target language. KorTTY detects the mix locally and only offers what actually applies; an orchestration format (Azure DevOps pipeline, GitHub Actions, Jenkinsfile, Ansible, …) embeds shell by design and is therefore never migrated as a whole — only its script steps can be unified, and a platform conversion is offered but never preselected. See Language unification . Security Check — Generates a security report. Select findings to fix; KorTTY applies them with a before/after preview that highlights what changed and why. See Security Check below. Diagram — Opens the snippet's saved diagrams, or generates the first logical-structure flowchart when none exists yet. A snippet can store several diagrams across five families — logical structure, sequence, state, class, and ER. See Mermaid diagrams below. The editor context menu also offers AI Assistant… , which opens an instruction dialog for the current cursor position: KorTTY sends the full snippet, cursor offset, line, column, and your instruction to the configured AI profile and shows the result as a before/after preview. The context menu additionally has a Generate diagram submenu listing the five diagram families. With a code selection its label switches to Generate diagram from selection and the chosen family diagrams only the selected lines — the selection is snapped to whole lines and the resulting diagram remembers its line range. Without a selection the whole snippet is diagrammed. Every generated diagram is added to the snippet's saved diagrams; see Mermaid diagrams . Readability, performance, comment optimization, and a custom improvement rewrite only the selected region when both hardening panels have no selected rules, so select a code region first . Improve robustness and Custom improvement instead rewrite the complete snippet whenever a classic hardening option or supported Input hardening guard is active, because those rules may need the prologue and epilogue. Every result is shown as a before/after preview (the Review AI change window) before anything is applied; an incomplete full-script response with an omission marker is refused. Warning Snippet AI actions send the current snippet content, selection or cursor metadata, and prompt instructions to the configured default AI profile (or, for Security Check, the dedicated security-check profile). Eligible actions can additionally send enabled, configurable AI Skills; the source-only diagram request sends no configurable library skill and instead always carries korTTY's compact built-in action skill for the chosen diagram family. Snippet AI actions do not enable internet tools, even when the selected profile has internet access. Code completion is more restrained: the local candidates in the completion list are computed by korTTY itself and never leave the machine, and an AI completion request sends only the code around the cursor — up to 6,000 characters before it and 1,500 after it — together with the names of the snippet's arrays, variables and functions, rather than the whole snippet. Completion requests skip knowledge stores; locally matched AI Skills still apply. Auto AI Complete repeats that request after every typing pause while it is on, so leave it off for sensitive snippets unless you trust the configured endpoint.","title": "AI Code actions"},{"location": "features/snippets.html#ai-profile-re-run-and-zoom","text": "The AI-code report windows (Full code analysis, Security Check, the technical-description and alternative-solution dialogs, and the change-review diff) share a small toolbar: AI profile — Pick a different AI profile for the next run of that window. The choice is transient: it resets to the default profile when the window is reopened. (Security Check keeps its own permanently remembered Security profile instead.) Re-run — Repeat the request with the currently selected profile. A− / A+ — Adjust the reading or preview font size; the chosen size is remembered across sessions, separately per window type. Copy — Copy the report or content to the clipboard.","title": "AI profile, re-run and zoom"},{"location": "features/snippets.html#ai-skills","text": "When AI Skills are configured, the snippet editor shows an AI skills picker. Skills relevant to the snippet's language are pre-selected automatically, and any skill you tick here is applied to skill-relevant AI-code actions such as completion, analysis, improvement and security checks regardless of the skill's configured target. The fixed-contract Diagram action intentionally omits those configurable library skills to keep the source-grounded request small and predictable; korTTY always supplies its separate compact action skill for the requested diagram family, which is internal and therefore does not appear in the picker or count toward the 39 configurable built-in AI Skills. The picker appears only when at least one configurable AI Skill is enabled. The Full code analysis window surfaces this same selection as a row of chips — labelled (auto-selected) or (manual) — and lets you refine it just for that analysis through a searchable picker. Changes made there apply after you next click Re-run . See Full code analysis .","title": "AI skills"},{"location": "features/snippets.html#text-correction-and-translation","text": "For selection-based text correction and translation, KorTTY only rewrites editable comment text, string literals, and user-facing text segments. A selection may begin or end inside such a segment: KorTTY uses the surrounding snippet to recognize the selected words and replaces only the overlapping text. It does not rewrite logical code structure.","title": "Text correction and translation"},{"location": "features/snippets.html#technical-descriptions","text": "If text is selected, the AI describes only that region. If nothing is selected, the AI describes the whole snippet. The description dialog lets you: Copy the generated description Format it with the comment syntax of the current snippet language Insert it into the snippet above the selected code or at the top","title": "Technical descriptions"},{"location": "features/snippets.html#alternative-solutions","text": "Right-click a selected code region and choose Alternative solution to: Request multiple alternative implementations (up to the configured limit) Add a 3-line field for additional instructions Reload and regenerate new alternatives Zoom an individual preview to the full dialog area Apply exactly the originally selected code when ready","title": "Alternative solutions"},{"location": "features/snippets.html#mermaid-diagrams","text": "Mermaid diagrams are stored with the snippet, and a snippet can keep several of them side by side. If the snippet content changes after diagram generation, KorTTY marks a diagram as possibly outdated and offers regeneration. Diagram families: Five families can be generated, each restricted to a compact, safe dialect: Logical structure ( flowchart TD — the control-flow view and the default), Sequence diagram ( sequenceDiagram — who talks to whom at runtime: the script, remote hosts, external commands, APIs), State diagram ( stateDiagram-v2 — observable states and transitions such as connection lifecycles and retry logic), Class diagram ( classDiagram — types, members and relations declared in the code), and ER diagram ( erDiagram — data entities and relations the code implies, such as SQL tables). Saved diagrams dialog: The list on the left shows every stored diagram with its family, title and — for a selection-scoped diagram — its line range. New diagram is a menu offering the five families, Regenerate re-runs the selected diagram with its original family and scope, and Delete removes the selected diagram after a confirmation. Diagrams are persisted when the snippet is saved. Diagrams from a selection: The editor context menu's Generate diagram from selection entries diagram only the selected lines. The selection is snapped to whole lines, the AI sees just that region, and the stored diagram remembers the line range: its code references point at the absolute snippet lines, and Regenerate re-reads the same lines from the current content. Generation: Logical-structure diagrams use stable node IDs with the semantic classes setup , work , success , and failure , complete decisions with localized yes / no edge labels, and a source mapping from every node to exact snippet lines; the request asks for a complete mapping, and a node the model leaves unmapped keeps its place in the diagram without a hover reference. The other families keep source references optional: entries that name a declared participant, state, class, or entity are stored, incomplete mappings are simply dropped. Every family's AI request carries its own small internal action skill with family-specific quality rules — grouped runtime behavior for flowcharts and sequence diagrams, observable states instead of statements, only declared types and members, no invented schemas. Hover code references and click-to-code navigation are a logical-structure feature. Rendering: Local only — the SHA-256-pinned Mermaid 11.17.2 browser bundle is included with KorTTY and runs in an isolated, lazily created JavaFX WebView. No rendering server, Graphviz installation, Java subprocess, or first-use download is required. Dialog features: Sanitized SVG display with JavaScript disabled, scaling without distortion, zoom slider and buttons with fit, SVG/PNG export, image and Mermaid-source clipboard copy, hover code references, and the shared Diagram appearance controls. Safety and recovery: A diagram whose JSON envelope is unreadable is recovered from the answer text and then validated like any other, so a mis-escaped reply costs no second request. KorTTY rejects frontmatter, directives, links, callbacks, external images/icons, oversized sources and overly complex graphs before any rendering, and validates every generated diagram against its family's restricted grammar with per-family compactness caps (for example at most 12 sequence participants, 12 states, 12 classes, or 12 entities; a flowchart may declare 12 action/decision nodes for a snippet of up to 200 lines and up to 24 for a script of 1,000 lines or more). A rejected whole-snippet logical-structure result falls back to the deterministic local flowchart without another AI request, and the editor's status line names the rejection reason together with the fallback; selection-scoped and non-flowchart generations report the failure and its reason instead, and nothing broken is ever saved. A response that was cut off at the output-token cap is reported as such rather than as an ordinary failure, because the usual cause is a thinking model spending the whole budget on reasoning — a smaller selection or a model that reasons less then helps. Safe restricted diagrams saved by older korTTY versions remain renderable even when they predate these stricter generation-quality rules. Requests are serialized with a 30-second timeout; cancellation or timeout discards the renderer, and the hidden WebView is released after idle time. Upgrade cleanup: Saved legacy diagram entries are discarded without removing their owning snippets or chats. KorTTY also removes its retired diagram-renderer download cache and abandoned temporary render directories without following symbolic links.","title": "Mermaid diagrams"},{"location": "features/snippets.html#diagram-appearance","text": "Both diagram windows — the standalone Diagram dialog and the Full code analysis flow diagram — share the same zoom controls and two appearance controls; the appearance controls remember their setting across sessions: Zoom — − and + step the scale, the slider between them sets it directly, and Fit returns to the size that shows the whole diagram in the pane. The slider's track runs in powers of two, so the fitted size sits exactly in its middle and one step to either side halves or doubles it; the percentage beside the slider is the resulting scale, which for a large diagram fitted into a small pane starts below 100 %. Dragging the slider redraws the diagram a few times a second rather than on every pixel, so a large flowchart follows the drag without stuttering, and the diagram is always redrawn at the value you let go on. Zoom is deliberately not remembered: every diagram opens fitted to its pane. Dark mode — A Dark mode button with three choices: Auto — follows the operating system's light/dark appearance. When you switch the OS to dark mode the diagram follows on the next render (and when the window regains focus). Light — always light. Dark — always dark. A manual choice is permanent until you change it. Dark mode recolours the whole diagram — a dark canvas, darkened node cards with light text, and light connectors and labels — not just the page margin. - Background — A colour picker for the page/canvas colour in light mode. It applies to the diagram itself and to any exported SVG/PNG. The picker is disabled while dark mode is active, because dark mode drives the appearance.","title": "Diagram appearance"},{"location": "features/snippets.html#placeholder-variables","text": "Snippets can contain placeholder variables that are replaced when you insert the snippet.","title": "Placeholder variables"},{"location": "features/snippets.html#built-in-variables","text": "These variables are automatically replaced: Variable Replacement ${date} Current date in YYYY-MM-DD format ${time} Current time in HH:MM:SS format ${datetime} Current date and time in YYYY-MM-DD HH:MM:SS format ${hostname} Local machine hostname ${username} Current system username ${clipboard} Current clipboard content ${cursor} Cursor position (removed from text; position returned)","title": "Built-in variables"},{"location": "features/snippets.html#custom-variables","text": "Any ${variableName} not in the built-in list is treated as a custom variable. When you insert the snippet: KorTTY checks the Variable Manager for stored values Variables without stored values prompt for input","title": "Custom variables"},{"location": "features/snippets.html#sending-snippets-to-the-terminal","text": "The Snippet Manager can send a selected snippet directly to the active terminal.","title": "Sending snippets to the terminal"},{"location": "features/snippets.html#send-to-terminal","text": "Keeps existing behavior Supported script languages are embedded as a terminal one-liner where possible Other snippets use the existing fallback path","title": "Send to Terminal"},{"location": "features/snippets.html#send-to-terminal-with-parameters","text": "Opens a dialog for missing ${...} placeholder variables and script arguments Script arguments are entered one per line; empty lines are ignored If you confirm without script arguments, the result is the same as Send to Terminal , but missing placeholder variables can still be filled in","title": "Send to Terminal with Parameters"},{"location": "features/snippets.html#script-arguments","text": "Supported for Bash/shell, Python, Perl, and Ruby snippets: Arguments are passed individually and shell-quoted Not appended as raw shell text If arguments are entered for unsupported languages, KorTTY shows an information message and sends nothing","title": "Script arguments"},{"location": "features/snippets.html#terminal-display","text": "For embedded/base64 one-liners, the terminal shows the KorTTY snippet: ... label instead of echoing the full generated command.","title": "Terminal display"},{"location": "features/snippets.html#import-and-export","text": "Snippets can be imported and exported in multiple formats.","title": "Import and export"},{"location": "features/snippets.html#data-format-exports","text": "Use Export to save selected snippets, or all snippets when nothing is selected. Use Import to merge snippets from a file. Format Extension Use case JSON .json Data interchange, programmatic access XML .xml Structured data, tool integration YAML .yaml Human-readable, configuration-friendly","title": "Data format exports"},{"location": "features/snippets.html#script-focused-exports","text": "For script-specific exports, choose:","title": "Script-focused exports"},{"location": "features/teamwork.html","text": "Teamwork (shared connections) Share SSH connections with your team by syncing them from a Git repository or shared file. Teamwork sources are loaded into the Connection Manager alongside your local connections, kept in sync automatically, and safely stripped of inline passwords so credentials come only from your local encrypted storage. Overview Teamwork lets teams maintain a centralized library of connection configurations: Git repositories — Clone and keep in sync with a Git repo containing a kortty-teamwork-connections.xml file (or legacy connections.xml ). Shared files — Load connections from a local or network path (read-only or read-write). Automatic sync — Background syncing at a configurable interval checks for updates. Credential security — Shared connections do NOT carry inline passwords; only credential IDs and SSH key references. Local overrides — Your local credentials and SSH keys are merged with shared connection definitions. Read-only mode — Mark sources as read-only to prevent accidental writes back. Setting up teamwork sources Open Teamwork → Teamwork Settings… (or Configuration → Global Settings… → Teamwork ) to configure sources. Add a source Click Add to create a new source. Choose the source Type : Git — Clone from an HTTPS, SSH, or git:// URL. Shared File — Read from a local or network path (e.g., file:///mnt/share/connections.xml or //host/share/connections.xml ). Enter the Location : For Git: the clone URL. For Shared File: a local/network file path (can be a file:// URI or a UNC path). Set the Check Interval (1–1440 minutes; default: 15). Optionally enable Read-Only to prevent writes back (Git sources only). Click OK to save. Manage sources The Teamwork Settings dialog lists all sources with their type, location, and sync interval: Column Meaning Type Git or Shared File Location Repository URL or file path Interval Minutes between sync checks Enabled Toggle to enable/disable without deleting Use the buttons to: - Add — Create a new source. - Edit — Modify the selected source. - Remove — Delete the selected source. - Enable/Disable — Toggle the enabled state for the selected sources. At the bottom, set the Default Check Interval (applies to new sources that don't specify one). How synchronization works Background sync Once you save the Teamwork Settings: KorTTY starts a background sync thread. Every N minutes (based on the minimum interval among enabled sources), it: Pulls/clones each source (Git) or reads the file (Shared File). Loads the connections XML. Merges the results into the cache and Connection Manager. If a source update fails, the previous cached version is kept. Manual sync Use Teamwork → Teamwork Settings… and click OK to trigger a sync immediately. Conflict detection Each sync records a version token: - Git — The current commit hash. - Shared File — The file's last-modified timestamp. If a shared connection's version token changes between syncs, a new version was fetched. If you have made local edits to a teamwork connection and the source updates with a conflicting change, the local edits are preserved (no automatic overwrite). Shared connection file format Create a kortty-teamwork-connections.xml file (or connections.xml for backward compatibility) in the root of your Git repo or shared file: <?xml version=\"1.0\" encoding=\"UTF-8\"?> <connections> <connection id= \"prod-web-1\" > <name> Prod Web Server 1 </name> <host> web1.example.com </host> <port> 22 </port> <username> deploy </username> <group> Production/Web </group> <authMethod> SSH_KEY </authMethod> <sshKeyId> key-prod-deploy </sshKeyId> <credentialId> cred-prod-user </credentialId> </connection> </connections> Do not include inline secrets Teamwork connections must not include encryptedPassword , privateKeyPath , or privateKeyPassphrase . Instead: - Use credentialId to reference a stored credential in Security → Credentials… . - Use sshKeyId to reference a stored SSH key in Security → SSH-Keys… . If inline secrets are found in the shared file, KorTTY strips them automatically (they are not loaded). Using teamwork connections Once a source is synced: Open Manage Connections… (or press Ctrl + M ). Teamwork connections appear in the tree with a [Teamwork] label and their source ID. Click a teamwork connection to view or use it. Cannot edit directly — Teamwork connections are read-only unless their source is marked as writable and you own edit rights (determined by teamworkRole ). Local overrides The merged credential and SSH key references are resolved from your local storage. If a credential or key is not found locally, you are prompted to provide it when connecting. Your local copy of a teamwork connection can override authentication by assigning a different credential or key. Distinguish sources In the Connection Manager, teamwork connections are marked by their source ID. Hover over or inspect the connection properties to see which teamwork source it came from. Git repository setup To share connections via Git: Create a repository (e.g., ssh-connections ). Add a kortty-teamwork-connections.xml file to the root with your connection definitions. Commit and push. Share the repository URL (HTTPS or SSH) with team members. Team members add the URL in Teamwork → Teamwork Settings… → Add . SSH vs. HTTPS HTTPS — Works without SSH key setup; may require a GitHub Personal Access Token or username/password (store the token securely). SSH — Requires git and a local SSH key in ~/.ssh/id_rsa (or configured in ssh-add ). Example repository layout ssh-connections/ ├── kortty-teamwork-connections.xml ├── .gitignore └── README.md Optional: store versions in Git Use the commit hash as the version token so KorTTY can detect updates: git log -1 --pretty = %H Shared file setup To share connections via a file: Export your connections to a file: Connections → Export… → select connections → save as .xml . Place the file on a shared network path (e.g., //server/share/connections.xml ). Set read/write permissions as needed. Team members add the file path in Teamwork → Teamwork Settings… → Add . Example paths Platform Path format Windows (network share) //server/share/connections.xml or file:////server/share/connections.xml Linux/macOS (NFS mount) /mnt/teamshare/connections.xml or file:///mnt/teamshare/connections.xml SMB/CIFS (mounted) /Volumes/teamshare/connections.xml (macOS) Security considerations Credentials are local-only Shared connections do not carry passwords or key passphrases. Your local encrypted storage (master-password protected) holds the actual secrets. Team members must have their own credentials set up locally. Git repositories should not store secrets Never commit passwords, SSH key content, or API tokens to the teamwork repository. Use only credential IDs and key references. File permissions For shared files on network paths, restrict read/write access to team members only. Ensure the path is not world-readable. Audit trail For Git-based teamwork, the commit history provides an audit trail. Review changes before pulling by checking the remote branch. Troubleshooting Source is not syncing Open Teamwork → Teamwork Settings… . Verify the source is Enabled . Check that the Location is correct and accessible: Git — Run git clone <url> manually to test. Shared File — Verify the file exists and is readable from your machine. Click OK to trigger a manual sync. Check the application log ( ~/.kortty/kortty.log ) for errors. Connections appear but credentials are missing Open Security → Credentials… and Security → SSH-Keys… . Verify that the credential IDs or SSH key IDs in the shared connections exist locally. If missing, add them manually or ask your team administrator to provide the IDs. Cannot push changes to Git repo Verify the Git URL uses SSH or an HTTPS token (not username/password). Ensure your SSH key is registered with the remote (GitHub, GitLab, etc.). In Teamwork Settings , mark the source as Read-Only if you don't need to write back. File path is not recognized (Windows/UNC) Use forward slashes or the file:// URI format: //server/share/connections.xml ✓ \\\\server\\share\\connections.xml ✗ (backslashes may not parse correctly) file:////server/share/connections.xml ✓ (UNC notation)","title": "Teamwork (shared connections)"},{"location": "features/teamwork.html#teamwork-shared-connections","text": "Share SSH connections with your team by syncing them from a Git repository or shared file. Teamwork sources are loaded into the Connection Manager alongside your local connections, kept in sync automatically, and safely stripped of inline passwords so credentials come only from your local encrypted storage.","title": "Teamwork (shared connections)"},{"location": "features/teamwork.html#overview","text": "Teamwork lets teams maintain a centralized library of connection configurations: Git repositories — Clone and keep in sync with a Git repo containing a kortty-teamwork-connections.xml file (or legacy connections.xml ). Shared files — Load connections from a local or network path (read-only or read-write). Automatic sync — Background syncing at a configurable interval checks for updates. Credential security — Shared connections do NOT carry inline passwords; only credential IDs and SSH key references. Local overrides — Your local credentials and SSH keys are merged with shared connection definitions. Read-only mode — Mark sources as read-only to prevent accidental writes back.","title": "Overview"},{"location": "features/teamwork.html#setting-up-teamwork-sources","text": "Open Teamwork → Teamwork Settings… (or Configuration → Global Settings… → Teamwork ) to configure sources.","title": "Setting up teamwork sources"},{"location": "features/teamwork.html#add-a-source","text": "Click Add to create a new source. Choose the source Type : Git — Clone from an HTTPS, SSH, or git:// URL. Shared File — Read from a local or network path (e.g., file:///mnt/share/connections.xml or //host/share/connections.xml ). Enter the Location : For Git: the clone URL. For Shared File: a local/network file path (can be a file:// URI or a UNC path). Set the Check Interval (1–1440 minutes; default: 15). Optionally enable Read-Only to prevent writes back (Git sources only). Click OK to save.","title": "Add a source"},{"location": "features/teamwork.html#manage-sources","text": "The Teamwork Settings dialog lists all sources with their type, location, and sync interval: Column Meaning Type Git or Shared File Location Repository URL or file path Interval Minutes between sync checks Enabled Toggle to enable/disable without deleting Use the buttons to: - Add — Create a new source. - Edit — Modify the selected source. - Remove — Delete the selected source. - Enable/Disable — Toggle the enabled state for the selected sources. At the bottom, set the Default Check Interval (applies to new sources that don't specify one).","title": "Manage sources"},{"location": "features/teamwork.html#how-synchronization-works","text": "","title": "How synchronization works"},{"location": "features/teamwork.html#background-sync","text": "Once you save the Teamwork Settings: KorTTY starts a background sync thread. Every N minutes (based on the minimum interval among enabled sources), it: Pulls/clones each source (Git) or reads the file (Shared File). Loads the connections XML. Merges the results into the cache and Connection Manager. If a source update fails, the previous cached version is kept.","title": "Background sync"},{"location": "features/teamwork.html#manual-sync","text": "Use Teamwork → Teamwork Settings… and click OK to trigger a sync immediately.","title": "Manual sync"},{"location": "features/teamwork.html#conflict-detection","text": "Each sync records a version token: - Git — The current commit hash. - Shared File — The file's last-modified timestamp. If a shared connection's version token changes between syncs, a new version was fetched. If you have made local edits to a teamwork connection and the source updates with a conflicting change, the local edits are preserved (no automatic overwrite).","title": "Conflict detection"},{"location": "features/teamwork.html#shared-connection-file-format","text": "Create a kortty-teamwork-connections.xml file (or connections.xml for backward compatibility) in the root of your Git repo or shared file: <?xml version=\"1.0\" encoding=\"UTF-8\"?> <connections> <connection id= \"prod-web-1\" > <name> Prod Web Server 1 </name> <host> web1.example.com </host> <port> 22 </port> <username> deploy </username> <group> Production/Web </group> <authMethod> SSH_KEY </authMethod> <sshKeyId> key-prod-deploy </sshKeyId> <credentialId> cred-prod-user </credentialId> </connection> </connections> Do not include inline secrets Teamwork connections must not include encryptedPassword , privateKeyPath , or privateKeyPassphrase . Instead: - Use credentialId to reference a stored credential in Security → Credentials… . - Use sshKeyId to reference a stored SSH key in Security → SSH-Keys… . If inline secrets are found in the shared file, KorTTY strips them automatically (they are not loaded).","title": "Shared connection file format"},{"location": "features/teamwork.html#using-teamwork-connections","text": "Once a source is synced: Open Manage Connections… (or press Ctrl + M ). Teamwork connections appear in the tree with a [Teamwork] label and their source ID. Click a teamwork connection to view or use it. Cannot edit directly — Teamwork connections are read-only unless their source is marked as writable and you own edit rights (determined by teamworkRole ).","title": "Using teamwork connections"},{"location": "features/teamwork.html#local-overrides","text": "The merged credential and SSH key references are resolved from your local storage. If a credential or key is not found locally, you are prompted to provide it when connecting. Your local copy of a teamwork connection can override authentication by assigning a different credential or key.","title": "Local overrides"},{"location": "features/teamwork.html#distinguish-sources","text": "In the Connection Manager, teamwork connections are marked by their source ID. Hover over or inspect the connection properties to see which teamwork source it came from.","title": "Distinguish sources"},{"location": "features/teamwork.html#git-repository-setup","text": "To share connections via Git: Create a repository (e.g., ssh-connections ). Add a kortty-teamwork-connections.xml file to the root with your connection definitions. Commit and push. Share the repository URL (HTTPS or SSH) with team members. Team members add the URL in Teamwork → Teamwork Settings… → Add .","title": "Git repository setup"},{"location": "features/teamwork.html#ssh-vs-https","text": "HTTPS — Works without SSH key setup; may require a GitHub Personal Access Token or username/password (store the token securely). SSH — Requires git and a local SSH key in ~/.ssh/id_rsa (or configured in ssh-add ).","title": "SSH vs. HTTPS"},{"location": "features/teamwork.html#example-repository-layout","text": "ssh-connections/ ├── kortty-teamwork-connections.xml ├── .gitignore └── README.md","title": "Example repository layout"},{"location": "features/teamwork.html#optional-store-versions-in-git","text": "Use the commit hash as the version token so KorTTY can detect updates: git log -1 --pretty = %H","title": "Optional: store versions in Git"},{"location": "features/teamwork.html#shared-file-setup","text": "To share connections via a file: Export your connections to a file: Connections → Export… → select connections → save as .xml . Place the file on a shared network path (e.g., //server/share/connections.xml ). Set read/write permissions as needed. Team members add the file path in Teamwork → Teamwork Settings… → Add .","title": "Shared file setup"},{"location": "features/teamwork.html#example-paths","text": "Platform Path format Windows (network share) //server/share/connections.xml or file:////server/share/connections.xml Linux/macOS (NFS mount) /mnt/teamshare/connections.xml or file:///mnt/teamshare/connections.xml SMB/CIFS (mounted) /Volumes/teamshare/connections.xml (macOS)","title": "Example paths"},{"location": "features/teamwork.html#security-considerations","text": "Credentials are local-only Shared connections do not carry passwords or key passphrases. Your local encrypted storage (master-password protected) holds the actual secrets. Team members must have their own credentials set up locally. Git repositories should not store secrets Never commit passwords, SSH key content, or API tokens to the teamwork repository. Use only credential IDs and key references. File permissions For shared files on network paths, restrict read/write access to team members only. Ensure the path is not world-readable. Audit trail For Git-based teamwork, the commit history provides an audit trail. Review changes before pulling by checking the remote branch.","title": "Security considerations"},{"location": "features/teamwork.html#troubleshooting","text": "","title": "Troubleshooting"},{"location": "features/teamwork.html#source-is-not-syncing","text": "Open Teamwork → Teamwork Settings… . Verify the source is Enabled . Check that the Location is correct and accessible: Git — Run git clone <url> manually to test. Shared File — Verify the file exists and is readable from your machine. Click OK to trigger a manual sync. Check the application log ( ~/.kortty/kortty.log ) for errors.","title": "Source is not syncing"},{"location": "features/teamwork.html#connections-appear-but-credentials-are-missing","text": "Open Security → Credentials… and Security → SSH-Keys… . Verify that the credential IDs or SSH key IDs in the shared connections exist locally. If missing, add them manually or ask your team administrator to provide the IDs.","title": "Connections appear but credentials are missing"},{"location": "features/teamwork.html#cannot-push-changes-to-git-repo","text": "Verify the Git URL uses SSH or an HTTPS token (not username/password). Ensure your SSH key is registered with the remote (GitHub, GitLab, etc.). In Teamwork Settings , mark the source as Read-Only if you don't need to write back.","title": "Cannot push changes to Git repo"},{"location": "features/teamwork.html#file-path-is-not-recognized-windowsunc","text": "Use forward slashes or the file:// URI format: //server/share/connections.xml ✓ \\\\server\\share\\connections.xml ✗ (backslashes may not parse correctly) file:////server/share/connections.xml ✓ (UNC notation)","title": "File path is not recognized (Windows/UNC)"},{"location": "features/terminal-effect-plugins.html","text": "Terminal effect plugins KorTTY terminal-effect plugins are Java ServiceLoader plugins that can change the appearance and runtime behavior of a terminal session. They are trusted local code that runs inside the KorTTY JVM and integrate through a clean SPI interface. Capabilities A terminal-effect plugin can: Apply terminal appearance values such as font, foreground/background colors, cursor color, and cursor style Add JavaFX overlay nodes above the terminal area (for example scanlines, noise, vignettes, or line highlights) Wrap the SithTermFX TtyConnector to observe, pace, or transform terminal output before the terminal emulator receives it React to the user-selected terminal-effect animation speed Provide an animated preview that the plugin manager shows for the selected plugin (optional createPreview() ) Built-in Effects KorTTY ships eleven built-in effects as two bundled, exportable plugin JARs: the MOTHER reference implementation ( kortty-terminal-effect-mother.jar ) and the effect pack ( kortty-terminal-effect-pack.jar ) with ten themed effects. ID Name Description mother MU/TH/UR 6000 ALIEN-style green CRT terminal appearance with paced line output. amber-crt-90 Amber CRT '90 Amber phosphor CRT monitor from the 90s with scanlines, glow, flicker and a rolling refresh band. commodore-blue Commodore Heritage Classic C64 home computer look: light blue on blue with a chunky cursor and loader bars. neon-city Neon City Cyberpunk neon look with glitch tears, RGB-split flickers and pulsing glow. digital-rain Digital Rain Green-on-black matrix style with a faint stream of falling glyphs. hologram-hud Hologram HUD Translucent sci-fi hologram with interference bands, HUD corner brackets and flicker. poltergeist Poltergeist Haunted monochrome terminal with a breathing vignette, static bursts and ghostly flashes. vhs-1987 VHS 1987 Worn VHS tape playback with tracking noise, rolling distortion and a PLAY overlay. synthwave-horizon Synthwave Horizon Retro-80s synthwave palette with a glowing perspective grid on the horizon. deep-space-radar Deep Space Radar Tactical deep-space console with a slow radar sweep, faint blips and frame corners. typewriter-noir Typewriter Noir Sepia paper and ink noir look with per-character typewriter output pacing. All built-in effects override the terminal appearance (font, colors, cursor) while active and draw their animation as a mouse-transparent canvas overlay. Typewriter Noir additionally wraps the connector to pace output character by character. Effect names are proper nouns and stay untranslated; descriptions are localized through the plugin.terminalEffects.desc.* message keys with an English fallback. Runtime Model Terminal-effect plugins are loaded by de.kortty.core.TerminalEffectPluginManager in this order: Application classpath plugins Bundled plugin JARs listed by bundled-plugins/terminal-effects.index External JARs copied into ~/.kortty/plugins If two plugins expose the same plugin ID, the first one wins and later duplicates are ignored. This is intentional because connection settings persist the plugin ID. External plugins are loaded from one JAR at a time with KorTTY's application classloader as parent. Dependencies that are already part of KorTTY can be used from that parent classloader. Dependencies that are not part of KorTTY are not discovered from adjacent files automatically; either avoid them or shade them into the plugin JAR. Warning Terminal-effect plugins are trusted local Java code. KorTTY does not sandbox imported JARs. A plugin runs inside the KorTTY JVM with the same local process permissions as KorTTY itself. Only import plugins from sources you trust. Plugin Management in the UI Users manage terminal-effect plugins from Plugins > Terminal Effects . The Terminal Effects manager window is resizable, so you can expand the plugin table and descriptions while keeping the live preview visible. If tool windows are configured to open as tabs, the manager instead uses the available main-window tab area. The dialog shows the loaded plugin list with: Active/inactive state Display name Short description Next to the table, a preview panel plays a live animated preview of the selected plugin (a small fake terminal in the effect's colors with the effect overlay on top). If a plugin does not implement createPreview() , the panel shows a placeholder text instead. The preview stops when the selection changes, the global terminal-effects switch is turned off, or the dialog closes. Users can: Enable or disable a plugin Import an external .jar plugin, which KorTTY copies to ~/.kortty/plugins Export plugins that have a source JAR (the bundled MOTHER and effect-pack JARs are exportable) Users select effects per terminal session from the terminal context menu or View > Terminal Effect . Saved connections can also store the selected effect and animation speed through Quick Connect and Connection Manager. The speed slider covers 1x through 10x ; the numeric field accepts values up to 99x . SPI Classes The public SPI lives under de.kortty.plugin.terminaleffects . TerminalEffectPlugin Implement TerminalEffectPlugin as the ServiceLoader entry point. public interface TerminalEffectPlugin { String id (); String displayName (); default String description () { return \"\" ; } TerminalEffectSession createSession ( TerminalEffectContext context ); default TerminalEffectPreview createPreview () { return null ; } } Rules: id() must be stable because connection settings and disabled-plugin state persist it Valid IDs match the regex [a-z0-9][a-z0-9._-]{0,63} displayName() must not be blank description() is shown in the plugin-management table and should be one short sentence The provider class must be loadable by ServiceLoader ; use a public class with a public no-argument constructor createPreview() is optional; returning null (the default) shows a placeholder in the plugin manager TerminalEffectSession createSession returns one TerminalEffectSession per terminal tab/effect activation. public interface TerminalEffectSession extends AutoCloseable { default void start () {} default @NotNull TtyConnector wrapConnector ( @Nullable SithTermFxWidget widget , @NotNull TtyConnector connector ) { return connector ; } default void stop () {} } Responsibilities: Allocate UI resources in start() Decorate connectors in wrapConnector(...) when output pacing or filtering is needed Remove listeners, stop timelines, unbind properties, and release references in stop() stop() must be idempotent. It can be called when a user disables the effect, switches effects, closes a tab, or reloads plugins. TerminalEffectContext TerminalEffectContext exposes the active terminal environment: Method Purpose pluginId() The active plugin ID terminalView() The owning KorTTY terminal view overlayRoot() A JavaFX StackPane above the terminal area; add overlay nodes here widgets() Current SithTermFX widgets for the terminal view; split terminals can have more than one widget animationSpeed() User-selected speed normalized by TerminalEffectAnimationSpeed applyAppearance(TerminalEffectAppearance) Applies terminal appearance overrides restoreAppearance() Restores the baseline appearance captured before the effect was activated JavaFX scene-graph changes must run on the JavaFX application thread. If a callback can arrive from a terminal worker thread, use Platform.runLater(...) for overlay or UI updates. TerminalEffectAppearance TerminalEffectAppearance is a record with nullable fields: public record TerminalEffectAppearance ( @Nullable String fontFamily , @Nullable Integer fontSize , @Nullable String foregroundColor , @Nullable String backgroundColor , @Nullable String cursorColor , @Nullable String cursorStyle ) { } Use only the fields your effect needs. null leaves the current value unchanged. The current implementation passes color values as strings, for example #19FF4C . Cursor style is also a string; the MOTHER effect uses BLINK_BLOCK . Reuse values already accepted by KorTTY's terminal settings instead of inventing new names. TerminalEffectPreview createPreview() returns the animated preview shown in the plugin manager: public interface TerminalEffectPreview { @NotNull Node node (); default void start () {} default void stop () {} } Contract: One preview instance backs one displayed preview; the caller requests node() once, calls start() after attaching it, and stop() before discarding it Do not construct JavaFX objects before node() is called, so plugin metadata stays usable without a running JavaFX toolkit stop() must stop all timelines and release animation resources TerminalEffectPreviewCanvas (same package) is a reusable implementation used by all built-in effects: a 360x220 fake terminal with configurable colors, fake shell lines, a blinking cursor, and an optional effect overlay canvas on top. Its builder collects plain data only and creates JavaFX nodes lazily in node() . TerminalEffectConnectorWrapper If a plugin wraps a connector, implement TerminalEffectConnectorWrapper and delegate all TtyConnector methods to the wrapped connector unless the effect intentionally changes that method. public interface TerminalEffectConnectorWrapper extends TtyConnector { TtyConnector delegate (); } KorTTY uses this marker to unwrap effect connectors before applying another wrapper or before accessing the underlying SSH connector. Without it, reconnect, drag/drop upload, AI-agent current-directory tracking, or future connector-level features can see the wrong connector. Minimal Plugin Example Directory layout: example-terminal-effect/ ├── build.gradle.kts └── src/main ├── java/com/example/kortty/effects/AmberTerminalEffectPlugin.java └── resources/META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin ServiceLoader descriptor ( META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin ): com.example.kortty.effects.AmberTerminalEffectPlugin Example implementation: package com.example.kortty.effects ; import de.kortty.plugin.terminaleffects.TerminalEffectAppearance ; import de.kortty.plugin.terminaleffects.TerminalEffectContext ; import de.kortty.plugin.terminaleffects.TerminalEffectPlugin ; import de.kortty.plugin.terminaleffects.TerminalEffectSession ; public final class AmberTerminalEffectPlugin implements TerminalEffectPlugin { @Override public String id () { return \"amber\" ; } @Override public String displayName () { return \"Amber\" ; } @Override public String description () { return \"Amber monochrome terminal colors.\" ; } @Override public TerminalEffectSession createSession ( TerminalEffectContext context ) { return new TerminalEffectSession () { @Override public void start () { context . applyAppearance ( new TerminalEffectAppearance ( \"Monospaced\" , null , \"#FFB000\" , \"#050200\" , \"#FFD37A\" , \"BLINK_BLOCK\" )); } }; } } This example changes appearance only. It does not need an overlay or connector wrapper. Building and Packaging This repository does not currently define a separately published terminal-effect SDK artifact. Plugin code must compile against the KorTTY classes that contain de.kortty.plugin.terminaleffects.* and against the same public libraries the plugin directly imports, such as SithTermFX or JavaFX. For development inside this repository, use the MOTHER source-set pattern in build.gradle.kts : Put plugin sources under a separate source set Include sourceSets.main.output.classesDirs and configurations.compileClasspath in that source set's compile classpath Package only the plugin source-set output into a plugin JAR Include the ServiceLoader descriptor in the plugin JAR The MOTHER and effect-pack tasks are the concrete, tested examples: ./gradlew motherTerminalEffectPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-mother.jar ./gradlew effectPackPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-pack.jar A single plugin JAR can register several effects: the effect pack lists all ten provider classes in one ServiceLoader descriptor. Bundled JARs must additionally be listed in src/main/resources/bundled-plugins/terminal-effects.index , otherwise they are never extracted and loaded. The generated JAR must contain: META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin de/kortty/plugin/terminaleffects/mother/MotherTerminalEffectPlugin.class ... For an out-of-tree plugin, the same rule applies: produce one importable JAR containing your plugin classes and META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin . If you use libraries that KorTTY does not already ship, shade them into that same JAR or remove that dependency. Connector Wrapping Guidelines Use wrapConnector(...) only when the effect must observe, pace, or transform terminal I/O. Pure visual overlays and appearance changes should avoid wrapping connectors. When wrapping: Preserve the TtyConnector contract Delegate write , resize , waitFor , ready , getName , isConnected , and close unless the effect has a specific reason to change them Handle InterruptedException by restoring the interrupt flag and converting to an appropriate checked exception where the TtyConnector method requires it Avoid unbounded buffering because terminal output can be large Avoid delaying control sequences such as ANSI CSI/OSC commands unless the effect intentionally changes terminal protocol behavior Implement TerminalEffectConnectorWrapper so KorTTY can unwrap to the real connector MOTHER's MotherPacedTtyConnector is the current reference for output pacing. It splits terminal output into immediate control sequences and paced visible characters, uses the user animation speed, and bypasses pacing for high-volume output to keep the terminal responsive. Overlay Guidelines Overlays should be JavaFX nodes added to context.overlayRoot() . Good overlay behavior: Set setMouseTransparent(true) so the terminal keeps receiving mouse input Set setManaged(false) if the overlay should not affect layout Bind width and height to the overlay root Stop timelines/animations and unbind properties in stop() Keep drawing cheap enough for repeated repainting Remove the overlay node from overlayRoot() on shutdown Release the canvas backing store while the tab is in the background: a full-window canvas texture per hidden tab can exhaust the Prism texture pool once several effect tabs are open, which crashes rendering. All built-in effects (MOTHER and the effect pack) unbind and shrink their overlay canvas to 0x0 while it is not visible in the scene and rebind when the tab is shown again For split terminals, decide whether the overlay should cover the full terminal tab or track individual SithTermFX widgets. context.widgets() can return more than one widget. Animation Speed Animation speed is a shared user setting for terminal effects: Minimum: 1x Slider maximum: 10x Numeric maximum: 99x Invalid, non-finite, or non-positive values normalize to 1x Use context.animationSpeed() when computing effect timing. The convention used by MOTHER is: long scaledDelayMillis = Math . max ( 1L , Math . round ( baseDelayMillis / context . animationSpeed ())); Do not persist a separate speed value inside the plugin. KorTTY stores the connection/session speed and passes the normalized value through TerminalEffectContext . Import, Export, and Persistence External imports: The user selects a .jar in Plugins > Terminal Effects > Import... KorTTY copies it into ~/.kortty/plugins The plugin manager reloads plugins immediately Exports: A plugin is exportable when it was loaded from a real source JAR Bundled plugins are exportable because KorTTY copies their bundled JARs to ~/.kortty/bundled-plugins/terminal-effects before loading them Exporting one of the ten effect-pack effects exports the whole kortty-terminal-effect-pack.jar , because the JAR is the export unit Application-classpath plugins without a source JAR are not exportable Persistence: Disabled plugin IDs are stored in ~/.kortty/terminal-effect-plugins.disabled Saved connections and restored sessions store the selected terminal-effect plugin ID and animation speed If a saved plugin ID is unavailable or disabled, KorTTY cannot activate it and logs the issue Compatibility and Safety Checklist Before shipping a plugin JAR: Use a stable lowercase plugin ID matching [a-z0-9][a-z0-9._-]{0,63} Keep displayName() and description() non-empty and user-readable Include exactly one ServiceLoader descriptor for every provider class Keep JavaFX mutations on the JavaFX application thread Make stop() safe to call multiple times Remove listeners, timelines, bindings, and overlay nodes in stop() Avoid blocking the JavaFX application thread Avoid spawning unmanaged long-running threads Keep connector wrappers transparent and implement TerminalEffectConnectorWrapper Avoid logging secrets or raw terminal output unless the user explicitly chose that behavior Test with fast output, large output, ANSI color output, split terminals, reconnect, plugin disable/enable, and application shutdown Manual Validation Build and inspect: ./gradlew motherTerminalEffectPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-mother.jar Run KorTTY: ./gradlew run Validate plugin management: Open Plugins > Terminal Effects Confirm all built-in plugins appear with name and description Select a row and confirm the animated preview plays next to the table Disable and enable a plugin Export one and inspect the exported JAR with jar tf Import the exported JAR into a clean KorTTY config or another build The terminalEffectPreviewSmoke Gradle task renders every built-in preview and the manager dialog headless into build/smoke/ for a quick offline check. Validate session behavior: Activate the effect from the terminal context menu or View > Terminal Effect Select it in Quick Connect and in a saved Connection Manager entry Change animation speed with the slider and with the numeric field Run commands that produce slow, fast, colored, and large output Reconnect the tab and close it Check ~/.kortty/kortty.log for plugin loading warnings, duplicate IDs, invalid providers, or stop/cleanup errors","title": "Terminal effect plugins"},{"location": "features/terminal-effect-plugins.html#terminal-effect-plugins","text": "KorTTY terminal-effect plugins are Java ServiceLoader plugins that can change the appearance and runtime behavior of a terminal session. They are trusted local code that runs inside the KorTTY JVM and integrate through a clean SPI interface.","title": "Terminal effect plugins"},{"location": "features/terminal-effect-plugins.html#capabilities","text": "A terminal-effect plugin can: Apply terminal appearance values such as font, foreground/background colors, cursor color, and cursor style Add JavaFX overlay nodes above the terminal area (for example scanlines, noise, vignettes, or line highlights) Wrap the SithTermFX TtyConnector to observe, pace, or transform terminal output before the terminal emulator receives it React to the user-selected terminal-effect animation speed Provide an animated preview that the plugin manager shows for the selected plugin (optional createPreview() )","title": "Capabilities"},{"location": "features/terminal-effect-plugins.html#built-in-effects","text": "KorTTY ships eleven built-in effects as two bundled, exportable plugin JARs: the MOTHER reference implementation ( kortty-terminal-effect-mother.jar ) and the effect pack ( kortty-terminal-effect-pack.jar ) with ten themed effects. ID Name Description mother MU/TH/UR 6000 ALIEN-style green CRT terminal appearance with paced line output. amber-crt-90 Amber CRT '90 Amber phosphor CRT monitor from the 90s with scanlines, glow, flicker and a rolling refresh band. commodore-blue Commodore Heritage Classic C64 home computer look: light blue on blue with a chunky cursor and loader bars. neon-city Neon City Cyberpunk neon look with glitch tears, RGB-split flickers and pulsing glow. digital-rain Digital Rain Green-on-black matrix style with a faint stream of falling glyphs. hologram-hud Hologram HUD Translucent sci-fi hologram with interference bands, HUD corner brackets and flicker. poltergeist Poltergeist Haunted monochrome terminal with a breathing vignette, static bursts and ghostly flashes. vhs-1987 VHS 1987 Worn VHS tape playback with tracking noise, rolling distortion and a PLAY overlay. synthwave-horizon Synthwave Horizon Retro-80s synthwave palette with a glowing perspective grid on the horizon. deep-space-radar Deep Space Radar Tactical deep-space console with a slow radar sweep, faint blips and frame corners. typewriter-noir Typewriter Noir Sepia paper and ink noir look with per-character typewriter output pacing. All built-in effects override the terminal appearance (font, colors, cursor) while active and draw their animation as a mouse-transparent canvas overlay. Typewriter Noir additionally wraps the connector to pace output character by character. Effect names are proper nouns and stay untranslated; descriptions are localized through the plugin.terminalEffects.desc.* message keys with an English fallback.","title": "Built-in Effects"},{"location": "features/terminal-effect-plugins.html#runtime-model","text": "Terminal-effect plugins are loaded by de.kortty.core.TerminalEffectPluginManager in this order: Application classpath plugins Bundled plugin JARs listed by bundled-plugins/terminal-effects.index External JARs copied into ~/.kortty/plugins If two plugins expose the same plugin ID, the first one wins and later duplicates are ignored. This is intentional because connection settings persist the plugin ID. External plugins are loaded from one JAR at a time with KorTTY's application classloader as parent. Dependencies that are already part of KorTTY can be used from that parent classloader. Dependencies that are not part of KorTTY are not discovered from adjacent files automatically; either avoid them or shade them into the plugin JAR. Warning Terminal-effect plugins are trusted local Java code. KorTTY does not sandbox imported JARs. A plugin runs inside the KorTTY JVM with the same local process permissions as KorTTY itself. Only import plugins from sources you trust.","title": "Runtime Model"},{"location": "features/terminal-effect-plugins.html#plugin-management-in-the-ui","text": "Users manage terminal-effect plugins from Plugins > Terminal Effects . The Terminal Effects manager window is resizable, so you can expand the plugin table and descriptions while keeping the live preview visible. If tool windows are configured to open as tabs, the manager instead uses the available main-window tab area. The dialog shows the loaded plugin list with: Active/inactive state Display name Short description Next to the table, a preview panel plays a live animated preview of the selected plugin (a small fake terminal in the effect's colors with the effect overlay on top). If a plugin does not implement createPreview() , the panel shows a placeholder text instead. The preview stops when the selection changes, the global terminal-effects switch is turned off, or the dialog closes. Users can: Enable or disable a plugin Import an external .jar plugin, which KorTTY copies to ~/.kortty/plugins Export plugins that have a source JAR (the bundled MOTHER and effect-pack JARs are exportable) Users select effects per terminal session from the terminal context menu or View > Terminal Effect . Saved connections can also store the selected effect and animation speed through Quick Connect and Connection Manager. The speed slider covers 1x through 10x ; the numeric field accepts values up to 99x .","title": "Plugin Management in the UI"},{"location": "features/terminal-effect-plugins.html#spi-classes","text": "The public SPI lives under de.kortty.plugin.terminaleffects .","title": "SPI Classes"},{"location": "features/terminal-effect-plugins.html#terminaleffectplugin","text": "Implement TerminalEffectPlugin as the ServiceLoader entry point. public interface TerminalEffectPlugin { String id (); String displayName (); default String description () { return \"\" ; } TerminalEffectSession createSession ( TerminalEffectContext context ); default TerminalEffectPreview createPreview () { return null ; } } Rules: id() must be stable because connection settings and disabled-plugin state persist it Valid IDs match the regex [a-z0-9][a-z0-9._-]{0,63} displayName() must not be blank description() is shown in the plugin-management table and should be one short sentence The provider class must be loadable by ServiceLoader ; use a public class with a public no-argument constructor createPreview() is optional; returning null (the default) shows a placeholder in the plugin manager","title": "TerminalEffectPlugin"},{"location": "features/terminal-effect-plugins.html#terminaleffectsession","text": "createSession returns one TerminalEffectSession per terminal tab/effect activation. public interface TerminalEffectSession extends AutoCloseable { default void start () {} default @NotNull TtyConnector wrapConnector ( @Nullable SithTermFxWidget widget , @NotNull TtyConnector connector ) { return connector ; } default void stop () {} } Responsibilities: Allocate UI resources in start() Decorate connectors in wrapConnector(...) when output pacing or filtering is needed Remove listeners, stop timelines, unbind properties, and release references in stop() stop() must be idempotent. It can be called when a user disables the effect, switches effects, closes a tab, or reloads plugins.","title": "TerminalEffectSession"},{"location": "features/terminal-effect-plugins.html#terminaleffectcontext","text": "TerminalEffectContext exposes the active terminal environment: Method Purpose pluginId() The active plugin ID terminalView() The owning KorTTY terminal view overlayRoot() A JavaFX StackPane above the terminal area; add overlay nodes here widgets() Current SithTermFX widgets for the terminal view; split terminals can have more than one widget animationSpeed() User-selected speed normalized by TerminalEffectAnimationSpeed applyAppearance(TerminalEffectAppearance) Applies terminal appearance overrides restoreAppearance() Restores the baseline appearance captured before the effect was activated JavaFX scene-graph changes must run on the JavaFX application thread. If a callback can arrive from a terminal worker thread, use Platform.runLater(...) for overlay or UI updates.","title": "TerminalEffectContext"},{"location": "features/terminal-effect-plugins.html#terminaleffectappearance","text": "TerminalEffectAppearance is a record with nullable fields: public record TerminalEffectAppearance ( @Nullable String fontFamily , @Nullable Integer fontSize , @Nullable String foregroundColor , @Nullable String backgroundColor , @Nullable String cursorColor , @Nullable String cursorStyle ) { } Use only the fields your effect needs. null leaves the current value unchanged. The current implementation passes color values as strings, for example #19FF4C . Cursor style is also a string; the MOTHER effect uses BLINK_BLOCK . Reuse values already accepted by KorTTY's terminal settings instead of inventing new names.","title": "TerminalEffectAppearance"},{"location": "features/terminal-effect-plugins.html#terminaleffectpreview","text": "createPreview() returns the animated preview shown in the plugin manager: public interface TerminalEffectPreview { @NotNull Node node (); default void start () {} default void stop () {} } Contract: One preview instance backs one displayed preview; the caller requests node() once, calls start() after attaching it, and stop() before discarding it Do not construct JavaFX objects before node() is called, so plugin metadata stays usable without a running JavaFX toolkit stop() must stop all timelines and release animation resources TerminalEffectPreviewCanvas (same package) is a reusable implementation used by all built-in effects: a 360x220 fake terminal with configurable colors, fake shell lines, a blinking cursor, and an optional effect overlay canvas on top. Its builder collects plain data only and creates JavaFX nodes lazily in node() .","title": "TerminalEffectPreview"},{"location": "features/terminal-effect-plugins.html#terminaleffectconnectorwrapper","text": "If a plugin wraps a connector, implement TerminalEffectConnectorWrapper and delegate all TtyConnector methods to the wrapped connector unless the effect intentionally changes that method. public interface TerminalEffectConnectorWrapper extends TtyConnector { TtyConnector delegate (); } KorTTY uses this marker to unwrap effect connectors before applying another wrapper or before accessing the underlying SSH connector. Without it, reconnect, drag/drop upload, AI-agent current-directory tracking, or future connector-level features can see the wrong connector.","title": "TerminalEffectConnectorWrapper"},{"location": "features/terminal-effect-plugins.html#minimal-plugin-example","text": "Directory layout: example-terminal-effect/ ├── build.gradle.kts └── src/main ├── java/com/example/kortty/effects/AmberTerminalEffectPlugin.java └── resources/META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin ServiceLoader descriptor ( META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin ): com.example.kortty.effects.AmberTerminalEffectPlugin Example implementation: package com.example.kortty.effects ; import de.kortty.plugin.terminaleffects.TerminalEffectAppearance ; import de.kortty.plugin.terminaleffects.TerminalEffectContext ; import de.kortty.plugin.terminaleffects.TerminalEffectPlugin ; import de.kortty.plugin.terminaleffects.TerminalEffectSession ; public final class AmberTerminalEffectPlugin implements TerminalEffectPlugin { @Override public String id () { return \"amber\" ; } @Override public String displayName () { return \"Amber\" ; } @Override public String description () { return \"Amber monochrome terminal colors.\" ; } @Override public TerminalEffectSession createSession ( TerminalEffectContext context ) { return new TerminalEffectSession () { @Override public void start () { context . applyAppearance ( new TerminalEffectAppearance ( \"Monospaced\" , null , \"#FFB000\" , \"#050200\" , \"#FFD37A\" , \"BLINK_BLOCK\" )); } }; } } This example changes appearance only. It does not need an overlay or connector wrapper.","title": "Minimal Plugin Example"},{"location": "features/terminal-effect-plugins.html#building-and-packaging","text": "This repository does not currently define a separately published terminal-effect SDK artifact. Plugin code must compile against the KorTTY classes that contain de.kortty.plugin.terminaleffects.* and against the same public libraries the plugin directly imports, such as SithTermFX or JavaFX. For development inside this repository, use the MOTHER source-set pattern in build.gradle.kts : Put plugin sources under a separate source set Include sourceSets.main.output.classesDirs and configurations.compileClasspath in that source set's compile classpath Package only the plugin source-set output into a plugin JAR Include the ServiceLoader descriptor in the plugin JAR The MOTHER and effect-pack tasks are the concrete, tested examples: ./gradlew motherTerminalEffectPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-mother.jar ./gradlew effectPackPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-pack.jar A single plugin JAR can register several effects: the effect pack lists all ten provider classes in one ServiceLoader descriptor. Bundled JARs must additionally be listed in src/main/resources/bundled-plugins/terminal-effects.index , otherwise they are never extracted and loaded. The generated JAR must contain: META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin de/kortty/plugin/terminaleffects/mother/MotherTerminalEffectPlugin.class ... For an out-of-tree plugin, the same rule applies: produce one importable JAR containing your plugin classes and META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin . If you use libraries that KorTTY does not already ship, shade them into that same JAR or remove that dependency.","title": "Building and Packaging"},{"location": "features/terminal-effect-plugins.html#connector-wrapping-guidelines","text": "Use wrapConnector(...) only when the effect must observe, pace, or transform terminal I/O. Pure visual overlays and appearance changes should avoid wrapping connectors. When wrapping: Preserve the TtyConnector contract Delegate write , resize , waitFor , ready , getName , isConnected , and close unless the effect has a specific reason to change them Handle InterruptedException by restoring the interrupt flag and converting to an appropriate checked exception where the TtyConnector method requires it Avoid unbounded buffering because terminal output can be large Avoid delaying control sequences such as ANSI CSI/OSC commands unless the effect intentionally changes terminal protocol behavior Implement TerminalEffectConnectorWrapper so KorTTY can unwrap to the real connector MOTHER's MotherPacedTtyConnector is the current reference for output pacing. It splits terminal output into immediate control sequences and paced visible characters, uses the user animation speed, and bypasses pacing for high-volume output to keep the terminal responsive.","title": "Connector Wrapping Guidelines"},{"location": "features/terminal-effect-plugins.html#overlay-guidelines","text": "Overlays should be JavaFX nodes added to context.overlayRoot() . Good overlay behavior: Set setMouseTransparent(true) so the terminal keeps receiving mouse input Set setManaged(false) if the overlay should not affect layout Bind width and height to the overlay root Stop timelines/animations and unbind properties in stop() Keep drawing cheap enough for repeated repainting Remove the overlay node from overlayRoot() on shutdown Release the canvas backing store while the tab is in the background: a full-window canvas texture per hidden tab can exhaust the Prism texture pool once several effect tabs are open, which crashes rendering. All built-in effects (MOTHER and the effect pack) unbind and shrink their overlay canvas to 0x0 while it is not visible in the scene and rebind when the tab is shown again For split terminals, decide whether the overlay should cover the full terminal tab or track individual SithTermFX widgets. context.widgets() can return more than one widget.","title": "Overlay Guidelines"},{"location": "features/terminal-effect-plugins.html#animation-speed","text": "Animation speed is a shared user setting for terminal effects: Minimum: 1x Slider maximum: 10x Numeric maximum: 99x Invalid, non-finite, or non-positive values normalize to 1x Use context.animationSpeed() when computing effect timing. The convention used by MOTHER is: long scaledDelayMillis = Math . max ( 1L , Math . round ( baseDelayMillis / context . animationSpeed ())); Do not persist a separate speed value inside the plugin. KorTTY stores the connection/session speed and passes the normalized value through TerminalEffectContext .","title": "Animation Speed"},{"location": "features/terminal-effect-plugins.html#import-export-and-persistence","text": "External imports: The user selects a .jar in Plugins > Terminal Effects > Import... KorTTY copies it into ~/.kortty/plugins The plugin manager reloads plugins immediately Exports: A plugin is exportable when it was loaded from a real source JAR Bundled plugins are exportable because KorTTY copies their bundled JARs to ~/.kortty/bundled-plugins/terminal-effects before loading them Exporting one of the ten effect-pack effects exports the whole kortty-terminal-effect-pack.jar , because the JAR is the export unit Application-classpath plugins without a source JAR are not exportable Persistence: Disabled plugin IDs are stored in ~/.kortty/terminal-effect-plugins.disabled Saved connections and restored sessions store the selected terminal-effect plugin ID and animation speed If a saved plugin ID is unavailable or disabled, KorTTY cannot activate it and logs the issue","title": "Import, Export, and Persistence"},{"location": "features/terminal-effect-plugins.html#compatibility-and-safety-checklist","text": "Before shipping a plugin JAR: Use a stable lowercase plugin ID matching [a-z0-9][a-z0-9._-]{0,63} Keep displayName() and description() non-empty and user-readable Include exactly one ServiceLoader descriptor for every provider class Keep JavaFX mutations on the JavaFX application thread Make stop() safe to call multiple times Remove listeners, timelines, bindings, and overlay nodes in stop() Avoid blocking the JavaFX application thread Avoid spawning unmanaged long-running threads Keep connector wrappers transparent and implement TerminalEffectConnectorWrapper Avoid logging secrets or raw terminal output unless the user explicitly chose that behavior Test with fast output, large output, ANSI color output, split terminals, reconnect, plugin disable/enable, and application shutdown","title": "Compatibility and Safety Checklist"},{"location": "features/terminal-effect-plugins.html#manual-validation","text": "Build and inspect: ./gradlew motherTerminalEffectPluginJar jar tf build/terminal-effect-plugins/kortty-terminal-effect-mother.jar Run KorTTY: ./gradlew run Validate plugin management: Open Plugins > Terminal Effects Confirm all built-in plugins appear with name and description Select a row and confirm the animated preview plays next to the table Disable and enable a plugin Export one and inspect the exported JAR with jar tf Import the exported JAR into a clean KorTTY config or another build The terminalEffectPreviewSmoke Gradle task renders every built-in preview and the manager dialog headless into build/smoke/ for a quick offline check. Validate session behavior: Activate the effect from the terminal context menu or View > Terminal Effect Select it in Quick Connect and in a saved Connection Manager entry Change animation speed with the slider and with the numeric field Run commands that produce slow, fast, colored, and large output Reconnect the tab and close it Check ~/.kortty/kortty.log for plugin loading warnings, duplicate IDs, invalid providers, or stop/cleanup errors","title": "Manual Validation"},{"location": "features/terminal.html","text": "Terminal sessions KorTTY provides a tabbed terminal interface with support for multiple simultaneous SSH connections, split-screen layouts, and interactive terminal management features. This guide covers tab operations, multi-window support, terminal customization, and advanced session features. Session lifecycle The following diagram shows the terminal tab and session lifecycle, including split-screen and broadcast modes. Working with tabs Manage multiple SSH sessions with these tab operations: Action Shortcut New Tab Ctrl + T (Cmd+T on macOS) — opens Quick Connect to start a new session Close Tab Ctrl + W (Cmd+W on macOS) — closes the active tab. You are only asked to confirm when there is something to lose: the tab has split panes, or a command is still running (a local shell with a running child process, or an SSH session that is not at its prompt). An idle single terminal closes immediately. The per-connection Close without confirmation setting suppresses the prompt entirely. Next Tab ++ctrl+Tab++ Previous Tab ++ctrl+shift+Tab++ Reconnect Right-click a tab, the terminal area, or a server entry in the Dashboard. If the connection is active, it is closed and re-established immediately; if disconnected, it is re-established. The terminal window stays open. Tab Groups Right-click a tab to assign it to a named group for better organization Connecting safely Interactive SSH terminals share host-key trust with SFTP and the SSH bootstrap used by Mosh. The first connection to a normalized host and port shows the key algorithm and OpenSSH SHA-256 fingerprint with No selected by default. After you verify and accept it, exact matches connect silently; a changed key is hard-blocked with no automatic retry. See SSH host-key verification . Opening a same-server or newly selected connection in a split shows a progress dialog while the SSH handshake runs on a worker. The interface remains responsive for both the host-key confirmation and keyboard-interactive authentication prompts. Some failures are refused outright rather than retried, because repeating the attempt cannot change the outcome — a changed host key, a Mosh connection configured with a jump server, or a missing Mosh runtime. The terminal clears and shows the reason immediately instead of working through the retry count. See Jump server for the Mosh restriction. KorTTY's pinned SithTermFX build also includes a reviewed bottom-row boundary fix: moving over a hyperlink or the final visible terminal row no longer asks TerminalTextBuffer for the non-existent row at line == height . Connection loss and automatic reconnect When an established SSH connection is lost — network drop, VPN cut, server gone — the tab does not close. It switches to a red disconnected state instead: the tab title gets a (DISCONNECT) suffix, the tab turns dark red, a red status bar shows the time the connection was lost, and the terminal cursor stops blinking so a dead session no longer looks alive. Only a normal remote logout (typing exit , or Ctrl + D at the prompt) closes the tab. KorTTY notices a silent transport death within about ten seconds: every few seconds it sends an SSH liveness probe (a global request the server must answer, the same technique as OpenSSH's ServerAliveInterval ) and treats two consecutive unanswered probes as a lost connection. The probe only arms itself after the server has answered once, so servers that never reply to such requests keep their sessions untouched. This is independent of the SSH keep-alive heartbeat, which keeps idle connections open but does not detect a dead one. To pick the session back up in the same tab, double-click the red status bar or the red tab, or use Reconnect in the tab, terminal, or Dashboard context menu. In a split tab, panes whose connection died close individually; the last remaining pane keeps the tab open and carries the reconnect offer. With Automatically reconnect lost connections enabled ( Settings → Terminal , on by default), the tab reconnects on its own: attempts start after 3 seconds and back off through 5, 10, 20 and 30 seconds up to one attempt per minute, and the red status bar counts down to the next attempt. A successful reconnect, a manual reconnect, or closing the tab ends the automatic attempts. Permanent failures — authentication, host-key verification, configuration refusals — stop them too, so a wrong password is never hammered against the server. While a session journal is running, its red decision bar takes precedence and no automatic attempt starts — the journal asks whether to reconnect and continue or to end with its closing summary. See Settings → Terminal for the setting. Multi-window support Open additional windows to organize connections by project or environment: New Window : Ctrl + Shift + N (Cmd+Shift+N on macOS) opens a new KorTTY window. Each window can have its own set of tabs and connections. Move tabs between windows : Drag a tab from the tab bar and drop it onto another KorTTY window's tab bar to move that tab (and its session, including any split terminals) into the other window. Reorder tabs : Drag a tab within the same window to change its order; the \"+\" tab stays at the end. Font size and zoom Adjust the font size of the active terminal on the fly without reconnecting: Shortcut Action Alt + + Zoom in (increase font size) Alt + - Zoom out (decrease font size) Alt + 0 Reset zoom to saved/default font Ctrl + mouse wheel Zoom in/out over the terminal (Cmd + wheel on macOS) Holding Ctrl (or Cmd on macOS) and scrolling the mouse wheel over the terminal changes the font size — wheel up enlarges, wheel down shrinks — instead of scrolling the buffer. This complements the Alt + + / Alt + - / Alt + 0 shortcuts. Reset zoom restores the font size and family to what the connection had when you opened the tab (or the connection's saved settings, or the global default). The same reset is available via the terminal context menu: right-click → Font size → Reset . The zoom level applies only to the currently focused terminal. Background transparency View → Zoom → Background Transparency is a slider (0–100 %) that makes the terminal background see-through to the desktop while the text stays fully opaque and sharp. At 0 % the background is solid; higher values let more of the desktop show through. The value is saved and restored across restarts. Only the terminal area becomes transparent — the title bar, menu bar, status bar and any tab without a terminal stay solid, so the window never turns into a see-through hole. Horizontal, vertical and nested split terminals inherit the active transparency level, including panes added after transparency was enabled. Entering fullscreen with F12 or terminal-only fullscreen with Ctrl + Shift + F temporarily renders the terminal area opaque without changing the saved value; leaving fullscreen restores that value to every pane. Because a see-through window uses a different window style that the operating system fixes when the window opens, switching transparency on or off (crossing 0 %) only takes full effect after a restart ; the status bar shows a hint when you cross that threshold. Adjusting the level while already in transparent mode applies live. In transparent mode the window uses a lightweight custom title bar (drag to move, buttons to minimise/maximise/close, double-click the strip to maximise, drag the edges to resize). The slider lives in the in-window menu bar only (the native macOS menu bar cannot host a slider). Local shell tabs Besides SSH and Mosh, a terminal tab can host a Local Shell — the local machine's own shell, opened via a pseudo-terminal (see Local Shell ). A few terminal behaviors are local-shell aware: Ctrl + D closes the tab for local cmd.exe/PowerShell sessions. Those Windows shells do not exit on EOF, so Ctrl + D would otherwise have no effect. For bash-family shells (Git Bash/Cygwin/WSL, macOS/Linux) and SSH, Ctrl + D keeps its normal EOF meaning — the shell exits and the local tab then auto-closes. Close confirmation uses local-shell wording rather than \"End SSH connection?\", and the window-close prompt is transport-neutral (\"Active sessions\"), since one window can mix SSH, Mosh and local-shell tabs. The current directory follows the interactive shell. On macOS and Linux, korTTY refreshes it from the local shell process; native PowerShell and cmd prompts supply absolute Windows paths. After cd , pushd , popd , or Set-Location , Open in Snippet Editor resolves a selected file name against that current directory instead of the tab's start directory. If the directory cannot be determined or mapped safely, korTTY stops with an error rather than opening a same-named file from the wrong directory. After an identity switch, Open in Snippet Editor is greyed out. When the session no longer runs as the identity the tab was opened with — after su , an inner ssh , or a shell-opening sudo — the context-menu entry is disabled, in SSH tabs as well as local-shell tabs: the tab's tracked directories and file access still belong to the original login and would resolve the wrong path. The entry re-enables on its own once the prompt shows the original user again (typically after exit ). A local-shell tab whose configured shell command is itself a remote client such as ssh or mosh keeps the entry disabled for the whole tab. If the load is triggered anyway, korTTY stops with an error instead of resolving the wrong path. The AI context-menu actions follow the same rule: they no longer offer to attach the selected file name's content (see Attaching a selected file to the chat ). Clipboard text is preserved in agent shortcuts. Typed and pasted text travel through the same terminal-input filter, including bracketed paste and split UTF-8 input, so a pasted file name remains part of the agent ... request and Enter dispatches it exactly once. Session journal Every terminal tab can keep a session journal : server output and typed commands go into a capture log, an AI condenses them into a readable timeline, and screenshots and notes can be added from the journal bar or the terminal's right-click menu. Journals start automatically for connections that enable them, or retroactively for a running session via Tools > Start/Stop Session Journal — the existing scrollback is imported. See Session journal . Split-screen with broadcast Split the terminal view to display multiple connections side by side, and optionally send input to all panes at once. Split operations Split Pane : Create horizontal or vertical splits within a tab via the context menu or keyboard shortcuts. Independent Sessions : Each pane can show a different SSH connection. Resizable Panes : Drag dividers to adjust pane sizes. Access reason asked once per tab : when a server asks for a reason for the connection, as a CyberArk-style jump host does, a split does not ask again. korTTY sends the reason that was given when the tab was opened, because a server that asks for one closes a session that answers with nothing. A split to a different server, or a server asking something else, is asked once as well, and a new tab always starts by asking. If the server refuses the reason, for example because a ticket number has expired in the meantime, korTTY drops it and asks again on the next attempt. Move Panes : Hold Shift + Alt (Windows/Linux) or Shift + Option (macOS) and drag a pane onto another to reorder. Without the modifiers, mouse drag is used for text selection in the terminal. Broadcast mode When Broadcast Mode is enabled, keyboard input is sent simultaneously to all visible panes. This is useful for running the same commands on multiple servers. Terminal effects Terminal effects can change the visible terminal style and output animation. Effects are Java plugins managed from Plugins > Terminal Effects . User controls Current terminal : Use View > Terminal Effect or the terminal context menu to choose an effect for the active terminal. Quick Connect : Choose the effect and speed before opening a temporary or saved connection. Connection Manager : Store the effect and speed on a saved connection so new tabs use it automatically. Speed : Use the slider for 1x through 10x ; if that is still too slow, type a custom value up to 99x in the numeric speed field. Plugin management Open Plugins > Terminal Effects to manage plugins. The table lists loaded plugins with active state, name, and description. Disable a plugin to keep it installed but unavailable for activation. Import external .jar plugins. KorTTY copies them into ~/.kortty/plugins . Export plugins that have a source JAR. The bundled MOTHER effect is exportable. Warning Imported terminal-effect plugins are trusted Java code and are not sandboxed. Only import plugins from sources you trust. For detailed plugin development documentation, see Terminal effect plugins . SSH keep-alive Prevent connections from dropping due to inactivity by configuring SSH keep-alive messages: Enable SSH Keep-Alive in the connection's Terminal tab or in Settings > Terminal . Set the interval (5 to 600 seconds, default: 60). KorTTY sends SSH_MSG_IGNORE heartbeat messages at the configured interval and enables TCP socket keepalive while the option is active. Note If a server, firewall, VPN, or NAT gateway closes idle sessions sooner than the configured interval, the connection can still end. In that case, check the server-side SSH configuration and network idle-timeout settings as well as the KorTTY log. Terminal logging Writes a connection's terminal output to a file, for audit and debugging. This is independent of the Session Journal : it is a plain transcript with no summaries, markers or screenshots, and the two can run at the same time. Configure it in either place: Connection Manager > Edit connection > Logging for a saved connection. Quick Connect > Terminal log for a one-off session, or to change the setting for the connection you are about to open. Enable logging. Choose a log folder . Left empty, KorTTY uses ~/.kortty/terminal-logs . You pick the folder; the file names are KorTTY's. Choose a log format: Plain Text - One timestamped line per line of output. XML - Structured XML with timestamps. JSON - Structured JSON with timestamps. Optionally adjust the maximum file size (default: 10 MB) and the retention period (default: 30 days), and turn off Start a new file every day or Compress closed files (gzip) — both are on by default. Quick Connect's Terminal log section covers enable, folder, format and compression; size limit, retention and daily rotation keep their configured or default values. File names Every file is named <date>-<time>-<server>_<number> , for example 2026-08-04-14-30-12-web01_1.log.gz . The date leads so a folder listing sorts chronologically, and the trailing number distinguishes connections that are open at the same time — two tabs on the same server get _1 and _2 and never write into one another's file. Rotation, compression and retention By default a new file is started every day , and always again whenever the maximum size is reached (those parts are numbered .p2 , .p3 , …); daily rotation can be turned off to roll only by size. Nothing is ever overwritten or deleted by rotation. Closed files are gzipped by default; the file currently being written always stays uncompressed so that a crash cannot truncate it. Turn off Compress closed files (gzip) to keep finished files as plain text instead. A connection that produces no output creates no file at all. Files older than the retention period are deleted automatically when a connection starts and after each daily rollover. Set the retention to 0 to keep everything. Only KorTTY's own log files are ever removed — anything else in the folder is left alone, so it is safe to point the setting at a folder you also use for other things. What is removed before writing Captured lines go through the same redaction as the Session Journal , on the capture thread, before anything is buffered or written: the connection's own password and any replacement rules your organisation's policy defines. The secret never reaches the file, so there is nothing to clean up afterwards. Log files and a log folder KorTTY created itself are set to owner-only permissions where the filesystem supports it. A folder you chose yourself is left with the permissions you gave it. Redaction only covers what KorTTY knows A password KorTTY stores for the connection is redacted. A secret you type into a command yourself, or one a program prints, is not — KorTTY has no way to recognise it. Treat the log folder as sensitive, and use policy replacement rules for patterns that recur. Terminal recording Terminal recording is designed as a low-resource replay feature. KorTTY records terminal screen-state changes and timing events into one JDK/GZIP streaming-compressed .korttyrec.jsonl.gz file per terminal tab session. Legacy .korttyrec.jsonl replay files remain readable. Configure recordings To enable recording automatically after every app restart, open Settings > Video and enable Enable terminal recording after app restart . To enable recording only for this session, open Tools > Video Manager... and select Enable terminal recording for this app session . Set the Storage path . If left at the default, KorTTY uses ~/.kortty/recordings . Choose the default format and default split scope. KorTTY replay is always available; video export requires ffmpeg . Enable or disable Auto-pause when the terminal is idle and set the idle threshold (default: 20 seconds). Optional: enable Capture terminal colors in new recordings if exported videos should reproduce terminal colors. Optional: set the ffmpeg path and click Check . If ffmpeg is missing, video export stays disabled but replay files remain usable. Click Save . Start and stop recording Open or focus an SSH terminal tab. If terminal recording is enabled, click Start recording in the terminal bar, choose Tools > Start/Stop Terminal Recording , or press Ctrl + Shift + E (Cmd+Shift+E on macOS). If the tab contains multiple split terminals, choose whether to record only the active split or the whole tab. Click Stop recording or press Ctrl + Shift + E again to stop the current segment. Start and stop as often as needed in the same tab. KorTTY appends all segments to the same replay file until the tab closes. Export a video Open Tools > Video Manager... . Select a .korttyrec.jsonl.gz replay file from the list. Verify that the ffmpeg status says video export is enabled. Click Export... . In the export options: Choose Export entire recording or enter start/end times with minute or MM:SS format. Choose whether to include terminal colors (available only if the replay contains color data). Choose WebM/VP9 or MKV/FFV1 format, then select an output path. While KorTTY renders frames and runs ffmpeg , the export progress dialog shows the current phase, progress bar, and estimated remaining time. Export uses the recorded terminal geometry so large terminal screens are not cropped. View and manage recordings Open Tools > Video Manager... . Select a .korttyrec.jsonl.gz replay file. Click View to play the replay directly inside KorTTY. Use the replay viewer timeline to scrub, or enter a Time jump value such as 5 for minute 5 or 5:30 for minute 5 and 30 seconds. Set Speed between 1x and 20x to control playback speed. Click Rename... to rename the replay file. Click Delete to delete the selected replay after confirmation.","title": "Terminal sessions"},{"location": "features/terminal.html#terminal-sessions","text": "KorTTY provides a tabbed terminal interface with support for multiple simultaneous SSH connections, split-screen layouts, and interactive terminal management features. This guide covers tab operations, multi-window support, terminal customization, and advanced session features.","title": "Terminal sessions"},{"location": "features/terminal.html#session-lifecycle","text": "The following diagram shows the terminal tab and session lifecycle, including split-screen and broadcast modes.","title": "Session lifecycle"},{"location": "features/terminal.html#working-with-tabs","text": "Manage multiple SSH sessions with these tab operations: Action Shortcut New Tab Ctrl + T (Cmd+T on macOS) — opens Quick Connect to start a new session Close Tab Ctrl + W (Cmd+W on macOS) — closes the active tab. You are only asked to confirm when there is something to lose: the tab has split panes, or a command is still running (a local shell with a running child process, or an SSH session that is not at its prompt). An idle single terminal closes immediately. The per-connection Close without confirmation setting suppresses the prompt entirely. Next Tab ++ctrl+Tab++ Previous Tab ++ctrl+shift+Tab++ Reconnect Right-click a tab, the terminal area, or a server entry in the Dashboard. If the connection is active, it is closed and re-established immediately; if disconnected, it is re-established. The terminal window stays open. Tab Groups Right-click a tab to assign it to a named group for better organization","title": "Working with tabs"},{"location": "features/terminal.html#connecting-safely","text": "Interactive SSH terminals share host-key trust with SFTP and the SSH bootstrap used by Mosh. The first connection to a normalized host and port shows the key algorithm and OpenSSH SHA-256 fingerprint with No selected by default. After you verify and accept it, exact matches connect silently; a changed key is hard-blocked with no automatic retry. See SSH host-key verification . Opening a same-server or newly selected connection in a split shows a progress dialog while the SSH handshake runs on a worker. The interface remains responsive for both the host-key confirmation and keyboard-interactive authentication prompts. Some failures are refused outright rather than retried, because repeating the attempt cannot change the outcome — a changed host key, a Mosh connection configured with a jump server, or a missing Mosh runtime. The terminal clears and shows the reason immediately instead of working through the retry count. See Jump server for the Mosh restriction. KorTTY's pinned SithTermFX build also includes a reviewed bottom-row boundary fix: moving over a hyperlink or the final visible terminal row no longer asks TerminalTextBuffer for the non-existent row at line == height .","title": "Connecting safely"},{"location": "features/terminal.html#connection-loss-and-automatic-reconnect","text": "When an established SSH connection is lost — network drop, VPN cut, server gone — the tab does not close. It switches to a red disconnected state instead: the tab title gets a (DISCONNECT) suffix, the tab turns dark red, a red status bar shows the time the connection was lost, and the terminal cursor stops blinking so a dead session no longer looks alive. Only a normal remote logout (typing exit , or Ctrl + D at the prompt) closes the tab. KorTTY notices a silent transport death within about ten seconds: every few seconds it sends an SSH liveness probe (a global request the server must answer, the same technique as OpenSSH's ServerAliveInterval ) and treats two consecutive unanswered probes as a lost connection. The probe only arms itself after the server has answered once, so servers that never reply to such requests keep their sessions untouched. This is independent of the SSH keep-alive heartbeat, which keeps idle connections open but does not detect a dead one. To pick the session back up in the same tab, double-click the red status bar or the red tab, or use Reconnect in the tab, terminal, or Dashboard context menu. In a split tab, panes whose connection died close individually; the last remaining pane keeps the tab open and carries the reconnect offer. With Automatically reconnect lost connections enabled ( Settings → Terminal , on by default), the tab reconnects on its own: attempts start after 3 seconds and back off through 5, 10, 20 and 30 seconds up to one attempt per minute, and the red status bar counts down to the next attempt. A successful reconnect, a manual reconnect, or closing the tab ends the automatic attempts. Permanent failures — authentication, host-key verification, configuration refusals — stop them too, so a wrong password is never hammered against the server. While a session journal is running, its red decision bar takes precedence and no automatic attempt starts — the journal asks whether to reconnect and continue or to end with its closing summary. See Settings → Terminal for the setting.","title": "Connection loss and automatic reconnect"},{"location": "features/terminal.html#multi-window-support","text": "Open additional windows to organize connections by project or environment: New Window : Ctrl + Shift + N (Cmd+Shift+N on macOS) opens a new KorTTY window. Each window can have its own set of tabs and connections. Move tabs between windows : Drag a tab from the tab bar and drop it onto another KorTTY window's tab bar to move that tab (and its session, including any split terminals) into the other window. Reorder tabs : Drag a tab within the same window to change its order; the \"+\" tab stays at the end.","title": "Multi-window support"},{"location": "features/terminal.html#font-size-and-zoom","text": "Adjust the font size of the active terminal on the fly without reconnecting: Shortcut Action Alt + + Zoom in (increase font size) Alt + - Zoom out (decrease font size) Alt + 0 Reset zoom to saved/default font Ctrl + mouse wheel Zoom in/out over the terminal (Cmd + wheel on macOS) Holding Ctrl (or Cmd on macOS) and scrolling the mouse wheel over the terminal changes the font size — wheel up enlarges, wheel down shrinks — instead of scrolling the buffer. This complements the Alt + + / Alt + - / Alt + 0 shortcuts. Reset zoom restores the font size and family to what the connection had when you opened the tab (or the connection's saved settings, or the global default). The same reset is available via the terminal context menu: right-click → Font size → Reset . The zoom level applies only to the currently focused terminal.","title": "Font size and zoom"},{"location": "features/terminal.html#background-transparency","text": "View → Zoom → Background Transparency is a slider (0–100 %) that makes the terminal background see-through to the desktop while the text stays fully opaque and sharp. At 0 % the background is solid; higher values let more of the desktop show through. The value is saved and restored across restarts. Only the terminal area becomes transparent — the title bar, menu bar, status bar and any tab without a terminal stay solid, so the window never turns into a see-through hole. Horizontal, vertical and nested split terminals inherit the active transparency level, including panes added after transparency was enabled. Entering fullscreen with F12 or terminal-only fullscreen with Ctrl + Shift + F temporarily renders the terminal area opaque without changing the saved value; leaving fullscreen restores that value to every pane. Because a see-through window uses a different window style that the operating system fixes when the window opens, switching transparency on or off (crossing 0 %) only takes full effect after a restart ; the status bar shows a hint when you cross that threshold. Adjusting the level while already in transparent mode applies live. In transparent mode the window uses a lightweight custom title bar (drag to move, buttons to minimise/maximise/close, double-click the strip to maximise, drag the edges to resize). The slider lives in the in-window menu bar only (the native macOS menu bar cannot host a slider).","title": "Background transparency"},{"location": "features/terminal.html#local-shell-tabs","text": "Besides SSH and Mosh, a terminal tab can host a Local Shell — the local machine's own shell, opened via a pseudo-terminal (see Local Shell ). A few terminal behaviors are local-shell aware: Ctrl + D closes the tab for local cmd.exe/PowerShell sessions. Those Windows shells do not exit on EOF, so Ctrl + D would otherwise have no effect. For bash-family shells (Git Bash/Cygwin/WSL, macOS/Linux) and SSH, Ctrl + D keeps its normal EOF meaning — the shell exits and the local tab then auto-closes. Close confirmation uses local-shell wording rather than \"End SSH connection?\", and the window-close prompt is transport-neutral (\"Active sessions\"), since one window can mix SSH, Mosh and local-shell tabs. The current directory follows the interactive shell. On macOS and Linux, korTTY refreshes it from the local shell process; native PowerShell and cmd prompts supply absolute Windows paths. After cd , pushd , popd , or Set-Location , Open in Snippet Editor resolves a selected file name against that current directory instead of the tab's start directory. If the directory cannot be determined or mapped safely, korTTY stops with an error rather than opening a same-named file from the wrong directory. After an identity switch, Open in Snippet Editor is greyed out. When the session no longer runs as the identity the tab was opened with — after su , an inner ssh , or a shell-opening sudo — the context-menu entry is disabled, in SSH tabs as well as local-shell tabs: the tab's tracked directories and file access still belong to the original login and would resolve the wrong path. The entry re-enables on its own once the prompt shows the original user again (typically after exit ). A local-shell tab whose configured shell command is itself a remote client such as ssh or mosh keeps the entry disabled for the whole tab. If the load is triggered anyway, korTTY stops with an error instead of resolving the wrong path. The AI context-menu actions follow the same rule: they no longer offer to attach the selected file name's content (see Attaching a selected file to the chat ). Clipboard text is preserved in agent shortcuts. Typed and pasted text travel through the same terminal-input filter, including bracketed paste and split UTF-8 input, so a pasted file name remains part of the agent ... request and Enter dispatches it exactly once.","title": "Local shell tabs"},{"location": "features/terminal.html#session-journal","text": "Every terminal tab can keep a session journal : server output and typed commands go into a capture log, an AI condenses them into a readable timeline, and screenshots and notes can be added from the journal bar or the terminal's right-click menu. Journals start automatically for connections that enable them, or retroactively for a running session via Tools > Start/Stop Session Journal — the existing scrollback is imported. See Session journal .","title": "Session journal"},{"location": "features/terminal.html#split-screen-with-broadcast","text": "Split the terminal view to display multiple connections side by side, and optionally send input to all panes at once.","title": "Split-screen with broadcast"},{"location": "features/terminal.html#split-operations","text": "Split Pane : Create horizontal or vertical splits within a tab via the context menu or keyboard shortcuts. Independent Sessions : Each pane can show a different SSH connection. Resizable Panes : Drag dividers to adjust pane sizes. Access reason asked once per tab : when a server asks for a reason for the connection, as a CyberArk-style jump host does, a split does not ask again. korTTY sends the reason that was given when the tab was opened, because a server that asks for one closes a session that answers with nothing. A split to a different server, or a server asking something else, is asked once as well, and a new tab always starts by asking. If the server refuses the reason, for example because a ticket number has expired in the meantime, korTTY drops it and asks again on the next attempt. Move Panes : Hold Shift + Alt (Windows/Linux) or Shift + Option (macOS) and drag a pane onto another to reorder. Without the modifiers, mouse drag is used for text selection in the terminal.","title": "Split operations"},{"location": "features/terminal.html#broadcast-mode","text": "When Broadcast Mode is enabled, keyboard input is sent simultaneously to all visible panes. This is useful for running the same commands on multiple servers.","title": "Broadcast mode"},{"location": "features/terminal.html#terminal-effects","text": "Terminal effects can change the visible terminal style and output animation. Effects are Java plugins managed from Plugins > Terminal Effects .","title": "Terminal effects"},{"location": "features/terminal.html#user-controls","text": "Current terminal : Use View > Terminal Effect or the terminal context menu to choose an effect for the active terminal. Quick Connect : Choose the effect and speed before opening a temporary or saved connection. Connection Manager : Store the effect and speed on a saved connection so new tabs use it automatically. Speed : Use the slider for 1x through 10x ; if that is still too slow, type a custom value up to 99x in the numeric speed field.","title": "User controls"},{"location": "features/terminal.html#plugin-management","text": "Open Plugins > Terminal Effects to manage plugins. The table lists loaded plugins with active state, name, and description. Disable a plugin to keep it installed but unavailable for activation. Import external .jar plugins. KorTTY copies them into ~/.kortty/plugins . Export plugins that have a source JAR. The bundled MOTHER effect is exportable. Warning Imported terminal-effect plugins are trusted Java code and are not sandboxed. Only import plugins from sources you trust. For detailed plugin development documentation, see Terminal effect plugins .","title": "Plugin management"},{"location": "features/terminal.html#ssh-keep-alive","text": "Prevent connections from dropping due to inactivity by configuring SSH keep-alive messages: Enable SSH Keep-Alive in the connection's Terminal tab or in Settings > Terminal . Set the interval (5 to 600 seconds, default: 60). KorTTY sends SSH_MSG_IGNORE heartbeat messages at the configured interval and enables TCP socket keepalive while the option is active. Note If a server, firewall, VPN, or NAT gateway closes idle sessions sooner than the configured interval, the connection can still end. In that case, check the server-side SSH configuration and network idle-timeout settings as well as the KorTTY log.","title": "SSH keep-alive"},{"location": "features/terminal.html#terminal-logging","text": "Writes a connection's terminal output to a file, for audit and debugging. This is independent of the Session Journal : it is a plain transcript with no summaries, markers or screenshots, and the two can run at the same time. Configure it in either place: Connection Manager > Edit connection > Logging for a saved connection. Quick Connect > Terminal log for a one-off session, or to change the setting for the connection you are about to open. Enable logging. Choose a log folder . Left empty, KorTTY uses ~/.kortty/terminal-logs . You pick the folder; the file names are KorTTY's. Choose a log format: Plain Text - One timestamped line per line of output. XML - Structured XML with timestamps. JSON - Structured JSON with timestamps. Optionally adjust the maximum file size (default: 10 MB) and the retention period (default: 30 days), and turn off Start a new file every day or Compress closed files (gzip) — both are on by default. Quick Connect's Terminal log section covers enable, folder, format and compression; size limit, retention and daily rotation keep their configured or default values.","title": "Terminal logging"},{"location": "features/terminal.html#file-names","text": "Every file is named <date>-<time>-<server>_<number> , for example 2026-08-04-14-30-12-web01_1.log.gz . The date leads so a folder listing sorts chronologically, and the trailing number distinguishes connections that are open at the same time — two tabs on the same server get _1 and _2 and never write into one another's file.","title": "File names"},{"location": "features/terminal.html#rotation-compression-and-retention","text": "By default a new file is started every day , and always again whenever the maximum size is reached (those parts are numbered .p2 , .p3 , …); daily rotation can be turned off to roll only by size. Nothing is ever overwritten or deleted by rotation. Closed files are gzipped by default; the file currently being written always stays uncompressed so that a crash cannot truncate it. Turn off Compress closed files (gzip) to keep finished files as plain text instead. A connection that produces no output creates no file at all. Files older than the retention period are deleted automatically when a connection starts and after each daily rollover. Set the retention to 0 to keep everything. Only KorTTY's own log files are ever removed — anything else in the folder is left alone, so it is safe to point the setting at a folder you also use for other things.","title": "Rotation, compression and retention"},{"location": "features/terminal.html#what-is-removed-before-writing","text": "Captured lines go through the same redaction as the Session Journal , on the capture thread, before anything is buffered or written: the connection's own password and any replacement rules your organisation's policy defines. The secret never reaches the file, so there is nothing to clean up afterwards. Log files and a log folder KorTTY created itself are set to owner-only permissions where the filesystem supports it. A folder you chose yourself is left with the permissions you gave it. Redaction only covers what KorTTY knows A password KorTTY stores for the connection is redacted. A secret you type into a command yourself, or one a program prints, is not — KorTTY has no way to recognise it. Treat the log folder as sensitive, and use policy replacement rules for patterns that recur.","title": "What is removed before writing"},{"location": "features/terminal.html#terminal-recording","text": "Terminal recording is designed as a low-resource replay feature. KorTTY records terminal screen-state changes and timing events into one JDK/GZIP streaming-compressed .korttyrec.jsonl.gz file per terminal tab session. Legacy .korttyrec.jsonl replay files remain readable.","title": "Terminal recording"},{"location": "features/terminal.html#configure-recordings","text": "To enable recording automatically after every app restart, open Settings > Video and enable Enable terminal recording after app restart . To enable recording only for this session, open Tools > Video Manager... and select Enable terminal recording for this app session . Set the Storage path . If left at the default, KorTTY uses ~/.kortty/recordings . Choose the default format and default split scope. KorTTY replay is always available; video export requires ffmpeg . Enable or disable Auto-pause when the terminal is idle and set the idle threshold (default: 20 seconds). Optional: enable Capture terminal colors in new recordings if exported videos should reproduce terminal colors. Optional: set the ffmpeg path and click Check . If ffmpeg is missing, video export stays disabled but replay files remain usable. Click Save .","title": "Configure recordings"},{"location": "features/terminal.html#start-and-stop-recording","text": "Open or focus an SSH terminal tab. If terminal recording is enabled, click Start recording in the terminal bar, choose Tools > Start/Stop Terminal Recording , or press Ctrl + Shift + E (Cmd+Shift+E on macOS). If the tab contains multiple split terminals, choose whether to record only the active split or the whole tab. Click Stop recording or press Ctrl + Shift + E again to stop the current segment. Start and stop as often as needed in the same tab. KorTTY appends all segments to the same replay file until the tab closes.","title": "Start and stop recording"},{"location": "features/terminal.html#export-a-video","text": "Open Tools > Video Manager... . Select a .korttyrec.jsonl.gz replay file from the list. Verify that the ffmpeg status says video export is enabled. Click Export... . In the export options: Choose Export entire recording or enter start/end times with minute or MM:SS format. Choose whether to include terminal colors (available only if the replay contains color data). Choose WebM/VP9 or MKV/FFV1 format, then select an output path. While KorTTY renders frames and runs ffmpeg , the export progress dialog shows the current phase, progress bar, and estimated remaining time. Export uses the recorded terminal geometry so large terminal screens are not cropped.","title": "Export a video"},{"location": "features/terminal.html#view-and-manage-recordings","text": "Open Tools > Video Manager... . Select a .korttyrec.jsonl.gz replay file. Click View to play the replay directly inside KorTTY. Use the replay viewer timeline to scrub, or enter a Time jump value such as 5 for minute 5 or 5:30 for minute 5 and 30 seconds. Set Speed between 1x and 20x to control playback speed. Click Rename... to rename the replay file. Click Delete to delete the selected replay after confirmation.","title": "View and manage recordings"},{"location": "features/tunnels.html","text": "SSH Tunnels (Port Forwarding) SSH tunnels securely forward traffic between local and remote ports through an encrypted SSH connection. korTTY supports three tunnel types: local port forwarding, remote port forwarding, and dynamic port forwarding (SOCKS proxy). Configuring Tunnels Open a connection for editing: Connections > Manage Connections → select the connection → Edit . Navigate to the SSH Tunnels tab. Click Add Tunnel and configure the tunnel. Tunnel Configuration Fields Field Description Type Local ( -L ), Remote ( -R ), or Dynamic ( -D ) Local Host Local bind address (typically localhost ) Local Port Local port number Remote Host Target host from the SSH server's perspective Remote Port Target port on the remote host Description Optional label for the tunnel Enabled Toggle on/off without deleting the configuration Multiple tunnels can be configured per connection. Disabled tunnels remain in the configuration but are not activated when the connection opens. Tunnel Types Local Port Forwarding ( -L ) Forward a local port to a remote service through the SSH tunnel. Your machine:8080 --> SSH Server --> database-server:5432 Example use case: Access a remote database server that is not directly reachable from your machine. Local Host: localhost Local Port: 8080 Remote Host: database-server (or IP) Remote Port: 5432 Once configured, connect to the remote database using localhost:8080 on your machine. Remote Port Forwarding ( -R ) Make a local service accessible from the remote server's network. Remote:9090 --> SSH Server --> Your machine:3000 Example use case: Allow a remote team member to access a local development server on your machine. Remote Port: 9090 (the port the remote side uses to reach your service) Local Host: localhost Local Port: 3000 (your local service) After connecting, the remote machine can access your service at localhost:9090 . Dynamic Port Forwarding ( -D ) Create a SOCKS5 proxy for all network traffic through the SSH tunnel. Your machine:1080 --> SSH Server --> (any destination) Example use case: Encrypt all traffic from your browser or application by routing it through the SSH tunnel. Local Port: 1080 (or any available port) Configure your browser or application to use localhost:1080 as a SOCKS5 proxy. All traffic will be securely forwarded through the SSH server. Note For Dynamic Port Forwarding, only the local port is required. The remote host and remote port fields are not used. Managing Tunnels Enable/Disable: Toggle the Enabled checkbox on a tunnel to activate or deactivate it without removing the configuration. Edit: Select a tunnel and modify its settings. Delete: Remove a tunnel from the connection. Multiple Tunnels: Any number of tunnels can be configured on a single connection and activated simultaneously. Tunnels are established when the SSH connection is opened and remain active for the duration of the session. Warning When using local or remote port forwarding on ports below 1024 (such as port 80 or 443), your system may require elevated privileges. Use ports 1024 and above to avoid permission issues.","title": "SSH Tunnels (Port Forwarding)"},{"location": "features/tunnels.html#ssh-tunnels-port-forwarding","text": "SSH tunnels securely forward traffic between local and remote ports through an encrypted SSH connection. korTTY supports three tunnel types: local port forwarding, remote port forwarding, and dynamic port forwarding (SOCKS proxy).","title": "SSH Tunnels (Port Forwarding)"},{"location": "features/tunnels.html#configuring-tunnels","text": "Open a connection for editing: Connections > Manage Connections → select the connection → Edit . Navigate to the SSH Tunnels tab. Click Add Tunnel and configure the tunnel.","title": "Configuring Tunnels"},{"location": "features/tunnels.html#tunnel-configuration-fields","text": "Field Description Type Local ( -L ), Remote ( -R ), or Dynamic ( -D ) Local Host Local bind address (typically localhost ) Local Port Local port number Remote Host Target host from the SSH server's perspective Remote Port Target port on the remote host Description Optional label for the tunnel Enabled Toggle on/off without deleting the configuration Multiple tunnels can be configured per connection. Disabled tunnels remain in the configuration but are not activated when the connection opens.","title": "Tunnel Configuration Fields"},{"location": "features/tunnels.html#tunnel-types","text": "","title": "Tunnel Types"},{"location": "features/tunnels.html#local-port-forwarding-l","text": "Forward a local port to a remote service through the SSH tunnel. Your machine:8080 --> SSH Server --> database-server:5432 Example use case: Access a remote database server that is not directly reachable from your machine. Local Host: localhost Local Port: 8080 Remote Host: database-server (or IP) Remote Port: 5432 Once configured, connect to the remote database using localhost:8080 on your machine.","title": "Local Port Forwarding (-L)"},{"location": "features/tunnels.html#remote-port-forwarding-r","text": "Make a local service accessible from the remote server's network. Remote:9090 --> SSH Server --> Your machine:3000 Example use case: Allow a remote team member to access a local development server on your machine. Remote Port: 9090 (the port the remote side uses to reach your service) Local Host: localhost Local Port: 3000 (your local service) After connecting, the remote machine can access your service at localhost:9090 .","title": "Remote Port Forwarding (-R)"},{"location": "features/tunnels.html#dynamic-port-forwarding-d","text": "Create a SOCKS5 proxy for all network traffic through the SSH tunnel. Your machine:1080 --> SSH Server --> (any destination) Example use case: Encrypt all traffic from your browser or application by routing it through the SSH tunnel. Local Port: 1080 (or any available port) Configure your browser or application to use localhost:1080 as a SOCKS5 proxy. All traffic will be securely forwarded through the SSH server. Note For Dynamic Port Forwarding, only the local port is required. The remote host and remote port fields are not used.","title": "Dynamic Port Forwarding (-D)"},{"location": "features/tunnels.html#managing-tunnels","text": "Enable/Disable: Toggle the Enabled checkbox on a tunnel to activate or deactivate it without removing the configuration. Edit: Select a tunnel and modify its settings. Delete: Remove a tunnel from the connection. Multiple Tunnels: Any number of tunnels can be configured on a single connection and activated simultaneously. Tunnels are established when the SSH connection is opened and remain active for the duration of the session. Warning When using local or remote port forwarding on ports below 1024 (such as port 80 or 443), your system may require elevated privileges. Use ports 1024 and above to avoid permission issues.","title": "Managing Tunnels"},{"location": "getting-started/building-packages.html","text": "Building korTTY packages korTTY can be built as Java artifacts, a self-contained portable application image or a native installer. Native packages must be built on the target operating system and processor architecture because jpackage , JavaFX and the bundled native tools select platform-specific files from the active build machine. Choose an artifact Goal Gradle task Local output Java required on the destination? Thin application JAR jar build/libs/korTTY-<version>.jar Yes, plus every runtime dependency; this JAR is not a stand-alone executable JAR Java distribution distZip / distTar build/distributions/korTTY-<version>.zip / .tar Yes; the archive includes dependencies and launch scripts but no Java runtime Self-contained app image jpackage Platform app bundle or directory under build/jpackage/ No; jpackage includes a trimmed Java runtime Native installer jpackageDmg , jpackageMsi , jpackageDeb or jpackageRpm DMG, MSI, DEB or RPM under build/jpackage/ No; the installer contains the self-contained app image Portable native archive jpackage , then the OS archive command shown below ZIP or TAR containing the entire app image No; keep the bundle or directory intact after extraction Flatpak bundle jpackage , then flatpak-builder and flatpak build-bundle build/jpackage/kortty-Linux-<version>-<architecture>.flatpak No; install the Freedesktop runtime through Flatpak An app image is not one executable file On macOS the app image is the complete korTTY.app bundle, on Windows it is the complete korTTY directory containing korTTY.exe , and on Linux it is the complete korTTY directory containing bin/korTTY . Moving only the launcher breaks the application because its runtime and libraries remain in sibling directories. The Linux directory is not a .AppImage file. Two launchers per image Every app image also carries kortty-cli , the client for the control API , declared once through jpackage 's --add-launcher in the shared argument list so that every image and installer inherits it. Its properties file is generated by the prepareCliLauncherProperties task into build/jpackage-launchers/ — deliberately outside build/jpackage-input/ , everything in which is copied verbatim into the shipped image. The DMG task repackages the finished .app and inherits the launcher rather than declaring it a second time. The kortty-cli name is not interchangeable with kortty : the graphical launcher is korTTY , and on case-insensitive macOS and Windows filesystems the two would collide. Common prerequisites Install these tools before building on any operating system: Requirement Why korTTY needs it A full JDK supported by the pinned Gradle wrapper Starts Gradle; korTTY then resolves a matching Temurin JDK 25 toolchain for compilation, jlink and jpackage Git Clones korTTY and the pinned SithTermFX source dependency Apache Maven Builds SithTermFX into the local Maven repository before korTTY compilation curl Downloads the pinned mosh4j artifacts used by packaged builds Outbound HTTPS access The wrapper, Maven/Gradle dependencies, SithTermFX, SHA-256-pinned mosh4j, the Monaco build toolchain, formatter browser bundles and chat-render resources are downloaded during a clean first build Gradle Wrapper from this repository gradlew and gradlew.bat download and run the repository-pinned Gradle version; do not install or substitute a system Gradle Node.js and MkDocs are not common prerequisites for an application build: Gradle downloads a pinned Node.js runtime into an isolated build directory only to compile Monaco, never into the application image, and the normal application build uses the committed offline guide resources. Prettier Standalone and sql-formatter are copied as compact browser bundles and execute offline in JavaFX WebView without Node. Packaging is pinned to the JDK 25 toolchain The packaged runtime explicitly includes jdk.jsobject , which JavaFX WebView needs and which was removed in JDK 26. The removal is tracked as OpenJDK issue JDK-8362628 : https://bugs.openjdk.org/browse/JDK-8362628 . Every packaging task resolves jpackage from the same Gradle-selected Temurin JDK 25 toolchain as compilation, so a newer system java or jpackage cannot silently create an incompatible runtime. The selected toolchain must still match the target CPU because jpackage does not cross-compile. Clone the source once: git clone https://github.com/chardonnay/korTTY.git cd korTTY Verify the host tools and inspect the JDKs Gradle can select before the first build. The system JDK may be newer than 25, but ./gradlew javaToolchains must list a matching JDK 25 for the target CPU after provisioning; Maven must use a JDK that can build SithTermFX. macOS / Linux Windows PowerShell command -v java git mvn curl java -XshowSettings:properties -version 2 > & 1 | grep -E 'java.specification.version|os.arch' git --version mvn --version curl --version ./gradlew --version ./gradlew -q javaToolchains Get-Command java , git , mvn , curl java -XshowSettings : properties -version 2 >& 1 | Select-String 'java.specification.version|os.arch' git - -version mvn - -version curl - -version .\\ gradlew . bat - -version .\\ gradlew . bat -q javaToolchains The first wrapper invocation downloads Gradle. A clean application build also clones SithTermFX when necessary, installs it through Maven, downloads the pinned build inputs, compiles korTTY and runs the tests. Clean, target-specific package staging prepareJpackage assembles build/jpackage-input/libs with one final Gradle Sync . It removes stale dependency versions and formatter trees, keeps only the current mosh4j architecture and protobuf, reuses the application's parent-loaded Bouncy Castle, excludes SithTermFX's test dependency, and replaces JNA/pty4j with JARs that retain all Java classes, services, manifests and licences but only the target native path and binary architecture. The verification task deliberately seeds obsolete files before Sync and runs a real JNA/PTY smoke against the slim JARs: ./gradlew verifyJpackageStaging slimNativeRuntimeSmoke Package-size reports and budgets The platform-independent size reporter separates the app image, runtime, formatter/Mosh payloads, dependency JARs and compressed application-JAR resources, then writes JSON and Markdown. It understands the macOS Contents/app and Contents/runtime , Windows app and runtime , and Linux lib/app and lib/runtime layouts produced by jpackage . CI compares native installers with the committed release baseline, requires at least 15% reduction, applies the 190 MiB app-image and 150 MiB DMG limits, and uses a 2% regression tolerance once a new platform size is verified: DMG = \" $( find build/jpackage -maxdepth 1 -name 'korTTY-*.dmg' -print -quit ) \" python3 scripts/package-size-report.py \\ --app-image build/jpackage/korTTY.app \\ --artifact \"macos-aarch64-dmg= $DMG \" \\ --baselines package/size-baselines.json \\ --min-app-bytes 104857600 \\ --max-app-bytes 199229440 \\ --fail-on-budget \\ --output-json build/package-size/local.json \\ --output-markdown build/package-size/local.md Before a Windows archive is created, the release workflow resolves the generated app image and requires an application JAR, jvm.dll , at least 100 MiB of plausible content and x86_64 PE headers for both launcher and JVM. Linux RPM size enforcement runs after rpmsign , so the measured file is the one that is distributed. Build the Java artifacts Use the platform command below to compile, test and create the thin JAR plus the Java ZIP and TAR distributions: macOS / Linux Windows PowerShell ./gradlew clean build ls -l build/libs/ build/distributions/ .\\ gradlew . bat clean build Get-ChildItem .\\ build \\ libs , .\\ build \\ distributions The thin JAR contains korTTY classes and resources but not its runtime dependencies and does not provide the complete deployment by itself. For a Java-based portable layout, extract the generated ZIP or TAR and start bin/korTTY on macOS/Linux or bin\\korTTY.bat on Windows; the destination still needs JDK 25. These distributions also contain the native JavaFX dependencies selected on the build host, so build them on the same OS and architecture on which they will run. Every korTTY release format carries the repository's canonical MIT LICENSE . The RPM and pacman metadata also declare MIT ; scripts/check-release-licenses.py is the CI gate that prevents application package metadata from drifting to another license. Licences belonging to the Gradle wrapper, bundled runtimes, models or other dependencies remain separate and are not rewritten as korTTY's licence. Build Pacman packages Regular releases publish two Pacman packages: x86_64 for Arch Linux and aarch64 for Arch Linux ARM. The application images are built natively on the matching Ubuntu runners first. Both Pacman jobs then run makepkg in the official Arch Linux x86_64 container because the PKGBUILD only copies that pre-built image and does not compile or execute target code. The AArch64 job supplies a separate makepkg.conf with CARCH=aarch64 and CHOST=aarch64-unknown-linux-gnu . A CI guard rejects AArch64 packaging if prepare() , build() or check() is ever added to the PKGBUILD; such a change requires a native Arch Linux ARM builder. The package also disables stripping and debug-package generation so x86_64 tools never modify a pre-built AArch64 ELF. Before signing, CI requires the exact package filename and checks .PKGINFO , the installed MIT licence, desktop entry and launcher symlink. It also uses readelf to verify that the native launcher and bundled JVM match the advertised architecture. The detached signature is then verified locally before the package becomes an Actions artifact. For a controlled Pacman-only backfill, dispatch Build Release Binaries with scope pacman-only , the numeric version, the new pacman_pkgrel and the full commit expected behind the source tag. Use release_tag=ci-only first and inspect both architecture-specific Actions artifacts. Repeat with the real v<version> release tag only after inspection. The publishing job refuses immutable releases and filename collisions, uploads without clobbering, compares every remote GitHub digest with the local SHA-256 value and never deletes an existing or partially uploaded asset automatically. Build a Flatpak bundle Flatpak bundles are built natively on Linux for x86_64 and aarch64 . The manifest uses application ID io.github.chardonnay.korTTY with the Freedesktop 25.08 runtime and packages the self-contained Linux application image produced by jpackage . Install Flatpak, flatpak-builder and appstreamcli , then run: ./gradlew clean build jpackage flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo flatpak install --user --noninteractive flathub org.freedesktop.Platform//25.08 org.freedesktop.Sdk//25.08 flatpak-builder --user --force-clean --disable-rofiles-fuse --repo = build/flatpak-repo --default-branch = stable build/flatpak-build package/flatpak/io.github.chardonnay.korTTY.yml flatpak build-bundle --arch = \" $( flatpak --default-arch ) \" build/flatpak-repo \"build/jpackage/kortty-Linux-<version>- $( flatpak --default-arch ) .flatpak\" io.github.chardonnay.korTTY stable The package grants network, X11, audio, GPU and host-filesystem access because an SSH terminal client must reach remote hosts, render JavaFX/WebView, use terminal media and operate on explicitly selected host files. It also grants access to org.freedesktop.Flatpak ; local shells and their AI-agent commands are launched on the host through flatpak-spawn --host . Validate a local bundle by installing it, checking its architecture and running the packaged WebView smoke: flatpak install --user ./build/jpackage/kortty-Linux-<version>-<architecture>.flatpak flatpak info --user --show-ref io.github.chardonnay.korTTY xvfb-run -a flatpak run io.github.chardonnay.korTTY --webview-jit-smoke GitHub Actions runs this sequence on native ubuntu-latest and ubuntu-24.04-arm runners, signs both bundles with the Linux release key and uploads them beside the distro-native packages. A manual flatpak-only dispatch accepts a fixed source commit and a separate existing release tag so a bundle can be backfilled without moving the tag or replacing another asset. Build a separate llama.cpp runtime package Integrated local AI uses a native artifact that is intentionally separate from every jpackage app image and installer. The upstream tag, full commit, and source-archive SHA-256 are pinned in gradle/llama-cpp-pins.properties — the single file a runtime bump rewrites, alongside a row per shipped pin — while build.gradle.kts keeps the korTTY runtime revision and API-contract version. verifyLlamaCppPin fails closed when those values disagree, the active tag has no pin row, or the requested backend is invalid. Install CMake and a native C/C++ toolchain. A Vulkan package additionally needs the Vulkan headers, loader, and shader compiler; Metal is available only on macOS with the Apple development tools. Then build the package for the current machine: CPU macOS Metal Windows/Linux Vulkan ./gradlew generateLlamaRuntimeManifest -Pllama.backend = CPU ./gradlew generateLlamaRuntimeManifest -Pllama.backend = METAL ./gradlew generateLlamaRuntimeManifest -Pllama.backend = VULKAN The task chain downloads only the pinned commit archive, verifies its SHA-256, extracts it, configures a Release build with curl, RPC, tests, examples, tools, and server Web UI disabled, builds llama-server , stages its required libraries and license, and creates a reproducible ZIP plus JSON descriptor under build/llama-runtime/packages/ . The descriptor records the immutable runtime ID, tag/commit, API-contract version, minimum korTTY version, platform, architecture, concrete backend, package size/SHA-256, publication URL placeholder, entrypoint, and revocation flag. One build host cannot create the whole matrix Native llama.cpp artifacts follow the same target-platform rule as jpackage : build each operating-system/architecture/backend combination on a matching host. The runtime workflow covers macOS arm64/x86_64 CPU+Metal, Windows x86_64 CPU+Vulkan, Linux x86_64/arm64 CPU+Vulkan. CUDA is not in the first matrix. Candidate detection, contract tests, and promotion .github/workflows/llama-runtime.yml checks upstream tags weekly; only the newest tag is ever picked, so a tighter cadence would only pile up unreviewed candidates. A change opens a candidate PR that updates the source pin but does not publish it. A scope job then sizes the build per pull request: a PR that touches gradle/llama-cpp-pins.properties , the workflow itself, or the llama/runtime-update Java sources runs the full ten-leg native matrix, while a build.gradle.kts -only change runs a single Linux x86_64 CPU smoke leg — this is why the pin lives in its own file, and a manual workflow_dispatch build is the escape hatch that always forces the full matrix. Every built artifact must start and report its version, the Linux CPU reference job additionally verifies unauthenticated rejection, authenticated model listing, completion, sleep/wake, JSON-schema chat completion, embeddings, and two simultaneously usable sidecars, and a pinned Qdrant 1.18.2 service runs the real vector-store contract. The korTTY repository owns only candidate detection and source-side matrix validation: it has no stable-promotion action, cross-repository release token, or runtime signing private key. Stable publication belongs to the public chardonnay/kortty-llama-runtimes repository. Its explicit human-dispatched workflow accepts the exact reviewed korTTY source commit, rebuilds the native matrix, runs authentication, chat/completion, embeddings, JSON-schema, sleep/wake, and parallel-sidecar smoke tests on every package, and runs the pinned Qdrant contract separately. After those checks it enters the protected llama-runtime-signing environment, verifies that LLAMA_RUNTIME_ED25519_PRIVATE_KEY_PEM matches the published trust root, verifies and extends the cumulative signed index, and creates an immutable runtime-only release with that repository's scoped github.token ; no cross-repository personal access token is required. An existing runtime ID is never overwritten. Regular promotion is limited to once every seven days, while an audited security or model-support reason can override that cadence. The application installer therefore stays unchanged while a compatible native runtime can be promoted or withdrawn independently. Publish the model and prompt catalog The reviewed canonical catalog is ai-catalog/model-prompt-catalog-v1.json . It uses strict schema v1 for local-model recommendations and model-name-to-preset mappings; application code and prompt-contract text are not part of this independently released payload. .github/workflows/ai-catalog-release.yml is manual-only, accepts dispatches only from main , and has no preview or automatic promotion path. Its dispatch requires the exact reviewed catalogVersion and a release repository, defaulting to chardonnay/kortty-ai-catalog . Before signing, it verifies that the positive catalog sequence is strictly greater than the latest published sequence and runs the authoritative Java schema/trust-chain tests. The ai-catalog-signing GitHub environment must have required reviewers before production secrets are configured; inside it, the workflow checks that KORTTY_AI_CATALOG_PUBLIC_KEY matches the Ed25519 private signing secret, signs the unchanged JSON bytes, verifies the signature, and publishes immutable model-prompt-catalog-v1.json plus model-prompt-catalog-v1.sig . An existing version tag or release is refused. Configure AI_CATALOG_ED25519_PRIVATE_KEY_PEM and AI_CATALOG_RELEASE_TOKEN only as environment-scoped promotion secrets. Embed the local-AI trust roots Every application release that may install runtimes or refresh the catalog must embed both Ed25519 public verification keys. The runtime-channel trust root is public, auditable, and pinned in config/trust/llama-runtime-ed25519-public.pem , so normal local builds use the same key as official packages. The catalog trust root is supplied separately. Never provide either signing private key to an application build. Channel Source default Environment variable Gradle property llama.cpp runtime config/trust/llama-runtime-ed25519-public.pem Optional KORTTY_LLAMA_RUNTIME_PUBLIC_KEY ; must match the pinned key exactly Optional kortty.llamaRuntimePublicKey ; must match the pinned key exactly Model/prompt catalog None KORTTY_AI_CATALOG_PUBLIC_KEY kortty.aiCatalogPublicKey The generated resources store the fixed channel URLs and public trust roots with the application: macOS / Linux Windows PowerShell export KORTTY_AI_CATALOG_PUBLIC_KEY = \" $( cat ai-catalog-signing-public.pem ) \" ./gradlew generateLlamaRuntimeReleaseConfig generateAiCatalogReleaseConfig build $env:KORTTY_AI_CATALOG_PUBLIC_KEY = Get-Content .\\ ai-catalog-signing-public . pem -Raw .\\ gradlew . bat generateLlamaRuntimeReleaseConfig generateAiCatalogReleaseConfig build The official application-release workflow reads both names from GitHub Actions repository variables, rejects missing values or private-key material, and lets Gradle validate the X.509 Ed25519 keys before packaging. The runtime variable is a redundant CI identity check and must encode the same public key as the tracked PEM; a different override fails the build instead of replacing the trust root. If the pinned runtime key or generated release configuration is missing or malformed, runtime installation/update fails closed before fetching the index. Without the catalog key, korTTY makes no catalog request, ignores any cache, and uses its built-in bootstrap. macOS Additional software Install the common prerequisites and Apple command-line tooling. The unsigned .app build uses the JDK tools; DMG creation uses hdiutil and the configured custom icon, while signing and notarization additionally use codesign , security , notarytool and stapler . Select a current Xcode installation before a release build. xcode-select -p xcrun --find codesign xcrun --find notarytool xcrun --find stapler command -v hdiutil ditto Run xcode-select --install if the command-line tools are absent. A release intended for other users also needs an Apple Developer account, a Developer ID Application certificate and App Store Connect API credentials for notarization; an unsigned local build does not. Build an unsigned app image, portable ZIP and DMG ./gradlew clean build ./gradlew jpackage ditto -c -k --keepParent build/jpackage/korTTY.app \"build/jpackage/korTTY-macOS-local- $( uname -m ) .zip\" ./gradlew jpackageDmg Artifact Output Self-contained app image build/jpackage/korTTY.app Portable archive build/jpackage/korTTY-macOS-local-<architecture>.zip Disk image build/jpackage/korTTY-<version>.dmg jpackageDmg packages the app image jpackage produced — it does not build a second one — so run the two in that order; the task fails with an explicit message when build/jpackage/korTTY.app is missing. Before the DMG is finished, the app inside it is mounted and checked: its signature must verify, its payload must match the app image on disk, and its signing authority must be the same one. jpackage always re-signs the copy it places in the DMG (it swaps Contents/app/.jpackage.xml for a .package marker, which breaks the seal), and without an identity it falls back to an ad-hoc signature that Apple's notary rejects — this check is what catches that, and any other signature defect, before a notarization submit rather than minutes into one. Open the app image for a local smoke test and verify the DMG container: open build/jpackage/korTTY.app hdiutil verify build/jpackage/korTTY-*.dmg ./gradlew verifyMacDmgAppImage Build macOS for Intel ( x86_64 ) The reliable paths are an Intel Mac with an x86_64 JDK 25 or the repository's GitHub Actions job on macos-15-intel . jpackage does not cross-compile, so an ARM JDK running natively on Apple Silicon creates an ARM app even if the desired filename says x86_64 . On an Intel Mac, validate the machine, Java runtime and generated launcher before distributing the result: export JAVA_HOME = \" $( /usr/libexec/java_home -v 25 -a x86_64 -F ) \" export PATH = \" $JAVA_HOME /bin: $PATH \" test \" $( uname -m ) \" = \"x86_64\" java -XshowSettings:properties -version 2 > & 1 | grep 'os.arch = x86_64' test \" $( jpackage --version | cut -d. -f1 ) \" = \"25\" ./gradlew --stop ./gradlew clean build ./gradlew jpackage ./gradlew jpackageDmg lipo -archs build/jpackage/korTTY.app/Contents/MacOS/korTTY The final lipo command must print only x86_64 . Perform the same build on Apple Silicon with an ARM JDK to obtain the separate aarch64 artifacts; the project does not create a universal binary. Intel build with GitHub Actions The Build Release Binaries workflow contains separate macOS matrix entries for macos-latest / aarch64 and macos-15-intel / x86_64 . It installs Temurin JDK 25, rejects a JDK or runner architecture mismatch, builds both the signed app and DMG, verifies the launcher with lipo , notarizes both deliverables and uploads architecture-suffixed artifacts. Configure these repository secrets before running the workflow: Secret Content APPLE_SIGNING_CERT_P12_BASE64 Base64-encoded Developer ID Application certificate and private key in P12 form APPLE_SIGNING_CERT_PASSWORD Password protecting the P12 file APPLE_SIGNING_IDENTITY Exact Developer ID Application identity shown by security find-identity APPLE_API_KEY_ID App Store Connect API key ID APPLE_ISSUER_ID App Store Connect issuer ID APPLE_API_KEY Complete private API key contents Open Actions → Build Release Binaries → Run workflow , choose the branch or tag in ref , and enter its numeric application version. A branch run uploads the macOS build artifacts for inspection; a published GitHub Release, or a manual run whose ref is a release tag, also feeds the final release-upload job. The Intel artifacts are named korTTY-macOS-<version>-x86_64.zip and korTTY-macOS-<version>-x86_64.dmg . Rosetta is an unverified advanced path An Apple Silicon host can experimentally run an x86_64 JDK 25 under Rosetta, but this is not the repository's verified release path. Install Rosetta, select an Intel JDK for both JAVA_HOME and PATH , stop existing Gradle daemons, run the complete build in an x86_64 shell and confirm the launcher with lipo . Test the result on real Intel hardware before distribution; use macos-15-intel when a reproducible release artifact is required. softwareupdate --install-rosetta --agree-to-license arch -x86_64 /bin/zsh export JAVA_HOME = \"/path/to/x86_64-jdk-25/Contents/Home\" export PATH = \" $JAVA_HOME /bin: $PATH \" java -XshowSettings:properties -version 2 > & 1 | grep 'os.arch = x86_64' ./gradlew --stop ./gradlew --no-daemon clean build ./gradlew --no-daemon jpackage ./gradlew --no-daemon jpackageDmg lipo -archs build/jpackage/korTTY.app/Contents/MacOS/korTTY Sign the app and DMG Signing is opt-in. The Gradle tasks use the repository properties below to sign bundled JNA/pty4j native libraries and pass the identity to jpackage ; do not replace this flow with an ad-hoc signature over only the outer app bundle. security find-identity -v -p codesigning export KORTTY_SIGNING_IDENTITY = \"Developer ID Application: Your Name (TEAMID)\" ./gradlew clean build ./gradlew \\ -Pkortty.macos.sign = true \\ -Pkortty.macos.signingIdentity = \" $KORTTY_SIGNING_IDENTITY \" \\ jpackage ./gradlew \\ -Pkortty.macos.sign = true \\ -Pkortty.macos.signingIdentity = \" $KORTTY_SIGNING_IDENTITY \" \\ jpackageDmg If the identity is stored in a non-default keychain, add -Pkortty.macos.signingKeychain=/absolute/path/to/keychain-db to both packaging commands. Signing and notarization are separate operations: the Gradle properties sign the artifacts but do not submit them to Apple's notary service. Notarize and staple locally The release workflow is the canonical automated implementation. To mirror its notarization phase locally, store the App Store Connect API credentials in a keychain profile, submit a ZIP of the signed .app , staple the accepted ticket, then submit and staple the signed DMG. export APPLE_API_KEY_ID = \"<key-id>\" export APPLE_ISSUER_ID = \"<issuer-id>\" export APPLE_API_KEY_PATH = \"/absolute/path/to/AuthKey_<key-id>.p8\" xcrun notarytool store-credentials \"kortty-notary\" \\ --key \" $APPLE_API_KEY_PATH \" \\ --key-id \" $APPLE_API_KEY_ID \" \\ --issuer \" $APPLE_ISSUER_ID \" ditto -c -k --keepParent build/jpackage/korTTY.app build/jpackage/korTTY-notarization.zip xcrun notarytool submit build/jpackage/korTTY-notarization.zip --keychain-profile \"kortty-notary\" --wait xcrun stapler staple build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY.app DMG = \" $( find build/jpackage -maxdepth 1 -name 'korTTY-*.dmg' -print -quit ) \" test -n \" $DMG \" xcrun notarytool submit \" $DMG \" --keychain-profile \"kortty-notary\" --wait xcrun stapler staple \" $DMG \" xcrun stapler validate \" $DMG \" ditto -c -k --keepParent build/jpackage/korTTY.app \"build/jpackage/korTTY-macOS-local- $( uname -m ) .zip\" Inspect any rejected submission with xcrun notarytool log <submission-id> --keychain-profile \"kortty-notary\" . Before publishing, verify Gatekeeper, the embedded signatures, the stapled tickets and the launcher architecture: codesign --verify --deep --strict --verbose = 2 build/jpackage/korTTY.app spctl --assess --type execute --verbose = 4 build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY-*.dmg lipo -archs build/jpackage/korTTY.app/Contents/MacOS/korTTY macOS troubleshooting jpackage reports a signing identity error: run security find-identity -v -p codesigning , copy the complete Developer ID Application identity and confirm that the selected keychain is unlocked. java , jpackage , uname or the generated launcher report different architectures: install a matching JDK, update JAVA_HOME and PATH , run ./gradlew --stop , then rebuild from clean . Notarization fails: read the submission log instead of re-signing individual JavaFX libraries; the repository deliberately preserves their upstream signatures while separately signing the native libraries covered by its Gradle task. The packaged app can reach a LAN host but ./gradlew run cannot: launch the .app so macOS can grant Local Network permission to the application bundle. Further reading for macOS Oracle jpackage packaging prerequisites Apple: Signing Mac software with Developer ID Apple: Notarizing macOS software before distribution Apple: Customizing the notarization workflow GitHub-hosted runner reference Linux Additional software The app-image task needs the common prerequisites. DEB creation needs fakeroot ; RPM creation needs the RPM build tools; zip is needed only when you want the portable ZIP in addition to the TAR.GZ archive. Debian / Ubuntu Fedora / RHEL sudo apt update sudo apt install git maven curl fakeroot zip sudo dnf install git maven curl rpm-build zip Install and select the full JDK 25 separately if the distribution command does not provide that exact JDK. Build DEB packages on Debian/Ubuntu and RPM packages on an RPM-based distribution; the portable app image is available on either family. Build the app image, portable archives and packages ./gradlew clean build ./gradlew jpackage tar -C build/jpackage -czf \"build/jpackage/korTTY-Linux-local- $( uname -m ) .tar.gz\" korTTY ( cd build/jpackage && zip -r \"korTTY-Linux-local- $( uname -m ) .zip\" korTTY ) Then create the native package for the current distribution: Debian / Ubuntu Fedora / RHEL ./gradlew jpackageDeb ./gradlew jpackageRpm Artifact Output Self-contained app image build/jpackage/korTTY/ Portable archives build/jpackage/korTTY-Linux-local-<architecture>.tar.gz and .zip Debian package build/jpackage/kortty*.deb RPM package build/jpackage/kortty*.rpm Run and inspect the outputs without installing the packages: test -x build/jpackage/korTTY/bin/korTTY file build/jpackage/korTTY/bin/korTTY ./build/jpackage/korTTY/bin/korTTY tar -tzf build/jpackage/korTTY-Linux-local-*.tar.gz | head dpkg-deb --info build/jpackage/kortty*.deb rpm -qpi build/jpackage/kortty*.rpm Use only the inspection command available for the package you built. Test installer installation and removal in a disposable virtual machine or container matching the target distribution before publishing it. Linux troubleshooting jpackageDeb fails with a missing fakeroot : install fakeroot and rerun from a clean build directory. jpackageRpm cannot find rpmbuild : install rpm-build on RPM distributions or rpm on Debian/Ubuntu, then confirm command -v rpmbuild . The jpackage task succeeds but no .AppImage appears: this is expected; inspect build/jpackage/korTTY/ , which is the self-contained jpackage app-image directory. jpackage reports that the destination already exists: run ./gradlew clean before rebuilding the app image. Further reading for Linux Oracle jpackage packaging prerequisites Debian Handbook: inspecting packages with dpkg Debian fakeroot manual RPM documentation Windows Additional software Use a PowerShell terminal with the common prerequisites. The portable app-image task needs no external installer builder, but MSI creation requires WiX Toolset 3 or later with candle.exe and light.exe available in PATH . The release workflow uses the WiX 3 toolchain for its Windows packaging jobs. Get-Command java , javac , jpackage , git , mvn , curl Get-Command candle . exe , light . exe Restart PowerShell after changing JAVA_HOME , PATH or the WiX installation so the wrapper and jpackage see the new values. Build the app image, portable ZIP and MSI .\\ gradlew . bat clean build .\\ gradlew . bat jpackage Compress-Archive -Path .\\ build \\ jpackage \\ korTTY -DestinationPath .\\ build \\ jpackage \\ korTTY-Windows-local . zip -Force .\\ gradlew . bat jpackageMsi Artifact Output Self-contained app image build\\jpackage\\korTTY\\ Windows launcher inside the app image build\\jpackage\\korTTY\\korTTY.exe Portable archive build\\jpackage\\korTTY-Windows-local.zip Installer build\\jpackage\\korTTY-<version>.msi The ZIP is portable because it contains the complete korTTY directory. korTTY.exe is a launcher within that directory, not a stand-alone binary; extract the entire directory before running it. The release workflow currently publishes the validated x86_64 Windows package only. OpenJFX does not provide native Windows ARM artifacts for the pinned JavaFX line, so Windows-on-ARM uses this x86_64 package through Windows emulation; the former mislabeled ARM archive is no longer produced or used as a size baseline. Native Windows ARM packaging requires a separate JavaFX/JDK packaging track and native WebView/PTY validation. Test-Path .\\ build \\ jpackage \\ korTTY \\ korTTY . exe & .\\ build \\ jpackage \\ korTTY \\ korTTY . exe Get-ChildItem .\\ build \\ jpackage \\*. msi Get-Item .\\ build \\ jpackage \\ korTTY-Windows-local . zip Test MSI installation, launch, upgrade and removal in a disposable Windows virtual machine matching the target architecture before publishing it. Windows troubleshooting jpackageMsi reports that WiX cannot be found: confirm that Get-Command candle.exe, light.exe succeeds in the same PowerShell session, then rerun clean and jpackageMsi . Gradle cannot resolve the packaging JDK: run .\\gradlew.bat -q javaToolchains , allow toolchain downloads or install a native JDK 25, then stop existing daemons with .\\gradlew.bat --stop . Maven cannot build SithTermFX: confirm that mvn --version reports JDK 25 and that mvn.cmd is available in PATH . The app-image destination already exists: run .\\gradlew.bat clean before rebuilding. Further reading for Windows Oracle jpackage packaging prerequisites WiX Toolset documentation WiX Toolset 3 releases Microsoft PowerShell: Compress-Archive Common troubleshooting and further reading A packaging task for another operating system is absent: this is expected because build.gradle.kts registers jpackageDmg , jpackageMsi , jpackageDeb and jpackageRpm only on their matching host platforms. A clean build cannot resolve dependencies or download a pinned asset: confirm outbound HTTPS access and proxy configuration for GitHub, Maven repositories, Gradle services, Node.js and the npm registry, then rerun the failed Gradle task. Gradle uses a different Java runtime after JAVA_HOME changes: stop the daemon with ./gradlew --stop or .\\gradlew.bat --stop , verify ./gradlew --version , and rebuild from clean . A package has the wrong architecture: compare the OS architecture with the os.arch line from java -XshowSettings:properties -version ; install a native JDK 25 matching the target and rebuild rather than renaming the output. Official references: Oracle Packaging Tool User's Guide for JDK 25 Oracle jpackage command reference Gradle Wrapper user guide Apache Maven installation guide Git installation guide Eclipse Temurin JDK downloads Back to installation →","title": "Building korTTY packages"},{"location": "getting-started/building-packages.html#building-kortty-packages","text": "korTTY can be built as Java artifacts, a self-contained portable application image or a native installer. Native packages must be built on the target operating system and processor architecture because jpackage , JavaFX and the bundled native tools select platform-specific files from the active build machine.","title": "Building korTTY packages"},{"location": "getting-started/building-packages.html#choose-an-artifact","text": "Goal Gradle task Local output Java required on the destination? Thin application JAR jar build/libs/korTTY-<version>.jar Yes, plus every runtime dependency; this JAR is not a stand-alone executable JAR Java distribution distZip / distTar build/distributions/korTTY-<version>.zip / .tar Yes; the archive includes dependencies and launch scripts but no Java runtime Self-contained app image jpackage Platform app bundle or directory under build/jpackage/ No; jpackage includes a trimmed Java runtime Native installer jpackageDmg , jpackageMsi , jpackageDeb or jpackageRpm DMG, MSI, DEB or RPM under build/jpackage/ No; the installer contains the self-contained app image Portable native archive jpackage , then the OS archive command shown below ZIP or TAR containing the entire app image No; keep the bundle or directory intact after extraction Flatpak bundle jpackage , then flatpak-builder and flatpak build-bundle build/jpackage/kortty-Linux-<version>-<architecture>.flatpak No; install the Freedesktop runtime through Flatpak An app image is not one executable file On macOS the app image is the complete korTTY.app bundle, on Windows it is the complete korTTY directory containing korTTY.exe , and on Linux it is the complete korTTY directory containing bin/korTTY . Moving only the launcher breaks the application because its runtime and libraries remain in sibling directories. The Linux directory is not a .AppImage file. Two launchers per image Every app image also carries kortty-cli , the client for the control API , declared once through jpackage 's --add-launcher in the shared argument list so that every image and installer inherits it. Its properties file is generated by the prepareCliLauncherProperties task into build/jpackage-launchers/ — deliberately outside build/jpackage-input/ , everything in which is copied verbatim into the shipped image. The DMG task repackages the finished .app and inherits the launcher rather than declaring it a second time. The kortty-cli name is not interchangeable with kortty : the graphical launcher is korTTY , and on case-insensitive macOS and Windows filesystems the two would collide.","title": "Choose an artifact"},{"location": "getting-started/building-packages.html#common-prerequisites","text": "Install these tools before building on any operating system: Requirement Why korTTY needs it A full JDK supported by the pinned Gradle wrapper Starts Gradle; korTTY then resolves a matching Temurin JDK 25 toolchain for compilation, jlink and jpackage Git Clones korTTY and the pinned SithTermFX source dependency Apache Maven Builds SithTermFX into the local Maven repository before korTTY compilation curl Downloads the pinned mosh4j artifacts used by packaged builds Outbound HTTPS access The wrapper, Maven/Gradle dependencies, SithTermFX, SHA-256-pinned mosh4j, the Monaco build toolchain, formatter browser bundles and chat-render resources are downloaded during a clean first build Gradle Wrapper from this repository gradlew and gradlew.bat download and run the repository-pinned Gradle version; do not install or substitute a system Gradle Node.js and MkDocs are not common prerequisites for an application build: Gradle downloads a pinned Node.js runtime into an isolated build directory only to compile Monaco, never into the application image, and the normal application build uses the committed offline guide resources. Prettier Standalone and sql-formatter are copied as compact browser bundles and execute offline in JavaFX WebView without Node. Packaging is pinned to the JDK 25 toolchain The packaged runtime explicitly includes jdk.jsobject , which JavaFX WebView needs and which was removed in JDK 26. The removal is tracked as OpenJDK issue JDK-8362628 : https://bugs.openjdk.org/browse/JDK-8362628 . Every packaging task resolves jpackage from the same Gradle-selected Temurin JDK 25 toolchain as compilation, so a newer system java or jpackage cannot silently create an incompatible runtime. The selected toolchain must still match the target CPU because jpackage does not cross-compile. Clone the source once: git clone https://github.com/chardonnay/korTTY.git cd korTTY Verify the host tools and inspect the JDKs Gradle can select before the first build. The system JDK may be newer than 25, but ./gradlew javaToolchains must list a matching JDK 25 for the target CPU after provisioning; Maven must use a JDK that can build SithTermFX. macOS / Linux Windows PowerShell command -v java git mvn curl java -XshowSettings:properties -version 2 > & 1 | grep -E 'java.specification.version|os.arch' git --version mvn --version curl --version ./gradlew --version ./gradlew -q javaToolchains Get-Command java , git , mvn , curl java -XshowSettings : properties -version 2 >& 1 | Select-String 'java.specification.version|os.arch' git - -version mvn - -version curl - -version .\\ gradlew . bat - -version .\\ gradlew . bat -q javaToolchains The first wrapper invocation downloads Gradle. A clean application build also clones SithTermFX when necessary, installs it through Maven, downloads the pinned build inputs, compiles korTTY and runs the tests.","title": "Common prerequisites"},{"location": "getting-started/building-packages.html#clean-target-specific-package-staging","text": "prepareJpackage assembles build/jpackage-input/libs with one final Gradle Sync . It removes stale dependency versions and formatter trees, keeps only the current mosh4j architecture and protobuf, reuses the application's parent-loaded Bouncy Castle, excludes SithTermFX's test dependency, and replaces JNA/pty4j with JARs that retain all Java classes, services, manifests and licences but only the target native path and binary architecture. The verification task deliberately seeds obsolete files before Sync and runs a real JNA/PTY smoke against the slim JARs: ./gradlew verifyJpackageStaging slimNativeRuntimeSmoke","title": "Clean, target-specific package staging"},{"location": "getting-started/building-packages.html#package-size-reports-and-budgets","text": "The platform-independent size reporter separates the app image, runtime, formatter/Mosh payloads, dependency JARs and compressed application-JAR resources, then writes JSON and Markdown. It understands the macOS Contents/app and Contents/runtime , Windows app and runtime , and Linux lib/app and lib/runtime layouts produced by jpackage . CI compares native installers with the committed release baseline, requires at least 15% reduction, applies the 190 MiB app-image and 150 MiB DMG limits, and uses a 2% regression tolerance once a new platform size is verified: DMG = \" $( find build/jpackage -maxdepth 1 -name 'korTTY-*.dmg' -print -quit ) \" python3 scripts/package-size-report.py \\ --app-image build/jpackage/korTTY.app \\ --artifact \"macos-aarch64-dmg= $DMG \" \\ --baselines package/size-baselines.json \\ --min-app-bytes 104857600 \\ --max-app-bytes 199229440 \\ --fail-on-budget \\ --output-json build/package-size/local.json \\ --output-markdown build/package-size/local.md Before a Windows archive is created, the release workflow resolves the generated app image and requires an application JAR, jvm.dll , at least 100 MiB of plausible content and x86_64 PE headers for both launcher and JVM. Linux RPM size enforcement runs after rpmsign , so the measured file is the one that is distributed.","title": "Package-size reports and budgets"},{"location": "getting-started/building-packages.html#build-the-java-artifacts","text": "Use the platform command below to compile, test and create the thin JAR plus the Java ZIP and TAR distributions: macOS / Linux Windows PowerShell ./gradlew clean build ls -l build/libs/ build/distributions/ .\\ gradlew . bat clean build Get-ChildItem .\\ build \\ libs , .\\ build \\ distributions The thin JAR contains korTTY classes and resources but not its runtime dependencies and does not provide the complete deployment by itself. For a Java-based portable layout, extract the generated ZIP or TAR and start bin/korTTY on macOS/Linux or bin\\korTTY.bat on Windows; the destination still needs JDK 25. These distributions also contain the native JavaFX dependencies selected on the build host, so build them on the same OS and architecture on which they will run. Every korTTY release format carries the repository's canonical MIT LICENSE . The RPM and pacman metadata also declare MIT ; scripts/check-release-licenses.py is the CI gate that prevents application package metadata from drifting to another license. Licences belonging to the Gradle wrapper, bundled runtimes, models or other dependencies remain separate and are not rewritten as korTTY's licence.","title": "Build the Java artifacts"},{"location": "getting-started/building-packages.html#build-pacman-packages","text": "Regular releases publish two Pacman packages: x86_64 for Arch Linux and aarch64 for Arch Linux ARM. The application images are built natively on the matching Ubuntu runners first. Both Pacman jobs then run makepkg in the official Arch Linux x86_64 container because the PKGBUILD only copies that pre-built image and does not compile or execute target code. The AArch64 job supplies a separate makepkg.conf with CARCH=aarch64 and CHOST=aarch64-unknown-linux-gnu . A CI guard rejects AArch64 packaging if prepare() , build() or check() is ever added to the PKGBUILD; such a change requires a native Arch Linux ARM builder. The package also disables stripping and debug-package generation so x86_64 tools never modify a pre-built AArch64 ELF. Before signing, CI requires the exact package filename and checks .PKGINFO , the installed MIT licence, desktop entry and launcher symlink. It also uses readelf to verify that the native launcher and bundled JVM match the advertised architecture. The detached signature is then verified locally before the package becomes an Actions artifact. For a controlled Pacman-only backfill, dispatch Build Release Binaries with scope pacman-only , the numeric version, the new pacman_pkgrel and the full commit expected behind the source tag. Use release_tag=ci-only first and inspect both architecture-specific Actions artifacts. Repeat with the real v<version> release tag only after inspection. The publishing job refuses immutable releases and filename collisions, uploads without clobbering, compares every remote GitHub digest with the local SHA-256 value and never deletes an existing or partially uploaded asset automatically.","title": "Build Pacman packages"},{"location": "getting-started/building-packages.html#build-a-flatpak-bundle","text": "Flatpak bundles are built natively on Linux for x86_64 and aarch64 . The manifest uses application ID io.github.chardonnay.korTTY with the Freedesktop 25.08 runtime and packages the self-contained Linux application image produced by jpackage . Install Flatpak, flatpak-builder and appstreamcli , then run: ./gradlew clean build jpackage flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo flatpak install --user --noninteractive flathub org.freedesktop.Platform//25.08 org.freedesktop.Sdk//25.08 flatpak-builder --user --force-clean --disable-rofiles-fuse --repo = build/flatpak-repo --default-branch = stable build/flatpak-build package/flatpak/io.github.chardonnay.korTTY.yml flatpak build-bundle --arch = \" $( flatpak --default-arch ) \" build/flatpak-repo \"build/jpackage/kortty-Linux-<version>- $( flatpak --default-arch ) .flatpak\" io.github.chardonnay.korTTY stable The package grants network, X11, audio, GPU and host-filesystem access because an SSH terminal client must reach remote hosts, render JavaFX/WebView, use terminal media and operate on explicitly selected host files. It also grants access to org.freedesktop.Flatpak ; local shells and their AI-agent commands are launched on the host through flatpak-spawn --host . Validate a local bundle by installing it, checking its architecture and running the packaged WebView smoke: flatpak install --user ./build/jpackage/kortty-Linux-<version>-<architecture>.flatpak flatpak info --user --show-ref io.github.chardonnay.korTTY xvfb-run -a flatpak run io.github.chardonnay.korTTY --webview-jit-smoke GitHub Actions runs this sequence on native ubuntu-latest and ubuntu-24.04-arm runners, signs both bundles with the Linux release key and uploads them beside the distro-native packages. A manual flatpak-only dispatch accepts a fixed source commit and a separate existing release tag so a bundle can be backfilled without moving the tag or replacing another asset.","title": "Build a Flatpak bundle"},{"location": "getting-started/building-packages.html#build-a-separate-llamacpp-runtime-package","text": "Integrated local AI uses a native artifact that is intentionally separate from every jpackage app image and installer. The upstream tag, full commit, and source-archive SHA-256 are pinned in gradle/llama-cpp-pins.properties — the single file a runtime bump rewrites, alongside a row per shipped pin — while build.gradle.kts keeps the korTTY runtime revision and API-contract version. verifyLlamaCppPin fails closed when those values disagree, the active tag has no pin row, or the requested backend is invalid. Install CMake and a native C/C++ toolchain. A Vulkan package additionally needs the Vulkan headers, loader, and shader compiler; Metal is available only on macOS with the Apple development tools. Then build the package for the current machine: CPU macOS Metal Windows/Linux Vulkan ./gradlew generateLlamaRuntimeManifest -Pllama.backend = CPU ./gradlew generateLlamaRuntimeManifest -Pllama.backend = METAL ./gradlew generateLlamaRuntimeManifest -Pllama.backend = VULKAN The task chain downloads only the pinned commit archive, verifies its SHA-256, extracts it, configures a Release build with curl, RPC, tests, examples, tools, and server Web UI disabled, builds llama-server , stages its required libraries and license, and creates a reproducible ZIP plus JSON descriptor under build/llama-runtime/packages/ . The descriptor records the immutable runtime ID, tag/commit, API-contract version, minimum korTTY version, platform, architecture, concrete backend, package size/SHA-256, publication URL placeholder, entrypoint, and revocation flag. One build host cannot create the whole matrix Native llama.cpp artifacts follow the same target-platform rule as jpackage : build each operating-system/architecture/backend combination on a matching host. The runtime workflow covers macOS arm64/x86_64 CPU+Metal, Windows x86_64 CPU+Vulkan, Linux x86_64/arm64 CPU+Vulkan. CUDA is not in the first matrix.","title": "Build a separate llama.cpp runtime package"},{"location": "getting-started/building-packages.html#candidate-detection-contract-tests-and-promotion","text": ".github/workflows/llama-runtime.yml checks upstream tags weekly; only the newest tag is ever picked, so a tighter cadence would only pile up unreviewed candidates. A change opens a candidate PR that updates the source pin but does not publish it. A scope job then sizes the build per pull request: a PR that touches gradle/llama-cpp-pins.properties , the workflow itself, or the llama/runtime-update Java sources runs the full ten-leg native matrix, while a build.gradle.kts -only change runs a single Linux x86_64 CPU smoke leg — this is why the pin lives in its own file, and a manual workflow_dispatch build is the escape hatch that always forces the full matrix. Every built artifact must start and report its version, the Linux CPU reference job additionally verifies unauthenticated rejection, authenticated model listing, completion, sleep/wake, JSON-schema chat completion, embeddings, and two simultaneously usable sidecars, and a pinned Qdrant 1.18.2 service runs the real vector-store contract. The korTTY repository owns only candidate detection and source-side matrix validation: it has no stable-promotion action, cross-repository release token, or runtime signing private key. Stable publication belongs to the public chardonnay/kortty-llama-runtimes repository. Its explicit human-dispatched workflow accepts the exact reviewed korTTY source commit, rebuilds the native matrix, runs authentication, chat/completion, embeddings, JSON-schema, sleep/wake, and parallel-sidecar smoke tests on every package, and runs the pinned Qdrant contract separately. After those checks it enters the protected llama-runtime-signing environment, verifies that LLAMA_RUNTIME_ED25519_PRIVATE_KEY_PEM matches the published trust root, verifies and extends the cumulative signed index, and creates an immutable runtime-only release with that repository's scoped github.token ; no cross-repository personal access token is required. An existing runtime ID is never overwritten. Regular promotion is limited to once every seven days, while an audited security or model-support reason can override that cadence. The application installer therefore stays unchanged while a compatible native runtime can be promoted or withdrawn independently.","title": "Candidate detection, contract tests, and promotion"},{"location": "getting-started/building-packages.html#publish-the-model-and-prompt-catalog","text": "The reviewed canonical catalog is ai-catalog/model-prompt-catalog-v1.json . It uses strict schema v1 for local-model recommendations and model-name-to-preset mappings; application code and prompt-contract text are not part of this independently released payload. .github/workflows/ai-catalog-release.yml is manual-only, accepts dispatches only from main , and has no preview or automatic promotion path. Its dispatch requires the exact reviewed catalogVersion and a release repository, defaulting to chardonnay/kortty-ai-catalog . Before signing, it verifies that the positive catalog sequence is strictly greater than the latest published sequence and runs the authoritative Java schema/trust-chain tests. The ai-catalog-signing GitHub environment must have required reviewers before production secrets are configured; inside it, the workflow checks that KORTTY_AI_CATALOG_PUBLIC_KEY matches the Ed25519 private signing secret, signs the unchanged JSON bytes, verifies the signature, and publishes immutable model-prompt-catalog-v1.json plus model-prompt-catalog-v1.sig . An existing version tag or release is refused. Configure AI_CATALOG_ED25519_PRIVATE_KEY_PEM and AI_CATALOG_RELEASE_TOKEN only as environment-scoped promotion secrets.","title": "Publish the model and prompt catalog"},{"location": "getting-started/building-packages.html#embed-the-local-ai-trust-roots","text": "Every application release that may install runtimes or refresh the catalog must embed both Ed25519 public verification keys. The runtime-channel trust root is public, auditable, and pinned in config/trust/llama-runtime-ed25519-public.pem , so normal local builds use the same key as official packages. The catalog trust root is supplied separately. Never provide either signing private key to an application build. Channel Source default Environment variable Gradle property llama.cpp runtime config/trust/llama-runtime-ed25519-public.pem Optional KORTTY_LLAMA_RUNTIME_PUBLIC_KEY ; must match the pinned key exactly Optional kortty.llamaRuntimePublicKey ; must match the pinned key exactly Model/prompt catalog None KORTTY_AI_CATALOG_PUBLIC_KEY kortty.aiCatalogPublicKey The generated resources store the fixed channel URLs and public trust roots with the application: macOS / Linux Windows PowerShell export KORTTY_AI_CATALOG_PUBLIC_KEY = \" $( cat ai-catalog-signing-public.pem ) \" ./gradlew generateLlamaRuntimeReleaseConfig generateAiCatalogReleaseConfig build $env:KORTTY_AI_CATALOG_PUBLIC_KEY = Get-Content .\\ ai-catalog-signing-public . pem -Raw .\\ gradlew . bat generateLlamaRuntimeReleaseConfig generateAiCatalogReleaseConfig build The official application-release workflow reads both names from GitHub Actions repository variables, rejects missing values or private-key material, and lets Gradle validate the X.509 Ed25519 keys before packaging. The runtime variable is a redundant CI identity check and must encode the same public key as the tracked PEM; a different override fails the build instead of replacing the trust root. If the pinned runtime key or generated release configuration is missing or malformed, runtime installation/update fails closed before fetching the index. Without the catalog key, korTTY makes no catalog request, ignores any cache, and uses its built-in bootstrap.","title": "Embed the local-AI trust roots"},{"location": "getting-started/building-packages.html#macos","text": "","title": "macOS"},{"location": "getting-started/building-packages.html#additional-software","text": "Install the common prerequisites and Apple command-line tooling. The unsigned .app build uses the JDK tools; DMG creation uses hdiutil and the configured custom icon, while signing and notarization additionally use codesign , security , notarytool and stapler . Select a current Xcode installation before a release build. xcode-select -p xcrun --find codesign xcrun --find notarytool xcrun --find stapler command -v hdiutil ditto Run xcode-select --install if the command-line tools are absent. A release intended for other users also needs an Apple Developer account, a Developer ID Application certificate and App Store Connect API credentials for notarization; an unsigned local build does not.","title": "Additional software"},{"location": "getting-started/building-packages.html#build-an-unsigned-app-image-portable-zip-and-dmg","text": "./gradlew clean build ./gradlew jpackage ditto -c -k --keepParent build/jpackage/korTTY.app \"build/jpackage/korTTY-macOS-local- $( uname -m ) .zip\" ./gradlew jpackageDmg Artifact Output Self-contained app image build/jpackage/korTTY.app Portable archive build/jpackage/korTTY-macOS-local-<architecture>.zip Disk image build/jpackage/korTTY-<version>.dmg jpackageDmg packages the app image jpackage produced — it does not build a second one — so run the two in that order; the task fails with an explicit message when build/jpackage/korTTY.app is missing. Before the DMG is finished, the app inside it is mounted and checked: its signature must verify, its payload must match the app image on disk, and its signing authority must be the same one. jpackage always re-signs the copy it places in the DMG (it swaps Contents/app/.jpackage.xml for a .package marker, which breaks the seal), and without an identity it falls back to an ad-hoc signature that Apple's notary rejects — this check is what catches that, and any other signature defect, before a notarization submit rather than minutes into one. Open the app image for a local smoke test and verify the DMG container: open build/jpackage/korTTY.app hdiutil verify build/jpackage/korTTY-*.dmg ./gradlew verifyMacDmgAppImage","title": "Build an unsigned app image, portable ZIP and DMG"},{"location": "getting-started/building-packages.html#build-macos-for-intel-x86_64","text": "The reliable paths are an Intel Mac with an x86_64 JDK 25 or the repository's GitHub Actions job on macos-15-intel . jpackage does not cross-compile, so an ARM JDK running natively on Apple Silicon creates an ARM app even if the desired filename says x86_64 . On an Intel Mac, validate the machine, Java runtime and generated launcher before distributing the result: export JAVA_HOME = \" $( /usr/libexec/java_home -v 25 -a x86_64 -F ) \" export PATH = \" $JAVA_HOME /bin: $PATH \" test \" $( uname -m ) \" = \"x86_64\" java -XshowSettings:properties -version 2 > & 1 | grep 'os.arch = x86_64' test \" $( jpackage --version | cut -d. -f1 ) \" = \"25\" ./gradlew --stop ./gradlew clean build ./gradlew jpackage ./gradlew jpackageDmg lipo -archs build/jpackage/korTTY.app/Contents/MacOS/korTTY The final lipo command must print only x86_64 . Perform the same build on Apple Silicon with an ARM JDK to obtain the separate aarch64 artifacts; the project does not create a universal binary.","title": "Build macOS for Intel (x86_64)"},{"location": "getting-started/building-packages.html#sign-the-app-and-dmg","text": "Signing is opt-in. The Gradle tasks use the repository properties below to sign bundled JNA/pty4j native libraries and pass the identity to jpackage ; do not replace this flow with an ad-hoc signature over only the outer app bundle. security find-identity -v -p codesigning export KORTTY_SIGNING_IDENTITY = \"Developer ID Application: Your Name (TEAMID)\" ./gradlew clean build ./gradlew \\ -Pkortty.macos.sign = true \\ -Pkortty.macos.signingIdentity = \" $KORTTY_SIGNING_IDENTITY \" \\ jpackage ./gradlew \\ -Pkortty.macos.sign = true \\ -Pkortty.macos.signingIdentity = \" $KORTTY_SIGNING_IDENTITY \" \\ jpackageDmg If the identity is stored in a non-default keychain, add -Pkortty.macos.signingKeychain=/absolute/path/to/keychain-db to both packaging commands. Signing and notarization are separate operations: the Gradle properties sign the artifacts but do not submit them to Apple's notary service.","title": "Sign the app and DMG"},{"location": "getting-started/building-packages.html#notarize-and-staple-locally","text": "The release workflow is the canonical automated implementation. To mirror its notarization phase locally, store the App Store Connect API credentials in a keychain profile, submit a ZIP of the signed .app , staple the accepted ticket, then submit and staple the signed DMG. export APPLE_API_KEY_ID = \"<key-id>\" export APPLE_ISSUER_ID = \"<issuer-id>\" export APPLE_API_KEY_PATH = \"/absolute/path/to/AuthKey_<key-id>.p8\" xcrun notarytool store-credentials \"kortty-notary\" \\ --key \" $APPLE_API_KEY_PATH \" \\ --key-id \" $APPLE_API_KEY_ID \" \\ --issuer \" $APPLE_ISSUER_ID \" ditto -c -k --keepParent build/jpackage/korTTY.app build/jpackage/korTTY-notarization.zip xcrun notarytool submit build/jpackage/korTTY-notarization.zip --keychain-profile \"kortty-notary\" --wait xcrun stapler staple build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY.app DMG = \" $( find build/jpackage -maxdepth 1 -name 'korTTY-*.dmg' -print -quit ) \" test -n \" $DMG \" xcrun notarytool submit \" $DMG \" --keychain-profile \"kortty-notary\" --wait xcrun stapler staple \" $DMG \" xcrun stapler validate \" $DMG \" ditto -c -k --keepParent build/jpackage/korTTY.app \"build/jpackage/korTTY-macOS-local- $( uname -m ) .zip\" Inspect any rejected submission with xcrun notarytool log <submission-id> --keychain-profile \"kortty-notary\" . Before publishing, verify Gatekeeper, the embedded signatures, the stapled tickets and the launcher architecture: codesign --verify --deep --strict --verbose = 2 build/jpackage/korTTY.app spctl --assess --type execute --verbose = 4 build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY.app xcrun stapler validate build/jpackage/korTTY-*.dmg lipo -archs build/jpackage/korTTY.app/Contents/MacOS/korTTY","title": "Notarize and staple locally"},{"location": "getting-started/building-packages.html#macos-troubleshooting","text": "jpackage reports a signing identity error: run security find-identity -v -p codesigning , copy the complete Developer ID Application identity and confirm that the selected keychain is unlocked. java , jpackage , uname or the generated launcher report different architectures: install a matching JDK, update JAVA_HOME and PATH , run ./gradlew --stop , then rebuild from clean . Notarization fails: read the submission log instead of re-signing individual JavaFX libraries; the repository deliberately preserves their upstream signatures while separately signing the native libraries covered by its Gradle task. The packaged app can reach a LAN host but ./gradlew run cannot: launch the .app so macOS can grant Local Network permission to the application bundle.","title": "macOS troubleshooting"},{"location": "getting-started/building-packages.html#further-reading-for-macos","text": "Oracle jpackage packaging prerequisites Apple: Signing Mac software with Developer ID Apple: Notarizing macOS software before distribution Apple: Customizing the notarization workflow GitHub-hosted runner reference","title": "Further reading for macOS"},{"location": "getting-started/building-packages.html#linux","text": "","title": "Linux"},{"location": "getting-started/building-packages.html#additional-software_1","text": "The app-image task needs the common prerequisites. DEB creation needs fakeroot ; RPM creation needs the RPM build tools; zip is needed only when you want the portable ZIP in addition to the TAR.GZ archive. Debian / Ubuntu Fedora / RHEL sudo apt update sudo apt install git maven curl fakeroot zip sudo dnf install git maven curl rpm-build zip Install and select the full JDK 25 separately if the distribution command does not provide that exact JDK. Build DEB packages on Debian/Ubuntu and RPM packages on an RPM-based distribution; the portable app image is available on either family.","title": "Additional software"},{"location": "getting-started/building-packages.html#build-the-app-image-portable-archives-and-packages","text": "./gradlew clean build ./gradlew jpackage tar -C build/jpackage -czf \"build/jpackage/korTTY-Linux-local- $( uname -m ) .tar.gz\" korTTY ( cd build/jpackage && zip -r \"korTTY-Linux-local- $( uname -m ) .zip\" korTTY ) Then create the native package for the current distribution: Debian / Ubuntu Fedora / RHEL ./gradlew jpackageDeb ./gradlew jpackageRpm Artifact Output Self-contained app image build/jpackage/korTTY/ Portable archives build/jpackage/korTTY-Linux-local-<architecture>.tar.gz and .zip Debian package build/jpackage/kortty*.deb RPM package build/jpackage/kortty*.rpm Run and inspect the outputs without installing the packages: test -x build/jpackage/korTTY/bin/korTTY file build/jpackage/korTTY/bin/korTTY ./build/jpackage/korTTY/bin/korTTY tar -tzf build/jpackage/korTTY-Linux-local-*.tar.gz | head dpkg-deb --info build/jpackage/kortty*.deb rpm -qpi build/jpackage/kortty*.rpm Use only the inspection command available for the package you built. Test installer installation and removal in a disposable virtual machine or container matching the target distribution before publishing it.","title": "Build the app image, portable archives and packages"},{"location": "getting-started/building-packages.html#linux-troubleshooting","text": "jpackageDeb fails with a missing fakeroot : install fakeroot and rerun from a clean build directory. jpackageRpm cannot find rpmbuild : install rpm-build on RPM distributions or rpm on Debian/Ubuntu, then confirm command -v rpmbuild . The jpackage task succeeds but no .AppImage appears: this is expected; inspect build/jpackage/korTTY/ , which is the self-contained jpackage app-image directory. jpackage reports that the destination already exists: run ./gradlew clean before rebuilding the app image.","title": "Linux troubleshooting"},{"location": "getting-started/building-packages.html#further-reading-for-linux","text": "Oracle jpackage packaging prerequisites Debian Handbook: inspecting packages with dpkg Debian fakeroot manual RPM documentation","title": "Further reading for Linux"},{"location": "getting-started/building-packages.html#windows","text": "","title": "Windows"},{"location": "getting-started/building-packages.html#additional-software_2","text": "Use a PowerShell terminal with the common prerequisites. The portable app-image task needs no external installer builder, but MSI creation requires WiX Toolset 3 or later with candle.exe and light.exe available in PATH . The release workflow uses the WiX 3 toolchain for its Windows packaging jobs. Get-Command java , javac , jpackage , git , mvn , curl Get-Command candle . exe , light . exe Restart PowerShell after changing JAVA_HOME , PATH or the WiX installation so the wrapper and jpackage see the new values.","title": "Additional software"},{"location": "getting-started/building-packages.html#build-the-app-image-portable-zip-and-msi","text": ".\\ gradlew . bat clean build .\\ gradlew . bat jpackage Compress-Archive -Path .\\ build \\ jpackage \\ korTTY -DestinationPath .\\ build \\ jpackage \\ korTTY-Windows-local . zip -Force .\\ gradlew . bat jpackageMsi Artifact Output Self-contained app image build\\jpackage\\korTTY\\ Windows launcher inside the app image build\\jpackage\\korTTY\\korTTY.exe Portable archive build\\jpackage\\korTTY-Windows-local.zip Installer build\\jpackage\\korTTY-<version>.msi The ZIP is portable because it contains the complete korTTY directory. korTTY.exe is a launcher within that directory, not a stand-alone binary; extract the entire directory before running it. The release workflow currently publishes the validated x86_64 Windows package only. OpenJFX does not provide native Windows ARM artifacts for the pinned JavaFX line, so Windows-on-ARM uses this x86_64 package through Windows emulation; the former mislabeled ARM archive is no longer produced or used as a size baseline. Native Windows ARM packaging requires a separate JavaFX/JDK packaging track and native WebView/PTY validation. Test-Path .\\ build \\ jpackage \\ korTTY \\ korTTY . exe & .\\ build \\ jpackage \\ korTTY \\ korTTY . exe Get-ChildItem .\\ build \\ jpackage \\*. msi Get-Item .\\ build \\ jpackage \\ korTTY-Windows-local . zip Test MSI installation, launch, upgrade and removal in a disposable Windows virtual machine matching the target architecture before publishing it.","title": "Build the app image, portable ZIP and MSI"},{"location": "getting-started/building-packages.html#windows-troubleshooting","text": "jpackageMsi reports that WiX cannot be found: confirm that Get-Command candle.exe, light.exe succeeds in the same PowerShell session, then rerun clean and jpackageMsi . Gradle cannot resolve the packaging JDK: run .\\gradlew.bat -q javaToolchains , allow toolchain downloads or install a native JDK 25, then stop existing daemons with .\\gradlew.bat --stop . Maven cannot build SithTermFX: confirm that mvn --version reports JDK 25 and that mvn.cmd is available in PATH . The app-image destination already exists: run .\\gradlew.bat clean before rebuilding.","title": "Windows troubleshooting"},{"location": "getting-started/building-packages.html#further-reading-for-windows","text": "Oracle jpackage packaging prerequisites WiX Toolset documentation WiX Toolset 3 releases Microsoft PowerShell: Compress-Archive","title": "Further reading for Windows"},{"location": "getting-started/building-packages.html#common-troubleshooting-and-further-reading","text": "A packaging task for another operating system is absent: this is expected because build.gradle.kts registers jpackageDmg , jpackageMsi , jpackageDeb and jpackageRpm only on their matching host platforms. A clean build cannot resolve dependencies or download a pinned asset: confirm outbound HTTPS access and proxy configuration for GitHub, Maven repositories, Gradle services, Node.js and the npm registry, then rerun the failed Gradle task. Gradle uses a different Java runtime after JAVA_HOME changes: stop the daemon with ./gradlew --stop or .\\gradlew.bat --stop , verify ./gradlew --version , and rebuild from clean . A package has the wrong architecture: compare the OS architecture with the os.arch line from java -XshowSettings:properties -version ; install a native JDK 25 matching the target and rebuild rather than renaming the output. Official references: Oracle Packaging Tool User's Guide for JDK 25 Oracle jpackage command reference Gradle Wrapper user guide Apache Maven installation guide Git installation guide Eclipse Temurin JDK downloads Back to installation →","title": "Common troubleshooting and further reading"},{"location": "getting-started/first-launch.html","text": "First launch — master password On first launch, korTTY asks you to create a master password . This password encrypts all stored connection passwords, SSH key passphrases and credentials using AES-256-GCM . Enter a password (minimum 6 characters). The field border turns green when long enough and red when too short; a strength indicator rates the quality. A weak or common password shows a warning but can still be used if you confirm. Confirm the password. Leave Share anonymous usage statistics ticked to help improve korTTY, or clear the checkbox if you would rather share nothing. It is pre-selected, but the choice is yours before you confirm — what is collected is fully anonymous and GDPR-compliant, and the setting can be changed any time in Settings → Privacy . The ? button opens Anonymous data for application optimization . An organization policy that forbids telemetry locks the checkbox and leaves it clear. Click Setup . On subsequent launches you are prompted to enter the master password to unlock your encrypted data. Disabling the prompt Settings → Security offers two ways to skip the prompt. Turning off Require master password on startup hides the prompt, but stored passwords remain inaccessible until you enter the master password manually. Disable master password prompt on startup (auto-login) instead unlocks the vault automatically from a copy of your master password kept on disk — only obfuscated, not encrypted, so reserve it for throwaway or test environments. See the Security settings for details, including unattended first launches that skip the setup dialog entirely. What is encrypted Data File Protection Connection passwords ~/.kortty/connections.xml AES-256-GCM (master-password-derived key) Stored credentials ~/.kortty/credentials.xml AES-256-GCM SSH key passphrases ~/.kortty/ssh-keys.xml AES-256-GCM Master password ~/.kortty/master.key salted hash (verification only) Remembered master password (auto-login only) ~/.kortty/master.autounlock obfuscated only — not encrypted; owner-only file permissions See the Security reference for the full encryption and backup model. Next: Main window overview →","title": "First launch — master password"},{"location": "getting-started/first-launch.html#first-launch-master-password","text": "On first launch, korTTY asks you to create a master password . This password encrypts all stored connection passwords, SSH key passphrases and credentials using AES-256-GCM . Enter a password (minimum 6 characters). The field border turns green when long enough and red when too short; a strength indicator rates the quality. A weak or common password shows a warning but can still be used if you confirm. Confirm the password. Leave Share anonymous usage statistics ticked to help improve korTTY, or clear the checkbox if you would rather share nothing. It is pre-selected, but the choice is yours before you confirm — what is collected is fully anonymous and GDPR-compliant, and the setting can be changed any time in Settings → Privacy . The ? button opens Anonymous data for application optimization . An organization policy that forbids telemetry locks the checkbox and leaves it clear. Click Setup . On subsequent launches you are prompted to enter the master password to unlock your encrypted data. Disabling the prompt Settings → Security offers two ways to skip the prompt. Turning off Require master password on startup hides the prompt, but stored passwords remain inaccessible until you enter the master password manually. Disable master password prompt on startup (auto-login) instead unlocks the vault automatically from a copy of your master password kept on disk — only obfuscated, not encrypted, so reserve it for throwaway or test environments. See the Security settings for details, including unattended first launches that skip the setup dialog entirely.","title": "First launch — master password"},{"location": "getting-started/first-launch.html#what-is-encrypted","text": "Data File Protection Connection passwords ~/.kortty/connections.xml AES-256-GCM (master-password-derived key) Stored credentials ~/.kortty/credentials.xml AES-256-GCM SSH key passphrases ~/.kortty/ssh-keys.xml AES-256-GCM Master password ~/.kortty/master.key salted hash (verification only) Remembered master password (auto-login only) ~/.kortty/master.autounlock obfuscated only — not encrypted; owner-only file permissions See the Security reference for the full encryption and backup model. Next: Main window overview →","title": "What is encrypted"},{"location": "getting-started/installation.html","text": "Installation korTTY runs on macOS, Windows and Linux . The recommended installation is a self-contained package from the GitHub Releases page ; these packages include the Java runtime needed by korTTY, so users do not need to install Java separately. Choose the correct package Each native asset name includes its processor architecture. Use aarch64 or arm64 for a native ARM package where one is listed and x86_64 for an Intel or AMD package; Windows is the exception described below. macOS Windows Linux Use the aarch64 assets on Apple Silicon and the x86_64 assets on an Intel Mac. The DMG provides the normal installation experience; the ZIP contains the complete portable .app bundle. Use the x86_64 assets on Intel/AMD PCs and on Windows on ARM, where Windows runs them through x64 emulation. korTTY detects that emulated Windows ARM combination at startup and uses JavaFX's software renderer because the emulated Direct3D pipeline can leave windows blank; native x64 systems retain hardware rendering. korTTY does not currently publish a native Windows ARM package because the pinned OpenJFX line has no Windows ARM natives. The MSI is the installer; the ZIP contains the complete portable application directory, including korTTY.exe and its runtime. The executable inside that directory is not a stand-alone single-file application. Use the package for your architecture and distribution: DEB for Debian/Ubuntu, RPM for RPM-based distributions, Pacman x86_64 for Arch Linux, Pacman aarch64 for Arch Linux ARM, or the ZIP/TAR archive for a portable installation. The archive contains a complete jpackage application-image directory; it is not a Linux .AppImage file. The kortty-cli command Every native package installs a second launcher, kortty-cli , beside the application: the client for korTTY's default-off control API . It sits in the same directory as the graphical launcher — korTTY.app/Contents/MacOS/kortty-cli on macOS, next to korTTY.exe on Windows, bin/kortty-cli inside the Linux application directory — and only the Arch package puts it on your PATH , as /usr/bin/kortty-cli . kortty-cli --version is the one command that works without a running korTTY, so it is the quickest way to confirm the installation. See Control CLI for the per-platform PATH setup. Optional system tools Some features call external programs only when you use them: Feature Optional requirement JobScheduler Rsync Local rsync and ssh in PATH , or a configured rsync binary path Terminal video export Local ffmpeg in PATH , or a path configured in Tools → Video Manager Optional integrated local AI The base korTTY package deliberately contains neither model weights nor a native llama.cpp runtime. When you enable integrated local AI, verified runtime packages from the public korTTY llama.cpp runtime channel are installed independently under ~/.kortty/llm/runtime/ and GGUF models are downloaded or imported under ~/.kortty/llm/models/ (or referenced from a path you choose). Each runtime index and package is checked against the public Ed25519 trust root pinned in the korTTY source. This keeps the normal SSH client installer small and lets llama.cpp receive compatible stable updates without replacing the whole application. Platform Runtime packages macOS arm64 / x86_64 CPU and Metal Windows x86_64 CPU and Vulkan Linux x86_64 / arm64 CPU and Vulkan CUDA is not part of the first runtime matrix. Under AI Manager > Local AI , choose the preferred runtime backend: Auto/CPU/Metal on macOS or Auto/CPU/Vulkan on Windows/Linux. On a first installation, Auto prefers Metal on macOS and falls back to CPU when no compatible Metal package is available; on Windows and Linux it prefers the portable CPU package and falls back to Vulkan only when CPU is unavailable. Updates keep the active backend when a compatible package exists. Starting a model configured for another supported GPU backend offers to install the matching signed package. Runtime and model downloads can be several gigabytes, so verify free disk space and available RAM/VRAM before installation. After installing korTTY, open AI > AI Manager > Local Models > Setup assistant . It reviews privacy, detects memory, recommends Text/Coding/embedding GGUF models, shows the repository license and exact size, verifies the immutable download, assigns roles, and runs a real local chat or embedding function test. If no runtime is active, accept the signed-runtime installation prompt, or choose Install runtime in the same tab before importing a GGUF. The Hub's context length remains visible, while new models start conservatively at 4,096 runtime tokens until you change Configure > Context size . Official packages can refresh recommendations and prompt-family mappings from a separate signed catalog; a build without that catalog trust root stays fully usable with its offline bootstrap. See Local models with llama.cpp and RAG knowledge stores . Build your own package Building from source requires a complete JDK and platform packaging tools. The dedicated guide covers the thin JAR, Java ZIP/TAR distributions, self-contained portable app images and native installers for every supported operating system, including Intel macOS builds, signing and notarization. Build korTTY packages locally → macOS Local Network privacy When launched via the Gradle daemon, korTTY runs as a child of a background process that has no Local Network permission, so connecting to LAN or private-IP hosts may fail. For LAN SSH, launch the packaged .app , which prompts for Local Network access on first launch, or start korTTY as a child of Terminal. Next: First launch & master password →","title": "Installation"},{"location": "getting-started/installation.html#installation","text": "korTTY runs on macOS, Windows and Linux . The recommended installation is a self-contained package from the GitHub Releases page ; these packages include the Java runtime needed by korTTY, so users do not need to install Java separately.","title": "Installation"},{"location": "getting-started/installation.html#choose-the-correct-package","text": "Each native asset name includes its processor architecture. Use aarch64 or arm64 for a native ARM package where one is listed and x86_64 for an Intel or AMD package; Windows is the exception described below. macOS Windows Linux Use the aarch64 assets on Apple Silicon and the x86_64 assets on an Intel Mac. The DMG provides the normal installation experience; the ZIP contains the complete portable .app bundle. Use the x86_64 assets on Intel/AMD PCs and on Windows on ARM, where Windows runs them through x64 emulation. korTTY detects that emulated Windows ARM combination at startup and uses JavaFX's software renderer because the emulated Direct3D pipeline can leave windows blank; native x64 systems retain hardware rendering. korTTY does not currently publish a native Windows ARM package because the pinned OpenJFX line has no Windows ARM natives. The MSI is the installer; the ZIP contains the complete portable application directory, including korTTY.exe and its runtime. The executable inside that directory is not a stand-alone single-file application. Use the package for your architecture and distribution: DEB for Debian/Ubuntu, RPM for RPM-based distributions, Pacman x86_64 for Arch Linux, Pacman aarch64 for Arch Linux ARM, or the ZIP/TAR archive for a portable installation. The archive contains a complete jpackage application-image directory; it is not a Linux .AppImage file.","title": "Choose the correct package"},{"location": "getting-started/installation.html#the-kortty-cli-command","text": "Every native package installs a second launcher, kortty-cli , beside the application: the client for korTTY's default-off control API . It sits in the same directory as the graphical launcher — korTTY.app/Contents/MacOS/kortty-cli on macOS, next to korTTY.exe on Windows, bin/kortty-cli inside the Linux application directory — and only the Arch package puts it on your PATH , as /usr/bin/kortty-cli . kortty-cli --version is the one command that works without a running korTTY, so it is the quickest way to confirm the installation. See Control CLI for the per-platform PATH setup.","title": "The kortty-cli command"},{"location": "getting-started/installation.html#optional-system-tools","text": "Some features call external programs only when you use them: Feature Optional requirement JobScheduler Rsync Local rsync and ssh in PATH , or a configured rsync binary path Terminal video export Local ffmpeg in PATH , or a path configured in Tools → Video Manager","title": "Optional system tools"},{"location": "getting-started/installation.html#optional-integrated-local-ai","text": "The base korTTY package deliberately contains neither model weights nor a native llama.cpp runtime. When you enable integrated local AI, verified runtime packages from the public korTTY llama.cpp runtime channel are installed independently under ~/.kortty/llm/runtime/ and GGUF models are downloaded or imported under ~/.kortty/llm/models/ (or referenced from a path you choose). Each runtime index and package is checked against the public Ed25519 trust root pinned in the korTTY source. This keeps the normal SSH client installer small and lets llama.cpp receive compatible stable updates without replacing the whole application. Platform Runtime packages macOS arm64 / x86_64 CPU and Metal Windows x86_64 CPU and Vulkan Linux x86_64 / arm64 CPU and Vulkan CUDA is not part of the first runtime matrix. Under AI Manager > Local AI , choose the preferred runtime backend: Auto/CPU/Metal on macOS or Auto/CPU/Vulkan on Windows/Linux. On a first installation, Auto prefers Metal on macOS and falls back to CPU when no compatible Metal package is available; on Windows and Linux it prefers the portable CPU package and falls back to Vulkan only when CPU is unavailable. Updates keep the active backend when a compatible package exists. Starting a model configured for another supported GPU backend offers to install the matching signed package. Runtime and model downloads can be several gigabytes, so verify free disk space and available RAM/VRAM before installation. After installing korTTY, open AI > AI Manager > Local Models > Setup assistant . It reviews privacy, detects memory, recommends Text/Coding/embedding GGUF models, shows the repository license and exact size, verifies the immutable download, assigns roles, and runs a real local chat or embedding function test. If no runtime is active, accept the signed-runtime installation prompt, or choose Install runtime in the same tab before importing a GGUF. The Hub's context length remains visible, while new models start conservatively at 4,096 runtime tokens until you change Configure > Context size . Official packages can refresh recommendations and prompt-family mappings from a separate signed catalog; a build without that catalog trust root stays fully usable with its offline bootstrap. See Local models with llama.cpp and RAG knowledge stores .","title": "Optional integrated local AI"},{"location": "getting-started/installation.html#build-your-own-package","text": "Building from source requires a complete JDK and platform packaging tools. The dedicated guide covers the thin JAR, Java ZIP/TAR distributions, self-contained portable app images and native installers for every supported operating system, including Intel macOS builds, signing and notarization. Build korTTY packages locally → macOS Local Network privacy When launched via the Gradle daemon, korTTY runs as a child of a background process that has no Local Network permission, so connecting to LAN or private-IP hosts may fail. For LAN SSH, launch the packaged .app , which prompts for Local Network access on first launch, or start korTTY as a child of Terminal. Next: First launch & master password →","title": "Build your own package"},{"location": "getting-started/main-window.html","text": "Main window overview A fresh korTTY window: the menu bar, the terminal area (where session tabs and the optional dashboard appear once you connect), and the status bar. The following diagram maps the same regions: korTTY's main window has these regions: Menu bar — File · Edit · Connections · Configuration · Tools · AI · Teamwork · Plugins · View · Help. All features are reachable here and via keyboard shortcuts . A live JobScheduler status menu appears after Help when a scheduled entry is active. While a guide translation runs, a progress indicator (bar, percentage and estimated time remaining) sits at the right end of the menu-bar row and stays visible even when the menu bar itself is hidden. Tab bar — each SSH/Mosh session runs in its own tab. Ctrl + T opens Quick Connect for a new tab; Ctrl + Tab / Ctrl + Shift + Tab switch tabs. With Open tool windows as tabs enabled ( Window settings ), management tools such as Snippets, the JobScheduler or the AI Manager open here as tabs too — in the window whose menu you used — instead of as separate windows. Dashboard (toggle Ctrl + Shift + D ) — a side panel listing every open connection with status dots, protocol badges and AI-agent badges. See Dashboard below. File browser ( View → File Browser ▸ Show on Left / Show on Right ) — a dockable local file manager with navigation toolbar, path bar, filter, type icons and a folder/file/selection counter. Its side, width, hidden-file state and last directory are restored on the next launch. See File browser . Live journal panel ( View → Live Journal , Ctrl + Alt + L ) — the active tab's running session journal as its full journal page, docked and updating in real time; side and width are restored on the next launch. Terminal area — the active terminal, with optional split-screen and broadcast input. Status bar — connection state, host/IP, active protocol, temporary SSH-key timer and connection duration. Terminal-only fullscreen Press Ctrl + Shift + F (or View → Terminal-only Fullscreen ) to show the whole korTTY window — menus, tabs and status bar included — kept at its previous window size and centered on an empty fullscreen background, so the desktop and other windows stop competing for attention. View → Hide terminal scrollbars in fullscreen removes the scrollbars too. A transparent terminal background becomes opaque while fullscreen is active and returns to its saved level when you leave. Press Ctrl + Shift + F again to restore. Dashboard Toggle the dashboard with Ctrl + Shift + D or View → Show Dashboard . It slides in on the left, sizes its width to the longest entry, and follows the active App Design's colors. The header shows the panel title with two buttons: a collapse/expand toggle (collapses everything while any node is open, expands everything otherwise) and a refresh button. Below it, connections are organized as a tree: Main window — the root node, with an active/total session count. Environments — connections whose stored credential has an environment (for example Production or Test ) are clustered under an environment node; connections without one sit directly under the main window. Groups — tabs assigned to a connection group appear under their group node. Saving a changed group in the Connection Manager updates open tabs immediately. Each connection row shows a type icon, a status dot, the server name, and a protocol badge ( ssh , mosh or local ). The status dot distinguishes three states: filled green for a healthy connection, filled red for a connection that dropped unexpectedly (including a Mosh network interruption), and a hollow outline for a session that ended normally. Terminals with AI-agent runs carry the same ✋/⚡/⏸/✓ badge as elsewhere. Hovering a row shows user@host and the connection state; double-click (or Enter ) focuses the session's tab. Right-click a connection for Focus , Duplicate , Reconnect , SFTP-Client... (connected sessions only) and Close . A footer keeps a running \"connected of total\" count, and an empty panel shows a placeholder until the first session opens. macOS Dock menu On macOS, right-click (or Control-click) the korTTY icon in the Dock for quick actions without switching to the app — they apply to the current (focused) window, opening one first if none is open: New Window New Tab in Current Window Manage Connections… (Connection Manager) Open Project… Manual (the in-app guide) About korTTY See the menu reference for every menu item and the keyboard-shortcuts reference for the full list of accelerators.","title": "Main window overview"},{"location": "getting-started/main-window.html#main-window-overview","text": "A fresh korTTY window: the menu bar, the terminal area (where session tabs and the optional dashboard appear once you connect), and the status bar. The following diagram maps the same regions: korTTY's main window has these regions: Menu bar — File · Edit · Connections · Configuration · Tools · AI · Teamwork · Plugins · View · Help. All features are reachable here and via keyboard shortcuts . A live JobScheduler status menu appears after Help when a scheduled entry is active. While a guide translation runs, a progress indicator (bar, percentage and estimated time remaining) sits at the right end of the menu-bar row and stays visible even when the menu bar itself is hidden. Tab bar — each SSH/Mosh session runs in its own tab. Ctrl + T opens Quick Connect for a new tab; Ctrl + Tab / Ctrl + Shift + Tab switch tabs. With Open tool windows as tabs enabled ( Window settings ), management tools such as Snippets, the JobScheduler or the AI Manager open here as tabs too — in the window whose menu you used — instead of as separate windows. Dashboard (toggle Ctrl + Shift + D ) — a side panel listing every open connection with status dots, protocol badges and AI-agent badges. See Dashboard below. File browser ( View → File Browser ▸ Show on Left / Show on Right ) — a dockable local file manager with navigation toolbar, path bar, filter, type icons and a folder/file/selection counter. Its side, width, hidden-file state and last directory are restored on the next launch. See File browser . Live journal panel ( View → Live Journal , Ctrl + Alt + L ) — the active tab's running session journal as its full journal page, docked and updating in real time; side and width are restored on the next launch. Terminal area — the active terminal, with optional split-screen and broadcast input. Status bar — connection state, host/IP, active protocol, temporary SSH-key timer and connection duration. Terminal-only fullscreen Press Ctrl + Shift + F (or View → Terminal-only Fullscreen ) to show the whole korTTY window — menus, tabs and status bar included — kept at its previous window size and centered on an empty fullscreen background, so the desktop and other windows stop competing for attention. View → Hide terminal scrollbars in fullscreen removes the scrollbars too. A transparent terminal background becomes opaque while fullscreen is active and returns to its saved level when you leave. Press Ctrl + Shift + F again to restore.","title": "Main window overview"},{"location": "getting-started/main-window.html#dashboard","text": "Toggle the dashboard with Ctrl + Shift + D or View → Show Dashboard . It slides in on the left, sizes its width to the longest entry, and follows the active App Design's colors. The header shows the panel title with two buttons: a collapse/expand toggle (collapses everything while any node is open, expands everything otherwise) and a refresh button. Below it, connections are organized as a tree: Main window — the root node, with an active/total session count. Environments — connections whose stored credential has an environment (for example Production or Test ) are clustered under an environment node; connections without one sit directly under the main window. Groups — tabs assigned to a connection group appear under their group node. Saving a changed group in the Connection Manager updates open tabs immediately. Each connection row shows a type icon, a status dot, the server name, and a protocol badge ( ssh , mosh or local ). The status dot distinguishes three states: filled green for a healthy connection, filled red for a connection that dropped unexpectedly (including a Mosh network interruption), and a hollow outline for a session that ended normally. Terminals with AI-agent runs carry the same ✋/⚡/⏸/✓ badge as elsewhere. Hovering a row shows user@host and the connection state; double-click (or Enter ) focuses the session's tab. Right-click a connection for Focus , Duplicate , Reconnect , SFTP-Client... (connected sessions only) and Close . A footer keeps a running \"connected of total\" count, and an empty panel shows a placeholder until the first session opens.","title": "Dashboard"},{"location": "getting-started/main-window.html#macos-dock-menu","text": "On macOS, right-click (or Control-click) the korTTY icon in the Dock for quick actions without switching to the app — they apply to the current (focused) window, opening one first if none is open: New Window New Tab in Current Window Manage Connections… (Connection Manager) Open Project… Manual (the in-app guide) About korTTY See the menu reference for every menu item and the keyboard-shortcuts reference for the full list of accelerators.","title": "macOS Dock menu"},{"location": "reference/architecture.html","text": "Architecture (Developer) korTTY is a modular JavaFX SSH client built on a layered architecture. This guide describes the core modules, their responsibilities, dependencies, and how they interact to deliver a seamless terminal experience with advanced features like job scheduling, AI integration, and terminal effect plugins. Architecture Overview The following diagram shows the main components of korTTY and how they relate: Modules and Dependencies KorTTY is organized into distinct functional modules. The diagram below groups them by function: Module Breakdown Module Purpose Key Classes core SSH connectivity, shared interactive host-key trust, session management, AI integration, terminal automation SSHSession , SshHostKeyTrustManager , AiChatManager , TerminalAgentService , Mosh4jTtyConnector ai Signed model/prompt catalog, Hugging Face metadata/downloads, embedded llama.cpp and MLX runtimes, and signed runtime packages AiCatalogService , HuggingFaceClient , LlamaRuntimeManager , LlamaRuntimePackageInstaller , EmbeddedMlxAiService , MlxRuntimeLocator rag Safe source scanning, extraction, chunking, embeddings, vector stores, synchronization, and bounded retrieval RagSourceScanner , RagSourceSynchronizer , LocalHnswStore , RagRuntimeService ui JavaFX user interface, dialogs, terminal views, SFTP manager TerminalView , TerminalTab , ConnectionEditDialog , SFTPManagerDialog , SnippetEditDialog model Domain objects for connections, credentials, snippets, jobs ServerConnection , StoredCredential , Snippet , JobSchedule jobscheduler Background job scheduling and execution JobSchedulerService , JobSchedulerJobRunner , JobJournalEntry security Master password, encryption/decryption, password vault MasterPasswordManager , EncryptionService , PasswordVault persistence XML serialization, file I/O, repository pattern XMLConnectionRepository , HistoryStorage plugin Terminal effect plugins (ServiceLoader SPI) TerminalEffectPlugin , TerminalEffectSession power Activity-aware sleep and App Nap management per platform PowerManagementCoordinator , MacPowerManagementBackend telemetry Consent-gated anonymous usage events TelemetryService , Telemetry control Default-off local control API: transport, token, verbs, event bus (see Control API ) ControlApiServer , ControlVerbs , ControlSurface , ControlEventBus , ControlApiGate cli The kortty-cli client that speaks the control API (see Control CLI ) KorttyCli teamwork Collaboration and remote access features Team-based session sharing and coordination jmx Java Management Extensions monitoring SSHClientMonitor , SSHClientMonitorMBean update Version checking and update notifications Update service and version metadata SithTermFX Terminal Engine KorTTY uses SithTermFX 1.2.2 as its primary terminal emulator, built from source during the build process. SithTermFX provides: Terminal emulation : VT100/xterm-compatible terminal rendering powered by a custom JavaFX control OSC 8 hyperlinks : Starting with SithTermFX 1.2.0, support for clickable explicit hyperlinks (restricted to safe URI schemes: http , https , mailto , ftp , ftps , news ; file:// limited to local host) Session integration : Direct JAXB marshaling of terminal state for session recording and replay Color support : Configurable ANSI and TrueColor handling with per-connection overrides Reviewed boundary fix : A pinned korTTY patch rejects the non-existent row at line == height during hyperlink hit-testing, preventing bottom-row TerminalTextBuffer range errors Reviewed shortcut-chord fix : A second pinned korTTY patch stops shortcut-chord KEY_TYPED characters (for example Cmd + Shift + D ) from reaching the pty or broadcast panes Scroll-region correctness : The scrolling region does not confine the cursor while origin mode (DECOM) is off. Text addressed above the top margin or below the bottom one stays on that line instead of being pulled into the region, and only a line feed or autowrap that actually crosses the bottom margin scrolls — so tmux's first pane row, its status line and the cursor all land where the application put them. Carried as a korTTY patch until it shipped upstream in SithTermFX 1.2.2 Build Integration The build process automatically: Clones SithTermFX at tag v1.2.2 into vendor/sithtermfx (no GitHub token required) Applies the reviewed patches in patches/sithtermfx/ — 1.2.2-terminal-panel-bottom-row.patch and 1.2.2-terminal-panel-meta-shortcut-key-typed.patch — in order, failing if a patch neither applies nor already matches the source Builds it locally using Maven via the installSithtermfxLocal task Installs artifacts to the local Maven repo ( mavenLocal() ), including one marker resource per patch that lets Gradle reject an unpatched cached UI JAR Links SithTermFX core and UI modules into the korTTY JAR No network access is needed after cloning; all build steps are deterministic and reproducible. Persistence and Configuration KorTTY stores its main configuration, credentials, and session state under ~/.kortty/ , primarily as JAXB XML. Local-model registration also uses JAXB XML, while knowledge-store configuration is strict JSON and the local vector graph is a regenerable binary snapshot. This approach ensures: Portability : Easy manual inspection and migration across systems Encryption : Sensitive data (passwords, SSH key passphrases, master password hash) is encrypted with AES-256-GCM Atomic writes : File updates use temporary files and atomic rename to prevent corruption on crash No required database : Local HNSW is self-contained; Qdrant is an optional second vector backend Configuration File Structure ~/.kortty/ ├── connections.xml # Saved SSH/Mosh connections ├── credentials.xml # Stored usernames/passwords for env-specific targets ├── ssh-keys.xml # SSH key references and encrypted passphrases ├── gpg-keys.xml # GPG key management for backup encryption ├── global-settings.xml # Application-wide settings (theme, language, AI profiles) ├── llm/ │ ├── models.xml # Local GGUF registrations and typed launch settings │ ├── models/ # Managed GGUF weights (regenerable; excluded from backup) │ ├── runtime/ # Versioned native llama.cpp packages │ ├── catalog/ # Regenerable signature-verified catalog cache │ └── run/ # Temporary sidecar key files and logs ├── rag/ │ ├── stores.json # Knowledge-store and source configuration │ └── stores/ # Regenerable local HNSW snapshots ├── job-scheduler.xml # JobScheduler jobs, host-key pins, sudo secrets, journal ├── ssh-host-keys.properties # Shared interactive Terminal/SFTP/Mosh host-key pins ├── terminal-effect-plugins.disabled # Disabled terminal-effect plugin IDs (one per line) ├── master.key # PBKDF2-hashed master password (310,000 iterations) ├── kortty.log # Application log file ├── history/ # Compressed terminal session logs ├── plugins/ # Imported external terminal-effect plugin JARs ├── bundled-plugins/ # Runtime copies of bundled exportable plugin JARs ├── projects/ # Project files (connection sets with saved layout) ├── i18n/ # Dynamically generated language property files └── ssh-keys/ # Optional: copied SSH keys (included in backups) Serialization and Encryption JAXB Marshaling converts domain objects to/from XML with automatic schema validation. Key repositories include: XMLConnectionRepository : Manages Connection objects with SFTP, jump server, and tunnel details CredentialRepository : Stores environment-specific credential patterns SSHKeyManager : Wraps imported SSH keys with encrypted passphrases JobSchedulerPersistence : Persists JobScheduleEntry objects and execution journal AES-256-GCM encryption protects sensitive fields: Master password is hashed once on first login, stored as master.key All encrypted fields use AES-256-GCM with random IVs derived from the master password SSH key passphrases, connection passwords, and sudo secrets are encrypted before persistence Backup archives can be encrypted with password-protected ZIP or GPG Core Module Organization SSH and Session Management Component Responsibility SSHSession Wraps Apache SSHD client and manages connection lifecycle SshHostKeyTrustManager Shares normalized host:port TOFU pins across interactive Terminal, SFTP, and Mosh bootstrap connections with atomic, cross-process persistence SSHKeyManager Centralized SSH key storage with encrypted passphrase support Mosh4jTtyConnector Mosh protocol connector using the mosh4j library (dynamically loaded) NativeMoshTtyConnector Native OS Mosh support (fallback) TemporarySSHKeyManager Time-limited SSH keys (e.g., from CyberArk) AI and Agent Integration Component Responsibility AiChatManager Manages AI chat sessions and conversation history AiServiceFactory Maps a profile to HTTP, Anthropic, local CLI, or embedded llama.cpp transport, then adds prompt presets and optional RAG AiCatalogService Returns a reverified cache/bootstrap immediately and schedules one signed stable-channel refresh for recommendations and preset-family mappings LlamaRuntimeManager Shares one isolated authenticated sidecar per compatible GGUF configuration and grants reference-counted request leases RagAugmentedAiService Retrieves bounded excerpts for ordinary AI actions and adds the untrusted context before the model preset TerminalAgentService Executes agent workflows: probes SSH session, sends task to model, validates/executes commands AiInternetAccessConfiguration Configures web tools (Tavily, Brave Search, SearXNG, etc.) per profile AiChatExportService Exports AI chats to Markdown, PDF, YAML, JSON, XML, Asciidoctor Job Scheduling Component Responsibility JobScheduler Main scheduler service; manages scheduled jobs and execution queue JobExecutor Executes individual jobs (SSH command, script, AI agent, SFTP, Rsync) JobJournal Persistent audit log with redaction support and auto-cleanup (14 days default) UI Module Organization The UI layer is built on JavaFX and organized into logical components: Component Purpose MainWindow Top-level application window containing menu bar, tab bar, terminal panes, dashboard, SFTP browser TerminalPane Single terminal tab with split-pane support and inline AI activity panel ConnectionDialog Multi-tab editor for connection details (SSH, tunnels, jump server, logging, etc.) SFTPManagerDialog Dual-panel file manager for local and remote file operations SnippetEditor Monaco-powered code editor with syntax highlighting, AI assistance, and Mermaid flowcharts LocalModelManagerPane Searches/downloads/imports GGUF files and controls concurrent llama.cpp sidecars RagKnowledgeStorePane Creates knowledge stores, previews sources, displays persisted indexing state, synchronizes them, and runs retrieval tests JobSchedulerDialog Job creation, scheduling, and journal inspection QuickConnectDialog Fast connection search and frequently-used connection shortcuts Security Architecture Master Password and Encryption First Launch : User creates a master password (minimum 6 characters, strength-checked via zxcvbn) Storage : Password is hashed using PBKDF2 with 310,000 iterations and stored in ~/.kortty/master.key Unlocking : Master password unlocks all encrypted data (connection passwords, SSH key passphrases, credentials, backup archive passwords) Encryption : AES-256-GCM with random IVs; encryption/decryption happens on-demand when accessing sensitive fields Host Key Verification Interactive Terminal/SFTP/Mosh bootstrap : One shared TOFU verifier is keyed by normalized host name and port. First use shows the OpenSSH SHA-256 fingerprint with No as the default; an exact match is silent, while a changed key is hard-blocked without retry. Interactive storage : ssh-host-keys.properties stores public-key material through an atomic replacement guarded by both in-process and cross-process locks. Its transient .lock companion is not backed up. JobScheduler : Unattended SSH, SFTP, and Rsync use separate connection-ID-based pins in job-scheduler.xml , including OpenSSH public-key material needed by Rsync. A per-job override can disable that verification only when the risk is explicitly accepted. Credential Management Environment-specific : Credentials can be scoped to Production, Development, Test, or Staging Glob patterns : Server patterns like *.example.com or 10.0.0.* automatically match connections Stored encrypted : All credential passwords use AES-256-GCM Embedded AI and RAG isolation Each embedded model process binds only to 127.0.0.1 on a random port and requires a generated API key stored in an owner-only temporary file. The fixed llama.cpp launch command enables offline mode and disables the web UI, agent, UI MCP proxy, and slots endpoint; inherited LLAMA_ARG_* and Hugging Face token variables are removed. Hugging Face tokens and AI profile API keys are encrypted with the master password. Model downloads are pinned to an immutable revision and verified with SHA-256. RAG accepts only centrally allowlisted, content-validated text. Retrieved excerpts are bounded and wrapped as explicitly untrusted data so indexed instructions cannot replace korTTY's system/action contract. Runtime update indexes are verified as exact bytes with Ed25519 before package URLs are parsed; packages also have signed size/SHA-256 metadata and safe ZIP extraction limits. Runtime withdrawals are persisted before process shutdown through a runtime-root denylist plus package marker. Active pointers and unsafe rollback-history entries are removed, registered models are rebound to a non-executable quarantine marker, and the launch path rechecks the marker so stale registry state cannot revive a withdrawn binary. A package that passes the lightweight version health check remains pending until its first real GGUF-backed authenticated API start. LlamaRuntimeFirstLaunchRecovery confirms it on readiness or restores the newest healthy non-revoked installation and model bindings after a start failure. The independent model/prompt catalog has its own Ed25519 trust root and strict schema. Its cache is reverified before use; a missing key disables both cache trust and network refresh and selects the compiled bootstrap. Remote Qdrant uses HTTPS, with plain HTTP restricted to a loopback test/local service. Plugin System KorTTY supports terminal effect plugins via Java ServiceLoader for customizing terminal appearance and behavior. Plugin Architecture SPI Interface Responsibility TerminalEffectPlugin Plugin metadata (ID, name, description) and session factory TerminalEffectSession Lifecycle hooks (init, render, cleanup) and optional TtyConnector wrapping TerminalEffectContext Access to overlay API, current SithTermFX widgets, animation speed, appearance TerminalEffectAppearance Optional font/color/cursor overrides TerminalEffectConnectorWrapper Transparent command stream wrapper; KorTTY can safely unwrap Plugin Loading Bundled plugins : Loaded from the application classpath (e.g., MOTHER effect) External plugins : Imported as .jar files into ~/.kortty/plugins/ Management : Enable/disable individual plugins via Plugins → Terminal Effects without uninstalling Exportable plugins : Some plugins can be exported as standalone JARs for distribution Security Note External plugins are trusted local code and are not sandboxed. Only import JARs from sources you trust. Dependencies not already bundled with korTTY must be shaded into the plugin JAR. External Dependencies KorTTY relies on carefully curated, production-tested dependencies: Category Library Version Purpose SSH Apache SSHD (core, common, sftp) 2.19.0 SSH protocol implementation BouncyCastle (bcprov, bcpkix) 1.86 Cryptographic provider, SSH key parsing and Ed25519/EdDSA key support Terminal SithTermFX (core, ui) 1.2.2 plus pinned korTTY boundary and shortcut-chord patches Terminal emulator engine Lanterna 3.1.5 Text-based UI components pty4j (JetBrains) 0.12.25 PTY allocation for Mosh Platform JNA (jna, jna-platform) 5.19.1 Native desktop power-management integration Data Jakarta XML Bind 4.0.5 (jaxb-runtime 4.0.9) JAXB serialization Gson 2.14.0 JSON parsing zip4j 2.11.6 ZIP encryption jtokkit 1.1.0 Token counting for AI requests PDFBox 3.0.8 PDF export and RAG text extraction Archive Apache Commons Compress 1.28.0 TAR, BZ2, XZ support Tukaani xz 1.12 XZ compression zstd-jni 1.5.7-16 zstd compression for rotated session-journal parts UI JavaFX 21.0.12 Application framework AtlantaFX Base 2.1.0 Seven selectable Primer, Nord, Cupertino and Dracula JavaFX user-agent themes; its transitive OpenJFX dependency is excluded Monaco Editor 0.56.0 Code editor component Mermaid 12.0.0 Local diagram parsing, SVG rendering, and PNG rasterization MathJax 3.2.2 Local AI-chat formula rendering google-java-format 1.36.1 Java code formatting Utilities jfiglet 0.0.9 ASCII art banners zxcvbn 1.9.0 Password strength (offline) Logging SLF4J / Logback 2.0.19 / 1.6.3 Structured logging Optional mosh4j 2.0.2 Mosh protocol (dynamically loaded) Local AI llama.cpp llama-server Source-pinned runtime package Local GGUF chat-completions and embeddings sidecar Dynamic Dependencies mosh4j : Its five SHA-256-pinned, architecture-specific release JARs and protobuf are bundled in native builds and dynamically loaded only when Mosh is needed. The child loader reuses Bouncy Castle from the application parent instead of shipping a second copy. rsync / ssh : External commands used by JobScheduler Rsync jobs ffmpeg : Optional; used for terminal recording video export llama.cpp runtime : Downloaded independently under ~/.kortty/llm/runtime/ ; CPU packages cover every supported platform, Metal is available on macOS, and Vulkan is available for supported Windows/Linux targets. It is never folded into the base installer. Qdrant : Optional external vector service. Remote endpoints require HTTPS; HTTP is loopback-only. Local HNSW is dependency-free and remains the default knowledge-store backend. Build Process Compilation and Packaging SithTermFX build : Cloned and built locally (Maven); its accidentally published JUnit dependency is excluded from the runtime. Browser assets : An isolated, pinned Node.js is used only at build time for Monaco and for JavaFX-WebKit compatibility processing of the SHA-256-pinned Mermaid bundle. Monaco's editor and diff pages share one mode-aware IIFE/CSS pair while retaining all five workers and language services. Mermaid and MathJax remain separate lazy resources; Prettier Standalone with five selected plugins and the sql-formatter UMD build run without Node at runtime. External formatter payload : Only shfmt, Perl::Tidy and their manifest are staged beside the app; the logo video is stored once per source surface as H.264/yuv420p at 640×360 without audio. Clean native staging : prepareJpackage uses a final Gradle Sync , so obsolete dependencies, formatter trees and Mosh architectures are deleted. Bouncy Castle is deduplicated, and JNA/pty4j are repacked with only the current target's native paths and binary architecture. Native packaging and gates : The selected Gradle JDK 25 toolchain supplies jpackage for .app/.dmg, .exe/.msi, .deb and .rpm output. scripts/package-size-report.py emits JSON/Markdown component reports and CI enforces the committed release comparison, at least 15% installer reduction, absolute app/DMG limits and frozen verified-size budgets with 2% tolerance. JavaFX dependency alignment : verifyJavaFxDependencyAlignment checks every resolved compile, runtime and test classpath before compilation and packaging. Standard OpenJFX modules must remain at 21.0.12; only the compile-time jdk-jsobject bridge uses its separately pinned version, and it may not enter a runtime classpath. llama.cpp runtime packaging : Separate Gradle tasks verify the pinned upstream tag/commit/archive SHA-256, build only llama-server plus required shared libraries, stage a backend-specific tree, and produce a reproducible immutable ZIP and signed-index descriptor input. The weekly runtime workflow opens candidate PRs; a scope job runs the full platform/backend matrix only when the pin file, the workflow, or the llama Java sources changed (a build.gradle.kts -only change gets a single smoke leg); every built leg runs a native link smoke, the reference package runs the full authenticated chat/embedding/JSON/sleep/parallel-sidecar contract, and a protected llama-runtime-signing environment with required reviewers gates manual stable promotion from main . Model/prompt catalog promotion : A separate manual main -only workflow validates the canonical strict-schema JSON, requires a sequence greater than the latest immutable release, runs schema/trust-chain tests, matches the signing key to the application trust root, signs the exact bytes, and publishes through the reviewer-protected ai-catalog-signing environment without a preview channel. Classpath and Module Path Compile-time : On JDK 25 the JavaFX jdk-jsobject artifact is supplied on the upgrade module path so WebView bridges compile consistently; the module is absent from JDK 26. Packaging-time : Gradle resolves and invokes the same Temurin JDK 25 toolchain used for compilation, so the trimmed runtime contains jdk.jsobject even when the system java / jpackage is newer. Module path : JavaFX, SithTermFX and Apache SSHD are available during compilation; unused javafx.fxml and java.scripting are not included in the packaged runtime. Data Flow and Integration Points SSH Connection Flow User Input (Quick Connect) ↓ Connection Manager (lookup saved or new connection) ↓ Worker-thread SSH handshake + progress UI ↓ Shared host:port TOFU verification (Terminal/SFTP/Mosh bootstrap) ↓ SSHSession (Apache SSHD or Mosh4j connector) ↓ Terminal Pane (SithTermFX rendering) ↓ Dashboard (status display, job monitor) AI Integration Flow Selected Terminal Text ↓ AI Action + deterministic Text/Coding role ↓ AI profile (HTTP / CLI / embedded GGUF) ↓ Action contract → AI Skills → optional bounded RAG → model preset ↓ AiServiceFactory (remote provider, local CLI, or runtime lease) ↓ AI Response Tab (render with follow-up composer) ↓ Persistence (saved chats stored in global-settings.xml) Job Scheduling Flow User creates scheduled job ↓ JobScheduler stores in job-scheduler.xml ↓ JobExecutor runs on schedule (background thread) ↓ JobJournal records output with redaction ↓ Menu-bar status displays next runs / live countdown Logging and Diagnostics Log file : ~/.kortty/kortty.log (SLF4J with Logback backend) JMX monitoring : MBean de.kortty:type=SSHClient exposes active connections, memory, buffered text size JobScheduler journal : Detailed execution logs with configurable retention (14 days default, unlimited if set to 0) Terminal history : Compressed session logs in ~/.kortty/history/ Terminal recording : Optional replay files in ~/.kortty/recordings/ Test isolation : The test Logback configuration writes to its own console only and never creates or appends to the real user's ~/.kortty/logs Thread Model UI Thread : JavaFX application thread handles all rendering and user interaction SSH Session Threads : One thread per active SSH connection (Apache SSHD pool) Split-connection handshake : Same-server and newly selected split connections perform network setup on a worker while a JavaFX progress dialog keeps host-key and keyboard-interactive prompts responsive. Job Executor Threads : Background thread pool for JobScheduler execution AI Chat Threads : Background threads for API requests (non-blocking UI) AI catalog refresh : The first catalog consumer gets verified cache/bootstrap data immediately and starts at most one background stable-channel refresh. Local-model provisioning : Setup-assistant metadata inspection, signed runtime installation, resumable GGUF download/verification, registration, and real chat/embedding tests run on background futures; JavaFX receives only progress and final state updates. Text/Coding/RAG assignments are persisted after every selected model passes. llama.cpp sidecars : One native process per compatible loaded GGUF configuration; different models can load and generate concurrently, while leases keep active requests from being stopped. Runtime-configuration saves first request an idle stop and are rejected while a lease is busy. RAG workers : Source previews, extraction, embedding batches, and HNSW candidate builds run outside the JavaFX thread; a daemon WatchService thread debounces automatic source changes. The pane permits only one active scan/index operation and returns the reviewed preview to JavaFX before configuration or indexing changes. RAG coordinator : One serialized worker and watcher per store prevents overlapping writes, reconciles Automatic sources during application startup independently of credential/JobScheduler startup, persists source hashes/counts/status, and reloads UI state after completion. File I/O Threads : Async writes for history, journal, and recordings Web formatter requests : Background callers are serialized with one total timeout; creation, loading and invocation of the lazy Prettier/SQL WebView remain confined to the JavaFX application thread, and failures discard the engine generation. Mermaid render requests : Background callers receive CompletableFuture results while all access to the single lazy renderer WebView stays on the JavaFX application thread. Requests are serialized; cancellation, a 30-second timeout, or a WebEngine error discards the engine generation, and idle cleanup releases the hidden WebView. Extensibility Points For Plugin Developers Terminal Effects : Implement TerminalEffectPlugin and register via ServiceLoader (see TERMINAL_EFFECT_PLUGINS.adoc ) Custom Formatters : Add support in the snippet editor for new languages AI Skills : Import custom AI instruction sets via AI → AI Manager → AI Skills For Integrators JAXB Repositories : Extend XMLConnectionRepository or CredentialRepository to add custom data sources SSH Session Hooks : Subclass SSHSession to inject custom connection logic JobScheduler Actions : Add new action types to JobExecutor (e.g., custom SFTP operations) Performance Considerations Session caching : Active SSH connections are cached to avoid reconnection overhead Lazy loading : Connection details loaded on demand, not all at once Compression : Terminal history and terminal logs use gzip; rotated session-journal parts use zstd (legacy .gz parts stay readable) Throttling : Terminal rendering updates are batched to reduce UI thread load Memory pooling : Large buffers for terminal text and SFTP file listing are reused Security Best Practices Master password : Set a strong, unique password; it is never transmitted or logged SSH keys : Store in ~/.kortty/ssh-keys/ for backup inclusion; passphrases are encrypted in ssh-keys.xml , and the AES-256/GPG-encrypted backup carries the key files Host key verification : Verify the OpenSSH SHA-256 fingerprint before accepting an interactive first-use prompt; keep host-key pinning enabled for unattended JobScheduler execution Backup encryption : Use password-protected ZIP or GPG encryption AI profiles : Prefer an integrated local GGUF model for sensitive data; verify the trust and data policy of every remote endpoint Avoid logging secrets : JobScheduler journal redacts stored secrets before persistence","title": "Architecture"},{"location": "reference/architecture.html#architecture-developer","text": "korTTY is a modular JavaFX SSH client built on a layered architecture. This guide describes the core modules, their responsibilities, dependencies, and how they interact to deliver a seamless terminal experience with advanced features like job scheduling, AI integration, and terminal effect plugins.","title": "Architecture (Developer)"},{"location": "reference/architecture.html#architecture-overview","text": "The following diagram shows the main components of korTTY and how they relate:","title": "Architecture Overview"},{"location": "reference/architecture.html#modules-and-dependencies","text": "KorTTY is organized into distinct functional modules. The diagram below groups them by function:","title": "Modules and Dependencies"},{"location": "reference/architecture.html#module-breakdown","text": "Module Purpose Key Classes core SSH connectivity, shared interactive host-key trust, session management, AI integration, terminal automation SSHSession , SshHostKeyTrustManager , AiChatManager , TerminalAgentService , Mosh4jTtyConnector ai Signed model/prompt catalog, Hugging Face metadata/downloads, embedded llama.cpp and MLX runtimes, and signed runtime packages AiCatalogService , HuggingFaceClient , LlamaRuntimeManager , LlamaRuntimePackageInstaller , EmbeddedMlxAiService , MlxRuntimeLocator rag Safe source scanning, extraction, chunking, embeddings, vector stores, synchronization, and bounded retrieval RagSourceScanner , RagSourceSynchronizer , LocalHnswStore , RagRuntimeService ui JavaFX user interface, dialogs, terminal views, SFTP manager TerminalView , TerminalTab , ConnectionEditDialog , SFTPManagerDialog , SnippetEditDialog model Domain objects for connections, credentials, snippets, jobs ServerConnection , StoredCredential , Snippet , JobSchedule jobscheduler Background job scheduling and execution JobSchedulerService , JobSchedulerJobRunner , JobJournalEntry security Master password, encryption/decryption, password vault MasterPasswordManager , EncryptionService , PasswordVault persistence XML serialization, file I/O, repository pattern XMLConnectionRepository , HistoryStorage plugin Terminal effect plugins (ServiceLoader SPI) TerminalEffectPlugin , TerminalEffectSession power Activity-aware sleep and App Nap management per platform PowerManagementCoordinator , MacPowerManagementBackend telemetry Consent-gated anonymous usage events TelemetryService , Telemetry control Default-off local control API: transport, token, verbs, event bus (see Control API ) ControlApiServer , ControlVerbs , ControlSurface , ControlEventBus , ControlApiGate cli The kortty-cli client that speaks the control API (see Control CLI ) KorttyCli teamwork Collaboration and remote access features Team-based session sharing and coordination jmx Java Management Extensions monitoring SSHClientMonitor , SSHClientMonitorMBean update Version checking and update notifications Update service and version metadata","title": "Module Breakdown"},{"location": "reference/architecture.html#sithtermfx-terminal-engine","text": "KorTTY uses SithTermFX 1.2.2 as its primary terminal emulator, built from source during the build process. SithTermFX provides: Terminal emulation : VT100/xterm-compatible terminal rendering powered by a custom JavaFX control OSC 8 hyperlinks : Starting with SithTermFX 1.2.0, support for clickable explicit hyperlinks (restricted to safe URI schemes: http , https , mailto , ftp , ftps , news ; file:// limited to local host) Session integration : Direct JAXB marshaling of terminal state for session recording and replay Color support : Configurable ANSI and TrueColor handling with per-connection overrides Reviewed boundary fix : A pinned korTTY patch rejects the non-existent row at line == height during hyperlink hit-testing, preventing bottom-row TerminalTextBuffer range errors Reviewed shortcut-chord fix : A second pinned korTTY patch stops shortcut-chord KEY_TYPED characters (for example Cmd + Shift + D ) from reaching the pty or broadcast panes Scroll-region correctness : The scrolling region does not confine the cursor while origin mode (DECOM) is off. Text addressed above the top margin or below the bottom one stays on that line instead of being pulled into the region, and only a line feed or autowrap that actually crosses the bottom margin scrolls — so tmux's first pane row, its status line and the cursor all land where the application put them. Carried as a korTTY patch until it shipped upstream in SithTermFX 1.2.2","title": "SithTermFX Terminal Engine"},{"location": "reference/architecture.html#build-integration","text": "The build process automatically: Clones SithTermFX at tag v1.2.2 into vendor/sithtermfx (no GitHub token required) Applies the reviewed patches in patches/sithtermfx/ — 1.2.2-terminal-panel-bottom-row.patch and 1.2.2-terminal-panel-meta-shortcut-key-typed.patch — in order, failing if a patch neither applies nor already matches the source Builds it locally using Maven via the installSithtermfxLocal task Installs artifacts to the local Maven repo ( mavenLocal() ), including one marker resource per patch that lets Gradle reject an unpatched cached UI JAR Links SithTermFX core and UI modules into the korTTY JAR No network access is needed after cloning; all build steps are deterministic and reproducible.","title": "Build Integration"},{"location": "reference/architecture.html#persistence-and-configuration","text": "KorTTY stores its main configuration, credentials, and session state under ~/.kortty/ , primarily as JAXB XML. Local-model registration also uses JAXB XML, while knowledge-store configuration is strict JSON and the local vector graph is a regenerable binary snapshot. This approach ensures: Portability : Easy manual inspection and migration across systems Encryption : Sensitive data (passwords, SSH key passphrases, master password hash) is encrypted with AES-256-GCM Atomic writes : File updates use temporary files and atomic rename to prevent corruption on crash No required database : Local HNSW is self-contained; Qdrant is an optional second vector backend","title": "Persistence and Configuration"},{"location": "reference/architecture.html#configuration-file-structure","text": "~/.kortty/ ├── connections.xml # Saved SSH/Mosh connections ├── credentials.xml # Stored usernames/passwords for env-specific targets ├── ssh-keys.xml # SSH key references and encrypted passphrases ├── gpg-keys.xml # GPG key management for backup encryption ├── global-settings.xml # Application-wide settings (theme, language, AI profiles) ├── llm/ │ ├── models.xml # Local GGUF registrations and typed launch settings │ ├── models/ # Managed GGUF weights (regenerable; excluded from backup) │ ├── runtime/ # Versioned native llama.cpp packages │ ├── catalog/ # Regenerable signature-verified catalog cache │ └── run/ # Temporary sidecar key files and logs ├── rag/ │ ├── stores.json # Knowledge-store and source configuration │ └── stores/ # Regenerable local HNSW snapshots ├── job-scheduler.xml # JobScheduler jobs, host-key pins, sudo secrets, journal ├── ssh-host-keys.properties # Shared interactive Terminal/SFTP/Mosh host-key pins ├── terminal-effect-plugins.disabled # Disabled terminal-effect plugin IDs (one per line) ├── master.key # PBKDF2-hashed master password (310,000 iterations) ├── kortty.log # Application log file ├── history/ # Compressed terminal session logs ├── plugins/ # Imported external terminal-effect plugin JARs ├── bundled-plugins/ # Runtime copies of bundled exportable plugin JARs ├── projects/ # Project files (connection sets with saved layout) ├── i18n/ # Dynamically generated language property files └── ssh-keys/ # Optional: copied SSH keys (included in backups)","title": "Configuration File Structure"},{"location": "reference/architecture.html#serialization-and-encryption","text": "JAXB Marshaling converts domain objects to/from XML with automatic schema validation. Key repositories include: XMLConnectionRepository : Manages Connection objects with SFTP, jump server, and tunnel details CredentialRepository : Stores environment-specific credential patterns SSHKeyManager : Wraps imported SSH keys with encrypted passphrases JobSchedulerPersistence : Persists JobScheduleEntry objects and execution journal AES-256-GCM encryption protects sensitive fields: Master password is hashed once on first login, stored as master.key All encrypted fields use AES-256-GCM with random IVs derived from the master password SSH key passphrases, connection passwords, and sudo secrets are encrypted before persistence Backup archives can be encrypted with password-protected ZIP or GPG","title": "Serialization and Encryption"},{"location": "reference/architecture.html#core-module-organization","text": "","title": "Core Module Organization"},{"location": "reference/architecture.html#ssh-and-session-management","text": "Component Responsibility SSHSession Wraps Apache SSHD client and manages connection lifecycle SshHostKeyTrustManager Shares normalized host:port TOFU pins across interactive Terminal, SFTP, and Mosh bootstrap connections with atomic, cross-process persistence SSHKeyManager Centralized SSH key storage with encrypted passphrase support Mosh4jTtyConnector Mosh protocol connector using the mosh4j library (dynamically loaded) NativeMoshTtyConnector Native OS Mosh support (fallback) TemporarySSHKeyManager Time-limited SSH keys (e.g., from CyberArk)","title": "SSH and Session Management"},{"location": "reference/architecture.html#ai-and-agent-integration","text": "Component Responsibility AiChatManager Manages AI chat sessions and conversation history AiServiceFactory Maps a profile to HTTP, Anthropic, local CLI, or embedded llama.cpp transport, then adds prompt presets and optional RAG AiCatalogService Returns a reverified cache/bootstrap immediately and schedules one signed stable-channel refresh for recommendations and preset-family mappings LlamaRuntimeManager Shares one isolated authenticated sidecar per compatible GGUF configuration and grants reference-counted request leases RagAugmentedAiService Retrieves bounded excerpts for ordinary AI actions and adds the untrusted context before the model preset TerminalAgentService Executes agent workflows: probes SSH session, sends task to model, validates/executes commands AiInternetAccessConfiguration Configures web tools (Tavily, Brave Search, SearXNG, etc.) per profile AiChatExportService Exports AI chats to Markdown, PDF, YAML, JSON, XML, Asciidoctor","title": "AI and Agent Integration"},{"location": "reference/architecture.html#job-scheduling","text": "Component Responsibility JobScheduler Main scheduler service; manages scheduled jobs and execution queue JobExecutor Executes individual jobs (SSH command, script, AI agent, SFTP, Rsync) JobJournal Persistent audit log with redaction support and auto-cleanup (14 days default)","title": "Job Scheduling"},{"location": "reference/architecture.html#ui-module-organization","text": "The UI layer is built on JavaFX and organized into logical components: Component Purpose MainWindow Top-level application window containing menu bar, tab bar, terminal panes, dashboard, SFTP browser TerminalPane Single terminal tab with split-pane support and inline AI activity panel ConnectionDialog Multi-tab editor for connection details (SSH, tunnels, jump server, logging, etc.) SFTPManagerDialog Dual-panel file manager for local and remote file operations SnippetEditor Monaco-powered code editor with syntax highlighting, AI assistance, and Mermaid flowcharts LocalModelManagerPane Searches/downloads/imports GGUF files and controls concurrent llama.cpp sidecars RagKnowledgeStorePane Creates knowledge stores, previews sources, displays persisted indexing state, synchronizes them, and runs retrieval tests JobSchedulerDialog Job creation, scheduling, and journal inspection QuickConnectDialog Fast connection search and frequently-used connection shortcuts","title": "UI Module Organization"},{"location": "reference/architecture.html#security-architecture","text": "","title": "Security Architecture"},{"location": "reference/architecture.html#master-password-and-encryption","text": "First Launch : User creates a master password (minimum 6 characters, strength-checked via zxcvbn) Storage : Password is hashed using PBKDF2 with 310,000 iterations and stored in ~/.kortty/master.key Unlocking : Master password unlocks all encrypted data (connection passwords, SSH key passphrases, credentials, backup archive passwords) Encryption : AES-256-GCM with random IVs; encryption/decryption happens on-demand when accessing sensitive fields","title": "Master Password and Encryption"},{"location": "reference/architecture.html#host-key-verification","text": "Interactive Terminal/SFTP/Mosh bootstrap : One shared TOFU verifier is keyed by normalized host name and port. First use shows the OpenSSH SHA-256 fingerprint with No as the default; an exact match is silent, while a changed key is hard-blocked without retry. Interactive storage : ssh-host-keys.properties stores public-key material through an atomic replacement guarded by both in-process and cross-process locks. Its transient .lock companion is not backed up. JobScheduler : Unattended SSH, SFTP, and Rsync use separate connection-ID-based pins in job-scheduler.xml , including OpenSSH public-key material needed by Rsync. A per-job override can disable that verification only when the risk is explicitly accepted.","title": "Host Key Verification"},{"location": "reference/architecture.html#credential-management","text": "Environment-specific : Credentials can be scoped to Production, Development, Test, or Staging Glob patterns : Server patterns like *.example.com or 10.0.0.* automatically match connections Stored encrypted : All credential passwords use AES-256-GCM","title": "Credential Management"},{"location": "reference/architecture.html#embedded-ai-and-rag-isolation","text": "Each embedded model process binds only to 127.0.0.1 on a random port and requires a generated API key stored in an owner-only temporary file. The fixed llama.cpp launch command enables offline mode and disables the web UI, agent, UI MCP proxy, and slots endpoint; inherited LLAMA_ARG_* and Hugging Face token variables are removed. Hugging Face tokens and AI profile API keys are encrypted with the master password. Model downloads are pinned to an immutable revision and verified with SHA-256. RAG accepts only centrally allowlisted, content-validated text. Retrieved excerpts are bounded and wrapped as explicitly untrusted data so indexed instructions cannot replace korTTY's system/action contract. Runtime update indexes are verified as exact bytes with Ed25519 before package URLs are parsed; packages also have signed size/SHA-256 metadata and safe ZIP extraction limits. Runtime withdrawals are persisted before process shutdown through a runtime-root denylist plus package marker. Active pointers and unsafe rollback-history entries are removed, registered models are rebound to a non-executable quarantine marker, and the launch path rechecks the marker so stale registry state cannot revive a withdrawn binary. A package that passes the lightweight version health check remains pending until its first real GGUF-backed authenticated API start. LlamaRuntimeFirstLaunchRecovery confirms it on readiness or restores the newest healthy non-revoked installation and model bindings after a start failure. The independent model/prompt catalog has its own Ed25519 trust root and strict schema. Its cache is reverified before use; a missing key disables both cache trust and network refresh and selects the compiled bootstrap. Remote Qdrant uses HTTPS, with plain HTTP restricted to a loopback test/local service.","title": "Embedded AI and RAG isolation"},{"location": "reference/architecture.html#plugin-system","text": "KorTTY supports terminal effect plugins via Java ServiceLoader for customizing terminal appearance and behavior.","title": "Plugin System"},{"location": "reference/architecture.html#plugin-architecture","text": "SPI Interface Responsibility TerminalEffectPlugin Plugin metadata (ID, name, description) and session factory TerminalEffectSession Lifecycle hooks (init, render, cleanup) and optional TtyConnector wrapping TerminalEffectContext Access to overlay API, current SithTermFX widgets, animation speed, appearance TerminalEffectAppearance Optional font/color/cursor overrides TerminalEffectConnectorWrapper Transparent command stream wrapper; KorTTY can safely unwrap","title": "Plugin Architecture"},{"location": "reference/architecture.html#plugin-loading","text": "Bundled plugins : Loaded from the application classpath (e.g., MOTHER effect) External plugins : Imported as .jar files into ~/.kortty/plugins/ Management : Enable/disable individual plugins via Plugins → Terminal Effects without uninstalling Exportable plugins : Some plugins can be exported as standalone JARs for distribution","title": "Plugin Loading"},{"location": "reference/architecture.html#security-note","text": "External plugins are trusted local code and are not sandboxed. Only import JARs from sources you trust. Dependencies not already bundled with korTTY must be shaded into the plugin JAR.","title": "Security Note"},{"location": "reference/architecture.html#external-dependencies","text": "KorTTY relies on carefully curated, production-tested dependencies: Category Library Version Purpose SSH Apache SSHD (core, common, sftp) 2.19.0 SSH protocol implementation BouncyCastle (bcprov, bcpkix) 1.86 Cryptographic provider, SSH key parsing and Ed25519/EdDSA key support Terminal SithTermFX (core, ui) 1.2.2 plus pinned korTTY boundary and shortcut-chord patches Terminal emulator engine Lanterna 3.1.5 Text-based UI components pty4j (JetBrains) 0.12.25 PTY allocation for Mosh Platform JNA (jna, jna-platform) 5.19.1 Native desktop power-management integration Data Jakarta XML Bind 4.0.5 (jaxb-runtime 4.0.9) JAXB serialization Gson 2.14.0 JSON parsing zip4j 2.11.6 ZIP encryption jtokkit 1.1.0 Token counting for AI requests PDFBox 3.0.8 PDF export and RAG text extraction Archive Apache Commons Compress 1.28.0 TAR, BZ2, XZ support Tukaani xz 1.12 XZ compression zstd-jni 1.5.7-16 zstd compression for rotated session-journal parts UI JavaFX 21.0.12 Application framework AtlantaFX Base 2.1.0 Seven selectable Primer, Nord, Cupertino and Dracula JavaFX user-agent themes; its transitive OpenJFX dependency is excluded Monaco Editor 0.56.0 Code editor component Mermaid 12.0.0 Local diagram parsing, SVG rendering, and PNG rasterization MathJax 3.2.2 Local AI-chat formula rendering google-java-format 1.36.1 Java code formatting Utilities jfiglet 0.0.9 ASCII art banners zxcvbn 1.9.0 Password strength (offline) Logging SLF4J / Logback 2.0.19 / 1.6.3 Structured logging Optional mosh4j 2.0.2 Mosh protocol (dynamically loaded) Local AI llama.cpp llama-server Source-pinned runtime package Local GGUF chat-completions and embeddings sidecar","title": "External Dependencies"},{"location": "reference/architecture.html#dynamic-dependencies","text": "mosh4j : Its five SHA-256-pinned, architecture-specific release JARs and protobuf are bundled in native builds and dynamically loaded only when Mosh is needed. The child loader reuses Bouncy Castle from the application parent instead of shipping a second copy. rsync / ssh : External commands used by JobScheduler Rsync jobs ffmpeg : Optional; used for terminal recording video export llama.cpp runtime : Downloaded independently under ~/.kortty/llm/runtime/ ; CPU packages cover every supported platform, Metal is available on macOS, and Vulkan is available for supported Windows/Linux targets. It is never folded into the base installer. Qdrant : Optional external vector service. Remote endpoints require HTTPS; HTTP is loopback-only. Local HNSW is dependency-free and remains the default knowledge-store backend.","title": "Dynamic Dependencies"},{"location": "reference/architecture.html#build-process","text": "","title": "Build Process"},{"location": "reference/architecture.html#compilation-and-packaging","text": "SithTermFX build : Cloned and built locally (Maven); its accidentally published JUnit dependency is excluded from the runtime. Browser assets : An isolated, pinned Node.js is used only at build time for Monaco and for JavaFX-WebKit compatibility processing of the SHA-256-pinned Mermaid bundle. Monaco's editor and diff pages share one mode-aware IIFE/CSS pair while retaining all five workers and language services. Mermaid and MathJax remain separate lazy resources; Prettier Standalone with five selected plugins and the sql-formatter UMD build run without Node at runtime. External formatter payload : Only shfmt, Perl::Tidy and their manifest are staged beside the app; the logo video is stored once per source surface as H.264/yuv420p at 640×360 without audio. Clean native staging : prepareJpackage uses a final Gradle Sync , so obsolete dependencies, formatter trees and Mosh architectures are deleted. Bouncy Castle is deduplicated, and JNA/pty4j are repacked with only the current target's native paths and binary architecture. Native packaging and gates : The selected Gradle JDK 25 toolchain supplies jpackage for .app/.dmg, .exe/.msi, .deb and .rpm output. scripts/package-size-report.py emits JSON/Markdown component reports and CI enforces the committed release comparison, at least 15% installer reduction, absolute app/DMG limits and frozen verified-size budgets with 2% tolerance. JavaFX dependency alignment : verifyJavaFxDependencyAlignment checks every resolved compile, runtime and test classpath before compilation and packaging. Standard OpenJFX modules must remain at 21.0.12; only the compile-time jdk-jsobject bridge uses its separately pinned version, and it may not enter a runtime classpath. llama.cpp runtime packaging : Separate Gradle tasks verify the pinned upstream tag/commit/archive SHA-256, build only llama-server plus required shared libraries, stage a backend-specific tree, and produce a reproducible immutable ZIP and signed-index descriptor input. The weekly runtime workflow opens candidate PRs; a scope job runs the full platform/backend matrix only when the pin file, the workflow, or the llama Java sources changed (a build.gradle.kts -only change gets a single smoke leg); every built leg runs a native link smoke, the reference package runs the full authenticated chat/embedding/JSON/sleep/parallel-sidecar contract, and a protected llama-runtime-signing environment with required reviewers gates manual stable promotion from main . Model/prompt catalog promotion : A separate manual main -only workflow validates the canonical strict-schema JSON, requires a sequence greater than the latest immutable release, runs schema/trust-chain tests, matches the signing key to the application trust root, signs the exact bytes, and publishes through the reviewer-protected ai-catalog-signing environment without a preview channel.","title": "Compilation and Packaging"},{"location": "reference/architecture.html#classpath-and-module-path","text": "Compile-time : On JDK 25 the JavaFX jdk-jsobject artifact is supplied on the upgrade module path so WebView bridges compile consistently; the module is absent from JDK 26. Packaging-time : Gradle resolves and invokes the same Temurin JDK 25 toolchain used for compilation, so the trimmed runtime contains jdk.jsobject even when the system java / jpackage is newer. Module path : JavaFX, SithTermFX and Apache SSHD are available during compilation; unused javafx.fxml and java.scripting are not included in the packaged runtime.","title": "Classpath and Module Path"},{"location": "reference/architecture.html#data-flow-and-integration-points","text": "","title": "Data Flow and Integration Points"},{"location": "reference/architecture.html#ssh-connection-flow","text": "User Input (Quick Connect) ↓ Connection Manager (lookup saved or new connection) ↓ Worker-thread SSH handshake + progress UI ↓ Shared host:port TOFU verification (Terminal/SFTP/Mosh bootstrap) ↓ SSHSession (Apache SSHD or Mosh4j connector) ↓ Terminal Pane (SithTermFX rendering) ↓ Dashboard (status display, job monitor)","title": "SSH Connection Flow"},{"location": "reference/architecture.html#ai-integration-flow","text": "Selected Terminal Text ↓ AI Action + deterministic Text/Coding role ↓ AI profile (HTTP / CLI / embedded GGUF) ↓ Action contract → AI Skills → optional bounded RAG → model preset ↓ AiServiceFactory (remote provider, local CLI, or runtime lease) ↓ AI Response Tab (render with follow-up composer) ↓ Persistence (saved chats stored in global-settings.xml)","title": "AI Integration Flow"},{"location": "reference/architecture.html#job-scheduling-flow","text": "User creates scheduled job ↓ JobScheduler stores in job-scheduler.xml ↓ JobExecutor runs on schedule (background thread) ↓ JobJournal records output with redaction ↓ Menu-bar status displays next runs / live countdown","title": "Job Scheduling Flow"},{"location": "reference/architecture.html#logging-and-diagnostics","text": "Log file : ~/.kortty/kortty.log (SLF4J with Logback backend) JMX monitoring : MBean de.kortty:type=SSHClient exposes active connections, memory, buffered text size JobScheduler journal : Detailed execution logs with configurable retention (14 days default, unlimited if set to 0) Terminal history : Compressed session logs in ~/.kortty/history/ Terminal recording : Optional replay files in ~/.kortty/recordings/ Test isolation : The test Logback configuration writes to its own console only and never creates or appends to the real user's ~/.kortty/logs","title": "Logging and Diagnostics"},{"location": "reference/architecture.html#thread-model","text": "UI Thread : JavaFX application thread handles all rendering and user interaction SSH Session Threads : One thread per active SSH connection (Apache SSHD pool) Split-connection handshake : Same-server and newly selected split connections perform network setup on a worker while a JavaFX progress dialog keeps host-key and keyboard-interactive prompts responsive. Job Executor Threads : Background thread pool for JobScheduler execution AI Chat Threads : Background threads for API requests (non-blocking UI) AI catalog refresh : The first catalog consumer gets verified cache/bootstrap data immediately and starts at most one background stable-channel refresh. Local-model provisioning : Setup-assistant metadata inspection, signed runtime installation, resumable GGUF download/verification, registration, and real chat/embedding tests run on background futures; JavaFX receives only progress and final state updates. Text/Coding/RAG assignments are persisted after every selected model passes. llama.cpp sidecars : One native process per compatible loaded GGUF configuration; different models can load and generate concurrently, while leases keep active requests from being stopped. Runtime-configuration saves first request an idle stop and are rejected while a lease is busy. RAG workers : Source previews, extraction, embedding batches, and HNSW candidate builds run outside the JavaFX thread; a daemon WatchService thread debounces automatic source changes. The pane permits only one active scan/index operation and returns the reviewed preview to JavaFX before configuration or indexing changes. RAG coordinator : One serialized worker and watcher per store prevents overlapping writes, reconciles Automatic sources during application startup independently of credential/JobScheduler startup, persists source hashes/counts/status, and reloads UI state after completion. File I/O Threads : Async writes for history, journal, and recordings Web formatter requests : Background callers are serialized with one total timeout; creation, loading and invocation of the lazy Prettier/SQL WebView remain confined to the JavaFX application thread, and failures discard the engine generation. Mermaid render requests : Background callers receive CompletableFuture results while all access to the single lazy renderer WebView stays on the JavaFX application thread. Requests are serialized; cancellation, a 30-second timeout, or a WebEngine error discards the engine generation, and idle cleanup releases the hidden WebView.","title": "Thread Model"},{"location": "reference/architecture.html#extensibility-points","text": "","title": "Extensibility Points"},{"location": "reference/architecture.html#for-plugin-developers","text": "Terminal Effects : Implement TerminalEffectPlugin and register via ServiceLoader (see TERMINAL_EFFECT_PLUGINS.adoc ) Custom Formatters : Add support in the snippet editor for new languages AI Skills : Import custom AI instruction sets via AI → AI Manager → AI Skills","title": "For Plugin Developers"},{"location": "reference/architecture.html#for-integrators","text": "JAXB Repositories : Extend XMLConnectionRepository or CredentialRepository to add custom data sources SSH Session Hooks : Subclass SSHSession to inject custom connection logic JobScheduler Actions : Add new action types to JobExecutor (e.g., custom SFTP operations)","title": "For Integrators"},{"location": "reference/architecture.html#performance-considerations","text": "Session caching : Active SSH connections are cached to avoid reconnection overhead Lazy loading : Connection details loaded on demand, not all at once Compression : Terminal history and terminal logs use gzip; rotated session-journal parts use zstd (legacy .gz parts stay readable) Throttling : Terminal rendering updates are batched to reduce UI thread load Memory pooling : Large buffers for terminal text and SFTP file listing are reused","title": "Performance Considerations"},{"location": "reference/architecture.html#security-best-practices","text": "Master password : Set a strong, unique password; it is never transmitted or logged SSH keys : Store in ~/.kortty/ssh-keys/ for backup inclusion; passphrases are encrypted in ssh-keys.xml , and the AES-256/GPG-encrypted backup carries the key files Host key verification : Verify the OpenSSH SHA-256 fingerprint before accepting an interactive first-use prompt; keep host-key pinning enabled for unattended JobScheduler execution Backup encryption : Use password-protected ZIP or GPG encryption AI profiles : Prefer an integrated local GGUF model for sensitive data; verify the trust and data policy of every remote endpoint Avoid logging secrets : JobScheduler journal redacts stored secrets before persistence","title": "Security Best Practices"},{"location": "reference/cli.html","text": "Control CLI kortty-cli is the command-line client for korTTY's Control API . It ships inside every korTTY package as a second launcher beside the application itself, finds the running korTTY on its own, authenticates, and turns the API's answers into something a shell script or a coding agent can act on. It talks only to a korTTY running on the same computer as you , and only while that korTTY has the Control API switched on. Everything the API refuses, the CLI refuses too. Switch the API on first Without Settings › Terminal › Control API ticked there is nothing to connect to, and every command exits with code 3. See Control API for what you are turning on and what it can do. Where the command lives kortty-cli is not a script — it is a real launcher built into the packaged application image, so it starts the bundled JVM without needing Java on your PATH . Package Path macOS app bundle /Applications/korTTY.app/Contents/MacOS/kortty-cli Windows installer C:\\Program Files\\korTTY\\kortty-cli.exe Linux deb / rpm /opt/kortty/bin/kortty-cli Arch (pacman) /usr/bin/kortty-cli → /usr/lib/kortty/bin/kortty-cli Flatpak flatpak run --command=kortty-cli io.github.chardonnay.korTTY Portable archive (Linux) <extracted directory>/korTTY/bin/kortty-cli Portable archive (Windows) <extracted directory>\\korTTY\\kortty-cli.exe — at the image root, not under bin Portable archive (macOS) <extracted directory>/korTTY.app/Contents/MacOS/kortty-cli Only the pacman package puts it on your PATH for you. Everywhere else, add the directory to PATH or link the launcher into a directory that is already on it: # macOS sudo ln -s /Applications/korTTY.app/Contents/MacOS/kortty-cli /usr/local/bin/kortty-cli # Linux deb/rpm sudo ln -s /opt/kortty/bin/kortty-cli /usr/local/bin/kortty-cli # Windows, for the current user [Environment] :: SetEnvironmentVariable ( 'Path' , $env:Path + ';C:\\Program Files\\korTTY' , 'User' ) The launcher is deliberately not called kortty : that name belongs to the graphical launcher ( /usr/bin/kortty on Arch, korTTY elsewhere), and on macOS and Windows, whose filesystems ignore case, a second launcher called kortty would collide with it. Java distribution archives The korTTY-Java-*.zip and .tar archives carry no launchers beyond the graphical one. From those, start the client as java -cp korTTY-<version>.jar de.kortty.cli.KorttyCli <command> . Shape of a command kortty-cli <group> <command> [ options ] The groups are pane , tab , window , agent , events , notify , raw , ping and schema . Inside a group, a command name is the API method after the dot with its underscores spelled as dashes — pane.send_text is pane send-text . Four methods do not follow from their name and are worth learning: api.schema is schema , events.subscribe is events , notification.show is notify , and anything without a command of its own — events.unsubscribe , pane.resolve — is reached through raw . Everything a command needs is a flag. There are no positional arguments except the key names of pane send-keys and agent send-keys , and the method and JSON of raw . kortty-cli ping # is a korTTY listening? kortty-cli pane list # every open pane kortty-cli pane read --focused --recent --lines 200 kortty-cli pane run --pane p1a2b3c4d --command 'make test' kortty-cli pane wait-output --pane p1a2b3c4d --contains 'BUILD SUCCESSFUL' --timeout-ms 300000 kortty-cli raw events.unsubscribe '{\"subscription\":\"s1\"}' kortty-cli schema prints the complete machine-readable command surface — every command, its options, its result shape, its errors and a worked example — straight from the running korTTY. The parameters, results and errors come from the same declarations the server dispatches, and each entry's cli line is checked against this client's own parser before release, so both describe the version you are actually talking to; use it rather than guessing, especially from a script that has to keep working across versions. Choosing a pane Every pane and agent command takes exactly one selector, and every selector is a flag: Selector Means --pane p1a2b3c4d That pane, which must be unique among the open ones --pane w1:t9f3a…:p1a2b3c4d The fully qualified address --tab t9f3a… That tab's focused pane --focused The pane you are looking at --current The pane the command is running in Giving none of them, or more than one, is a syntax error (exit 2) that names the four; a bare p1a2b3c4d or @focused written where a flag belongs is a syntax error too. --current is resolved by the client, not the server: it walks its own process ancestry and asks korTTY which pane owns one of those process ids. That is why it works from a sub-shell, a Makefile recipe or a nested script without any setup, and why it fails loudly with a clear message where it cannot work rather than addressing the wrong pane. --current needs a local shell It matches against the operating-system process id of a pane's local shell. Inside an SSH session, inside a container, or in the Flatpak package — where local shells run on the host through flatpak-spawn and korTTY never sees their process ids — there is nothing to match and --current cannot resolve. Address the pane explicitly there. Waiting Commands that wait — pane wait-output , agent wait , agent prompt --wait-until , agent start --wait — block until the condition is met or the timeout expires, and exit 4 on a timeout. Every wait has a server-side hard cap of ten minutes; a longer request is clamped and the answer says so. Requests on one connection run one at a time, so a script that wants to wait on one pane while doing something in another simply runs two commands concurrently — each opens its own connection. Up to eight connections may be open at once. Watching events kortty-cli events subscribes to korTTY's coding-agent events and prints one JSON object per line, as they happen, until it is told to stop. kortty-cli events --kinds agent.state_changed --panes p1a2b3c4d --count 1 kortty-cli events --include-evidence --timeout 60000 --kinds and --panes take comma-separated lists and narrow what is delivered; --include-evidence adds the detector's evidence to each event; --count N stops after N events. Three things end the stream: --count is reached, the --timeout passes, or you interrupt it. The first two exit 0. An interrupt is not handled — there is deliberately no signal handler, so the process ends with the shell's 128 + SIGINT = 130, which is worth allowing for in a CI job that wraps the stream in timeout . With neither --count nor --timeout , the stream has no deadline at all and runs until korTTY exits or you stop it. Exit codes The exit code comes from the server, not from a table the client keeps, so the two can never disagree. Code Meaning Retry? 0 Success — 1 The request was valid but could not be carried out: no such pane, tab or agent; not connected; the write failed; the last pane cannot be closed Sometimes — the answer's retryable field says 2 The request itself was wrong: a bad selector, an unknown key name, an invalid regular expression, a missing or out-of-range parameter, an unknown command No 3 Refused: the Control API is off, enterprise policy denies it, the token was rejected, korTTY is not ready yet, or too many connections are open No, until you change something 4 A wait timed out Usually 130 kortty-cli events was interrupted (Ctrl-C). The stream has no signal handler, so this is the shell's own 128 + SIGINT — Every failure prints a one-line diagnostic on stderr in the form kortty-cli: <message> (<code>) , where <code> is the stable wire code — not_found , timeout , blocked_by_policy — that the exit code above was derived from. Add --pretty to get the whole JSON-RPC error object instead, which is what a script should branch on: kortty-cli pane read --focused --pretty 2 >err.json || code = $( jq -r .error.data.code err.json ) Failures the client diagnoses on its own are the exception: an unparseable command line, a korTTY that is not running, a --current that matches no pane and an expired client deadline never reached the server, so they carry no wire code and print a bare kortty-cli: <message> on either setting. Branch on the exit code for those. Checking the installation kortty-cli --version prints the version and exits 0. It is the only command that does not need a running korTTY, which makes it the right thing to put in an installation check or a CI smoke test. kortty-cli --version || echo 'kortty-cli is not installed or not on PATH' Scripting notes Nothing is written to stdout except the command's result, so kortty-cli pane read --focused pipes cleanly. Diagnostics, warnings and error objects go to stderr. The authentication token is read from ~/.kortty/control/endpoint.json by the client. There is no --token option and no environment variable, deliberately: a token on a command line ends up in the process list and in your shell history. korTTY logs one line per action the CLI performs, and raises one desktop notification the first time a program types into a pane. Your script is not invisible, by design.","title": "Control CLI"},{"location": "reference/cli.html#control-cli","text": "kortty-cli is the command-line client for korTTY's Control API . It ships inside every korTTY package as a second launcher beside the application itself, finds the running korTTY on its own, authenticates, and turns the API's answers into something a shell script or a coding agent can act on. It talks only to a korTTY running on the same computer as you , and only while that korTTY has the Control API switched on. Everything the API refuses, the CLI refuses too. Switch the API on first Without Settings › Terminal › Control API ticked there is nothing to connect to, and every command exits with code 3. See Control API for what you are turning on and what it can do.","title": "Control CLI"},{"location": "reference/cli.html#where-the-command-lives","text": "kortty-cli is not a script — it is a real launcher built into the packaged application image, so it starts the bundled JVM without needing Java on your PATH . Package Path macOS app bundle /Applications/korTTY.app/Contents/MacOS/kortty-cli Windows installer C:\\Program Files\\korTTY\\kortty-cli.exe Linux deb / rpm /opt/kortty/bin/kortty-cli Arch (pacman) /usr/bin/kortty-cli → /usr/lib/kortty/bin/kortty-cli Flatpak flatpak run --command=kortty-cli io.github.chardonnay.korTTY Portable archive (Linux) <extracted directory>/korTTY/bin/kortty-cli Portable archive (Windows) <extracted directory>\\korTTY\\kortty-cli.exe — at the image root, not under bin Portable archive (macOS) <extracted directory>/korTTY.app/Contents/MacOS/kortty-cli Only the pacman package puts it on your PATH for you. Everywhere else, add the directory to PATH or link the launcher into a directory that is already on it: # macOS sudo ln -s /Applications/korTTY.app/Contents/MacOS/kortty-cli /usr/local/bin/kortty-cli # Linux deb/rpm sudo ln -s /opt/kortty/bin/kortty-cli /usr/local/bin/kortty-cli # Windows, for the current user [Environment] :: SetEnvironmentVariable ( 'Path' , $env:Path + ';C:\\Program Files\\korTTY' , 'User' ) The launcher is deliberately not called kortty : that name belongs to the graphical launcher ( /usr/bin/kortty on Arch, korTTY elsewhere), and on macOS and Windows, whose filesystems ignore case, a second launcher called kortty would collide with it. Java distribution archives The korTTY-Java-*.zip and .tar archives carry no launchers beyond the graphical one. From those, start the client as java -cp korTTY-<version>.jar de.kortty.cli.KorttyCli <command> .","title": "Where the command lives"},{"location": "reference/cli.html#shape-of-a-command","text": "kortty-cli <group> <command> [ options ] The groups are pane , tab , window , agent , events , notify , raw , ping and schema . Inside a group, a command name is the API method after the dot with its underscores spelled as dashes — pane.send_text is pane send-text . Four methods do not follow from their name and are worth learning: api.schema is schema , events.subscribe is events , notification.show is notify , and anything without a command of its own — events.unsubscribe , pane.resolve — is reached through raw . Everything a command needs is a flag. There are no positional arguments except the key names of pane send-keys and agent send-keys , and the method and JSON of raw . kortty-cli ping # is a korTTY listening? kortty-cli pane list # every open pane kortty-cli pane read --focused --recent --lines 200 kortty-cli pane run --pane p1a2b3c4d --command 'make test' kortty-cli pane wait-output --pane p1a2b3c4d --contains 'BUILD SUCCESSFUL' --timeout-ms 300000 kortty-cli raw events.unsubscribe '{\"subscription\":\"s1\"}' kortty-cli schema prints the complete machine-readable command surface — every command, its options, its result shape, its errors and a worked example — straight from the running korTTY. The parameters, results and errors come from the same declarations the server dispatches, and each entry's cli line is checked against this client's own parser before release, so both describe the version you are actually talking to; use it rather than guessing, especially from a script that has to keep working across versions.","title": "Shape of a command"},{"location": "reference/cli.html#choosing-a-pane","text": "Every pane and agent command takes exactly one selector, and every selector is a flag: Selector Means --pane p1a2b3c4d That pane, which must be unique among the open ones --pane w1:t9f3a…:p1a2b3c4d The fully qualified address --tab t9f3a… That tab's focused pane --focused The pane you are looking at --current The pane the command is running in Giving none of them, or more than one, is a syntax error (exit 2) that names the four; a bare p1a2b3c4d or @focused written where a flag belongs is a syntax error too. --current is resolved by the client, not the server: it walks its own process ancestry and asks korTTY which pane owns one of those process ids. That is why it works from a sub-shell, a Makefile recipe or a nested script without any setup, and why it fails loudly with a clear message where it cannot work rather than addressing the wrong pane. --current needs a local shell It matches against the operating-system process id of a pane's local shell. Inside an SSH session, inside a container, or in the Flatpak package — where local shells run on the host through flatpak-spawn and korTTY never sees their process ids — there is nothing to match and --current cannot resolve. Address the pane explicitly there.","title": "Choosing a pane"},{"location": "reference/cli.html#waiting","text": "Commands that wait — pane wait-output , agent wait , agent prompt --wait-until , agent start --wait — block until the condition is met or the timeout expires, and exit 4 on a timeout. Every wait has a server-side hard cap of ten minutes; a longer request is clamped and the answer says so. Requests on one connection run one at a time, so a script that wants to wait on one pane while doing something in another simply runs two commands concurrently — each opens its own connection. Up to eight connections may be open at once.","title": "Waiting"},{"location": "reference/cli.html#watching-events","text": "kortty-cli events subscribes to korTTY's coding-agent events and prints one JSON object per line, as they happen, until it is told to stop. kortty-cli events --kinds agent.state_changed --panes p1a2b3c4d --count 1 kortty-cli events --include-evidence --timeout 60000 --kinds and --panes take comma-separated lists and narrow what is delivered; --include-evidence adds the detector's evidence to each event; --count N stops after N events. Three things end the stream: --count is reached, the --timeout passes, or you interrupt it. The first two exit 0. An interrupt is not handled — there is deliberately no signal handler, so the process ends with the shell's 128 + SIGINT = 130, which is worth allowing for in a CI job that wraps the stream in timeout . With neither --count nor --timeout , the stream has no deadline at all and runs until korTTY exits or you stop it.","title": "Watching events"},{"location": "reference/cli.html#exit-codes","text": "The exit code comes from the server, not from a table the client keeps, so the two can never disagree. Code Meaning Retry? 0 Success — 1 The request was valid but could not be carried out: no such pane, tab or agent; not connected; the write failed; the last pane cannot be closed Sometimes — the answer's retryable field says 2 The request itself was wrong: a bad selector, an unknown key name, an invalid regular expression, a missing or out-of-range parameter, an unknown command No 3 Refused: the Control API is off, enterprise policy denies it, the token was rejected, korTTY is not ready yet, or too many connections are open No, until you change something 4 A wait timed out Usually 130 kortty-cli events was interrupted (Ctrl-C). The stream has no signal handler, so this is the shell's own 128 + SIGINT — Every failure prints a one-line diagnostic on stderr in the form kortty-cli: <message> (<code>) , where <code> is the stable wire code — not_found , timeout , blocked_by_policy — that the exit code above was derived from. Add --pretty to get the whole JSON-RPC error object instead, which is what a script should branch on: kortty-cli pane read --focused --pretty 2 >err.json || code = $( jq -r .error.data.code err.json ) Failures the client diagnoses on its own are the exception: an unparseable command line, a korTTY that is not running, a --current that matches no pane and an expired client deadline never reached the server, so they carry no wire code and print a bare kortty-cli: <message> on either setting. Branch on the exit code for those.","title": "Exit codes"},{"location": "reference/cli.html#checking-the-installation","text": "kortty-cli --version prints the version and exits 0. It is the only command that does not need a running korTTY, which makes it the right thing to put in an installation check or a CI smoke test. kortty-cli --version || echo 'kortty-cli is not installed or not on PATH'","title": "Checking the installation"},{"location": "reference/cli.html#scripting-notes","text": "Nothing is written to stdout except the command's result, so kortty-cli pane read --focused pipes cleanly. Diagnostics, warnings and error objects go to stderr. The authentication token is read from ~/.kortty/control/endpoint.json by the client. There is no --token option and no environment variable, deliberately: a token on a command line ends up in the process list and in your shell history. korTTY logs one line per action the CLI performs, and raises one desktop notification the first time a program types into a pane. Your script is not invisible, by design.","title": "Scripting notes"},{"location": "reference/config-files.html","text": "Configuration files KorTTY stores all application data and configuration under the ~/.kortty/ directory in your home folder. This guide documents every file and subdirectory, their purpose, and how they are used. Directory structure ~/.kortty/ ├── connections.xml # Saved SSH connections ├── credentials.xml # Stored credentials (encrypted) ├── ssh-keys.xml # SSH key management ├── gpg-keys.xml # GPG keys for backup encryption ├── global-settings.xml # Global application settings ├── llm/ │ ├── models.xml # Local GGUF registrations/settings │ ├── models/ # Managed GGUF weights │ ├── runtime/ # Versioned llama.cpp packages, activation and revocation state │ ├── catalog/ # Verified model/prompt catalog cache │ └── run/ # Temporary sidecar state ├── rag/ │ ├── stores.json # Knowledge stores and source configuration │ └── stores/ # Local HNSW snapshots ├── ai-chats.xml # Saved AI conversations ├── snippets.xml # Code snippets and scripts ├── snippet-variables.xml # Snippet variable storage ├── job-scheduler.xml # JobScheduler jobs, host-key pins, sudo secrets, journal ├── ssh-host-keys.properties # Interactive Terminal/SFTP/Mosh host-key pins ├── ssh-host-keys.properties.lock # Transient cross-process writer lock (not backed up) ├── master.key # Hashed master password (PBKDF2) ├── master.autounlock # Optional auto-login password (obfuscated; owner-only) ├── terminal-effect-plugins.disabled # Disabled terminal-effect plugin IDs ├── coding-agents/ # User overrides for coding-agent detection rules ├── control/ # Control API endpoint (owner-only 0700; only while enabled) │ ├── control.sock # Unix socket (Linux/macOS); absent on Windows │ └── endpoint.json # Transport, port and access token (0600); deleted on exit ├── kortty.log # Application log file ├── history/ # Terminal session history (compressed) ├── journals/ # Session journals (one directory per journal) ├── terminal-logs/ # Default folder for per-connection terminal logs ├── plugins/ # Imported terminal-effect plugin JARs ├── bundled-plugins/ # Runtime copies of bundled exportable plugins ├── projects/ # Project files (.kortty) ├── i18n/ # Generated language files (messages_*.properties) └── ssh-keys/ # Optional copied SSH keys (included in backups) Core configuration files connections.xml Contains all saved SSH connections with their settings. Includes: - Connection name, host, port, username - Authentication method (password, SSH key, temporary SSH key) - Terminal appearance overrides (font, colors, size) - SSH tunnels and jump server configuration - Optional per-connection SSH host-key verification override (verify, don't verify, or inherit) - Terminal effect plugins and animation speed - Connection-specific terminal logging settings - Per-connection session journal settings (enable, capture typed input, AI summaries, summary interval) - Window geometry preferences - Group/folder organization - Optional free-text tag (used for search, bulk tagging and tag-based export) Security: Connection passwords are encrypted with AES-256-GCM using the master password. Note If a connection password is encrypted, it is stored in a hashed/encrypted format and cannot be viewed as plain text. When you open a saved connection, the password is automatically decrypted using your master password. credentials.xml Centralized credential storage for username/password pairs. Includes: - Credential name, username, password - Environment (Production, Development, Test, Staging) - Server pattern (glob patterns like *.example.com or 10.0.0.* ) - Auto-assignment to connections matching the pattern Security: All passwords are encrypted with AES-256-GCM. ssh-keys.xml Manages centralized SSH key storage. Includes: - SSH key path - Key passphrase (if the key is protected) - Optional description - Key fingerprint Security: Key passphrases are encrypted with AES-256-GCM using the master password. Tip SSH keys referenced in this file can be kept in their original location or copied to ~/.kortty/ssh-keys/ via the Copy to User Directory action in SSH key management. Copied keys are included in encrypted backups; keys left in their original locations are only referenced and must be migrated separately. ssh-host-keys.properties The versioned trust-on-first-use store for interactive Terminal and SFTP connections and the SSH bootstrap used by Mosh. Entries are keyed by normalized host name and port and contain the public-key algorithm, OpenSSH SHA-256 fingerprint, OpenSSH public-key line, and trust timestamp. A matching key is accepted silently after first-use confirmation; a changed key is hard-blocked and is not replaced automatically. When host-key verification is relaxed to accept-new for a connection, an unknown key is pinned without the confirmation prompt — a changed key is still refused in both modes. Writes use a temporary file plus atomic replacement, while ssh-host-keys.properties.lock coordinates separate korTTY processes so their pins are merged safely. The properties file is included in encrypted backups; the transient lock file is not. This endpoint-based store is separate from the JobScheduler host-key pins in job-scheduler.xml , which are keyed by connection ID for unattended operations. gpg-keys.xml Stores GPG key information for backup encryption. Includes: - GPG key ID - Key email address - Optional fingerprint - Import source (system keyring or manual entry) global-settings.xml Global application preferences and defaults. Contains: every preference of the Global Settings dialog, grouped below by the area it belongs to. Appearance and fonts UI theme and appearance settings Font family and default size UI font size (percent, 80–160%, or auto-derived from the display resolution) and the manual's separate text size (70–250%) Terminal color configuration Window, menus and panels Main-window geometry and state (position, size, maximized status), plus a named geometry entry for every user-resizable application dialog Menu bar visibility preference Dashboard visibility state \"Open tool windows as tabs\" flag Docked live session-journal panel: placement (hidden/left/right) and width JobScheduler status display preference Last ASCII Art dialog preview zoom level Last ASCII Art picture size (Small to Extra large) for the AI Picture tab, and separately for the Image File tab Last ASCII Art copy format: comment style, output-code style and gap Terminal and connections Terminal logging defaults Terminal effect plugin defaults SSH keep-alive settings Connection timeout and retry defaults AI, models and knowledge stores AI profile defaults, Text/Coding role assignments, embedded GGUF references, prompt presets, and knowledge-store associations RAG embedding model ID and llama.cpp preferred runtime backend/update policy Optional encrypted Hugging Face token AI request timeout in minutes (0 = no limit) Session journal Storage folder and capture-log format AI summaries (interval and profile) and the AI screenshot analysis toggle Note translation language AI line window and token budget Journal page appearance and live-log tail height Custom markers and marker rules The remembered journal window geometries Snippets and translation Snippet input-hardening defaults (enabled, options, max file size) Snippet translation target language Translation API settings Export and recording PDF export watermark (off by default; custom text and colour) Document export footer (on by default; custom text) Video/recording preferences Security and backup Master password auto-login flag ( skipMasterPasswordPrompt ) SSH host-key verification opt-out: the global flag and the list of connection groups whose checking is relaxed to accept-new Backup encryption method and retention settings llm/models.xml The atomically written JAXB registry for locally installed or referenced GGUF models. Contains: Stable model ID and display name GGUF path and compatible llama-server executable path Backend ( AUTO , CPU , METAL , or VULKAN ) Context size, CPU threads, GPU layers, and idle-unload minutes The registry contains paths and settings, not model weights or API keys. llm/models/ holds managed GGUF copies, llm/runtime/ holds independently updated native packages, and llm/run/ holds temporary process directories, logs, and owner-only generated key files. Temporary keys are removed when the sidecar stops. llm/runtime/ The regenerable native-runtime area contains immutable package directories plus small atomic state files: active-v1 points to the currently selected installation. pending-first-launch-v1 records a candidate and its rollback base until a real GGUF-backed authenticated API start succeeds. healthy-history-v1 retains at most the two newest confirmed, non-revoked installations. revoked-v1 is the durable denylist learned from verified signed indexes; a revoked package also contains .kortty-runtime-revoked . blocked-active-v1 remembers the runtime ID removed from active use by a withdrawal so the UI can explain why local AI remains blocked. pinned-runtime-v1 holds the runtime ID the user switched to with Other version… ; while present, update checks keep that version. The MLX runtime area llm/mlx/runtime/ uses a file of the same name. packages/ contains extracted verified installations, while downloads/ is temporary staging protected by the updater lock. Do not edit or delete the denylist/quarantine markers to re-enable a package. Runtime launches independently enforce them, and a compatible signed replacement must be installed instead. The entire directory is excluded from backup because packages and state can be recreated from the signed stable channel. llm/catalog/last-valid-catalog-v1.json An atomic cache envelope containing the last model/prompt catalog payload and its detached signature. korTTY re-verifies the signature and strict schema before every cache use. If the application has no valid catalog public trust root, this file is ignored and the built-in bootstrap is used without a network refresh. The cache is regenerable and is not included in backups. rag/stores.json The atomically written, owner-readable JSON registry for knowledge stores and their sources. Contains: Store ID/name/type, local snapshot directory or Qdrant endpoint/collection, embedding model ID and vector dimensions Text, Coding, and autonomous-use assignments Per-source stable ID, canonical path, file/directory type, enabled flag, automatic/manual sync mode, size limit, include/exclude globs, .gitignore preference, content hashes, last status, file/chunk/problem counts, and last successful index time The rag/stores/ subdirectory holds regenerable index.hnsw snapshots. A v2 snapshot embeds its format version, vector dimensions, embedding model ID, hierarchical graph parameters, entry point, chunk metadata, vectors, node levels, and per-layer neighbors; a mismatch is rejected and requires a rebuild. A valid legacy single-layer v1 snapshot is rebuilt and atomically migrated when opened. job-scheduler.xml All JobScheduler jobs and related data. Contains: - Job name, enabled status, action type (COMMAND, SNIPPET_SCRIPT, AI_AGENT, SFTP, RSYNC_SYNC) - Schedule configuration (weekdays, times, intervals, date ranges) - Target servers or groups - Host-key pins (OpenSSH public-key material for unattended execution) - Sudo passwords for servers and groups (encrypted) - Journal entries with timestamps, exit codes, and redacted output - Journal retention settings (auto-delete entries older than 14 days by default) Security: - Host-key pinning is required by default for unattended SSH/SFTP/Rsync execution - Sudo passwords are encrypted with the master password - Journal entries are stored with KorTTY-managed secrets redacted before persistence - Archive passwords and backup encryption credentials are stored encrypted Warning If the master password is locked and a job needs SSH, sudo, API, or archive secrets, the job is blocked and a journal entry is created explaining the issue. ai-chats.xml Saved AI conversations and chat history. Includes: - Chat title and creation timestamp - Conversation messages and responses - Associated AI profile used for the chat - Follow-up prompt history (for context) Note: AI result tabs are not automatically saved. You must explicitly save them from the AI tab using the Save button to add them to this file. snippets.xml Code snippets, scripts, and templates. Includes: - Snippet name, description, language - Code content with syntax highlighting metadata - Category/folder organization - Tags and metadata - Target system (Operating System column: Linux, macOS, Windows, etc.) - Import/export history Features: - Support for JSON/XML/YAML import/export - Plain-text script export - ZIP archives with optional password or GPG encryption - Local syntax highlighting with Monaco editor - AI-assisted editing and code generation - Persisted Mermaid diagrams (logical-structure flowchart, sequence, state, class, ER) with stable code-reference node IDs and, for selection-scoped diagrams, the covered line range - One-liner export with optional script arguments snippet-variables.xml Stores variable definitions for use within snippets. Includes: - Variable name, value, type - Scope (local or shared) - Default values and validation rules master.key Binary file containing the hashed master password. Format: PBKDF2 hash with 310,000 iterations Security: This file does not contain the actual master password—only a cryptographic hash used to verify the password you enter on startup. If this file is lost or corrupted, you must restart KorTTY and set a new master password (though you will lose access to previously stored encrypted credentials and SSH key passphrases). Warning If you forget your master password, delete master.key and credentials.xml , restart KorTTY, set a new master password, and re-enter your passwords. There is no recovery mechanism for the lost password. master.autounlock Written only while auto-login is enabled: a copy of the master password, obfuscated only — not encrypted , with owner-only file permissions. Deleting it (or disabling the option) restores the normal startup prompt. terminal-effect-plugins.disabled Text file listing disabled terminal-effect plugin IDs (one per line). Purpose: When you disable a terminal-effect plugin via Plugins > Terminal Effects , its ID is written to this file so it remains disabled after restart. coding-agents/ Optional user overrides for the coding-agent detection rules, one JSON file per agent ( claude-code.json , codex.json , gemini-cli.json ). Purpose: A file here replaces the bundled rule file of the same agent entirely. An invalid file is reported in the log as a coding-agents warning and the bundled rules stay in effect. The directory does not exist until you create it. See Coding agents → Custom rules . kortty.log Application log file. Contains: - Startup messages - Connection attempts and results - Terminal session events - Configuration changes - Errors and warnings - Performance metrics (on request via JMX) Rotation: The log file grows throughout the application session. Old logs are not automatically rotated (the log file persists until KorTTY exits or you manually delete it). Tip Check this file when troubleshooting connection issues, plugin loading problems, or unexpected behavior. Common issues logged here include SSH errors, encryption failures, and import/export problems. Directories history/ Compressed terminal session history. Format: GZIP-compressed text files, one per terminal session Naming: {session-id}_{timestamp}.history.gz (for session history from terminal logging) Purpose: Stores project/session scrollback history so reopened sessions can restore their terminal content. Access: Terminal history is loaded automatically when you open a saved connection and displayed in the terminal history search feature. Note Per-connection Terminal Logging does not write here: its generated log files go to the folder configured on the connection's Terminal Logging tab, or to ~/.kortty/terminal-logs/ when that folder is left empty. journals/ Session journals — one self-contained directory per journal (location configurable in Settings > Logging > Session Journal ). Each journal directory holds journal.xml (the curated document: metadata, AI summaries, markers, notes, screenshot references), the append-only capture log session-log.json / .xml / .yaml (JSON Lines by default) with zstd-compressed rotated parts (part size and part count are configurable per connection on the Journal tab, defaults 25 MB and 20 parts; journals from older versions keep their gzip-compressed .gz parts), the generated journal.html timeline page, and screenshots/*.png . See Session journal . terminal-logs/ Default target folder for per-connection terminal logs when a connection's log folder field is left empty. File naming, daily rotation, compression and retention follow the connection's logging configuration. plugins/ User-imported terminal-effect plugin JARs. Purpose: External terminal-effect plugins that you import via Plugins > Terminal Effects > Import . Security: Imported plugins are trusted Java code and are not sandboxed. Only import JARs from sources you trust. Cleanup: If you delete a plugin from this directory, it will no longer be available in KorTTY. Disabled plugins remain in this directory but are listed in terminal-effect-plugins.disabled . Warning Plugin dependencies must be shaded into the plugin JAR. Adjacent dependency JARs are not loaded automatically. See Terminal effect plugins for packaging guidelines. bundled-plugins/ Runtime copies of bundled exportable terminal-effect plugin JARs. Purpose: Backup and working copies of built-in plugins that can be exported to external users. Contents: The MU/TH/UR 6000 effect plugin and any other exportable bundled plugins. Auto-management: KorTTY automatically manages this directory. Users should not edit it manually. projects/ Project files for saving and loading connection sets. Format: KorTTY XML-based project format ( .kortty files) Naming: User-defined project names with .kortty extension (e.g., production.kortty , development.kortty ) Contains: - List of open connections/tabs at project save time - Window state (tabs, sizes, active tab) - Dashboard state - Project metadata and creation timestamp Purpose: Quickly open a pre-configured set of connections for a specific project or workflow. Usage: Save a project via File > Save Project , restore via File > Open Project or the project history menu. i18n/ Dynamically generated language translation files. Format: Java properties files ( .properties ) Naming: messages_{language-code}.properties (e.g., messages_de.properties for German, messages_fr.properties for French) Purpose: Stores translations generated by translation APIs (Google Translate, DeepL, LibreTranslate, Microsoft Translator, or Yandex) when you choose to generate language files via Settings > Translation . Auto-refresh: When you upgrade KorTTY to a new version, generated language files are marked as outdated. Use Settings > Translation > Regenerate outdated to refresh them with any new or changed UI keys. Note Built-in languages (English, German, Italian, Spanish, Portuguese, French, Croatian, Dutch) are shipped with the application and do not use this directory. This directory is only used for dynamically generated translations. ssh-keys/ Optional directory for copied SSH keys. Purpose: Stores copies of SSH keys in one place for backup and migration. How to use: 1. Open Management > Manage SSH Keys... 2. Select an SSH key and click Copy to User Directory 3. The key is copied to ~/.kortty/ssh-keys/ Backup: The key files in this directory are included when you create a backup via Edit > Create Backup (the archive is AES-256- or GPG-encrypted). On import they are restored with owner-only file permissions, and the import merges: local keys are never deleted, and existing files are only replaced with Overwrite enabled. Benefits: - Centralized location for all SSH keys - Included in encrypted backups - Easy migration to new machines Security summary Item Security Method Master password PBKDF2 hashing with 310,000 iterations Connection passwords AES-256-GCM encryption SSH key passphrases AES-256-GCM encryption Interactive Terminal/SFTP/Mosh host keys Normalized host:port TOFU with OpenSSH SHA-256 fingerprints and fail-closed change detection; the optional per-connection/group/global opt-out relaxes only unknown-key prompting to accept-new Credentials (username/password) AES-256-GCM encryption JobScheduler sudo passwords AES-256-GCM encryption JobScheduler journal entries Redacted secrets before persistence Backup files Password-protected ZIP or GPG encryption API keys (AI profiles) AES-256-GCM encryption with master password Terminal effect plugins Not encrypted; trusted local code, not sandboxed File locations by platform All files are stored in the same ~/.kortty/ directory across platforms: macOS: /Users/{username}/.kortty/ Windows: C:\\Users\\{username}\\.kortty\\ (or %USERPROFILE%\\.kortty\\ ) Linux: /home/{username}/.kortty/ (or $HOME/.kortty/ ) Backup and recovery When you create a backup via Edit > Create Backup , the following configuration is included: All .xml configuration files (connections, credentials, SSH key references and passphrases, GPG keys, global settings, JobScheduler, snippets, snippet variables, AI chats) master.key projects/ directory ssh-keys/ directory — copied SSH key files (restored with owner-only permissions; imports merge and never delete local keys) ssh-host-keys.properties interactive host-key trust store (not its transient .lock file) llm/models.xml local-model registrations rag/stores.json knowledge-store/source metadata Managed GGUF weights, native runtime packages, temporary sidecar data, original source documents, and HNSW snapshots are excluded because they are large or regenerable. The history/ and i18n/ directories are also not part of a backup, and neither is the auto-login file master.autounlock . The backup is encrypted (password-protected ZIP or GPG) and saved to a location you specify. Tip Backups are the recommended way to migrate KorTTY to a new machine or to recover from data loss. Create a backup regularly and store it in a safe location. Accessing configuration files You can edit KorTTY configuration directly by: Closing KorTTY completely Opening ~/.kortty/ in your file manager or terminal Editing the XML or JSON file with a text editor Restarting KorTTY to load the changes Warning Editing XML/JSON files directly can corrupt your data if not done carefully. Never edit a binary index.hnsw snapshot. Always create a backup before manual editing. For most configuration tasks, use the KorTTY UI instead—it handles encryption, validation, and file format correctly. Troubleshooting Configuration file not found: - KorTTY creates the ~/.kortty/ directory and all necessary subdirectories on first launch. - If a configuration file is missing, KorTTY uses sensible defaults and creates the file on next save. Encryption errors: - If you forget your master password, you must delete master.key and set a new password. Previously encrypted data will be inaccessible. Corrupted XML files: - If a .xml file becomes corrupted, restore from a backup or delete the file. KorTTY will recreate it with defaults on next save. Knowledge-store registry or snapshot cannot be read: - Restore rag/stores.json from a backup or recreate the store in the AI Manager. Delete only the affected regenerable index.hnsw , then choose Update now to rebuild it from the configured source files. Interactive SSH host key changed: - Do not reconnect until you have verified the new OpenSSH SHA-256 fingerprint with the server administrator and ruled out DNS, routing, or man-in-the-middle problems. KorTTY deliberately blocks the mismatch without retrying or replacing the stored pin. Plugin loading issues: - Check kortty.log for error messages related to plugin loading (e.g., duplicate IDs, missing services, class loading errors). - Ensure the plugin JAR is in ~/.kortty/plugins/ and contains META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin . - Use Plugins > Terminal Effects > Reload to refresh the plugin list. Log file size: - kortty.log grows during the application session. It is not automatically rotated. You can safely delete it while KorTTY is closed. Master password recovery: - There is no recovery for a forgotten master password. If you lose it, delete master.key and credentials.xml , restart KorTTY, and set a new password.","title": "Configuration files"},{"location": "reference/config-files.html#configuration-files","text": "KorTTY stores all application data and configuration under the ~/.kortty/ directory in your home folder. This guide documents every file and subdirectory, their purpose, and how they are used.","title": "Configuration files"},{"location": "reference/config-files.html#directory-structure","text": "~/.kortty/ ├── connections.xml # Saved SSH connections ├── credentials.xml # Stored credentials (encrypted) ├── ssh-keys.xml # SSH key management ├── gpg-keys.xml # GPG keys for backup encryption ├── global-settings.xml # Global application settings ├── llm/ │ ├── models.xml # Local GGUF registrations/settings │ ├── models/ # Managed GGUF weights │ ├── runtime/ # Versioned llama.cpp packages, activation and revocation state │ ├── catalog/ # Verified model/prompt catalog cache │ └── run/ # Temporary sidecar state ├── rag/ │ ├── stores.json # Knowledge stores and source configuration │ └── stores/ # Local HNSW snapshots ├── ai-chats.xml # Saved AI conversations ├── snippets.xml # Code snippets and scripts ├── snippet-variables.xml # Snippet variable storage ├── job-scheduler.xml # JobScheduler jobs, host-key pins, sudo secrets, journal ├── ssh-host-keys.properties # Interactive Terminal/SFTP/Mosh host-key pins ├── ssh-host-keys.properties.lock # Transient cross-process writer lock (not backed up) ├── master.key # Hashed master password (PBKDF2) ├── master.autounlock # Optional auto-login password (obfuscated; owner-only) ├── terminal-effect-plugins.disabled # Disabled terminal-effect plugin IDs ├── coding-agents/ # User overrides for coding-agent detection rules ├── control/ # Control API endpoint (owner-only 0700; only while enabled) │ ├── control.sock # Unix socket (Linux/macOS); absent on Windows │ └── endpoint.json # Transport, port and access token (0600); deleted on exit ├── kortty.log # Application log file ├── history/ # Terminal session history (compressed) ├── journals/ # Session journals (one directory per journal) ├── terminal-logs/ # Default folder for per-connection terminal logs ├── plugins/ # Imported terminal-effect plugin JARs ├── bundled-plugins/ # Runtime copies of bundled exportable plugins ├── projects/ # Project files (.kortty) ├── i18n/ # Generated language files (messages_*.properties) └── ssh-keys/ # Optional copied SSH keys (included in backups)","title": "Directory structure"},{"location": "reference/config-files.html#core-configuration-files","text": "","title": "Core configuration files"},{"location": "reference/config-files.html#connectionsxml","text": "Contains all saved SSH connections with their settings. Includes: - Connection name, host, port, username - Authentication method (password, SSH key, temporary SSH key) - Terminal appearance overrides (font, colors, size) - SSH tunnels and jump server configuration - Optional per-connection SSH host-key verification override (verify, don't verify, or inherit) - Terminal effect plugins and animation speed - Connection-specific terminal logging settings - Per-connection session journal settings (enable, capture typed input, AI summaries, summary interval) - Window geometry preferences - Group/folder organization - Optional free-text tag (used for search, bulk tagging and tag-based export) Security: Connection passwords are encrypted with AES-256-GCM using the master password. Note If a connection password is encrypted, it is stored in a hashed/encrypted format and cannot be viewed as plain text. When you open a saved connection, the password is automatically decrypted using your master password.","title": "connections.xml"},{"location": "reference/config-files.html#credentialsxml","text": "Centralized credential storage for username/password pairs. Includes: - Credential name, username, password - Environment (Production, Development, Test, Staging) - Server pattern (glob patterns like *.example.com or 10.0.0.* ) - Auto-assignment to connections matching the pattern Security: All passwords are encrypted with AES-256-GCM.","title": "credentials.xml"},{"location": "reference/config-files.html#ssh-keysxml","text": "Manages centralized SSH key storage. Includes: - SSH key path - Key passphrase (if the key is protected) - Optional description - Key fingerprint Security: Key passphrases are encrypted with AES-256-GCM using the master password. Tip SSH keys referenced in this file can be kept in their original location or copied to ~/.kortty/ssh-keys/ via the Copy to User Directory action in SSH key management. Copied keys are included in encrypted backups; keys left in their original locations are only referenced and must be migrated separately.","title": "ssh-keys.xml"},{"location": "reference/config-files.html#ssh-host-keysproperties","text": "The versioned trust-on-first-use store for interactive Terminal and SFTP connections and the SSH bootstrap used by Mosh. Entries are keyed by normalized host name and port and contain the public-key algorithm, OpenSSH SHA-256 fingerprint, OpenSSH public-key line, and trust timestamp. A matching key is accepted silently after first-use confirmation; a changed key is hard-blocked and is not replaced automatically. When host-key verification is relaxed to accept-new for a connection, an unknown key is pinned without the confirmation prompt — a changed key is still refused in both modes. Writes use a temporary file plus atomic replacement, while ssh-host-keys.properties.lock coordinates separate korTTY processes so their pins are merged safely. The properties file is included in encrypted backups; the transient lock file is not. This endpoint-based store is separate from the JobScheduler host-key pins in job-scheduler.xml , which are keyed by connection ID for unattended operations.","title": "ssh-host-keys.properties"},{"location": "reference/config-files.html#gpg-keysxml","text": "Stores GPG key information for backup encryption. Includes: - GPG key ID - Key email address - Optional fingerprint - Import source (system keyring or manual entry)","title": "gpg-keys.xml"},{"location": "reference/config-files.html#global-settingsxml","text": "Global application preferences and defaults. Contains: every preference of the Global Settings dialog, grouped below by the area it belongs to.","title": "global-settings.xml"},{"location": "reference/config-files.html#llmmodelsxml","text": "The atomically written JAXB registry for locally installed or referenced GGUF models. Contains: Stable model ID and display name GGUF path and compatible llama-server executable path Backend ( AUTO , CPU , METAL , or VULKAN ) Context size, CPU threads, GPU layers, and idle-unload minutes The registry contains paths and settings, not model weights or API keys. llm/models/ holds managed GGUF copies, llm/runtime/ holds independently updated native packages, and llm/run/ holds temporary process directories, logs, and owner-only generated key files. Temporary keys are removed when the sidecar stops.","title": "llm/models.xml"},{"location": "reference/config-files.html#llmruntime","text": "The regenerable native-runtime area contains immutable package directories plus small atomic state files: active-v1 points to the currently selected installation. pending-first-launch-v1 records a candidate and its rollback base until a real GGUF-backed authenticated API start succeeds. healthy-history-v1 retains at most the two newest confirmed, non-revoked installations. revoked-v1 is the durable denylist learned from verified signed indexes; a revoked package also contains .kortty-runtime-revoked . blocked-active-v1 remembers the runtime ID removed from active use by a withdrawal so the UI can explain why local AI remains blocked. pinned-runtime-v1 holds the runtime ID the user switched to with Other version… ; while present, update checks keep that version. The MLX runtime area llm/mlx/runtime/ uses a file of the same name. packages/ contains extracted verified installations, while downloads/ is temporary staging protected by the updater lock. Do not edit or delete the denylist/quarantine markers to re-enable a package. Runtime launches independently enforce them, and a compatible signed replacement must be installed instead. The entire directory is excluded from backup because packages and state can be recreated from the signed stable channel.","title": "llm/runtime/"},{"location": "reference/config-files.html#llmcataloglast-valid-catalog-v1json","text": "An atomic cache envelope containing the last model/prompt catalog payload and its detached signature. korTTY re-verifies the signature and strict schema before every cache use. If the application has no valid catalog public trust root, this file is ignored and the built-in bootstrap is used without a network refresh. The cache is regenerable and is not included in backups.","title": "llm/catalog/last-valid-catalog-v1.json"},{"location": "reference/config-files.html#ragstoresjson","text": "The atomically written, owner-readable JSON registry for knowledge stores and their sources. Contains: Store ID/name/type, local snapshot directory or Qdrant endpoint/collection, embedding model ID and vector dimensions Text, Coding, and autonomous-use assignments Per-source stable ID, canonical path, file/directory type, enabled flag, automatic/manual sync mode, size limit, include/exclude globs, .gitignore preference, content hashes, last status, file/chunk/problem counts, and last successful index time The rag/stores/ subdirectory holds regenerable index.hnsw snapshots. A v2 snapshot embeds its format version, vector dimensions, embedding model ID, hierarchical graph parameters, entry point, chunk metadata, vectors, node levels, and per-layer neighbors; a mismatch is rejected and requires a rebuild. A valid legacy single-layer v1 snapshot is rebuilt and atomically migrated when opened.","title": "rag/stores.json"},{"location": "reference/config-files.html#job-schedulerxml","text": "All JobScheduler jobs and related data. Contains: - Job name, enabled status, action type (COMMAND, SNIPPET_SCRIPT, AI_AGENT, SFTP, RSYNC_SYNC) - Schedule configuration (weekdays, times, intervals, date ranges) - Target servers or groups - Host-key pins (OpenSSH public-key material for unattended execution) - Sudo passwords for servers and groups (encrypted) - Journal entries with timestamps, exit codes, and redacted output - Journal retention settings (auto-delete entries older than 14 days by default) Security: - Host-key pinning is required by default for unattended SSH/SFTP/Rsync execution - Sudo passwords are encrypted with the master password - Journal entries are stored with KorTTY-managed secrets redacted before persistence - Archive passwords and backup encryption credentials are stored encrypted Warning If the master password is locked and a job needs SSH, sudo, API, or archive secrets, the job is blocked and a journal entry is created explaining the issue.","title": "job-scheduler.xml"},{"location": "reference/config-files.html#ai-chatsxml","text": "Saved AI conversations and chat history. Includes: - Chat title and creation timestamp - Conversation messages and responses - Associated AI profile used for the chat - Follow-up prompt history (for context) Note: AI result tabs are not automatically saved. You must explicitly save them from the AI tab using the Save button to add them to this file.","title": "ai-chats.xml"},{"location": "reference/config-files.html#snippetsxml","text": "Code snippets, scripts, and templates. Includes: - Snippet name, description, language - Code content with syntax highlighting metadata - Category/folder organization - Tags and metadata - Target system (Operating System column: Linux, macOS, Windows, etc.) - Import/export history Features: - Support for JSON/XML/YAML import/export - Plain-text script export - ZIP archives with optional password or GPG encryption - Local syntax highlighting with Monaco editor - AI-assisted editing and code generation - Persisted Mermaid diagrams (logical-structure flowchart, sequence, state, class, ER) with stable code-reference node IDs and, for selection-scoped diagrams, the covered line range - One-liner export with optional script arguments","title": "snippets.xml"},{"location": "reference/config-files.html#snippet-variablesxml","text": "Stores variable definitions for use within snippets. Includes: - Variable name, value, type - Scope (local or shared) - Default values and validation rules","title": "snippet-variables.xml"},{"location": "reference/config-files.html#masterkey","text": "Binary file containing the hashed master password. Format: PBKDF2 hash with 310,000 iterations Security: This file does not contain the actual master password—only a cryptographic hash used to verify the password you enter on startup. If this file is lost or corrupted, you must restart KorTTY and set a new master password (though you will lose access to previously stored encrypted credentials and SSH key passphrases). Warning If you forget your master password, delete master.key and credentials.xml , restart KorTTY, set a new master password, and re-enter your passwords. There is no recovery mechanism for the lost password.","title": "master.key"},{"location": "reference/config-files.html#masterautounlock","text": "Written only while auto-login is enabled: a copy of the master password, obfuscated only — not encrypted , with owner-only file permissions. Deleting it (or disabling the option) restores the normal startup prompt.","title": "master.autounlock"},{"location": "reference/config-files.html#terminal-effect-pluginsdisabled","text": "Text file listing disabled terminal-effect plugin IDs (one per line). Purpose: When you disable a terminal-effect plugin via Plugins > Terminal Effects , its ID is written to this file so it remains disabled after restart.","title": "terminal-effect-plugins.disabled"},{"location": "reference/config-files.html#coding-agents","text": "Optional user overrides for the coding-agent detection rules, one JSON file per agent ( claude-code.json , codex.json , gemini-cli.json ). Purpose: A file here replaces the bundled rule file of the same agent entirely. An invalid file is reported in the log as a coding-agents warning and the bundled rules stay in effect. The directory does not exist until you create it. See Coding agents → Custom rules .","title": "coding-agents/"},{"location": "reference/config-files.html#korttylog","text": "Application log file. Contains: - Startup messages - Connection attempts and results - Terminal session events - Configuration changes - Errors and warnings - Performance metrics (on request via JMX) Rotation: The log file grows throughout the application session. Old logs are not automatically rotated (the log file persists until KorTTY exits or you manually delete it). Tip Check this file when troubleshooting connection issues, plugin loading problems, or unexpected behavior. Common issues logged here include SSH errors, encryption failures, and import/export problems.","title": "kortty.log"},{"location": "reference/config-files.html#directories","text": "","title": "Directories"},{"location": "reference/config-files.html#history","text": "Compressed terminal session history. Format: GZIP-compressed text files, one per terminal session Naming: {session-id}_{timestamp}.history.gz (for session history from terminal logging) Purpose: Stores project/session scrollback history so reopened sessions can restore their terminal content. Access: Terminal history is loaded automatically when you open a saved connection and displayed in the terminal history search feature. Note Per-connection Terminal Logging does not write here: its generated log files go to the folder configured on the connection's Terminal Logging tab, or to ~/.kortty/terminal-logs/ when that folder is left empty.","title": "history/"},{"location": "reference/config-files.html#journals","text": "Session journals — one self-contained directory per journal (location configurable in Settings > Logging > Session Journal ). Each journal directory holds journal.xml (the curated document: metadata, AI summaries, markers, notes, screenshot references), the append-only capture log session-log.json / .xml / .yaml (JSON Lines by default) with zstd-compressed rotated parts (part size and part count are configurable per connection on the Journal tab, defaults 25 MB and 20 parts; journals from older versions keep their gzip-compressed .gz parts), the generated journal.html timeline page, and screenshots/*.png . See Session journal .","title": "journals/"},{"location": "reference/config-files.html#terminal-logs","text": "Default target folder for per-connection terminal logs when a connection's log folder field is left empty. File naming, daily rotation, compression and retention follow the connection's logging configuration.","title": "terminal-logs/"},{"location": "reference/config-files.html#plugins","text": "User-imported terminal-effect plugin JARs. Purpose: External terminal-effect plugins that you import via Plugins > Terminal Effects > Import . Security: Imported plugins are trusted Java code and are not sandboxed. Only import JARs from sources you trust. Cleanup: If you delete a plugin from this directory, it will no longer be available in KorTTY. Disabled plugins remain in this directory but are listed in terminal-effect-plugins.disabled . Warning Plugin dependencies must be shaded into the plugin JAR. Adjacent dependency JARs are not loaded automatically. See Terminal effect plugins for packaging guidelines.","title": "plugins/"},{"location": "reference/config-files.html#bundled-plugins","text": "Runtime copies of bundled exportable terminal-effect plugin JARs. Purpose: Backup and working copies of built-in plugins that can be exported to external users. Contents: The MU/TH/UR 6000 effect plugin and any other exportable bundled plugins. Auto-management: KorTTY automatically manages this directory. Users should not edit it manually.","title": "bundled-plugins/"},{"location": "reference/config-files.html#projects","text": "Project files for saving and loading connection sets. Format: KorTTY XML-based project format ( .kortty files) Naming: User-defined project names with .kortty extension (e.g., production.kortty , development.kortty ) Contains: - List of open connections/tabs at project save time - Window state (tabs, sizes, active tab) - Dashboard state - Project metadata and creation timestamp Purpose: Quickly open a pre-configured set of connections for a specific project or workflow. Usage: Save a project via File > Save Project , restore via File > Open Project or the project history menu.","title": "projects/"},{"location": "reference/config-files.html#i18n","text": "Dynamically generated language translation files. Format: Java properties files ( .properties ) Naming: messages_{language-code}.properties (e.g., messages_de.properties for German, messages_fr.properties for French) Purpose: Stores translations generated by translation APIs (Google Translate, DeepL, LibreTranslate, Microsoft Translator, or Yandex) when you choose to generate language files via Settings > Translation . Auto-refresh: When you upgrade KorTTY to a new version, generated language files are marked as outdated. Use Settings > Translation > Regenerate outdated to refresh them with any new or changed UI keys. Note Built-in languages (English, German, Italian, Spanish, Portuguese, French, Croatian, Dutch) are shipped with the application and do not use this directory. This directory is only used for dynamically generated translations.","title": "i18n/"},{"location": "reference/config-files.html#ssh-keys","text": "Optional directory for copied SSH keys. Purpose: Stores copies of SSH keys in one place for backup and migration. How to use: 1. Open Management > Manage SSH Keys... 2. Select an SSH key and click Copy to User Directory 3. The key is copied to ~/.kortty/ssh-keys/ Backup: The key files in this directory are included when you create a backup via Edit > Create Backup (the archive is AES-256- or GPG-encrypted). On import they are restored with owner-only file permissions, and the import merges: local keys are never deleted, and existing files are only replaced with Overwrite enabled. Benefits: - Centralized location for all SSH keys - Included in encrypted backups - Easy migration to new machines","title": "ssh-keys/"},{"location": "reference/config-files.html#security-summary","text": "Item Security Method Master password PBKDF2 hashing with 310,000 iterations Connection passwords AES-256-GCM encryption SSH key passphrases AES-256-GCM encryption Interactive Terminal/SFTP/Mosh host keys Normalized host:port TOFU with OpenSSH SHA-256 fingerprints and fail-closed change detection; the optional per-connection/group/global opt-out relaxes only unknown-key prompting to accept-new Credentials (username/password) AES-256-GCM encryption JobScheduler sudo passwords AES-256-GCM encryption JobScheduler journal entries Redacted secrets before persistence Backup files Password-protected ZIP or GPG encryption API keys (AI profiles) AES-256-GCM encryption with master password Terminal effect plugins Not encrypted; trusted local code, not sandboxed","title": "Security summary"},{"location": "reference/config-files.html#file-locations-by-platform","text": "All files are stored in the same ~/.kortty/ directory across platforms: macOS: /Users/{username}/.kortty/ Windows: C:\\Users\\{username}\\.kortty\\ (or %USERPROFILE%\\.kortty\\ ) Linux: /home/{username}/.kortty/ (or $HOME/.kortty/ )","title": "File locations by platform"},{"location": "reference/config-files.html#backup-and-recovery","text": "When you create a backup via Edit > Create Backup , the following configuration is included: All .xml configuration files (connections, credentials, SSH key references and passphrases, GPG keys, global settings, JobScheduler, snippets, snippet variables, AI chats) master.key projects/ directory ssh-keys/ directory — copied SSH key files (restored with owner-only permissions; imports merge and never delete local keys) ssh-host-keys.properties interactive host-key trust store (not its transient .lock file) llm/models.xml local-model registrations rag/stores.json knowledge-store/source metadata Managed GGUF weights, native runtime packages, temporary sidecar data, original source documents, and HNSW snapshots are excluded because they are large or regenerable. The history/ and i18n/ directories are also not part of a backup, and neither is the auto-login file master.autounlock . The backup is encrypted (password-protected ZIP or GPG) and saved to a location you specify. Tip Backups are the recommended way to migrate KorTTY to a new machine or to recover from data loss. Create a backup regularly and store it in a safe location.","title": "Backup and recovery"},{"location": "reference/config-files.html#accessing-configuration-files","text": "You can edit KorTTY configuration directly by: Closing KorTTY completely Opening ~/.kortty/ in your file manager or terminal Editing the XML or JSON file with a text editor Restarting KorTTY to load the changes Warning Editing XML/JSON files directly can corrupt your data if not done carefully. Never edit a binary index.hnsw snapshot. Always create a backup before manual editing. For most configuration tasks, use the KorTTY UI instead—it handles encryption, validation, and file format correctly.","title": "Accessing configuration files"},{"location": "reference/config-files.html#troubleshooting","text": "Configuration file not found: - KorTTY creates the ~/.kortty/ directory and all necessary subdirectories on first launch. - If a configuration file is missing, KorTTY uses sensible defaults and creates the file on next save. Encryption errors: - If you forget your master password, you must delete master.key and set a new password. Previously encrypted data will be inaccessible. Corrupted XML files: - If a .xml file becomes corrupted, restore from a backup or delete the file. KorTTY will recreate it with defaults on next save. Knowledge-store registry or snapshot cannot be read: - Restore rag/stores.json from a backup or recreate the store in the AI Manager. Delete only the affected regenerable index.hnsw , then choose Update now to rebuild it from the configured source files. Interactive SSH host key changed: - Do not reconnect until you have verified the new OpenSSH SHA-256 fingerprint with the server administrator and ruled out DNS, routing, or man-in-the-middle problems. KorTTY deliberately blocks the mismatch without retrying or replacing the stored pin. Plugin loading issues: - Check kortty.log for error messages related to plugin loading (e.g., duplicate IDs, missing services, class loading errors). - Ensure the plugin JAR is in ~/.kortty/plugins/ and contains META-INF/services/de.kortty.plugin.terminaleffects.TerminalEffectPlugin . - Use Plugins > Terminal Effects > Reload to refresh the plugin list. Log file size: - kortty.log grows during the application session. It is not automatically rotated. You can safely delete it while KorTTY is closed. Master password recovery: - There is no recovery for a forgotten master password. If you lose it, delete master.key and credentials.xml , restart KorTTY, and set a new password.","title": "Troubleshooting"},{"location": "reference/control-api.html","text": "Control API korTTY can open a local control socket so that a script or a coding agent running on the same computer can list your windows, read what a terminal pane is showing and type into it. It is the machine-facing twin of what you do with the mouse and the keyboard: nothing it can do is something you could not do yourself in an open pane. It is off by default and has to be switched on per installation under Settings › Terminal › Control API . Nothing is reachable over the network at any point, and no other user of the same computer can connect. Understand what you are enabling While the Control API is on, any program running under your user account on this computer can read every open pane — local shells and SSH sessions alike — and type anything into them, including pressing Enter . That is the same power as sitting at your keyboard. Switch it on when you want a coding agent or a script to drive korTTY, and off again when you do not. Switching it on Open Settings › Terminal and scroll to Control API . Tick Allow a local program to read and control this korTTY . Save. The status line underneath tells you what happened right away — no restart is needed. Status line Meaning Status: off The checkbox is unticked, or korTTY has shut the listener down. Status: blocked by your organization Enterprise policy denies the control-api feature; the checkbox is locked. Status: listening on … A listener is up and endpoint.json has been written. The text names the socket or the loopback port. Status: could not start — … The listener refused to start. The most common reason is that another korTTY already owns the socket; the text says which. Unticking the box stops the listener and deletes the socket immediately. Where the endpoint lives Everything the API owns lives in its own directory, ~/.kortty/control/ , created with owner-only permissions ( 0700 ) before anything binds. The directory, not the socket file, is the real protection: a socket inode is created with the process umask, which on most systems is world-readable. Platform Listener Linux, macOS A unix domain socket at ~/.kortty/control/control.sock Windows TCP on 127.0.0.1 with an operating-system-assigned port A client finds the endpoint by reading ~/.kortty/control/endpoint.json (mode 0600 ): { \"transport\" : \"unix\" , \"path\" : \"/home/you/.kortty/control/control.sock\" , \"host\" : null , \"port\" : 0 , \"token\" : \"kQ7f…\" , \"pid\" : 38211 , \"app_version\" : \"…\" , \"protocol_version\" : 1 , \"instance_id\" : \"6f0c2a1e-…\" , \"started_at_millis\" : 1736000000000 } The file is written last , after the listener is bound, so its existence always implies a reachable endpoint and a readable token. korTTY deletes it, and the socket, when the API is switched off and when the application exits. The token is 32 random bytes, regenerated on every start, and is required on both transports. On Linux and macOS the owner-only directory is the primary control and the token is defence in depth — against a loosened umask, a shared or network-mounted home directory, or a backup that copied the directory somewhere readable. On Windows it is the only control. It is never logged, never echoed in an error message, never passed on a command line and never shown in a notification: the legitimate client reads it from the file, which is the whole point. Speaking the protocol The protocol is newline-delimited JSON-RPC 2.0 — exactly one JSON object per line, UTF-8, at most 1 MiB per line. The first request on a connection must be auth ; anything else, and the connection is closed. One failed token closes the connection too. → {\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"auth\",\"params\":{\"token\":\"kQ7f…\",\"client\":\"my-script\"}} ← {\"jsonrpc\":\"2.0\",\"id\":1,\"result\":{\"api\":\"kortty-control\",\"protocol_version\":1,\"transport\":\"unix\",\"ids_survive_restart\":false,\"capabilities\":[\"events\",\"split\",\"agent_start\",\"pane_resolve\"]}} Requests on one connection are executed one at a time : the next line is read only after the previous answer has been queued. A client that needs a long wait and concurrent calls opens a second connection — up to eight at once. Rather than duplicating the method list here, ask korTTY: api.schema returns the full machine-readable surface — every method with its parameters, result shape, errors, CLI equivalent and a worked example, plus the key vocabulary, the error table and the limits. The parameters, results, errors and limits are generated from the same declarations the dispatcher registers, so they cannot drift from the implementation; each cli line is a command the shipped kortty-cli is tested to accept for that very method. What the methods do Group Methods Purpose api ping , auth , api.schema Handshake and discovery. events events.subscribe , events.unsubscribe Push notifications when a coding agent changes state. window / tab window.list , tab.list , tab.focus Enumerate the windows and tabs, and bring one to the front. pane pane.list , pane.current , pane.get , pane.resolve , pane.focus , pane.read , pane.send_text , pane.run , pane.send_keys , pane.wait_output , pane.split , pane.close Read a pane, type into it, wait for output, split a local shell or close a split. agent agent.list , agent.get , agent.explain , agent.prompt , agent.send_keys , agent.wait , agent.rename , agent.start Work with the coding agents korTTY has detected — see Coding agents . notification notification.show Raise one desktop notification. tab.create , tab.close and tab.rename are reserved : they answer a definite \"not implemented in this version\" rather than an unknown-method error, and they are listed as reserved in api.schema , so a client can tell \"korTTY will never do this for you\" apart from \"you spelled it wrong\". Addressing a pane Id Shape Lifetime Window w1 The window's life. It is a counter, not a position, so closing another window never renumbers it. Tab t + a UUID The tab instance. Pane p + a short hex string The pane's life. Qualified w1:t9f3a…:p1a2b3c4d As above. Every parameter called pane also accepts a bare tab id, meaning that tab's focused pane, and the alias @focused , meaning the pane you are looking at. A bare window id where a pane is expected is refused as ambiguous rather than guessed at — a window usually holds several panes, and picking one silently is how a script types into the wrong terminal. No id survives a restart Ids are minted per korTTY run. Every reply that carries ids also carries the instance of the running korTTY, and every mutating method accepts an optional instance parameter: when it does not match, the call fails with stale_instance instead of writing into whatever pane inherited the id. Re-enumerate whenever instance changes. Reading a pane pane.read has three modes. visible returns the live screen, including the alternate buffer, right-trimmed. recent returns the scrollback plus the screen, read under a single buffer lock, newest lines rows. detection returns what korTTY's coding-agent detector last published for the pane — the detector is never re-run for a request. pane.wait_output blocks until a regular expression or a literal string appears, polling the pane. Output that appears and scrolls away inside a single poll window can be missed in visible mode; the default recent mode also searches the scrollback and does not have that gap. Splitting pane.split works only on a pane whose tab is a local shell. Anything else is refused with unsupported . The new shell is started without any dialog, so a script never ends up waiting on a window it cannot see; a split that would need a new connection needs a human, and is not offered. pane.close refuses a tab's last pane. Closing it would leave an empty terminal area inside a still-open tab, which is exactly why korTTY's own \"Close split\" menu item is greyed out in that case. Close the tab yourself. What it cannot do The threat model is a local process running as you, while the API is on. Such a process can enumerate your windows, read any pane, type into any pane including submitting commands, split a local shell, close a split pane, drive a detected coding agent, and raise a notification. It cannot : reach anything before you tick the box, or at all when enterprise policy denies the feature; open a new tab, a new window or a connection of any kind — there is no way to reach a server that is not already open; split anything other than a same-server local shell; close a tab, close korTTY's last pane, or rename a tab; read or write your settings, your credentials, the vault, your SSH keys or the master password; reach anything over the network — the listener is a unix socket or loopback, never anything else, and there is no setting that can widen it; act without leaving an audit line, or without a desktop notification on its first write of the run. Writes are not restricted to local shells — on purpose Typing is allowed into remote panes too. Restricting it would break answering a coding agent that runs over SSH, and it would not be a real boundary anyway: a caller who can reach the socket can simply run ssh in a local pane instead. The honest controls are the default-off switch, the policy deny, the audit line and the notification — not a filter that looks like a boundary and is not one. Audit and visibility Every action the API takes writes exactly one line to korTTY's log, carrying byte counts and shapes only — bytes=28 bracketed=false submitted=true , keys=2 , orientation=vertical — never terminal text. The line goes to korTTY's log and only there — the API writes nothing into a tab's session journal. The first time a program types into a pane during a korTTY run you get one desktop notification. One, not one per keystroke: the point is that a takeover is never silent, not that it becomes noise you learn to dismiss. korTTY also logs a warning naming the endpoint whenever the listener starts. Actions a coding agent performs through the API are logged distinguishably from the ones you trigger in the Coding Agents panel, so reading the log tells you which was which. Limits Limit Value Concurrent connections 8; a ninth is refused and closed Line size, in and out 1 MiB Result size 512 KiB, truncated with truncated: true Unauthenticated connection closed after 5 s Longest wait 10 minutes; a larger request is clamped and says so Event queue per subscription 256 frames; overflow drops the oldest and reports how many pane.read rows 10 000 A client that stops reading is disconnected rather than buffered. Enterprise policy An administrator can deny the Control API outright with the control-api feature in kortty-policy.toml : [[rule]] name = \"no-local-automation\" [rule.features] control-api = \"deny\" The checkbox is then locked with the \"Managed by your organization\" hint, the setting is forced off both when it is loaded and when it is saved, and the listener answers blocked_by_policy immediately — the gate is re-checked on every request, not only when a connection is accepted, so a policy change stops service before teardown finishes. See Policy configuration . Note Writing control-api = \"allow\" also locks the checkbox — in the on position. A policy file that mentions a setting takes it over, whichever way it decides it. Leave the key out entirely to leave the choice with the user. The client The kortty-cli command that ships beside korTTY speaks this protocol and handles endpoint discovery, authentication and exit codes for you. See Control CLI .","title": "Control API"},{"location": "reference/control-api.html#control-api","text": "korTTY can open a local control socket so that a script or a coding agent running on the same computer can list your windows, read what a terminal pane is showing and type into it. It is the machine-facing twin of what you do with the mouse and the keyboard: nothing it can do is something you could not do yourself in an open pane. It is off by default and has to be switched on per installation under Settings › Terminal › Control API . Nothing is reachable over the network at any point, and no other user of the same computer can connect. Understand what you are enabling While the Control API is on, any program running under your user account on this computer can read every open pane — local shells and SSH sessions alike — and type anything into them, including pressing Enter . That is the same power as sitting at your keyboard. Switch it on when you want a coding agent or a script to drive korTTY, and off again when you do not.","title": "Control API"},{"location": "reference/control-api.html#switching-it-on","text": "Open Settings › Terminal and scroll to Control API . Tick Allow a local program to read and control this korTTY . Save. The status line underneath tells you what happened right away — no restart is needed. Status line Meaning Status: off The checkbox is unticked, or korTTY has shut the listener down. Status: blocked by your organization Enterprise policy denies the control-api feature; the checkbox is locked. Status: listening on … A listener is up and endpoint.json has been written. The text names the socket or the loopback port. Status: could not start — … The listener refused to start. The most common reason is that another korTTY already owns the socket; the text says which. Unticking the box stops the listener and deletes the socket immediately.","title": "Switching it on"},{"location": "reference/control-api.html#where-the-endpoint-lives","text": "Everything the API owns lives in its own directory, ~/.kortty/control/ , created with owner-only permissions ( 0700 ) before anything binds. The directory, not the socket file, is the real protection: a socket inode is created with the process umask, which on most systems is world-readable. Platform Listener Linux, macOS A unix domain socket at ~/.kortty/control/control.sock Windows TCP on 127.0.0.1 with an operating-system-assigned port A client finds the endpoint by reading ~/.kortty/control/endpoint.json (mode 0600 ): { \"transport\" : \"unix\" , \"path\" : \"/home/you/.kortty/control/control.sock\" , \"host\" : null , \"port\" : 0 , \"token\" : \"kQ7f…\" , \"pid\" : 38211 , \"app_version\" : \"…\" , \"protocol_version\" : 1 , \"instance_id\" : \"6f0c2a1e-…\" , \"started_at_millis\" : 1736000000000 } The file is written last , after the listener is bound, so its existence always implies a reachable endpoint and a readable token. korTTY deletes it, and the socket, when the API is switched off and when the application exits. The token is 32 random bytes, regenerated on every start, and is required on both transports. On Linux and macOS the owner-only directory is the primary control and the token is defence in depth — against a loosened umask, a shared or network-mounted home directory, or a backup that copied the directory somewhere readable. On Windows it is the only control. It is never logged, never echoed in an error message, never passed on a command line and never shown in a notification: the legitimate client reads it from the file, which is the whole point.","title": "Where the endpoint lives"},{"location": "reference/control-api.html#speaking-the-protocol","text": "The protocol is newline-delimited JSON-RPC 2.0 — exactly one JSON object per line, UTF-8, at most 1 MiB per line. The first request on a connection must be auth ; anything else, and the connection is closed. One failed token closes the connection too. → {\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"auth\",\"params\":{\"token\":\"kQ7f…\",\"client\":\"my-script\"}} ← {\"jsonrpc\":\"2.0\",\"id\":1,\"result\":{\"api\":\"kortty-control\",\"protocol_version\":1,\"transport\":\"unix\",\"ids_survive_restart\":false,\"capabilities\":[\"events\",\"split\",\"agent_start\",\"pane_resolve\"]}} Requests on one connection are executed one at a time : the next line is read only after the previous answer has been queued. A client that needs a long wait and concurrent calls opens a second connection — up to eight at once. Rather than duplicating the method list here, ask korTTY: api.schema returns the full machine-readable surface — every method with its parameters, result shape, errors, CLI equivalent and a worked example, plus the key vocabulary, the error table and the limits. The parameters, results, errors and limits are generated from the same declarations the dispatcher registers, so they cannot drift from the implementation; each cli line is a command the shipped kortty-cli is tested to accept for that very method.","title": "Speaking the protocol"},{"location": "reference/control-api.html#what-the-methods-do","text": "Group Methods Purpose api ping , auth , api.schema Handshake and discovery. events events.subscribe , events.unsubscribe Push notifications when a coding agent changes state. window / tab window.list , tab.list , tab.focus Enumerate the windows and tabs, and bring one to the front. pane pane.list , pane.current , pane.get , pane.resolve , pane.focus , pane.read , pane.send_text , pane.run , pane.send_keys , pane.wait_output , pane.split , pane.close Read a pane, type into it, wait for output, split a local shell or close a split. agent agent.list , agent.get , agent.explain , agent.prompt , agent.send_keys , agent.wait , agent.rename , agent.start Work with the coding agents korTTY has detected — see Coding agents . notification notification.show Raise one desktop notification. tab.create , tab.close and tab.rename are reserved : they answer a definite \"not implemented in this version\" rather than an unknown-method error, and they are listed as reserved in api.schema , so a client can tell \"korTTY will never do this for you\" apart from \"you spelled it wrong\".","title": "What the methods do"},{"location": "reference/control-api.html#addressing-a-pane","text": "Id Shape Lifetime Window w1 The window's life. It is a counter, not a position, so closing another window never renumbers it. Tab t + a UUID The tab instance. Pane p + a short hex string The pane's life. Qualified w1:t9f3a…:p1a2b3c4d As above. Every parameter called pane also accepts a bare tab id, meaning that tab's focused pane, and the alias @focused , meaning the pane you are looking at. A bare window id where a pane is expected is refused as ambiguous rather than guessed at — a window usually holds several panes, and picking one silently is how a script types into the wrong terminal. No id survives a restart Ids are minted per korTTY run. Every reply that carries ids also carries the instance of the running korTTY, and every mutating method accepts an optional instance parameter: when it does not match, the call fails with stale_instance instead of writing into whatever pane inherited the id. Re-enumerate whenever instance changes.","title": "Addressing a pane"},{"location": "reference/control-api.html#reading-a-pane","text": "pane.read has three modes. visible returns the live screen, including the alternate buffer, right-trimmed. recent returns the scrollback plus the screen, read under a single buffer lock, newest lines rows. detection returns what korTTY's coding-agent detector last published for the pane — the detector is never re-run for a request. pane.wait_output blocks until a regular expression or a literal string appears, polling the pane. Output that appears and scrolls away inside a single poll window can be missed in visible mode; the default recent mode also searches the scrollback and does not have that gap.","title": "Reading a pane"},{"location": "reference/control-api.html#splitting","text": "pane.split works only on a pane whose tab is a local shell. Anything else is refused with unsupported . The new shell is started without any dialog, so a script never ends up waiting on a window it cannot see; a split that would need a new connection needs a human, and is not offered. pane.close refuses a tab's last pane. Closing it would leave an empty terminal area inside a still-open tab, which is exactly why korTTY's own \"Close split\" menu item is greyed out in that case. Close the tab yourself.","title": "Splitting"},{"location": "reference/control-api.html#what-it-cannot-do","text": "The threat model is a local process running as you, while the API is on. Such a process can enumerate your windows, read any pane, type into any pane including submitting commands, split a local shell, close a split pane, drive a detected coding agent, and raise a notification. It cannot : reach anything before you tick the box, or at all when enterprise policy denies the feature; open a new tab, a new window or a connection of any kind — there is no way to reach a server that is not already open; split anything other than a same-server local shell; close a tab, close korTTY's last pane, or rename a tab; read or write your settings, your credentials, the vault, your SSH keys or the master password; reach anything over the network — the listener is a unix socket or loopback, never anything else, and there is no setting that can widen it; act without leaving an audit line, or without a desktop notification on its first write of the run. Writes are not restricted to local shells — on purpose Typing is allowed into remote panes too. Restricting it would break answering a coding agent that runs over SSH, and it would not be a real boundary anyway: a caller who can reach the socket can simply run ssh in a local pane instead. The honest controls are the default-off switch, the policy deny, the audit line and the notification — not a filter that looks like a boundary and is not one.","title": "What it cannot do"},{"location": "reference/control-api.html#audit-and-visibility","text": "Every action the API takes writes exactly one line to korTTY's log, carrying byte counts and shapes only — bytes=28 bracketed=false submitted=true , keys=2 , orientation=vertical — never terminal text. The line goes to korTTY's log and only there — the API writes nothing into a tab's session journal. The first time a program types into a pane during a korTTY run you get one desktop notification. One, not one per keystroke: the point is that a takeover is never silent, not that it becomes noise you learn to dismiss. korTTY also logs a warning naming the endpoint whenever the listener starts. Actions a coding agent performs through the API are logged distinguishably from the ones you trigger in the Coding Agents panel, so reading the log tells you which was which.","title": "Audit and visibility"},{"location": "reference/control-api.html#limits","text": "Limit Value Concurrent connections 8; a ninth is refused and closed Line size, in and out 1 MiB Result size 512 KiB, truncated with truncated: true Unauthenticated connection closed after 5 s Longest wait 10 minutes; a larger request is clamped and says so Event queue per subscription 256 frames; overflow drops the oldest and reports how many pane.read rows 10 000 A client that stops reading is disconnected rather than buffered.","title": "Limits"},{"location": "reference/control-api.html#enterprise-policy","text": "An administrator can deny the Control API outright with the control-api feature in kortty-policy.toml : [[rule]] name = \"no-local-automation\" [rule.features] control-api = \"deny\" The checkbox is then locked with the \"Managed by your organization\" hint, the setting is forced off both when it is loaded and when it is saved, and the listener answers blocked_by_policy immediately — the gate is re-checked on every request, not only when a connection is accepted, so a policy change stops service before teardown finishes. See Policy configuration . Note Writing control-api = \"allow\" also locks the checkbox — in the on position. A policy file that mentions a setting takes it over, whichever way it decides it. Leave the key out entirely to leave the choice with the user.","title": "Enterprise policy"},{"location": "reference/control-api.html#the-client","text": "The kortty-cli command that ships beside korTTY speaks this protocol and handles endpoint discovery, authentication and exit codes for you. See Control CLI .","title": "The client"},{"location": "reference/enterprise-policy.html","text": "Policy configuration In managed environments an administrator can restrict or preconfigure korTTY through a single TOML file, kortty-policy.toml , placed in the installation directory. Users cannot change or bypass it: the file is only read from the admin-writable installation folder, locked settings appear grayed out with a \"Managed by your organization\" hint, and hand-editing global-settings.xml is undone on the next load. This chapter explains where the file lives, how rules target users, groups and servers, and documents every available parameter with examples. Note Without a kortty-policy.toml nothing changes — korTTY behaves exactly as before. The shipped policy/kortty-policy.toml.example is a fully commented template with everything disabled. File location and security model korTTY loads the policy exclusively from the policy/ folder of its installation directory — the folder that also holds the application jar — and never from ~/.kortty/ or any other user-writable location. A ready-to-copy template ships as policy/kortty-policy.toml.example ; copy it to kortty-policy.toml in the same folder and restart korTTY (there is no hot reload). Platform Policy file location macOS /Applications/KorTTY.app/Contents/app/policy/kortty-policy.toml Windows C:\\Program Files\\KorTTY\\app\\policy\\kortty-policy.toml Linux (deb/rpm) /opt/kortty/lib/app/policy/kortty-policy.toml The enforcement model relies on the operating system's file permissions: the installation directory must be writable only by administrators, which is the default for the locations above. korTTY additionally logs a warning when the active policy file is writable by the current user. During development (never in a packaged installation) a policy can be tested with -Dkortty.policy.file=/path/to/policy.toml . If the file exists but cannot be parsed or contains an invalid value, korTTY starts in a fail-safe lockdown: every policy-controllable feature is denied, no server connection is allowed, and a startup dialog names the file and the exact error position. A typo can therefore never silently disable enforcement. One invalid rule rejects the entire file; unknown keys only produce log warnings, so a policy written for a newer korTTY does not lock out users of an older version. Users, groups and rule precedence Rules target the current OS login name (matched lowercase). Group membership comes from two sources at once: groups defined in the policy's [groups] table, and the user's OS-level group memberships. On domain-joined Windows machines the OS groups include Active Directory groups, so rules can target AD groups directly — both fully qualified ( ACME\\Operations ) and as the bare name ( operations ) — without any directory server configuration. Each [[rule]] block optionally names users and/or groups ; a rule naming neither applies to every user. Per setting, the most specific tier that sets it wins: user beats group beats everyone . That is the familiar group-policy pattern — lock everything down in a baseline rule for everyone, then relax individual settings for a trusted group. When several rules of the same tier set the same key, the most restrictive value applies ( deny over read-only over confirm over allow ); for server rules, a connection must pass every applicable restriction of the winning tier. The order of rules in the file has no significance. [meta] schema-version = 1 organization = \"ACME Corp\" [groups] devs = [ \"alice\" , \"bob\" ] [[rule]] # applies to ALL users name = \"company-baseline\" [rule.features] ai-agent = \"deny\" [[rule]] # group tier: relaxes the baseline for ops name = \"ops-exception\" groups = [ \"ops\" , \"ACME \\\\ Operations\" ] # policy group OR OS/AD group [rule.features] ai-agent = \"allow\" ai-agent-execution = \"confirm\" [[rule]] # user tier: beats eve's group users = [ \"eve\" ] [rule.features] ai-agent = \"deny\" Server access control The [rule.servers] table restricts which servers a user may connect to — as an allow-list ( mode = \"allow\" : only listed servers are reachable) or a deny-list ( mode = \"deny\" : listed servers are blocked). The restriction is enforced centrally for every connection path: saved connections, QuickConnect, session restore, SFTP, teamwork-shared connections, AI swarm targets and scheduled jobs, including the jump host of a connection. Blocked connections stay visible in the connection manager but are grayed out with a lock marker, and any connect attempt shows a clear policy message. Patterns match the host string exactly as configured in the connection — korTTY never resolves DNS for policy checks, so host names and IP addresses are separate namespaces: if a server is reachable both ways, list both. Pattern form Example Matches Exact host name db01.acme.com that host, case-insensitive, any port Host-name glob *.prod.acme.com , web-??.acme.com * = any characters, ? = one character Host with port vault.acme.com:22 only connections to that port Single IP address 192.168.10.42 , 2001:db8::1 that address (IPv4 or IPv6) IP with port 192.168.10.42:22 , [2001:db8::1]:22 IPv6 ports require the bracket form CIDR network 10.99.0.0/16 , 2001:db8::/32 every address in the network IP range 10.20.0.100-10.20.0.199 inclusive from–to range [[rule]] [rule.servers] mode = \"deny\" hosts = [ \"*.prod.acme.com\" , \"vault.acme.com:22\" , \"192.168.10.42\" , \"10.99.0.0/16\" , \"10.20.0.100-10.20.0.199\" ] Parameter reference [meta] Key Type Values Required Effect schema-version integer 1 yes Rejected (lockdown) when this korTTY does not understand the version organization string free text no Shown in every \"Managed by your organization\" hint and dialog [groups] Key Type Effect <group-name> array of user names Defines a policy group; rules referencing the name target its members. OS/AD groups match automatically and need no entry here [[rule]] scope Key Type Effect name string Optional label used in log messages users array of user names Targets the listed OS login names (user tier) groups array of group names Targets members of policy groups and/or OS/AD groups (group tier); a rule with neither users nor groups applies to everyone [rule.servers] Key Type Values Effect mode string allow , deny Allow-list or deny-list semantics for hosts hosts array of patterns see table above Non-empty list of server patterns [rule.features] Key Type Values Restricts ai string allow , deny Master switch: deny disables every AI capability at once ai-agent string allow , deny AI Agent (menu, terminal context menu, keyboard shortcut, headless job runs) ai-chat string allow , deny AI chat, Saved Chats and the terminal-selection AI actions ai-swarm string allow , deny AI Swarm, including scheduled swarm jobs ai-planning string allow , deny AI Planning teamwork string allow , deny Teamwork shared-connections sync (service is not started, menu locked) plugins string allow , deny Plugin loading and the Plugins menu (e.g. terminal effects) session-journal string allow , deny The session journal : capture, journal bar, manager, viewer and exports. Not chained to ai — with AI denied the journal still records raw activity control-api string allow , deny The control API and its kortty-cli client: deny stops the listener, locks the Settings checkbox and forces the setting off. Not chained to ai — the API is local automation, not an AI capability ai-agent-execution string allow , confirm , read-only confirm forces interactive approval of every mutating command set and defeats the auto-approve option; read-only lets the agent plan and chat but never execute commands Naming a feature takes it over, whichever way you decide it A policy file that mentions a key locks the corresponding control, in the position the policy chose. Writing control-api = \"allow\" therefore locks the checkbox on — users can no longer switch the API off themselves. Leave the key out entirely to leave the choice with the user; write deny to take it away. [rule.security] Key Type Values Effect require-master-password boolean true Forces the master-password gate at startup; the setting is locked enforce-host-key-check boolean true SSH host key verification cannot be disabled anywhere — globally, per group or per connection allow-telemetry boolean false Forbids anonymous usage statistics allow-terminal-recording boolean false Forbids terminal session recording, including the session-level toggle clipboard-mode string system , internal internal confines korTTY to its own in-memory clipboard — see below [rule.teamwork] , [rule.snippets] , [rule.ai-profiles] Key Type Values Effect allow-custom-sources boolean false Users cannot add teamwork sources; only [[teamwork-source]] entries remain allow-custom-script-headers boolean false Users cannot create script headers; only [[script-header]] entries remain allow-create boolean false Users cannot create AI profiles (buttons and wizard are locked) allow-edit boolean false Users cannot edit their existing AI profiles either allow-internet boolean false Forbids every AI internet-access mode — see below allow-internet = false cuts AI web access at three levels The internet access dropdown of an AI profile selects a web-search or MCP browsing backend. Forbidding it is enforced in three places, because a single one would leave a gap: Stored settings — every profile's mode is reset to Disabled whenever the settings are loaded or saved, so a value from before the policy arrived does not survive, and neither does a hand-edit of global-settings.xml . The interface — the dropdown is locked with the \"managed by your organization\" hint in both the AI Manager and Settings → AI . Every AI request — korTTY refuses to build a service for a profile whose mode is enabled, which closes the window between two clamps. The request fails with a policy message rather than quietly answering without the web tool the profile asked for. This is about the search backends only. It does not stop a cloud AI profile from reaching its own provider — for that, deny the ai feature or provision [[ai-profile]] entries that point at an internal endpoint. [rule.ai-runtime] Key Type Values Effect allow-runtime-downloads boolean false No llama.cpp/MLX runtime downloads or update checks allow-model-downloads boolean false The Hugging Face model browser and downloads are disabled allow-user-models boolean false Only admin-provisioned [[ai-runtime.model]] entries can be loaded by embedded AI profiles [rule.updates] Key Type Values Effect enabled boolean false Disables the automatic update check and the manual check in the About dialog feed-url string http(s) URL Update checks query this endpoint instead of GitHub; it must return the same JSON shape as the GitHub releases/latest API ( tag_name , assets[] with name and browser_download_url ) [rule.terminal] Key Type Values Effect load-into-snippet-editor string allow , read-only , deny read-only keeps loading remote files into the snippet editor but forbids writing back to the target system; deny removes the feature entirely [rule.logging] korTTY rotates its log daily ( kortty.YYYY-MM-DD.log ); this table controls where the files go, how long they live, how they are formatted and how rotation is capped. Directory and retention are forced into the corresponding (then locked) user settings; format, compression and the rotation caps have no user setting and act directly. The configuration is applied before the very first log line of a start, so even startup logging follows the admin's scheme. Key Type Values Effect directory string absolute or ~/ -relative path Log directory (default ~/.kortty/logs ); the setting is locked retention-days integer 0 = keep forever Rotated logs older than N days are deleted; the setting is locked compress boolean default true Gzip rotated logs after one day format string text , json json writes one structured JSON object per event (logback JSON encoder — convenient for SIEM/central log collection) rotation-max-files integer 0 = unlimited Keep at most N rotated daily files rotation-total-size-mb integer 0 = uncapped Cap the total size of all rotated files (oldest deleted first) [[rule]] [rule.logging] directory = \"/var/log/kortty\" retention-days = 30 compress = true format = \"json\" rotation-max-files = 14 rotation-total-size-mb = 512 Note When several same-tier rules configure logging, each key resolves separately: shorter retention and tighter caps win, compression on wins, json wins over text . In practice, put the logging configuration into a single all-users rule. The chosen directory must be writable by the user running korTTY. [rule.session-journal] Mandates for the session journal . Forced values lock the corresponding controls in the journal options, the settings dialog and the connection editor with a \"managed by your organization\" hint. Key Type Values Effect enforced boolean true A journal is written for every connection, regardless of the per-connection setting; users cannot stop it and the enable switch is locked on log-format string json (default), xml , yaml Forces the capture-log format for new journals ai-max-lines integer 0 = context fill Forces the AI evaluation window (max terminal lines per summary) storage-path string absolute path Forces the journal storage directory; the setting is locked allow-rename boolean false Journals cannot be renamed in the manager allow-delete boolean false Journals cannot be deleted in the manager name-template string template Initial journal title, with {connection} , {host} , {user} , {date} and {time} placeholders ai-title boolean true The closing AI title is generated regardless of the user setting ai-screenshot-analysis boolean true / false true forces the AI screenshot analysis on, false forbids it — including the manual per-screenshot run; the journal option is locked either way ai-ask boolean false Forbids on-demand AI over journal content: the viewer's Q&A panel and the manager's cross-journal AI search disappear; AI summaries are unaffected max-log-parts integer ≥ 1 Caps the number of rotated capture-log parts per journal; the effective limit is the minimum of this cap and the per-connection setting, and the connection editor's spinner is clamped to it [[rule]] [rule.features] session-journal = \"allow\" [rule.session-journal] enforced = true log-format = \"json\" storage-path = \"/srv/audit/kortty-journals\" allow-delete = false name-template = \"{connection} {date} {time} ({user})\" Note enforced mandates capture, not AI: with AI denied or unavailable the enforced journal records raw activity entries. When several same-tier rules configure the journal, enforced and ai-title resolve to true if any rule sets them, allow-rename / allow-delete to false if any rule forbids them, ai-screenshot-analysis and ai-ask to off if any rule switches them off, the line cap resolves to the tighter value ( 0 counts as unlimited), and max-log-parts resolves to the lower cap. [[rule.session-journal.replace]] Automatic search-and-replace in every journal — the way to keep a whole category of secret out of the transcript, instead of relying on the user to notice it. Each entry is one rule, and a rule may use a regular expression. Key Type Default Effect pattern string required The text to find. With regex enabled it is a regular expression replacement string *** The text that replaces each match. In a regex rule $1 inserts a captured group regex boolean false Treats pattern as a regular expression ignore-case boolean false Matches every casing label string – Description for the administrator; korTTY only counts the rules in its UI hint [[rule]] [rule.session-journal] enforced = true [[rule.session-journal.replace]] pattern = \"AKIA[0-9A-Z]{16}\" replacement = \"***AWS-ACCESS-KEY***\" regex = true label = \"AWS access keys\" [[rule.session-journal.replace]] pattern = \"(?i)bearer \\\\ s+[A-Za-z0-9._-]{20,}\" replacement = \"Bearer ***\" regex = true label = \"Bearer tokens\" [[rule.session-journal.replace]] pattern = \"vpn.internal.acme.corp\" replacement = \"<internal-host>\" ignore-case = true The rules run on the capture thread, before a line is written , so a matching text never reaches the log file at all, and they are applied to AI summaries and user notes as well. Unlike the other keys, replacement rules are merged across every matching rule and every tier rather than the highest tier winning: more redaction is the more restrictive outcome, so a user- or group-specific rule can only add patterns, never switch the organization-wide ones off. Duplicate entries are collapsed. Warning A pattern that is not a valid regular expression is a policy error , not a warning — the file is rejected. A rule that silently matches nothing is worse than one the administrator has to fix, because the redaction would appear to be in place. Note Only journals written while the rule is in force are covered; existing journals are not rewritten retroactively. Users can clean those up with the viewer's Search & replace. Admin-provided objects These top-level tables define objects that appear read-only for every user, marked \"Provided by your organization\". They are rebuilt from the policy on every start and are never written into the user's configuration files — removing them from the policy removes them from korTTY. Table Keys Notes [[script-header]] name , content Immutable script header in the snippet system's Script-Header category [[ai-profile]] id , name , provider , endpoint , model , api-key-encrypted id must start with policy- ; provider is one of anthropic , openai-compatible , lm-studio , embedded-llama , embedded-mlx (embedded providers read model as the local model id) [[ai-runtime.model]] name , runtime , source runtime is llama or mlx ; source is an absolute local/UNC path, or for GGUF models an http(s) URL that korTTY downloads once at startup [[teamwork-source]] name , type , url type is git or shared-file ; injected as a read-only teamwork source Internal clipboard mode With clipboard-mode = \"internal\" korTTY detaches from the operating system clipboard entirely and uses its own in-memory clipboard instead: text copied in another application cannot be pasted anywhere in korTTY, and text copied in korTTY never reaches the OS clipboard — while copy, cut and paste within korTTY keep working everywhere, because terminals, SFTP views, the snippet editor and all dialogs share the same internal buffer. Because this is pure application logic and no OS clipboard isolation API is involved, it behaves identically on macOS, Windows and Linux, including the X11 primary selection (middle-click paste): pasting middle-click content from other applications is blocked, middle-click paste of korTTY-internal copies keeps working. The mode covers the terminal (shortcuts, context menu, middle-click), the code editor, all copy buttons, the ${clipboard} snippet variable, and the copy/cut/paste shortcuts of plain input fields. Copying images (AI-generated pictures, diagram exports) is unavailable in internal mode, since an image can only be shared via the OS clipboard. Scope The internal clipboard is a policy tool against casual data transfer through the clipboard, not a hard air gap: a user can still read text on screen. The right-click Paste entry of plain text fields is provided by the UI toolkit and may still access the OS clipboard — the keyboard shortcut and every korTTY-provided menu are covered. Encrypted API keys An AI profile's API key never appears in plain text in the policy. The administrator encrypts it once from a terminal — the command prints a kortty-enc:v1: value for the api-key-encrypted key: korTTY --encrypt-policy-value Users see only \"API key provided by your organization\" in the profile; the key is decrypted in memory at the moment a request is made. Security scope The envelope uses AES-256-GCM with an application-wide key, so it protects against casual disclosure (shoulder surfing, config diffs, backups) and detects tampering — it is not hard secrecy, since anyone with the korTTY binary could recover the application key. The installation directory's OS permissions remain the actual security boundary; prefer per-user keys via the normal profile flow when that boundary is not enough. Troubleshooting Symptom Cause and remedy Startup dialog \"Organization policy could not be loaded\" The policy file has a syntax error or invalid value; the dialog and the log name the exact position. korTTY stays in fail-safe lockdown until the file is fixed Policy seems ignored The file is not named kortty-policy.toml , not in the installation's policy/ folder, or korTTY was not restarted. The log's startup lines state which policy file (if any) was loaded A rule does not apply to a user Rule scoping is lowercase OS login names; check [groups] membership and remember that a more specific tier (user > group > everyone) overrides less specific rules Warning \"policy file is writable by the current user\" The installation directory permissions are too open — the enforcement model relies on admin-only write access Admin model does not appear See the log: GGUF URL downloads happen in the background at startup, and registration requires an installed llama.cpp runtime; MLX sources must be local safetensors directories","title": "Policy configuration"},{"location": "reference/enterprise-policy.html#policy-configuration","text": "In managed environments an administrator can restrict or preconfigure korTTY through a single TOML file, kortty-policy.toml , placed in the installation directory. Users cannot change or bypass it: the file is only read from the admin-writable installation folder, locked settings appear grayed out with a \"Managed by your organization\" hint, and hand-editing global-settings.xml is undone on the next load. This chapter explains where the file lives, how rules target users, groups and servers, and documents every available parameter with examples. Note Without a kortty-policy.toml nothing changes — korTTY behaves exactly as before. The shipped policy/kortty-policy.toml.example is a fully commented template with everything disabled.","title": "Policy configuration"},{"location": "reference/enterprise-policy.html#file-location-and-security-model","text": "korTTY loads the policy exclusively from the policy/ folder of its installation directory — the folder that also holds the application jar — and never from ~/.kortty/ or any other user-writable location. A ready-to-copy template ships as policy/kortty-policy.toml.example ; copy it to kortty-policy.toml in the same folder and restart korTTY (there is no hot reload). Platform Policy file location macOS /Applications/KorTTY.app/Contents/app/policy/kortty-policy.toml Windows C:\\Program Files\\KorTTY\\app\\policy\\kortty-policy.toml Linux (deb/rpm) /opt/kortty/lib/app/policy/kortty-policy.toml The enforcement model relies on the operating system's file permissions: the installation directory must be writable only by administrators, which is the default for the locations above. korTTY additionally logs a warning when the active policy file is writable by the current user. During development (never in a packaged installation) a policy can be tested with -Dkortty.policy.file=/path/to/policy.toml . If the file exists but cannot be parsed or contains an invalid value, korTTY starts in a fail-safe lockdown: every policy-controllable feature is denied, no server connection is allowed, and a startup dialog names the file and the exact error position. A typo can therefore never silently disable enforcement. One invalid rule rejects the entire file; unknown keys only produce log warnings, so a policy written for a newer korTTY does not lock out users of an older version.","title": "File location and security model"},{"location": "reference/enterprise-policy.html#users-groups-and-rule-precedence","text": "Rules target the current OS login name (matched lowercase). Group membership comes from two sources at once: groups defined in the policy's [groups] table, and the user's OS-level group memberships. On domain-joined Windows machines the OS groups include Active Directory groups, so rules can target AD groups directly — both fully qualified ( ACME\\Operations ) and as the bare name ( operations ) — without any directory server configuration. Each [[rule]] block optionally names users and/or groups ; a rule naming neither applies to every user. Per setting, the most specific tier that sets it wins: user beats group beats everyone . That is the familiar group-policy pattern — lock everything down in a baseline rule for everyone, then relax individual settings for a trusted group. When several rules of the same tier set the same key, the most restrictive value applies ( deny over read-only over confirm over allow ); for server rules, a connection must pass every applicable restriction of the winning tier. The order of rules in the file has no significance. [meta] schema-version = 1 organization = \"ACME Corp\" [groups] devs = [ \"alice\" , \"bob\" ] [[rule]] # applies to ALL users name = \"company-baseline\" [rule.features] ai-agent = \"deny\" [[rule]] # group tier: relaxes the baseline for ops name = \"ops-exception\" groups = [ \"ops\" , \"ACME \\\\ Operations\" ] # policy group OR OS/AD group [rule.features] ai-agent = \"allow\" ai-agent-execution = \"confirm\" [[rule]] # user tier: beats eve's group users = [ \"eve\" ] [rule.features] ai-agent = \"deny\"","title": "Users, groups and rule precedence"},{"location": "reference/enterprise-policy.html#server-access-control","text": "The [rule.servers] table restricts which servers a user may connect to — as an allow-list ( mode = \"allow\" : only listed servers are reachable) or a deny-list ( mode = \"deny\" : listed servers are blocked). The restriction is enforced centrally for every connection path: saved connections, QuickConnect, session restore, SFTP, teamwork-shared connections, AI swarm targets and scheduled jobs, including the jump host of a connection. Blocked connections stay visible in the connection manager but are grayed out with a lock marker, and any connect attempt shows a clear policy message. Patterns match the host string exactly as configured in the connection — korTTY never resolves DNS for policy checks, so host names and IP addresses are separate namespaces: if a server is reachable both ways, list both. Pattern form Example Matches Exact host name db01.acme.com that host, case-insensitive, any port Host-name glob *.prod.acme.com , web-??.acme.com * = any characters, ? = one character Host with port vault.acme.com:22 only connections to that port Single IP address 192.168.10.42 , 2001:db8::1 that address (IPv4 or IPv6) IP with port 192.168.10.42:22 , [2001:db8::1]:22 IPv6 ports require the bracket form CIDR network 10.99.0.0/16 , 2001:db8::/32 every address in the network IP range 10.20.0.100-10.20.0.199 inclusive from–to range [[rule]] [rule.servers] mode = \"deny\" hosts = [ \"*.prod.acme.com\" , \"vault.acme.com:22\" , \"192.168.10.42\" , \"10.99.0.0/16\" , \"10.20.0.100-10.20.0.199\" ]","title": "Server access control"},{"location": "reference/enterprise-policy.html#parameter-reference","text": "","title": "Parameter reference"},{"location": "reference/enterprise-policy.html#meta","text": "Key Type Values Required Effect schema-version integer 1 yes Rejected (lockdown) when this korTTY does not understand the version organization string free text no Shown in every \"Managed by your organization\" hint and dialog","title": "[meta]"},{"location": "reference/enterprise-policy.html#groups","text": "Key Type Effect <group-name> array of user names Defines a policy group; rules referencing the name target its members. OS/AD groups match automatically and need no entry here","title": "[groups]"},{"location": "reference/enterprise-policy.html#rule-scope","text": "Key Type Effect name string Optional label used in log messages users array of user names Targets the listed OS login names (user tier) groups array of group names Targets members of policy groups and/or OS/AD groups (group tier); a rule with neither users nor groups applies to everyone","title": "[[rule]] scope"},{"location": "reference/enterprise-policy.html#ruleservers","text": "Key Type Values Effect mode string allow , deny Allow-list or deny-list semantics for hosts hosts array of patterns see table above Non-empty list of server patterns","title": "[rule.servers]"},{"location": "reference/enterprise-policy.html#rulefeatures","text": "Key Type Values Restricts ai string allow , deny Master switch: deny disables every AI capability at once ai-agent string allow , deny AI Agent (menu, terminal context menu, keyboard shortcut, headless job runs) ai-chat string allow , deny AI chat, Saved Chats and the terminal-selection AI actions ai-swarm string allow , deny AI Swarm, including scheduled swarm jobs ai-planning string allow , deny AI Planning teamwork string allow , deny Teamwork shared-connections sync (service is not started, menu locked) plugins string allow , deny Plugin loading and the Plugins menu (e.g. terminal effects) session-journal string allow , deny The session journal : capture, journal bar, manager, viewer and exports. Not chained to ai — with AI denied the journal still records raw activity control-api string allow , deny The control API and its kortty-cli client: deny stops the listener, locks the Settings checkbox and forces the setting off. Not chained to ai — the API is local automation, not an AI capability ai-agent-execution string allow , confirm , read-only confirm forces interactive approval of every mutating command set and defeats the auto-approve option; read-only lets the agent plan and chat but never execute commands Naming a feature takes it over, whichever way you decide it A policy file that mentions a key locks the corresponding control, in the position the policy chose. Writing control-api = \"allow\" therefore locks the checkbox on — users can no longer switch the API off themselves. Leave the key out entirely to leave the choice with the user; write deny to take it away.","title": "[rule.features]"},{"location": "reference/enterprise-policy.html#rulesecurity","text": "Key Type Values Effect require-master-password boolean true Forces the master-password gate at startup; the setting is locked enforce-host-key-check boolean true SSH host key verification cannot be disabled anywhere — globally, per group or per connection allow-telemetry boolean false Forbids anonymous usage statistics allow-terminal-recording boolean false Forbids terminal session recording, including the session-level toggle clipboard-mode string system , internal internal confines korTTY to its own in-memory clipboard — see below","title": "[rule.security]"},{"location": "reference/enterprise-policy.html#ruleteamwork-rulesnippets-ruleai-profiles","text": "Key Type Values Effect allow-custom-sources boolean false Users cannot add teamwork sources; only [[teamwork-source]] entries remain allow-custom-script-headers boolean false Users cannot create script headers; only [[script-header]] entries remain allow-create boolean false Users cannot create AI profiles (buttons and wizard are locked) allow-edit boolean false Users cannot edit their existing AI profiles either allow-internet boolean false Forbids every AI internet-access mode — see below allow-internet = false cuts AI web access at three levels The internet access dropdown of an AI profile selects a web-search or MCP browsing backend. Forbidding it is enforced in three places, because a single one would leave a gap: Stored settings — every profile's mode is reset to Disabled whenever the settings are loaded or saved, so a value from before the policy arrived does not survive, and neither does a hand-edit of global-settings.xml . The interface — the dropdown is locked with the \"managed by your organization\" hint in both the AI Manager and Settings → AI . Every AI request — korTTY refuses to build a service for a profile whose mode is enabled, which closes the window between two clamps. The request fails with a policy message rather than quietly answering without the web tool the profile asked for. This is about the search backends only. It does not stop a cloud AI profile from reaching its own provider — for that, deny the ai feature or provision [[ai-profile]] entries that point at an internal endpoint.","title": "[rule.teamwork], [rule.snippets], [rule.ai-profiles]"},{"location": "reference/enterprise-policy.html#ruleai-runtime","text": "Key Type Values Effect allow-runtime-downloads boolean false No llama.cpp/MLX runtime downloads or update checks allow-model-downloads boolean false The Hugging Face model browser and downloads are disabled allow-user-models boolean false Only admin-provisioned [[ai-runtime.model]] entries can be loaded by embedded AI profiles","title": "[rule.ai-runtime]"},{"location": "reference/enterprise-policy.html#ruleupdates","text": "Key Type Values Effect enabled boolean false Disables the automatic update check and the manual check in the About dialog feed-url string http(s) URL Update checks query this endpoint instead of GitHub; it must return the same JSON shape as the GitHub releases/latest API ( tag_name , assets[] with name and browser_download_url )","title": "[rule.updates]"},{"location": "reference/enterprise-policy.html#ruleterminal","text": "Key Type Values Effect load-into-snippet-editor string allow , read-only , deny read-only keeps loading remote files into the snippet editor but forbids writing back to the target system; deny removes the feature entirely","title": "[rule.terminal]"},{"location": "reference/enterprise-policy.html#rulelogging","text": "korTTY rotates its log daily ( kortty.YYYY-MM-DD.log ); this table controls where the files go, how long they live, how they are formatted and how rotation is capped. Directory and retention are forced into the corresponding (then locked) user settings; format, compression and the rotation caps have no user setting and act directly. The configuration is applied before the very first log line of a start, so even startup logging follows the admin's scheme. Key Type Values Effect directory string absolute or ~/ -relative path Log directory (default ~/.kortty/logs ); the setting is locked retention-days integer 0 = keep forever Rotated logs older than N days are deleted; the setting is locked compress boolean default true Gzip rotated logs after one day format string text , json json writes one structured JSON object per event (logback JSON encoder — convenient for SIEM/central log collection) rotation-max-files integer 0 = unlimited Keep at most N rotated daily files rotation-total-size-mb integer 0 = uncapped Cap the total size of all rotated files (oldest deleted first) [[rule]] [rule.logging] directory = \"/var/log/kortty\" retention-days = 30 compress = true format = \"json\" rotation-max-files = 14 rotation-total-size-mb = 512 Note When several same-tier rules configure logging, each key resolves separately: shorter retention and tighter caps win, compression on wins, json wins over text . In practice, put the logging configuration into a single all-users rule. The chosen directory must be writable by the user running korTTY.","title": "[rule.logging]"},{"location": "reference/enterprise-policy.html#rulesession-journal","text": "Mandates for the session journal . Forced values lock the corresponding controls in the journal options, the settings dialog and the connection editor with a \"managed by your organization\" hint. Key Type Values Effect enforced boolean true A journal is written for every connection, regardless of the per-connection setting; users cannot stop it and the enable switch is locked on log-format string json (default), xml , yaml Forces the capture-log format for new journals ai-max-lines integer 0 = context fill Forces the AI evaluation window (max terminal lines per summary) storage-path string absolute path Forces the journal storage directory; the setting is locked allow-rename boolean false Journals cannot be renamed in the manager allow-delete boolean false Journals cannot be deleted in the manager name-template string template Initial journal title, with {connection} , {host} , {user} , {date} and {time} placeholders ai-title boolean true The closing AI title is generated regardless of the user setting ai-screenshot-analysis boolean true / false true forces the AI screenshot analysis on, false forbids it — including the manual per-screenshot run; the journal option is locked either way ai-ask boolean false Forbids on-demand AI over journal content: the viewer's Q&A panel and the manager's cross-journal AI search disappear; AI summaries are unaffected max-log-parts integer ≥ 1 Caps the number of rotated capture-log parts per journal; the effective limit is the minimum of this cap and the per-connection setting, and the connection editor's spinner is clamped to it [[rule]] [rule.features] session-journal = \"allow\" [rule.session-journal] enforced = true log-format = \"json\" storage-path = \"/srv/audit/kortty-journals\" allow-delete = false name-template = \"{connection} {date} {time} ({user})\" Note enforced mandates capture, not AI: with AI denied or unavailable the enforced journal records raw activity entries. When several same-tier rules configure the journal, enforced and ai-title resolve to true if any rule sets them, allow-rename / allow-delete to false if any rule forbids them, ai-screenshot-analysis and ai-ask to off if any rule switches them off, the line cap resolves to the tighter value ( 0 counts as unlimited), and max-log-parts resolves to the lower cap.","title": "[rule.session-journal]"},{"location": "reference/enterprise-policy.html#rulesession-journalreplace","text": "Automatic search-and-replace in every journal — the way to keep a whole category of secret out of the transcript, instead of relying on the user to notice it. Each entry is one rule, and a rule may use a regular expression. Key Type Default Effect pattern string required The text to find. With regex enabled it is a regular expression replacement string *** The text that replaces each match. In a regex rule $1 inserts a captured group regex boolean false Treats pattern as a regular expression ignore-case boolean false Matches every casing label string – Description for the administrator; korTTY only counts the rules in its UI hint [[rule]] [rule.session-journal] enforced = true [[rule.session-journal.replace]] pattern = \"AKIA[0-9A-Z]{16}\" replacement = \"***AWS-ACCESS-KEY***\" regex = true label = \"AWS access keys\" [[rule.session-journal.replace]] pattern = \"(?i)bearer \\\\ s+[A-Za-z0-9._-]{20,}\" replacement = \"Bearer ***\" regex = true label = \"Bearer tokens\" [[rule.session-journal.replace]] pattern = \"vpn.internal.acme.corp\" replacement = \"<internal-host>\" ignore-case = true The rules run on the capture thread, before a line is written , so a matching text never reaches the log file at all, and they are applied to AI summaries and user notes as well. Unlike the other keys, replacement rules are merged across every matching rule and every tier rather than the highest tier winning: more redaction is the more restrictive outcome, so a user- or group-specific rule can only add patterns, never switch the organization-wide ones off. Duplicate entries are collapsed. Warning A pattern that is not a valid regular expression is a policy error , not a warning — the file is rejected. A rule that silently matches nothing is worse than one the administrator has to fix, because the redaction would appear to be in place. Note Only journals written while the rule is in force are covered; existing journals are not rewritten retroactively. Users can clean those up with the viewer's Search & replace.","title": "[[rule.session-journal.replace]]"},{"location": "reference/enterprise-policy.html#admin-provided-objects","text": "These top-level tables define objects that appear read-only for every user, marked \"Provided by your organization\". They are rebuilt from the policy on every start and are never written into the user's configuration files — removing them from the policy removes them from korTTY. Table Keys Notes [[script-header]] name , content Immutable script header in the snippet system's Script-Header category [[ai-profile]] id , name , provider , endpoint , model , api-key-encrypted id must start with policy- ; provider is one of anthropic , openai-compatible , lm-studio , embedded-llama , embedded-mlx (embedded providers read model as the local model id) [[ai-runtime.model]] name , runtime , source runtime is llama or mlx ; source is an absolute local/UNC path, or for GGUF models an http(s) URL that korTTY downloads once at startup [[teamwork-source]] name , type , url type is git or shared-file ; injected as a read-only teamwork source","title": "Admin-provided objects"},{"location": "reference/enterprise-policy.html#internal-clipboard-mode","text": "With clipboard-mode = \"internal\" korTTY detaches from the operating system clipboard entirely and uses its own in-memory clipboard instead: text copied in another application cannot be pasted anywhere in korTTY, and text copied in korTTY never reaches the OS clipboard — while copy, cut and paste within korTTY keep working everywhere, because terminals, SFTP views, the snippet editor and all dialogs share the same internal buffer. Because this is pure application logic and no OS clipboard isolation API is involved, it behaves identically on macOS, Windows and Linux, including the X11 primary selection (middle-click paste): pasting middle-click content from other applications is blocked, middle-click paste of korTTY-internal copies keeps working. The mode covers the terminal (shortcuts, context menu, middle-click), the code editor, all copy buttons, the ${clipboard} snippet variable, and the copy/cut/paste shortcuts of plain input fields. Copying images (AI-generated pictures, diagram exports) is unavailable in internal mode, since an image can only be shared via the OS clipboard. Scope The internal clipboard is a policy tool against casual data transfer through the clipboard, not a hard air gap: a user can still read text on screen. The right-click Paste entry of plain text fields is provided by the UI toolkit and may still access the OS clipboard — the keyboard shortcut and every korTTY-provided menu are covered.","title": "Internal clipboard mode"},{"location": "reference/enterprise-policy.html#encrypted-api-keys","text": "An AI profile's API key never appears in plain text in the policy. The administrator encrypts it once from a terminal — the command prints a kortty-enc:v1: value for the api-key-encrypted key: korTTY --encrypt-policy-value Users see only \"API key provided by your organization\" in the profile; the key is decrypted in memory at the moment a request is made. Security scope The envelope uses AES-256-GCM with an application-wide key, so it protects against casual disclosure (shoulder surfing, config diffs, backups) and detects tampering — it is not hard secrecy, since anyone with the korTTY binary could recover the application key. The installation directory's OS permissions remain the actual security boundary; prefer per-user keys via the normal profile flow when that boundary is not enough.","title": "Encrypted API keys"},{"location": "reference/enterprise-policy.html#troubleshooting","text": "Symptom Cause and remedy Startup dialog \"Organization policy could not be loaded\" The policy file has a syntax error or invalid value; the dialog and the log name the exact position. korTTY stays in fail-safe lockdown until the file is fixed Policy seems ignored The file is not named kortty-policy.toml , not in the installation's policy/ folder, or korTTY was not restarted. The log's startup lines state which policy file (if any) was loaded A rule does not apply to a user Rule scoping is lowercase OS login names; check [groups] membership and remember that a more specific tier (user > group > everyone) overrides less specific rules Warning \"policy file is writable by the current user\" The installation directory permissions are too open — the enforcement model relies on admin-only write access Admin model does not appear See the log: GGUF URL downloads happen in the background at startup, and registration requires an installed llama.cpp runtime; MLX sources must be local safetensors directories","title": "Troubleshooting"},{"location": "reference/hardening-options.html","text": "Hardening options Hardening options are a set of production-quality techniques you can ask the AI to bake into a script it generates or rewrites. Instead of writing the same instruction (\"add error handling\", \"make it re-runnable\", \"give it a --help \") by hand every time, you tick the techniques you want and KorTTY turns each one into a precise rule that is appended to the AI prompt. The result is a script that behaves like something a careful engineer would ship: it fails loudly instead of silently, cleans up after itself, logs what it does, and can be run again without surprises. The same eleven options are used everywhere KorTTY generates or improves a script, so they behave identically no matter where you start from. Not the same as Input hardening Hardening options shape how the script is written (structure, error handling, logging, CLI). The separate Input hardening panel instead asks the AI to build a concrete input-validation guard block into the script that checks parameters and input files at run time. The two appear side by side and can be combined. Where they appear The Hardening options panel shows up in these places: Where How it looks Applied when Terminal → Generate Workflow Script (the Workflow button after an agent run) Collapsible Hardening options panel (collapsed by default) You click Generate AI Swarm → Generate multi-server workflow No panel — the default all-on selection is applied automatically You generate the multi-server script Snippet editor → AI Code → Improve robustness Options panel with all boxes ticked You confirm the dialog; KorTTY rewrites the complete snippet when any hardening rule is active Snippet editor → AI Code → Custom improvement… Options panel plus a free-text instruction field You confirm the dialog; KorTTY rewrites the complete snippet when any hardening rule is active Snippet editor → AI Code → Full code analysis Collapsible Hardening options panel at the bottom of the window, with a live (N) count of ticked options in its title; korTTY remembers whether you left it open or closed You click Apply selected Not shown for every action Improve readability and Improve performance deliberately do not show hardening options — those actions are meant to stay close to the original code. Hardening options appear only where adding robustness is the point: Improve robustness , Custom improvement , Full code analysis , and the two workflow-script generators. Every option is ticked by default . Untick the ones you do not want. An unticked option contributes nothing to the prompt. In Improve robustness and Custom improvement , active hardening rules are applied to the complete snippet , even if you marked only one region. This is required because options such as strict mode, a configuration block, --help , and a final summary must change the prologue or epilogue rather than only the selected lines. If you use Clear and leave Input hardening off, the ordinary improvement remains limited to the selected region. The complete-snippet result is still shown in the before/after review and is rejected if the AI abbreviates unchanged code with an omission marker. Below the options, three buttons help you manage the selection: All — tick every option. Clear — untick every option (generate/apply with no hardening rules). Save — remember the current selection permanently. From then on, every hardening panel opens with your saved selection instead of the all-on default — so you can set your preferred hardening once and have it applied everywhere. How they are applied Each ticked option becomes one authoritative hardening rule that KorTTY appends to the request sent to the AI (under Apply these hardening techniques: in the snippet editor, or ADDITIONAL REQUIREMENTS: in the workflow generator). In the Full code analysis apply flow, every selected rule is additionally assigned its own mandatory identifier: the AI returns the identifiers it completed in one compact list, and korTTY rejects the whole response if any identifier is missing. Explicit CLI and guard literals such as --dry-run , --yes , --help , and --verbose must also be present when the corresponding options are selected. Workflow generation also adds the corresponding target-language idioms — for example the Bash spelling of strict mode — only while that option is selected. Unticking an option removes both its rule and its option-specific language idioms; Clear therefore sends no hardening behaviour, apart from neutral requirements needed to produce valid code in the chosen language. The wording of each rule adapts to the target language : Imperative scripts — Bash, Python, Perl, Ruby, PowerShell, Windows-CMD and AppleScript get the imperative phrasing (flags, traps, exit codes, …). Declarative artefacts — Ansible playbooks, and snippets whose language is YAML / YML or contains ansible , get Ansible-idiomatic phrasing instead ( block / rescue / always , assert , vars: , check mode, …). So the idea of each option is the same everywhere, but a Bash script gets a set -euo pipefail style rule while an Ansible playbook gets an assert / failed_when style rule for the very same tick-box. The two groups The eleven options fall into two groups by their effect on the script: Behaviour-preserving hardening (the first seven) only add documentation, logging, structure, and safety nets. They make the script sturdier and easier to read without changing what it actually does . These are safe to leave on for almost any script. Behavioural / interactive changes (the last four) can change control flow or add a command-line interface — precondition gates, re-run detection, a dry-run mode, argument parsing. Leave them off if you want the rewrite to stay as close as possible to the original behaviour. Option reference Each option below lists what it is for and the exact rule KorTTY sends to the AI — for imperative scripts and for Ansible playbooks. Behaviour-preserving hardening Strict mode (abort on error) What it is for — Stop the script the moment something goes wrong instead of blindly continuing on a half-failed state. Imperative scripts — Enable the language's strict / abort-on-error mode (for example set -euo pipefail in Bash, Set-StrictMode -Version Latest with $ErrorActionPreference = 'Stop' in PowerShell, use strict; use warnings; in Perl). Ansible playbooks — Validate prerequisites with assert / failed_when so bad state fails the play immediately. Error trap & cleanup What it is for — Guarantee that a failure is reported clearly and that any temporary state (temp files, mounts, locks) is cleaned up even when the script aborts. Imperative scripts — Add an error trap / finally / ensure block that reports failures and cleans up temporary state. Ansible playbooks — Use block / rescue / always so failures are caught and cleanup always runs. Meaningful exit codes What it is for — Let whoever calls the script (a scheduler, a CI job, another script) tell why it failed, not just that it failed. Imperative scripts — Use distinct, documented non-zero exit codes for distinct failure classes. Ansible playbooks — Make failing tasks stop the play with a clear message ( any_errors_fatal where sensible). Logging ( --verbose ) What it is for — Make the script's progress visible and debuggable without cluttering normal output. Imperative scripts — Emit timestamped log messages to stderr and support a --verbose / -v flag. Ansible playbooks — Use the debug module for progress output (visible with -v ). Configuration block for literals What it is for — Collect the values you are most likely to change (paths, hostnames, package names) in one obvious place instead of scattering them through the script. Imperative scripts — Hoist all literals (paths, hosts, packages) into a clearly commented configuration block near the top. Ansible playbooks — Hoist all literals into a vars: block at the top. Final summary What it is for — End with a short report so the operator can see at a glance what happened. Imperative scripts — Print a final summary of what was done (with success/failure counts). Ansible playbooks — End with a debug summary of what changed. Style-guide / linter clean What it is for — Produce code that passes the language's standard linter, so it reads consistently and avoids common footguns. Imperative scripts — Follow the language style guide and keep it linter-clean (for example ShellCheck-clean for Bash). Ansible playbooks — Follow ansible-lint conventions and use fully-qualified module names. Behavioural / interactive changes Precondition checks What it is for — Fail fast, before touching anything, if the environment isn't ready — a missing command, insufficient privileges, or no network. Imperative scripts — Before doing work, verify required commands, privileges (root/sudo) and connectivity. Ansible playbooks — Add pre_tasks / assert checks for required privileges, packages and connectivity before any change. Idempotency (skip completed steps) What it is for — Make the script safe to run a second time: steps that are already done are detected and skipped instead of repeated or erroring out. Imperative scripts — Detect already-completed steps and skip them so the script is safe to re-run. Ansible playbooks — Ensure the playbook is fully idempotent (safe to re-run; rely on module idempotency and creates / removes ). Safe mode ( --dry-run + confirm) What it is for — Let the operator preview what the script would do without making any changes, and ask for confirmation before anything destructive. Imperative scripts — Support a --dry-run flag that prints intended actions without executing, and confirm before destructive operations (suppressible with --yes ). Ansible playbooks — Support check mode ( --check ) and guard destructive tasks so a dry run makes no changes. --help & argument parsing What it is for — Turn the script into a proper command-line tool with documented, overridable inputs instead of hard-coded values. Imperative scripts — Provide a --help /usage message and parse command-line arguments for the configurable values. Ansible playbooks — Document all variables and how to override them via --extra-vars at the top of the file. Tips Start with the defaults (all on) for a throwaway or personal script — the behaviour-preserving group costs you nothing and the interactive group makes the script friendlier. For a rewrite where you want the smallest possible diff, untick the four behavioural options and keep only the behaviour-preserving group. The options are independent — you can tick any combination. KorTTY only sends rules for the boxes that are ticked.","title": "Hardening options"},{"location": "reference/hardening-options.html#hardening-options","text": "Hardening options are a set of production-quality techniques you can ask the AI to bake into a script it generates or rewrites. Instead of writing the same instruction (\"add error handling\", \"make it re-runnable\", \"give it a --help \") by hand every time, you tick the techniques you want and KorTTY turns each one into a precise rule that is appended to the AI prompt. The result is a script that behaves like something a careful engineer would ship: it fails loudly instead of silently, cleans up after itself, logs what it does, and can be run again without surprises. The same eleven options are used everywhere KorTTY generates or improves a script, so they behave identically no matter where you start from. Not the same as Input hardening Hardening options shape how the script is written (structure, error handling, logging, CLI). The separate Input hardening panel instead asks the AI to build a concrete input-validation guard block into the script that checks parameters and input files at run time. The two appear side by side and can be combined.","title": "Hardening options"},{"location": "reference/hardening-options.html#where-they-appear","text": "The Hardening options panel shows up in these places: Where How it looks Applied when Terminal → Generate Workflow Script (the Workflow button after an agent run) Collapsible Hardening options panel (collapsed by default) You click Generate AI Swarm → Generate multi-server workflow No panel — the default all-on selection is applied automatically You generate the multi-server script Snippet editor → AI Code → Improve robustness Options panel with all boxes ticked You confirm the dialog; KorTTY rewrites the complete snippet when any hardening rule is active Snippet editor → AI Code → Custom improvement… Options panel plus a free-text instruction field You confirm the dialog; KorTTY rewrites the complete snippet when any hardening rule is active Snippet editor → AI Code → Full code analysis Collapsible Hardening options panel at the bottom of the window, with a live (N) count of ticked options in its title; korTTY remembers whether you left it open or closed You click Apply selected Not shown for every action Improve readability and Improve performance deliberately do not show hardening options — those actions are meant to stay close to the original code. Hardening options appear only where adding robustness is the point: Improve robustness , Custom improvement , Full code analysis , and the two workflow-script generators. Every option is ticked by default . Untick the ones you do not want. An unticked option contributes nothing to the prompt. In Improve robustness and Custom improvement , active hardening rules are applied to the complete snippet , even if you marked only one region. This is required because options such as strict mode, a configuration block, --help , and a final summary must change the prologue or epilogue rather than only the selected lines. If you use Clear and leave Input hardening off, the ordinary improvement remains limited to the selected region. The complete-snippet result is still shown in the before/after review and is rejected if the AI abbreviates unchanged code with an omission marker. Below the options, three buttons help you manage the selection: All — tick every option. Clear — untick every option (generate/apply with no hardening rules). Save — remember the current selection permanently. From then on, every hardening panel opens with your saved selection instead of the all-on default — so you can set your preferred hardening once and have it applied everywhere.","title": "Where they appear"},{"location": "reference/hardening-options.html#how-they-are-applied","text": "Each ticked option becomes one authoritative hardening rule that KorTTY appends to the request sent to the AI (under Apply these hardening techniques: in the snippet editor, or ADDITIONAL REQUIREMENTS: in the workflow generator). In the Full code analysis apply flow, every selected rule is additionally assigned its own mandatory identifier: the AI returns the identifiers it completed in one compact list, and korTTY rejects the whole response if any identifier is missing. Explicit CLI and guard literals such as --dry-run , --yes , --help , and --verbose must also be present when the corresponding options are selected. Workflow generation also adds the corresponding target-language idioms — for example the Bash spelling of strict mode — only while that option is selected. Unticking an option removes both its rule and its option-specific language idioms; Clear therefore sends no hardening behaviour, apart from neutral requirements needed to produce valid code in the chosen language. The wording of each rule adapts to the target language : Imperative scripts — Bash, Python, Perl, Ruby, PowerShell, Windows-CMD and AppleScript get the imperative phrasing (flags, traps, exit codes, …). Declarative artefacts — Ansible playbooks, and snippets whose language is YAML / YML or contains ansible , get Ansible-idiomatic phrasing instead ( block / rescue / always , assert , vars: , check mode, …). So the idea of each option is the same everywhere, but a Bash script gets a set -euo pipefail style rule while an Ansible playbook gets an assert / failed_when style rule for the very same tick-box.","title": "How they are applied"},{"location": "reference/hardening-options.html#the-two-groups","text": "The eleven options fall into two groups by their effect on the script: Behaviour-preserving hardening (the first seven) only add documentation, logging, structure, and safety nets. They make the script sturdier and easier to read without changing what it actually does . These are safe to leave on for almost any script. Behavioural / interactive changes (the last four) can change control flow or add a command-line interface — precondition gates, re-run detection, a dry-run mode, argument parsing. Leave them off if you want the rewrite to stay as close as possible to the original behaviour.","title": "The two groups"},{"location": "reference/hardening-options.html#option-reference","text": "Each option below lists what it is for and the exact rule KorTTY sends to the AI — for imperative scripts and for Ansible playbooks.","title": "Option reference"},{"location": "reference/hardening-options.html#behaviour-preserving-hardening","text": "","title": "Behaviour-preserving hardening"},{"location": "reference/hardening-options.html#behavioural-interactive-changes","text": "","title": "Behavioural / interactive changes"},{"location": "reference/hardening-options.html#tips","text": "Start with the defaults (all on) for a throwaway or personal script — the behaviour-preserving group costs you nothing and the interactive group makes the script friendlier. For a rewrite where you want the smallest possible diff, untick the four behavioural options and keep only the behaviour-preserving group. The options are independent — you can tick any combination. KorTTY only sends rules for the boxes that are ticked.","title": "Tips"},{"location": "reference/input-hardening.html","text": "Input hardening Input hardening asks the AI to build an input-validation guard block directly into a script it generates or rewrites. The guard runs at the very top of the script, before any real work, and rejects bad input on the host that executes the script: wrong parameter counts, forbidden characters, oversized values, missing input files, files in the wrong format, and files above a configurable size limit. Violations stop the script with a clear message and a documented exit code — unless the run is deliberately forced. The guard lives in the script, not in korTTY korTTY performs no validation of its own when a snippet or workflow script runs. Input hardening changes the script : the AI writes the checks into the code, so they protect every future run of that script — also when it is started outside korTTY, by a scheduler, or on a remote host. If you edit the script later, the guard is ordinary script code you can read, adjust, or remove. Input hardening vs. Hardening options Despite the similar name, this is a different feature than Hardening options . Hardening options are general production-quality techniques (strict mode, error traps, logging, --help , …) that shape how the script is written . Input hardening is about what the script accepts : it adds a concrete guard block that validates parameters and input files at run time. The two panels appear side by side and can be combined freely. Where it appears The Input hardening panel shows up in the same places as the classic hardening panel, plus the AI-Swarm generator: Where Applied when Terminal → Generate Workflow Script (the Workflow button after an agent run) You click Generate AI Swarm → Generate multi-server workflow You generate the multi-server script Snippet editor → AI Code → Improve robustness You confirm the dialog Snippet editor → AI Code → Custom improvement… You confirm the dialog Snippet editor → AI Code → Full code analysis You click Apply selected ; each enabled guard rule becomes a separately tracked mandatory requirement, and an incomplete replacement is rejected before review Unlike the classic hardening options, input hardening is strictly opt-in : the master check box starts unticked and the panel starts collapsed, because the guard changes the script's runtime behaviour — a script that previously accepted any input will start rejecting input that violates the rules. Tick Input hardening (validate script inputs) to activate it for the current generation or rewrite. Declarative YAML/YML/Ansible targets cannot receive this imperative argument-and-file guard. KorTTY disables the panel for those targets in the workflow, swarm, Improve robustness , Custom improvement , and Full code analysis dialogs, and an already-saved enabled default yields no effective guard configuration there. For snippet improvements in a supported language, enabling Input hardening changes the target to the complete snippet so the guard can be placed before all real work; the complete result is reviewed before it is applied. The five guard behaviours With the master check box ticked, five sub-options control what the guard enforces. All five are pre-ticked; untick what you do not want. Parameter allowlists & length limits Every parameter the script is called with is validated: the exact expected parameter count, a per-parameter character allowlist derived from how the script actually uses the value (a number, a file path, a host name, a keyword, free text), and a maximum length per parameter. Control characters, NUL bytes and shell metacharacters ( ; , | , & , ` , $ , \\ , < , > , embedded newlines) are rejected for every parameter that does not legitimately need them, so over-long or maliciously crafted values are refused instead of processed. Input file format checks Every parameter the script uses as an input file path is checked before first use: the file must exist and be readable, and its content format must match what the script can process. A text-processing script rejects binary files — the guard scans the first bytes for NUL bytes and additionally consults file --mime-type only where that command exists, falling back to the built-in check where it does not. Max. input file size (script variable) The guard defines a MAX_FILE_SIZE variable (in bytes) in its configuration section. When MAX_FILE_SIZE is greater than 0 , the guard obtains the file size from metadata before any operation reads file content — including the format check's initial content scan. A larger file is rejected with exit code 65 and its content is not read. If no metadata-only size query is available, the guard rejects the file without reading it. 0 means unlimited and skips the size check entirely. The size spinner in the panel accepts 0 – 1024 MB and sets the generated default to 10 MB unless you change it. Security warnings to stderr & script log Every violation is reported as a timestamped security warning line starting with SECURITY: on stderr. If FORCE=1 / --force override is also selected, every forced bypass is reported the same way. If the script writes its own log file, the guard appends the same warning line there too, so the script's log carries a complete security trail. FORCE=1 / --force override Blocking is the default, but a run can be forced: when the environment variable FORCE is set to 1 or the script is invoked with the --force parameter, the guard downgrades every violation to a warning and continues. The guard accepts --force anywhere on the command line and excludes it from parameter validation. Each individual violation is still reported, plus one extra warning that enforcement was bypassed — a forced run always leaves a complete trace. Exit codes When the guard blocks a run (and no force override is active), it uses distinct, documented exit codes so callers can tell why the input was rejected. The prompt names only codes created by the selected checks: format-only and size-only selections each describe only their own 65 case, and 66 is included only with Input file format checks . Exit code Meaning 64 A parameter violated the rules (count, characters, length) 65 An input file failed the format or size checks 66 An input file is missing or unreadable Language awareness The guard uses each language's own built-ins and standard library wherever available, and it probes optional platform tools before using them. Bash guards use $# , pattern matching, GNU stat -c %s or BSD/macOS stat -f %z for a metadata-only size check, and a NUL-byte scan only after that size check passes; if neither stat form works, the guard rejects the file without reading it. Python guards use sys.argv , re , os.path.getsize and a binary read; Perl guards use @ARGV , untainting regex allowlists, taint mode ( -T ) where the script allows it and the metadata-only -s operator; Ruby guards use ARGV , Regexp , File.size and File.binread . Other script languages get generic guidance only for the native argument, file, logging or environment facilities required by the selected sub-options. Declarative YAML/YML/Ansible targets are disabled because they take no positional parameters this way. Managing the selection Below the sub-options, All and Clear tick or untick every sub-option, and Save remembers the master toggle, the sub-option set and the size limit as your default — every Input hardening panel then opens with that selection. The panel title in the Full code analysis window shows a live count of the effectively active sub-options, which is 0 while the master toggle is off.","title": "Input hardening"},{"location": "reference/input-hardening.html#input-hardening","text": "Input hardening asks the AI to build an input-validation guard block directly into a script it generates or rewrites. The guard runs at the very top of the script, before any real work, and rejects bad input on the host that executes the script: wrong parameter counts, forbidden characters, oversized values, missing input files, files in the wrong format, and files above a configurable size limit. Violations stop the script with a clear message and a documented exit code — unless the run is deliberately forced. The guard lives in the script, not in korTTY korTTY performs no validation of its own when a snippet or workflow script runs. Input hardening changes the script : the AI writes the checks into the code, so they protect every future run of that script — also when it is started outside korTTY, by a scheduler, or on a remote host. If you edit the script later, the guard is ordinary script code you can read, adjust, or remove.","title": "Input hardening"},{"location": "reference/input-hardening.html#input-hardening-vs-hardening-options","text": "Despite the similar name, this is a different feature than Hardening options . Hardening options are general production-quality techniques (strict mode, error traps, logging, --help , …) that shape how the script is written . Input hardening is about what the script accepts : it adds a concrete guard block that validates parameters and input files at run time. The two panels appear side by side and can be combined freely.","title": "Input hardening vs. Hardening options"},{"location": "reference/input-hardening.html#where-it-appears","text": "The Input hardening panel shows up in the same places as the classic hardening panel, plus the AI-Swarm generator: Where Applied when Terminal → Generate Workflow Script (the Workflow button after an agent run) You click Generate AI Swarm → Generate multi-server workflow You generate the multi-server script Snippet editor → AI Code → Improve robustness You confirm the dialog Snippet editor → AI Code → Custom improvement… You confirm the dialog Snippet editor → AI Code → Full code analysis You click Apply selected ; each enabled guard rule becomes a separately tracked mandatory requirement, and an incomplete replacement is rejected before review Unlike the classic hardening options, input hardening is strictly opt-in : the master check box starts unticked and the panel starts collapsed, because the guard changes the script's runtime behaviour — a script that previously accepted any input will start rejecting input that violates the rules. Tick Input hardening (validate script inputs) to activate it for the current generation or rewrite. Declarative YAML/YML/Ansible targets cannot receive this imperative argument-and-file guard. KorTTY disables the panel for those targets in the workflow, swarm, Improve robustness , Custom improvement , and Full code analysis dialogs, and an already-saved enabled default yields no effective guard configuration there. For snippet improvements in a supported language, enabling Input hardening changes the target to the complete snippet so the guard can be placed before all real work; the complete result is reviewed before it is applied.","title": "Where it appears"},{"location": "reference/input-hardening.html#the-five-guard-behaviours","text": "With the master check box ticked, five sub-options control what the guard enforces. All five are pre-ticked; untick what you do not want.","title": "The five guard behaviours"},{"location": "reference/input-hardening.html#exit-codes","text": "When the guard blocks a run (and no force override is active), it uses distinct, documented exit codes so callers can tell why the input was rejected. The prompt names only codes created by the selected checks: format-only and size-only selections each describe only their own 65 case, and 66 is included only with Input file format checks . Exit code Meaning 64 A parameter violated the rules (count, characters, length) 65 An input file failed the format or size checks 66 An input file is missing or unreadable","title": "Exit codes"},{"location": "reference/input-hardening.html#language-awareness","text": "The guard uses each language's own built-ins and standard library wherever available, and it probes optional platform tools before using them. Bash guards use $# , pattern matching, GNU stat -c %s or BSD/macOS stat -f %z for a metadata-only size check, and a NUL-byte scan only after that size check passes; if neither stat form works, the guard rejects the file without reading it. Python guards use sys.argv , re , os.path.getsize and a binary read; Perl guards use @ARGV , untainting regex allowlists, taint mode ( -T ) where the script allows it and the metadata-only -s operator; Ruby guards use ARGV , Regexp , File.size and File.binread . Other script languages get generic guidance only for the native argument, file, logging or environment facilities required by the selected sub-options. Declarative YAML/YML/Ansible targets are disabled because they take no positional parameters this way.","title": "Language awareness"},{"location": "reference/input-hardening.html#managing-the-selection","text": "Below the sub-options, All and Clear tick or untick every sub-option, and Save remembers the master toggle, the sub-option set and the size limit as your default — every Input hardening panel then opens with that selection. The panel title in the Full code analysis window shows a live count of the effectively active sub-options, which is 0 while the master toggle is off.","title": "Managing the selection"},{"location": "reference/keyboard-shortcuts.html","text": "Keyboard shortcuts On macOS, use Cmd where Ctrl is shown. General Shortcut Action Ctrl + T New Tab (Quick Connect) Ctrl + W Close Tab Ctrl + Shift + N New Window Ctrl + Shift + W Close Window Ctrl + Tab Next Tab Ctrl + Shift + Tab Previous Tab Ctrl + O Open Project Ctrl + S Save Project Ctrl + Shift + B Create Backup Ctrl + Q Quit Ctrl + X Cut (disabled for terminal tabs) Ctrl + C Copy Ctrl + V Paste Ctrl + F Find in the active tab Ctrl + K Quick Connect Ctrl + M Manage Connections Ctrl + Shift + U SFTP client Ctrl + Shift + P Manage credentials Ctrl + Shift + G Manage GPG keys Ctrl + Shift + I Manage SSH keys Ctrl + , Global Settings Ctrl + Shift + S Snippet Manager Ctrl + Shift + J JobScheduler Ctrl + Shift + V Video Manager Ctrl + Shift + E Start / stop terminal recording Ctrl + Alt + J Session Journals Ctrl + Alt + T Start / stop the session journal of the active tab Ctrl + Alt + C Add a screenshot to the running session journal Ctrl + Alt + L Show/hide the live journal panel on its last-used side Ctrl + Alt + G Show/hide the Coding Agents panel on its last-used side Ctrl + Alt + N Jump to the next coding agent waiting for a decision, across windows Ctrl + Shift + A ASCII Art Ctrl + Shift + Y Open AI Manager Ctrl + Alt + A Open AI Agent Ctrl + Alt + P Open AI Planning Ctrl + Alt + S Open AI Swarm Ctrl + Shift + D Toggle Dashboard Ctrl + Shift + T Toggle command timestamps Ctrl + Shift + L Show / Hide Menu Bar Ctrl + Shift + K Dock the file browser on the left Ctrl + Shift + R Dock the file browser on the right Alt + + Zoom In Alt + - Zoom Out Alt + 0 Reset Zoom Ctrl + mouse wheel Zoom the terminal font in/out (Cmd + wheel on macOS) Ctrl + D Close a local cmd.exe/PowerShell tab (EOF for bash-family shells and SSH) F1 Open the manual ( Help → Manual ) F12 Toggle Fullscreen Ctrl + Shift + F Toggle Terminal-only Fullscreen Snippet editor These keys work inside the snippet editor's code field; see AI Code completions . Shortcut Action Shift + Tab (cursor at the end of a line with text) Open the completion list (the combination is configurable, see Snippet Editor settings ) Ctrl + Space Open the completion list anywhere (on macOS the physical Ctrl key, not Cmd ; Cmd + I and Alt + Esc work there too, because macOS often reserves Ctrl + Space for switching input sources) Up / Down (list open) Move the selection; typing filters the list Tab or Enter (list open) Insert the selected entry Esc Close the list, or dismiss ghost text Tab (ghost text visible) Accept the ghost text Alt + ] / Alt + [ (ghost text visible) Next / previous ghost-text candidate (physical ] and [ keys of a US layout) Tab (inside an inserted idiom template) Jump to the next placeholder Shift + Tab (anywhere else, or when another combination is configured) Outdent the line, as before Terminal AI agent Shortcut Action agent + Tab (at shell prompt) Show agent command variants ( agent , agent-ask , agent-plan ) agent + Tab (at shell prompt) Show recent agent-prompt history (newest first) Esc or Ctrl + C (during a run) Cancel the selected agent run's tab Ctrl + R (during a run) Toggle thinking details for the selected run ⏸ (activity panel) Pause the selected run at a safe checkpoint ▶️ (activity panel) Resume a paused run Note The complete, always-current shortcut list is generated from the application's accelerator definitions. If a shortcut here differs from what the app shows, the app is authoritative.","title": "Keyboard shortcuts"},{"location": "reference/keyboard-shortcuts.html#keyboard-shortcuts","text": "On macOS, use Cmd where Ctrl is shown.","title": "Keyboard shortcuts"},{"location": "reference/keyboard-shortcuts.html#general","text": "Shortcut Action Ctrl + T New Tab (Quick Connect) Ctrl + W Close Tab Ctrl + Shift + N New Window Ctrl + Shift + W Close Window Ctrl + Tab Next Tab Ctrl + Shift + Tab Previous Tab Ctrl + O Open Project Ctrl + S Save Project Ctrl + Shift + B Create Backup Ctrl + Q Quit Ctrl + X Cut (disabled for terminal tabs) Ctrl + C Copy Ctrl + V Paste Ctrl + F Find in the active tab Ctrl + K Quick Connect Ctrl + M Manage Connections Ctrl + Shift + U SFTP client Ctrl + Shift + P Manage credentials Ctrl + Shift + G Manage GPG keys Ctrl + Shift + I Manage SSH keys Ctrl + , Global Settings Ctrl + Shift + S Snippet Manager Ctrl + Shift + J JobScheduler Ctrl + Shift + V Video Manager Ctrl + Shift + E Start / stop terminal recording Ctrl + Alt + J Session Journals Ctrl + Alt + T Start / stop the session journal of the active tab Ctrl + Alt + C Add a screenshot to the running session journal Ctrl + Alt + L Show/hide the live journal panel on its last-used side Ctrl + Alt + G Show/hide the Coding Agents panel on its last-used side Ctrl + Alt + N Jump to the next coding agent waiting for a decision, across windows Ctrl + Shift + A ASCII Art Ctrl + Shift + Y Open AI Manager Ctrl + Alt + A Open AI Agent Ctrl + Alt + P Open AI Planning Ctrl + Alt + S Open AI Swarm Ctrl + Shift + D Toggle Dashboard Ctrl + Shift + T Toggle command timestamps Ctrl + Shift + L Show / Hide Menu Bar Ctrl + Shift + K Dock the file browser on the left Ctrl + Shift + R Dock the file browser on the right Alt + + Zoom In Alt + - Zoom Out Alt + 0 Reset Zoom Ctrl + mouse wheel Zoom the terminal font in/out (Cmd + wheel on macOS) Ctrl + D Close a local cmd.exe/PowerShell tab (EOF for bash-family shells and SSH) F1 Open the manual ( Help → Manual ) F12 Toggle Fullscreen Ctrl + Shift + F Toggle Terminal-only Fullscreen","title": "General"},{"location": "reference/keyboard-shortcuts.html#snippet-editor","text": "These keys work inside the snippet editor's code field; see AI Code completions . Shortcut Action Shift + Tab (cursor at the end of a line with text) Open the completion list (the combination is configurable, see Snippet Editor settings ) Ctrl + Space Open the completion list anywhere (on macOS the physical Ctrl key, not Cmd ; Cmd + I and Alt + Esc work there too, because macOS often reserves Ctrl + Space for switching input sources) Up / Down (list open) Move the selection; typing filters the list Tab or Enter (list open) Insert the selected entry Esc Close the list, or dismiss ghost text Tab (ghost text visible) Accept the ghost text Alt + ] / Alt + [ (ghost text visible) Next / previous ghost-text candidate (physical ] and [ keys of a US layout) Tab (inside an inserted idiom template) Jump to the next placeholder Shift + Tab (anywhere else, or when another combination is configured) Outdent the line, as before","title": "Snippet editor"},{"location": "reference/keyboard-shortcuts.html#terminal-ai-agent","text": "Shortcut Action agent + Tab (at shell prompt) Show agent command variants ( agent , agent-ask , agent-plan ) agent + Tab (at shell prompt) Show recent agent-prompt history (newest first) Esc or Ctrl + C (during a run) Cancel the selected agent run's tab Ctrl + R (during a run) Toggle thinking details for the selected run ⏸ (activity panel) Pause the selected run at a safe checkpoint ▶️ (activity panel) Resume a paused run Note The complete, always-current shortcut list is generated from the application's accelerator definitions. If a shortcut here differs from what the app shows, the app is authoritative.","title": "Terminal AI agent"},{"location": "reference/language-migration.html","text": "Language unification Language unification rewrites a snippet so that all of it is written in one programming language. Real-world admin scripts often are not: a Bash frame that pipes a heredoc into Perl, an inline awk program, a python3 -c one-liner. Such a script has no single formatter, no single linter, and no analysis that can see across the language boundary. KorTTY detects the mix locally — no AI request is needed to decide whether the option is offered — and only then asks a model to perform the rewrite. A rewrite is not a redesign The migration preserves observable behaviour and nothing else. Anything that cannot be carried over is reported back as a note instead of being dropped silently or replaced with an invented equivalent. Always read the notes and the before/after preview before you apply. Where it appears Where What it does Snippet editor → AI Code → Migrate into one language… Opens the migration dialog directly and shows the result as a before/after preview Snippet editor → AI Code → Full code analysis A collapsed Language unification panel; the migration then runs as the first stage of Apply selected , so every improvement and hardening stage afterwards works on the migrated script Snippet editor → AI Code → Security Check The same panel; the migration runs before the security fixes, so the fixes are written in the target language Terminal → Generate Workflow Script , AI Swarm The Target language only check box, which forbids embedded foreign-language parts in the generated script from the start Target languages Bash, Python, Perl, Ruby, PowerShell, Windows-CMD, AppleScript, JavaScript (Node) and Groovy. Each target brings its own shebang, file extension and comment prefix, and the same per-language idioms the workflow-script generator uses. After a whole-script migration KorTTY also updates the snippet's Language , its file extension and its auto-detected AI skills, so the snippet is consistently the new kind of file. Ansible is deliberately not a target: turning an imperative script into a declarative playbook is a re-modelling, not a language migration. Orchestration formats An Azure DevOps pipeline, a GitHub Actions workflow, a GitLab CI file, a Jenkinsfile, an Ansible playbook, a Puppet manifest and a Dockerfile invoke Bash or PowerShell by construction . Embedded shell is their design, not a defect, so KorTTY never reports such a document as \"mixed\" and never offers to migrate the document itself. Two narrower things are offered instead: Unify the script steps Offered only when the document's own script steps disagree — a pipeline with both - bash: and - pwsh: steps, a Jenkinsfile mixing sh and bat . Only the bodies of those steps are rewritten. Every other line — structure, keys, indentation, display names, conditions, task invocations, comments — must come back character for character, and the step type is adjusted where the format requires it ( - pwsh: instead of - bash: ). A result that changed anything outside the script steps is discarded , not offered. The snippet's language, file name and skills stay as they are: it is still the same pipeline. Convert to another platform Choosing a Target platform converts the host document into another platform's schema — a Jenkinsfile into an Azure DevOps pipeline, for instance. This is never suggested and never preselected : the selection starts on Unchanged , and only an explicit choice activates it. The conversion is deliberately lossy. Platform semantics are not equivalent, so KorTTY carries over what has a real counterpart (triggers, agent or pool selection, variables, secrets, dependencies, artifacts, conditions) and reports everything else — approvals, environments, platform-specific tasks, matrix semantics, plugin calls — as notes you have to redo by hand. A result that is not recognisable as the requested target platform is discarded. Dockerfiles are excluded from platform conversion in both directions: an image build recipe is not a CI pipeline. What is detected For a plain script KorTTY looks for heredocs fed to another interpreter ( perl <<'EOF' , python3 <<PY , node <<'JS' , …), inline one-liners ( perl -e , python3 -c , node -e , ruby -e , awk '…' ), and shell programs handed to another language's process API. Matches inside comments are ignored. sed -e expressions are deliberately not flagged: they are ubiquitous in shell, and treating them as a foreign language would make almost every script look mixed. A single external command call is likewise not an embedded language — it stays a plain process call after the migration too. When a result is refused Message Cause The AI returned no usable script. The reply carried no script at all The AI returned an incomplete script. The result lost most of the program, or contains an omission marker such as “rest unchanged” The AI changed the document outside its script steps. A step unification rewrote part of the pipeline scaffold The result is not a valid … document. A platform conversion did not reach the requested target format In every case the snippet is left untouched. Re-running with a stronger model, or with the profile picker in the preview window, is usually enough.","title": "Language unification"},{"location": "reference/language-migration.html#language-unification","text": "Language unification rewrites a snippet so that all of it is written in one programming language. Real-world admin scripts often are not: a Bash frame that pipes a heredoc into Perl, an inline awk program, a python3 -c one-liner. Such a script has no single formatter, no single linter, and no analysis that can see across the language boundary. KorTTY detects the mix locally — no AI request is needed to decide whether the option is offered — and only then asks a model to perform the rewrite. A rewrite is not a redesign The migration preserves observable behaviour and nothing else. Anything that cannot be carried over is reported back as a note instead of being dropped silently or replaced with an invented equivalent. Always read the notes and the before/after preview before you apply.","title": "Language unification"},{"location": "reference/language-migration.html#where-it-appears","text": "Where What it does Snippet editor → AI Code → Migrate into one language… Opens the migration dialog directly and shows the result as a before/after preview Snippet editor → AI Code → Full code analysis A collapsed Language unification panel; the migration then runs as the first stage of Apply selected , so every improvement and hardening stage afterwards works on the migrated script Snippet editor → AI Code → Security Check The same panel; the migration runs before the security fixes, so the fixes are written in the target language Terminal → Generate Workflow Script , AI Swarm The Target language only check box, which forbids embedded foreign-language parts in the generated script from the start","title": "Where it appears"},{"location": "reference/language-migration.html#target-languages","text": "Bash, Python, Perl, Ruby, PowerShell, Windows-CMD, AppleScript, JavaScript (Node) and Groovy. Each target brings its own shebang, file extension and comment prefix, and the same per-language idioms the workflow-script generator uses. After a whole-script migration KorTTY also updates the snippet's Language , its file extension and its auto-detected AI skills, so the snippet is consistently the new kind of file. Ansible is deliberately not a target: turning an imperative script into a declarative playbook is a re-modelling, not a language migration.","title": "Target languages"},{"location": "reference/language-migration.html#orchestration-formats","text": "An Azure DevOps pipeline, a GitHub Actions workflow, a GitLab CI file, a Jenkinsfile, an Ansible playbook, a Puppet manifest and a Dockerfile invoke Bash or PowerShell by construction . Embedded shell is their design, not a defect, so KorTTY never reports such a document as \"mixed\" and never offers to migrate the document itself. Two narrower things are offered instead:","title": "Orchestration formats"},{"location": "reference/language-migration.html#what-is-detected","text": "For a plain script KorTTY looks for heredocs fed to another interpreter ( perl <<'EOF' , python3 <<PY , node <<'JS' , …), inline one-liners ( perl -e , python3 -c , node -e , ruby -e , awk '…' ), and shell programs handed to another language's process API. Matches inside comments are ignored. sed -e expressions are deliberately not flagged: they are ubiquitous in shell, and treating them as a foreign language would make almost every script look mixed. A single external command call is likewise not an embedded language — it stays a plain process call after the migration too.","title": "What is detected"},{"location": "reference/language-migration.html#when-a-result-is-refused","text": "Message Cause The AI returned no usable script. The reply carried no script at all The AI returned an incomplete script. The result lost most of the program, or contains an omission marker such as “rest unchanged” The AI changed the document outside its script steps. A step unification rewrote part of the pipeline scaffold The result is not a valid … document. A platform conversion did not reach the requested target format In every case the snippet is left untouched. Re-running with a stronger model, or with the profile picker in the preview window, is usually enough.","title": "When a result is refused"},{"location": "reference/menu.html","text": "Menu reference Every item in korTTY's menu bar, with its shortcut (where defined) and what it does. The menu bar can be hidden with Ctrl + Shift + L and shown again by right-clicking the terminal or status bar. File Item Shortcut Description New Tab Ctrl + T Open Quick Connect in a new terminal tab Close Tab Ctrl + W Close the active terminal tab Close All Tabs Close every tab in the current window New Window Ctrl + Shift + N Open an additional, independent main window Close Window Ctrl + Shift + W Close the current window Open Project… Ctrl + O Restore a saved project (windows, tabs, layout) Save Project… Ctrl + S Save the current session as a project ( .kortty ) Create Backup… Ctrl + Shift + B Create an encrypted backup (ZIP password or GPG) Import Backup… Restore from a backup file Quit Ctrl + Q Exit korTTY Connection entries (Quick Connect, Manage/Import/Export Connections) live in the Connections menu. Edit Item Shortcut Description Cut Ctrl + X Cut (disabled for terminal tabs) Copy Ctrl + C Copy the terminal selection Paste Ctrl + V Paste into the terminal Find… Ctrl + F Search the active tab (terminal scrollback or open editor) Connections Item Description Quick Connect… Connect to a host without saving Manage Connections… Open the Connection Manager (tree, search, edit) Import… Import connections from other clients Export… Export connections SFTP-Client… Open the dual-panel SFTP file manager Security Item Description Credentials… Manage stored credentials (encrypted) GPG-Keys… Manage GPG keys used for backup encryption SSH-Keys… Manage SSH keys and passphrases Configuration Item Description Global Settings… Open the global Settings dialog (all tabs) Prevent System Sleep On macOS and Windows, enable activity-based system-sleep prevention. The computer is kept awake only while a terminal is connected, an enabled Scheduler job has a future run or is running, or an AI request is in progress. With none of these activities, system sleep remains available even while the item is checked. Display sleep is unaffected. On Linux the item is visible but disabled. See the Settings reference for every individual setting. Tools Item Description Snippet Manager… Create, edit, organize, send and export command snippets JobScheduler… Schedule background command / snippet / AI-agent / AI-swarm / SFTP / Rsync jobs Video Manager… Manage terminal recordings and export to WebM/MKV via ffmpeg Start/Stop Terminal Recording Toggle recording of the active terminal ( Ctrl + Shift + E ) Session Journals… Manage session journals : search, open, rename, describe, export and delete them, and set the journal options ( Ctrl + Alt + J ) Start/Stop Session Journal Toggle the session journal of the active terminal tab; starting mid-session imports the existing scrollback ( Ctrl + Alt + T ) Add Journal Screenshot Snapshot the active terminal into its running session journal ( Ctrl + Alt + C ) ASCII Art… Two tabs in one dialog: Text Banner renders text as a FIGlet banner in multiple font styles, AI Picture lets an AI profile draw a subject as ASCII art The three session-journal items stay visible but are disabled when an enterprise policy denies the session-journal feature. AI Item Description AI Manager… Manage AI profiles, integrated GGUF models, RAG knowledge stores, the AI Skills library, and Text/Coding roles Saved Chats… Open the saved AI chat conversations directly in their own window; invoking it again brings the existing window to the front AI Agent… Open the terminal AI agent AI Planning… Open the AI planning workflow AI Swarm… Broadcast one AI task to many servers and compare the answers ( Ctrl + Alt + S ) AI Manager opens as a modeless window, so it can remain visible while you use the main window. Invoking it again restores and focuses the same manager for that main window instead of creating a duplicate. Its five sections are Profiles (connection mode, model, prompt preset, reasoning, internet access and token budget), Local Models (search/download/import GGUF models), Knowledge Stores (RAG sources), AI Skills (the skill library, moved here from the global settings dialog) and Local AI (Text/Coding/embedding roles and the local runtime). The open primary section remains marked by a bold accent underline after you move focus into its tables, fields, or buttons: Plugins Item Description Terminal Effects… Enable/disable, configure, import/export terminal-effect plugins View Item Shortcut Description Show Dashboard Ctrl + Shift + D Toggle the connections dashboard Show Command Timestamps Ctrl + Shift + T Toggle inline command timestamps Show Menu Bar Ctrl + Shift + L Toggle the menu bar File Browser ▸ Show on Left Ctrl + Shift + K Dock the local file browser to the left of the terminal; unchecking the active side hides it File Browser ▸ Show on Right Ctrl + Shift + R Dock the local file browser to the right of the terminal; unchecking the active side hides it Zoom In Alt + + Increase terminal font size Zoom Out Alt + - Decrease terminal font size Reset Zoom Alt + 0 Reset terminal font size Background Transparency Slider (0–100 %) that makes the terminal background see-through to the desktop while the text stays sharp; every split pane inherits the value. The value is saved across restarts; fullscreen temporarily renders terminal backgrounds opaque and restores the value when you leave. Switching it on or off needs a restart, so the status bar shows a hint when you cross that threshold. Shown in the in-window menu bar only. Fullscreen F12 Toggle window fullscreen Terminal-only Fullscreen Ctrl + Shift + F Show the whole korTTY window — menus, tabs and status bar included — kept at its previous window size and centered on an empty fullscreen background, hiding the desktop and other windows Hide terminal scrollbars in fullscreen Also hide scrollbars while in fullscreen AI Agent Panel ▸ At Bottom / Dock Left / Dock Right Choose where the AI agent activity panel lives Live Journal ▸ Dock Left / Dock Right Dock the live journal panel beside the terminal; selecting the active side hides it Live Journal ▸ Show/Hide Ctrl + Alt + L Toggle the live journal panel on its last-used side (right by default) Coding Agents ▸ Dock Left / Dock Right Dock the Coding Agents panel beside the terminal; selecting the active side hides it Coding Agents ▸ Show/Hide Ctrl + Alt + G Toggle the Coding Agents panel on its last-used side (right by default) Coding Agents ▸ Next Blocked Agent Ctrl + Alt + N Bring the next coding agent that is waiting for a decision to the front, across windows Teamwork Item Description Teamwork Settings… Configure shared connection sources and synchronization Help Item Shortcut Description Manual F1 Open this documentation inside korTTY About korTTY Version and project information The manual has its own text-size buttons at the top left of its window: A- , the current percentage, and A+ . Clicking the percentage resets it. The same three actions have keyboard shortcuts: Cmd + + , Cmd + - , Cmd + 0 . korTTY remembers the size, and the size covers the whole window — the page and, when it is open, the AI search panel beside it. See Manual text size . Screenshots and diagrams enlarge on click: the picture opens over the page at the largest size the window allows, with − / + buttons and a percentage that returns it to that fitted size. Zoom further to read a single setting row, drag the picture to pan, and close with the × , Esc or a click next to the picture. Ctrl and the wheel zoom as well. The same works in the online guide. macOS Dock & menu bar On macOS the packaged app keeps running in the background (so the JobScheduler can run scheduled jobs) even after the last window is closed. korTTY therefore adds two extra entry points so it stays reachable — and quittable — with no window open: Dock icon menu — right-click korTTY's Dock icon for quick actions: New Window , New Tab , Manage Connections… , Open Project… , Manual , About korTTY , and Quit . Menu-bar (status) icon — a system-tray icon with New Window and Quit ; clicking the icon opens a new window. Both provide a reliable Quit even when every window is closed.","title": "Menu reference"},{"location": "reference/menu.html#menu-reference","text": "Every item in korTTY's menu bar, with its shortcut (where defined) and what it does. The menu bar can be hidden with Ctrl + Shift + L and shown again by right-clicking the terminal or status bar.","title": "Menu reference"},{"location": "reference/menu.html#file","text": "Item Shortcut Description New Tab Ctrl + T Open Quick Connect in a new terminal tab Close Tab Ctrl + W Close the active terminal tab Close All Tabs Close every tab in the current window New Window Ctrl + Shift + N Open an additional, independent main window Close Window Ctrl + Shift + W Close the current window Open Project… Ctrl + O Restore a saved project (windows, tabs, layout) Save Project… Ctrl + S Save the current session as a project ( .kortty ) Create Backup… Ctrl + Shift + B Create an encrypted backup (ZIP password or GPG) Import Backup… Restore from a backup file Quit Ctrl + Q Exit korTTY Connection entries (Quick Connect, Manage/Import/Export Connections) live in the Connections menu.","title": "File"},{"location": "reference/menu.html#edit","text": "Item Shortcut Description Cut Ctrl + X Cut (disabled for terminal tabs) Copy Ctrl + C Copy the terminal selection Paste Ctrl + V Paste into the terminal Find… Ctrl + F Search the active tab (terminal scrollback or open editor)","title": "Edit"},{"location": "reference/menu.html#connections","text": "Item Description Quick Connect… Connect to a host without saving Manage Connections… Open the Connection Manager (tree, search, edit) Import… Import connections from other clients Export… Export connections SFTP-Client… Open the dual-panel SFTP file manager","title": "Connections"},{"location": "reference/menu.html#security","text": "Item Description Credentials… Manage stored credentials (encrypted) GPG-Keys… Manage GPG keys used for backup encryption SSH-Keys… Manage SSH keys and passphrases","title": "Security"},{"location": "reference/menu.html#configuration","text": "Item Description Global Settings… Open the global Settings dialog (all tabs) Prevent System Sleep On macOS and Windows, enable activity-based system-sleep prevention. The computer is kept awake only while a terminal is connected, an enabled Scheduler job has a future run or is running, or an AI request is in progress. With none of these activities, system sleep remains available even while the item is checked. Display sleep is unaffected. On Linux the item is visible but disabled. See the Settings reference for every individual setting.","title": "Configuration"},{"location": "reference/menu.html#tools","text": "Item Description Snippet Manager… Create, edit, organize, send and export command snippets JobScheduler… Schedule background command / snippet / AI-agent / AI-swarm / SFTP / Rsync jobs Video Manager… Manage terminal recordings and export to WebM/MKV via ffmpeg Start/Stop Terminal Recording Toggle recording of the active terminal ( Ctrl + Shift + E ) Session Journals… Manage session journals : search, open, rename, describe, export and delete them, and set the journal options ( Ctrl + Alt + J ) Start/Stop Session Journal Toggle the session journal of the active terminal tab; starting mid-session imports the existing scrollback ( Ctrl + Alt + T ) Add Journal Screenshot Snapshot the active terminal into its running session journal ( Ctrl + Alt + C ) ASCII Art… Two tabs in one dialog: Text Banner renders text as a FIGlet banner in multiple font styles, AI Picture lets an AI profile draw a subject as ASCII art The three session-journal items stay visible but are disabled when an enterprise policy denies the session-journal feature.","title": "Tools"},{"location": "reference/menu.html#ai","text": "Item Description AI Manager… Manage AI profiles, integrated GGUF models, RAG knowledge stores, the AI Skills library, and Text/Coding roles Saved Chats… Open the saved AI chat conversations directly in their own window; invoking it again brings the existing window to the front AI Agent… Open the terminal AI agent AI Planning… Open the AI planning workflow AI Swarm… Broadcast one AI task to many servers and compare the answers ( Ctrl + Alt + S ) AI Manager opens as a modeless window, so it can remain visible while you use the main window. Invoking it again restores and focuses the same manager for that main window instead of creating a duplicate. Its five sections are Profiles (connection mode, model, prompt preset, reasoning, internet access and token budget), Local Models (search/download/import GGUF models), Knowledge Stores (RAG sources), AI Skills (the skill library, moved here from the global settings dialog) and Local AI (Text/Coding/embedding roles and the local runtime). The open primary section remains marked by a bold accent underline after you move focus into its tables, fields, or buttons:","title": "AI"},{"location": "reference/menu.html#plugins","text": "Item Description Terminal Effects… Enable/disable, configure, import/export terminal-effect plugins","title": "Plugins"},{"location": "reference/menu.html#view","text": "Item Shortcut Description Show Dashboard Ctrl + Shift + D Toggle the connections dashboard Show Command Timestamps Ctrl + Shift + T Toggle inline command timestamps Show Menu Bar Ctrl + Shift + L Toggle the menu bar File Browser ▸ Show on Left Ctrl + Shift + K Dock the local file browser to the left of the terminal; unchecking the active side hides it File Browser ▸ Show on Right Ctrl + Shift + R Dock the local file browser to the right of the terminal; unchecking the active side hides it Zoom In Alt + + Increase terminal font size Zoom Out Alt + - Decrease terminal font size Reset Zoom Alt + 0 Reset terminal font size Background Transparency Slider (0–100 %) that makes the terminal background see-through to the desktop while the text stays sharp; every split pane inherits the value. The value is saved across restarts; fullscreen temporarily renders terminal backgrounds opaque and restores the value when you leave. Switching it on or off needs a restart, so the status bar shows a hint when you cross that threshold. Shown in the in-window menu bar only. Fullscreen F12 Toggle window fullscreen Terminal-only Fullscreen Ctrl + Shift + F Show the whole korTTY window — menus, tabs and status bar included — kept at its previous window size and centered on an empty fullscreen background, hiding the desktop and other windows Hide terminal scrollbars in fullscreen Also hide scrollbars while in fullscreen AI Agent Panel ▸ At Bottom / Dock Left / Dock Right Choose where the AI agent activity panel lives Live Journal ▸ Dock Left / Dock Right Dock the live journal panel beside the terminal; selecting the active side hides it Live Journal ▸ Show/Hide Ctrl + Alt + L Toggle the live journal panel on its last-used side (right by default) Coding Agents ▸ Dock Left / Dock Right Dock the Coding Agents panel beside the terminal; selecting the active side hides it Coding Agents ▸ Show/Hide Ctrl + Alt + G Toggle the Coding Agents panel on its last-used side (right by default) Coding Agents ▸ Next Blocked Agent Ctrl + Alt + N Bring the next coding agent that is waiting for a decision to the front, across windows","title": "View"},{"location": "reference/menu.html#teamwork","text": "Item Description Teamwork Settings… Configure shared connection sources and synchronization","title": "Teamwork"},{"location": "reference/menu.html#help","text": "Item Shortcut Description Manual F1 Open this documentation inside korTTY About korTTY Version and project information The manual has its own text-size buttons at the top left of its window: A- , the current percentage, and A+ . Clicking the percentage resets it. The same three actions have keyboard shortcuts: Cmd + + , Cmd + - , Cmd + 0 . korTTY remembers the size, and the size covers the whole window — the page and, when it is open, the AI search panel beside it. See Manual text size . Screenshots and diagrams enlarge on click: the picture opens over the page at the largest size the window allows, with − / + buttons and a percentage that returns it to that fitted size. Zoom further to read a single setting row, drag the picture to pan, and close with the × , Esc or a click next to the picture. Ctrl and the wheel zoom as well. The same works in the online guide.","title": "Help"},{"location": "reference/menu.html#macos-dock-menu-bar","text": "On macOS the packaged app keeps running in the background (so the JobScheduler can run scheduled jobs) even after the last window is closed. korTTY therefore adds two extra entry points so it stays reachable — and quittable — with no window open: Dock icon menu — right-click korTTY's Dock icon for quick actions: New Window , New Tab , Manage Connections… , Open Project… , Manual , About korTTY , and Quit . Menu-bar (status) icon — a system-tray icon with New Window and Quit ; clicking the icon opens a new window. Both provide a reliable Quit even when every window is closed.","title": "macOS Dock &amp; menu bar"},{"location": "reference/settings/index.html","text": "Settings reference korTTY's settings live in Configuration → Global Settings… . They are stored in ~/.kortty/global-settings.xml . This reference documents every setting, organized by tab. Each per-tab page lists settings as a table: Column Meaning Setting The label shown in the dialog Type toggle · dropdown · number · text · color · path Values Allowed values / range Default Out-of-the-box value Stored as The field in global-settings.xml Settings tabs Tab What it controls Appearance, themes & font App design (Default, Matrix, Holographic, Klingon, Elegant Dark), terminal themes, font family & size, UI font size Colors Color profile, text/background/cursor/selection colors, cursor blink, the 16-color ANSI palette Terminal Columns/rows, scrollback, encoding, SSH keep-alive, SSH host key verification, connection retries, drag-drop, timestamps Window Window geometry restore, fixed geometry, dashboard state, menu bar Resources Opt-in JVM heap/GC profile (Balanced / High / Maximum) for larger workloads Logging Terminal log directory and retention; session journal storage, AI summaries, interval and profile Export PDF watermark and document footer for exported session journals and AI chats Backup Encryption type (ZIP-password / GPG), max backup count Updates Automatic update checking and interval Security Master-password prompt, change master password, temporary SSH keys Privacy Consent for anonymous usage statistics (Aptabase, EU/GDPR) Language UI language selection (8 built-in) + auto-detect Translation External or local-AI translation provider, credentials, target language, generate language file Video Terminal recording / ffmpeg video export AI AI features, agent execution, HTTP/CLI/embedded profiles, prompt presets, reasoning, image input (vision), quota, internet tools SFTP Manager SFTP tab auto-close, remote ZIP defaults, JobScheduler rsync binary Editor Cursor style and color for editor tabs Snippet Editor Font, color and cursor overrides for snippet windows Local-model downloads, Text/Coding role routing, embedding selection, llama.cpp runtime policy, knowledge-source synchronization, and the AI Skills library live in AI > AI Manager rather than the global Settings window; see Local models and RAG knowledge stores . Completeness The per-tab pages enumerate every individual setting. Coverage is verified automatically against the application's setting keys, so no setting is left undocumented. Visual reference A few of the configuration tabs (the per-tab pages show each in detail): Colors — terminal palette, cursor and ANSI colors Terminal — scrollback, encoding, keep-alive, retries Backup — max backups, ZIP-password or GPG encryption AI — agent execution, profiles, prompt settings, internet tools","title": "Settings reference"},{"location": "reference/settings/index.html#settings-reference","text": "korTTY's settings live in Configuration → Global Settings… . They are stored in ~/.kortty/global-settings.xml . This reference documents every setting, organized by tab. Each per-tab page lists settings as a table: Column Meaning Setting The label shown in the dialog Type toggle · dropdown · number · text · color · path Values Allowed values / range Default Out-of-the-box value Stored as The field in global-settings.xml","title": "Settings reference"},{"location": "reference/settings/index.html#settings-tabs","text": "Tab What it controls Appearance, themes & font App design (Default, Matrix, Holographic, Klingon, Elegant Dark), terminal themes, font family & size, UI font size Colors Color profile, text/background/cursor/selection colors, cursor blink, the 16-color ANSI palette Terminal Columns/rows, scrollback, encoding, SSH keep-alive, SSH host key verification, connection retries, drag-drop, timestamps Window Window geometry restore, fixed geometry, dashboard state, menu bar Resources Opt-in JVM heap/GC profile (Balanced / High / Maximum) for larger workloads Logging Terminal log directory and retention; session journal storage, AI summaries, interval and profile Export PDF watermark and document footer for exported session journals and AI chats Backup Encryption type (ZIP-password / GPG), max backup count Updates Automatic update checking and interval Security Master-password prompt, change master password, temporary SSH keys Privacy Consent for anonymous usage statistics (Aptabase, EU/GDPR) Language UI language selection (8 built-in) + auto-detect Translation External or local-AI translation provider, credentials, target language, generate language file Video Terminal recording / ffmpeg video export AI AI features, agent execution, HTTP/CLI/embedded profiles, prompt presets, reasoning, image input (vision), quota, internet tools SFTP Manager SFTP tab auto-close, remote ZIP defaults, JobScheduler rsync binary Editor Cursor style and color for editor tabs Snippet Editor Font, color and cursor overrides for snippet windows Local-model downloads, Text/Coding role routing, embedding selection, llama.cpp runtime policy, knowledge-source synchronization, and the AI Skills library live in AI > AI Manager rather than the global Settings window; see Local models and RAG knowledge stores . Completeness The per-tab pages enumerate every individual setting. Coverage is verified automatically against the application's setting keys, so no setting is left undocumented.","title": "Settings tabs"},{"location": "reference/settings/index.html#visual-reference","text": "A few of the configuration tabs (the per-tab pages show each in detail): Colors — terminal palette, cursor and ANSI colors Terminal — scrollback, encoding, keep-alive, retries Backup — max backups, ZIP-password or GPG encryption AI — agent execution, profiles, prompt settings, internet tools","title": "Visual reference"},{"location": "reference/settings/ai-skills.html","text": "AI Skills Configure custom AI skills that enhance AI interactions. This tab lets you manage a library of markdown-based skills that are automatically or manually included in AI requests. Open via AI → AI Manager → AI Skills ; stored in ~/.kortty/global-settings.xml . Moved out of Global Settings The skill library used to be a tab in Configuration → Global Settings . It now lives in the AI Manager , next to profiles, local models and knowledge stores. The stored data and the settings file are unchanged. Global Settings Setting Type Values Default Stored as Enable AI Skills toggle — On aiSkillsEnabled Automatically send only matching skills toggle — On aiSkillAutoDetectionEnabled Show hidden built-in skills toggle — Off — (view filter) Search skills text Filters the list by name, description or tags — — (view filter) Sort menu button Alphabetical, Status (enabled first) — — Save button Writes the library to the global settings file — — A count line below the list summarizes the whole library — Total , Active (enabled and not hidden), and Inactive/hidden — regardless of the current search or hidden filter. Skill Editor Fields When you select or create a skill, the right panel shows per-skill fields. Each skill is persisted individually in the skill list. Setting Type Values Default Stored as Skill name text — \"AI Skill\" name (on AiSkill object) Description text — — description Tags text Comma-separated tags (e.g. linux, bash) — tags Target dropdown AI Chat/Functions, AI Agent, Both, Connection Both target Active toggle — On enabled Skill Markdown text Markdown-formatted skill content — content Built-in skills korTTY ships 39 built-in best-practice skills covering shells (Bash, KornShell, Zsh, Csh, POSIX sh, PowerShell), programming languages (Python, C, C++, Java, C#, JavaScript, Visual Basic, SQL, R, Rust, Go, PHP, Swift, Assembly, Macro Assembler, Ruby, Perl, Lua, Groovy, TypeScript, Kotlin, Dart), markup and data formats (HTML, XML, YAML, JSON) and automation/observability tools (Puppet, Ansible, Azure DevOps Pipelines, Jenkins Declarative and Scripted Pipelines, Filebeat, Logstash). Each skill carries professional guidance on code commenting, robustness, common pitfalls to avoid, and language-specific security practices. They are added to the library on first start and appear with a Built-in badge. Built-in skills behave like your own skills — they can be edited, deactivated and assigned to connections — with these differences: They cannot be deleted, only hidden. Hide removes a built-in from the list and from all skill pickers, and a hidden built-in is no longer sent with any AI request — even on connections it was previously assigned to. Show hidden built-in skills reveals hidden entries, marked with a Hidden badge, so they can be brought back with Unhide . Unmodified built-ins update automatically. When a new korTTY release ships improved skill content, unchanged built-ins are replaced silently at startup (your Active/hidden choices are kept). Modified built-ins are never touched. Once you edit a built-in it shows a Built-in (modified) badge and stops auto-updating. Reset to shipped version discards your changes and restores the delivered version your edits were based on. If a newer delivered version exists, the entry shows 🔄 Update available and Update to latest shipped version adopts it. Your own skills always take precedence. When one of your enabled skills carries a tag matching a built-in's topic (for example a personal skill tagged perl ), the built-in is suppressed: it shows Overridden by user skill , is grayed out, and is no longer sent with any AI request. Deleting or deactivating your skill immediately reactivates the built-in. Deactivated, hidden and overridden entries are rendered grayed out; this works in every application design theme. Switching off auto-detection deactivates built-ins Without auto-detection every enabled skill is sent with every AI request, which would inflate prompts massively with 39 built-in skills. Turning Automatically send only matching skills off therefore asks for confirmation and then deactivates all built-in skills; re-enable the ones you need individually. Built-ins delivered by later releases also arrive deactivated while auto-detection is off. Notes Auto-detection behavior When Automatically send only matching skills is enabled, korTTY scores each skill against the current request across four fields — tags, name, description, and the headings in the skill's Markdown — and includes at most the two highest-scoring skills that clear the relevance threshold (ties broken by catalog order). Explicitly pinned and connection-assigned skills are added on top and do not consume that limit. When the local scores are inconclusive (zero or one skill matched, or exactly two matched with nearly equal scores), korTTY additionally asks the model to classify the request and prefers that answer — likewise capped at two skills — falling back to the local result if the call fails. When disabled, all applicable skills are sent without scoring — a skill is still skipped when it is inactive, has empty content, or does not match the current target. Skill targets AI Chat/Functions : Skills available in AI Chat and function call contexts AI Agent : Skills used by the terminal AI agent Both : Available to both AI Chat and AI Agent contexts Connection : Available to both AI Chat and AI Agent, but only on the connections the skill is assigned to. Such skills are always sent on those connections and bypass auto-detection. Skill lifecycle Skills are stored as XML elements within the global settings file. Use Import to load skills from markdown files and Export to save selected skills as markdown files. Delete applies to your own skills only; built-in skills are hidden instead (see above). Reset, update, hide and unhide are available from the list's context menu and from the banner above the editor when a built-in skill is selected. The skill list can be sorted alphabetically by name or by status (enabled first). Save persists the library immediately and confirms next to the button; pending edits are also written when the AI Manager window is closed. Importing a markdown file always creates an independent user skill — even a file exported from a built-in skill. Choosing skills per request This tab manages the global library. Which of these enabled skills apply to a given action is chosen elsewhere: the snippet editor's AI skills picker pins your selection to every snippet AI action, and the Full code analysis window shows the included skills as chips — labelled (auto-selected) or (manual) — with a searchable picker whose changes take effect on the next re-run. Within snippet AI actions the picker replaces auto-detection: only the ticked skills plus connection-assigned skills are sent, and clearing the picker sends no library skills at all. The snippet Correct and Translate selection actions and the fixed Diagram request never include library skills, regardless of target or pinning. See Snippets → AI skills .","title": "AI Skills"},{"location": "reference/settings/ai-skills.html#ai-skills","text": "Configure custom AI skills that enhance AI interactions. This tab lets you manage a library of markdown-based skills that are automatically or manually included in AI requests. Open via AI → AI Manager → AI Skills ; stored in ~/.kortty/global-settings.xml . Moved out of Global Settings The skill library used to be a tab in Configuration → Global Settings . It now lives in the AI Manager , next to profiles, local models and knowledge stores. The stored data and the settings file are unchanged.","title": "AI Skills"},{"location": "reference/settings/ai-skills.html#global-settings","text": "Setting Type Values Default Stored as Enable AI Skills toggle — On aiSkillsEnabled Automatically send only matching skills toggle — On aiSkillAutoDetectionEnabled Show hidden built-in skills toggle — Off — (view filter) Search skills text Filters the list by name, description or tags — — (view filter) Sort menu button Alphabetical, Status (enabled first) — — Save button Writes the library to the global settings file — — A count line below the list summarizes the whole library — Total , Active (enabled and not hidden), and Inactive/hidden — regardless of the current search or hidden filter.","title": "Global Settings"},{"location": "reference/settings/ai-skills.html#skill-editor-fields","text": "When you select or create a skill, the right panel shows per-skill fields. Each skill is persisted individually in the skill list. Setting Type Values Default Stored as Skill name text — \"AI Skill\" name (on AiSkill object) Description text — — description Tags text Comma-separated tags (e.g. linux, bash) — tags Target dropdown AI Chat/Functions, AI Agent, Both, Connection Both target Active toggle — On enabled Skill Markdown text Markdown-formatted skill content — content","title": "Skill Editor Fields"},{"location": "reference/settings/ai-skills.html#built-in-skills","text": "korTTY ships 39 built-in best-practice skills covering shells (Bash, KornShell, Zsh, Csh, POSIX sh, PowerShell), programming languages (Python, C, C++, Java, C#, JavaScript, Visual Basic, SQL, R, Rust, Go, PHP, Swift, Assembly, Macro Assembler, Ruby, Perl, Lua, Groovy, TypeScript, Kotlin, Dart), markup and data formats (HTML, XML, YAML, JSON) and automation/observability tools (Puppet, Ansible, Azure DevOps Pipelines, Jenkins Declarative and Scripted Pipelines, Filebeat, Logstash). Each skill carries professional guidance on code commenting, robustness, common pitfalls to avoid, and language-specific security practices. They are added to the library on first start and appear with a Built-in badge. Built-in skills behave like your own skills — they can be edited, deactivated and assigned to connections — with these differences: They cannot be deleted, only hidden. Hide removes a built-in from the list and from all skill pickers, and a hidden built-in is no longer sent with any AI request — even on connections it was previously assigned to. Show hidden built-in skills reveals hidden entries, marked with a Hidden badge, so they can be brought back with Unhide . Unmodified built-ins update automatically. When a new korTTY release ships improved skill content, unchanged built-ins are replaced silently at startup (your Active/hidden choices are kept). Modified built-ins are never touched. Once you edit a built-in it shows a Built-in (modified) badge and stops auto-updating. Reset to shipped version discards your changes and restores the delivered version your edits were based on. If a newer delivered version exists, the entry shows 🔄 Update available and Update to latest shipped version adopts it. Your own skills always take precedence. When one of your enabled skills carries a tag matching a built-in's topic (for example a personal skill tagged perl ), the built-in is suppressed: it shows Overridden by user skill , is grayed out, and is no longer sent with any AI request. Deleting or deactivating your skill immediately reactivates the built-in. Deactivated, hidden and overridden entries are rendered grayed out; this works in every application design theme. Switching off auto-detection deactivates built-ins Without auto-detection every enabled skill is sent with every AI request, which would inflate prompts massively with 39 built-in skills. Turning Automatically send only matching skills off therefore asks for confirmation and then deactivates all built-in skills; re-enable the ones you need individually. Built-ins delivered by later releases also arrive deactivated while auto-detection is off.","title": "Built-in skills"},{"location": "reference/settings/ai-skills.html#notes","text": "Auto-detection behavior When Automatically send only matching skills is enabled, korTTY scores each skill against the current request across four fields — tags, name, description, and the headings in the skill's Markdown — and includes at most the two highest-scoring skills that clear the relevance threshold (ties broken by catalog order). Explicitly pinned and connection-assigned skills are added on top and do not consume that limit. When the local scores are inconclusive (zero or one skill matched, or exactly two matched with nearly equal scores), korTTY additionally asks the model to classify the request and prefers that answer — likewise capped at two skills — falling back to the local result if the call fails. When disabled, all applicable skills are sent without scoring — a skill is still skipped when it is inactive, has empty content, or does not match the current target. Skill targets AI Chat/Functions : Skills available in AI Chat and function call contexts AI Agent : Skills used by the terminal AI agent Both : Available to both AI Chat and AI Agent contexts Connection : Available to both AI Chat and AI Agent, but only on the connections the skill is assigned to. Such skills are always sent on those connections and bypass auto-detection. Skill lifecycle Skills are stored as XML elements within the global settings file. Use Import to load skills from markdown files and Export to save selected skills as markdown files. Delete applies to your own skills only; built-in skills are hidden instead (see above). Reset, update, hide and unhide are available from the list's context menu and from the banner above the editor when a built-in skill is selected. The skill list can be sorted alphabetically by name or by status (enabled first). Save persists the library immediately and confirms next to the button; pending edits are also written when the AI Manager window is closed. Importing a markdown file always creates an independent user skill — even a file exported from a built-in skill. Choosing skills per request This tab manages the global library. Which of these enabled skills apply to a given action is chosen elsewhere: the snippet editor's AI skills picker pins your selection to every snippet AI action, and the Full code analysis window shows the included skills as chips — labelled (auto-selected) or (manual) — with a searchable picker whose changes take effect on the next re-run. Within snippet AI actions the picker replaces auto-detection: only the ticked skills plus connection-assigned skills are sent, and clearing the picker sends no library skills at all. The snippet Correct and Translate selection actions and the fixed Diagram request never include library skills, regardless of target or pinning. See Snippets → AI skills .","title": "Notes"},{"location": "reference/settings/ai.html","text": "AI Configure AI profiles and terminal AI Agent settings. This is the largest settings tab, encompassing AI features enablement, profiles (with model, API endpoint, connection mode, and reasoning levels), token quota management, snippet editor settings, and internet access configuration. Open via Configuration → Global Settings → AI ; stored in ~/.kortty/global-settings.xml . Core Settings Setting Type Values Default Stored as Enable AI features toggle — On aiFeaturesEnabled Show confirmation dialog before sending AI requests toggle — On aiConfirmBeforeSend Terminal AI Agent Setting Type Values Default Stored as Enable AI Agent execution toggle — On terminalAgentExecutionEnabled Ask before AI Agent changes the target system toggle — Off terminalAgentConfirmMutatingCommandSets Use OSC 133 prompt markers when the shell already provides them toggle — On defaultPromptHookEnabled Show agent debug messages toggle — Off terminalAgentShowDebugMessages Show agent runtime messages toggle — Off terminalAgentShowRuntimeMessages Show terminal agent setup dialog before each run toggle — On terminalAgentShowRunDialog Agent command name text — agent terminalAgentCommandName Match agent command name case-insensitively toggle — Off terminalAgentCommandNameCaseInsensitive AI agent task target dropdown Terminal window, New chat window Terminal window terminalAgentExecutionTarget Terminal agent input history size number 5–100 20 terminalAgentInputHistorySize AI Profiles Setting Type Values Default Stored as Default profile dropdown (list of configured profiles) — defaultAiProfileId AI request timeout number 0–1440 minutes 0 (no timeout) aiRequestTimeoutMinutes Security-check profile dropdown (list of configured profiles; empty = use default profile) — securityCheckAiProfileId The security-check profile is a dedicated AI profile for snippet Security Check actions. Leave it empty (or use Clear ) to reuse the default profile. It can also be set directly in the snippet Security Check window, and both places share the same remembered setting. AI request timeout is the maximum runtime of a single AI request and applies to every profile. The default 0 means korTTY imposes no timeout at all: long-running tasks such as the snippet editor's Full code analysis run until the model answers. Set a positive number of minutes to cancel requests that exceed it. A profile can override the value — see Timeout for this profile below. Profile Settings (in Editor Grid) The same fields are edited in AI > AI Manager > Profiles , where the whole form is visible at once: Setting Type Values Default Stored as Profile name text — AI Profile (profile name field) Connection dropdown HTTP API, Local CLI, Integrated llama.cpp, Integrated MLX (Apple Silicon; offered on Apple Silicon Macs only) HTTP API (profile connectionMode field) API URL text — — (profile apiUrl field) CLI provider dropdown (registered providers) — (profile cliProviderId field) CLI executable text — — (profile cliExecutablePath field) Model dropdown/text (editable; \"Default\", curated cloud-provider suggestions plus live-loaded models; \"Auto\" only for local LM Studio endpoints) — (profile model field) Local GGUF model dropdown Installed chat models; available when Connection is Integrated llama.cpp — (profile embeddedModelId field) Local MLX model dropdown Installed MLX models; available when Connection is Integrated MLX (Apple Silicon) — (profile embeddedModelId field) Custom model text — — (profile cliCustomModel field) Prompt optimization dropdown Auto (model detection), Generic, Llama, Qwen, Mistral, Gemma, DeepSeek, Phi, GPT-OSS Auto (profile promptPreset field) Reasoning dropdown Disabled, None, Minimal, Low, Medium, High, Extra high Disabled (profile reasoningEffort field) Image input (vision) dropdown Auto (detect), Enabled, Disabled Auto (detect) (profile visionSupport field) Internet access dropdown Disabled, KorTTY Tavily Tool, LM Studio Tavily MCP, Bright Data Web MCP, Brave Search MCP, SearXNG MCP, LM Studio Toolpack Disabled (profile internetAccessMode field) API Key (optional) text (password field) — (profile encryptedApiKey field) Max characters number 1–50,000,000 100,000 (profile maxSelectionChars field) Timeout for this profile check box + number Own timeout off = follow the global timeout; on: 0–1440 minutes (0 = never time out) Off (profile requestTimeoutMinutes field) Tokenizer dropdown Estimate, OpenAI cl100k_base, OpenAI o200k_base, OpenAI p50k_base, OpenAI r50k_base Estimate (profile tokenizerType field) Max tokens number + unit (amount: 0–1,000,000; unit: Thousands or Millions) 0 (unlimited) (profile tokenLimitAmount , tokenLimitUnit fields) Warning thresholds number pair Yellow %: 0–100, Red %: 0–100 75%, 90% (profile tokenWarningYellowPercent , tokenWarningRedPercent fields) Reset number + anchor date Period: 1–3650 days; Anchor date 30 days (profile tokenResetPeriodDays , tokenResetAnchorDate fields) Test AI Connection button — — (action only) Snippet Editor Setting Type Values Default Stored as Default language for AI text in code dropdown (available language options) — aiCodeTextDefaultLanguage Show optional additional instructions for AI actions in the snippet editor toggle — Off aiSnippetEditorAdditionalInstructionsEnabled Maximum alternative solutions number 1–10 3 aiSnippetAlternativeSolutionCount Internet Access Configuration Setting Type Values Default Stored as Tavily API key text (password field) — encryptedAiTavilyApiKey Bright Data API token text (password field) — encryptedAiBrightDataApiToken Brave Search API key text (password field) — encryptedAiBraveSearchApiKey SearXNG URL text — — aiSearxngUrl Tavily MCP server label text — tavily aiTavilyMcpServerLabel Bright Data MCP server label text — bright-data aiBrightDataMcpServerLabel Brave Search MCP plugin ID text — — aiBraveSearchMcpPluginId SearXNG MCP plugin ID text — — aiSearxngMcpPluginId LM Studio Toolpack MCP plugin ID text — — aiLmStudioToolpackMcpPluginId AI agent: offer internet research on every step toggle — Off aiAgentAlwaysOfferWebTools AI agent: offer internet research on every step only affects the KorTTY Tavily Tool mode. When off, a Terminal AI Agent step gets the web tools only if its task contains a web signal word or a URL; when on, every step gets them and the model decides whether to search or read a page. Notes AI Profiles KorTTY stores multiple named AI profiles, each with its own model, connection method, reasoning settings, prompt preset, and optional knowledge stores. Each profile tracks its own token usage separately. Profiles support three connection modes: HTTP API : Direct connection to an OpenAI-compatible REST endpoint (specify API URL, model name, and optional API key). Local CLI : Execute a local command-line AI client (configure CLI provider, custom executable, arguments template, and custom model name). Integrated llama.cpp : Choose the installed chat GGUF in Local GGUF model . korTTY acquires a private loopback llama-server lease for it; the API URL and profile API key are managed by korTTY and are not editable. An explicitly chosen or security-check profile remains most specific. Otherwise terminal text actions use the configured Text profile, code actions use the Coding profile, and an unassigned role falls back to the Default profile . Configure those roles and the local runtime under AI > AI Manager > Local AI ; see Local models with llama.cpp . The AI Manager is modeless and can remain open while you use the main window. Invoking it again restores and focuses the same manager for that main window, and its open primary section remains visibly marked with a bold accent underline when you interact with controls inside that section. Local AI manager settings Setting Values Default Stored as Text and translation profile Configured AI profile, or use default Use default textAiProfileId Coding profile Configured AI profile, or use default Use default codingAiProfileId Session journal profile Configured AI profile, or use default Use default sessionJournalAiProfileId RAG embedding model ID Installed local embedding model — ragEmbeddingModelId llama.cpp runtime updates Off, Notify me, Install stable updates automatically Notify me llamaRuntimeUpdatePolicy Preferred runtime backend Auto/CPU/Metal on macOS; Auto/CPU/Vulkan on Windows/Linux Auto preferredLlamaRuntimeBackend Hugging Face token Optional encrypted token for gated/private repositories — encryptedHuggingFaceToken Automatic (keep active backend) retains the active runtime package backend for updates. With no installed package, Auto initially selects Metal on macOS and CPU elsewhere. Starting a model configured for another supported GPU backend offers to install the matching signed package. The Local Models > Setup assistant exposes optional Text, Coding, and RAG-embedding slots. It verifies every selected fixed revision, quantization, license, and exact size before starting its asynchronous runtime/model installation, runs a real chat or embedding test for each installed GGUF, and saves the resulting role assignments only after all tests succeed. The Text and Coding slots may share one model. Configure refuses to replace a model's persisted runtime settings while that model is serving an active request. Knowledge stores assigned to the Text/Coding roles add only bounded, cited excerpts to matching normal terminal and snippet AI requests, never the complete knowledge store. A cloud Text/Coding profile receives those excerpts over its configured provider connection, so assigning the knowledge store to that role/profile is explicit permission for that disclosure. Agent, Planning, Swarm, and scheduled autonomous prompts remain a separate opt-in; see RAG knowledge stores . Prompt optimization presets Auto (model detection) resolves common Llama, Qwen, Mistral/Mixtral, Gemma, DeepSeek, Phi, and GPT-OSS names. A family preset adds concise compatibility guidance while leaving korTTY's strict JSON/code contracts authoritative; Generic adds no family-specific guidance. llama.cpp still applies the GGUF's native chat template. Reasoning Effort Levels Reasoning effort configures how deeply the AI thinks before responding. Available levels depend on the model and endpoint: Disabled : No reasoning parameter sent; model uses its default behavior. None : Explicitly disable reasoning with the transport's supported off value. Minimal : Light reasoning; fastest execution. Low : Low effort reasoning; balance between speed and depth. Medium : Medium effort; reasonable depth. High : High effort; more thorough reasoning. Extra high : Maximum reasoning effort; slowest but most comprehensive. Not all models support all levels. When LM Studio publishes capabilities.reasoning.allowed_options through its native model metadata, korTTY uses that exact list instead of treating a silently converted value as supported, and it reads that list automatically : picking another model or endpoint in the profile editor re-reads the metadata for the model you just chose, so the dropdown follows the profile without any further action. The list is only read from the endpoint's model metadata here — a single request that sends no prompt. Use the Refresh reasoning options button when you want to force a re-check, or for an endpoint that publishes no such metadata: that button also runs active connection probes. For a binary off / on model, an explicit none request switches this feature off, while omitting the reasoning parameter uses the model's published default; the unsupported Minimal, Low, Medium, High, and Extra high levels are not offered. A model whose LM Studio metadata publishes no reasoning capability at all — including a virtual model that overrides its reasoning metadata to false — offers only Disabled : LM Studio skips an unsupported request-time reasoning value with a log warning instead of rejecting the request, so an active probe would mistake every level for supported. A detected list belongs to the endpoint and model it was read for. Changing either discards it, and where korTTY cannot read the new combination's metadata — a CLI profile, a cloud endpoint — the profile falls back to the conservative defaults for its model name until you press Refresh reasoning options . A level that is no longer offered is dropped from the request with a log line naming the level actually used. Fields that a profile's own connection mode does not use, such as the CLI provider of an HTTP profile, never invalidate a detected list. Profiles using an integrated model keep their detected levels. For MiniMax endpoints (directly or through an aggregator whose model name contains minimax ), both Disabled and an explicit none are sent as MiniMax's own thinking: disabled parameter, because those models ignore reasoning_effort and otherwise think by default — billing the hidden reasoning as completion tokens and, for MiniMax-M3, opening the reply with an inline <think> block. An explicit effort level is passed through unchanged. For the native Anthropic (Claude) endpoint, an enabled reasoning level requests extended thinking with a level-dependent thinking budget; models that do not support extended thinking are retried once without it. The model's reasoning is shown in the Terminal AI Agent's 💭 thinking rows. Image input (vision) Image input (vision) decides whether korTTY may attach images to a prompt for this profile — used by the session journal's AI screenshot analysis . Auto (detect) derives the capability from the endpoint: for a local LM Studio endpoint the model metadata is authoritative (a vlm model counts as image-capable; the answer is read together with the reasoning levels — by the automatic metadata read and by Refresh reasoning options — and cached with them), the native Anthropic endpoint always counts as image-capable, and other endpoints are recognized by well-known vision model names (GPT-4o/4.⅕, o3/o4, Gemini, Gemma 3, Qwen-VL, LLaVA, Pixtral and similar). Enabled / Disabled override the detection for models it misjudges. CLI and integrated (llama.cpp/MLX) profiles cannot send images. Local LM Studio vision models ( vlm ) also appear in the model dropdown. Token Quota Management Each AI profile maintains a token usage quota with the following controls: Tokenizer : Choose which tokenizer estimates token counts—useful when switching between OpenAI and other providers. Options are Estimate (generic), cl100k_base (GPT-3.5/4), o200k_base (o1/o1-mini), p50k_base (Codex), and r50k_base (GPT-2). Max tokens limit : Set a spending cap (in thousands or millions of tokens, or unlimited). Token count resets on a rolling schedule. Reset period : Number of days between resets (1–3650), with an optional anchor date for predictable reset timing. Warning thresholds : Yellow warning triggers at a percentage of the limit; red warning at a higher percentage. Configure both as integers 0–100. Token usage is displayed as a colored bar and summary on the profile editor, and the profile list shows token status inline. Internet Access Modes Per-profile internet access strategy for AI requests. Each mode requires different credentials and MCP configuration: Disabled (default): No internet access. KorTTY Tavily Tool : Built-in web search using Tavily API directly (requires Tavily API key). LM Studio Tavily MCP : Web search via an LM Studio Tavily MCP instance (requires Tavily API key and MCP server label). Bright Data Web MCP : Structured data extraction and browsing via Bright Data Web MCP (requires Bright Data API token and MCP server label). Brave Search MCP : Search via Brave Search MCP (requires Brave Search API key and MCP plugin ID in mcp/<server_label> format). SearXNG MCP : Search via a SearXNG MCP instance (requires SearXNG URL and MCP plugin ID in mcp/<server_label> format). LM Studio Toolpack : Community LM Studio Toolpack web-search server (requires plugin ID in mcp/<server_label> format). Credentials are encrypted and stored securely. Use the Clear toggle adjacent to each secret field to erase stored values on the next save. Embedded profiles support only the KorTTY Tavily Tool A profile whose connection mode is Integrated llama.cpp or Integrated MLX can use KorTTY Tavily Tool and nothing else. The five MCP modes route the request through LM Studio's native API, which an embedded model never goes through — selecting one on an embedded profile fails the request with an explicit message instead of silently answering without web access. Local CLI profiles have no internet modes at all; the dropdown is disabled for them. An organization can forbid web access entirely with the allow-internet policy key — see Enterprise policy .","title": "AI"},{"location": "reference/settings/ai.html#ai","text": "Configure AI profiles and terminal AI Agent settings. This is the largest settings tab, encompassing AI features enablement, profiles (with model, API endpoint, connection mode, and reasoning levels), token quota management, snippet editor settings, and internet access configuration. Open via Configuration → Global Settings → AI ; stored in ~/.kortty/global-settings.xml .","title": "AI"},{"location": "reference/settings/ai.html#core-settings","text": "Setting Type Values Default Stored as Enable AI features toggle — On aiFeaturesEnabled Show confirmation dialog before sending AI requests toggle — On aiConfirmBeforeSend","title": "Core Settings"},{"location": "reference/settings/ai.html#terminal-ai-agent","text": "Setting Type Values Default Stored as Enable AI Agent execution toggle — On terminalAgentExecutionEnabled Ask before AI Agent changes the target system toggle — Off terminalAgentConfirmMutatingCommandSets Use OSC 133 prompt markers when the shell already provides them toggle — On defaultPromptHookEnabled Show agent debug messages toggle — Off terminalAgentShowDebugMessages Show agent runtime messages toggle — Off terminalAgentShowRuntimeMessages Show terminal agent setup dialog before each run toggle — On terminalAgentShowRunDialog Agent command name text — agent terminalAgentCommandName Match agent command name case-insensitively toggle — Off terminalAgentCommandNameCaseInsensitive AI agent task target dropdown Terminal window, New chat window Terminal window terminalAgentExecutionTarget Terminal agent input history size number 5–100 20 terminalAgentInputHistorySize","title": "Terminal AI Agent"},{"location": "reference/settings/ai.html#ai-profiles","text": "Setting Type Values Default Stored as Default profile dropdown (list of configured profiles) — defaultAiProfileId AI request timeout number 0–1440 minutes 0 (no timeout) aiRequestTimeoutMinutes Security-check profile dropdown (list of configured profiles; empty = use default profile) — securityCheckAiProfileId The security-check profile is a dedicated AI profile for snippet Security Check actions. Leave it empty (or use Clear ) to reuse the default profile. It can also be set directly in the snippet Security Check window, and both places share the same remembered setting. AI request timeout is the maximum runtime of a single AI request and applies to every profile. The default 0 means korTTY imposes no timeout at all: long-running tasks such as the snippet editor's Full code analysis run until the model answers. Set a positive number of minutes to cancel requests that exceed it. A profile can override the value — see Timeout for this profile below.","title": "AI Profiles"},{"location": "reference/settings/ai.html#profile-settings-in-editor-grid","text": "The same fields are edited in AI > AI Manager > Profiles , where the whole form is visible at once: Setting Type Values Default Stored as Profile name text — AI Profile (profile name field) Connection dropdown HTTP API, Local CLI, Integrated llama.cpp, Integrated MLX (Apple Silicon; offered on Apple Silicon Macs only) HTTP API (profile connectionMode field) API URL text — — (profile apiUrl field) CLI provider dropdown (registered providers) — (profile cliProviderId field) CLI executable text — — (profile cliExecutablePath field) Model dropdown/text (editable; \"Default\", curated cloud-provider suggestions plus live-loaded models; \"Auto\" only for local LM Studio endpoints) — (profile model field) Local GGUF model dropdown Installed chat models; available when Connection is Integrated llama.cpp — (profile embeddedModelId field) Local MLX model dropdown Installed MLX models; available when Connection is Integrated MLX (Apple Silicon) — (profile embeddedModelId field) Custom model text — — (profile cliCustomModel field) Prompt optimization dropdown Auto (model detection), Generic, Llama, Qwen, Mistral, Gemma, DeepSeek, Phi, GPT-OSS Auto (profile promptPreset field) Reasoning dropdown Disabled, None, Minimal, Low, Medium, High, Extra high Disabled (profile reasoningEffort field) Image input (vision) dropdown Auto (detect), Enabled, Disabled Auto (detect) (profile visionSupport field) Internet access dropdown Disabled, KorTTY Tavily Tool, LM Studio Tavily MCP, Bright Data Web MCP, Brave Search MCP, SearXNG MCP, LM Studio Toolpack Disabled (profile internetAccessMode field) API Key (optional) text (password field) — (profile encryptedApiKey field) Max characters number 1–50,000,000 100,000 (profile maxSelectionChars field) Timeout for this profile check box + number Own timeout off = follow the global timeout; on: 0–1440 minutes (0 = never time out) Off (profile requestTimeoutMinutes field) Tokenizer dropdown Estimate, OpenAI cl100k_base, OpenAI o200k_base, OpenAI p50k_base, OpenAI r50k_base Estimate (profile tokenizerType field) Max tokens number + unit (amount: 0–1,000,000; unit: Thousands or Millions) 0 (unlimited) (profile tokenLimitAmount , tokenLimitUnit fields) Warning thresholds number pair Yellow %: 0–100, Red %: 0–100 75%, 90% (profile tokenWarningYellowPercent , tokenWarningRedPercent fields) Reset number + anchor date Period: 1–3650 days; Anchor date 30 days (profile tokenResetPeriodDays , tokenResetAnchorDate fields) Test AI Connection button — — (action only)","title": "Profile Settings (in Editor Grid)"},{"location": "reference/settings/ai.html#snippet-editor","text": "Setting Type Values Default Stored as Default language for AI text in code dropdown (available language options) — aiCodeTextDefaultLanguage Show optional additional instructions for AI actions in the snippet editor toggle — Off aiSnippetEditorAdditionalInstructionsEnabled Maximum alternative solutions number 1–10 3 aiSnippetAlternativeSolutionCount","title": "Snippet Editor"},{"location": "reference/settings/ai.html#internet-access-configuration","text": "Setting Type Values Default Stored as Tavily API key text (password field) — encryptedAiTavilyApiKey Bright Data API token text (password field) — encryptedAiBrightDataApiToken Brave Search API key text (password field) — encryptedAiBraveSearchApiKey SearXNG URL text — — aiSearxngUrl Tavily MCP server label text — tavily aiTavilyMcpServerLabel Bright Data MCP server label text — bright-data aiBrightDataMcpServerLabel Brave Search MCP plugin ID text — — aiBraveSearchMcpPluginId SearXNG MCP plugin ID text — — aiSearxngMcpPluginId LM Studio Toolpack MCP plugin ID text — — aiLmStudioToolpackMcpPluginId AI agent: offer internet research on every step toggle — Off aiAgentAlwaysOfferWebTools AI agent: offer internet research on every step only affects the KorTTY Tavily Tool mode. When off, a Terminal AI Agent step gets the web tools only if its task contains a web signal word or a URL; when on, every step gets them and the model decides whether to search or read a page.","title": "Internet Access Configuration"},{"location": "reference/settings/ai.html#notes","text": "","title": "Notes"},{"location": "reference/settings/ai.html#ai-profiles_1","text": "KorTTY stores multiple named AI profiles, each with its own model, connection method, reasoning settings, prompt preset, and optional knowledge stores. Each profile tracks its own token usage separately. Profiles support three connection modes: HTTP API : Direct connection to an OpenAI-compatible REST endpoint (specify API URL, model name, and optional API key). Local CLI : Execute a local command-line AI client (configure CLI provider, custom executable, arguments template, and custom model name). Integrated llama.cpp : Choose the installed chat GGUF in Local GGUF model . korTTY acquires a private loopback llama-server lease for it; the API URL and profile API key are managed by korTTY and are not editable. An explicitly chosen or security-check profile remains most specific. Otherwise terminal text actions use the configured Text profile, code actions use the Coding profile, and an unassigned role falls back to the Default profile . Configure those roles and the local runtime under AI > AI Manager > Local AI ; see Local models with llama.cpp . The AI Manager is modeless and can remain open while you use the main window. Invoking it again restores and focuses the same manager for that main window, and its open primary section remains visibly marked with a bold accent underline when you interact with controls inside that section.","title": "AI Profiles"},{"location": "reference/settings/ai.html#local-ai-manager-settings","text": "Setting Values Default Stored as Text and translation profile Configured AI profile, or use default Use default textAiProfileId Coding profile Configured AI profile, or use default Use default codingAiProfileId Session journal profile Configured AI profile, or use default Use default sessionJournalAiProfileId RAG embedding model ID Installed local embedding model — ragEmbeddingModelId llama.cpp runtime updates Off, Notify me, Install stable updates automatically Notify me llamaRuntimeUpdatePolicy Preferred runtime backend Auto/CPU/Metal on macOS; Auto/CPU/Vulkan on Windows/Linux Auto preferredLlamaRuntimeBackend Hugging Face token Optional encrypted token for gated/private repositories — encryptedHuggingFaceToken Automatic (keep active backend) retains the active runtime package backend for updates. With no installed package, Auto initially selects Metal on macOS and CPU elsewhere. Starting a model configured for another supported GPU backend offers to install the matching signed package. The Local Models > Setup assistant exposes optional Text, Coding, and RAG-embedding slots. It verifies every selected fixed revision, quantization, license, and exact size before starting its asynchronous runtime/model installation, runs a real chat or embedding test for each installed GGUF, and saves the resulting role assignments only after all tests succeed. The Text and Coding slots may share one model. Configure refuses to replace a model's persisted runtime settings while that model is serving an active request. Knowledge stores assigned to the Text/Coding roles add only bounded, cited excerpts to matching normal terminal and snippet AI requests, never the complete knowledge store. A cloud Text/Coding profile receives those excerpts over its configured provider connection, so assigning the knowledge store to that role/profile is explicit permission for that disclosure. Agent, Planning, Swarm, and scheduled autonomous prompts remain a separate opt-in; see RAG knowledge stores .","title": "Local AI manager settings"},{"location": "reference/settings/ai.html#prompt-optimization-presets","text": "Auto (model detection) resolves common Llama, Qwen, Mistral/Mixtral, Gemma, DeepSeek, Phi, and GPT-OSS names. A family preset adds concise compatibility guidance while leaving korTTY's strict JSON/code contracts authoritative; Generic adds no family-specific guidance. llama.cpp still applies the GGUF's native chat template.","title": "Prompt optimization presets"},{"location": "reference/settings/ai.html#reasoning-effort-levels","text": "Reasoning effort configures how deeply the AI thinks before responding. Available levels depend on the model and endpoint: Disabled : No reasoning parameter sent; model uses its default behavior. None : Explicitly disable reasoning with the transport's supported off value. Minimal : Light reasoning; fastest execution. Low : Low effort reasoning; balance between speed and depth. Medium : Medium effort; reasonable depth. High : High effort; more thorough reasoning. Extra high : Maximum reasoning effort; slowest but most comprehensive. Not all models support all levels. When LM Studio publishes capabilities.reasoning.allowed_options through its native model metadata, korTTY uses that exact list instead of treating a silently converted value as supported, and it reads that list automatically : picking another model or endpoint in the profile editor re-reads the metadata for the model you just chose, so the dropdown follows the profile without any further action. The list is only read from the endpoint's model metadata here — a single request that sends no prompt. Use the Refresh reasoning options button when you want to force a re-check, or for an endpoint that publishes no such metadata: that button also runs active connection probes. For a binary off / on model, an explicit none request switches this feature off, while omitting the reasoning parameter uses the model's published default; the unsupported Minimal, Low, Medium, High, and Extra high levels are not offered. A model whose LM Studio metadata publishes no reasoning capability at all — including a virtual model that overrides its reasoning metadata to false — offers only Disabled : LM Studio skips an unsupported request-time reasoning value with a log warning instead of rejecting the request, so an active probe would mistake every level for supported. A detected list belongs to the endpoint and model it was read for. Changing either discards it, and where korTTY cannot read the new combination's metadata — a CLI profile, a cloud endpoint — the profile falls back to the conservative defaults for its model name until you press Refresh reasoning options . A level that is no longer offered is dropped from the request with a log line naming the level actually used. Fields that a profile's own connection mode does not use, such as the CLI provider of an HTTP profile, never invalidate a detected list. Profiles using an integrated model keep their detected levels. For MiniMax endpoints (directly or through an aggregator whose model name contains minimax ), both Disabled and an explicit none are sent as MiniMax's own thinking: disabled parameter, because those models ignore reasoning_effort and otherwise think by default — billing the hidden reasoning as completion tokens and, for MiniMax-M3, opening the reply with an inline <think> block. An explicit effort level is passed through unchanged. For the native Anthropic (Claude) endpoint, an enabled reasoning level requests extended thinking with a level-dependent thinking budget; models that do not support extended thinking are retried once without it. The model's reasoning is shown in the Terminal AI Agent's 💭 thinking rows.","title": "Reasoning Effort Levels"},{"location": "reference/settings/ai.html#image-input-vision","text": "Image input (vision) decides whether korTTY may attach images to a prompt for this profile — used by the session journal's AI screenshot analysis . Auto (detect) derives the capability from the endpoint: for a local LM Studio endpoint the model metadata is authoritative (a vlm model counts as image-capable; the answer is read together with the reasoning levels — by the automatic metadata read and by Refresh reasoning options — and cached with them), the native Anthropic endpoint always counts as image-capable, and other endpoints are recognized by well-known vision model names (GPT-4o/4.⅕, o3/o4, Gemini, Gemma 3, Qwen-VL, LLaVA, Pixtral and similar). Enabled / Disabled override the detection for models it misjudges. CLI and integrated (llama.cpp/MLX) profiles cannot send images. Local LM Studio vision models ( vlm ) also appear in the model dropdown.","title": "Image input (vision)"},{"location": "reference/settings/ai.html#token-quota-management","text": "Each AI profile maintains a token usage quota with the following controls: Tokenizer : Choose which tokenizer estimates token counts—useful when switching between OpenAI and other providers. Options are Estimate (generic), cl100k_base (GPT-3.5/4), o200k_base (o1/o1-mini), p50k_base (Codex), and r50k_base (GPT-2). Max tokens limit : Set a spending cap (in thousands or millions of tokens, or unlimited). Token count resets on a rolling schedule. Reset period : Number of days between resets (1–3650), with an optional anchor date for predictable reset timing. Warning thresholds : Yellow warning triggers at a percentage of the limit; red warning at a higher percentage. Configure both as integers 0–100. Token usage is displayed as a colored bar and summary on the profile editor, and the profile list shows token status inline.","title": "Token Quota Management"},{"location": "reference/settings/ai.html#internet-access-modes","text": "Per-profile internet access strategy for AI requests. Each mode requires different credentials and MCP configuration: Disabled (default): No internet access. KorTTY Tavily Tool : Built-in web search using Tavily API directly (requires Tavily API key). LM Studio Tavily MCP : Web search via an LM Studio Tavily MCP instance (requires Tavily API key and MCP server label). Bright Data Web MCP : Structured data extraction and browsing via Bright Data Web MCP (requires Bright Data API token and MCP server label). Brave Search MCP : Search via Brave Search MCP (requires Brave Search API key and MCP plugin ID in mcp/<server_label> format). SearXNG MCP : Search via a SearXNG MCP instance (requires SearXNG URL and MCP plugin ID in mcp/<server_label> format). LM Studio Toolpack : Community LM Studio Toolpack web-search server (requires plugin ID in mcp/<server_label> format). Credentials are encrypted and stored securely. Use the Clear toggle adjacent to each secret field to erase stored values on the next save. Embedded profiles support only the KorTTY Tavily Tool A profile whose connection mode is Integrated llama.cpp or Integrated MLX can use KorTTY Tavily Tool and nothing else. The five MCP modes route the request through LM Studio's native API, which an embedded model never goes through — selecting one on an embedded profile fails the request with an explicit message instead of silently answering without web access. Local CLI profiles have no internet modes at all; the dropdown is disabled for them. An organization can forbid web access entirely with the allow-internet policy key — see Enterprise policy .","title": "Internet Access Modes"},{"location": "reference/settings/appearance.html","text": "Appearance, themes & font korTTY's visual configuration spans three tabs: Appearance controls the app-level UI design, Font configures terminal typeface and size, and Themes manages reusable terminal color profiles. All settings persist to ~/.kortty/global-settings.xml . Appearance tab Customize the application window and dialog visual style. Setting Type Values Default Stored as App Design dropdown AtlantaFX Primer Dark, AtlantaFX Primer Light, AtlantaFX Nord Dark, AtlantaFX Nord Light, AtlantaFX Cupertino Dark, AtlantaFX Cupertino Light, AtlantaFX Dracula, Default, Matrix Terminal, Holographic Interface, Klingon Tactical, Elegant Dark, Amber CRT, Synthwave '84, Gruvbox Retro, Nord Arctic, Dracula AtlantaFX Primer Dark (new installations) appDesign Enable design animations toggle On, Off On appDesignAnimationsEnabled UI font size number 80–160 % in steps of 5 100 % uiFontScalePercent Match display resolution toggle On, Off On (new installations) uiFontScaleAuto Chat color profile dropdown Automatic (theme), Original, Paper, Midnight, Cyberpunk, Retrowave, Forest, Ocean, Terminal, GPT, Cute Automatic (theme) chatColorProfileId The ◀ and ▶ buttons next to the dropdown step backward and forward through the designs (wrapping around at the ends). When a design other than Default is selected, a preview image of that design is shown below the controls; the Default design has no preview and shows a short note in its place. Enable design animations doubles as a reduce-motion switch: turning it off stops the animated design effects while leaving the colors in place. The seven AtlantaFX entries apply AtlantaFX 2.1.0's Primer Dark , Primer Light , Nord Dark , Nord Light , Cupertino Dark , Cupertino Light , or Dracula user-agent theme to native JavaFX controls, with a small korTTY overlay for the main window chrome and korTTY-specific controls. They are grouped at the top of the dropdown, and a new installation starts with AtlantaFX Primer Dark . Existing settings are not migrated: an existing Default selection stays Default. Changing to Default or any older korTTY design restores JavaFX Modena before that design's stylesheet is applied. The change is live across open windows, dialogs, menus, tooltips, and completion popups. Chat color profile themes the AI chat and swarm surfaces. The default Automatic (theme) derives its palette from the active terminal theme's agent-panel colors; the other profiles are fixed palettes. Note App Design applies only to korTTY's application windows and dialogs. Terminal sessions, the Monaco file editor, session-journal pages, and AI chat content retain their own independent color settings (configured via the Colors or Themes tabs or their own appearance controls). UI font size UI font size scales korTTY's own interface — the menu bar and its dropdowns, dialogs, form labels, buttons, tab titles, the dashboard, the file browser and the status bar. It takes effect the moment you save; no restart is needed, and windows that are already open resize with it. The main window's first-launch size grows along with it, and dialogs that remember their size are re-measured after a change rather than reopening at a size that no longer fits their text. It deliberately does not touch surfaces that already have their own size control, so the two never fight each other: Surface Where its size is set instead Terminal sessions The Font tab, plus the View menu's zoom shortcuts File editor The Editor tab AI chat, agent plan and activity panels Their own A- / A+ buttons The manual ( Help → Manual ) The A- / A+ buttons in its own window — see below Session journal page The journal viewer's appearance popover Two further parts of the interface keep their size at every setting: the macOS application menu (the one in the system menu bar), because macOS draws it rather than korTTY, and the AI swarm status strip, whose labels are painted onto a canvas together with hand-computed positions. Match display resolution derives the size from the primary display instead, and disables the number field while it is on. A new installation starts with it on , so korTTY fits itself to the screen before anyone visits this tab; updating an existing installation never changes it — whatever you had stays. The value comes from the display's logical height — the resolution the operating system reports after its own scaling: Logical screen height UI font size Below 1400 px 100 % 1400–1799 px 110 % 1800–2299 px 125 % 2300 px and above 140 % This is why a Retina MacBook stays at 100 %: macOS already scales it, so it reports roughly 1100 logical pixels and needs no help. The case the option exists for is a 4K or 5K display running at 100 % operating-system scaling, where korTTY really does get the full 2160 or 2880 pixels to draw into. Note Automatic is a suggestion, not a measurement. JavaFX cannot report a display's physical size, so korTTY cannot tell a 27-inch 4K panel from a 32-inch one at the same resolution, even though they need different sizes. If the result does not suit you, turn the option off and set the percentage yourself — your manual value is remembered while automatic is on. Automatic tops out at 140 % and the manual setting at 160 %. The ceiling is deliberate: a handful of dialogs size themselves from fixed pixel widths, and beyond that point their text starts to crowd. korTTY re-reads the display when you save the settings, so after changing your monitor setup, open Settings and save again (or restart) to pick up the new resolution. Manual text size The manual ( Help → Manual , F1 ) has its own text size. Three buttons at the top left of its window control it: A- , the current percentage, and A+ . The percentage is also the reset control. Click it to return to 100 %. There are keyboard shortcuts for all three: Cmd + + , Cmd + - , Cmd + 0 . On Windows and Linux they are Ctrl + + , Ctrl + - , Ctrl + 0 . They are registered on the manual's own window, so they never collide with the terminal zoom in the main window. The range is 70–250 % in steps of 10, wider than the interface setting because the manual is a reflowing document rather than a layout built from fixed widths. The size is remembered across restarts, applies to the AI search answers next to the manual as well, and stays put as you navigate between pages. It scales the page as a whole — text, images and diagrams together — the way a browser's zoom does. Note This is separate from UI font size above, which governs the buttons and window chrome around the manual. If the manual's own toolbar looks too small, that is the setting to change. Font tab Set the terminal and editor typeface and size. Setting Type Values Default Stored as Font Family dropdown Common monospace fonts (Monaco, Courier New, Menlo, Fira Code, etc.) and all system fonts Monospaced defaultTerminalSettings.fontFamily Font Size number 8–72 pt 14 defaultTerminalSettings.fontSize Note The Font tab sets global defaults for all new terminal connections. Individual connections can override these values via their own connection settings. A live preview is shown as you adjust the controls. Themes tab Create, edit, and manage terminal color themes. Themes define colors (text, background, cursor, ANSI) and styling for all terminal sessions. Theme management Action Description Add Create a new custom color theme Edit Modify the selected theme. The button stays enabled for built-in themes but does nothing when clicked — duplicate the theme first Duplicate Copy the selected theme as a new custom theme Delete Remove a custom theme (built-in themes cannot be deleted) Theme options Setting Type Default Stored as When enabled, applying a theme also changes font family and size. toggle Off applyThemeFonts Note When this option is enabled, selecting a theme from the Colors tab's color profile dropdown will also apply that theme's font family and size. When disabled, only the theme's colors are applied. Built-in vs. custom themes korTTY includes a set of built-in color themes (e.g., Default, Dark Mode, Solarized Light). Built-in and custom themes are stored together in a single file, ~/.kortty/themes.xml . All themes appear in the Colors tab's color profile selector and in connection-specific theme dropdowns. Warning Built-in themes cannot be edited or deleted. To modify a built-in theme, duplicate it first, then edit the custom copy. Theme preview A live theme preview displays the theme's colors. The preview shows six swatches — foreground, background and cursor, plus the AI agent panel's background, run and error colors — so you can evaluate a theme before applying it. Cross-tab integration Colors tab : Choose a theme via the \"Color Profile\" dropdown; if the Themes tab's font option is enabled, the selected theme's font is also applied. Connection settings : Each individual SSH connection can override the global font and theme via its own settings (accessible in Connection Manager or Quick Connect). Terminal emulation : Font and theme colors affect only the terminal content; the application UI style (menu bar, dialogs, tabs) is determined by the Appearance tab's app design setting.","title": "Appearance, themes & font"},{"location": "reference/settings/appearance.html#appearance-themes-font","text": "korTTY's visual configuration spans three tabs: Appearance controls the app-level UI design, Font configures terminal typeface and size, and Themes manages reusable terminal color profiles. All settings persist to ~/.kortty/global-settings.xml .","title": "Appearance, themes &amp; font"},{"location": "reference/settings/appearance.html#appearance-tab","text": "Customize the application window and dialog visual style. Setting Type Values Default Stored as App Design dropdown AtlantaFX Primer Dark, AtlantaFX Primer Light, AtlantaFX Nord Dark, AtlantaFX Nord Light, AtlantaFX Cupertino Dark, AtlantaFX Cupertino Light, AtlantaFX Dracula, Default, Matrix Terminal, Holographic Interface, Klingon Tactical, Elegant Dark, Amber CRT, Synthwave '84, Gruvbox Retro, Nord Arctic, Dracula AtlantaFX Primer Dark (new installations) appDesign Enable design animations toggle On, Off On appDesignAnimationsEnabled UI font size number 80–160 % in steps of 5 100 % uiFontScalePercent Match display resolution toggle On, Off On (new installations) uiFontScaleAuto Chat color profile dropdown Automatic (theme), Original, Paper, Midnight, Cyberpunk, Retrowave, Forest, Ocean, Terminal, GPT, Cute Automatic (theme) chatColorProfileId The ◀ and ▶ buttons next to the dropdown step backward and forward through the designs (wrapping around at the ends). When a design other than Default is selected, a preview image of that design is shown below the controls; the Default design has no preview and shows a short note in its place. Enable design animations doubles as a reduce-motion switch: turning it off stops the animated design effects while leaving the colors in place. The seven AtlantaFX entries apply AtlantaFX 2.1.0's Primer Dark , Primer Light , Nord Dark , Nord Light , Cupertino Dark , Cupertino Light , or Dracula user-agent theme to native JavaFX controls, with a small korTTY overlay for the main window chrome and korTTY-specific controls. They are grouped at the top of the dropdown, and a new installation starts with AtlantaFX Primer Dark . Existing settings are not migrated: an existing Default selection stays Default. Changing to Default or any older korTTY design restores JavaFX Modena before that design's stylesheet is applied. The change is live across open windows, dialogs, menus, tooltips, and completion popups. Chat color profile themes the AI chat and swarm surfaces. The default Automatic (theme) derives its palette from the active terminal theme's agent-panel colors; the other profiles are fixed palettes. Note App Design applies only to korTTY's application windows and dialogs. Terminal sessions, the Monaco file editor, session-journal pages, and AI chat content retain their own independent color settings (configured via the Colors or Themes tabs or their own appearance controls).","title": "Appearance tab"},{"location": "reference/settings/appearance.html#ui-font-size","text": "UI font size scales korTTY's own interface — the menu bar and its dropdowns, dialogs, form labels, buttons, tab titles, the dashboard, the file browser and the status bar. It takes effect the moment you save; no restart is needed, and windows that are already open resize with it. The main window's first-launch size grows along with it, and dialogs that remember their size are re-measured after a change rather than reopening at a size that no longer fits their text. It deliberately does not touch surfaces that already have their own size control, so the two never fight each other: Surface Where its size is set instead Terminal sessions The Font tab, plus the View menu's zoom shortcuts File editor The Editor tab AI chat, agent plan and activity panels Their own A- / A+ buttons The manual ( Help → Manual ) The A- / A+ buttons in its own window — see below Session journal page The journal viewer's appearance popover Two further parts of the interface keep their size at every setting: the macOS application menu (the one in the system menu bar), because macOS draws it rather than korTTY, and the AI swarm status strip, whose labels are painted onto a canvas together with hand-computed positions. Match display resolution derives the size from the primary display instead, and disables the number field while it is on. A new installation starts with it on , so korTTY fits itself to the screen before anyone visits this tab; updating an existing installation never changes it — whatever you had stays. The value comes from the display's logical height — the resolution the operating system reports after its own scaling: Logical screen height UI font size Below 1400 px 100 % 1400–1799 px 110 % 1800–2299 px 125 % 2300 px and above 140 % This is why a Retina MacBook stays at 100 %: macOS already scales it, so it reports roughly 1100 logical pixels and needs no help. The case the option exists for is a 4K or 5K display running at 100 % operating-system scaling, where korTTY really does get the full 2160 or 2880 pixels to draw into. Note Automatic is a suggestion, not a measurement. JavaFX cannot report a display's physical size, so korTTY cannot tell a 27-inch 4K panel from a 32-inch one at the same resolution, even though they need different sizes. If the result does not suit you, turn the option off and set the percentage yourself — your manual value is remembered while automatic is on. Automatic tops out at 140 % and the manual setting at 160 %. The ceiling is deliberate: a handful of dialogs size themselves from fixed pixel widths, and beyond that point their text starts to crowd. korTTY re-reads the display when you save the settings, so after changing your monitor setup, open Settings and save again (or restart) to pick up the new resolution.","title": "UI font size"},{"location": "reference/settings/appearance.html#manual-text-size","text": "The manual ( Help → Manual , F1 ) has its own text size. Three buttons at the top left of its window control it: A- , the current percentage, and A+ . The percentage is also the reset control. Click it to return to 100 %. There are keyboard shortcuts for all three: Cmd + + , Cmd + - , Cmd + 0 . On Windows and Linux they are Ctrl + + , Ctrl + - , Ctrl + 0 . They are registered on the manual's own window, so they never collide with the terminal zoom in the main window. The range is 70–250 % in steps of 10, wider than the interface setting because the manual is a reflowing document rather than a layout built from fixed widths. The size is remembered across restarts, applies to the AI search answers next to the manual as well, and stays put as you navigate between pages. It scales the page as a whole — text, images and diagrams together — the way a browser's zoom does. Note This is separate from UI font size above, which governs the buttons and window chrome around the manual. If the manual's own toolbar looks too small, that is the setting to change.","title": "Manual text size"},{"location": "reference/settings/appearance.html#font-tab","text": "Set the terminal and editor typeface and size. Setting Type Values Default Stored as Font Family dropdown Common monospace fonts (Monaco, Courier New, Menlo, Fira Code, etc.) and all system fonts Monospaced defaultTerminalSettings.fontFamily Font Size number 8–72 pt 14 defaultTerminalSettings.fontSize Note The Font tab sets global defaults for all new terminal connections. Individual connections can override these values via their own connection settings. A live preview is shown as you adjust the controls.","title": "Font tab"},{"location": "reference/settings/appearance.html#themes-tab","text": "Create, edit, and manage terminal color themes. Themes define colors (text, background, cursor, ANSI) and styling for all terminal sessions.","title": "Themes tab"},{"location": "reference/settings/appearance.html#theme-management","text": "Action Description Add Create a new custom color theme Edit Modify the selected theme. The button stays enabled for built-in themes but does nothing when clicked — duplicate the theme first Duplicate Copy the selected theme as a new custom theme Delete Remove a custom theme (built-in themes cannot be deleted)","title": "Theme management"},{"location": "reference/settings/appearance.html#theme-options","text": "Setting Type Default Stored as When enabled, applying a theme also changes font family and size. toggle Off applyThemeFonts Note When this option is enabled, selecting a theme from the Colors tab's color profile dropdown will also apply that theme's font family and size. When disabled, only the theme's colors are applied.","title": "Theme options"},{"location": "reference/settings/appearance.html#built-in-vs-custom-themes","text": "korTTY includes a set of built-in color themes (e.g., Default, Dark Mode, Solarized Light). Built-in and custom themes are stored together in a single file, ~/.kortty/themes.xml . All themes appear in the Colors tab's color profile selector and in connection-specific theme dropdowns. Warning Built-in themes cannot be edited or deleted. To modify a built-in theme, duplicate it first, then edit the custom copy.","title": "Built-in vs. custom themes"},{"location": "reference/settings/appearance.html#theme-preview","text": "A live theme preview displays the theme's colors. The preview shows six swatches — foreground, background and cursor, plus the AI agent panel's background, run and error colors — so you can evaluate a theme before applying it.","title": "Theme preview"},{"location": "reference/settings/appearance.html#cross-tab-integration","text": "Colors tab : Choose a theme via the \"Color Profile\" dropdown; if the Themes tab's font option is enabled, the selected theme's font is also applied. Connection settings : Each individual SSH connection can override the global font and theme via its own settings (accessible in Connection Manager or Quick Connect). Terminal emulation : Font and theme colors affect only the terminal content; the application UI style (menu bar, dialogs, tabs) is determined by the Appearance tab's app design setting.","title": "Cross-tab integration"},{"location": "reference/settings/backup.html","text": "Backup Configure backup retention policy and encryption method for korTTY session backups. Open via Configuration → Global Settings → Backup ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Maximum number of backups number 0–100 (0 = unlimited) 10 maxBackupCount ZIP with Password toggle — On backupEncryptionType Credential dropdown Available stored credentials — backupCredentialId GPG Encryption toggle — Off backupEncryptionType GPG Key dropdown Available GPG keys — backupGpgKeyId Warning Backups are ALWAYS encrypted. Both encryption modes (ZIP with password and GPG) are mandatory — at least one must be configured before backups can be performed. Note Maximum Backups: Set to 0 to keep backups indefinitely; any other value (1–100) will automatically delete the oldest backups once the limit is reached. When using password-based encryption, select a credential from the management system. When using GPG encryption, select an available GPG key. The encryption type is stored in the global settings and determines which credential or key ID is used for future backups. Backups include ssh-host-keys.properties , so normalized host:port trust decisions shared by interactive Terminal, SFTP, and Mosh bootstrap connections survive restore; the transient cross-process .lock file is not included. JobScheduler host-key pins remain separately stored in job-scheduler.xml . Local-AI backups include llm/models.xml and rag/stores.json so model registrations, role assignments, and knowledge-source definitions can be restored. They exclude GGUF weights, llama.cpp runtime packages, temporary sidecar data, source documents, and regenerable HNSW snapshots; see Backup & restore .","title": "Backup"},{"location": "reference/settings/backup.html#backup","text": "Configure backup retention policy and encryption method for korTTY session backups. Open via Configuration → Global Settings → Backup ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Maximum number of backups number 0–100 (0 = unlimited) 10 maxBackupCount ZIP with Password toggle — On backupEncryptionType Credential dropdown Available stored credentials — backupCredentialId GPG Encryption toggle — Off backupEncryptionType GPG Key dropdown Available GPG keys — backupGpgKeyId Warning Backups are ALWAYS encrypted. Both encryption modes (ZIP with password and GPG) are mandatory — at least one must be configured before backups can be performed. Note Maximum Backups: Set to 0 to keep backups indefinitely; any other value (1–100) will automatically delete the oldest backups once the limit is reached. When using password-based encryption, select a credential from the management system. When using GPG encryption, select an available GPG key. The encryption type is stored in the global settings and determines which credential or key ID is used for future backups. Backups include ssh-host-keys.properties , so normalized host:port trust decisions shared by interactive Terminal, SFTP, and Mosh bootstrap connections survive restore; the transient cross-process .lock file is not included. JobScheduler host-key pins remain separately stored in job-scheduler.xml . Local-AI backups include llm/models.xml and rag/stores.json so model registrations, role assignments, and knowledge-source definitions can be restored. They exclude GGUF weights, llama.cpp runtime packages, temporary sidecar data, source documents, and regenerable HNSW snapshots; see Backup & restore .","title": "Backup"},{"location": "reference/settings/colors.html","text": "Colors Configure terminal display colors including text, background, cursor, selection, and the 16-color ANSI palette. Open via Configuration → Global Settings → Colors ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Color Profile dropdown Theme names (e.g., GitHub Dark, Dracula, etc.) GitHub Dark themeId Text Color color RGB hex color #FFFFFF foregroundColor Background color RGB hex color #1E1E1E backgroundColor Cursor color RGB hex color #FFFFFF cursorColor Cursor blinks toggle — On terminalCursorBlink (mirrored into cursorStyle ) Selection color RGB hex color #3399FF selectionColor Enable terminal colors toggle — On terminalColorsEnabled Normal: Black color RGB hex color #000000 ansiBlack Normal: Red color RGB hex color #CD0000 ansiRed Normal: Green color RGB hex color #00CD00 ansiGreen Normal: Yellow color RGB hex color #CDCD00 ansiYellow Normal: Blue color RGB hex color #0000EE ansiBlue Normal: Magenta color RGB hex color #CD00CD ansiMagenta Normal: Cyan color RGB hex color #00CDCD ansiCyan Normal: White color RGB hex color #E5E5E5 ansiWhite Bright: Black color RGB hex color #7F7F7F ansiBrightBlack Bright: Red color RGB hex color #FF0000 ansiBrightRed Bright: Green color RGB hex color #00FF00 ansiBrightGreen Bright: Yellow color RGB hex color #FFFF00 ansiBrightYellow Bright: Blue color RGB hex color #5C5CFF ansiBrightBlue Bright: Magenta color RGB hex color #FF00FF ansiBrightMagenta Bright: Cyan color RGB hex color #00FFFF ansiBrightCyan Bright: White color RGB hex color #FFFFFF ansiBrightWhite Notes Color Profiles Color Profile allows you to select a preset theme that applies foreground, background, cursor, and cursor shape settings at once. Switching profiles will update the individual color controls. If Apply Profile is available, it will reset colors to the selected theme's defaults. Cursor blinks Cursor blinks is your own preference and is kept when you switch color profiles: a profile contributes the cursor shape (block, underline, vertical bar), while the blinking on/off state stays as you set it. The terminal reads the flag as part of cursorStyle (for example STEADY_BLOCK when blinking is off), but your choice is stored separately as terminalCursorBlink and wins over whatever style a profile, a theme or a per-connection setting carries — so switching profiles cannot turn blinking back on, and the setting survives restarts. A settings file written before that field existed keeps working: the choice is then read out of the stored cursorStyle . ANSI Colors The Normal and Bright color palettes define the 16 ANSI colors (0–7 normal, 8–15 bright) used when Enable terminal colors is on. Each set of 8 colors corresponds to black, red, green, yellow, blue, magenta, cyan, and white. When terminal colors are disabled, only the configured Text Color and Background are used, ignoring all ANSI and TrueColor sequences.","title": "Colors"},{"location": "reference/settings/colors.html#colors","text": "Configure terminal display colors including text, background, cursor, selection, and the 16-color ANSI palette. Open via Configuration → Global Settings → Colors ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Color Profile dropdown Theme names (e.g., GitHub Dark, Dracula, etc.) GitHub Dark themeId Text Color color RGB hex color #FFFFFF foregroundColor Background color RGB hex color #1E1E1E backgroundColor Cursor color RGB hex color #FFFFFF cursorColor Cursor blinks toggle — On terminalCursorBlink (mirrored into cursorStyle ) Selection color RGB hex color #3399FF selectionColor Enable terminal colors toggle — On terminalColorsEnabled Normal: Black color RGB hex color #000000 ansiBlack Normal: Red color RGB hex color #CD0000 ansiRed Normal: Green color RGB hex color #00CD00 ansiGreen Normal: Yellow color RGB hex color #CDCD00 ansiYellow Normal: Blue color RGB hex color #0000EE ansiBlue Normal: Magenta color RGB hex color #CD00CD ansiMagenta Normal: Cyan color RGB hex color #00CDCD ansiCyan Normal: White color RGB hex color #E5E5E5 ansiWhite Bright: Black color RGB hex color #7F7F7F ansiBrightBlack Bright: Red color RGB hex color #FF0000 ansiBrightRed Bright: Green color RGB hex color #00FF00 ansiBrightGreen Bright: Yellow color RGB hex color #FFFF00 ansiBrightYellow Bright: Blue color RGB hex color #5C5CFF ansiBrightBlue Bright: Magenta color RGB hex color #FF00FF ansiBrightMagenta Bright: Cyan color RGB hex color #00FFFF ansiBrightCyan Bright: White color RGB hex color #FFFFFF ansiBrightWhite","title": "Colors"},{"location": "reference/settings/colors.html#notes","text": "Color Profiles Color Profile allows you to select a preset theme that applies foreground, background, cursor, and cursor shape settings at once. Switching profiles will update the individual color controls. If Apply Profile is available, it will reset colors to the selected theme's defaults. Cursor blinks Cursor blinks is your own preference and is kept when you switch color profiles: a profile contributes the cursor shape (block, underline, vertical bar), while the blinking on/off state stays as you set it. The terminal reads the flag as part of cursorStyle (for example STEADY_BLOCK when blinking is off), but your choice is stored separately as terminalCursorBlink and wins over whatever style a profile, a theme or a per-connection setting carries — so switching profiles cannot turn blinking back on, and the setting survives restarts. A settings file written before that field existed keeps working: the choice is then read out of the stored cursorStyle . ANSI Colors The Normal and Bright color palettes define the 16 ANSI colors (0–7 normal, 8–15 bright) used when Enable terminal colors is on. Each set of 8 colors corresponds to black, red, green, yellow, blue, magenta, cyan, and white. When terminal colors are disabled, only the configured Text Color and Background are used, ignoring all ANSI and TrueColor sequences.","title": "Notes"},{"location": "reference/settings/editor.html","text": "Editor Cursor appearance for korTTY's file editor tabs. Open via Configuration → Global Settings → Editor ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Cursor Style dropdown BLOCK, LINE, UNDERSCORE BLOCK editorCursorStyle Cursor Color color — #FF0000 editorCursorColor Cursor styles BLOCK is a wide cursor (2 px), LINE a thin vertical line (1 px), UNDERSCORE a medium width (1.5 px). Everything else is inherited The editor uses the same font family, font size, foreground and background colors that terminals use, so those are configured on the Appearance and Colors tabs rather than here. The Snippet Editor tab can override them for snippet windows.","title": "Editor"},{"location": "reference/settings/editor.html#editor","text": "Cursor appearance for korTTY's file editor tabs. Open via Configuration → Global Settings → Editor ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Cursor Style dropdown BLOCK, LINE, UNDERSCORE BLOCK editorCursorStyle Cursor Color color — #FF0000 editorCursorColor Cursor styles BLOCK is a wide cursor (2 px), LINE a thin vertical line (1 px), UNDERSCORE a medium width (1.5 px). Everything else is inherited The editor uses the same font family, font size, foreground and background colors that terminals use, so those are configured on the Appearance and Colors tabs rather than here. The Snippet Editor tab can override them for snippet windows.","title": "Editor"},{"location": "reference/settings/export.html","text": "Export Watermark and footer of the documents korTTY exports — session journals and AI chats alike. Open via Configuration → Global Settings → Export ; stored in ~/.kortty/global-settings.xml . AI chat PDFs embed fallback fonts for Unicode symbols and emoji, so characters such as ✓ , ★ , 😀 , and 🚀 remain visible and searchable in the exported document instead of being replaced with question marks. Setting Type Values Default Stored as Add a watermark to exported PDFs boolean on/off off pdfWatermarkEnabled Watermark text text — Built-in korTTY watermark pdfWatermarkText Watermark colour colour — Grey ( #6b7280 ) pdfWatermarkColor Show a footer in exported documents boolean on/off on exportFooterEnabled Footer text text — Built-in brand line exportFooterText Watermark The watermark is off by default — a document gets marked when you decide it should be. Once enabled, it is drawn faintly and diagonally across the middle of every PDF page, sized to fit the page width, in the colour you pick. It applies to session journal and AI chat PDF exports. Leaving the text field empty uses the built-in korTTY watermark, which additionally prints the project repository link beneath it. A text of your own is used verbatim — nothing is appended to it. Tip A watermark such as CONFIDENTIAL or your organization's name is a visual marker, not protection. Anyone can remove it from a PDF. For journals that must not be readable by others, export an encrypted archive instead. Footer The footer is on by default and appears in every exported format that has one: the bottom line of each PDF page, the last line of a Markdown export, and the footer of the exported journal page. Page numbers stay in the PDF footer regardless of this setting. Leaving the text field empty uses the built-in line, which names korTTY and appends the repository link (clickable in the PDF). A text of your own replaces it completely, without the link. Turning the footer off removes it from all of those formats.","title": "Export"},{"location": "reference/settings/export.html#export","text": "Watermark and footer of the documents korTTY exports — session journals and AI chats alike. Open via Configuration → Global Settings → Export ; stored in ~/.kortty/global-settings.xml . AI chat PDFs embed fallback fonts for Unicode symbols and emoji, so characters such as ✓ , ★ , 😀 , and 🚀 remain visible and searchable in the exported document instead of being replaced with question marks. Setting Type Values Default Stored as Add a watermark to exported PDFs boolean on/off off pdfWatermarkEnabled Watermark text text — Built-in korTTY watermark pdfWatermarkText Watermark colour colour — Grey ( #6b7280 ) pdfWatermarkColor Show a footer in exported documents boolean on/off on exportFooterEnabled Footer text text — Built-in brand line exportFooterText","title": "Export"},{"location": "reference/settings/export.html#watermark","text": "The watermark is off by default — a document gets marked when you decide it should be. Once enabled, it is drawn faintly and diagonally across the middle of every PDF page, sized to fit the page width, in the colour you pick. It applies to session journal and AI chat PDF exports. Leaving the text field empty uses the built-in korTTY watermark, which additionally prints the project repository link beneath it. A text of your own is used verbatim — nothing is appended to it. Tip A watermark such as CONFIDENTIAL or your organization's name is a visual marker, not protection. Anyone can remove it from a PDF. For journals that must not be readable by others, export an encrypted archive instead.","title": "Watermark"},{"location": "reference/settings/export.html#footer","text": "The footer is on by default and appears in every exported format that has one: the bottom line of each PDF page, the last line of a Markdown export, and the footer of the exported journal page. Page numbers stay in the PDF footer regardless of this setting. Leaving the text field empty uses the built-in line, which names korTTY and appends the repository link (clickable in the PDF). A text of your own replaces it completely, without the link. Turning the footer off removes it from all of those formats.","title": "Footer"},{"location": "reference/settings/language.html","text": "Language Configure the user interface language for korTTY. Supports 8 built-in languages plus automatic detection based on your system settings. Open via Configuration → Global Settings → Language ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Select Language: dropdown Auto-detect (System Language), English, German, Italian, Spanish, Portuguese, French, Croatian, Dutch Auto-detect (System Language) language Note Language changes take effect after the application is restarted. The setting will apply to the user interface labels, menus, and dialogs the next time korTTY launches. Date and number formats The installed application bundles Java locale data only for the 8 supported interface languages. If your operating system runs in a locale outside this list, dates and numbers are formatted using English conventions.","title": "Language"},{"location": "reference/settings/language.html#language","text": "Configure the user interface language for korTTY. Supports 8 built-in languages plus automatic detection based on your system settings. Open via Configuration → Global Settings → Language ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Select Language: dropdown Auto-detect (System Language), English, German, Italian, Spanish, Portuguese, French, Croatian, Dutch Auto-detect (System Language) language Note Language changes take effect after the application is restarted. The setting will apply to the user interface labels, menus, and dialogs the next time korTTY launches. Date and number formats The installed application bundles Java locale data only for the 8 supported interface languages. If your operating system runs in a locale outside this list, dates and numbers are formatted using English conventions.","title": "Language"},{"location": "reference/settings/logging.html","text": "Logging Configure terminal session logging, including where logs are stored and how long they are retained. Open via Configuration → Global Settings → Logging ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Log directory path — ~/.kortty/logs logDirectoryPath Keep logs number 0–3650 days 7 days logRetentionDays Note Log retention : Set to 0 for unlimited retention; otherwise, log archives older than the specified number of days are deleted automatically. Archives older than 24 hours are compressed automatically. Session Journal The same tab also holds the global session journal settings: the journal storage folder, whether AI summaries are generated by default, the default summary interval and the AI profile used for summaries. They are documented on the session journal page.","title": "Logging"},{"location": "reference/settings/logging.html#logging","text": "Configure terminal session logging, including where logs are stored and how long they are retained. Open via Configuration → Global Settings → Logging ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Log directory path — ~/.kortty/logs logDirectoryPath Keep logs number 0–3650 days 7 days logRetentionDays Note Log retention : Set to 0 for unlimited retention; otherwise, log archives older than the specified number of days are deleted automatically. Archives older than 24 hours are compressed automatically.","title": "Logging"},{"location": "reference/settings/logging.html#session-journal","text": "The same tab also holds the global session journal settings: the journal storage folder, whether AI summaries are generated by default, the default summary interval and the AI profile used for summaries. They are documented on the session journal page.","title": "Session Journal"},{"location": "reference/settings/resources.html","text": "Resources Choose how much memory korTTY may use. The default keeps a low, bounded footprint; the other profiles let the packaged application use more of your machine's resources for very large sessions (huge scrollback, many split panes, long AI chats). Open via Configuration → Global Settings → Resources ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Resource profile: dropdown Balanced, High, Maximum Balanced jvmResourceProfile Profiles Profile Heap limit Garbage collector Relaunch Balanced (recommended) Fixed 2 GB G1, with idle memory return No High ~50% of physical RAM G1 Yes, once at startup Maximum ~75% of physical RAM Z Garbage Collector (low pause) Yes, once at startup The Resources tab shows your machine's detected memory and the approximate heap limit each profile would apply on it. Notes Applies to the packaged application only This setting is applied by the packaged app (the .dmg / .msi /AppImage build), which briefly relaunches itself once at startup to switch the heap size and garbage collector — the Java runtime cannot change these while running, and editing the signed application bundle would break its signature. When korTTY is started from the plain .jar , set JVM options yourself (for example -Xmx8g ) instead. Takes effect after a restart Changing the profile takes effect the next time korTTY starts. The Balanced default never relaunches; High and Maximum relaunch once per start, so their cold start is slightly slower. Leave headroom for the rest of your system Higher profiles let korTTY reserve much more memory. Terminal and editor rendering (the embedded browser engines) also use memory outside the Java heap, so the Maximum profile deliberately caps the heap at about three quarters of RAM rather than removing the limit entirely — a truly unbounded heap could starve the operating system.","title": "Resources"},{"location": "reference/settings/resources.html#resources","text": "Choose how much memory korTTY may use. The default keeps a low, bounded footprint; the other profiles let the packaged application use more of your machine's resources for very large sessions (huge scrollback, many split panes, long AI chats). Open via Configuration → Global Settings → Resources ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Resource profile: dropdown Balanced, High, Maximum Balanced jvmResourceProfile","title": "Resources"},{"location": "reference/settings/resources.html#profiles","text": "Profile Heap limit Garbage collector Relaunch Balanced (recommended) Fixed 2 GB G1, with idle memory return No High ~50% of physical RAM G1 Yes, once at startup Maximum ~75% of physical RAM Z Garbage Collector (low pause) Yes, once at startup The Resources tab shows your machine's detected memory and the approximate heap limit each profile would apply on it.","title": "Profiles"},{"location": "reference/settings/resources.html#notes","text": "Applies to the packaged application only This setting is applied by the packaged app (the .dmg / .msi /AppImage build), which briefly relaunches itself once at startup to switch the heap size and garbage collector — the Java runtime cannot change these while running, and editing the signed application bundle would break its signature. When korTTY is started from the plain .jar , set JVM options yourself (for example -Xmx8g ) instead. Takes effect after a restart Changing the profile takes effect the next time korTTY starts. The Balanced default never relaunches; High and Maximum relaunch once per start, so their cold start is slightly slower. Leave headroom for the rest of your system Higher profiles let korTTY reserve much more memory. Terminal and editor rendering (the embedded browser engines) also use memory outside the Java heap, so the Maximum profile deliberately caps the heap at about three quarters of RAM rather than removing the limit entirely — a truly unbounded heap could starve the operating system.","title": "Notes"},{"location": "reference/settings/security.html","text": "Security This tab manages password vault security and SSH key authentication options. Open via Configuration → Global Settings → Security ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Change Master Password button — — — Require master password on startup toggle — On requireMasterPasswordOnStartup Disable master password prompt on startup (auto-login) toggle — Off skipMasterPasswordPrompt Enable temporary SSH key option toggle — Off temporarySshKeyEnabled Master password on startup If \"Require master password on startup\" is disabled, encrypted passwords and SSH keys cannot be automatically decrypted without manual password entry. This is a security risk and should only be disabled if you understand the consequences. Auto-login stores your master password on disk Enabling \"Disable master password prompt on startup\" makes korTTY remember your master password — stored only obfuscated (not securely encrypted) in ~/.kortty/master.autounlock with owner-only file permissions — and unlock the vault automatically on every start, with no dialog. Unlike disabling the option above, encrypted data (AI profiles, SSH passwords, credentials) stays usable, because the vault is actually unlocked. The trade-off: anyone who can read your ~/.kortty folder or a backup can then decrypt all saved passwords, SSH keys and API keys — the file permissions are the only remaining protection. On a brand-new profile korTTY sets up a default password automatically, so the app can start with no input at all. korTTY asks you to confirm before enabling it, and it is intended only for throwaway/test environments such as a VM. While auto-login is enabled, Require master password on startup is switched off and greyed out — the two options are mutually exclusive. An enterprise policy that requires a master password overrides this option. Unattended first launch (automation / test VMs) To bring korTTY up with no dialog at all — even on a brand-new profile that has never been unlocked — create ~/.kortty/global-settings.xml before the first launch with the option already enabled: <?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?> <globalSettings> <skipMasterPasswordPrompt> true </skipMasterPasswordPrompt> </globalSettings> On that first start korTTY sets up the default master password kortty-auto automatically and unlocks the vault, so scripted or CI runs need no interaction. If you later turn auto-login off on such a profile, kortty-auto is the password to enter — change it immediately via Change Master Password if the profile is meant to live on. Intended for disposable/test environments only — see the security warning above. Change Master Password button Opens a dialog asking for the Current Password , the New Password (at least 6 characters) and a Confirm New Password repetition; Change applies it. korTTY rejects a wrong current password, a new password under six characters, and a mismatch between the two new entries, each with its own message. On success it reports how many secrets were re-encrypted: every master-password-protected secret is decrypted with the old password and re-encrypted with the new one — connection passwords and key passphrases, jump-server passwords, SSH-key passphrases, stored credentials, AI-profile API keys and the other AI/translation keys, and RAG and Job Scheduler secrets — so nothing becomes unreadable after the change. The new password only takes over once every store has been migrated, so an error part-way through leaves the old password in effect instead of locking you out of half your data. If an individual secret cannot be migrated (for example one that was encrypted with a different password), korTTY leaves that value untouched, reports how many items were affected, and notes them in the log — those secrets keep the old password and must be re-entered manually. With auto-login enabled, the remembered password in ~/.kortty/master.autounlock is rewritten to the new master password, so automatic unlocking keeps working after the change.","title": "Security"},{"location": "reference/settings/security.html#security","text": "This tab manages password vault security and SSH key authentication options. Open via Configuration → Global Settings → Security ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Change Master Password button — — — Require master password on startup toggle — On requireMasterPasswordOnStartup Disable master password prompt on startup (auto-login) toggle — Off skipMasterPasswordPrompt Enable temporary SSH key option toggle — Off temporarySshKeyEnabled Master password on startup If \"Require master password on startup\" is disabled, encrypted passwords and SSH keys cannot be automatically decrypted without manual password entry. This is a security risk and should only be disabled if you understand the consequences. Auto-login stores your master password on disk Enabling \"Disable master password prompt on startup\" makes korTTY remember your master password — stored only obfuscated (not securely encrypted) in ~/.kortty/master.autounlock with owner-only file permissions — and unlock the vault automatically on every start, with no dialog. Unlike disabling the option above, encrypted data (AI profiles, SSH passwords, credentials) stays usable, because the vault is actually unlocked. The trade-off: anyone who can read your ~/.kortty folder or a backup can then decrypt all saved passwords, SSH keys and API keys — the file permissions are the only remaining protection. On a brand-new profile korTTY sets up a default password automatically, so the app can start with no input at all. korTTY asks you to confirm before enabling it, and it is intended only for throwaway/test environments such as a VM. While auto-login is enabled, Require master password on startup is switched off and greyed out — the two options are mutually exclusive. An enterprise policy that requires a master password overrides this option. Unattended first launch (automation / test VMs) To bring korTTY up with no dialog at all — even on a brand-new profile that has never been unlocked — create ~/.kortty/global-settings.xml before the first launch with the option already enabled: <?xml version=\"1.0\" encoding=\"UTF-8\" standalone=\"yes\"?> <globalSettings> <skipMasterPasswordPrompt> true </skipMasterPasswordPrompt> </globalSettings> On that first start korTTY sets up the default master password kortty-auto automatically and unlocks the vault, so scripted or CI runs need no interaction. If you later turn auto-login off on such a profile, kortty-auto is the password to enter — change it immediately via Change Master Password if the profile is meant to live on. Intended for disposable/test environments only — see the security warning above. Change Master Password button Opens a dialog asking for the Current Password , the New Password (at least 6 characters) and a Confirm New Password repetition; Change applies it. korTTY rejects a wrong current password, a new password under six characters, and a mismatch between the two new entries, each with its own message. On success it reports how many secrets were re-encrypted: every master-password-protected secret is decrypted with the old password and re-encrypted with the new one — connection passwords and key passphrases, jump-server passwords, SSH-key passphrases, stored credentials, AI-profile API keys and the other AI/translation keys, and RAG and Job Scheduler secrets — so nothing becomes unreadable after the change. The new password only takes over once every store has been migrated, so an error part-way through leaves the old password in effect instead of locking you out of half your data. If an individual secret cannot be migrated (for example one that was encrypted with a different password), korTTY leaves that value untouched, reports how many items were affected, and notes them in the log — those secrets keep the old password and must be re-entered manually. With auto-login enabled, the remembered password in ~/.kortty/master.autounlock is rewritten to the new master password, so automatic unlocking keeps working after the change.","title": "Security"},{"location": "reference/settings/sftp.html","text": "SFTP Manager Defaults for the dual-panel SFTP file manager and for the JobScheduler's Rsync jobs. Open via Configuration → Global Settings → SFTP Manager ; stored in ~/.kortty/global-settings.xml . SFTP Manager Settings Setting Type Values Default Stored as Auto-close SFTP tabs after inactivity toggle — Off sftpAutoCloseMinutes (unset or 0 ) Timeout (minutes) number 1–120 10 sftpAutoCloseMinutes Auto-close An idle SFTP tab closes itself after the timeout, which frees the server-side connection when you forget to close the manager. The timeout field is only editable while the toggle is on, and the two share one stored value: switching the toggle off stores no timeout at all. ZIP Creation Settings These are the defaults for creating a ZIP archive on the remote server from the SFTP manager. Setting Type Values Default Stored as Default ZIP path text Remote absolute path /tmp sftpDefaultZipPath Default compression (0-9) number 0–9 6 sftpDefaultZipCompression Compression levels 0 means no compression (fastest) and 9 the best compression (slowest). JobScheduler Rsync Setting Type Values Default Stored as Rsync binary path path — empty (resolve rsync from PATH ) jobSchedulerRsyncBinaryPath Requirements Leave the path empty to use the rsync found on PATH . Browse picks a binary explicitly. Rsync jobs additionally require ssh to be available on PATH ; see JobScheduler .","title": "SFTP Manager"},{"location": "reference/settings/sftp.html#sftp-manager","text": "Defaults for the dual-panel SFTP file manager and for the JobScheduler's Rsync jobs. Open via Configuration → Global Settings → SFTP Manager ; stored in ~/.kortty/global-settings.xml .","title": "SFTP Manager"},{"location": "reference/settings/sftp.html#sftp-manager-settings","text": "Setting Type Values Default Stored as Auto-close SFTP tabs after inactivity toggle — Off sftpAutoCloseMinutes (unset or 0 ) Timeout (minutes) number 1–120 10 sftpAutoCloseMinutes Auto-close An idle SFTP tab closes itself after the timeout, which frees the server-side connection when you forget to close the manager. The timeout field is only editable while the toggle is on, and the two share one stored value: switching the toggle off stores no timeout at all.","title": "SFTP Manager Settings"},{"location": "reference/settings/sftp.html#zip-creation-settings","text": "These are the defaults for creating a ZIP archive on the remote server from the SFTP manager. Setting Type Values Default Stored as Default ZIP path text Remote absolute path /tmp sftpDefaultZipPath Default compression (0-9) number 0–9 6 sftpDefaultZipCompression Compression levels 0 means no compression (fastest) and 9 the best compression (slowest).","title": "ZIP Creation Settings"},{"location": "reference/settings/sftp.html#jobscheduler-rsync","text": "Setting Type Values Default Stored as Rsync binary path path — empty (resolve rsync from PATH ) jobSchedulerRsyncBinaryPath Requirements Leave the path empty to use the rsync found on PATH . Browse picks a binary explicitly. Rsync jobs additionally require ssh to be available on PATH ; see JobScheduler .","title": "JobScheduler Rsync"},{"location": "reference/settings/terminal.html","text": "Terminal Configure terminal display and behavior settings, including dimensions, scrollback, character encoding, and SSH connection management. Open via Configuration → Global Settings → Terminal ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Columns: number 40–500 80 terminalColumns Rows: number 10–200 24 terminalRows Scrollback: number 100–100,000 10,000 scrollbackLines Encoding: dropdown UTF-8, ISO-8859-1, ISO-8859-15, Windows-1252 UTF-8 encoding Bold as bright color toggle — On boldAsBright Show scrollbar in terminal toggle — On showTerminalScrollbar Show command timestamps toggle — Off commandTimestampsEnabled Allow drag-and-drop file copy into terminal toggle — On terminalDragDropEnabled Copy selection to clipboard automatically toggle — On terminalCopyOnSelectEnabled Close active terminal windows without confirmation toggle — Off closeActiveTerminalWindowsWithoutConfirmation Enable SSH Keep-Alive toggle — On sshKeepAliveEnabled Interval (seconds): number 5–600 60 sshKeepAliveInterval Enable connection retries toggle — On connectionRetriesEnabled Automatically reconnect lost connections toggle — On autoReconnectEnabled Disable host key verification for all connections toggle — Off hostKeyCheckDisabledForAllConnections Detect coding agents (Claude Code, Codex, Gemini CLI) in local shell tabs toggle — On codingAgentDetectionEnabled Desktop notification when a coding agent needs a decision or finishes while you are not looking at its pane toggle — On codingAgentNotificationsEnabled Show the number of agents waiting for a decision on the app icon toggle — On codingAgentAppBadgeEnabled Allow a local program to read and control this korTTY toggle — Off controlApiEnabled Notes Scrollback Controls how many lines of output each terminal pane keeps in its scrollback buffer. The value is read when a terminal is created, so a change applies to newly opened tabs and split panes — already-open terminals keep their current buffer size. Larger values use more memory per pane. SSH Keep-Alive When enabled, korTTY sends periodic keep-alive packets to prevent SSH sessions from timing out during idle periods. The interval setting controls how often (in seconds) these packets are sent. The spinner range is 5–600 seconds; the interval is disabled if SSH Keep-Alive is toggled off. Disable host key verification for all connections This is the global, lowest-precedence host-key setting: it relaxes verification to accept-new for every connection that does not set its own or its group's override. Accept-new still hard-blocks a changed key on a host already pinned, and a jump server's own key is always verified strictly — but disabling first-use verification removes protection against a man-in-the-middle on the very first connection. Off by default. Per-connection and per-group overrides are set in the Connection Manager; see Security → Relaxing host-key verification . Drag-and-Drop File Copy When enabled, you can drop files or folders from your file manager (Finder on macOS, Explorer on Windows) directly into the terminal window. The files will be copied to the remote SSH server via SFTP. Command Timestamps When enabled, a sidebar appears on the left side of the terminal displaying the date and time each command was entered, useful for audit trails and session logging. Connection Retries When enabled, failed SSH connections are automatically retried. Disabling this prevents automatic reconnection attempts for failed connections. Retries only cover failures that a further attempt could resolve. A changed host key, a Mosh connection configured with a jump server, or a missing Mosh runtime is refused immediately regardless of this setting. Automatically reconnect lost connections When enabled and an established SSH connection is lost (network drop, server gone), the tab reconnects on its own with increasing delays — 3, 5, 10, 20, 30, then every 60 seconds — and the red status bar counts down to the next attempt. A double-click on the bar still reconnects immediately, and a successful reconnect or closing the tab stops the automatic attempts. Failed logins and other permanent failures (authentication, host key, configuration) are never retried automatically, and a connection that never got established is not retried by this setting either — that is what Enable connection retries covers. See Terminal sessions → Connection loss . Allow a local program to read and control this korTTY This is the Control API, and it is the only setting in this tab that is off by default. While it is on, any program running under your user account on this computer can list your windows, read every open pane — local shells and SSH sessions alike — and type into them, including pressing Enter . Nothing is reachable over the network and no other user of the computer can connect, but that is the extent of the boundary: within your own account it is the same power as sitting at your keyboard. A status line under the checkbox reports what the listener is actually doing, because the checkbox and the listener can legitimately disagree — enterprise policy can deny the feature, and a start can fail because another korTTY already owns the socket. Every action is logged with byte counts only, never terminal text, and the first time a program types into a pane you get one desktop notification. See Control API for the security model and Control CLI for the kortty-cli client that speaks it. Detect coding agents When enabled, korTTY watches every local shell pane for a running Claude Code, Codex or Gemini CLI and tracks whether it is working, blocked on a question, done or idle. The screen is analysed locally and nothing leaves the computer; the change applies immediately to open tabs. The two toggles below it control the desktop notification for an agent that needs a decision or finishes while you are not looking at its pane, and the count of waiting agents on the app icon (or in the window title where no icon badge exists); both read the setting live, so a change applies at once. See Coding agents .","title": "Terminal"},{"location": "reference/settings/terminal.html#terminal","text": "Configure terminal display and behavior settings, including dimensions, scrollback, character encoding, and SSH connection management. Open via Configuration → Global Settings → Terminal ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Columns: number 40–500 80 terminalColumns Rows: number 10–200 24 terminalRows Scrollback: number 100–100,000 10,000 scrollbackLines Encoding: dropdown UTF-8, ISO-8859-1, ISO-8859-15, Windows-1252 UTF-8 encoding Bold as bright color toggle — On boldAsBright Show scrollbar in terminal toggle — On showTerminalScrollbar Show command timestamps toggle — Off commandTimestampsEnabled Allow drag-and-drop file copy into terminal toggle — On terminalDragDropEnabled Copy selection to clipboard automatically toggle — On terminalCopyOnSelectEnabled Close active terminal windows without confirmation toggle — Off closeActiveTerminalWindowsWithoutConfirmation Enable SSH Keep-Alive toggle — On sshKeepAliveEnabled Interval (seconds): number 5–600 60 sshKeepAliveInterval Enable connection retries toggle — On connectionRetriesEnabled Automatically reconnect lost connections toggle — On autoReconnectEnabled Disable host key verification for all connections toggle — Off hostKeyCheckDisabledForAllConnections Detect coding agents (Claude Code, Codex, Gemini CLI) in local shell tabs toggle — On codingAgentDetectionEnabled Desktop notification when a coding agent needs a decision or finishes while you are not looking at its pane toggle — On codingAgentNotificationsEnabled Show the number of agents waiting for a decision on the app icon toggle — On codingAgentAppBadgeEnabled Allow a local program to read and control this korTTY toggle — Off controlApiEnabled","title": "Terminal"},{"location": "reference/settings/terminal.html#notes","text": "Scrollback Controls how many lines of output each terminal pane keeps in its scrollback buffer. The value is read when a terminal is created, so a change applies to newly opened tabs and split panes — already-open terminals keep their current buffer size. Larger values use more memory per pane. SSH Keep-Alive When enabled, korTTY sends periodic keep-alive packets to prevent SSH sessions from timing out during idle periods. The interval setting controls how often (in seconds) these packets are sent. The spinner range is 5–600 seconds; the interval is disabled if SSH Keep-Alive is toggled off. Disable host key verification for all connections This is the global, lowest-precedence host-key setting: it relaxes verification to accept-new for every connection that does not set its own or its group's override. Accept-new still hard-blocks a changed key on a host already pinned, and a jump server's own key is always verified strictly — but disabling first-use verification removes protection against a man-in-the-middle on the very first connection. Off by default. Per-connection and per-group overrides are set in the Connection Manager; see Security → Relaxing host-key verification . Drag-and-Drop File Copy When enabled, you can drop files or folders from your file manager (Finder on macOS, Explorer on Windows) directly into the terminal window. The files will be copied to the remote SSH server via SFTP. Command Timestamps When enabled, a sidebar appears on the left side of the terminal displaying the date and time each command was entered, useful for audit trails and session logging. Connection Retries When enabled, failed SSH connections are automatically retried. Disabling this prevents automatic reconnection attempts for failed connections. Retries only cover failures that a further attempt could resolve. A changed host key, a Mosh connection configured with a jump server, or a missing Mosh runtime is refused immediately regardless of this setting. Automatically reconnect lost connections When enabled and an established SSH connection is lost (network drop, server gone), the tab reconnects on its own with increasing delays — 3, 5, 10, 20, 30, then every 60 seconds — and the red status bar counts down to the next attempt. A double-click on the bar still reconnects immediately, and a successful reconnect or closing the tab stops the automatic attempts. Failed logins and other permanent failures (authentication, host key, configuration) are never retried automatically, and a connection that never got established is not retried by this setting either — that is what Enable connection retries covers. See Terminal sessions → Connection loss . Allow a local program to read and control this korTTY This is the Control API, and it is the only setting in this tab that is off by default. While it is on, any program running under your user account on this computer can list your windows, read every open pane — local shells and SSH sessions alike — and type into them, including pressing Enter . Nothing is reachable over the network and no other user of the computer can connect, but that is the extent of the boundary: within your own account it is the same power as sitting at your keyboard. A status line under the checkbox reports what the listener is actually doing, because the checkbox and the listener can legitimately disagree — enterprise policy can deny the feature, and a start can fail because another korTTY already owns the socket. Every action is logged with byte counts only, never terminal text, and the first time a program types into a pane you get one desktop notification. See Control API for the security model and Control CLI for the kortty-cli client that speaks it. Detect coding agents When enabled, korTTY watches every local shell pane for a running Claude Code, Codex or Gemini CLI and tracks whether it is working, blocked on a question, done or idle. The screen is analysed locally and nothing leaves the computer; the change applies immediately to open tabs. The two toggles below it control the desktop notification for an agent that needs a decision or finishes while you are not looking at its pane, and the count of waiting agents on the app icon (or in the window title where no icon badge exists); both read the setting live, so a change applies at once. See Coding agents .","title": "Notes"},{"location": "reference/settings/translation.html","text": "Translation Configure dynamic translation of korTTY's user interface using external translation APIs. This tab allows you to select a translation provider, authenticate with its API, and generate language files to display the UI in your target language. Open via Configuration → Global Settings → Translation ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as System language text — System locale — Translation API dropdown Google Translate, DeepL, LibreTranslate, Microsoft Translator, Yandex Translate, Local AI text profile Google Translate translationApiProvider AI profile dropdown \"Default (local text profile)\" plus every configured AI profile Default (local text profile) — API Key text — — encryptedTranslationApiKey API URL (optional) text — — (null = use provider default) translationApiUrl Azure region (optional) text — — (null = global resource) translationApiRegion Test API Connection button — — — Target language dropdown System locale and available locales (Locale objects) System locale — Generate Language File button — — — Generated languages list Available dynamically translated language files — — Delete button (removes selected generated language file) — — Regenerate outdated button (visible when outdated files exist) — — Note API Key storage: The API key is encrypted using your master password and stored securely in global-settings.xml . If the master password vault is locked, you will receive a warning and the key will be kept in the field until you can unlock the vault or set a master password in Settings. Note Generated languages: The \"Generated languages\" list displays language files that have been created via the Generate Language File button. Each generated file corresponds to a dynamically translated UI in that target language. Use the Delete button to remove a language file, or use Regenerate outdated to update files created with an older app version to include newly added translation keys. Provider credentials and endpoints Each keyed provider expects its own kind of credential in the API Key field. The API URL field stays empty unless you deliberately point korTTY at a different host — a self-hosted LibreTranslate, a regional Azure endpoint, or a proxy. Provider API version korTTY calls What goes in API Key Default endpoint Google Translate Cloud Translation v2 (Basic) A Google Cloud API key with the Cloud Translation API enabled https://translation.googleapis.com/language/translate/v2 DeepL DeepL API v2 Your DeepL auth key, sent as DeepL-Auth-Key https://api.deepl.com/v2/translate , or https://api-free.deepl.com/v2/translate for a free key LibreTranslate LibreTranslate /translate Optional; required by public instances that enforce quotas https://libretranslate.com Microsoft Translator Azure AI Translator v3.0 The Azure Translator resource subscription key https://api.cognitive.microsofttranslator.com Yandex Yandex Cloud Translate v2 A Yandex Cloud service account API key , sent as Api-Key https://translate.api.cloud.yandex.net/translate/v2 Yandex: the v1.5 API is no longer usable Yandex stopped issuing keys for the old Translate API v1.5 ( translate.yandex.net/api/v1.5 ) and switched off the free keys already in circulation. The host still answers, but no key you can obtain authenticates against it. Create a service account in the Yandex Cloud console, assign it the ai.translate.user role, issue an API key for that account, and store that key here. Leave API URL empty: an address still pointing at the v1.5 host is ignored, and korTTY writes a warning to the log until you clear it. The folder is implied by the service account, so nothing else has to be configured. Note DeepL Free vs Pro: korTTY guesses the endpoint from the key — historically only Free keys end in :fx — and corrects itself once if the other endpoint turns out to be the right one. Setting API URL explicitly pins the endpoint and disables that correction. Note Regional Azure resources: the Azure region row appears only while Microsoft Translator is selected, because only that provider reads it. Leave it empty for a resource created in the Global region. A resource created in a specific region — or one with a custom domain or a virtual network — rejects any call that does not name its region, so enter the region shown on the resource's Keys and Endpoint page, for example germanywestcentral . A custom-domain resource also needs its full path in API URL : https://<resource>.cognitiveservices.azure.com/translator/text/v3.0 . Local translation Choose Local AI text profile to translate through an AI profile instead of a translation API. API URL and API Key are disabled for this provider because the profile brings its own endpoint and credentials. The model must return one strict JSON translations array with the same number and order of input strings; a batch that comes back malformed is retried and split in half rather than aborting the run, and a string the model never returns in usable form keeps its English text. The AI profile dropdown next to it decides which profile does the work: A profile you pick is always used, whatever its connection mode — an embedded llama.cpp or MLX model, a cloud endpoint such as Anthropic or an OpenAI-compatible API, or a local CLI profile. Its API key is resolved the same way the rest of the application resolves one, so the master-password vault must be unlocked when the profile needs an encrypted secret. Default (local text profile) uses the profile assigned to the Text/translation role in AI > AI Manager , and only if that profile runs an embedded (local) model. The default never falls back to a cloud profile automatically: this provider exists for people who cannot or do not want to send their interface strings to an external service. If that role holds a cloud or CLI profile, korTTY names it and directs you to this dropdown instead of failing with a generic error. This mirrors the AI profile dropdown in the guide-translation section below, so both translation jobs can be pointed at the model you want — for example a small local model for the interface strings and a stronger cloud model for the guide, or the same profile for both. Warning Credentials: External providers require their normal API key, except a LibreTranslate endpoint that is explicitly configured without one. Local AI requires no translation-provider key, but an embedded model needs its GGUF/MLX model and runtime installed, and the master-password vault must be unlocked when the selected AI profile needs any encrypted secret. Guide translation A second, independent job below translates the bundled offline guide — the same site this page is part of — into the target language selected above, so the in-app Help → Guide window and its search can be read fully in that language rather than only the interface labels. Setting Type Values Default Stored as AI profile dropdown \"Default (text profile)\" plus every configured AI profile Default (text profile) — Estimate duration button — — — Start translation button — — — Cancel button (shown while a translation or a duration estimate is running) — — Translated guides list Guide languages translated so far — — Delete button (removes the selected translated guide, its assets and its search index) — — Picking a profile in the AI profile dropdown here always uses that profile, whatever its connection mode. Leaving it on Default (text profile) follows the Translation API selection above: with the provider set to Local AI text profile , the guide uses the profile assigned to the Text/translation role in AI Manager — and only if that profile runs an embedded (local) model — while any other provider with a stored key translates the guide through that translation API instead. Start translation with no guide profile picked requires a workable Translation API above: with the provider on a keyed service (for example Google Translate) and no API key stored, starting is refused with \"Please enter an API key.\" — a picked AI profile always suffices on its own. If the run cannot start for another reason, the message names the cause: a picked profile whose model is not downloaded or selected, no guide profile and no API key, or no local text profile configured. Deleting a translated guide removes its whole directory under ~/.kortty/guide/<language>/ — the translated pages, the staged theme assets, its search index and its resume checkpoint — after a confirmation prompt. Why this runs in the background Translating the guide is not a quick action: the bundled site holds roughly 5,500 distinct pieces of text once repeated navigation and headings are deduplicated, and depending on the model this can take anywhere from a few minutes to most of a day. Starting it does not block the Settings dialog or the rest of korTTY: The translation keeps running after you close Settings, switch tabs, or continue working in terminals. A small progress indicator — a bar, a percentage and an estimated time remaining — appears at the right end of korTTY's menu bar for as long as a guide translation is in progress, and disappears again once it finishes. While the run is active, the dialog shows a progress bar and a status line (\"Translating… 42%\"). Cancel stops the job at the next safe point rather than losing what has already been translated: progress is checkpointed to disk as it goes, so starting the same language again continues from where it left off instead of starting over. The in-dialog progress display and Cancel belong to the Settings dialog that started the run — reopening Settings later does not re-attach to a running job, and Start translation then only reports \"A guide translation is already running.\" After closing Settings, the running job is paused via the quit dialog below (or simply left to finish). If you try to quit korTTY while a guide translation is running, a dialog offers to pause it and quit, or to keep korTTY open; choosing to pause leaves the checkpoint in place for the next run. After installing a korTTY update that changed the guide's content, and if you already have a translated guide for your language, korTTY offers once per run — a few seconds after the window opens, and never while a translation is already running — to bring it up to date; Update now opens this Settings tab, where you start the run yourself. Because progress is checkpointed by the exact English text of each piece, updating only re-translates sentences that actually changed in the release — everything else is reused as-is. Why a reasoning model is a poor fit Some AI models are built to \"think out loud\" before answering — writing an extended chain of reasoning into their reply before the actual output, a technique tuned for hard problems like math or coding. That reasoning is not part of the translation, but the model still has to generate it, and generating text is the slow part of running a local model. Measured on this project's own guide, a reasoning model produced about 4.4 completion tokens for every input token — roughly 4,400 tokens of reasoning to translate 1,000 characters of prose. On identical hardware this was the difference between a translation run of about an hour and one of six hours or more. If the AI profile you pick for guide translation is a reasoning model, korTTY warns you before starting or estimating, naming the model and letting you continue anyway or cancel. The check looks at the model's name (publishers of reasoning models advertise it there — \"reasoning\", \"thinking\", \"R1\", \"QwQ\", \"o1\" and similar are recognized), so it costs nothing to run and may occasionally miss a differently-named reasoning model, but it will not falsely warn about a plain instruct model. Suitable models are instruction-tuned (\"instruct\") chat models without a reasoning/thinking mode — the kind normally used for translation, summarization or rewriting rather than multi-step problem solving. For an embedded local profile, look for MLX or GGUF builds whose name includes \"instruct\" and not \"reasoning\", \"thinking\" or \"R1\", for example a Qwen2.5-Instruct or Phi-4-mini-instruct build at a size your hardware can run comfortably (4-bit quantized 7-8B models are a practical default on Apple Silicon). Larger instruct models generally translate more fluently at the cost of speed; the duration estimate below lets you compare before committing to a full run. Estimating how long a full translation will take Estimate duration measures the AI profile selected above against a real, representative sample of the guide's text and projects how long translating everything still outstanding would take, without committing to the full run: It sends one small, untimed \"warm-up\" request first, so that a local model's one-time loading cost — which is paid once for an entire run, not per batch — is not mistaken for ongoing translation speed and multiplied across every batch of the projection. It then translates one real, budget-sized batch of guide text and times it. This is a genuine translation, not a simulation: the sample is kept, so estimating first and then starting a full run does not repeat that work. From that single timed batch, it computes two projections for the remaining text — one assuming cost scales with the number of requests, one assuming it scales with the amount of text — and reports the resulting range together with how long the remaining pages and text would take. Reporting a range rather than one number reflects that a single sample cannot fully separate a model's fixed per-request overhead from its per-character cost; the estimate keeps that uncertainty visible rather than hiding it behind a single overly precise number. If the sample fails outright — the AI profile is unreachable, misconfigured, or produced nothing usable — the estimate reports that as a connection problem instead of a duration, so it is not mistaken for \"translation will be instant.\" And if nothing is left to translate, the estimate simply reports that the guide is already fully translated for this language. Note The reasoning-model warning above appears for Estimate duration as well as Start translation , since running an estimate against a reasoning model already spends real time on it.","title": "Translation"},{"location": "reference/settings/translation.html#translation","text": "Configure dynamic translation of korTTY's user interface using external translation APIs. This tab allows you to select a translation provider, authenticate with its API, and generate language files to display the UI in your target language. Open via Configuration → Global Settings → Translation ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as System language text — System locale — Translation API dropdown Google Translate, DeepL, LibreTranslate, Microsoft Translator, Yandex Translate, Local AI text profile Google Translate translationApiProvider AI profile dropdown \"Default (local text profile)\" plus every configured AI profile Default (local text profile) — API Key text — — encryptedTranslationApiKey API URL (optional) text — — (null = use provider default) translationApiUrl Azure region (optional) text — — (null = global resource) translationApiRegion Test API Connection button — — — Target language dropdown System locale and available locales (Locale objects) System locale — Generate Language File button — — — Generated languages list Available dynamically translated language files — — Delete button (removes selected generated language file) — — Regenerate outdated button (visible when outdated files exist) — — Note API Key storage: The API key is encrypted using your master password and stored securely in global-settings.xml . If the master password vault is locked, you will receive a warning and the key will be kept in the field until you can unlock the vault or set a master password in Settings. Note Generated languages: The \"Generated languages\" list displays language files that have been created via the Generate Language File button. Each generated file corresponds to a dynamically translated UI in that target language. Use the Delete button to remove a language file, or use Regenerate outdated to update files created with an older app version to include newly added translation keys.","title": "Translation"},{"location": "reference/settings/translation.html#provider-credentials-and-endpoints","text": "Each keyed provider expects its own kind of credential in the API Key field. The API URL field stays empty unless you deliberately point korTTY at a different host — a self-hosted LibreTranslate, a regional Azure endpoint, or a proxy. Provider API version korTTY calls What goes in API Key Default endpoint Google Translate Cloud Translation v2 (Basic) A Google Cloud API key with the Cloud Translation API enabled https://translation.googleapis.com/language/translate/v2 DeepL DeepL API v2 Your DeepL auth key, sent as DeepL-Auth-Key https://api.deepl.com/v2/translate , or https://api-free.deepl.com/v2/translate for a free key LibreTranslate LibreTranslate /translate Optional; required by public instances that enforce quotas https://libretranslate.com Microsoft Translator Azure AI Translator v3.0 The Azure Translator resource subscription key https://api.cognitive.microsofttranslator.com Yandex Yandex Cloud Translate v2 A Yandex Cloud service account API key , sent as Api-Key https://translate.api.cloud.yandex.net/translate/v2 Yandex: the v1.5 API is no longer usable Yandex stopped issuing keys for the old Translate API v1.5 ( translate.yandex.net/api/v1.5 ) and switched off the free keys already in circulation. The host still answers, but no key you can obtain authenticates against it. Create a service account in the Yandex Cloud console, assign it the ai.translate.user role, issue an API key for that account, and store that key here. Leave API URL empty: an address still pointing at the v1.5 host is ignored, and korTTY writes a warning to the log until you clear it. The folder is implied by the service account, so nothing else has to be configured. Note DeepL Free vs Pro: korTTY guesses the endpoint from the key — historically only Free keys end in :fx — and corrects itself once if the other endpoint turns out to be the right one. Setting API URL explicitly pins the endpoint and disables that correction. Note Regional Azure resources: the Azure region row appears only while Microsoft Translator is selected, because only that provider reads it. Leave it empty for a resource created in the Global region. A resource created in a specific region — or one with a custom domain or a virtual network — rejects any call that does not name its region, so enter the region shown on the resource's Keys and Endpoint page, for example germanywestcentral . A custom-domain resource also needs its full path in API URL : https://<resource>.cognitiveservices.azure.com/translator/text/v3.0 .","title": "Provider credentials and endpoints"},{"location": "reference/settings/translation.html#local-translation","text": "Choose Local AI text profile to translate through an AI profile instead of a translation API. API URL and API Key are disabled for this provider because the profile brings its own endpoint and credentials. The model must return one strict JSON translations array with the same number and order of input strings; a batch that comes back malformed is retried and split in half rather than aborting the run, and a string the model never returns in usable form keeps its English text. The AI profile dropdown next to it decides which profile does the work: A profile you pick is always used, whatever its connection mode — an embedded llama.cpp or MLX model, a cloud endpoint such as Anthropic or an OpenAI-compatible API, or a local CLI profile. Its API key is resolved the same way the rest of the application resolves one, so the master-password vault must be unlocked when the profile needs an encrypted secret. Default (local text profile) uses the profile assigned to the Text/translation role in AI > AI Manager , and only if that profile runs an embedded (local) model. The default never falls back to a cloud profile automatically: this provider exists for people who cannot or do not want to send their interface strings to an external service. If that role holds a cloud or CLI profile, korTTY names it and directs you to this dropdown instead of failing with a generic error. This mirrors the AI profile dropdown in the guide-translation section below, so both translation jobs can be pointed at the model you want — for example a small local model for the interface strings and a stronger cloud model for the guide, or the same profile for both. Warning Credentials: External providers require their normal API key, except a LibreTranslate endpoint that is explicitly configured without one. Local AI requires no translation-provider key, but an embedded model needs its GGUF/MLX model and runtime installed, and the master-password vault must be unlocked when the selected AI profile needs any encrypted secret.","title": "Local translation"},{"location": "reference/settings/translation.html#guide-translation","text": "A second, independent job below translates the bundled offline guide — the same site this page is part of — into the target language selected above, so the in-app Help → Guide window and its search can be read fully in that language rather than only the interface labels. Setting Type Values Default Stored as AI profile dropdown \"Default (text profile)\" plus every configured AI profile Default (text profile) — Estimate duration button — — — Start translation button — — — Cancel button (shown while a translation or a duration estimate is running) — — Translated guides list Guide languages translated so far — — Delete button (removes the selected translated guide, its assets and its search index) — — Picking a profile in the AI profile dropdown here always uses that profile, whatever its connection mode. Leaving it on Default (text profile) follows the Translation API selection above: with the provider set to Local AI text profile , the guide uses the profile assigned to the Text/translation role in AI Manager — and only if that profile runs an embedded (local) model — while any other provider with a stored key translates the guide through that translation API instead. Start translation with no guide profile picked requires a workable Translation API above: with the provider on a keyed service (for example Google Translate) and no API key stored, starting is refused with \"Please enter an API key.\" — a picked AI profile always suffices on its own. If the run cannot start for another reason, the message names the cause: a picked profile whose model is not downloaded or selected, no guide profile and no API key, or no local text profile configured. Deleting a translated guide removes its whole directory under ~/.kortty/guide/<language>/ — the translated pages, the staged theme assets, its search index and its resume checkpoint — after a confirmation prompt.","title": "Guide translation"},{"location": "reference/settings/translation.html#why-this-runs-in-the-background","text": "Translating the guide is not a quick action: the bundled site holds roughly 5,500 distinct pieces of text once repeated navigation and headings are deduplicated, and depending on the model this can take anywhere from a few minutes to most of a day. Starting it does not block the Settings dialog or the rest of korTTY: The translation keeps running after you close Settings, switch tabs, or continue working in terminals. A small progress indicator — a bar, a percentage and an estimated time remaining — appears at the right end of korTTY's menu bar for as long as a guide translation is in progress, and disappears again once it finishes. While the run is active, the dialog shows a progress bar and a status line (\"Translating… 42%\"). Cancel stops the job at the next safe point rather than losing what has already been translated: progress is checkpointed to disk as it goes, so starting the same language again continues from where it left off instead of starting over. The in-dialog progress display and Cancel belong to the Settings dialog that started the run — reopening Settings later does not re-attach to a running job, and Start translation then only reports \"A guide translation is already running.\" After closing Settings, the running job is paused via the quit dialog below (or simply left to finish). If you try to quit korTTY while a guide translation is running, a dialog offers to pause it and quit, or to keep korTTY open; choosing to pause leaves the checkpoint in place for the next run. After installing a korTTY update that changed the guide's content, and if you already have a translated guide for your language, korTTY offers once per run — a few seconds after the window opens, and never while a translation is already running — to bring it up to date; Update now opens this Settings tab, where you start the run yourself. Because progress is checkpointed by the exact English text of each piece, updating only re-translates sentences that actually changed in the release — everything else is reused as-is.","title": "Why this runs in the background"},{"location": "reference/settings/translation.html#why-a-reasoning-model-is-a-poor-fit","text": "Some AI models are built to \"think out loud\" before answering — writing an extended chain of reasoning into their reply before the actual output, a technique tuned for hard problems like math or coding. That reasoning is not part of the translation, but the model still has to generate it, and generating text is the slow part of running a local model. Measured on this project's own guide, a reasoning model produced about 4.4 completion tokens for every input token — roughly 4,400 tokens of reasoning to translate 1,000 characters of prose. On identical hardware this was the difference between a translation run of about an hour and one of six hours or more. If the AI profile you pick for guide translation is a reasoning model, korTTY warns you before starting or estimating, naming the model and letting you continue anyway or cancel. The check looks at the model's name (publishers of reasoning models advertise it there — \"reasoning\", \"thinking\", \"R1\", \"QwQ\", \"o1\" and similar are recognized), so it costs nothing to run and may occasionally miss a differently-named reasoning model, but it will not falsely warn about a plain instruct model. Suitable models are instruction-tuned (\"instruct\") chat models without a reasoning/thinking mode — the kind normally used for translation, summarization or rewriting rather than multi-step problem solving. For an embedded local profile, look for MLX or GGUF builds whose name includes \"instruct\" and not \"reasoning\", \"thinking\" or \"R1\", for example a Qwen2.5-Instruct or Phi-4-mini-instruct build at a size your hardware can run comfortably (4-bit quantized 7-8B models are a practical default on Apple Silicon). Larger instruct models generally translate more fluently at the cost of speed; the duration estimate below lets you compare before committing to a full run.","title": "Why a reasoning model is a poor fit"},{"location": "reference/settings/translation.html#estimating-how-long-a-full-translation-will-take","text": "Estimate duration measures the AI profile selected above against a real, representative sample of the guide's text and projects how long translating everything still outstanding would take, without committing to the full run: It sends one small, untimed \"warm-up\" request first, so that a local model's one-time loading cost — which is paid once for an entire run, not per batch — is not mistaken for ongoing translation speed and multiplied across every batch of the projection. It then translates one real, budget-sized batch of guide text and times it. This is a genuine translation, not a simulation: the sample is kept, so estimating first and then starting a full run does not repeat that work. From that single timed batch, it computes two projections for the remaining text — one assuming cost scales with the number of requests, one assuming it scales with the amount of text — and reports the resulting range together with how long the remaining pages and text would take. Reporting a range rather than one number reflects that a single sample cannot fully separate a model's fixed per-request overhead from its per-character cost; the estimate keeps that uncertainty visible rather than hiding it behind a single overly precise number. If the sample fails outright — the AI profile is unreachable, misconfigured, or produced nothing usable — the estimate reports that as a connection problem instead of a duration, so it is not mistaken for \"translation will be instant.\" And if nothing is left to translate, the estimate simply reports that the guide is already fully translated for this language. Note The reasoning-model warning above appears for Estimate duration as well as Start translation , since running an estimate against a reasoning model already spends real time on it.","title": "Estimating how long a full translation will take"},{"location": "reference/settings/updates.html","text": "Updates Configure automatic update checking and the frequency at which korTTY queries GitHub for new releases. Open via Configuration → Global Settings → Updates ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Check automatically for KorTTY updates toggle — On updateChecksEnabled Check interval slider 1–30 days 1 day updateCheckIntervalDays Note Automatic update checks run silently in the background and only display a notification dialog when a newer compatible version is available. Manual update checks are available via the About dialog at any time. Downloading and installing an update korTTY selects the native release asset that matches the current operating system, processor architecture and package type, downloads it to the platform's normal Downloads directory and verifies the SHA-256 digest published by GitHub before making the file available. A Flatpak installation only accepts a matching .flatpak bundle; it never offers a DEB, RPM, pacman package or portable archive as an in-place replacement. Flatpak bundles are downloaded but not installed automatically. After the download, korTTY shows the exact host-terminal command, equivalent to the following example, and keeps the downloaded path selectable: flatpak install --user ./kortty-Linux-<version>-<architecture>.flatpak The GitHub release page remains available through the guide if the package manager requires additional confirmation or a system-wide installation.","title": "Updates"},{"location": "reference/settings/updates.html#updates","text": "Configure automatic update checking and the frequency at which korTTY queries GitHub for new releases. Open via Configuration → Global Settings → Updates ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Check automatically for KorTTY updates toggle — On updateChecksEnabled Check interval slider 1–30 days 1 day updateCheckIntervalDays Note Automatic update checks run silently in the background and only display a notification dialog when a newer compatible version is available. Manual update checks are available via the About dialog at any time.","title": "Updates"},{"location": "reference/settings/updates.html#downloading-and-installing-an-update","text": "korTTY selects the native release asset that matches the current operating system, processor architecture and package type, downloads it to the platform's normal Downloads directory and verifies the SHA-256 digest published by GitHub before making the file available. A Flatpak installation only accepts a matching .flatpak bundle; it never offers a DEB, RPM, pacman package or portable archive as an in-place replacement. Flatpak bundles are downloaded but not installed automatically. After the download, korTTY shows the exact host-terminal command, equivalent to the following example, and keeps the downloaded path selectable: flatpak install --user ./kortty-Linux-<version>-<architecture>.flatpak The GitHub release page remains available through the guide if the package manager requires additional confirmation or a system-wide installation.","title": "Downloading and installing an update"},{"location": "reference/settings/video.html","text": "Video Configure terminal recording and replay options. These settings control how korTTY captures and replays terminal sessions, including color preservation and automatic recording availability. Open via Configuration → Global Settings → Video ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Enable terminal recording after app restart toggle — Off terminalRecordingEnabled Capture terminal colors in recordings toggle — Off terminalRecordingCaptureColorsEnabled Note Enabling Capture terminal colors in recordings stores per-cell terminal color information in new replay files. This increases the replay file size but preserves the original terminal appearance, including ANSI colors and background colors, for accurate replay.","title": "Video"},{"location": "reference/settings/video.html#video","text": "Configure terminal recording and replay options. These settings control how korTTY captures and replays terminal sessions, including color preservation and automatic recording availability. Open via Configuration → Global Settings → Video ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Enable terminal recording after app restart toggle — Off terminalRecordingEnabled Capture terminal colors in recordings toggle — Off terminalRecordingCaptureColorsEnabled Note Enabling Capture terminal colors in recordings stores per-cell terminal color information in new replay files. This increases the replay file size but preserves the original terminal appearance, including ANSI colors and background colors, for accurate replay.","title": "Video"},{"location": "reference/settings/window.html","text": "Window This tab configures window geometry behavior, dashboard state retention, and menu bar visibility. Open via Configuration → Global Settings → Window ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Remember window geometry toggle — On rememberWindowGeometry Remember dashboard state toggle — On rememberDashboardState Open tool windows as tabs toggle — Off openToolWindowsAsTabs Use fixed window geometry toggle — Off useFixedWindowGeometry Width: number 400–4000 — fixedWindowGeometry.width Height: number 300–3000 — fixedWindowGeometry.height X Position: number 0–5000 — fixedWindowGeometry.x Y Position: number 0–3000 — fixedWindowGeometry.y With Remember window geometry enabled, KorTTY stores the position and size of every user-resizable application window and named dialog separately. Reopening a window restores the geometry chosen for that window type; if its previous monitor is no longer connected, KorTTY moves it back onto an available screen. On macOS, a main window's saved bounds are reapplied after its native unified title bar is ready so the system cannot shift the restored position while opening it. A changed UI font scale keeps the remembered position but lets the window calculate a fresh size so translated or enlarged labels still fit. Short-lived confirmations and progress notices keep their content-derived size. Note When Use fixed window geometry is enabled, it takes precedence over Remember window geometry for main terminal windows. Dialogs continue to use their own remembered geometry. Note The Remember dashboard state setting preserves whether the dashboard panel was open or closed the last time you closed the application, and restores that state on the next launch. Note With Open tool windows as tabs enabled, management tools (Snippets, JobScheduler, AI Manager, Saved Chats, Session Journals, Credential/GPG/SSH key management, Video Manager, Teamwork settings, Terminal Effects) open as tabs in the main window instead of separate windows. The tab opens in the window whose menu you used, so with several main windows open each window collects its own tool tabs. Reopening a tool focuses its existing tab; the snippet editor, the AI code analysis and the session journal viewer open a new tab each time. The setting takes effect the next time a tool is opened. A tool hosted as a tab has no separate window geometry. Its available size follows the main window and its saved main-window geometry.","title": "Window"},{"location": "reference/settings/window.html#window","text": "This tab configures window geometry behavior, dashboard state retention, and menu bar visibility. Open via Configuration → Global Settings → Window ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Remember window geometry toggle — On rememberWindowGeometry Remember dashboard state toggle — On rememberDashboardState Open tool windows as tabs toggle — Off openToolWindowsAsTabs Use fixed window geometry toggle — Off useFixedWindowGeometry Width: number 400–4000 — fixedWindowGeometry.width Height: number 300–3000 — fixedWindowGeometry.height X Position: number 0–5000 — fixedWindowGeometry.x Y Position: number 0–3000 — fixedWindowGeometry.y With Remember window geometry enabled, KorTTY stores the position and size of every user-resizable application window and named dialog separately. Reopening a window restores the geometry chosen for that window type; if its previous monitor is no longer connected, KorTTY moves it back onto an available screen. On macOS, a main window's saved bounds are reapplied after its native unified title bar is ready so the system cannot shift the restored position while opening it. A changed UI font scale keeps the remembered position but lets the window calculate a fresh size so translated or enlarged labels still fit. Short-lived confirmations and progress notices keep their content-derived size. Note When Use fixed window geometry is enabled, it takes precedence over Remember window geometry for main terminal windows. Dialogs continue to use their own remembered geometry. Note The Remember dashboard state setting preserves whether the dashboard panel was open or closed the last time you closed the application, and restores that state on the next launch. Note With Open tool windows as tabs enabled, management tools (Snippets, JobScheduler, AI Manager, Saved Chats, Session Journals, Credential/GPG/SSH key management, Video Manager, Teamwork settings, Terminal Effects) open as tabs in the main window instead of separate windows. The tab opens in the window whose menu you used, so with several main windows open each window collects its own tool tabs. Reopening a tool focuses its existing tab; the snippet editor, the AI code analysis and the session journal viewer open a new tab each time. The setting takes effect the next time a tool is opened. A tool hosted as a tab has no separate window geometry. Its available size follows the main window and its saved main-window geometry.","title": "Window"},{"location": "reference/settings/snippet-editor/index.html","text": "Snippet Editor Font, color and cursor overrides for the Snippet Manager and the Snippet Edit dialog. Open via Configuration → Global Settings → Snippet Editor ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Font Family dropdown Empty (inherit) or an installed monospace family empty (inherit) snippetFontFamily Font Size number 0–72 (0 = inherit) 0 (inherit) snippetFontSize Foreground Color color — inherit (picker shows #d4d4d4 ) snippetForegroundColor Background Color color — inherit (picker shows #1e1e1e ) snippetBackgroundColor Cursor Style dropdown empty, BLOCK, LINE, UNDERSCORE empty (inherit) snippetCursorStyle Cursor Color color — inherit (picker shows #FF0000 ) snippetCursorColor AI Completion Shortcut key recorder One key, optionally with up to three modifiers (Ctrl/Cmd, Shift, Alt) Shift+Tab snippetCompletionShortcut Keep a pre-warmed editor ready checkbox on / off on snippetEditorPrewarmEnabled Choosing the completion shortcut Click the AI Completion Shortcut field and press the combination you want — the field records what you pressed, and Reset restores Shift + Tab . A combination is one main key (a letter, digit, function key, Tab , Space , an arrow, Enter and so on) with none, one, two or three modifiers, so Tab alone, Shift + Tab and Ctrl + Alt + K are all valid. On macOS the Ctrl modifier is the Cmd key, matching the editor's own shortcuts. The new combination applies to snippet editors opened afterwards; Ctrl + Space opens the list as well and cannot be reassigned. Choosing something other than Shift + Tab gives Shift + Tab back its normal editor meaning: it removes one level of indentation. Pre-warmed editor With Keep a pre-warmed editor ready on, korTTY boots a spare editor in the background about two seconds after the first snippet editor of a session has opened, so the next Snippet Edit dialog or Snippet Manager preview starts with a ready editor instead of loading Monaco while you wait. The spare is only created after you have used an editor once, and it holds a WebKit page in memory while idle — switch the option off on machines with little RAM to trade a short boot per open for that memory. Inheriting instead of overriding These settings override the terminal/editor defaults for snippet windows only. Leave a field empty — or set the font size to 0 — to inherit the general setting from Appearance , Colors and Editor instead. The snippet editor itself, including its AI code actions and language fields, is described under Snippets .","title": "Snippet Editor"},{"location": "reference/settings/snippet-editor/index.html#snippet-editor","text": "Font, color and cursor overrides for the Snippet Manager and the Snippet Edit dialog. Open via Configuration → Global Settings → Snippet Editor ; stored in ~/.kortty/global-settings.xml . Setting Type Values Default Stored as Font Family dropdown Empty (inherit) or an installed monospace family empty (inherit) snippetFontFamily Font Size number 0–72 (0 = inherit) 0 (inherit) snippetFontSize Foreground Color color — inherit (picker shows #d4d4d4 ) snippetForegroundColor Background Color color — inherit (picker shows #1e1e1e ) snippetBackgroundColor Cursor Style dropdown empty, BLOCK, LINE, UNDERSCORE empty (inherit) snippetCursorStyle Cursor Color color — inherit (picker shows #FF0000 ) snippetCursorColor AI Completion Shortcut key recorder One key, optionally with up to three modifiers (Ctrl/Cmd, Shift, Alt) Shift+Tab snippetCompletionShortcut Keep a pre-warmed editor ready checkbox on / off on snippetEditorPrewarmEnabled Choosing the completion shortcut Click the AI Completion Shortcut field and press the combination you want — the field records what you pressed, and Reset restores Shift + Tab . A combination is one main key (a letter, digit, function key, Tab , Space , an arrow, Enter and so on) with none, one, two or three modifiers, so Tab alone, Shift + Tab and Ctrl + Alt + K are all valid. On macOS the Ctrl modifier is the Cmd key, matching the editor's own shortcuts. The new combination applies to snippet editors opened afterwards; Ctrl + Space opens the list as well and cannot be reassigned. Choosing something other than Shift + Tab gives Shift + Tab back its normal editor meaning: it removes one level of indentation. Pre-warmed editor With Keep a pre-warmed editor ready on, korTTY boots a spare editor in the background about two seconds after the first snippet editor of a session has opened, so the next Snippet Edit dialog or Snippet Manager preview starts with a ready editor instead of loading Monaco while you wait. The spare is only created after you have used an editor once, and it holds a WebKit page in memory while idle — switch the option off on machines with little RAM to trade a short boot per open for that memory. Inheriting instead of overriding These settings override the terminal/editor defaults for snippet windows only. Leave a field empty — or set the font size to 0 — to inherit the general setting from Appearance , Colors and Editor instead. The snippet editor itself, including its AI code actions and language fields, is described under Snippets .","title": "Snippet Editor"}]}